---
phase: 31-config-bucket
plan: 07
type: execute
wave: 3
depends_on: [02, 03, 04, 05, 06]
files_modified:
    - pnpm-lock.yaml
autonomous: true
requirements: [SYNC-2.0.1-02, SYNC-2.0.1-03]
must_haves:
    truths:
        - "All 9 hand-resolved Bucket A files are conflict-free before the lockfile is regenerated"
        - "pnpm-lock.yaml is freshly generated by `pnpm install` (D-31-16)"
        - "`pnpm install` exits 0 (D-31-17 gate 2 / SYNC-2.0.1-03 literal text)"
        - "`pnpm install --frozen-lockfile` exits 0 on a second run (D-31-17 gate 3 — committed lockfile internally consistent)"
        - "Lockfile drift summary against fork/sync/upstream-v2.0.1:pnpm-lock.yaml is recorded in the lockfile commit body (D-31-17 gate 5 / D-31-18)"
        - "Workspace package resolution succeeds — `pnpm nx run-many -t typecheck --projects=@vortex/shared,@vortex/paths` (or equivalent partial typecheck on 1-2 packages) exits 0 as a smoke for D-31-17 gate 4 IDE/TS-server load"
    artifacts:
        - path: "pnpm-lock.yaml"
          provides: "Regenerated lockfile consistent with all package.jsons in the workspace (D-31-16)"
    key_links:
        - from: "pnpm-lock.yaml"
          to: "package.json (Plan 31-05) + pnpm-workspace.yaml catalog (Plan 31-06)"
          via: "pnpm install resolution"
          pattern: "pnpm install --frozen-lockfile passes (lockfile internally consistent with both inputs)"
        - from: "pnpm install"
          to: "packages/paths/package.json + packages/paths-node/package.json (Plan 31-01 R1 restore)"
          via: "pnpm-workspace.yaml `./packages/*` glob with failIfNoMatch: true"
          pattern: "pnpm install does not error on orphan workspace dirs because Plan 31-01 restored the package.json files"
---

<objective>
Regenerate `pnpm-lock.yaml` cleanly per D-31-16, then run the Phase 31 done-gate (D-31-17): `pnpm install` succeeds, `pnpm install --frozen-lockfile` succeeds on a second run, and a lockfile drift summary is captured in the commit body. Smoke-test workspace package resolution via a partial nx typecheck.

Purpose: Hand-merging the lockfile is brittle and produces non-deterministic results in pnpm strict catalog mode (45 conflict markers per RESEARCH.md). Fresh regeneration produces a deterministic lockfile from the resolved package.json + pnpm-workspace.yaml. Plan 31-01's R1 restore ensures the orphan packages/paths{,-node} situation doesn't trip `pnpm install`.

Output: One atomic lockfile commit with the chore(deps) title format and drift summary in the body. Phase 31 done-gate satisfied.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31-config-bucket/31-CONTEXT.md
@.planning/phases/31-config-bucket/31-RESEARCH.md
@.planning/milestones/v8.0-phases/24-config-bucket/24-07-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight check — verify all 9 hand-resolved files are conflict-free and committed</name>
  <files>(no file modifications — verification only)</files>
  <read_first>
    - .planning/phases/31-config-bucket/31-CONTEXT.md (D-31-17: done-gate prerequisites)
    - .planning/phases/31-config-bucket/31-RESEARCH.md (Conflict File Inventory: the 9 hand-resolved files)
  </read_first>
  <action>
    Before regenerating the lockfile, verify Plans 31-02..31-06 each resolved their files cleanly. If any conflict markers remain in the 9 hand-resolved files, STOP and re-run the responsible plan — `pnpm install` would error on conflict markers in `package.json` or `pnpm-workspace.yaml`.

    Run:
    ```
    git grep -l '^<<<<<<< ' -- \
      package.json \
      pnpm-workspace.yaml \
      vitest.config.ts \
      'src/main/eslint.config.mjs' \
      'src/preload/eslint.config.mjs' \
      'src/renderer/eslint.config.mjs' \
      'src/shared/eslint.config.mjs' \
      'src/main/prepare-dist-package.mjs' \
      'docker/windows/Dockerfile'
    ```

    Expected: zero output (no files listed). If any file is listed, the corresponding resolution plan (31-02..31-06) did not complete cleanly — return to that plan before continuing.

    Also verify the working tree is clean for the 9 files (all changes already committed):
    ```
    git status --porcelain -- \
      package.json pnpm-workspace.yaml vitest.config.ts \
      'src/main/eslint.config.mjs' 'src/preload/eslint.config.mjs' \
      'src/renderer/eslint.config.mjs' 'src/shared/eslint.config.mjs' \
      'src/main/prepare-dist-package.mjs' 'docker/windows/Dockerfile'
    ```
    Expected: empty output.

    Also verify the R1 restore from Plan 31-01 is intact:
    ```
    test -f packages/paths/package.json && test -f packages/paths-node/package.json
    ```
    Expected: exit 0 (both files present — pnpm install relies on this).

  </action>
  <verify>
    <automated>test -z "$(git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- package.json pnpm-workspace.yaml vitest.config.ts 'src/main/eslint.config.mjs' 'src/preload/eslint.config.mjs' 'src/renderer/eslint.config.mjs' 'src/shared/eslint.config.mjs' 'src/main/prepare-dist-package.mjs' 'docker/windows/Dockerfile' 2&gt;/dev/null)" &amp;&amp; test -z "$(git status --porcelain -- package.json pnpm-workspace.yaml vitest.config.ts 'src/main/eslint.config.mjs' 'src/preload/eslint.config.mjs' 'src/renderer/eslint.config.mjs' 'src/shared/eslint.config.mjs' 'src/main/prepare-dist-package.mjs' 'docker/windows/Dockerfile')" &amp;&amp; test -f packages/paths/package.json &amp;&amp; test -f packages/paths-node/package.json</automated>
  </verify>
  <acceptance_criteria>
    - The git grep command returns zero files (zero output)
    - `git status --porcelain` for the 9 files returns empty (all committed)
    - `packages/paths/package.json` and `packages/paths-node/package.json` both exist (R1 restore intact)
  </acceptance_criteria>
  <done>All 9 hand-resolved Bucket A files are conflict-free and committed; R1 restore intact. Safe to regenerate the lockfile.</done>
</task>

<task type="auto">
  <name>Task 2: Capture pre-regeneration lockfile reference for drift comparison</name>
  <files>/tmp/upstream-pre-merge-lockfile.yaml (transient — not committed)</files>
  <read_first>
    - .planning/phases/31-config-bucket/31-RESEARCH.md (Lockfile Regen Approach: drift comparison target is `fork/sync/upstream-v2.0.1:pnpm-lock.yaml`)
  </read_first>
  <action>
    Save a copy of `fork/sync/upstream-v2.0.1:pnpm-lock.yaml` (the auto-merged-with-conflict-markers lockfile that PR #5 produced) for later drift comparison.

    Steps:
    1. `git show fork/sync/upstream-v2.0.1:pnpm-lock.yaml > /tmp/upstream-pre-merge-lockfile.yaml`
    2. Verify the file is non-empty: `test -s /tmp/upstream-pre-merge-lockfile.yaml` exits 0.
    3. Note that this reference lockfile contains 45 conflict markers per RESEARCH.md — that's expected. The drift summary in Task 4 compares high-level structure, not byte-equality.

    Do NOT commit `/tmp/upstream-pre-merge-lockfile.yaml`. It's a transient artifact for the drift summary.

  </action>
  <verify>
    <automated>test -s /tmp/upstream-pre-merge-lockfile.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/upstream-pre-merge-lockfile.yaml` exists and is non-empty
    - File content matches `git show fork/sync/upstream-v2.0.1:pnpm-lock.yaml` byte-for-byte
  </acceptance_criteria>
  <done>Pre-merge lockfile saved for drift comparison.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate pnpm-lock.yaml (D-31-16: rm + pnpm install)</name>
  <files>pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/31-config-bucket/31-CONTEXT.md (D-31-16: regenerate fresh — `rm pnpm-lock.yaml && pnpm install`; D-31-17 gate 2: pnpm install must succeed)
    - .planning/phases/31-config-bucket/31-RESEARCH.md (First-install timing: 5-15 minutes; native build guards: allowBuilds correctly contains @electron/rebuild: true after Plan 31-06)
    - package.json (already resolved by Plan 31-05)
    - pnpm-workspace.yaml (already resolved by Plan 31-06 — catalog drives direct dep versions)
  </read_first>
  <action>
    Per D-31-16: regenerate `pnpm-lock.yaml` from scratch.

    Steps:
    1. `rm pnpm-lock.yaml` — delete the auto-merged-with-conflict-markers lockfile.
    2. `pnpm install` — let pnpm resolve and write a fresh lockfile from package.json + pnpm-workspace.yaml. Exit code MUST be 0. This is D-31-17 gate 2 / SYNC-2.0.1-03 literal text.
       - Expected duration per RESEARCH.md: 5-15 minutes (catalog adds 4 entries; native builds for bsatk/core-js/drivelist/electron/esbuild/font-scanner/gamebryo-savegame/leveldown/loot/protobufjs/unrs-resolver/winapi-bindings/xxhash-addon/@parcel/watcher/@nexusmods/fomod-installer-native).
    3. Confirm `pnpm-lock.yaml` exists and is non-empty: `test -s pnpm-lock.yaml`.

    If `pnpm install` fails:
    - Read the error output carefully. Common failure modes:
      * "ENOLOCAL packages/paths" or similar — Plan 31-01 R1 restore not in place. Re-run Plan 31-01 Task 4.
      * Catalog version conflict — package.json or pnpm-workspace.yaml has an inconsistency. Hand-edit the relevant resolution plan's commit (`git commit --amend` or `git rebase -i`), then re-try.
      * Native build failure (e.g. xxhash-addon, leveldown) — usually a Linux/Windows toolchain issue, not a Phase 31 logic issue. Capture in SUMMARY and proceed if the lockfile was still written; the build failure is Phase 35 territory.
    - Document the failure mode in 31-07-SUMMARY.md regardless of outcome.

    Do NOT commit yet — Task 4 captures the drift summary for the commit body.

  </action>
  <verify>
    <automated>rm -f pnpm-lock.yaml &amp;&amp; pnpm install &amp;&amp; test -s pnpm-lock.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install` exit code is 0 (this satisfies D-31-17 gate 2 / SYNC-2.0.1-03)
    - `pnpm-lock.yaml` exists and is non-empty after the install
    - `git grep '^<<<<<<< ' -- pnpm-lock.yaml` returns no output (the regenerated lockfile is conflict-marker-free by construction)
  </acceptance_criteria>
  <done>pnpm-lock.yaml is freshly generated by pnpm. D-31-17 gate 2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 4: Capture drift summary, commit lockfile, run frozen-lockfile validation, smoke partial typecheck</name>
  <files>pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/31-config-bucket/31-CONTEXT.md (D-31-17 gates 3+4+5: --frozen-lockfile must succeed; IDE/TS-server load without parser errors; drift summary in commit body)
    - .planning/phases/31-config-bucket/31-RESEARCH.md (Lockfile Regen Approach: catalog mode strict expectations; v2.0.1 catalog adds 4 entries; ~4 new direct top-level entries + transitive trees; no removals from HEAD)
    - /tmp/upstream-pre-merge-lockfile.yaml (saved in Task 2 — drift comparison target)
    - pnpm-lock.yaml (the regenerated lockfile from Task 3)
  </read_first>
  <action>
    Capture the drift summary, commit the regenerated lockfile per D-31-03 atomic-commit cadence, and run `pnpm install --frozen-lockfile` as the second-run validator (D-31-17 gate 3). Then smoke-test workspace package resolution via a partial nx typecheck (D-31-17 gate 4 proxy).

    Steps:

    1. **Drift summary** (D-31-17 gate 5 / D-31-18):
       - Compare the regenerated `pnpm-lock.yaml` against `/tmp/upstream-pre-merge-lockfile.yaml` (saved in Task 2).
       - Focus on structural / direct-dep differences (catalog mode means transitive deps will drift the most — that's expected per D-31-18 and acceptable).
       - Useful diff strategies:
         * Top-level package count: `wc -l pnpm-lock.yaml /tmp/upstream-pre-merge-lockfile.yaml`
         * Direct dep version differences (compare top-level workspace `dependencies:` and `devDependencies:` sections)
         * Major-version bumps: scan for any package whose major version changed between the two files
         * New transitive packages introduced (informational, listed but not investigated)
         * Catalog-driven additions: confirm `esptk`, `exe-version`, `gamebryo-savegame`, `native-errors` appear in the regenerated lockfile but not the upstream-merge reference (those came from Plan 31-06)
         * `leveldown` / `levelup` should appear in regenerated and reference (HEAD-preserved)
       - Format the drift summary as a markdown bulleted list (D-31-18 discretion clause: table or bulleted prose; bulleted prose is fine). Save to `/tmp/lockfile-drift-summary.md` for inclusion in the commit body.
       - Required content: at minimum, note (a) lockfile size delta, (b) any direct-dep major-version bump, (c) confirmation of the four catalog additions, (d) "transitive drift accepted per D-31-18" line.

    2. **Commit the lockfile with the drift summary in the body:**
       ```
       git add pnpm-lock.yaml
       git commit -m "chore(deps): regenerate pnpm-lock.yaml after v2.0.1 sync" -F - <<EOF
       (additional message body here — assemble from /tmp/lockfile-drift-summary.md)
       EOF
       ```
       Concrete approach: assemble the full message in `/tmp/lockfile-commit-msg.txt`:
       ```
       cat > /tmp/lockfile-commit-msg.txt <<'HEAD_EOF'

chore(deps): regenerate pnpm-lock.yaml after v2.0.1 sync

Phase 31 / D-31-16 / D-31-17 — fresh lockfile from rm + pnpm install.
Replaces the auto-merged-with-45-conflict-markers lockfile from PR #5.

## Drift summary (D-31-17 gate 5)

HEAD_EOF
cat /tmp/lockfile-drift-summary.md >> /tmp/lockfile-commit-msg.txt
cat >> /tmp/lockfile-commit-msg.txt <<'TAIL_EOF'

Transitive drift accepted per D-31-18 (catalog mode pins direct deps in
pnpm-workspace.yaml; transitive surface is non-load-bearing for Phase 31
gates). Real impact, if any, surfaces at Phase 35 build verification.
TAIL_EOF
git commit -F /tmp/lockfile-commit-msg.txt
```

    3. **Run frozen-lockfile validation** (D-31-17 gate 3):
       - `pnpm install --frozen-lockfile` — must exit 0. This proves the committed lockfile is internally consistent with package.json + pnpm-workspace.yaml.
       - If this fails, the regenerated lockfile drifted from package.json on the second pass — investigate and fix before declaring Phase 31 done.

    4. **Workspace package resolution smoke** (D-31-17 gate 4 proxy — IDE/TS server loads without parser errors):
       - Stronger automated proxy than manual IDE confirmation: `pnpm nx run-many -t typecheck --projects=@vortex/shared` (small, fast, validates that nx + workspace package resolution works post-regen).
       - If `@vortex/shared` typechecks, that's strong evidence the IDE will too. If `@vortex/shared` fails for source-level reasons (e.g. Phase 32+ source-side conflicts not yet resolved), that's expected and out of scope — note in 31-07-SUMMARY.md as "Phase 32+ will resolve source conflicts that block full typecheck; partial nx typecheck on @vortex/shared/@vortex/paths confirms workspace package resolution works."
       - If the partial typecheck fails for **package-resolution** reasons (e.g. `Cannot find module '@vortex/paths'`), that IS in scope for Phase 31: investigate and fix.

    5. Update SUMMARY (`31-07-SUMMARY.md`) with: commit SHA of the lockfile commit, drift summary excerpt, frozen-lockfile result, partial typecheck result.

  </action>
  <verify>
    <automated>git grep '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- pnpm-lock.yaml; test $? -eq 1 &amp;&amp; pnpm install --frozen-lockfile</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=format:'%s' -- pnpm-lock.yaml` shows commit titled `chore(deps): regenerate pnpm-lock.yaml after v2.0.1 sync`
    - `git log -1 --pretty=format:'%b' -- pnpm-lock.yaml` (commit body) contains a "Drift summary" section with at least the four required observations (size delta; major-version bumps if any; four catalog additions confirmed; transitive drift acknowledged)
    - `pnpm install --frozen-lockfile` exits 0 (D-31-17 gate 3)
    - `git grep '^<<<<<<< ' -- pnpm-lock.yaml` returns no output (exit 1)
    - Partial nx typecheck result documented in 31-07-SUMMARY.md (pass = D-31-17 gate 4 automated; fail-for-source-reasons = noted with manual IDE fallback per ROADMAP)
  </acceptance_criteria>
  <done>Lockfile committed atomically with drift summary in the body. D-31-17 gates 2, 3, 5 satisfied. Phase 31 ROADMAP success criteria 2 (pnpm install), 3 (pnpm install --frozen-lockfile) met; criterion 4 (IDE/TS load) is automated by partial typecheck OR documented for manual IDE confirmation.</done>
</task>

<task type="auto">
  <name>Task 5: Final phase done-gate verification — assert all five D-31-17 / four ROADMAP success criteria</name>
  <files>(no file modifications — verification only)</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 31 Success Criteria 1-4)
    - .planning/phases/31-config-bucket/31-CONTEXT.md (D-31-17 done-gate, all 5 items)
  </read_first>
  <action>
    Run the four ROADMAP success-criteria assertions plus D-31-17's drift-summary item. All must pass before Phase 31 can be declared complete.

    1. **Zero conflict markers across Bucket A** (Success Criterion 1 / D-31-17 gate 1):
       ```
       git grep -l '^<<<<<<< ' -- \
         package.json pnpm-workspace.yaml pnpm-lock.yaml vitest.config.ts \
         'src/main/eslint.config.mjs' 'src/preload/eslint.config.mjs' \
         'src/renderer/eslint.config.mjs' 'src/shared/eslint.config.mjs' \
         'src/main/prepare-dist-package.mjs' 'docker/windows/Dockerfile'
       ```
       Expected: empty output. The 10 conflict-bearing Bucket A files are all conflict-free (9 hand-resolved + 1 lockfile regenerated).

    2. **`pnpm install` succeeds** (Success Criterion 2 / D-31-17 gate 2): already proven in Task 3. Re-confirm on idempotent run: `pnpm install` exits 0 even when `pnpm-lock.yaml` already exists.

    3. **`pnpm install --frozen-lockfile` succeeds** (Success Criterion 3 / D-31-17 gate 3): already proven in Task 4. Re-confirm: `pnpm install --frozen-lockfile` exits 0.

    4. **IDE/TypeScript server loads tree without parse errors** (Success Criterion 4 / D-31-17 gate 4):
       - Automated proxy from Task 4: `pnpm nx run-many -t typecheck --projects=@vortex/shared` exited 0 (or comparable result documented).
       - Strong proxy: workspace package resolution works (no `Cannot find module '@vortex/paths'` errors); full typecheck failures from source-level conflicts in Phases 32-34 are out of scope.

    5. **Lockfile drift summary in commit body** (D-31-17 gate 5):
       - `git log -1 --pretty=format:'%b' pnpm-lock.yaml` (or HEAD body) contains the drift summary with the four required observations.

    Capture the result of each assertion in `31-07-SUMMARY.md`. If any of 1-3 fail, Phase 31 is not done. If 4 fails for Phases 32-34 source reasons (not Phase 31 config reasons), document and proceed.

  </action>
  <verify>
    <automated>test -z "$(git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- package.json pnpm-workspace.yaml pnpm-lock.yaml vitest.config.ts 'src/main/eslint.config.mjs' 'src/preload/eslint.config.mjs' 'src/renderer/eslint.config.mjs' 'src/shared/eslint.config.mjs' 'src/main/prepare-dist-package.mjs' 'docker/windows/Dockerfile' 2&gt;/dev/null)" &amp;&amp; pnpm install --frozen-lockfile</automated>
  </verify>
  <acceptance_criteria>
    - Success Criterion 1 / D-31-17 gate 1: `git grep '^<<<<<<< ' -- <Bucket-A-files>` returns no output across all 10 conflict-bearing files
    - Success Criterion 2 / D-31-17 gate 2: `pnpm install` exits 0 (already proven Task 3; idempotent re-run also exits 0)
    - Success Criterion 3 / D-31-17 gate 3: `pnpm install --frozen-lockfile` exits 0
    - Success Criterion 4 / D-31-17 gate 4: documented as automated (partial nx typecheck passed) OR documented as manually-confirmed-via-IDE with a note about which source-restoration phases (32-34) must complete before full typecheck passes
    - D-31-17 gate 5: drift summary committed in the lockfile commit body
    - `31-07-SUMMARY.md` records the result for each of the five gates
  </acceptance_criteria>
  <done>All five D-31-17 done-gate items asserted and recorded. Phase 31 is complete; Plan 31-08 may push to remote.</done>
</task>

</tasks>

<verification>
- All 10 conflict-bearing Bucket A files have no conflict markers
- `pnpm install` exits 0 (D-31-17 gate 2, SYNC-2.0.1-03)
- `pnpm install --frozen-lockfile` exits 0 (D-31-17 gate 3)
- Lockfile commit body contains drift summary with the four required observations (D-31-17 gate 5)
- Partial nx typecheck result recorded in SUMMARY (D-31-17 gate 4 proxy)
</verification>

<success_criteria>
Phase 31 done-gate (D-31-17, all 5 items) fully satisfied. Lockfile is consistent with package.json + pnpm-workspace.yaml. Atomic lockfile commit recorded with drift summary in body. Phase 31 ready for the final push (Plan 31-08).
</success_criteria>

<rollback>
Task 3 (regen) failure: investigate and fix the underlying package.json / pnpm-workspace.yaml issue (likely amend a prior plan's commit), then re-run.
Task 4 (commit/frozen-lockfile) failure: if the commit landed but `--frozen-lockfile` fails, the lockfile drifted from inputs on the second pass. `git reset --soft HEAD^` to drop the lockfile commit, investigate the discrepancy (most likely a non-deterministic upstream registry lookup), re-run `pnpm install` to regenerate, and re-commit.
Task 5 gate failure: if Criterion 1-3 fail, Phase 31 is not done — return to the relevant plan. If only Criterion 4 fails for source reasons (Phases 32-34), document and proceed (per ROADMAP fallback).
</rollback>

<output>
Create `.planning/phases/31-config-bucket/31-07-SUMMARY.md` recording: lockfile commit SHA; drift summary highlights (size delta, four catalog additions confirmed, any direct-dep major-version bumps, transitive-drift acknowledgment); pnpm install + frozen-lockfile results; partial nx typecheck result; explicit pass/fail for each of the five D-31-17 gates.
</output>
