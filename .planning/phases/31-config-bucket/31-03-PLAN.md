---
phase: 31-config-bucket
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
    - src/main/eslint.config.mjs
    - src/preload/eslint.config.mjs
    - src/renderer/eslint.config.mjs
    - src/shared/eslint.config.mjs
autonomous: true
requirements: [SYNC-2.0.1-02]
must_haves:
    truths:
        - "All four eslint.config.mjs files contain no conflict markers"
        - "Each file's HEAD-side delegation to eslint.config.base.mjs survives intact (the post-base-extraction shape)"
        - "Each file's HEAD-only sections outside the conflict region (custom vortex rules, perfectionist sort, etc. on renderer) survive untouched"
        - "Four atomic commits per D-31-03 cadence, one per file, with title format `resolve(config): <file> — pick HEAD per D-31-10 (upstream-side duplicates eslint.config.base.mjs)`"
        - "Each file passes `node --check` post-resolution"
    artifacts:
        - path: "src/main/eslint.config.mjs"
          provides: "Main process ESLint configuration (pick HEAD per revised D-31-10 — upstream-side adds duplicate base)"
        - path: "src/preload/eslint.config.mjs"
          provides: "Preload process ESLint configuration (pick HEAD per revised D-31-10)"
        - path: "src/renderer/eslint.config.mjs"
          provides: "Renderer process ESLint configuration (pick HEAD per revised D-31-10)"
        - path: "src/shared/eslint.config.mjs"
          provides: "Shared types ESLint configuration (pick HEAD per revised D-31-10)"
    key_links:
        - from: "all four eslint.config.mjs files"
          to: "eslint.config.base.mjs (already merged-in upstream-clean by v2.0.1)"
          via: "pick-HEAD wholesale per revised D-31-10 (RESEARCH.md verified base file already contains every rule the upstream conflict-side proposed)"
          pattern: "post-resolve files contain `defineConfig([...baseConfig(import.meta.dirname)` or equivalent base-extends pattern"
---

<objective>
Resolve the four `eslint.config.mjs` workspace-tier configs (main + preload + renderer + shared) per the **revised D-31-10 stance**: **pick HEAD wholesale on all four**.

Stance shift vs v8.0 D-10: v8.0's hand-merge protocol was correct for v8.0 because that was the merge that introduced the base-config extraction. By v2.0.1 the `eslint.config.base.mjs` extraction is upstream-clean, so the upstream conflict-side is duplicate inline rules already covered by the base. Pick-HEAD is now the simpler correct stance.

Per RESEARCH.md verified `git show fork/sync/upstream-v2.0.1:eslint.config.base.mjs` — every upstream conflict-side addition (`parserOptions.projectService`, `parserOptions.tsconfigRootDir`, `plugins.perfectionist`, recommended/recommendedTypeChecked, sort-imports/sort-exports, consistent-type-imports error, no-unused-vars custom, separate `.mjs` glob block) is already in the base file.

Four separate atomic commits per D-31-03.

Output: Four files conflict-free. Four atomic commits matching D-31-03 title format.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31-config-bucket/31-CONTEXT.md
@.planning/phases/31-config-bucket/31-RESEARCH.md
@.planning/milestones/v8.0-phases/24-config-bucket/24-03-PLAN.md
</context>

<interfaces>
<!-- Key inheritance contract for all four files. Extracted from research. -->
<!-- Each workspace-tier eslint.config.mjs delegates to eslint.config.base.mjs. -->
<!-- Verify base file exists in tree before resolving any of the four below. -->

From eslint.config.base.mjs (root, on fork/sync/upstream-v2.0.1 — already merged in upstream-clean):

- exports `baseConfig(importMetaDirname)` function
- supplies: parserOptions.projectService, parserOptions.tsconfigRootDir, plugins.perfectionist, recommended/recommendedTypeChecked, sort-imports/sort-exports, consistent-type-imports error, no-unused-vars argsIgnorePattern: "^\_", separate `.mjs` glob block

HEAD-side post-resolve shape per file:

- src/preload/eslint.config.mjs: `export default defineConfig([...baseConfig(import.meta.dirname)]);` (single delegation line)
- src/main/eslint.config.mjs: HEAD's conflict region is empty (the file extends base; nothing to take from HEAD inside the conflict)
- src/renderer/eslint.config.mjs: conflict region only spans `parserOptions` block; significant renderer rules (custom vortex rules, perfectionist sort-jsx-props, stylistic plugin, react-x version pin, @eslint-react rules, better-tailwindcss, vortex/no-bluebird-\*) are HEAD-side OUTSIDE the conflict and survive automatically
- src/shared/eslint.config.mjs: HEAD has `files: ["src/**/*.ts"]` followed by `rules:` block (delegating language/parser/plugins to base via the file's existing extends)
  </interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Pick HEAD on src/preload/eslint.config.mjs (D-31-10 revised)</name>
  <files>src/preload/eslint.config.mjs</files>
  <read_first>
    - .planning/phases/31-config-bucket/31-RESEARCH.md (Per-File Notes section on src/preload/eslint.config.mjs — HEAD side is one line delegating to baseConfig; upstream side is ~50 lines of inline rules already in base; recommendation: pick HEAD wholesale)
    - eslint.config.base.mjs (read to confirm base contains the rules the upstream conflict side proposes — sanity check before pick-HEAD)
    - src/preload/eslint.config.mjs (the file being modified, with conflict markers)
  </read_first>
  <action>
    Per revised D-31-10: pick HEAD wholesale on src/preload/eslint.config.mjs.

    Steps:

    1. Open `eslint.config.base.mjs` in the merge tree and confirm it contains the rules the upstream conflict-side adds (recommended, recommendedTypeChecked, perfectionist sort-imports/sort-exports, consistent-type-imports error, no-unused-vars custom, separate `.mjs` glob). If the base file is missing a rule that the upstream conflict-side genuinely needs (rare, but possible if upstream added a brand-new v2.0.1 rule), this task downgrades to a hand-merge — note in commit body and add the missing rule explicitly.

    2. If base coverage is complete (RESEARCH.md says yes for v2.0.1):
       `git checkout --ours src/preload/eslint.config.mjs`

    3. Verify zero conflict markers:
       `git grep '^<<<<<<< ' -- src/preload/eslint.config.mjs` returns no output (exit 1).

    4. Verify the file is syntactically valid:
       `node --check src/preload/eslint.config.mjs` exits 0.

    5. Verify the base-extension pattern survived:
       `grep -E 'baseConfig\(import\.meta\.dirname\)' src/preload/eslint.config.mjs`
       Expected: at least 1 match.

    6. Stage and commit:
       `git add src/preload/eslint.config.mjs`
       Title: `resolve(config): src/preload/eslint.config.mjs — pick HEAD per D-31-10 (upstream-side duplicates eslint.config.base.mjs)`
       Commit body should explain: HEAD is the post-base-extraction shape; upstream conflict-side adds parserOptions.projectService, parserOptions.tsconfigRootDir, plugins.perfectionist, recommended, recommendedTypeChecked, sort-imports/sort-exports, consistent-type-imports error, no-unused-vars custom, separate `.mjs` glob block — all already present in eslint.config.base.mjs on the merge tree (verified via `git show fork/sync/upstream-v2.0.1:eslint.config.base.mjs`). Stance differs from v8.0 D-10 because v8.0 was the merge that introduced the base extraction; by v2.0.1 the base is upstream-clean and the upstream conflict-side is legacy noise.

  </action>
  <verify>
    <automated>git grep '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- src/preload/eslint.config.mjs; test $? -eq 1 &amp;&amp; node --check src/preload/eslint.config.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' -- src/preload/eslint.config.mjs` returns no output (exit 1)
    - `node --check src/preload/eslint.config.mjs` exits 0
    - `grep -E 'baseConfig\(import\.meta\.dirname\)' src/preload/eslint.config.mjs` returns at least 1 match
    - `git log -1 --pretty=format:'%s' -- src/preload/eslint.config.mjs` shows commit titled `resolve(config): src/preload/eslint.config.mjs — pick HEAD per D-31-10 (upstream-side duplicates eslint.config.base.mjs)`
  </acceptance_criteria>
  <done>src/preload/eslint.config.mjs is conflict-free, syntactically valid, base-delegation pattern survived, and the resolution is committed atomically with explanatory body.</done>
</task>

<task type="auto">
  <name>Task 2: Pick HEAD on src/main/eslint.config.mjs (D-31-10 revised)</name>
  <files>src/main/eslint.config.mjs</files>
  <read_first>
    - .planning/phases/31-config-bucket/31-RESEARCH.md (Per-File Notes section on src/main/eslint.config.mjs — HEAD side empty, upstream side has parserOptions.projectService + tsconfigRootDir + plugins.perfectionist all already in base)
    - eslint.config.base.mjs (sanity check)
    - src/main/eslint.config.mjs (the file being modified, with conflict markers)
  </read_first>
  <action>
    Per revised D-31-10: pick HEAD wholesale on src/main/eslint.config.mjs.

    Same protocol as Task 1:

    1. Confirm `eslint.config.base.mjs` contains the rules the upstream conflict side proposes (parserOptions.projectService, parserOptions.tsconfigRootDir, plugins.perfectionist).

    2. `git checkout --ours src/main/eslint.config.mjs`

    3. Verify: `git grep '^<<<<<<< ' -- src/main/eslint.config.mjs` returns no output (exit 1).

    4. `node --check src/main/eslint.config.mjs` exits 0.

    5. Stage and commit:
       Title: `resolve(config): src/main/eslint.config.mjs — pick HEAD per D-31-10 (upstream-side duplicates eslint.config.base.mjs)`
       Body: same explanation as Task 1.

  </action>
  <verify>
    <automated>git grep '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- src/main/eslint.config.mjs; test $? -eq 1 &amp;&amp; node --check src/main/eslint.config.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' -- src/main/eslint.config.mjs` returns no output (exit 1)
    - `node --check src/main/eslint.config.mjs` exits 0
    - `git log -1 --pretty=format:'%s' -- src/main/eslint.config.mjs` shows commit titled `resolve(config): src/main/eslint.config.mjs — pick HEAD per D-31-10 (upstream-side duplicates eslint.config.base.mjs)`
  </acceptance_criteria>
  <done>src/main/eslint.config.mjs is conflict-free, syntactically valid, and committed atomically.</done>
</task>

<task type="auto">
  <name>Task 3: Pick HEAD on src/renderer/eslint.config.mjs (D-31-10 revised)</name>
  <files>src/renderer/eslint.config.mjs</files>
  <read_first>
    - .planning/phases/31-config-bucket/31-RESEARCH.md (Per-File Notes section on src/renderer/eslint.config.mjs — conflict region only spans parserOptions; significant renderer-specific rules including custom vortex rules, perfectionist sort-jsx-props, stylistic plugin, react-x version pin, @eslint-react, better-tailwindcss, vortex/no-bluebird-* are HEAD-side outside the conflict region)
    - eslint.config.base.mjs (sanity check)
    - src/renderer/eslint.config.mjs (the file being modified, with conflict markers)
  </read_first>
  <action>
    Per revised D-31-10: pick HEAD wholesale on src/renderer/eslint.config.mjs.

    Critical: the renderer file has the most HEAD-side content OUTSIDE the conflict region — those sections survive automatically when picking HEAD wholesale on the conflict region. After resolution, verify the renderer-specific rules are still present (assertion below).

    Same protocol as Task 1:

    1. Confirm `eslint.config.base.mjs` contains the parserOptions/tsconfigRootDir rules the upstream conflict side proposes.

    2. `git checkout --ours src/renderer/eslint.config.mjs`

    3. Verify: `git grep '^<<<<<<< ' -- src/renderer/eslint.config.mjs` returns no output (exit 1).

    4. `node --check src/renderer/eslint.config.mjs` exits 0.

    5. Verify renderer-specific HEAD content survived (file should still be ~80+ lines with the renderer rules tail):
       `wc -l src/renderer/eslint.config.mjs` reports >= 50 lines (sanity — pick-HEAD on conflict region preserves the rules tail outside the conflict).
       `grep -cE 'globals\.(node|browser)' src/renderer/eslint.config.mjs` returns at least 1 (the `globals: { ...globals.node, ...globals.browser }` line is HEAD-side).

    6. Stage and commit:
       Title: `resolve(config): src/renderer/eslint.config.mjs — pick HEAD per D-31-10 (upstream-side duplicates eslint.config.base.mjs)`
       Body: same explanation as Task 1, plus note that the renderer-specific rules tail (custom vortex rules, perfectionist sort-jsx-props, react-x pin, @eslint-react, better-tailwindcss, vortex/no-bluebird-*) is HEAD-side outside the conflict region and survives automatically.

  </action>
  <verify>
    <automated>git grep '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- src/renderer/eslint.config.mjs; test $? -eq 1 &amp;&amp; node --check src/renderer/eslint.config.mjs &amp;&amp; test "$(grep -cE 'globals\.(node|browser)' src/renderer/eslint.config.mjs)" -ge "1"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' -- src/renderer/eslint.config.mjs` returns no output (exit 1)
    - `node --check src/renderer/eslint.config.mjs` exits 0
    - `grep -cE 'globals\.(node|browser)' src/renderer/eslint.config.mjs` returns at least 1 (HEAD-side globals line preserved)
    - `git log -1 --pretty=format:'%s' -- src/renderer/eslint.config.mjs` shows commit titled `resolve(config): src/renderer/eslint.config.mjs — pick HEAD per D-31-10 (upstream-side duplicates eslint.config.base.mjs)`
  </acceptance_criteria>
  <done>src/renderer/eslint.config.mjs is conflict-free, renderer-specific rules tail intact, and committed atomically.</done>
</task>

<task type="auto">
  <name>Task 4: Pick HEAD on src/shared/eslint.config.mjs (D-31-10 revised)</name>
  <files>src/shared/eslint.config.mjs</files>
  <read_first>
    - .planning/phases/31-config-bucket/31-RESEARCH.md (Per-File Notes section on src/shared/eslint.config.mjs — HEAD side has files: ["src/**/*.ts"] followed by rules:; upstream side has inline extends/languageOptions.parserOptions/plugins.perfectionist already in base)
    - eslint.config.base.mjs (sanity check)
    - src/shared/eslint.config.mjs (the file being modified, with conflict markers)
  </read_first>
  <action>
    Per revised D-31-10: pick HEAD wholesale on src/shared/eslint.config.mjs.

    Same protocol as Tasks 1-3:

    1. Confirm `eslint.config.base.mjs` contains the rules the upstream conflict side proposes (extends, languageOptions.parserOptions, plugins.perfectionist).

    2. `git checkout --ours src/shared/eslint.config.mjs`

    3. Verify: `git grep '^<<<<<<< ' -- src/shared/eslint.config.mjs` returns no output (exit 1).

    4. `node --check src/shared/eslint.config.mjs` exits 0.

    5. Verify HEAD's files-glob survived:
       `grep -E 'files:.*src/\*\*/\*\.ts' src/shared/eslint.config.mjs` returns at least 1 match.

    6. Stage and commit:
       Title: `resolve(config): src/shared/eslint.config.mjs — pick HEAD per D-31-10 (upstream-side duplicates eslint.config.base.mjs)`
       Body: same explanation as Task 1.

  </action>
  <verify>
    <automated>git grep '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- src/shared/eslint.config.mjs; test $? -eq 1 &amp;&amp; node --check src/shared/eslint.config.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' -- src/shared/eslint.config.mjs` returns no output (exit 1)
    - `node --check src/shared/eslint.config.mjs` exits 0
    - `git log -1 --pretty=format:'%s' -- src/shared/eslint.config.mjs` shows commit titled `resolve(config): src/shared/eslint.config.mjs — pick HEAD per D-31-10 (upstream-side duplicates eslint.config.base.mjs)`
  </acceptance_criteria>
  <done>src/shared/eslint.config.mjs is conflict-free, syntactically valid, and committed atomically.</done>
</task>

</tasks>

<verification>
- `git grep '^<<<<<<< ' -- 'src/main/eslint.config.mjs' 'src/preload/eslint.config.mjs' 'src/renderer/eslint.config.mjs' 'src/shared/eslint.config.mjs'` returns no output
- Four atomic commits exist on `v8.1/config-bucket` (one per file) with D-31-03 title format
- All four files pass `node --check`
- `git log --oneline | head -5` shows four `resolve(config): src/<area>/eslint.config.mjs — pick HEAD per D-31-10 ...` commits
</verification>

<success_criteria>
All four ESLint configs resolved via pick-HEAD per revised D-31-10. eslint.config.base.mjs delegation pattern preserved. v2.0.1's stance simplification (vs v8.0's hand-merge) documented in each commit body. Renderer file's significant rules tail outside the conflict region survived intact.
</success_criteria>

<rollback>
Per-task: if verification fails for a single file, `git reset --soft HEAD^` drops that commit and leaves the file in working tree for re-attempt.
Full plan: if all four committed but a downstream gate (e.g. typecheck) reveals a missing rule that the base doesn't provide, hand-edit the affected file to add the missing rule and amend the relevant commit (`git commit --amend --no-edit`), then re-run `node --check`.
</rollback>

<output>
Create `.planning/phases/31-config-bucket/31-03-SUMMARY.md` recording the four commit SHAs and a brief note confirming the stance shift (pick HEAD per revised D-31-10) was correct — the base file already provided every rule the upstream conflict-side proposed.
</output>
