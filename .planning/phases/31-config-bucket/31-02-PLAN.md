---
phase: 31-config-bucket
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
    - docker/windows/Dockerfile
autonomous: true
requirements: [SYNC-2.0.1-02]
must_haves:
    truths:
        - "docker/windows/Dockerfile contains no conflict markers"
        - "HEAD's LongPathsEnabled registry tweak survives the resolution (D-31-09; verified via grep for 'LongPathsEnabled')"
        - "Resolution committed atomically per D-31-03 atomic-commit format"
    artifacts:
        - path: "docker/windows/Dockerfile"
          provides: "Windows build container definition resolved per D-31-09 — keep HEAD long-path block"
    key_links:
        - from: "docker/windows/Dockerfile"
          to: "HEAD (fork)"
          via: "keep-HEAD per D-31-09 (RESEARCH.md confirmed: no fork-tooling refs in upstream side, HEAD adds genuinely valuable LongPathsEnabled registry tweak)"
          pattern: "post-resolve file contains 'LongPathsEnabled' literal"
---

<objective>
Resolve `docker/windows/Dockerfile` per D-31-09: keep HEAD wholesale on the long-path-support conflict region. Single-file plan (vs v8.0's two-file plan that bundled `.vscode/extensions.json` — that file has no conflict on v2.0.1 per RESEARCH.md).

Purpose: HEAD's `LongPathsEnabled` registry tweak is correctness-critical for Windows builds (Vortex paths into game directories regularly exceed 260 chars). Upstream side is empty. Same stance as v8.0 24-02.

Output: `docker/windows/Dockerfile` conflict-free with HEAD's long-path block preserved. One atomic commit matching D-31-03 title format.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31-config-bucket/31-CONTEXT.md
@.planning/phases/31-config-bucket/31-RESEARCH.md
@.planning/milestones/v8.0-phases/24-config-bucket/24-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read docker/windows/Dockerfile and resolve per D-31-09 (keep HEAD)</name>
  <files>docker/windows/Dockerfile</files>
  <read_first>
    - .planning/phases/31-config-bucket/31-CONTEXT.md (D-31-09: hand-resolve; one read to confirm no fork-tooling references; default to pick-theirs only after that read)
    - .planning/phases/31-config-bucket/31-RESEARCH.md (Per-File Notes section on docker/windows/Dockerfile — HEAD adds LongPathsEnabled registry tweak (~3 lines); upstream marker says v1.16.9 which is stale carry-forward (R4 informational); recommendation keep HEAD)
    - docker/windows/Dockerfile (the file being modified, with conflict markers)
  </read_first>
  <action>
    Per D-31-09 plus RESEARCH.md confirmation: keep HEAD wholesale on the conflict region.

    Step-by-step:

    1. Read docker/windows/Dockerfile end-to-end.

    2. For each conflict region (`<<<<<<< HEAD` ... `>>>>>>>` ...), confirm RESEARCH.md's read:
       - HEAD side contains the LongPathsEnabled registry block (`New-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem" -Name "LongPathsEnabled" -Value 1 -PropertyType DWORD -Force` or equivalent)
       - Upstream side (marker says `>>>>>>> v1.16.9`) is empty
       - Neither side references fork tooling: grep for `rebuild:native`, `rebuild-pnpm-natives.cjs`, `postinstall-libloot.cjs`, `libloot`, Linux-specific paths (`/usr/`, `~/.local/`), or fork-only env vars in BOTH sides — there should be no matches in either.

    3. Take HEAD wholesale:
       `git checkout --ours docker/windows/Dockerfile`
       If `--ours` doesn't work cleanly because the merge state is unusual, hand-edit: delete every `<<<<<<<`, `=======`, `>>>>>>>` line and the upstream block between `=======` and `>>>>>>>`, leaving HEAD's content.

    4. Verify zero conflict markers:
       `git grep '^<<<<<<< ' -- docker/windows/Dockerfile`
       Expected: empty output (exit 1).

    5. Verify HEAD's content survived — assert LongPathsEnabled appears in the resolved file:
       `grep -c 'LongPathsEnabled' docker/windows/Dockerfile`
       Expected: at least 1 match.

    6. Stage and commit (SSH-signed per project convention; do NOT pass `--no-gpg-sign`). Title: `resolve(config): docker/windows/Dockerfile — keep HEAD per D-31-09 (preserve LongPathsEnabled registry tweak)`.

       Commit body should record:
       - Rationale: HEAD adds LongPathsEnabled registry tweak required for Windows builds where Vortex paths into game directories regularly exceed 260 chars; upstream side is empty; no fork-tooling references on either side.
       - R4 (informational): Conflict marker on upstream side says `>>>>>>> v1.16.9` rather than v2.0.1. Implies the conflict isn't from v2.0.0->v2.0.1 churn but older drift carried forward by PR #5's upstream auto-merge. No resolution-stance impact; flagged for future sync forensics.
       - Same stance as v8.0 Phase 24-02.

  </action>
  <verify>
    <automated>git grep '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- docker/windows/Dockerfile; test $? -eq 1 &amp;&amp; test "$(grep -c 'LongPathsEnabled' docker/windows/Dockerfile)" -ge "1" &amp;&amp; git log -1 --pretty=format:'%s' docker/windows/Dockerfile | grep -q 'resolve(config): docker/windows/Dockerfile — keep HEAD per D-31-09'</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' -- docker/windows/Dockerfile` returns no output (exit 1)
    - `grep -c 'LongPathsEnabled' docker/windows/Dockerfile` returns at least 1
    - `git log -1 --pretty=format:'%s' docker/windows/Dockerfile` shows commit titled `resolve(config): docker/windows/Dockerfile — keep HEAD per D-31-09 (preserve LongPathsEnabled registry tweak)`
    - Commit body records the keep-HEAD rationale + R4 informational note about the v1.16.9 marker label
  </acceptance_criteria>
  <done>`docker/windows/Dockerfile` is conflict-free; LongPathsEnabled registry tweak preserved; resolution committed atomically with R4 forensic note in commit body.</done>
</task>

</tasks>

<verification>
- `git grep '^<<<<<<< ' -- docker/windows/Dockerfile` returns no output
- `grep -c 'LongPathsEnabled' docker/windows/Dockerfile` returns at least 1
- One atomic commit on `v8.1/config-bucket` with D-31-03 title format
</verification>

<success_criteria>
docker/windows/Dockerfile resolved per D-31-09 keep-HEAD stance. Long-path support preserved. Commit body documents R4 forensic curiosity for future syncs.
</success_criteria>

<rollback>
If verification fails (markers remain, LongPathsEnabled missing, or commit didn't land):
`git reset --soft HEAD^` to drop the commit, leave changes staged.
`git restore --staged docker/windows/Dockerfile && git checkout HEAD -- docker/windows/Dockerfile` to fully reset to pre-task state, then re-read the file and try again.
</rollback>

<output>
Create `.planning/phases/31-config-bucket/31-02-SUMMARY.md` recording the commit SHA, the resolution path (keep-HEAD), and the R4 forensic note about the v1.16.9 marker label for the milestone post-mortem.
</output>
