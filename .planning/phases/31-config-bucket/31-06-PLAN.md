---
phase: 31-config-bucket
plan: 06
type: execute
wave: 2
depends_on: [01]
files_modified:
    - pnpm-workspace.yaml
autonomous: true
requirements: [SYNC-2.0.1-02]
must_haves:
    truths:
        - "pnpm-workspace.yaml contains no conflict markers"
        - "pnpm-workspace.yaml `allowBuilds` block contains `@electron/rebuild: true` (D-31-07)"
        - "pnpm-workspace.yaml `catalog:` block retains HEAD's `leveldown` 5.6.0 + `levelup` 4.4.0 entries (RESEARCH.md: fork still uses LevelDB for legacy state per CLAUDE.md tech stack)"
        - "pnpm-workspace.yaml `catalog:` block adds upstream's new entries: `esptk`, `exe-version`, `gamebryo-savegame`, `native-errors` (additive non-conflicting per RESEARCH.md catalog deltas)"
        - "pnpm-workspace.yaml `catalogMode: strict` is preserved"
        - "pnpm-workspace.yaml `failIfNoMatch: true` is preserved (Plan 31-01's R1 restore puts back package.json files this guard would otherwise trip on)"
        - "pnpm-workspace.yaml `nx: ^22.7.1` catalog entry is preserved (D-31-14)"
        - "pnpm-workspace.yaml file parses as valid YAML"
    artifacts:
        - path: "pnpm-workspace.yaml"
          provides: "pnpm workspace + catalog + allowBuilds — keep-HEAD allowBuilds region per D-31-07; merge upstream catalog additions per RESEARCH.md"
    key_links:
        - from: "pnpm-workspace.yaml allowBuilds"
          to: "package.json `rebuild:native` script (D-31-06; resolved in 31-05)"
          via: "@electron/rebuild: true entry retained per D-31-07"
          pattern: "yaml allowBuilds block contains '@electron/rebuild' key with value true"
        - from: "pnpm-workspace.yaml catalog leveldown/levelup"
          to: "fork's legacy LevelDB persistence layer (CLAUDE.md tech stack)"
          via: "keep-HEAD on catalog leveldown/levelup conflict region"
          pattern: "yaml catalog contains 'leveldown' and 'levelup' keys"
---

<objective>
Resolve `pnpm-workspace.yaml` per D-31-07 + RESEARCH.md catalog deltas: keep HEAD on the four conflict regions where HEAD's value is correctness-critical (`@electron/rebuild: true`, `leveldown`, `levelup`), and merge upstream's additive catalog entries (`esptk`, `exe-version`, `gamebryo-savegame`, `native-errors`).

Purpose: pnpm strict allowBuilds enumerates every package permitted to execute build scripts. `@electron/rebuild` is paired with `rebuild:native` (D-31-06; resolved in 31-05) — dropping it would break the fork's native module rebuild flow. `leveldown`/`levelup` underpin fork's legacy LevelDB persistence layer (per CLAUDE.md tech stack: "levelup 4.4.0 + leveldown 5.6.0 — Legacy persistent key-value store"); upstream removed them but the fork still needs them. Catalog additions are mostly new direct deps from upstream v2.0.1 that the fork doesn't object to.

Output: `pnpm-workspace.yaml` conflict-free, allowBuilds region retains `@electron/rebuild: true`, catalog retains `leveldown`/`levelup` and adds `esptk`/`exe-version`/`gamebryo-savegame`/`native-errors`, atomic commit per D-31-03.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31-config-bucket/31-CONTEXT.md
@.planning/phases/31-config-bucket/31-RESEARCH.md
@.planning/milestones/v8.0-phases/24-config-bucket/24-06-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve pnpm-workspace.yaml per D-31-07 + RESEARCH.md catalog deltas</name>
  <files>pnpm-workspace.yaml</files>
  <read_first>
    - .planning/phases/31-config-bucket/31-CONTEXT.md (D-31-07: keep HEAD on allowBuilds; retain @electron/rebuild: true; merge upstream catalog additions elsewhere)
    - .planning/phases/31-config-bucket/31-RESEARCH.md (Per-File Notes section on pnpm-workspace.yaml — four conflict regions enumerated: 1=allowBuilds @electron/rebuild keep-HEAD; 2=catalog esptk+exe-version take upstream; 3=catalog gamebryo-savegame take upstream; 4=catalog leveldown/levelup keep-HEAD; 5=catalog native-errors take upstream; nx: ^22.7.1 confirmed present; catalogMode: strict + failIfNoMatch: true preserved)
    - pnpm-workspace.yaml (the file being modified, with conflict markers)
  </read_first>
  <action>
    Per D-31-07 + RESEARCH.md catalog deltas: targeted hand-resolution of `pnpm-workspace.yaml`.

    1. Read `pnpm-workspace.yaml` end-to-end. Identify every conflict region. Per RESEARCH.md there are four regions; structure follows:
       - `packages:` array (workspace globs) — typically not in conflict
       - `catalogMode: strict` line — must be preserved
       - `failIfNoMatch: true` — must be preserved
       - `catalog:` block — three of the four conflict regions are here (esptk/exe-version, gamebryo-savegame, leveldown/levelup, native-errors)
       - `allowBuilds:` block — one conflict region here (`@electron/rebuild: true`)

    2. **Region 1 (allowBuilds `@electron/rebuild: true`) — keep HEAD:** take HEAD's entire allowBuilds object. Verify after: `@electron/rebuild: true` is present. Other allowBuilds entries (likely native modules: `node-bsatk`, `node-ba2tk`, `vortexmt`, `turbowalk`, `permissions`, `bsatk`, `core-js`, `drivelist`, `electron`, `esbuild`, `font-scanner`, `gamebryo-savegame`, `leveldown`, `loot`, `protobufjs`, `unrs-resolver`, `winapi-bindings`, `xxhash-addon`, `@parcel/watcher`, `@nexusmods/fomod-installer-native`, `nx: false`) — retain all of them.

    3. **Region 2 (catalog `esptk` + `exe-version`) — take upstream:** HEAD doesn't list these; upstream adds them. Take upstream's additions wholesale.

    4. **Region 3 (catalog `gamebryo-savegame`) — take upstream:** HEAD doesn't list; upstream adds. Take upstream's addition.

    5. **Region 4 (catalog `leveldown` 5.6.0 + `levelup` 4.4.0) — keep HEAD:** HEAD has, upstream removes. Keep HEAD per CLAUDE.md tech stack ("levelup 4.4.0 + leveldown 5.6.0 — Legacy persistent key-value store"). Verify after: catalog block contains `leveldown` and `levelup`.

    6. **Region 5 (catalog `native-errors`) — take upstream:** HEAD doesn't list; upstream adds. Take upstream's addition.

    7. **Non-conflicting changes elsewhere:** preserve. RESEARCH.md confirms `nx: ^22.7.1` is still in catalog (D-31-14 holds), `catalogMode: strict` is preserved, `failIfNoMatch: true` is preserved.

    8. Apply the resolution by hand-editing the file. (`git checkout --ours` and `--theirs` are not granular enough for this 5-region case — hand-edit per region.) For each conflict marker block:
       - Decide per the disposition above (1, 4 keep HEAD; 2, 3, 5 take upstream).
       - Delete the conflict markers and the rejected side's content.

    9. After all regions resolved:
       - `git grep '^<<<<<<< ' -- pnpm-workspace.yaml` returns no output.
       - Verify YAML parses (preferred: `python3 -c "import yaml; yaml.safe_load(open('pnpm-workspace.yaml'))"` — if python3-yaml is not available, an alternative is `node -e "const yaml = require('yaml'); yaml.parse(require('fs').readFileSync('pnpm-workspace.yaml','utf8'))"` if the `yaml` package is in node_modules; otherwise the lockfile-regen step in 31-07 will reject malformed YAML).
       - Confirm allowBuilds entry: `grep -E '@electron/rebuild:\\s*true' pnpm-workspace.yaml` returns one match.
       - Confirm catalogMode strict: `grep -E 'catalogMode:\\s*strict' pnpm-workspace.yaml` returns one match.
       - Confirm failIfNoMatch true: `grep -E 'failIfNoMatch:\\s*true' pnpm-workspace.yaml` returns one match.
       - Confirm leveldown/levelup retained: `grep -F 'leveldown' pnpm-workspace.yaml && grep -F 'levelup' pnpm-workspace.yaml` both return at least one match.
       - Confirm upstream additions: `grep -F 'esptk' pnpm-workspace.yaml && grep -F 'exe-version' pnpm-workspace.yaml && grep -F 'gamebryo-savegame' pnpm-workspace.yaml && grep -F 'native-errors' pnpm-workspace.yaml` all return at least one match each.
       - Confirm nx still present: `grep -F 'nx:' pnpm-workspace.yaml` returns at least one match (D-31-14).

    10. Stage and commit. R5 mitigation: husky pre-commit may run oxfmt against the YAML file. Since this commit RESOLVES the YAML, oxfmt should now succeed against `pnpm-workspace.yaml` itself, but if `package.json` is still un-committed (i.e. Plan 31-05 hasn't run yet) and oxfmt also runs against it, pre-commit fails. RESEARCH.md R5 says either (a) sequence 31-06 after 31-05 OR (b) accept `--no-verify` here with documented rationale. The plan dependencies allow either order; document in commit body whichever path was taken.

       Title: `resolve(config): pnpm-workspace.yaml — keep HEAD allowBuilds region per D-31-07 + merge upstream catalog additions`
       Commit body should record:
       - allowBuilds: HEAD wins on `@electron/rebuild: true` (pairs with `rebuild:native` in package.json per D-31-06 / Plan 31-05).
       - Catalog: HEAD wins on `leveldown` 5.6.0 + `levelup` 4.4.0 (fork's legacy LevelDB layer per CLAUDE.md tech stack); upstream wins on additive entries `esptk`, `exe-version`, `gamebryo-savegame`, `native-errors`.
       - Preserved: `catalogMode: strict`, `failIfNoMatch: true`, `nx: ^22.7.1`.
       - R5 footnote: either "committed in normal flow (Plan 31-05 already ran)" OR "used --no-verify because Plan 31-05 hadn't yet committed package.json so oxfmt couldn't parse it; same workaround as v8.0 24-05."

  </action>
  <verify>
    <automated>git grep '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- pnpm-workspace.yaml; test $? -eq 1 &amp;&amp; grep -qE '@electron/rebuild:\s*true' pnpm-workspace.yaml &amp;&amp; grep -qE 'catalogMode:\s*strict' pnpm-workspace.yaml &amp;&amp; grep -qE 'failIfNoMatch:\s*true' pnpm-workspace.yaml &amp;&amp; grep -qF 'leveldown' pnpm-workspace.yaml &amp;&amp; grep -qF 'levelup' pnpm-workspace.yaml &amp;&amp; grep -qF 'esptk' pnpm-workspace.yaml &amp;&amp; grep -qF 'exe-version' pnpm-workspace.yaml &amp;&amp; grep -qF 'gamebryo-savegame' pnpm-workspace.yaml &amp;&amp; grep -qF 'native-errors' pnpm-workspace.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' -- pnpm-workspace.yaml` returns no output (exit 1)
    - `grep -E '@electron/rebuild:\s*true' pnpm-workspace.yaml` returns at least one match
    - `grep -E 'catalogMode:\s*strict' pnpm-workspace.yaml` returns at least one match
    - `grep -E 'failIfNoMatch:\s*true' pnpm-workspace.yaml` returns at least one match
    - `grep -F 'leveldown' pnpm-workspace.yaml` returns at least one match (HEAD-only catalog entry preserved)
    - `grep -F 'levelup' pnpm-workspace.yaml` returns at least one match (HEAD-only catalog entry preserved)
    - All four upstream catalog additions present: `esptk`, `exe-version`, `gamebryo-savegame`, `native-errors`
    - `grep -F 'nx:' pnpm-workspace.yaml` returns at least one match (D-31-14)
    - `git log -1 --pretty=format:'%s' -- pnpm-workspace.yaml` shows commit titled `resolve(config): pnpm-workspace.yaml — keep HEAD allowBuilds region per D-31-07 + merge upstream catalog additions`
  </acceptance_criteria>
  <done>pnpm-workspace.yaml is conflict-free, retains all five HEAD-survival items (@electron/rebuild, leveldown, levelup, catalogMode strict, failIfNoMatch true, nx), incorporates four upstream catalog additions, and is committed atomically with explanatory body.</done>
</task>

</tasks>

<verification>
- `git grep '^<<<<<<< ' -- pnpm-workspace.yaml` returns no output
- `@electron/rebuild: true`, `catalogMode: strict`, `failIfNoMatch: true` all present
- `leveldown`, `levelup`, `nx`, `esptk`, `exe-version`, `gamebryo-savegame`, `native-errors` all present
- One atomic commit on `v8.1/config-bucket` with D-31-03 title format
</verification>

<success_criteria>
pnpm-workspace.yaml resolved per D-31-07 + RESEARCH.md catalog deltas. Native module build permissions preserved (allowBuilds @electron/rebuild). Legacy LevelDB layer preserved (leveldown/levelup). Catalog additions from upstream merged (esptk, exe-version, gamebryo-savegame, native-errors). Nx orchestration preserved (D-31-14).
</success_criteria>

<rollback>
If verification fails (markers remain, YAML doesn't parse, or a survival item is missing): `git reset --soft HEAD^` drops the commit. Hand-edit the file to correct the disposition for the failing region, then re-commit. If the file is hopelessly tangled, restore the merge state with `git checkout --merge -- pnpm-workspace.yaml` and start over from the conflict markers.
</rollback>

<output>
Create `.planning/phases/31-config-bucket/31-06-SUMMARY.md` recording the commit SHA, the final allowBuilds entry list, the four-disposition catalog summary (HEAD wins on leveldown/levelup; upstream wins on esptk/exe-version/gamebryo-savegame/native-errors), and whether `--no-verify` was used (R5 footnote).
</output>
