---
phase: 31-config-bucket
plan: 08
type: execute
wave: 4
depends_on: [07]
files_modified: []
autonomous: false
requirements: [SYNC-2.0.1-02]
must_haves:
    truths:
        - "Local v8.1/config-bucket branch is fast-forward-only ahead of fork/sync/upstream-v2.0.1 OR a clean rebase has been performed if the cron raced"
        - "git push --force-with-lease succeeds and updates fork/sync/upstream-v2.0.1 to v8.1/config-bucket's HEAD (D-31-02 — defends against rebase-upstream.yml cron via lease check)"
        - "PR #5 head now reflects the resolved tree"
        - "Phase 31 atomic-commit history (1 restore + 9 file resolutions + 1 lockfile = 11 commits per D-31-03 cadence — exact count documented in 24-08 SUMMARY) is preserved on the remote"
    artifacts:
        - path: "(remote ref) fork/sync/upstream-v2.0.1"
          provides: "Updated PR #5 head with all Bucket A conflicts resolved"
    key_links:
        - from: "v8.1/config-bucket (local)"
          to: "fork/sync/upstream-v2.0.1 (remote)"
          via: "git push --force-with-lease"
          pattern: "lease check refuses if rebase-upstream.yml cron raced"
---

<objective>
Push the resolved `v8.1/config-bucket` branch to the fork as `sync/upstream-v2.0.1` using `--force-with-lease` per D-31-02. PR #5's review thread continues. Daily `rebase-upstream.yml` cron is defended against by the lease check.

Purpose: This plan is **autonomous-false** because force-pushing to a shared remote branch is a high-blast-radius destructive operation per CLAUDE.md operational rules ("Build over ask for reversible actions" applies — but force-push to a shared remote is NOT reversible without coordinating with reviewers). Specifically:

- `--force-with-lease` will refuse the push if the rebase-upstream.yml cron has updated `fork/sync/upstream-v2.0.1` since Plan 31-01 created the local branch — that race requires human inspection and decision (rebase vs replan).
- Final visible state on PR #5 is shared with reviewers — human confirmation that the local branch is in the desired state before pushing matches the GSD checkpoint:human-verify pattern.
- Per CLAUDE.md MEMORY `feedback_git_push_ssh.md`: sandbox blocks `.git/config` rewrites, so the push must use the inline SSH URL `git@github.com:atabisz/Vortex.git` rather than a remote name shortcut.

Output: Remote `fork/sync/upstream-v2.0.1` updated to point at the resolved tree. PR #5 reflects the resolution. Phase 31 fully landed.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31-config-bucket/31-CONTEXT.md
@.planning/phases/31-config-bucket/31-RESEARCH.md
@.planning/milestones/v8.0-phases/24-config-bucket/24-08-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-push sanity check — verify local branch state matches Phase 31 expectations</name>
  <files>(no file modifications — verification only)</files>
  <read_first>
    - .planning/phases/31-config-bucket/31-CONTEXT.md (D-31-02: push to fork/sync/upstream-v2.0.1 once at phase end with --force-with-lease; D-31-03: atomic commits — one per file + 1 lockfile commit)
  </read_first>
  <action>
    Verify local `v8.1/config-bucket` is in the expected state before any remote interaction.

    1. Confirm checked-out branch: `git rev-parse --abbrev-ref HEAD` returns `v8.1/config-bucket`.

    2. Confirm working tree is clean: `git status --porcelain` returns empty (no uncommitted changes, no untracked files in tracked-paths set).

    3. Count atomic commits introduced by Phase 31. Expected per the plan-level summary: 10 commits (1 Dockerfile + 4 eslint files + 2 vitest/prepare-dist + 1 package.json + 1 pnpm-workspace.yaml + 1 lockfile) plus the R1 restore commit from Plan 31-01 = 11 commits total.
       - Use `git log --oneline fork/sync/upstream-v2.0.1..v8.1/config-bucket | wc -l` to count.
       - Note: this counts commits since the local branch diverged from the *current* remote state. If the remote `fork/sync/upstream-v2.0.1` was updated by the cron during Phase 31 work (between Plan 31-01 and now), the count compares against the *current* remote and may differ — Task 2 detects that race.
       - Acceptable count: 11 commits (1 restore + 10 resolutions). Anything else suggests a missing or extra commit and warrants investigation — log the actual count in SUMMARY.

    4. Confirm zero conflict markers across the resolved Bucket A scope (other source files outside Bucket A still have markers — those are Phases 32-34 scope):
       ```
       git grep -l '^<<<<<<< ' -- \
         package.json pnpm-workspace.yaml pnpm-lock.yaml vitest.config.ts \
         'src/main/eslint.config.mjs' 'src/preload/eslint.config.mjs' \
         'src/renderer/eslint.config.mjs' 'src/shared/eslint.config.mjs' \
         'src/main/prepare-dist-package.mjs' 'docker/windows/Dockerfile'
       ```
       Expected: empty.

    5. Confirm `pnpm install --frozen-lockfile` still passes from Plan 31-07 (sanity re-run).

    6. Confirm R1 restore is intact:
       `test -f packages/paths/package.json && test -f packages/paths-node/package.json` exits 0.

  </action>
  <verify>
    <automated>test "$(git rev-parse --abbrev-ref HEAD)" = "v8.1/config-bucket" &amp;&amp; test -z "$(git status --porcelain)" &amp;&amp; test -z "$(git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- package.json pnpm-workspace.yaml pnpm-lock.yaml vitest.config.ts 'src/main/eslint.config.mjs' 'src/preload/eslint.config.mjs' 'src/renderer/eslint.config.mjs' 'src/shared/eslint.config.mjs' 'src/main/prepare-dist-package.mjs' 'docker/windows/Dockerfile' 2&gt;/dev/null)" &amp;&amp; test -f packages/paths/package.json &amp;&amp; test -f packages/paths-node/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse --abbrev-ref HEAD` returns `v8.1/config-bucket`
    - `git status --porcelain` returns empty
    - The Bucket A `git grep '^<<<<<<< '` check returns no files
    - Commit count between branch and `fork/sync/upstream-v2.0.1` is 11 (1 restore + 10 resolutions; any discrepancy noted in SUMMARY before proceeding)
    - `packages/paths/package.json` and `packages/paths-node/package.json` both exist (R1 restore intact)
  </acceptance_criteria>
  <done>Local branch state matches Phase 31 expectations; safe to attempt the lease-check fetch.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh remote ref and detect cron race</name>
  <files>(no file modifications — fetch only)</files>
  <read_first>
    - .planning/phases/31-config-bucket/31-CONTEXT.md (D-31-02: --force-with-lease defends against rebase-upstream.yml cron clobber)
  </read_first>
  <action>
    Refresh the remote reference and detect whether `fork/sync/upstream-v2.0.1` has been updated since Plan 31-01 created the local branch (cron race detection).

    1. `git fetch fork sync/upstream-v2.0.1` — refresh remote ref.

    2. Compute the remote tip we branched from (the original branch base):
       `BRANCH_BASE=$(git merge-base v8.1/config-bucket fork/sync/upstream-v2.0.1)`

    3. Compute the current remote tip:
       `REMOTE_HEAD=$(git rev-parse fork/sync/upstream-v2.0.1)`

    4. Compare:
       - If `BRANCH_BASE == REMOTE_HEAD`: no race. The remote hasn't moved since Plan 31-01. Safe to push with `--force-with-lease` directly. Proceed to Task 3 (push) without rebasing.
       - If `BRANCH_BASE != REMOTE_HEAD`: cron raced. The remote has new commits. Capture this finding in SUMMARY and STOP — do NOT proceed to Task 3 automatically. Task 3's checkpoint will route to the rebase-then-retry fallback under human review.

    5. Print the comparison result for the human reviewer:
       ```
       echo "BRANCH_BASE=$BRANCH_BASE"
       echo "REMOTE_HEAD=$REMOTE_HEAD"
       echo "Race detected: $([[ "$BRANCH_BASE" == "$REMOTE_HEAD" ]] && echo no || echo YES)"
       ```

  </action>
  <verify>
    <automated>git fetch fork sync/upstream-v2.0.1 &amp;&amp; git rev-parse fork/sync/upstream-v2.0.1 &gt; /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `git fetch fork sync/upstream-v2.0.1` exits 0
    - The race-detection result (no-race vs YES-race) is logged for the Task 3 checkpoint
  </acceptance_criteria>
  <done>Remote state refreshed; cron-race status known and recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-confirm push (force-with-lease) — fallback path if cron raced</name>
  <what-built>
    Plans 31-01 through 31-07 produced a local branch `v8.1/config-bucket` that resolves all 10 conflict-bearing Bucket A files on `fork/sync/upstream-v2.0.1` (9 hand-resolved + lockfile regenerated). R1 mitigation applied as the first commit (packages/paths{,-node} package.json + build configs restored from fork/master). Done-gate (D-31-17, all 5 items) satisfied. Tasks 1+2 confirmed local state and refreshed the remote ref.

    What remains: pushing `v8.1/config-bucket` to `fork/sync/upstream-v2.0.1` with `--force-with-lease` so PR #5 reflects the resolution.

    Why this requires human approval (autonomous: false):
    - Force-push to a shared remote is a high-blast-radius operation per CLAUDE.md operational rules.
    - PR #5 review thread is shared with reviewers — final tree state needs sign-off before it goes live.
    - `--force-with-lease` may refuse if cron raced; the rebase fallback decision is human-judgment-grade.

  </what-built>
  <how-to-verify>
    1. Review the cron-race status logged by Task 2 (`Race detected: no` vs `Race detected: YES`).

    2. **If `Race detected: no`:** Approve the push by running:
       ```
       git push --force-with-lease=sync/upstream-v2.0.1:$(git merge-base v8.1/config-bucket fork/sync/upstream-v2.0.1) git@github.com:atabisz/Vortex.git v8.1/config-bucket:sync/upstream-v2.0.1
       ```
       (Per MEMORY `feedback_git_push_ssh.md`: sandbox blocks `.git/config`, so push with the inline SSH URL `git@github.com:atabisz/Vortex.git`.)

       The lease form `--force-with-lease=<branch>:<expected-sha>` pins the lease to the exact SHA the local branch was based on, which matches `BRANCH_BASE` from Task 2.

       Expected outcome: push succeeds, remote is updated, PR #5 head reflects the resolved tree.

    3. **If `Race detected: YES`:** DO NOT push directly. The fallback path:
       a. Inspect the new remote commits: `git log v8.1/config-bucket..fork/sync/upstream-v2.0.1 --oneline`. These are commits the cron added.
       b. Decide: rebase (if cron commits are mechanical / non-conflicting with our resolutions) or abort + replan (if cron-introduced changes overlap with Bucket A files).
       c. If rebasing: `git rebase fork/sync/upstream-v2.0.1` on `v8.1/config-bucket`. Resolve any rebase conflicts following the same per-file stance from CONTEXT.md (D-31-05..D-31-15). Re-run `pnpm install --frozen-lockfile` to confirm the lockfile is still consistent post-rebase. Then re-run Task 2 to refresh the lease, and re-run this checkpoint with the updated `BRANCH_BASE`.
       d. If aborting: report the cron-introduced conflict scope back to planning; Phase 31 may need a follow-up plan or partial replan.

    4. Confirm the push landed by inspecting GitHub:
       `gh pr view 5 --repo atabisz/Vortex --json headRefName,headRefOid,statusCheckRollup` (use `GH_TOKEN` env var per MEMORY `reference_github_token.md` if `gh auth` is expired).
       Expected: `headRefOid` matches `git rev-parse v8.1/config-bucket` (local).

  </how-to-verify>
  <resume-signal>
    - Type "approved — pushed clean" if the no-race push succeeded
    - Type "approved — rebased and pushed" if the rebase-then-push fallback was used
    - Describe the issue and STOP if the rebase surfaces conflicts requiring replan
  </resume-signal>
</task>

</tasks>

<verification>
- `git rev-parse fork/sync/upstream-v2.0.1` matches `git rev-parse v8.1/config-bucket` after the push (refetch first: `git fetch fork sync/upstream-v2.0.1`)
- PR #5 (`atabisz/Vortex#5`) `headRefOid` matches the local branch's HEAD
- All 11 atomic commits (1 R1 restore + 10 resolutions) land on the remote in order

## Phase 31 Done Gate (D-31-17, all five items)

After Task 3's push lands, the phase-level done gate is fully satisfied:

1. `git grep '^<<<<<<< '` zero hits across all 10 conflict-bearing Bucket A files (proven Plan 31-07 Task 5).
2. `pnpm install` succeeds (proven Plan 31-07 Task 3).
3. `pnpm install --frozen-lockfile` succeeds (proven Plan 31-07 Task 4).
4. IDE/TypeScript server loads without parser errors and resolves all workspace packages — automated proxy via `pnpm nx run-many -t typecheck --projects=@vortex/shared` (Plan 31-07 Task 4); full typecheck is Phase 35 work since Phases 32-34 still need to resolve source-level conflicts.
5. Lockfile drift summary recorded in the chore(deps) commit body (proven Plan 31-07 Task 4).
   </verification>

<success_criteria>
Remote `fork/sync/upstream-v2.0.1` updated to the resolved tree. PR #5 review thread continues. Phase 31 work is now visible on the shared branch and is the foundation for Phases 32-37.
</success_criteria>

<rollback>
If the lease-checked push is rejected (cron raced and the rebase fallback is unsuitable): the local branch `v8.1/config-bucket` remains intact locally; report the cron diff scope to planning and Phase 31 needs a follow-up plan to either rebase-and-retry or surgically integrate the cron commits.
If the push lands but PR #5 status checks immediately fail (e.g. CI catches a regression that the local frozen-lockfile didn't): the local branch is still the truth; investigate the CI delta, plan a surgical follow-up commit, and push again with a fresh `--force-with-lease`. Don't roll back the push (that would clobber the resolved state on the remote and lose the work for reviewers).
</rollback>

<output>
Create `.planning/phases/31-config-bucket/31-08-SUMMARY.md` recording: race-detection result; whether direct push or rebase-then-push was used; final remote SHA; PR #5 status check result; final commit count on the branch (expected: 11).

If the rebase fallback was used, also document: which cron-introduced commits were rebased over; whether any rebase conflicts surfaced; if any did, which D-31-NN stance was applied to resolve them.
</output>
