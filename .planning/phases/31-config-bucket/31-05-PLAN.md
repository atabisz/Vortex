---
phase: 31-config-bucket
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
    - package.json
autonomous: true
requirements: [SYNC-2.0.1-02]
must_haves:
    truths:
        - "package.json contains no conflict markers"
        - "package.json retains the `rebuild:native` script entry (D-31-06; pairs with @electron/rebuild allowBuilds in pnpm-workspace.yaml — Plan 31-06)"
        - "package.json retains the `postinstall` script wired to scripts/postinstall-libloot.cjs (D-31-06: libloot wiring)"
        - "package.json retains the nx-orchestrated typecheck script (D-31-14: `pnpm nx run-many -t typecheck`)"
        - "package.json retains fork's existing build/dist/package/assets script structure (D-31-13: src/main/build output, no out/+dist/ split)"
        - "package.json retains fork's `package:local` script (HEAD-only per RESEARCH.md)"
        - "package.json does NOT contain upstream's new `dist:all`, `build:assets`, `dist:assets`, `dist:extensions`, `typecheck:extensions` scripts (D-31-15: drop)"
        - "package.json file parses as valid JSON"
    artifacts:
        - path: "package.json"
          provides: "Root package metadata, scripts, devDependencies — keep-HEAD scripts region per D-31-06"
    key_links:
        - from: "package.json `rebuild:native` script"
          to: "scripts/rebuild-pnpm-natives.cjs (HEAD-confirmed reusable asset per CONTEXT.md)"
          via: "preserved during keep-HEAD scripts resolution"
          pattern: "package.json contains literal `rebuild:native` key"
        - from: "package.json `postinstall` script"
          to: "scripts/postinstall-libloot.cjs (HEAD-confirmed reusable asset per CONTEXT.md)"
          via: "preserved during keep-HEAD scripts resolution"
          pattern: "package.json `postinstall` value references postinstall-libloot.cjs"
        - from: "package.json `typecheck` script"
          to: "nx run-many"
          via: "preserved during keep-HEAD scripts resolution per D-31-14"
          pattern: "package.json `typecheck` value contains 'nx run-many'"
---

<objective>
Resolve `package.json` per D-31-06: keep HEAD wholesale on the scripts conflict region. Drop upstream's new `dist:all`, `build:assets`, `dist:assets`, `dist:extensions`, `typecheck:extensions` scripts (D-31-15). Preserve fork's `rebuild:native` (D-31-06), `postinstall-libloot.cjs` wiring, nx-orchestrated typecheck (D-31-14), `src/main/build` output structure (D-31-13), and `package:local` script.

Purpose: package.json is the most consequential resolution in this phase. Adopting upstream's scripts cascades into release-linux.yml, electron-builder, postinstall, validateArchiveToStaging tests, bundledPlugins copy logic — explicitly out of scope per D-31-13. Keep-HEAD scripts region is the surgical choice. Same stance as v8.0 24-05.

Note v2.0.1-specific subtlety from RESEARCH.md: upstream's typecheck now invokes `pnpm -F @vortex/paths run build` as a prereq — but v2.0.1 also deletes `packages/paths/package.json` upstream-side. Keeping HEAD's `nx run-many -t typecheck` dodges this trap entirely.

Output: `package.json` conflict-free, all fork scripts preserved, upstream new scripts dropped, atomic commit per D-31-03.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31-config-bucket/31-CONTEXT.md
@.planning/phases/31-config-bucket/31-RESEARCH.md
@.planning/milestones/v8.0-phases/24-config-bucket/24-05-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve package.json — keep HEAD scripts region (D-31-06/D-31-13/D-31-14/D-31-15)</name>
  <files>package.json</files>
  <read_first>
    - .planning/phases/31-config-bucket/31-CONTEXT.md (D-31-06: keep HEAD scripts region wholesale; D-31-13: keep src/main/build output, no out/+dist/ split; D-31-14: keep nx-orchestrated typecheck; D-31-15: drop upstream's `dist:all`, `build:assets`, `dist:assets`, `dist:extensions`, `typecheck:extensions`)
    - .planning/phases/31-config-bucket/31-RESEARCH.md (Per-File Notes section on package.json — full HEAD/upstream side comparison; v2.0.1-surprise note about upstream's typecheck depending on packages/paths build that upstream itself deleted)
    - package.json (the file being modified, with conflict markers)
    - scripts/rebuild-pnpm-natives.cjs (referenced by `rebuild:native` — confirm script exists)
    - scripts/postinstall-libloot.cjs (referenced by `postinstall` — confirm script exists)
  </read_first>
  <action>
    Per D-31-06/D-31-13/D-31-14/D-31-15: surgical resolution of `package.json`.

    Step-by-step protocol:

    1. Read `package.json` end-to-end. Identify every conflict region. There are typically two:
       - The `scripts` block (the consequential one — keep HEAD per D-31-06).
       - Possibly `devDependencies` or `dependencies` blocks (handle separately — see step 4).

    2. Confirm the referenced fork scripts exist before resolving:
       `test -f scripts/rebuild-pnpm-natives.cjs` (exit 0 expected)
       `test -f scripts/postinstall-libloot.cjs` (exit 0 expected)
       If either is missing, STOP — the resolution would orphan the script reference.

    3. **Scripts conflict region — keep HEAD wholesale:**
       - For the conflict markers around the `"scripts": { ... }` block, take HEAD's entire scripts object.
       - Verify after resolution that these HEAD-side scripts are present:
         * `"rebuild:native"` — must exist (electron-rebuild + scripts/rebuild-pnpm-natives.cjs wiring per D-31-06)
         * `"postinstall"` — must exist; value must reference `scripts/postinstall-libloot.cjs` per D-31-06
         * `"typecheck"` — must contain `nx run-many` per D-31-14
         * `"build"`, `"dist"`, `"package"`, `"package:local"`, `"assets"` — fork's existing entries; must reference `src/main/build` paths where applicable per D-31-13
       - Verify after resolution that these upstream-introduced scripts are NOT present:
         * `"dist:all"` — drop per D-31-15
         * `"build:assets"` — drop per D-31-15
         * `"dist:assets"` — drop per D-31-15
         * `"dist:extensions"` — drop per D-31-15
         * `"typecheck:extensions"` — drop per D-31-15

    4. **dependencies / devDependencies conflict regions (if any):**
       - These are NOT covered by D-31-06's scripts-region rule. Hand-merge per package using union strategy:
         * If both sides have the same package at different versions: prefer upstream's version (likely a v2.0.1 bump). Exception: HEAD's version is a fork-specific custom branch (e.g. `github:atabisz/some-fork#linux-port`), KEEP HEAD.
         * If only one side has a package: include it.
       - Catalog mode (D-31-07: `pnpm-workspace.yaml: catalogMode: strict`) means most direct dep versions live in pnpm-workspace.yaml's catalog, so package.json typically has `"catalog:"` references — those don't conflict on version.
       - Watch for `nx`: D-31-14 says verify nx still present after lockfile regen. For this task: if upstream removed `nx` from `devDependencies`, KEEP HEAD's `nx` entry (RESEARCH.md confirms `nx: ^22.7.1` is still in catalog on v2.0.1).

    5. **Other top-level fields** (`name`, `version`, `engines`, `volta`, `pnpm`, `workspaces` if any):
       - Hand-merge: prefer upstream where it makes a meaningful update (e.g. version field), keep HEAD where fork has customizations (volta lockfile per CLAUDE.md: node 22.22.0).

    6. Apply the resolution. For most conflict regions hitting only the scripts block, `git checkout --ours package.json` may be sufficient. If non-scripts regions need union-merging, hand-edit instead.

    7. After all regions resolved:
       - `git grep '^<<<<<<< ' -- package.json` returns no output.
       - `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0.
       - Run an explicit script-presence/absence check (one-liner equivalent of v8.0 24-05's check):
         `node -e "const p=require('./package.json'); for (const k of ['rebuild:native','postinstall','typecheck']) if (!p.scripts[k]) { console.error('missing script:', k); process.exit(1); } for (const k of ['dist:all','build:assets','dist:assets','dist:extensions','typecheck:extensions']) if (p.scripts[k]) { console.error('upstream script not dropped:', k); process.exit(1); }"`
       - Confirm postinstall references libloot:
         `grep -E '"postinstall":\\s*"[^"]*postinstall-libloot' package.json` returns at least one match.
       - Confirm typecheck uses nx:
         `grep -E '"typecheck":\\s*"[^"]*nx run-many' package.json` returns at least one match.
       - Confirm rebuild:native script presence (RESEARCH.md grep):
         `grep -F 'rebuild:native' package.json` returns at least one match.
       - Confirm package:local survived:
         `grep -F 'package:local' package.json` returns at least one match.

    8. Stage and commit (SSH-signed):
       Title: `resolve(config): package.json — keep HEAD scripts region per D-31-06 (drop upstream dist:all/build:assets/dist:assets/dist:extensions/typecheck:extensions per D-31-15)`
       Commit body should record:
       - HEAD-side scripts preserved: rebuild:native (electron-rebuild + scripts/rebuild-pnpm-natives.cjs), postinstall (libloot via scripts/postinstall-libloot.cjs), typecheck (nx run-many per D-31-14), build/dist/package/package:local/assets (src/main/build output paths per D-31-13).
       - Upstream-side scripts dropped: dist:all, build:assets, dist:assets, dist:extensions, typecheck:extensions (D-31-15) — adopting them would cascade into release-linux.yml, electron-builder, postinstall, validateArchiveToStaging, bundledPlugins copy logic.
       - v2.0.1 surprise: upstream's typecheck depends on `pnpm -F @vortex/paths run build` while v2.0.1 itself deleted `packages/paths/package.json` — keeping HEAD's nx-based typecheck dodges that trap.
       - Same stance as v8.0 Phase 24-05.

    9. R5 mitigation: husky pre-commit may fail if oxfmt encounters unresolved YAML in `pnpm-workspace.yaml` (still has conflict markers if Plan 31-06 hasn't run yet). If pre-commit fails for that reason, run with `git commit --no-verify` and document in commit body: `Used --no-verify because pnpm-workspace.yaml still has unresolved markers (Plan 31-06 sequenced after this one); same workaround as v8.0 24-05.`

  </action>
  <verify>
    <automated>git grep '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- package.json; test $? -eq 1 &amp;&amp; node -e "const p=require('./package.json'); for (const k of ['rebuild:native','postinstall','typecheck']) if (!p.scripts[k]) { console.error('missing:', k); process.exit(1); } for (const k of ['dist:all','build:assets','dist:assets','dist:extensions','typecheck:extensions']) if (p.scripts[k]) { console.error('not dropped:', k); process.exit(1); } console.log('OK');" &amp;&amp; grep -F 'rebuild:native' package.json &gt; /dev/null &amp;&amp; grep -F 'package:local' package.json &gt; /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' -- package.json` returns no output (exit 1)
    - `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0
    - `node -e "console.log(!!require('./package.json').scripts['rebuild:native'])"` outputs `true`
    - `node -e "console.log(!!require('./package.json').scripts['postinstall'])"` outputs `true`
    - `grep -E '"postinstall":\s*"[^"]*postinstall-libloot' package.json` returns at least one match
    - `grep -E '"typecheck":\s*"[^"]*nx run-many' package.json` returns at least one match
    - `grep -F 'rebuild:native' package.json` returns at least one match
    - `grep -F 'package:local' package.json` returns at least one match
    - `node -e "for (const k of ['dist:all','build:assets','dist:assets','dist:extensions','typecheck:extensions']) if (require('./package.json').scripts[k]) { console.error('FAIL:', k); process.exit(1); }; console.log('clean')"` outputs `clean`
    - `git log -1 --pretty=format:'%s' -- package.json` shows commit titled `resolve(config): package.json — keep HEAD scripts region per D-31-06 ...`
  </acceptance_criteria>
  <done>package.json is conflict-free; fork scripts (rebuild:native, postinstall->libloot, nx typecheck, package:local) survive; upstream new scripts (dist:all, build:assets, dist:assets, dist:extensions, typecheck:extensions) are dropped; atomic commit recorded with explanatory body.</done>
</task>

</tasks>

<verification>
- `git grep '^<<<<<<< ' -- package.json` returns no output
- One atomic commit on `v8.1/config-bucket` with D-31-03 title format
- All script-survival assertions pass (rebuild:native, postinstall->libloot, typecheck->nx, package:local)
- All five script-drop assertions pass (dist:all, build:assets, dist:assets, dist:extensions, typecheck:extensions absent)
</verification>

<success_criteria>
package.json resolved per D-31-06/D-31-13/D-31-14/D-31-15. Fork's build wiring intact. The scripts cascade rejected by D-31-13 is avoided. Upstream's optional script additions deferred to a future cleanup milestone (D-31-15).
</success_criteria>

<rollback>
If verification fails: `git reset --soft HEAD^` to drop the commit. If a survival check failed (script missing or upstream script not dropped), hand-edit `package.json` correctly and re-commit. If the JSON parse fails, restore the file from the merge state via `git checkout --merge -- package.json` (re-conflicted) or `git checkout HEAD -- package.json` (post-revert) and retry.
</rollback>

<output>
Create `.planning/phases/31-config-bucket/31-05-SUMMARY.md` recording the commit SHA, the survival-list confirmation (all four required scripts present), the dropped-scripts list (all five upstream scripts absent), and whether `--no-verify` was used (per R5 mitigation).
</output>
