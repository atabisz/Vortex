---
phase: 31-config-bucket
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
    - packages/paths/package.json
    - packages/paths/tsconfig.json
    - packages/paths/tsdown.config.ts
    - packages/paths-node/package.json
    - packages/paths-node/tsconfig.json
    - packages/paths-node/tsconfig.build.json
    - packages/paths-node/vitest.config.ts
autonomous: true
requirements: [SYNC-2.0.1-02]
must_haves:
    truths:
        - "Local branch v8.1/config-bucket exists and is checked out (D-31-01)"
        - "Branch HEAD matches fork/sync/upstream-v2.0.1 at the moment of branching (D-31-01)"
        - "All 9 expected hand-resolved Bucket A files contain conflict markers (sanity check against research's authoritative table)"
        - ".vscode/extensions.json and src/renderer/tsconfig.api.json have NO conflict markers and NO diff vs fork/master (research §Per-File Notes — drop from D-31-08 / D-31-12 scope on this phase)"
        - "packages/paths/package.json + packages/paths-node/package.json restored from fork/master so pnpm install will succeed in 31-07 (R1 mitigation; collapses v8.0 Phase 25's Wave 1 work into Phase 31)"
        - "Default resolution stance is hand-resolve per file (D-31-05) — no blanket pick-ours/pick-theirs"
        - "Phase 31 does NOT FF-merge sync/upstream-v2.0.1 into master (D-31-04 — that happens at Phase 36)"
        - "R2 (Jest __mocks__/ reintroduced) and R3 (orphan electron-builder.config.json) surfaced and deferred — recorded in 31-01-SUMMARY.md for Phase 34 / Phase 35 follow-up"
    artifacts:
        - path: ".git/refs/heads/v8.1/config-bucket"
          provides: "Local working branch for Phase 31 resolutions"
        - path: "packages/paths/package.json"
          provides: "Restored @vortex/paths package manifest (R1 mitigation)"
        - path: "packages/paths-node/package.json"
          provides: "Restored @vortex/paths-node package manifest (R1 mitigation)"
    key_links:
        - from: "v8.1/config-bucket"
          to: "fork/sync/upstream-v2.0.1"
          via: "git branch from remote ref"
          pattern: "git branch v8.1/config-bucket fork/sync/upstream-v2.0.1"
        - from: "packages/paths/{package.json,tsconfig.json,tsdown.config.ts}"
          to: "fork/master:packages/paths/*"
          via: "git checkout fork/master -- packages/paths/<files>"
          pattern: "restore commit appears as first commit on v8.1/config-bucket"
---

<objective>
Bootstrap the Phase 31 working branch, capture a baseline conflict-marker inventory, and restore the orphan `packages/paths` + `packages/paths-node` package.json + build configs (R1 mitigation) before any resolution work begins.

Purpose: Subsequent plans (31-02..31-06) commit one resolution per file onto this branch. The baseline inventory acts as a sanity check that the conflict surface matches RESEARCH.md's authoritative table — drift here would invalidate the per-file stances locked in D-31-06..D-31-15.

The R1 restore is the critical addition vs v8.0 Phase 24-01: v2.0.1 deletes `packages/paths/package.json` and `packages/paths-node/package.json` upstream-side while leaving orphan `src/`/`README.md` files on the merge tree. Without restoration, `pnpm install` in 31-07 would fail because `pnpm-workspace.yaml` globs `./packages/*` with `failIfNoMatch: true`. v8.0 handled this in Phase 25; v8.1 collapses that work into Phase 31's first commit (per RESEARCH.md §R1 recommendation).

Output: Local branch `v8.1/config-bucket` checked out at `fork/sync/upstream-v2.0.1`; one restore commit on the branch; baseline conflict marker listing captured; R2/R3 deferred items recorded in 31-01-SUMMARY.md.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-config-bucket/31-CONTEXT.md
@.planning/phases/31-config-bucket/31-RESEARCH.md
@.planning/milestones/v8.0-phases/24-config-bucket/24-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch fork remote and create v8.1/config-bucket branch (D-31-01)</name>
  <files>.git/refs/heads/v8.1/config-bucket</files>
  <read_first>
    - .planning/phases/31-config-bucket/31-CONTEXT.md (D-31-01: branch named `v8.1/config-bucket`, branched from `fork/sync/upstream-v2.0.1`)
    - .planning/phases/31-config-bucket/31-RESEARCH.md (Valid until: SHA at fork/sync/upstream-v2.0.1 should still be 8054a935b6aad505798bba8a993d002718d119cb; if not, daily rebase-upstream.yml cron rewrote the branch — re-run the conflict enumeration before branching)
  </read_first>
  <action>
    Per D-31-01: create branch `v8.1/config-bucket` from `fork/sync/upstream-v2.0.1`.

    Steps:
    1. `git fetch fork sync/upstream-v2.0.1` — refresh the remote ref before branching (the rebase-upstream.yml cron may have updated it).
    2. Capture the remote SHA for the SUMMARY: `git rev-parse fork/sync/upstream-v2.0.1`. RESEARCH.md was authored against `8054a935b6aad505798bba8a993d002718d119cb`. If the SHA differs, log it in the SUMMARY and continue (the conflict surface may have shifted; subsequent task will re-verify).
    3. `git switch -c v8.1/config-bucket fork/sync/upstream-v2.0.1` — creates and checks out the local branch at the remote head. Use `git switch -C` to force-recreate if the branch already exists from a prior aborted run.
    4. Verify HEAD: `git rev-parse HEAD` matches `git rev-parse fork/sync/upstream-v2.0.1`.

    Do NOT commit anything in this task. No file modifications. The branch creation is the only state change.

  </action>
  <verify>
    <automated>test "$(git rev-parse HEAD)" = "$(git rev-parse fork/sync/upstream-v2.0.1)" &amp;&amp; test "$(git rev-parse --abbrev-ref HEAD)" = "v8.1/config-bucket"</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse --abbrev-ref HEAD` outputs `v8.1/config-bucket`
    - `git rev-parse HEAD` equals `git rev-parse fork/sync/upstream-v2.0.1`
    - Working tree is clean except for the inherited conflict markers from PR #5 auto-merge
  </acceptance_criteria>
  <done>Branch `v8.1/config-bucket` is checked out at the same commit as `fork/sync/upstream-v2.0.1`.</done>
</task>

<task type="auto">
  <name>Task 2: Capture baseline conflict marker inventory across Bucket A (research-authoritative table)</name>
  <files>(no file modifications — verification only)</files>
  <read_first>
    - .planning/phases/31-config-bucket/31-RESEARCH.md (§Conflict File Inventory authoritative table — 9 hand-resolved + 1 lockfile = 10 conflict-bearing files)
    - .planning/phases/31-config-bucket/31-CONTEXT.md (Bucket A files list at lines 11–24)
  </read_first>
  <action>
    Sanity-check the 9 hand-resolved Bucket A files all contain conflict markers, AND that the two RESEARCH-flagged "no conflict" files (`.vscode/extensions.json`, `src/renderer/tsconfig.api.json`) really have no markers.

    Run exactly:
    ```
    git grep -l '^<<<<<<< ' -- \
      package.json \
      pnpm-workspace.yaml \
      vitest.config.ts \
      'src/main/eslint.config.mjs' \
      'src/preload/eslint.config.mjs' \
      'src/renderer/eslint.config.mjs' \
      'src/shared/eslint.config.mjs' \
      'src/main/prepare-dist-package.mjs' \
      'docker/windows/Dockerfile'
    ```

    Expected: all 9 paths above are listed in the output (`pnpm-lock.yaml` is excluded — it gets regenerated in 31-07 per D-31-16, not hand-resolved).

    Then assert the no-conflict files truly have no markers:
    ```
    git grep -l '^<<<<<<< ' -- '.vscode/extensions.json' 'src/renderer/tsconfig.api.json'
    ```
    Expected: empty output (per RESEARCH.md §Per-File Notes — these have no conflict on v2.0.1, drop from D-31-08 / D-31-12 scope).

    If the 9-file output omits any expected file, OR the no-conflict assertion lists either file, STOP and report drift — do not start resolutions. The locked stances (D-31-06..D-31-15) assume the conflict surface matches RESEARCH.md's snapshot.

    No commits, no edits — this task is purely a guard against a stale RESEARCH.md.

  </action>
  <verify>
    <automated>test "$(git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- package.json pnpm-workspace.yaml vitest.config.ts 'src/main/eslint.config.mjs' 'src/preload/eslint.config.mjs' 'src/renderer/eslint.config.mjs' 'src/shared/eslint.config.mjs' 'src/main/prepare-dist-package.mjs' 'docker/windows/Dockerfile' | wc -l)" = "9" &amp;&amp; test -z "$(git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- '.vscode/extensions.json' 'src/renderer/tsconfig.api.json' 2&gt;/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - The 9-file `git grep -l '^<<<<<<< '` command lists exactly 9 files
    - The no-conflict assertion (`.vscode/extensions.json`, `src/renderer/tsconfig.api.json`) returns empty output
    - If counts mismatch, the task fails — drift must be reported before any resolution plan runs
  </acceptance_criteria>
  <done>Baseline inventory matches RESEARCH.md's authoritative table. Phase 31 may proceed to R1 restore + resolution plans.</done>
</task>

<task type="auto">
  <name>Task 3: Verify R1 — orphan packages/paths and packages/paths-node state</name>
  <files>(no file modifications — verification only)</files>
  <read_first>
    - .planning/phases/31-config-bucket/31-RESEARCH.md (§R1 — packages/paths and packages/paths-node orphan package.json)
  </read_first>
  <action>
    Confirm the R1 risk is real on the current branch tip before restoring.

    Steps:
    1. Assert `packages/paths/package.json` does NOT exist on the current tree:
       ```
       test ! -e packages/paths/package.json
       ```
       Expected: exit 0 (file absent — confirms the orphan situation).

    2. Assert `packages/paths-node/package.json` does NOT exist on the current tree:
       ```
       test ! -e packages/paths-node/package.json
       ```
       Expected: exit 0.

    3. Assert orphan src/ exists (the merge left these behind):
       ```
       test -d packages/paths/src
       ```
       Expected: exit 0 (orphan src/ confirms the auto-merge state RESEARCH.md described).

    4. Confirm fork/master has the files we want to restore:
       ```
       git cat-file -e fork/master:packages/paths/package.json
       git cat-file -e fork/master:packages/paths-node/package.json
       ```
       Both must exit 0.

    If any of the above fails, STOP and report — the R1 mitigation may already have been applied, OR the orphan situation is different from what RESEARCH.md captured. Either way, Task 4's restore command needs human review before running.

    No commits, no edits.

  </action>
  <verify>
    <automated>test ! -e packages/paths/package.json &amp;&amp; test ! -e packages/paths-node/package.json &amp;&amp; test -d packages/paths/src &amp;&amp; git cat-file -e fork/master:packages/paths/package.json &amp;&amp; git cat-file -e fork/master:packages/paths-node/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `packages/paths/package.json` and `packages/paths-node/package.json` are absent on the current tree (orphan state confirmed)
    - `packages/paths/src` exists (orphan src/ also confirmed)
    - `fork/master:packages/paths/package.json` and `fork/master:packages/paths-node/package.json` both exist (restore source confirmed)
  </acceptance_criteria>
  <done>R1 orphan situation reproduced and the restore source on `fork/master` is confirmed.</done>
</task>

<task type="auto">
  <name>Task 4: Restore packages/paths{,-node} package.json + build configs from fork/master (R1 mitigation, first commit)</name>
  <files>
    - packages/paths/package.json
    - packages/paths/tsconfig.json
    - packages/paths/tsdown.config.ts
    - packages/paths-node/package.json
    - packages/paths-node/tsconfig.json
    - packages/paths-node/tsconfig.build.json
    - packages/paths-node/vitest.config.ts
  </files>
  <read_first>
    - .planning/phases/31-config-bucket/31-RESEARCH.md (§R1 — Recommended handling: restore from fork/master as the first commit on v8.1/config-bucket; v8.0 Phase 25 already proved this restoration is the right move)
  </read_first>
  <action>
    Per R1 recommendation: restore the orphan `packages/paths{,-node}` package.json + build configs from `fork/master` as the first commit on `v8.1/config-bucket`. This collapses v8.0 Phase 25 Wave 1 into Phase 31 and ensures `pnpm install` in 31-07 will succeed.

    Steps:

    1. For each file path in the survival list below, confirm it exists on `fork/master` (Task 3 already confirmed package.json — verify the rest before checkout):
       ```
       for f in \
         packages/paths/package.json \
         packages/paths/tsconfig.json \
         packages/paths/tsdown.config.ts \
         packages/paths-node/package.json \
         packages/paths-node/tsconfig.json \
         packages/paths-node/tsconfig.build.json \
         packages/paths-node/vitest.config.ts; do
         git cat-file -e "fork/master:$f" || echo "MISSING: $f"
       done
       ```
       Expected: no `MISSING:` lines printed. If any file is missing on `fork/master`, drop it from the restore list and note the omission in the SUMMARY (the `tsdown.config.ts` / `tsconfig.build.json` / `vitest.config.ts` files are nice-to-haves; only the two `package.json` files are strictly required for `pnpm install` to succeed).

    2. Restore the files:
       ```
       git checkout fork/master -- \
         packages/paths/package.json \
         packages/paths/tsconfig.json \
         packages/paths/tsdown.config.ts \
         packages/paths-node/package.json \
         packages/paths-node/tsconfig.json \
         packages/paths-node/tsconfig.build.json \
         packages/paths-node/vitest.config.ts
       ```
       (If any of those files is missing on `fork/master` per step 1, omit it from the checkout command.)

    3. Verify the package.json files now exist and parse:
       ```
       node -e "JSON.parse(require('fs').readFileSync('packages/paths/package.json','utf8'))"
       node -e "JSON.parse(require('fs').readFileSync('packages/paths-node/package.json','utf8'))"
       ```
       Both must exit 0.

    4. Stage and commit. Use the project's SSH-signed commit convention (do NOT pass `--no-gpg-sign`; the repo signs SSH per CLAUDE.md MEMORY notes):
       ```
       git add packages/paths/package.json packages/paths/tsconfig.json packages/paths/tsdown.config.ts \
               packages/paths-node/package.json packages/paths-node/tsconfig.json packages/paths-node/tsconfig.build.json packages/paths-node/vitest.config.ts
       git commit -m "restore(workspaces): packages/paths{,-node} package.json + build configs (v8.0 Phase 25 carry-forward)" -m "$(cat <<'EOF'

v2.0.1 deleted packages/paths/package.json and packages/paths-node/package.json
upstream-side, leaving orphan src/ and README.md on the merge tree. pnpm-workspace.yaml
globs ./packages/\* with failIfNoMatch: true — pnpm install would fail in 31-07 without
restoration.

v8.0 handled this in Phase 25 Wave 1 ("restore dropped scaffolding"); v8.1 collapses
that work into Phase 31 per RESEARCH.md §R1.

Restored from fork/master @ b241b56c5 (v8.0 baseline tip).

Files restored:

- packages/paths/{package.json,tsconfig.json,tsdown.config.ts}
- packages/paths-node/{package.json,tsconfig.json,tsconfig.build.json,vitest.config.ts}
  EOF
  )"
  ```

        5. Verify the commit landed:
           ```
           git log -1 --pretty=format:'%s'
           ```
           Expected: `restore(workspaces): packages/paths{,-node} package.json + build configs (v8.0 Phase 25 carry-forward)`.

      </action>
      <verify>
        <automated>test -f packages/paths/package.json &amp;&amp; test -f packages/paths-node/package.json &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('packages/paths/package.json','utf8'))" &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('packages/paths-node/package.json','utf8'))" &amp;&amp; git log -1 --pretty=format:'%s' | grep -q '^restore(workspaces): packages/paths{,-node} package.json'</automated>
      </verify>
      <acceptance_criteria>
        - `packages/paths/package.json` and `packages/paths-node/package.json` both exist and parse as valid JSON
        - The restore commit is on top of `v8.1/config-bucket` with title `restore(workspaces): packages/paths{,-node} package.json + build configs (v8.0 Phase 25 carry-forward)`
        - Commit body documents the rationale (v2.0.1 upstream deletion + pnpm-workspace.yaml glob mismatch)
        - Working tree clean: `git status --porcelain` returns empty (or only conflict-marker-bearing files which are still pending resolution by 31-02..31-06)
      </acceptance_criteria>
      <done>R1 mitigation applied as the first commit on v8.1/config-bucket. pnpm install in 31-07 has the package.json files it needs to succeed.</done>
    </task>

<task type="auto">
  <name>Task 5: Surface R2/R3 deferred items in 31-01-SUMMARY.md</name>
  <files>.planning/phases/31-config-bucket/31-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/31-config-bucket/31-RESEARCH.md (§R2 — Reintroduced Jest scaffolding under src/renderer/src/__mocks__/; §R3 — src/main/electron-builder.config.json is new in v2.0.1)
  </read_first>
  <action>
    Record the Phase 31 baseline state and the two deferred risks (R2, R3) so downstream phases inherit visibility.

    Write `.planning/phases/31-config-bucket/31-01-SUMMARY.md` with:

    1. **Branch SHA**: `git rev-parse v8.1/config-bucket` and the upstream tip SHA (`git rev-parse fork/sync/upstream-v2.0.1`) at branch time.

    2. **Confirmed conflict files (9)**: list the 9 paths from Task 2, exact match against RESEARCH.md §Conflict File Inventory.

    3. **R1 mitigation applied**: SHA of the restore commit + restored file list.

    4. **R2 deferred to Phase 34**: note that v2.0.1 reintroduced `src/renderer/src/__mocks__/` (10+ files including collection.json, sdv_collection.json, state.json, ComponentEx.js, electron.js etc.) — banned by VORTEX-LINUX-MERGE-PLAYBOOK.md §11. Phase 34 (renderer + main spine) handles cleanup. Recommended commit when handled: `chore(playbook): remove reintroduced src/renderer/src/__mocks__/ scaffolding (§11 v2.0.1 carry-forward)`.

    5. **R3 deferred to Phase 35**: note that v2.0.1 added `src/main/electron-builder.config.json` (no conflict marker, pure addition) referencing `out/`+`dist/` paths — fork uses `electron-builder.config.cjs`. Phase 35 build verification confirms `pnpm dist` invokes the `.cjs` not the `.json`; if `.json` is genuinely orphan, defer deletion to a future cleanup milestone.

    6. **R4 informational**: Dockerfile conflict marker labels say `>>>>>>> v1.16.9` (not `v2.0.1`) — implies stale conflict carried forward by PR #5's auto-merge. No resolution-stance impact (still keep HEAD per D-31-09); 31-02's commit body should mention this for forensics.

    7. **R5 mitigation plan**: husky pre-commit may fail if oxfmt encounters unresolved YAML. Plan 31-06 (pnpm-workspace.yaml) sequencing or `--no-verify` documentation handles this; same hazard as v8.0 24-05.

    Use the standard SUMMARY.md template (see `@/home/alex/.claude/get-shit-done/templates/summary.md`).

    Note: `.planning/` is gitignored (per CLAUDE.md MEMORY `feedback_planning_gitignored.md`); use `git add -f` if committing this file. Phase 31 plans intentionally do not commit SUMMARYs — they're written for the orchestrator's consumption only.

  </action>
  <verify>
    <automated>test -f .planning/phases/31-config-bucket/31-01-SUMMARY.md &amp;&amp; grep -q 'R1' .planning/phases/31-config-bucket/31-01-SUMMARY.md &amp;&amp; grep -q 'R2' .planning/phases/31-config-bucket/31-01-SUMMARY.md &amp;&amp; grep -q 'R3' .planning/phases/31-config-bucket/31-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/31-config-bucket/31-01-SUMMARY.md` exists
    - Contains branch SHA, upstream tip SHA, restore commit SHA, the 9 confirmed conflict file paths
    - Records R2 deferred to Phase 34 and R3 deferred to Phase 35 with concrete pickup instructions
    - Records R4 (Dockerfile marker label) and R5 (husky/oxfmt) as informational notes
  </acceptance_criteria>
  <done>Baseline state and deferred risks captured in SUMMARY for downstream phase handoff.</done>
</task>

</tasks>

<verification>
- `git rev-parse --abbrev-ref HEAD` is `v8.1/config-bucket`
- 9 expected hand-resolved Bucket A files contain conflict markers (matches RESEARCH.md table)
- `.vscode/extensions.json` and `src/renderer/tsconfig.api.json` have NO conflict markers (RESEARCH-confirmed clean)
- One restore commit on the branch with title `restore(workspaces): packages/paths{,-node} package.json + build configs (v8.0 Phase 25 carry-forward)`
- `packages/paths/package.json` + `packages/paths-node/package.json` exist and parse
- 31-01-SUMMARY.md records R1 (applied), R2 (Phase 34), R3 (Phase 35), R4 (informational), R5 (mitigation plan)
</verification>

<success_criteria>
Phase 31 working branch ready for hand-resolution work. R1 risk closed. R2/R3 deferred with explicit pickup instructions for Phase 34 / Phase 35. RESEARCH.md per-file stances (D-31-06..D-31-15) match the actual conflict surface on this branch.
</success_criteria>

<output>
Create `.planning/phases/31-config-bucket/31-01-SUMMARY.md` recording: branch SHA + upstream tip SHA, the 9 confirmed conflict files, the R1 restore commit SHA, R2 deferred to Phase 34, R3 deferred to Phase 35, R4 informational note about Dockerfile marker label, R5 husky/oxfmt mitigation plan for 31-06.
</output>
