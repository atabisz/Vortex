---
phase: 26-mod-management-hot-zone
plan: 03
type: execute
wave: 2
depends_on: [26-02]
files_modified:
    - src/renderer/src/extensions/mod_management/eventHandlers.ts
autonomous: true
requirements: [SYNC-04]

must_haves:
    truths:
        - "All conflict markers in eventHandlers.ts are resolved"
        - "Single atomic commit on v8.0/config-bucket with title `resolve(mod-mgmt): eventHandlers.ts — <stance>`"
        - "scripts/grep-checkpoint.sh --skip-conflict-check still exits zero (no playbook gate touched here)"
        - "pnpm typecheck -F @vortex/renderer passes after the commit (D-26-04 cadence)"
    artifacts:
        - path: "src/renderer/src/extensions/mod_management/eventHandlers.ts"
          provides: "Event handler registrations for mod_management — second leaf in D-26-01 order"
          contains: "eventHandler"
    key_links:
        - from: "eventHandlers.ts"
          to: "Plans 04–09"
          via: "Leaf-first ordering — surrounding utilities settle before InstallManager.ts"
          pattern: "import"
---

<objective>
Resolve all conflict markers in `src/renderer/src/extensions/mod_management/eventHandlers.ts` per D-26-01 leaf-first order. Land a single atomic commit on `v8.0/config-bucket` with title `resolve(mod-mgmt): eventHandlers.ts — <stance>` per D-26-00, run typecheck per D-26-04, and run the grep-checkpoint harness per D-26-03.

Purpose: Event handler registration file. Does not touch playbook §6/§7a–d/140a57217. Resolution is routine — picks up where ModList.tsx finished in plan 02.

Output: One commit on `v8.0/config-bucket` whose only working-tree change is `eventHandlers.ts`, with all conflict markers gone.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-mod-management-hot-zone/26-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve eventHandlers.ts conflicts and commit</name>
  <read_first>
    - src/renderer/src/extensions/mod_management/eventHandlers.ts (current state — read in full so the executor sees every conflict region, not assumptions)
    - .planning/phases/26-mod-management-hot-zone/26-CONTEXT.md (D-26-00 commit format, D-26-01 leaf-first rationale, D-26-04 typecheck cadence, "Claude's discretion" block on per-region stance)
    - .planning/phases/24-config-bucket/24-CONTEXT.md (D-05 hand-resolve default; fork-side wins for Linux fixes; upstream wins for new feature scaffolding that doesn't touch playbook items)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md — confirm `eventHandlers.ts` is NOT named in §6, §7a–d, or the 140a57217 entry (it is not — but read so the executor verifies)
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Open `src/renderer/src/extensions/mod_management/eventHandlers.ts` and hand-resolve every conflict region. Default stance per CONTEXT.md "Claude's discretion": hand-merge keeping fork-side Linux fixes where present, upstream wins for new feature scaffolding that does NOT touch playbook items. eventHandlers does not host any playbook invariant, so per-region calls are routine.

    For each region, run `git log --oneline -5 -- src/renderer/src/extensions/mod_management/eventHandlers.ts` to see recent fork-side touches before deciding. Pay attention to event-name strings and reducer wiring — fork-side may register Linux-only events (e.g. polkit elevation events from Phase 11/12) that upstream lacks.

    Stage only the file. Commit with title `resolve(mod-mgmt): eventHandlers.ts — <stance>` where `<stance>` is a one-line per-region summary. Body lists each conflict region and which side won.

    Run `pnpm typecheck -F @vortex/renderer` per D-26-04. Run `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` per D-26-03. Both must exit zero.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/eventHandlers.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qF 'resolve(mod-mgmt): eventHandlers.ts —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check &amp;&amp; pnpm typecheck -F @vortex/renderer</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' src/renderer/src/extensions/mod_management/eventHandlers.ts` returns empty
    - HEAD commit title starts with `resolve(mod-mgmt): eventHandlers.ts —`
    - HEAD commit touches exactly one file
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
    - `pnpm typecheck -F @vortex/renderer` exits zero
    - Working tree clean except the staged-and-committed change
  </acceptance_criteria>
  <done>
    File 2 of 8 resolved.
  </done>
</task>

</tasks>

<verification>
- `git grep '^<<<<<<< ' src/renderer/src/extensions/mod_management/eventHandlers.ts` empty
- `pnpm typecheck -F @vortex/renderer` passes
- `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
</verification>

<success_criteria>

- File 2 of 8 resolved with D-26-00 title format and D-26-04 typecheck cadence
  </success_criteria>

<output>
After completion, create `.planning/phases/26-mod-management-hot-zone/26-03-SUMMARY.md`
</output>
