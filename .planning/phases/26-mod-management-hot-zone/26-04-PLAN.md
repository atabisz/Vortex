---
phase: 26-mod-management-hot-zone
plan: 04
type: execute
wave: 2
depends_on: [26-03]
files_modified:
    - src/renderer/src/extensions/mod_management/util/deploy.ts
autonomous: true
requirements: [SYNC-04]

must_haves:
    truths:
        - "All conflict markers in util/deploy.ts are resolved"
        - "Single atomic commit on v8.0/config-bucket with title `resolve(mod-mgmt): util/deploy.ts — <stance>`"
        - "scripts/grep-checkpoint.sh --skip-conflict-check exits zero (no playbook gate touched)"
        - "pnpm typecheck -F @vortex/renderer passes after the commit (D-26-04)"
    artifacts:
        - path: "src/renderer/src/extensions/mod_management/util/deploy.ts"
          provides: "Deploy utility functions — third leaf in D-26-01 order"
          contains: "deploy"
    key_links:
        - from: "util/deploy.ts"
          to: "Plans 05–09"
          via: "Leaf-first ordering — surrounding utilities settle before InstallManager.ts"
          pattern: "import"
---

<objective>
Resolve all conflict markers in `src/renderer/src/extensions/mod_management/util/deploy.ts` per D-26-01. Land a single atomic commit, run typecheck, run the checkpoint harness.

Purpose: Deploy utility helpers — third in leaf-first order. Does NOT host playbook §6/§7a–d/140a57217 (those live in InstallManager.ts and LinkingDeployment.ts/externalChanges.ts respectively). Watch for any imports of `resolvePathCase` — if they exist they belong to a different code path than the 140a57217 fix; preserve them.

Output: One commit on `v8.0/config-bucket` whose only working-tree change is `util/deploy.ts`, all conflict markers gone.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-mod-management-hot-zone/26-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve util/deploy.ts conflicts and commit</name>
  <read_first>
    - src/renderer/src/extensions/mod_management/util/deploy.ts (current state, full read)
    - .planning/phases/26-mod-management-hot-zone/26-CONTEXT.md (D-26-00, D-26-01, D-26-04, "Claude's discretion")
    - .planning/phases/24-config-bucket/24-CONTEXT.md (D-05 hand-resolve default)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md — confirm `util/deploy.ts` is NOT named as a playbook gate (it is not — but verify)
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Open the file and hand-resolve every conflict region. Default stance: fork-side wins for Linux fixes, upstream for cosmetic refactors.

    Watch for: any reference to `resolvePathCase`. If present, it is NOT the 140a57217 invariant (that lives in LinkingDeployment.ts + externalChanges.ts) — it would be an unrelated case-resolution call. Preserve fork-side calls; preserve upstream calls if they appear new.

    Stage only `util/deploy.ts`. Commit with title `resolve(mod-mgmt): util/deploy.ts — <stance>` where `<stance>` summarises per-region calls.

    Run `pnpm typecheck -F @vortex/renderer` (D-26-04). Run `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` (D-26-03). Both must exit zero. Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/util/deploy.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qF 'resolve(mod-mgmt): util/deploy.ts —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check &amp;&amp; pnpm typecheck -F @vortex/renderer</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' src/renderer/src/extensions/mod_management/util/deploy.ts` returns empty
    - HEAD commit title starts with `resolve(mod-mgmt): util/deploy.ts —`
    - HEAD commit touches exactly one file
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
    - `pnpm typecheck -F @vortex/renderer` exits zero
  </acceptance_criteria>
  <done>
    File 3 of 8 resolved.
  </done>
</task>

</tasks>

<verification>
- `git grep '^<<<<<<< ' src/renderer/src/extensions/mod_management/util/deploy.ts` empty
- `pnpm typecheck -F @vortex/renderer` passes
- `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
</verification>

<success_criteria>

- File 3 of 8 resolved
  </success_criteria>

<output>
After completion, create `.planning/phases/26-mod-management-hot-zone/26-04-SUMMARY.md`
</output>
