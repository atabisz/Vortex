---
phase: 26-mod-management-hot-zone
plan: 05
type: execute
wave: 2
depends_on: [26-04]
files_modified:
    - src/renderer/src/extensions/mod_management/stagingDirectory.ts
autonomous: true
requirements: [SYNC-04]

must_haves:
    truths:
        - "All conflict markers in stagingDirectory.ts are resolved"
        - "Single atomic commit on v8.0/config-bucket with title `resolve(mod-mgmt): stagingDirectory.ts — <stance>`"
        - "scripts/grep-checkpoint.sh --skip-conflict-check exits zero (no playbook gate touched here — §6 stagingDirHasFiles lives in InstallManager.ts, not stagingDirectory.ts)"
        - "pnpm typecheck -F @vortex/renderer passes after the commit (D-26-04)"
    artifacts:
        - path: "src/renderer/src/extensions/mod_management/stagingDirectory.ts"
          provides: "Staging directory selection and validation utilities — fourth leaf in D-26-01 order"
          contains: "staging"
    key_links:
        - from: "stagingDirectory.ts"
          to: "Plans 06–09"
          via: "Leaf-first ordering — utilities settle before LinkingDeployment / InstallManager are opened"
          pattern: "import"
---

<objective>
Resolve all conflict markers in `src/renderer/src/extensions/mod_management/stagingDirectory.ts` per D-26-01. Land a single atomic commit, run typecheck, run the checkpoint harness.

Purpose: Staging directory selection / validation. NOT the §6 staging-integrity helper — §6's `stagingDirHasFiles` lives in `util/stagingIntegrity.ts` (sibling, not edited this phase) and is invoked from InstallManager.ts. Resolution here is routine.

Output: One commit on `v8.0/config-bucket` whose only working-tree change is `stagingDirectory.ts`.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-mod-management-hot-zone/26-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve stagingDirectory.ts conflicts and commit</name>
  <read_first>
    - src/renderer/src/extensions/mod_management/stagingDirectory.ts (current state, full read)
    - .planning/phases/26-mod-management-hot-zone/26-CONTEXT.md (D-26-00, D-26-01, D-26-04, "Claude's discretion")
    - .planning/phases/24-config-bucket/24-CONTEXT.md (D-05 hand-resolve default)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md §6 — confirms `stagingDirHasFiles` lives in InstallManager.ts, not here. Read this section so the executor can distinguish the §6 invariant (handled in plan 08) from any incidental staging-related logic that happens to live in stagingDirectory.ts.
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Open the file and hand-resolve every conflict region. Default stance per CONTEXT.md "Claude's discretion": fork-side wins for Linux fixes (look for case-folding, polkit, or Linux-only path-shape helpers — Phase 14 / Phase 19 may have touched this), upstream wins for new feature scaffolding.

    Specifically check: if any region references `stagingDirHasFiles` or imports from `util/stagingIntegrity.ts`, KEEP the fork-side reference — it would be a §6 invariant entry-point. Most conflicts here will be unrelated to §6 (the actual call site is in InstallManager.ts) but verify each region.

    Stage only the file. Commit with title `resolve(mod-mgmt): stagingDirectory.ts — <stance>`.

    Run `pnpm typecheck -F @vortex/renderer` (D-26-04). Run `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` (D-26-03). Both must exit zero. Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/stagingDirectory.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qF 'resolve(mod-mgmt): stagingDirectory.ts —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check &amp;&amp; pnpm typecheck -F @vortex/renderer</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' src/renderer/src/extensions/mod_management/stagingDirectory.ts` returns empty
    - HEAD commit title starts with `resolve(mod-mgmt): stagingDirectory.ts —`
    - HEAD commit touches exactly one file
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
    - `pnpm typecheck -F @vortex/renderer` exits zero
  </acceptance_criteria>
  <done>
    File 4 of 8 resolved.
  </done>
</task>

</tasks>

<verification>
- `git grep '^<<<<<<< ' src/renderer/src/extensions/mod_management/stagingDirectory.ts` empty
- `pnpm typecheck -F @vortex/renderer` passes
- `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
</verification>

<success_criteria>

- File 4 of 8 resolved
  </success_criteria>

<output>
After completion, create `.planning/phases/26-mod-management-hot-zone/26-05-SUMMARY.md`
</output>
