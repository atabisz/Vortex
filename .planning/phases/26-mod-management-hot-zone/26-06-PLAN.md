---
phase: 26-mod-management-hot-zone
plan: 06
type: execute
wave: 2
depends_on: [26-05]
files_modified:
    - src/renderer/src/extensions/mod_management/util/externalChanges.ts
autonomous: true
requirements: [SYNC-04]

must_haves:
    truths:
        - "All conflict markers in util/externalChanges.ts are resolved"
        - "Single atomic commit on v8.0/config-bucket with title `resolve(mod-mgmt): util/externalChanges.ts — <stance>`"
        - "scripts/grep-checkpoint.sh --skip-conflict-check exits zero (this file does not host any playbook gate; passing here proves nothing adjacent broke)"
        - "pnpm typecheck -F @vortex/renderer passes after the commit (D-26-04)"
    artifacts:
        - path: "src/renderer/src/extensions/mod_management/util/externalChanges.ts"
          provides: "UI-side external-changes scanner — fifth in D-26-01 leaf-first order. Per D-26-03a this file is unrelated to commit 140a57217 (which lives entirely in LinkingDeployment.ts) — it appears in the bucket only because it has ordinary merge conflicts to resolve."
          contains: "externalChanges"
    key_links:
        - from: "util/externalChanges.ts"
          to: "Plan 07 (LinkingDeployment.ts)"
          via: "Leaf-first ordering — utilities settle before the playbook-heavy file is opened"
          pattern: "import"
---

<objective>
Resolve all conflict markers in `src/renderer/src/extensions/mod_management/util/externalChanges.ts` per D-26-01 leaf-first order (this is the fifth file in the order). Land a single atomic commit on `v8.0/config-bucket` with title `resolve(mod-mgmt): util/externalChanges.ts — <stance>` per D-26-00, run typecheck per D-26-04, and run the grep-checkpoint harness per D-26-03.

Purpose: This is the UI-side external-changes scanner. **Per D-26-03a it is NOT one of the two files mentioned in the playbook's `140a57217` entry — that entry names a _method_ (`externalChanges()`) inside `LinkingDeployment.ts`, not a separate file.** A planning defect originally framed this plan as "hosts the 140a57217 fix half"; that framing was wrong. This file contains zero `resolvePathCase` calls and does not host any playbook §6/§7a–d/140a57217 invariant. Resolution is straightforward — same shape as plans 02–05.

Output: One commit on `v8.0/config-bucket` whose only working-tree change is `util/externalChanges.ts`, with all conflict markers gone.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-mod-management-hot-zone/26-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve util/externalChanges.ts conflicts and commit</name>
  <read_first>
    - src/renderer/src/extensions/mod_management/util/externalChanges.ts (current state — read in full so the executor sees every conflict region, not assumptions)
    - .planning/phases/26-mod-management-hot-zone/26-CONTEXT.md (D-26-00 commit format, D-26-01 leaf-first order rationale, D-26-03a file/method distinction — confirms this file is unrelated to commit 140a57217, D-26-04 typecheck cadence, "Claude's discretion" block on per-region stance)
    - .planning/phases/24-config-bucket/24-CONTEXT.md (D-05 hand-resolve default; fork-side wins for Linux fixes; upstream wins for new feature scaffolding that doesn't touch playbook items)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md externalChanges/140a57217 entry (line 360) — read to confirm the entry describes a method inside LinkingDeployment.ts, NOT this file. Confirm by `git grep -n 'resolvePathCase' src/renderer/src/extensions/mod_management/util/externalChanges.ts` returning zero hits before starting work — sanity check that this file is indeed unrelated.
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Sanity-check: `git grep -n 'resolvePathCase' src/renderer/src/extensions/mod_management/util/externalChanges.ts` MUST return zero hits — if it does not, D-26-03a's assertion about this file is wrong and the executor must STOP and surface the discrepancy.

    Open `src/renderer/src/extensions/mod_management/util/externalChanges.ts` and hand-resolve every `<<<<<<<`/`=======`/`>>>>>>>` region. Default stance: hand-merge keeping fork-side Linux fixes where present, upstream-side wins for new feature scaffolding that does NOT touch any playbook item. The file does NOT host any playbook §6/§7a–d/140a57217 invariant, so per-region calls here are routine merge calls — pick whichever side preserves correctness, prefer upstream for purely-cosmetic React updates and keep fork-side for any imports that target Linux-port modules (`@vortex/paths`, anything in `util/` that has a fork-only sibling).

    For each region, before deciding: run `git log --oneline -5 src/renderer/src/extensions/mod_management/util/externalChanges.ts` to see recent history on both sides, then `git diff HEAD <upstream-merge-parent> -- <file>` if unsure. The atomic-commit + hand-resolve discipline is the same as plans 02–05.

    After all markers are resolved, run `git diff src/renderer/src/extensions/mod_management/util/externalChanges.ts` and confirm no remaining conflict markers. Stage only this file. Commit with title `resolve(mod-mgmt): util/externalChanges.ts — <stance>` where `<stance>` is a one-line summary of the per-region decisions. Body should list each conflict region resolved and which side won.

    Run `pnpm typecheck -F @vortex/renderer` per D-26-04. Failure blocks proceeding — fix typecheck errors in a follow-up amend (single commit per D-26-00) before running the checkpoint script.

    Run `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check`. All six non-conflict gates must still pass (this file does not touch any of those gates — passing here proves nothing was broken adjacent).

    Do NOT push. D-26-00 specifies one push at phase end via plan 10.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/util/externalChanges.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qF 'resolve(mod-mgmt): util/externalChanges.ts —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check &amp;&amp; pnpm typecheck -F @vortex/renderer</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' src/renderer/src/extensions/mod_management/util/externalChanges.ts` returns empty
    - HEAD commit title starts with `resolve(mod-mgmt): util/externalChanges.ts —` per D-26-00
    - HEAD commit touches exactly one file: `src/renderer/src/extensions/mod_management/util/externalChanges.ts`
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
    - `pnpm typecheck -F @vortex/renderer` exits zero
    - No push has happened
  </acceptance_criteria>
  <done>
    Single atomic commit lands; util/externalChanges.ts has no conflict markers; typecheck and checkpoint harness both pass. File 5 of 8 resolved.
  </done>
</task>

</tasks>

<verification>
- `git grep '^<<<<<<< ' src/renderer/src/extensions/mod_management/util/externalChanges.ts` empty
- `pnpm typecheck -F @vortex/renderer` passes
- `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
</verification>

<success_criteria>

- File 5 of 8 resolved
- Commit on `v8.0/config-bucket` matches D-26-00 title format
- Typecheck cadence (D-26-04) honoured
  </success_criteria>

<output>
After completion, create `.planning/phases/26-mod-management-hot-zone/26-06-SUMMARY.md`
</output>
