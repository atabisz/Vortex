---
phase: 26-mod-management-hot-zone
plan: 06
type: execute
wave: 2
depends_on: [26-05]
files_modified:
    - src/renderer/src/extensions/mod_management/externalChanges.ts
    - .planning/phases/26-mod-management-hot-zone/scripts/140a57217-pre.txt
autonomous: true
requirements: [SYNC-22, SYNC-23, SYNC-27]

must_haves:
    truths:
        - "Pre-resolution grep snapshot of `resolvePathCase` hits in externalChanges.ts + LinkingDeployment.ts is captured at scripts/140a57217-pre.txt BEFORE any conflict-region edit"
        - "All conflict markers in externalChanges.ts are resolved"
        - "`git grep -nE 'resolvePathCase\\(dataPath, relDataPath, dirCache\\)' src/renderer/src/extensions/mod_management/externalChanges.ts` returns ≥1 hit AFTER the commit (140a57217 fix preserved with exact arg shape)"
        - "Post-resolution grep diff shows the externalChanges.ts hit count did not decrease vs the pre-snapshot"
        - "Single atomic commit on v8.0/config-bucket with title `resolve(mod-mgmt): externalChanges.ts — <stance>` whose body contains the pre/post grep diff"
        - "scripts/grep-checkpoint.sh --skip-conflict-check exits zero (140a57217 externalChanges gate is now actively asserted)"
        - "pnpm typecheck -F @vortex/renderer passes after the commit (D-26-04)"
    artifacts:
        - path: "src/renderer/src/extensions/mod_management/externalChanges.ts"
          provides: "External-changes scanner — fifth in D-26-01 order; hosts the 140a57217 fix half (the other half is in LinkingDeployment.ts)"
          contains: "resolvePathCase(dataPath, relDataPath, dirCache)"
        - path: ".planning/phases/26-mod-management-hot-zone/scripts/140a57217-pre.txt"
          provides: "Pre-resolution grep snapshot for D-26-02 verification — kept per `Claude's discretion` block (provenance is cheap)"
          contains: "resolvePathCase"
    key_links:
        - from: "externalChanges.ts"
          to: "LinkingDeployment.ts (plan 07)"
          via: "Same 140a57217 fix; plan 07 verifies the LinkingDeployment.ts half against the same pre-snapshot"
          pattern: "resolvePathCase(dataPath, relDataPath, dirCache)"
        - from: "scripts/140a57217-pre.txt"
          to: "Plan 07 acceptance"
          via: "Plan 07 reuses the same pre-snapshot for grep-pre/post diff verification on LinkingDeployment.ts"
          pattern: "resolvePathCase"
---

<objective>
Resolve `src/renderer/src/extensions/mod_management/externalChanges.ts` per D-26-01 order, applying the D-26-02 grep-pre + hand-read + grep-post discipline so the `140a57217` `resolvePathCase(dataPath, relDataPath, dirCache)` invariant is preserved with the exact arg shape (detects upstream renames like `relDataPath` → `relativePath`).

Purpose: This is one of two files that host the 140a57217 fix (the LinkingDeployment.ts half is plan 07). Without this fix, `lstat` hits ENOENT on every manifest entry on Linux, fires "External Changes" on every deploy, and the redundancy check sees zero deployed files and pops "Some mods are redundant" for every SKSE-plugin mod. The grep-pre/post diff in the commit body is the durable verification — D-26-02 explicitly requires it in the body.

Output: One commit on `v8.0/config-bucket` whose only working-tree change is `externalChanges.ts`, plus a separate-but-co-located pre-snapshot file `.planning/phases/26-mod-management-hot-zone/scripts/140a57217-pre.txt` (committed in plan 07 alongside its commit, OR as part of this commit — either works; suggested: include in THIS commit so the pre-snapshot is durable from the moment the externalChanges fix is locked in).
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-mod-management-hot-zone/26-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture 140a57217 pre-resolution grep snapshot</name>
  <read_first>
    - .planning/phases/26-mod-management-hot-zone/26-CONTEXT.md (D-26-02 — exact `git grep -n 'resolvePathCase' …` command and the requirement that the snapshot live at `scripts/140a57217-pre.txt`; "Claude's discretion" — keep the snapshot)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md externalChanges/140a57217 entry (line 360) — explains why the arg shape `(dataPath, relDataPath, dirCache)` matters and what breaks without it
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Run the D-26-02 pre-resolution grep verbatim:

    `git grep -n 'resolvePathCase' src/renderer/src/extensions/mod_management/LinkingDeployment.ts src/renderer/src/extensions/mod_management/externalChanges.ts > .planning/phases/26-mod-management-hot-zone/scripts/140a57217-pre.txt`

    The file captures line numbers AND argument shape. Both files MUST appear in the snapshot — if `resolvePathCase` is absent from one or both, the merge has already eaten the fix and the assumption that this phase is "resolution only, not re-application" is wrong; STOP and surface to the user.

    Inspect the snapshot manually: each hit must have the form `resolvePathCase(dataPath, relDataPath, dirCache)`. If the arg shape on any hit shows upstream renames (e.g. `relDataPath` → `relativePath`), record that finding — Task 2 must rename them back to the fork-canonical form during conflict resolution to keep the grep-checkpoint passing (`scripts/grep-checkpoint.sh` regex tests for `resolvePathCase\(dataPath, relDataPath, dirCache\)` exactly).

    Do NOT commit the snapshot yet (Task 3 commits both file + snapshot together, OR Task 3 commits only the file and Task 3-tail of plan 07 commits the snapshot — see plan 07 Task 1 read_first). Decision per "Claude's discretion": include the snapshot in Task 3's commit here. Document it in the commit body.

  </action>
  <verify>
    <automated>test -s .planning/phases/26-mod-management-hot-zone/scripts/140a57217-pre.txt &amp;&amp; grep -q 'externalChanges.ts.*resolvePathCase' .planning/phases/26-mod-management-hot-zone/scripts/140a57217-pre.txt &amp;&amp; grep -q 'LinkingDeployment.ts.*resolvePathCase' .planning/phases/26-mod-management-hot-zone/scripts/140a57217-pre.txt</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/26-mod-management-hot-zone/scripts/140a57217-pre.txt` exists, non-empty
    - Snapshot contains ≥1 line referencing `externalChanges.ts` AND ≥1 line referencing `LinkingDeployment.ts`
    - Each `resolvePathCase` hit recorded with line number and surrounding arg shape (the `git grep -n` output format)
    - Any upstream rename of `relDataPath` (e.g. to `relativePath`) is noted for Task 2's hand-read
  </acceptance_criteria>
  <done>
    Pre-snapshot captured at the canonical path; both files have ≥1 `resolvePathCase` hit; arg-shape drift (if any) is identified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Hand-resolve externalChanges.ts with grep-post verification</name>
  <read_first>
    - src/renderer/src/extensions/mod_management/externalChanges.ts (current state, full read — every conflict region)
    - .planning/phases/26-mod-management-hot-zone/scripts/140a57217-pre.txt (just-captured pre-snapshot — drives Task 2's hand-read attention to the exact line ranges where `resolvePathCase` appears)
    - .planning/phases/26-mod-management-hot-zone/26-CONTEXT.md (D-26-02 grep-pre/read/grep-post discipline; D-26-00 commit format)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md externalChanges/140a57217 entry (line 360) — the failure mode without the fix
  </read_first>
  <action>
    Open `src/renderer/src/extensions/mod_management/externalChanges.ts`. For every conflict region:
    - If the region is at or near a line number that the pre-snapshot recorded as a `resolvePathCase` call site, the "ours" (fork) side MUST keep the call AND the arg shape `(dataPath, relDataPath, dirCache)`. If upstream renamed any of those identifiers (e.g. `relDataPath` → `relativePath`), keep the fork name `relDataPath` so `scripts/grep-checkpoint.sh` regex still hits.
    - For all other regions, default stance per CONTEXT.md "Claude's discretion": fork-side wins for Linux fixes, upstream wins for new feature scaffolding.

    After resolution, run the D-26-02 post-grep:

    `git grep -n 'resolvePathCase' src/renderer/src/extensions/mod_management/externalChanges.ts`

    The hit count for externalChanges.ts MUST match-or-exceed the pre-snapshot count for that file. The arg shape on every hit must be `(dataPath, relDataPath, dirCache)` exactly. Diff the post-grep against the externalChanges.ts portion of the pre-snapshot — record the diff (it's the body of the commit per D-26-02 step 4).

    Stage `externalChanges.ts` AND `.planning/phases/26-mod-management-hot-zone/scripts/140a57217-pre.txt`. Commit with title `resolve(mod-mgmt): externalChanges.ts — <stance>` per D-26-00. Body MUST include:
    - Per-region resolution summary
    - Pre-grep externalChanges.ts hit count
    - Post-grep externalChanges.ts hit count
    - Diff (or "no change") between them
    - Confirmation that the `(dataPath, relDataPath, dirCache)` arg shape is intact

    Run `pnpm typecheck -F @vortex/renderer` per D-26-04. Run `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` per D-26-03. Note: gate 7 (140a57217 externalChanges.ts) is now actively asserted — if this commit broke it, the checkpoint exits non-zero with a clear failure label. Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/externalChanges.ts; test $? -ne 0 &amp;&amp; git grep -nE 'resolvePathCase\(dataPath, relDataPath, dirCache\)' src/renderer/src/extensions/mod_management/externalChanges.ts &amp;&amp; git log -1 --format=%s | grep -qF 'resolve(mod-mgmt): externalChanges.ts —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check &amp;&amp; pnpm typecheck -F @vortex/renderer</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' src/renderer/src/extensions/mod_management/externalChanges.ts` returns empty
    - `git grep -nE 'resolvePathCase\(dataPath, relDataPath, dirCache\)' src/renderer/src/extensions/mod_management/externalChanges.ts` returns ≥1 hit (matches the pre-snapshot count for that file)
    - HEAD commit title starts with `resolve(mod-mgmt): externalChanges.ts —`
    - HEAD commit body contains the pre/post grep diff per D-26-02 step 4
    - HEAD commit touches exactly two paths: `src/renderer/src/extensions/mod_management/externalChanges.ts` and `.planning/phases/26-mod-management-hot-zone/scripts/140a57217-pre.txt`
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero — gate 7 (140a57217 externalChanges.ts) passes
    - `pnpm typecheck -F @vortex/renderer` exits zero
    - No upstream rename leaked through: every hit shows `(dataPath, relDataPath, dirCache)` exactly
  </acceptance_criteria>
  <done>
    File 5 of 8 resolved with 140a57217 fix half preserved and verified via pre/post grep diff.
  </done>
</task>

</tasks>

<verification>
- `git grep '^<<<<<<< ' src/renderer/src/extensions/mod_management/externalChanges.ts` empty
- 140a57217 externalChanges.ts gate (gate 7 in checkpoint script) passes
- `pnpm typecheck -F @vortex/renderer` passes
- Commit body cites pre/post grep diff
</verification>

<success_criteria>

- 140a57217 fix half preserved with exact arg shape `(dataPath, relDataPath, dirCache)` intact
- Pre-snapshot durably captured for plan 07's reuse
- File 5 of 8 resolved
  </success_criteria>

<output>
After completion, create `.planning/phases/26-mod-management-hot-zone/26-06-SUMMARY.md`
</output>
