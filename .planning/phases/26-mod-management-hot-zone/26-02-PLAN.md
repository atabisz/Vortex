---
phase: 26-mod-management-hot-zone
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
    - src/renderer/src/extensions/mod_management/views/ModList.tsx
autonomous: true
requirements: [SYNC-04]

must_haves:
    truths:
        - "All conflict markers in views/ModList.tsx are resolved"
        - "Single atomic commit on v8.0/config-bucket with title `resolve(mod-mgmt): views/ModList.tsx — <stance>`"
        - "scripts/grep-checkpoint.sh exits zero (without --skip-conflict-check) is NOT yet expected here — the other 7 files still have markers; the script is run with --skip-conflict-check, all seven non-conflict gates still pass"
        - "pnpm typecheck -F @vortex/renderer passes after the commit (D-26-04 cadence)"
    artifacts:
        - path: "src/renderer/src/extensions/mod_management/views/ModList.tsx"
          provides: "Mod list React view — first leaf in D-26-01 leaf-first order"
          contains: "ModList"
    key_links:
        - from: "views/ModList.tsx"
          to: "Plans 03–09"
          via: "Leaf-first ordering — utilities settle before InstallManager.ts is opened in plan 08"
          pattern: "import"
---

<objective>
Resolve all conflict markers in `src/renderer/src/extensions/mod_management/views/ModList.tsx` per D-26-01 leaf-first order (this is the first file in the order). Land a single atomic commit on `v8.0/config-bucket` with title `resolve(mod-mgmt): views/ModList.tsx — <stance>` per D-26-00, run typecheck per D-26-04, and run the grep-checkpoint harness per D-26-03.

Purpose: ModList is a pure React view. It does not touch playbook items §6, §7a–d, or 140a57217. Resolution is straightforward — the value of going first is that any import-shape regressions surface here before they propagate into the heavier files downstream.

Output: One commit on `v8.0/config-bucket` whose only working-tree change is `views/ModList.tsx`, with all conflict markers gone.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-mod-management-hot-zone/26-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve views/ModList.tsx conflicts and commit</name>
  <read_first>
    - src/renderer/src/extensions/mod_management/views/ModList.tsx (current state — read in full so the executor sees every conflict region, not assumptions)
    - .planning/phases/26-mod-management-hot-zone/26-CONTEXT.md (D-26-00 commit format, D-26-01 leaf-first order rationale, D-26-04 typecheck cadence, "Claude's discretion" block on per-region stance)
    - .planning/phases/24-config-bucket/24-CONTEXT.md (D-05 hand-resolve default; fork-side wins for Linux fixes; upstream wins for new feature scaffolding that doesn't touch playbook items)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md — confirm `views/ModList.tsx` is NOT named in §6, §7a–d, or the 140a57217 entry (it is not — but read so the executor verifies, not assumes)
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Open `src/renderer/src/extensions/mod_management/views/ModList.tsx` and hand-resolve every `<<<<<<<`/`=======`/`>>>>>>>` region. Default stance: hand-merge keeping fork-side Linux fixes where present, upstream-side wins for new feature scaffolding that does NOT touch any playbook item. Note the file does NOT host any playbook §6/§7a–d/140a57217 invariant, so per-region calls here are routine merge calls — pick whichever side preserves correctness, prefer upstream for purely-cosmetic React updates and keep fork-side for any imports that target Linux-port modules (`@vortex/paths`, anything in `util/` that has a fork-only sibling).

    For each region, before deciding: run `git log --oneline -5 src/renderer/src/extensions/mod_management/views/ModList.tsx` to see recent history on both sides, then `git diff HEAD <upstream-merge-parent> -- <file>` if unsure. The atomic-commit + hand-resolve discipline is the same as Phase 24/25.

    After all markers are resolved, run `git diff src/renderer/src/extensions/mod_management/views/ModList.tsx` and confirm no remaining conflict markers. Stage only this file. Commit with title `resolve(mod-mgmt): views/ModList.tsx — <stance>` where `<stance>` is a one-line summary of the per-region decisions (e.g. `upstream React 16.12 type tightening + fork-side Linux import preserved`). Body should list each conflict region resolved and which side won.

    Run `pnpm typecheck -F @vortex/renderer` per D-26-04. Failure blocks proceeding — fix typecheck errors in a follow-up amend (single commit per D-26-00) before running the checkpoint script.

    Run `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check`. All seven non-conflict gates must still pass (this file does not touch any of those gates — passing here proves nothing was broken adjacent).

    Do NOT push. D-26-00 specifies one push at phase end via plan 10.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/views/ModList.tsx; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qF 'resolve(mod-mgmt): views/ModList.tsx —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check &amp;&amp; pnpm typecheck -F @vortex/renderer</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' src/renderer/src/extensions/mod_management/views/ModList.tsx` returns empty (exit code 1, file has no marker lines)
    - HEAD commit title starts with `resolve(mod-mgmt): views/ModList.tsx —` per D-26-00
    - HEAD commit touches exactly one file: `src/renderer/src/extensions/mod_management/views/ModList.tsx`
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
    - `pnpm typecheck -F @vortex/renderer` exits zero
    - No push has happened (`git rev-list --count origin/sync/upstream-v2.0.0..v8.0/config-bucket` is at least 2 — commit 0 from plan 01 + this plan's commit, more once later plans land)
  </acceptance_criteria>
  <done>
    Single atomic commit lands; ModList.tsx has no conflict markers; typecheck and checkpoint harness both pass.
  </done>
</task>

</tasks>

<verification>
- `git grep '^<<<<<<< ' src/renderer/src/extensions/mod_management/views/ModList.tsx` empty
- `pnpm typecheck -F @vortex/renderer` passes
- `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
</verification>

<success_criteria>

- File 1 of 8 resolved
- Commit on `v8.0/config-bucket` matches D-26-00 title format
- Typecheck cadence (D-26-04) honoured
  </success_criteria>

<output>
After completion, create `.planning/phases/26-mod-management-hot-zone/26-02-SUMMARY.md`
</output>
