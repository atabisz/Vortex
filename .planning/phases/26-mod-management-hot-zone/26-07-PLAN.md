---
phase: 26-mod-management-hot-zone
plan: 07
type: execute
wave: 2
depends_on: [26-06]
files_modified:
    - src/renderer/src/extensions/mod_management/LinkingDeployment.ts
autonomous: true
requirements: [SYNC-22, SYNC-23, SYNC-27]

must_haves:
    truths:
        - "All conflict markers in LinkingDeployment.ts are resolved"
        - "`git grep -nE 'resolvePathCase\\(dataPath,' src/renderer/src/extensions/mod_management/LinkingDeployment.ts` returns ≥3 hits AFTER the commit (locks all three base-commit call sites at :523, :742, :799 — the `(dataPath, …)` shape that 140a57217 produced)"
        - "Single atomic commit on v8.0/config-bucket with title `resolve(mod-mgmt): LinkingDeployment.ts — <stance>` whose body confirms the three call sites are intact and notes any per-region calls that affected them"
        - "scripts/grep-checkpoint.sh --skip-conflict-check exits zero — gate 6 (140a57217 in LinkingDeployment.ts) actively asserts the ≥3 hit threshold"
        - "pnpm typecheck -F @vortex/renderer passes after the commit (D-26-04)"
    artifacts:
        - path: "src/renderer/src/extensions/mod_management/LinkingDeployment.ts"
          provides: "Hardlink/symlink deployment engine — sixth in D-26-01 order. Per D-26-03a this is the SOLE host of commit 140a57217 — the playbook entry 'externalChanges' refers to the `externalChanges()` *method* defined at LinkingDeployment.ts:513, not a separate file."
          contains: "resolvePathCase(dataPath,"
    key_links:
        - from: "LinkingDeployment.ts:523"
          to: "scripts/grep-checkpoint.sh gate 6"
          via: "First of three resolvePathCase(dataPath, …) call sites — inside externalChanges() method, the one 140a57217 added"
          pattern: "resolvePathCase(dataPath, relDataPath, dirCache)"
        - from: "LinkingDeployment.ts:742, :799"
          to: "scripts/grep-checkpoint.sh gate 6"
          via: "Two further resolvePathCase(dataPath, …) call sites in deploy/finalise flow — predate 140a57217 but share the same arg-shape invariant the gate locks"
          pattern: "resolvePathCase(dataPath, relOutputPath, this.mReaddirCache)"
---

<objective>
Resolve `src/renderer/src/extensions/mod_management/LinkingDeployment.ts` per D-26-01 order. This file is the **sole host** of commit `140a57217` (per D-26-03a) — the fix lives inside the `externalChanges()` method at LinkingDeployment.ts:513 (added by 140a57217). Hand-read every conflict region in this file using the D-26-02 read discipline; gate 6 in `scripts/grep-checkpoint.sh` provides the durable run-time verification (≥3 hits of `resolvePathCase\(dataPath,` in this file).

Purpose: Without the fix, hardlink deployment hits ENOENT on every manifest entry on Linux when staging-folder casing differs from on-disk casing — `lstat` fails, everything gets flagged `destDeleted`, the "External Changes" dialog pops on every deploy, and the redundancy check fires "Some mods are redundant" on every SKSE-plugin mod. The three call sites at :523 (`relDataPath, dirCache`), :742, and :799 (`relOutputPath, this.mReaddirCache`) all carry the `(dataPath, …)` arg shape; the harness regex is prefix-anchored so it locks all three at once.

Output: One commit on `v8.0/config-bucket` whose only working-tree change is `LinkingDeployment.ts`, with all conflict markers gone, all three `resolvePathCase(dataPath, …)` call sites preserved, and a commit body that summarises the per-region resolution decisions and confirms the call-site count post-resolution.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-mod-management-hot-zone/26-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hand-resolve LinkingDeployment.ts with grep-post verification (gate 6 in checkpoint harness)</name>
  <read_first>
    - src/renderer/src/extensions/mod_management/LinkingDeployment.ts (current state, full read — every conflict region; particular attention to the `externalChanges()` method around line 513 and the deploy/finalise paths around lines 742, 799 where the three `resolvePathCase(dataPath, …)` call sites live)
    - .planning/phases/26-mod-management-hot-zone/26-CONTEXT.md (D-26-02 hand-read discipline; D-26-03 gate 6 spec; D-26-03a file/method distinction — confirms LinkingDeployment.ts is the sole 140a57217 host; D-26-00 commit format)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md externalChanges/140a57217 entry (line 360) — failure mode without the fix; confirms "externalChanges" names the method, not a file
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Run a baseline grep BEFORE touching the file: `git grep -nE 'resolvePathCase\(dataPath,' src/renderer/src/extensions/mod_management/LinkingDeployment.ts` MUST return exactly 3 hits at lines :523, :742, :799 on the base commit. If the count is anything other than 3, STOP and surface — the assumption that this phase is "resolution only, not re-application" is wrong.

    Open `src/renderer/src/extensions/mod_management/LinkingDeployment.ts`. Note the three call-site line numbers from the baseline grep; treat any conflict region that overlaps or sits adjacent to those lines as critical.

    For every conflict region:
    - If the region overlaps a `resolvePathCase(dataPath, …)` call site, the "ours" (fork) side MUST keep the call AND the `(dataPath, …)` arg shape. Detect upstream renames (`relDataPath` → `relativePath`, `dirCache` → `cache`, etc.) — keep the fork name so the harness regex still hits.
    - For all other regions, default stance: fork-side wins for Linux fixes (case-folding, polkit, Linux-only path helpers), upstream wins for new feature scaffolding.

    After resolution, run the post-grep:

    `git grep -nE 'resolvePathCase\(dataPath,' src/renderer/src/extensions/mod_management/LinkingDeployment.ts`

    Hit count MUST be ≥3 (matches the baseline). Each hit's arg shape must start with `(dataPath,` and read as one of the two known shapes — `(dataPath, relDataPath, dirCache)` at :523-ish or `(dataPath, relOutputPath, this.mReaddirCache)` at :742-ish/:799-ish (line numbers may shift after conflict resolution; the call sites are what matter, not absolute line numbers). Record the post-grep output for the commit body.

    Stage only `LinkingDeployment.ts`. Commit with title `resolve(mod-mgmt): LinkingDeployment.ts — <stance>` per D-26-00. Body MUST include:
    - Per-region resolution summary
    - Pre-resolution `resolvePathCase(dataPath,` hit count (3 from baseline)
    - Post-resolution `resolvePathCase(dataPath,` hit count (≥3)
    - Confirmation that the `(dataPath, …)` arg shape is intact at every hit
    - Note that gate 6 of `scripts/grep-checkpoint.sh` is the durable verification (per D-26-02 + D-26-03)

    Run `pnpm typecheck -F @vortex/renderer` per D-26-04. Run `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` per D-26-03. Gate 6 (140a57217 in LinkingDeployment.ts, ≥3 hits) is now actively asserted — if this commit broke it, the checkpoint exits non-zero with a clear failure label. Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/LinkingDeployment.ts; test $? -ne 0 &amp;&amp; test $(git grep -cE 'resolvePathCase\(dataPath,' src/renderer/src/extensions/mod_management/LinkingDeployment.ts) -ge 3 &amp;&amp; git log -1 --format=%s | grep -qF 'resolve(mod-mgmt): LinkingDeployment.ts —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check &amp;&amp; pnpm typecheck -F @vortex/renderer</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' src/renderer/src/extensions/mod_management/LinkingDeployment.ts` returns empty
    - `git grep -nE 'resolvePathCase\(dataPath,' src/renderer/src/extensions/mod_management/LinkingDeployment.ts` returns ≥3 hits and matches-or-exceeds the pre-resolution baseline (3)
    - HEAD commit title starts with `resolve(mod-mgmt): LinkingDeployment.ts —`
    - HEAD commit body contains the pre/post grep counts and confirms the `(dataPath, …)` arg shape per D-26-02
    - HEAD commit touches exactly one file: `src/renderer/src/extensions/mod_management/LinkingDeployment.ts`
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero — gate 6 (140a57217 LinkingDeployment.ts ≥3 hits) passes
    - `pnpm typecheck -F @vortex/renderer` exits zero
    - No upstream rename leaked through: every hit reads `resolvePathCase(dataPath, …)` — the prefix is intact
  </acceptance_criteria>
  <done>
    File 6 of 8 resolved. The full 140a57217 invariant (all three `resolvePathCase(dataPath, …)` call sites in LinkingDeployment.ts) is preserved and actively asserted by the checkpoint harness.
  </done>
</task>

</tasks>

<verification>
- `git grep '^<<<<<<< ' src/renderer/src/extensions/mod_management/LinkingDeployment.ts` empty
- 140a57217 LinkingDeployment.ts gate (gate 6 in checkpoint script) passes — ≥3 hits
- `pnpm typecheck -F @vortex/renderer` passes
- Commit body confirms pre/post hit counts and arg-shape integrity
</verification>

<success_criteria>

- 140a57217 invariant preserved with all three `resolvePathCase(dataPath, …)` call sites intact
- Gate 6 in the checkpoint harness actively asserts the ≥3 hit threshold from this commit forward
- File 6 of 8 resolved
  </success_criteria>

<output>
After completion, create `.planning/phases/26-mod-management-hot-zone/26-07-SUMMARY.md`
</output>
