---
phase: 26-mod-management-hot-zone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
    - .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh
autonomous: true
requirements: [SYNC-22, SYNC-23]

must_haves:
    truths:
        - "Executable shell script `.planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh` exists and exits zero on the current tree (pre-resolution baseline; conflict-marker check excluded from baseline run because conflicts still exist)"
        - "Script encodes playbook §6, §7a, §7b, §7c, §7d, 140a57217 (LinkingDeployment.ts + externalChanges.ts), and the conflict-marker assertion as separate exit-non-zero gates"
        - "Script is committed as commit 0 on `v8.0/config-bucket` with title `resolve(mod-mgmt): scripts/grep-checkpoint.sh — encode playbook §6 + §7a–d + 140a57217 re-grep harness`"
    artifacts:
        - path: ".planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh"
          provides: "Per-file checkpoint harness — re-runs after every mod-mgmt commit in plans 02–09 and once more in plan 10's done-gate"
          contains: "stagingDirHasFiles"
    key_links:
        - from: "scripts/grep-checkpoint.sh"
          to: "Plans 02–09 acceptance criteria"
          via: "Each per-file resolution plan invokes the script after its commit lands"
          pattern: "scripts/grep-checkpoint.sh"
        - from: "scripts/grep-checkpoint.sh"
          to: "Plan 10 done-gate"
          via: "Done-gate runs full script (including conflict-marker assertion) as item #2 of D-26-05"
          pattern: "grep-checkpoint.sh"
---

<objective>
Create `.planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh` per D-26-03. The script encodes the playbook §6 + §7a–d + 140a57217 re-grep checks plus a no-conflict-markers assertion, with each gate exiting non-zero on failure and a clear failure label. Commit it as commit 0 (before any file resolution) so plans 02–09 can run it after each per-file commit.

Purpose: Mechanical reproducible verification. Without this harness, executors would re-derive the playbook grep commands per file by hand — fragile and slow. With it, each per-file plan runs one command. The script is durable: future sync milestones (v8.1, v9.0) reuse it.

Output: `.planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh` (executable, committed) — and a baseline run confirming all gates except the conflict-marker gate pass on the unmodified pre-resolution tree (because §6 + §7a–d + 140a57217 should already be intact on `v8.0/config-bucket` — they pre-date this merge).
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-mod-management-hot-zone/26-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
@.planning/phases/25-restore-dropped-scaffolding/25-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/grep-checkpoint.sh encoding playbook §6 + §7a–d + 140a57217 + conflict-marker gates</name>
  <read_first>
    - .planning/phases/26-mod-management-hot-zone/26-CONTEXT.md (D-26-03 — full script body specification, lists exact grep commands and expected hit counts)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md §6 (line 95 onward) — `stagingDirHasFiles` source-of-truth grep
    - VORTEX-LINUX-MERGE-PLAYBOOK.md §7a–d (lines 103–139) — four-fix backslash/case cluster: `normalizeBackslashPaths`, `mergeCaseConflictingDirs`, `replaceAll("\\","/")` on copy source AND destination, `resolvePathCase(tempPath, …)` in extractArchive
    - VORTEX-LINUX-MERGE-PLAYBOOK.md externalChanges/140a57217 entry (line 360) — confirms `resolvePathCase(dataPath, relDataPath, dirCache)` arg shape used in LinkingDeployment.ts + externalChanges.ts
    - .planning/phases/24-config-bucket/24-CONTEXT.md (atomic-commit + commit-title pattern carried into D-26-00)
  </read_first>
  <action>
    Create directory `.planning/phases/26-mod-management-hot-zone/scripts/` then author `grep-checkpoint.sh` as a `bash` script with `set -u` (NOT `set -e` — the script must run all gates and report all failures, not stop at the first). Set executable permission via `chmod +x`.

    Encode each gate as a labeled function or inline block. Each gate prints `OK: <label>` on pass and `FAIL: <label> (expected <N> hits, found <M>)` on fail, increments a `failures` counter, and continues. The script exits with `failures` as the exit code (zero = clean, non-zero = at least one gate failed).

    Required gates, in this order, with the exact grep commands from D-26-03:

    1. §6 stagingDirHasFiles — `git grep -nE '\bstagingDirHasFiles\b' src/renderer/src/extensions/mod_management/InstallManager.ts` must return ≥1 hit; AND `test -f src/renderer/src/extensions/mod_management/util/stagingIntegrity.ts`.
    2. §7a normalizeBackslashPaths — `git grep -nE '\bnormalizeBackslashPaths\b' src/renderer/src/extensions/mod_management/InstallManager.ts` must return ≥3 hits (import + 2 call sites).
    3. §7b mergeCaseConflictingDirs — `git grep -nE '\bmergeCaseConflictingDirs\b' src/renderer/src/extensions/mod_management/InstallManager.ts` must return ≥3 hits (import + 2 call sites).
    4. §7c copy-loop replaceAll — count hits of `replaceAll` in InstallManager.ts that match the backslash-to-slash pattern; D-26-03 specifies `git grep -n 'replaceAll' …InstallManager.ts | grep -E '\\\\\\\\.*"/"'` must return ≥2 hits (source + destination). Use the exact regex from D-26-03 (note the escaped backslash quartet — that is intentional).
    5. §7d resolvePathCase(tempPath…) — `git grep -nE 'resolvePathCase\(tempPath' src/renderer/src/extensions/mod_management/InstallManager.ts` must return ≥1 hit.
    6. 140a57217 LinkingDeployment.ts — `git grep -nE 'resolvePathCase\(dataPath, relDataPath, dirCache\)' src/renderer/src/extensions/mod_management/LinkingDeployment.ts` must return ≥1 hit.
    7. 140a57217 externalChanges.ts — same regex, file `src/renderer/src/extensions/mod_management/externalChanges.ts`, must return ≥1 hit.
    8. No conflict markers — `git grep -l '^<<<<<<< ' src/renderer/src/extensions/mod_management/` must return zero matching files. This gate is ALLOWED to fail in the pre-resolution baseline (conflicts are still there); plans 02–09 each tighten this gate as they land their commit.

    Add a `--skip-conflict-check` flag. When passed, gate 8 is skipped. Plan 01's baseline run uses `--skip-conflict-check` (because conflicts exist); plans 02–09 invoke the script WITHOUT the flag because their per-file commit is supposed to remove conflict markers from the file they touched.

    Header comment of the script: cite the four playbook sections by name (§6, §7a, §7b, §7c, §7d, 140a57217 entry) per D-26-03 "commit message body of the script's introductory commit cites the playbook section it corresponds to".

    After the script is written, run it once with `--skip-conflict-check`. Record the output. All seven non-conflict gates MUST pass on the unmodified `v8.0/config-bucket` tree because the playbook fixes pre-date the merge (they were applied long before v8.0 sync). If any gate fails on baseline, STOP — that means the merge has already eaten a fix and the assumption that this phase is "resolution only, not re-application" is wrong. Surface the failure to the user via summary text, do not proceed.

  </action>
  <verify>
    <automated>test -x .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh` and is executable (`test -x` passes)
    - Script header comment names all six playbook sections (§6, §7a, §7b, §7c, §7d, 140a57217) per D-26-03
    - Running the script with `--skip-conflict-check` exits zero on the pre-resolution `v8.0/config-bucket` tree
    - Running the script without `--skip-conflict-check` exits non-zero (because conflict markers still exist in the 8 mod-mgmt files at this point) AND the failure line names the conflict-marker gate explicitly
    - Script does NOT use `set -e` (must run all gates per gate-fail-aggregation requirement above)
    - `--skip-conflict-check` flag is honoured: with the flag, gate 8 is skipped; without it, gate 8 runs
  </acceptance_criteria>
  <done>
    Script authored, executable, baseline-clean (modulo expected conflict-marker failure), and ready for plans 02–09 to invoke after each per-file commit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit grep-checkpoint.sh as commit 0 on v8.0/config-bucket</name>
  <read_first>
    - .planning/phases/26-mod-management-hot-zone/26-CONTEXT.md (D-26-00 commit-title format; D-26-03 "commit message body of the script's introductory commit cites the playbook section it corresponds to")
    - .planning/phases/24-config-bucket/24-CONTEXT.md (D-02 push policy — push happens at phase end, not per commit)
  </read_first>
  <action>
    Confirm working tree is on branch `v8.0/config-bucket` (`git rev-parse --abbrev-ref HEAD`). Stage only `.planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh`. Commit with title `resolve(mod-mgmt): scripts/grep-checkpoint.sh — encode playbook §6 + §7a–d + 140a57217 re-grep harness` and a body that names each playbook section the script gates (§6 stagingDirHasFiles, §7a normalizeBackslashPaths, §7b mergeCaseConflictingDirs, §7c copy-loop replaceAll, §7d resolvePathCase(tempPath…), 140a57217 LinkingDeployment.ts + externalChanges.ts) and notes that the script is invoked by plans 02–09 after each per-file commit and again by plan 10's done-gate.

    Do NOT push. D-26-00 specifies one push at phase end via plan 10.

    Per CLAUDE.md memory note "SSH commit signing": commits are SSH-signed with `~/.ssh/id_ed25519`. Existing repo config handles this — no extra flags needed.

  </action>
  <verify>
    <automated>git log -1 --format=%s | grep -qF 'resolve(mod-mgmt): scripts/grep-checkpoint.sh — encode playbook §6 + §7a–d + 140a57217 re-grep harness' &amp;&amp; git log -1 --name-only --format= | grep -qF '.planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh'</automated>
  </verify>
  <acceptance_criteria>
    - HEAD commit title matches the D-26-00 format exactly: `resolve(mod-mgmt): scripts/grep-checkpoint.sh — encode playbook §6 + §7a–d + 140a57217 re-grep harness`
    - HEAD commit touches exactly one file: `.planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh`
    - Commit body cites all six playbook sections by name
    - Branch is still `v8.0/config-bucket` after the commit
    - No push happened (per D-26-00 — push is plan 10)
  </acceptance_criteria>
  <done>
    Commit 0 lands on `v8.0/config-bucket`. Plans 02–09 can now invoke `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh` after each per-file commit.
  </done>
</task>

</tasks>

<verification>
- `test -x .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh` passes
- `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero on the pre-resolution tree
- `git log -1 --format=%s` matches the D-26-00 title format
</verification>

<success_criteria>

- Harness exists, is executable, is committed
- All seven non-conflict-marker gates pass on baseline (proves the merge has not eaten any of the playbook fixes — this phase is resolution, not re-application)
- Plans 02–09 have a single command to invoke per-file
  </success_criteria>

<output>
After completion, create `.planning/phases/26-mod-management-hot-zone/26-01-SUMMARY.md`
</output>
