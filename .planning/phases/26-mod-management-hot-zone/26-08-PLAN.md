---
phase: 26-mod-management-hot-zone
plan: 08
type: execute
wave: 2
depends_on: [26-07]
files_modified:
    - src/renderer/src/extensions/mod_management/InstallManager.ts
autonomous: true
requirements: [SYNC-04, SYNC-22, SYNC-23]

must_haves:
    truths:
        - "All conflict markers in InstallManager.ts are resolved"
        - "§6 stagingDirHasFiles import + call site present in doDownload (gate 1 passes)"
        - "§7a normalizeBackslashPaths import + 2 call sites present (gate 2 passes — ≥3 hits)"
        - "§7b mergeCaseConflictingDirs import + 2 call sites present (gate 3 passes — ≥3 hits)"
        - "§7c replaceAll(\"\\\\\\\\\", \"/\") on copy source AND destination present in extractArchive (gate 4 passes — ≥2 hits)"
        - "§7d resolvePathCase(tempPath, …) in extractArchive copy loop present (gate 5 passes — ≥1 hit)"
        - "Single atomic commit on v8.0/config-bucket with title `resolve(mod-mgmt): InstallManager.ts — <stance>`"
        - "scripts/grep-checkpoint.sh --skip-conflict-check exits zero (FULL §6 + §7a–d coverage now asserted)"
        - "pnpm typecheck -F @vortex/renderer passes after the commit (D-26-04)"
    artifacts:
        - path: "src/renderer/src/extensions/mod_management/InstallManager.ts"
          provides: "Mod install pipeline — playbook hot zone, hosts §6 stagingDirHasFiles + §7a–d four-fix backslash/case cluster"
          contains: "stagingDirHasFiles"
    key_links:
        - from: "InstallManager.ts:doDownload"
          to: "util/stagingIntegrity.ts (sibling, not edited)"
          via: "§6 import + call — `stagingDirHasFiles` from `util/stagingIntegrity`"
          pattern: "stagingDirHasFiles"
        - from: "InstallManager.ts:extractArchive"
          to: "util/normalizeBackslashPaths.ts + util/mergeCaseConflictingDirs.ts (siblings, not edited)"
          via: "§7a + §7b call sites — both invoked before each `buildFileList(tempPath)`"
          pattern: "normalizeBackslashPaths|mergeCaseConflictingDirs"
        - from: "InstallManager.ts:extractArchive copy loop"
          to: "resolvePathCase + replaceAll calls"
          via: "§7c + §7d — copy `source`/`destination` get `replaceAll('\\\\', '/')` then `resolvePathCase(tempPath, source, caseCache)` replaces `path.join(tempPath, source)`"
          pattern: "resolvePathCase\\(tempPath"
---

<objective>
Resolve `src/renderer/src/extensions/mod_management/InstallManager.ts` per D-26-01 order. This is the playbook hot zone — §6 (`stagingDirHasFiles` in `doDownload`) and §7a–d (four-fix backslash/case cluster in `extractArchive`) all live in this file. Reverting any one of them produces the cryptic ENOENTs documented in the playbook (literal backslashed paths OR forward-slash paths whose casing differs from disk).

Purpose: Highest-stakes single file in the phase. The full grep-checkpoint harness (gates 1–5) becomes actively-asserted after this commit lands. If any gate fails, hand-fix and amend the commit until all five pass — D-26-00 still demands a single atomic commit per file.

Output: One commit on `v8.0/config-bucket` whose only working-tree change is `InstallManager.ts`, with all conflict markers gone, all five §6/§7a/§7b/§7c/§7d invariants intact and re-grep clean.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-mod-management-hot-zone/26-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-resolution baseline grep of all five §6 + §7a–d gates</name>
  <read_first>
    - src/renderer/src/extensions/mod_management/InstallManager.ts (full read; this is the largest file in the bucket — read first to map every conflict region against the §6 + §7a–d call sites)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md §6 (line 95) and §7a–d (lines 103–139) — exact grep commands and expected hit counts
    - .planning/phases/26-mod-management-hot-zone/26-CONTEXT.md (D-26-03 gate definitions; "Claude's discretion" stance default; D-26-04 typecheck cadence)
    - .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh (read so the executor sees the exact regex each gate uses — must produce output that matches those regexes)
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Run each playbook gate manually against InstallManager.ts to capture the pre-resolution baseline (expect all five to pass cleanly because the playbook fixes pre-date the merge — proves nothing was eaten):

    1. `git grep -nE '\bstagingDirHasFiles\b' src/renderer/src/extensions/mod_management/InstallManager.ts` (expect ≥1)
    2. `git grep -nE '\bnormalizeBackslashPaths\b' src/renderer/src/extensions/mod_management/InstallManager.ts` (expect ≥3)
    3. `git grep -nE '\bmergeCaseConflictingDirs\b' src/renderer/src/extensions/mod_management/InstallManager.ts` (expect ≥3)
    4. `git grep -n 'replaceAll' src/renderer/src/extensions/mod_management/InstallManager.ts | grep -E '\\\\\\\\.*"/"'` (expect ≥2)
    5. `git grep -nE 'resolvePathCase\(tempPath' src/renderer/src/extensions/mod_management/InstallManager.ts` (expect ≥1)

    Record line numbers for each hit. These line numbers map directly to the regions Task 2 must protect during conflict resolution. If any baseline fails, STOP and surface — this phase is "resolution only, not re-application" per CONTEXT.md framing; baseline failure means a prior commit ate a fix and the assumption is wrong.

    Do NOT modify InstallManager.ts in this task. No commit. The output of this task is the executor's mental map of "which conflict regions touch which playbook gate" before the hand-resolve in Task 2.

  </action>
  <verify>
    <automated>git grep -nE '\bstagingDirHasFiles\b' src/renderer/src/extensions/mod_management/InstallManager.ts &amp;&amp; git grep -nE '\bnormalizeBackslashPaths\b' src/renderer/src/extensions/mod_management/InstallManager.ts &amp;&amp; git grep -nE '\bmergeCaseConflictingDirs\b' src/renderer/src/extensions/mod_management/InstallManager.ts &amp;&amp; git grep -nE 'resolvePathCase\(tempPath' src/renderer/src/extensions/mod_management/InstallManager.ts</automated>
  </verify>
  <acceptance_criteria>
    - All five baseline greps return ≥ their playbook-specified minimum hit counts (≥1, ≥3, ≥3, ≥2, ≥1)
    - If any baseline fails, executor stops and surfaces a "playbook fix already eaten on this branch" report — does NOT proceed to Task 2
    - No working-tree change in this task
  </acceptance_criteria>
  <done>
    Five baseline gates pass; line-number map of which conflict regions touch which gate is in the executor's working memory.
  </done>
</task>

<task type="auto">
  <name>Task 2: Hand-resolve InstallManager.ts and run full grep-checkpoint</name>
  <read_first>
    - src/renderer/src/extensions/mod_management/InstallManager.ts (already read in Task 1 — re-scan only the conflict regions)
    - .planning/phases/26-mod-management-hot-zone/26-CONTEXT.md (D-26-00 commit format, "Claude's discretion" stance default, D-26-03 full checkpoint gate list, D-26-04 typecheck)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md §6 + §7a–d (already read in Task 1) — the four-fix cluster lesson on line 358 ("backslash normalisation is a paired invariant — disk layout AND instruction strings; reverting one and leaving the other produces cryptic failures downstream")
    - .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh (the harness this task must satisfy WITHOUT --skip-conflict-check at the end)
  </read_first>
  <action>
    For every conflict region in InstallManager.ts:

    A. **If the region is at or near a line number from Task 1's baseline (i.e. touches §6 or §7a/§7b/§7c/§7d):** the fork-side ("ours") MUST win for that region — playbook §6/§7a–d invariants are non-negotiable per D-26-CONTEXT framing. Pay specific attention:

       - §6 region: `stagingDirHasFiles` import (top of file) + call inside `doDownload`'s `.then(async (downloadId) => { ... })` block. Both must survive.
       - §7a region: `normalizeBackslashPaths` import + the two call sites BEFORE each `buildFileList(tempPath)` in `extractArchive`. All three must survive.
       - §7b region: `mergeCaseConflictingDirs` import + the two call sites IMMEDIATELY AFTER each `normalizeBackslashPaths(tempPath)` call. All three must survive.
       - §7c region: in the `extractArchive` copy loop, both `source` AND `destination` get `.replaceAll("\\", "/")` (or equivalent — the gate regex tolerates the escape style). BOTH replaceAll calls must survive — the four-fix cluster lesson on playbook line 358 is "reverting one and leaving the other produces cryptic failures downstream".
       - §7d region: in the `extractArchive` copy loop, `resolvePathCase(tempPath, source, caseCache)` REPLACES the older `path.join(tempPath, source)`. The fork-side replacement must win.

    B. **Otherwise (region does not touch any §6/§7a–d invariant):** default stance per CONTEXT.md "Claude's discretion": fork-side wins for Linux fixes; upstream wins for new feature scaffolding (e.g. v2.0.0's DownloadManager rewrite touches doDownload).

    C. After all markers resolved, run the full grep-checkpoint harness:

       `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check`

       (Still skipping the conflict-marker gate because index.ts in plan 09 still has markers.) Gates 1–5 (§6 + §7a–d) MUST all pass. Gate 6 (140a57217 LinkingDeployment.ts) and gate 7 (externalChanges.ts) must still pass because plans 06–07 already locked them in.

       If any gate fails, hand-fix the InstallManager.ts text and re-run UNTIL all gates pass. Do not proceed with a failing checkpoint.

    D. Stage only `InstallManager.ts`. Commit with title `resolve(mod-mgmt): InstallManager.ts — <stance>` per D-26-00. Body MUST include:
       - Per-region resolution summary
       - Confirmation that all five §6/§7a–d invariants are intact
       - Output of the post-resolution checkpoint run (or "all gates pass")

    E. Run `pnpm typecheck -F @vortex/renderer` per D-26-04. Failure blocks proceeding — amend the commit (still single atomic commit per D-26-00) until typecheck is clean. Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/InstallManager.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qF 'resolve(mod-mgmt): InstallManager.ts —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check &amp;&amp; pnpm typecheck -F @vortex/renderer</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' src/renderer/src/extensions/mod_management/InstallManager.ts` returns empty
    - HEAD commit title starts with `resolve(mod-mgmt): InstallManager.ts —`
    - HEAD commit touches exactly one file
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero — gates 1–7 all pass (gates 1–5 are the §6/§7a–d cluster newly asserted by THIS commit; gates 6–7 are the 140a57217 fix preserved by plans 06–07)
    - Each individual gate check passes when run by hand:
      - `git grep -nE '\bstagingDirHasFiles\b' src/renderer/src/extensions/mod_management/InstallManager.ts` ≥1 hit
      - `git grep -nE '\bnormalizeBackslashPaths\b' src/renderer/src/extensions/mod_management/InstallManager.ts` ≥3 hits
      - `git grep -nE '\bmergeCaseConflictingDirs\b' src/renderer/src/extensions/mod_management/InstallManager.ts` ≥3 hits
      - `git grep -n 'replaceAll' src/renderer/src/extensions/mod_management/InstallManager.ts | grep -E '\\\\\\\\.*"/"'` ≥2 hits
      - `git grep -nE 'resolvePathCase\(tempPath' src/renderer/src/extensions/mod_management/InstallManager.ts` ≥1 hit
    - `pnpm typecheck -F @vortex/renderer` exits zero
    - Commit body lists each §6/§7a–d invariant and confirms it is intact
  </acceptance_criteria>
  <done>
    File 7 of 8 resolved; the four-fix backslash/case cluster + staging-integrity guard are all actively asserted clean by the checkpoint harness.
  </done>
</task>

</tasks>

<verification>
- `git grep '^<<<<<<< ' src/renderer/src/extensions/mod_management/InstallManager.ts` empty
- All five §6/§7a–d gates pass via `scripts/grep-checkpoint.sh`
- `pnpm typecheck -F @vortex/renderer` passes
</verification>

<success_criteria>

- Playbook hot zone resolved with §6 + §7a–d all preserved
- Full checkpoint harness (gates 1–7) clean modulo the conflict-marker gate (still expected to fail because index.ts in plan 09 still has markers)
- File 7 of 8 resolved
  </success_criteria>

<output>
After completion, create `.planning/phases/26-mod-management-hot-zone/26-08-SUMMARY.md`
</output>
