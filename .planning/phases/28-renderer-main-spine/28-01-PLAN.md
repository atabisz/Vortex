---
phase: 28-renderer-main-spine
plan: 01
type: execute
wave: 2
depends_on: [28-00]
files_modified:
    - src/shared/src/errors.ts
    - src/shared/src/errors.test.ts
    - src/shared/src/telemetry/spans.ts
autonomous: true
requirements: [SYNC-08]

must_haves:
    truths:
        - "Three @vortex/shared conflict files resolved in leaf-first order per D-28-01: errors.ts → errors.test.ts → telemetry/spans.ts"
        - "Three atomic commits on `v8.0/config-bucket` each titled `resolve(shared): <file> — <stance>` per D-28-00"
        - "After the third commit, `pnpm typecheck -F @vortex/shared` exits zero (D-28-03 per-bucket cadence)"
        - "After EACH commit, `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero — the §2/§4/§8/§9 gates from 28-00 plus all Phase 26/27 gates stay green"
    artifacts:
        - path: "src/shared/src/errors.ts"
          provides: "Typed error classes used by main + renderer (UserCanceled, TimeoutError, etc.) — bottom of dependency tree per D-28-01"
          contains: "export class"
        - path: "src/shared/src/errors.test.ts"
          provides: "Vitest suite for errors.ts — depends on the just-resolved class shapes"
          contains: "describe"
        - path: "src/shared/src/telemetry/spans.ts"
          provides: "OpenTelemetry span helpers — leaf utility consumed by main + renderer telemetry"
          contains: "span"
    key_links:
        - from: "errors.ts"
          to: "errors.test.ts"
          via: "Test file imports class shapes — must follow source resolution"
          pattern: "from '../errors'"
        - from: "telemetry/spans.ts"
          to: "@vortex/main + @vortex/renderer"
          via: "Span helpers re-exported via shared barrel; main/renderer telemetry layers depend on this surface"
          pattern: "import .* from '@vortex/shared/.*spans'"
---

<objective>
Resolve the 3 conflict files in `src/shared/src/` per D-28-01 leaf-first sub-order: `errors.ts` → `errors.test.ts` → `telemetry/spans.ts`. Three atomic commits on `v8.0/config-bucket` matching D-28-00 title format `resolve(shared): <file> — <stance>`. Per-bucket typecheck after the third commit per D-28-03. Run grep-checkpoint script with `--skip-conflict-check` after each commit.

Purpose: First bucket in execution order per D-28-01 — `@vortex/shared` is the bottom of the dependency tree (typed errors + telemetry primitives consumed by main + renderer). Settles first so any merge-driver artefact patterns surface here before touching heavier buckets.

Output: Three commits on `v8.0/config-bucket`. After plan 28-01 lands, only 60 of the 63 Phase 28 conflict files remain (52 hand-resolved minus 3 = 49 hand-resolved + 11 fingerprints + 1 setup commit + 5 docs).
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-renderer-main-spine/28-CONTEXT.md
@.planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md
@.planning/phases/26-mod-management-hot-zone/26-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve errors.ts (leaf 1/3) and commit</name>
  <read_first>
    - src/shared/src/errors.ts (current state — read in full so executor sees every conflict region, not assumptions)
    - `git show f15bbabb8:src/shared/src/errors.ts` (base-commit reference for fork-side error class shapes)
    - .planning/phases/28-renderer-main-spine/28-CONTEXT.md (D-28-00 commit format; D-28-01 leaf-first; D-28-06 per-region stance defaults; "Integration Points" — errors.ts at bottom of dep tree)
    - .planning/phases/24-config-bucket/24-CONTEXT.md (D-05 hand-resolve default; fork-side wins for Linux fixes; upstream wins for new feature scaffolding that doesn't touch playbook items)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md — confirm `src/shared/src/errors.ts` is NOT named in §1, §2, §3, §4, §6, §7a–d, §8, §9, §10, BG3, or Morrowind gates (it is not — but read so executor verifies, not assumes)
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket` (`git rev-parse --abbrev-ref HEAD`). Open `src/shared/src/errors.ts` and hand-resolve every `<<<<<<<`/`=======`/`>>>>>>>` region. Default stance per D-28-06: hand-resolve, fork-side wins for Linux fixes, upstream wins for new feature scaffolding that doesn't touch playbook items. This file is typed error classes — no playbook items live here, so per-region calls are routine: upstream-side wins for new error classes, fork-side wins for any class that grew Linux-specific message text or platform-aware handling.

    Watch for the bluebird Promise trap (CLAUDE.md memory): if upstream introduces async functions annotated `:Promise<void>` in a file that imports bluebird Promise, do NOT take the upstream annotation verbatim — TS1064. Drop the explicit return type or use the bluebird Promise generic, whichever the surrounding fork-side style uses.

    For each region: run `git log --oneline -5 src/shared/src/errors.ts` to see recent history; if unsure, `git diff HEAD <upstream-merge-parent> -- <file>`.

    After all markers resolved, `grep -c '^<<<<<<< ' src/shared/src/errors.ts` must equal 0. Stage ONLY this file. Commit with title `resolve(shared): errors.ts — <stance>` where `<stance>` is a one-line summary in casual developer voice (per CLAUDE.md memory "casual project voice"). Body: list each conflict region resolved + which side won.

    Run `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check`. ALL fifteen non-conflict gates must still pass.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/shared/src/errors.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(shared\): errors\.ts —' &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' src/shared/src/errors.ts` returns empty (exit code 1)
    - HEAD commit title starts with `resolve(shared): errors.ts —`
    - HEAD commit touches exactly one file: `src/shared/src/errors.ts`
    - HEAD commit is SSH-signed
    - `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
  </acceptance_criteria>
  <done>
    First shared leaf resolved; one atomic commit lands; checkpoint stays green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolve errors.test.ts (leaf 2/3) and commit</name>
  <read_first>
    - src/shared/src/errors.test.ts (current state — every conflict region, in full)
    - src/shared/src/errors.ts (just-resolved final shape — test imports must align)
    - `git show f15bbabb8:src/shared/src/errors.test.ts` (base-commit reference)
  </read_first>
  <action>
    Open `src/shared/src/errors.test.ts` and hand-resolve every conflict region. Per D-28-01 this is the test file for the just-resolved errors.ts — re-imports must match the final class surface from Task 1's commit. Stance default: hand-resolve, upstream-side wins for new test cases, fork-side wins for any test that exercises Linux-specific message paths.

    After all markers resolved, stage ONLY this file. Commit with title `resolve(shared): errors.test.ts — <stance>`.

    Run `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check`. All fifteen non-conflict gates must pass.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/shared/src/errors.test.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(shared\): errors\.test\.ts —' &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' src/shared/src/errors.test.ts` returns empty
    - HEAD commit title starts with `resolve(shared): errors.test.ts —`
    - HEAD commit touches exactly one file
    - HEAD commit is SSH-signed
    - `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
  </acceptance_criteria>
  <done>
    Second shared leaf resolved; one atomic commit lands; checkpoint stays green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Resolve telemetry/spans.ts (leaf 3/3), commit, and run per-bucket typecheck</name>
  <read_first>
    - src/shared/src/telemetry/spans.ts (current state — every conflict region, in full)
    - `git show f15bbabb8:src/shared/src/telemetry/spans.ts` (base-commit reference)
    - .planning/phases/28-renderer-main-spine/28-CONTEXT.md (D-28-03 — per-bucket typecheck cadence; the @vortex/shared filter is `pnpm typecheck -F @vortex/shared`, confirmed via `grep '"name"' src/shared/package.json` returning `"@vortex/shared"`)
  </read_first>
  <action>
    Open `src/shared/src/telemetry/spans.ts` and hand-resolve every conflict region. OpenTelemetry span helpers — pure additive surface for new spans. Stance: upstream-side wins for new span types/names, fork-side wins for any span that already exists with Linux-specific attribute keys.

    Stage ONLY this file. Commit with title `resolve(shared): telemetry/spans.ts — <stance>`.

    Run `pnpm typecheck -F @vortex/shared` per D-28-03. Exit zero required. If typecheck fails, amend the spans.ts commit with the fix (`git commit --amend --no-edit` after staging the fix) before running the checkpoint script — single commit per file per D-28-00.

    Run `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check`. All fifteen non-conflict gates must pass.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/shared/src/telemetry/spans.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(shared\): telemetry/spans\.ts —' &amp;&amp; pnpm typecheck -F @vortex/shared &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' src/shared/src/telemetry/spans.ts` returns empty
    - `git grep -l '^<<<<<<< ' src/shared/` returns empty (entire shared bucket clean)
    - HEAD commit title starts with `resolve(shared): telemetry/spans.ts —`
    - HEAD commit touches exactly one file
    - HEAD commit is SSH-signed
    - `pnpm typecheck -F @vortex/shared` exits zero
    - `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
    - `git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\(shared\):'` returns 3
  </acceptance_criteria>
  <done>
    All three shared files resolved; per-bucket typecheck clean; checkpoint stays green; 3/52 hand-resolved files done.
  </done>
</task>

</tasks>

<verification>
- All three files conflict-clean
- Three atomic commits matching `resolve(shared): <file> — <stance>`
- `pnpm typecheck -F @vortex/shared` passes
- `scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
</verification>

<success_criteria>

- 3 of 52 hand-resolved Phase 28 files done
- @vortex/shared bucket typechecks clean
- Per-bucket typecheck cadence (D-28-03) honoured

</success_criteria>

<output>
After completion, create `.planning/phases/28-renderer-main-spine/28-01-SUMMARY.md`
</output>
