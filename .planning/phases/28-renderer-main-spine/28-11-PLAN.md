---
phase: 28-renderer-main-spine
plan: 11
type: execute
wave: 12
depends_on: [28-10]
files_modified: []
autonomous: false
requirements: [SYNC-07, SYNC-08, SYNC-09, SYNC-10, SYNC-18, SYNC-20, SYNC-24, SYNC-25, SYNC-26]

must_haves:
    truths:
        - "All eight D-28-05 done-gate items pass: zero conflict markers across the 7 Phase 28 directories, full grep-checkpoint.sh exits zero (16 gates including §2/§4/§8/§9), 4 per-bucket typechecks already-run pass, full repo `pnpm typecheck` exits zero, ~54 commit count check, fingerprints squash landed, force-with-lease push to fork/sync/upstream-v2.0.0 succeeds, no half-resolved files"
        - "All 9 phase requirement IDs (SYNC-07, SYNC-08, SYNC-09, SYNC-10, SYNC-18, SYNC-20, SYNC-24, SYNC-25, SYNC-26) verifiable as satisfied via the done-gate evidence"
        - "Branch v8.0/config-bucket is pushed to fork remote with --force-with-lease per D-28-00 — single push at phase end"
        - "NO new resolution commits created in this plan — verification + push only. ROADMAP/STATE updates land as separate `docs(...)` commits per the Phase 27 done-gate template."
    artifacts:
        - path: ".planning/phases/28-renderer-main-spine/28-DONE-GATE.md"
          provides: "Done-gate evidence record — captures the output of each of the 8 D-28-05 checks plus the push confirmation, for milestone audit trail"
          contains: "## D-28-05 Done Gate"
    key_links:
        - from: "28-DONE-GATE.md"
          to: "ROADMAP.md Phase 28 row"
          via: "Evidence that all 6 ROADMAP success criteria + the 2 added done-gate items (full typecheck + no half-resolved files) are met"
          pattern: "Phase 28"
        - from: "v8.0/config-bucket"
          to: "fork remote (atabisz/Vortex)"
          via: "--force-with-lease push to sync/upstream-v2.0.0"
          pattern: "fork/sync/upstream-v2.0.0"
---

<objective>
Run the full D-28-05 done-gate — eight checks — and push `v8.0/config-bucket` to the fork remote with `--force-with-lease` per D-28-00. Capture evidence in `.planning/phases/28-renderer-main-spine/28-DONE-GATE.md` for milestone audit trail. Update STATE.md and ROADMAP.md to mark Phase 28 complete.

Purpose: Single point where the phase declares done. Catches any regression that may have crept in between plan 28-10 and now (uncommitted edits, drift, missing files). Pushes ONLY after all eight checks pass. Same pattern as Phase 27 plan 27-08 (extended from six to eight items per D-28-05).

Output: Pushed branch on fork remote, evidence file `28-DONE-GATE.md`, ROADMAP.md Phase 28 row marked complete, STATE.md updated.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-renderer-main-spine/28-CONTEXT.md
@.planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All 8 D-28-05 done-gate checks have been executed and a force-with-lease push to `fork/sync/upstream-v2.0.0` has been performed. Evidence captured in `.planning/phases/28-renderer-main-spine/28-DONE-GATE.md`.

    The push step is gated `autonomous: false` for this plan because it mutates the fork remote — same policy as Phase 27 plan 27-08. Sandbox blocks `.git/config` per CLAUDE.md memory "Git push SSH URL", so the push uses the inline SSH URL: `git push --force-with-lease git@github.com:atabisz/Vortex.git v8.0/config-bucket:sync/upstream-v2.0.0`.

  </what-built>
  <how-to-verify>
    Confirm via `gh api repos/atabisz/Vortex/branches/sync/upstream-v2.0.0 --jq .commit.sha` that the new HEAD on the fork remote matches the local `v8.0/config-bucket` HEAD.

    Confirm `cat .planning/phases/28-renderer-main-spine/28-DONE-GATE.md | tail -3` shows `## Phase 28 status: COMPLETE` with a timestamp.

    Confirm `grep '\[x\] \*\*Phase 28' .planning/ROADMAP.md` returns the marked row.

  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 28 complete and unblock Phase 29 (build verification), or describe issues found.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Run all eight D-28-05 done-gate checks, write evidence file, update ROADMAP + STATE, then push</name>
  <read_first>
    - .planning/phases/28-renderer-main-spine/28-CONTEXT.md (D-28-05 — eight-item done-gate; D-28-00 — commit title format and force-with-lease push policy; D-28-03 — 4 per-bucket typecheck filters)
    - .planning/phases/27-gamebryo-per-game-extensions/27-08-PLAN.md (template for done-gate evidence file shape — extend from six items to eight)
    - .planning/phases/24-config-bucket/24-CONTEXT.md (D-02 push policy — force-with-lease to fork remote at phase end)
    - .planning/milestones/v8.0/scripts/grep-checkpoint.sh (extended harness — runs without flags here for the conflict-marker assertion against Phase 26/27 paths; Phase 28 paths verified separately by item #1)
    - CLAUDE.md memory note "Git push SSH URL" — sandbox blocks `.git/config`; push with inline SSH URL `git push git@github.com:atabisz/Vortex.git <branch>` if standard `fork` remote push fails
    - CLAUDE.md memory note "GitHub auth token" — `GH_TOKEN` env var available for atabisz when gh auth is expired
    - CLAUDE.md memory note "SSH commit signing" — all commits signed with SSH (`~/.ssh/id_ed25519`)
    - CLAUDE.md memory note ".planning/ is gitignored" — `git add -f` required for `.planning/phases/28-renderer-main-spine/28-DONE-GATE.md` and `.planning/ROADMAP.md` and `.planning/STATE.md`
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Run each D-28-05 check and capture output verbatim. Checks 1–7 must ALL pass before check 8 (the push). If any of 1–7 fails, STOP and surface to the user — no push happens.

    1. **Zero conflict markers across the 7 Phase 28 directories (D-28-05 #1, success criterion #1):**
       ```
       git grep '^<<<<<<< ' \
         src/renderer/ \
         src/main/ \
         src/preload/ \
         src/shared/ \
         extensions/nexus_integration/ \
         scripts/ \
         .github/actions/fingerprints/
       ```
       Expect non-zero exit (no matches).

       Note: `extensions/nexus_integration/` resolves to `src/renderer/src/extensions/nexus_integration/` in this codebase — adjust the path on the grep above if needed (the rendered path is the actual filesystem layout). Also `git grep` on the 7-directory list catches anything in those subtrees.

    2. **scripts/grep-checkpoint.sh exits zero — FULL run, NO `--skip-conflict-check` (D-28-05 #2):**
       ```
       bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh
       ```
       Capture full stdout/stderr. All 16 gates must pass: §6, §7a, §7b, §7c, §7d, 140a57217, §1, §3, §10, BG3 4-class, Morrowind migrate103, §2, §4 (negative), §8 (helpers + hide-on-spawn), §9 (findAllLinuxSteamPaths + libraryfolders.vdf), conflict-marker. The conflict-marker gate's path list is the Phase 26/27 directories; Phase 28's broader paths were verified by check 1.

    3. **Per-bucket typechecks per D-28-05 #3** (already-run during plans 28-01/02/03/07, but re-run here as defense-in-depth):
       ```
       pnpm typecheck -F @vortex/shared
       pnpm typecheck -F @vortex/preload
       pnpm typecheck -F @vortex/main
       pnpm typecheck -F @vortex/renderer
       ```
       All four must exit zero. Capture exit codes per bucket.

    4. **Phase-end full-repo `pnpm typecheck` (D-28-05 #4):**
       ```
       pnpm typecheck
       ```
       Capture stdout tail + exit code. Must exit zero. This is the cross-bucket drift check — catches anything per-bucket typechecks miss (e.g. scripts/* type drift, extension-package type drift relative to spine).

    5. **~54 atomic commits matching the title formats (D-28-05 #5):**
       ```
       git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | head -80
       ```
       Filter and count:
       ```
       git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 \
         | grep -cE '^[0-9a-f]+ resolve\((shared|preload|main|renderer|nexus|scripts|docs)\):'
       ```
       Expected: 52 hand-resolved files = 52 commits IF Mode A in plan 28-10, OR 48 commits IF Mode B in plan 28-10 (5 doc files batched into 1 commit). Acceptable range: 48–52.

       ```
       git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 \
         | grep -cE '^[0-9a-f]+ resolve\(fingerprints\): take upstream wholesale'
       ```
       Must return 1 (the fingerprints squash from plan 28-09).

       ```
       git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 \
         | grep -cE '^[0-9a-f]+ chore\(28-00\): move grep-checkpoint\.sh to milestone-shared location'
       ```
       Must return 1 (the relocation/setup commit from plan 28-00).

       Total Phase 28 commits over `origin/sync/upstream-v2.0.0`: 50–54 (52 + 1 fingerprints + 1 setup = 54 max if Mode A; 48 + 1 + 1 = 50 min if Mode B). Document the actual count in the evidence file.

    6. **Fingerprints squash landed and matches upstream tree (D-28-05 #6):**
       ```
       git diff <upstream-merge-parent> -- .github/actions/fingerprints/
       ```
       Must produce no output (the upstream tree at fingerprints is now ours). The `<upstream-merge-parent>` is the commit referred to as `theirs` during the merge — confirm by `git log --merges -1 --format=%P v8.0/config-bucket` and pick the second parent (the upstream side).

    7. **No half-resolved files (D-28-05 #8):**
       ```
       git status --short
       ```
       Must show no modified-but-unstaged or unmerged files. Empty output means clean tree.

    8. **Force-with-lease push (D-28-05 #7):**
       Per CLAUDE.md memory "Git push SSH URL" — sandbox blocks `.git/config` modifications, so use the inline SSH URL. Capture remote SHA before:
       ```
       git ls-remote git@github.com:atabisz/Vortex.git refs/heads/sync/upstream-v2.0.0
       ```
       Push:
       ```
       git push --force-with-lease git@github.com:atabisz/Vortex.git v8.0/config-bucket:sync/upstream-v2.0.0
       ```
       Capture push output (the new remote SHA).

       If `--force-with-lease` rejects (lease check failure means remote has moved), STOP and surface to user — do NOT escalate to `--force` without user approval.

    Write `.planning/phases/28-renderer-main-spine/28-DONE-GATE.md` with sections:
    - `## D-28-05 Done Gate`
    - `### 1. Zero conflict markers across 7 Phase 28 directories` (echo of `git grep` exit code + the path list grepped)
    - `### 2. grep-checkpoint.sh full run` (full stdout/stderr — 16 OK lines expected)
    - `### 3. Per-bucket typechecks` (table: bucket name → exit code → command used; @vortex/shared, @vortex/preload, @vortex/main, @vortex/renderer)
    - `### 4. Phase-end full-repo pnpm typecheck` (stdout tail + exit code)
    - `### 5. ~54 atomic commits` (the filtered `git log --oneline` output; per-scope counts: shared 3, preload 1, main 7, renderer 24 [9 leaves + 14 extensions + 1 ExtensionManager — but renderer scope tag also covers nexus, see breakdown], nexus 6, scripts 2, docs 1–5; fingerprints 1; setup 1)
    - `### 6. Fingerprints squash matches upstream tree` (`git diff` output empty)
    - `### 7. No half-resolved files` (`git status --short` empty)
    - `### 8. Force-with-lease push` (remote SHA before + after, push command used)
    - `## Requirements satisfied` — table mapping all 9 SYNC-* IDs to D-28-05 evidence
    - `## Phase 28 status: COMPLETE` (timestamped per system clock)

    Stage `.planning/phases/28-renderer-main-spine/28-DONE-GATE.md` with **`git add -f`** per CLAUDE.md memory ".planning/ is gitignored". Commit with title `docs(phase-28): done-gate evidence`. Push that commit too (`.planning/` lives on `master` / `v8.0/config-bucket` per CLAUDE.md branch policy — never cherry-picked to `linux-port`).

    Update `.planning/ROADMAP.md` Phase 28 row: change checkbox `[ ]` → `[x]`, add `(complete YYYY-MM-DD)` after the title; commit with `git add -f` as `docs(roadmap): mark Phase 28 complete`. Push.

    Update `.planning/STATE.md` `current_position` and `progress` blocks: increment completed_phases to 5, increment completed_plans by the 12 plans Phase 28 produced (28-00 through 28-11), set `stopped_at: Phase 28 complete; ready for Phase 29 build verification`. Commit with `git add -f` as `docs(state): mark Phase 28 complete; ready for Phase 29`. Push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/ src/main/ src/preload/ src/shared/ scripts/ .github/actions/fingerprints/; test $? -ne 0 &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh &amp;&amp; pnpm typecheck -F @vortex/shared &amp;&amp; pnpm typecheck -F @vortex/preload &amp;&amp; pnpm typecheck -F @vortex/main &amp;&amp; pnpm typecheck -F @vortex/renderer &amp;&amp; pnpm typecheck &amp;&amp; test -z "$(git status --short)" &amp;&amp; test $(git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\(fingerprints\): take upstream wholesale') -eq 1 &amp;&amp; test -f .planning/phases/28-renderer-main-spine/28-DONE-GATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' <7 Phase 28 directories>` returns non-zero (no matching files)
    - `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh` (NO --skip-conflict-check) exits zero — all 16 gates pass
    - All 4 per-bucket typechecks (`@vortex/shared`, `@vortex/preload`, `@vortex/main`, `@vortex/renderer`) exit zero
    - Phase-end full-repo `pnpm typecheck` exits zero
    - `git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\((shared|preload|main|renderer|nexus|scripts|docs)\):'` returns 48–52 (depending on plan 28-10 mode)
    - `git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\(fingerprints\): take upstream wholesale'` returns 1
    - `git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ chore\(28-00\): move grep-checkpoint\.sh'` returns 1
    - `git diff <upstream-merge-parent> -- .github/actions/fingerprints/` produces no output (fingerprints tree matches upstream)
    - `git status --short` returns empty (no half-resolved files)
    - `git ls-remote git@github.com:atabisz/Vortex.git refs/heads/sync/upstream-v2.0.0` returns the expected post-push SHA (force-with-lease succeeded)
    - `.planning/phases/28-renderer-main-spine/28-DONE-GATE.md` exists with all 8 gate sections + requirements table + COMPLETE status line
    - `.planning/ROADMAP.md` Phase 28 row shows `[x]` and `(complete <date>)`
    - `.planning/STATE.md` reflects Phase 28 complete; ready for Phase 29
    - **§2/§4/§8/§9 gates explicitly verified passing** in the full grep-checkpoint run (these were the four gates added in plan 28-00)
    - **Phase 26/27 invariants (§1, §3, §6, §7a–d, §10, 140a57217, BG3, Morrowind) still pass** — Phase 28's changes did not regress them
    - All commits signed with SSH (`git log --show-signature -10` shows `Good "git" signature`)
  </acceptance_criteria>
  <done>
    Phase 28 done-gate evidence captured; force-with-lease push succeeded; ROADMAP and STATE updated; ready for Phase 29 build verification.
  </done>
</task>

</tasks>

<verification>
- All 8 D-28-05 done-gate items satisfied
- grep-checkpoint.sh full run exits zero (16 gates clean)
- 4 per-bucket typechecks pass + phase-end full-repo `pnpm typecheck` passes
- ~54 atomic commits land on `v8.0/config-bucket`
- Fingerprints tree matches upstream
- `git status --short` empty (no half-resolved files)
- Force-with-lease push to fork/sync/upstream-v2.0.0 succeeds
- `.planning/phases/28-renderer-main-spine/28-DONE-GATE.md` written
- ROADMAP.md + STATE.md updated and pushed
</verification>

<success_criteria>

- Phase 28 marked complete in ROADMAP and STATE
- All 9 phase requirement IDs (SYNC-07, SYNC-08, SYNC-09, SYNC-10, SYNC-18, SYNC-20, SYNC-24, SYNC-25, SYNC-26) verifiable via done-gate
- §2/§4/§8/§9 invariants + Phase 26/27 invariants all green
- Branch on fork remote ready for Phase 29 (build verification) to build on

</success_criteria>

<output>
After completion, create `.planning/phases/28-renderer-main-spine/28-11-SUMMARY.md`
</output>
