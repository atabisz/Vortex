---
phase: 28-renderer-main-spine
plan: 09
type: execute
wave: 10
depends_on: [28-08]
files_modified:
    - .github/actions/fingerprints/dist/index.js
    - .github/actions/fingerprints/src/clickhouse.ts
    - .github/actions/fingerprints/src/collect-input.ts
    - .github/actions/fingerprints/src/collect-input.test.ts
    - .github/actions/fingerprints/src/collect-pr.ts
    - .github/actions/fingerprints/src/collect-pr.test.ts
    - .github/actions/fingerprints/src/collect-release.ts
    - .github/actions/fingerprints/src/collect-release.test.ts
    - .github/actions/fingerprints/src/index.ts
    - .github/actions/fingerprints/src/types.ts
    - .github/actions/fingerprints/tsconfig.json
autonomous: true
requirements: [SYNC-10]

must_haves:
    truths:
        - "All 11 fingerprints conflict files resolved by taking upstream-side wholesale per Phase 28 success criterion #1 phrasing — fork-side workflow disablement is preserved at the GitHub API layer (per SYNC-10), not in the action source"
        - "Single squash commit on `v8.0/config-bucket` titled `resolve(fingerprints): take upstream wholesale (per phase-28 success criteria)` per D-28-00"
        - "After the squash commit, the `.github/actions/fingerprints/**` tree matches the upstream side of the merge byte-for-byte (`git diff --quiet ORIG_HEAD..HEAD -- .github/actions/fingerprints/` reports zero — where ORIG_HEAD is the upstream merge parent's tree at fingerprints/)"
        - "After the commit, `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero"
    artifacts:
        - path: ".github/actions/fingerprints/"
          provides: "Fingerprints GitHub Action — taken upstream wholesale; CI tooling with no fork-side Linux hooks per CONTEXT 'Established Patterns'"
          contains: "fingerprints"
    key_links:
        - from: "fingerprints squash commit"
          to: "fork-side workflow disablement"
          via: "Fork-side disables the action via GitHub API (SYNC-10) — source-tree match to upstream is the correct stance per established practice for upstream-only directories"
          pattern: ".github/actions/fingerprints/"
---

<objective>
Resolve all 11 conflict files in `.github/actions/fingerprints/` by taking the upstream side wholesale per Phase 28 success criterion #1 ("fingerprints picked from upstream side wholesale") and CONTEXT "Established Patterns" ("Fingerprints (CI tooling, no fork-side Linux hooks) take upstream wholesale — established practice for upstream-only directories"). Single squash commit on `v8.0/config-bucket` titled `resolve(fingerprints): take upstream wholesale (per phase-28 success criteria)` per D-28-00. Run grep-checkpoint script with `--skip-conflict-check` after the commit.

Purpose: Ninth bucket per D-28-01. CI tooling with no fork-side Linux hooks; fork-side workflow disablement (SYNC-10) is enforced at the GitHub API layer (workflow disabled in the fork's Actions UI), not by editing the action source.

Output: One squash commit on `v8.0/config-bucket`. After plan 28-09 lands, 47 hand-resolved + 11 fingerprints + 1 setup = 59 files done; 5 doc borderlines remain (plan 28-10).
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-renderer-main-spine/28-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Take upstream side wholesale across all 11 fingerprint files in a single squash commit</name>
  <read_first>
    - .planning/phases/28-renderer-main-spine/28-CONTEXT.md (D-28-00 — fingerprints land as a single squash commit; D-28-06 — fork-side wins for Linux fixes BUT fingerprints are upstream-wholesale per success criterion #1; CONTEXT specifics: "Fingerprints squash takes upstream wholesale per Phase 28 success criterion #1 phrasing")
    - .planning/REQUIREMENTS.md SYNC-10 — "Fork-disabled fingerprint action … resolved by picking upstream-side; fork-side workflow disablement preserved at the GitHub API layer"
    - .github/actions/fingerprints/ — list contents to confirm all 11 files are present and conflict-marked
    - Current state of each file: `for f in .github/actions/fingerprints/dist/index.js .github/actions/fingerprints/src/{clickhouse,collect-input,collect-input.test,collect-pr,collect-pr.test,collect-release,collect-release.test,index,types}.ts .github/actions/fingerprints/tsconfig.json; do echo "== $f =="; grep -c '^<<<<<<< ' "$f" 2>/dev/null; done` — confirm conflict markers present
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Take upstream side wholesale for all 11 files via `git checkout --theirs` (the upstream side of the merge):

    ```
    git checkout --theirs .github/actions/fingerprints/dist/index.js
    git checkout --theirs .github/actions/fingerprints/src/clickhouse.ts
    git checkout --theirs .github/actions/fingerprints/src/collect-input.ts
    git checkout --theirs .github/actions/fingerprints/src/collect-input.test.ts
    git checkout --theirs .github/actions/fingerprints/src/collect-pr.ts
    git checkout --theirs .github/actions/fingerprints/src/collect-pr.test.ts
    git checkout --theirs .github/actions/fingerprints/src/collect-release.ts
    git checkout --theirs .github/actions/fingerprints/src/collect-release.test.ts
    git checkout --theirs .github/actions/fingerprints/src/index.ts
    git checkout --theirs .github/actions/fingerprints/src/types.ts
    git checkout --theirs .github/actions/fingerprints/tsconfig.json
    ```

    NOTE: `--theirs` during an in-progress merge resolution refers to the side being merged in (upstream `sync/upstream-v2.0.0` for this branch's tree shape). If the merge state has already been concluded (no MERGE_HEAD active), use `git show <upstream-merge-parent>:<path>` to retrieve and overwrite each file with the upstream version. Confirm at execution time which mode applies — `git status` will show `Unmerged paths:` if a merge is still in progress.

    After all 11 files are upstream-side, verify zero conflict markers:
    ```
    grep -l '^<<<<<<< ' .github/actions/fingerprints/dist/index.js .github/actions/fingerprints/src/*.ts .github/actions/fingerprints/tsconfig.json 2>/dev/null
    ```
    Must produce no output.

    Verify the resulting tree matches upstream byte-for-byte for the 11 paths:
    ```
    git diff <upstream-merge-parent> -- .github/actions/fingerprints/
    ```
    Must produce no output (the upstream tree at fingerprints is now ours).

    Stage ALL 11 files in a single `git add` and commit with title `resolve(fingerprints): take upstream wholesale (per phase-28 success criteria)` in casual developer voice. Body: "Fingerprints is CI tooling with no fork-side Linux hooks. Fork-side disables the action via GitHub API (SYNC-10), not by editing the source. Took upstream side for all 11 files in a single squash commit per D-28-00 + Phase 28 success criterion #1."

    Run `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check`. All fifteen non-conflict gates must pass.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' .github/actions/fingerprints/; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(fingerprints\): take upstream wholesale \(per phase-28 success criteria\)$' &amp;&amp; test $(git show --stat HEAD --format='' | wc -l) -ge 11 &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' .github/actions/fingerprints/` returns empty (entire fingerprints tree clean)
    - HEAD commit title equals exactly `resolve(fingerprints): take upstream wholesale (per phase-28 success criteria)`
    - HEAD commit touches all 11 named files (verify via `git show --stat HEAD`)
    - HEAD commit is SSH-signed
    - Checkpoint exits zero
    - Tree at `.github/actions/fingerprints/**` matches upstream side of the merge (`git diff <upstream-merge-parent> -- .github/actions/fingerprints/` produces no output)
  </acceptance_criteria>
  <done>
    Fingerprints squash commit lands; 47 hand-resolved + 11 fingerprints + 1 setup = 59 files done; 5 doc borderlines + done-gate remain.
  </done>
</task>

</tasks>

<verification>
- All 11 fingerprint files conflict-clean
- One squash commit titled exactly `resolve(fingerprints): take upstream wholesale (per phase-28 success criteria)`
- Tree matches upstream byte-for-byte at `.github/actions/fingerprints/**`
- `scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
</verification>

<success_criteria>

- Fingerprints bucket resolved per Phase 28 success criterion #1 ("fingerprints picked from upstream side wholesale")
- SYNC-10 satisfied (fork-side workflow disablement preserved at GitHub API layer; source-tree matches upstream)
- Single squash commit per D-28-00

</success_criteria>

<output>
After completion, create `.planning/phases/28-renderer-main-spine/28-09-SUMMARY.md`
</output>
