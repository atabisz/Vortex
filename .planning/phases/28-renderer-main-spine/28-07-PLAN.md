---
phase: 28-renderer-main-spine
plan: 07
type: execute
wave: 8
depends_on: [28-06]
files_modified:
    - src/renderer/src/ExtensionManager.ts
autonomous: true
requirements: [SYNC-07]

must_haves:
    truths:
        - "ExtensionManager.ts resolved LAST in renderer per D-28-01 — composes all renderer-side extensions (nexus_integration, health_check, extension_manager, installer_fomod_native, etc.) which were all settled in plans 28-04, 28-05, 28-06"
        - "One atomic commit on `v8.0/config-bucket` titled `resolve(renderer): ExtensionManager.ts — <stance>` per D-28-00"
        - "After the commit, `pnpm typecheck -F @vortex/renderer` exits zero (D-28-03 per-bucket cadence — final renderer typecheck after the heaviest single file lands)"
        - "After the commit, `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero"
    artifacts:
        - path: "src/renderer/src/ExtensionManager.ts"
          provides: "Renderer-side extension composer — heaviest single file in scope per CONTEXT 'Integration Points'; imports nexus_integration, health_check, extension_manager, installer_fomod_native; last in renderer per D-28-01 re-export-composer idiom"
          contains: "ExtensionManager"
    key_links:
        - from: "ExtensionManager.ts"
          to: "src/renderer/src/extensions/{nexus_integration,health_check,extension_manager,installer_fomod_native,…}"
          via: "Imports + dynamic loads — symbol shapes from those extensions (settled in plan 28-05) must already align"
          pattern: "import .* from './extensions/'"
        - from: "ExtensionManager.ts"
          to: "@vortex/renderer per-bucket typecheck"
          via: "After this commit, plan 28-07 runs `pnpm typecheck -F @vortex/renderer` — single per-bucket typecheck for the entire renderer (D-28-03)"
            pattern: "pnpm typecheck -F @vortex/renderer"
---

<objective>
Resolve the single conflict file `src/renderer/src/ExtensionManager.ts` per D-28-01 (last in renderer order; re-export-composer idiom). One atomic commit on `v8.0/config-bucket` matching D-28-00 title format `resolve(renderer): ExtensionManager.ts — <stance>`. Per-bucket typecheck for @vortex/renderer runs AFTER this commit per D-28-03 (single typecheck for the entire renderer bucket — plans 28-04/05/06 deferred to here). Run grep-checkpoint script with `--skip-conflict-check` after the commit.

Purpose: Seventh "bucket" per D-28-01 (single file). ExtensionManager.ts is the heaviest single file in scope (per CONTEXT "Integration Points") and depends on the final symbol shape of every extension settled in plan 28-05. Lands last in renderer so the typecheck has the complete picture.

Output: One commit on `v8.0/config-bucket`. After plan 28-07 lands, 45/52 hand-resolved files done.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-renderer-main-spine/28-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve ExtensionManager.ts (last in renderer) and run @vortex/renderer per-bucket typecheck</name>
  <read_first>
    - src/renderer/src/ExtensionManager.ts (current state — every conflict region, in full; this is the heaviest single file in Phase 28 per CONTEXT "Integration Points")
    - `git show f15bbabb8:src/renderer/src/ExtensionManager.ts` (base-commit reference)
    - src/renderer/src/extensions/nexus_integration/index.tsx (just-resolved final shape from plan 28-05)
    - src/renderer/src/extensions/health_check/views/HealthCheckPage.tsx (just-resolved final shape from plan 28-05)
    - src/renderer/src/extensions/extension_manager/installExtension.ts (just-resolved final shape from plan 28-05)
    - src/renderer/src/extensions/installer_fomod_native/installer.ts (just-resolved final shape from plan 28-05)
    - .planning/phases/28-renderer-main-spine/28-CONTEXT.md ("Integration Points" — ExtensionManager.ts heaviest single file; imports nexus_integration, health_check, extension_manager, installer_fomod_native; last in renderer; D-28-01 placement; D-28-03 per-bucket typecheck cadence)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md — confirm ExtensionManager.ts is NOT named in any playbook gate (it is not). Past-gotchas section may mention IPC handler renames that route through here — read past-gotchas.
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Open `src/renderer/src/ExtensionManager.ts` and hand-resolve every conflict region. **This is the heaviest file in Phase 28** — almost certainly imports from `extensions/nexus_integration`, `extensions/health_check`, `extensions/extension_manager`, `extensions/installer_fomod_native`. Hand-read every conflict region; symbol shapes from those extensions (settled in plan 28-05) must align.

    Stance per D-28-06: fork-side wins for any extension-loading code that handles `asar.unpacked` paths or carries Linux-specific module resolution; upstream wins for additive `context.register*` registrations or new lifecycle hooks. Watch for the bluebird Promise trap.

    After all markers resolved, stage ONLY this file. Commit with title `resolve(renderer): ExtensionManager.ts — <stance>` in casual developer voice. Body: enumerate the major conflict regions resolved + which side won (this is the busiest commit body in Phase 28 — be thorough so reviewers can follow).

    Run `pnpm typecheck -F @vortex/renderer` per D-28-03 — **single per-bucket typecheck for the entire renderer**, covering plans 28-04, 28-05, 28-06, and 28-07's resolutions in one pass. Exit zero required. If typecheck fails, amend (`git commit --amend --no-edit`) before running the checkpoint script. If the typecheck failure points to a file from an earlier plan (28-04/05/06), that's a sign of cross-file drift — fix in ExtensionManager.ts or amend the offending earlier commit (use `git rebase -i HEAD~N` only if the fix genuinely belongs in the earlier file; default is to fix in ExtensionManager.ts to keep the per-file commit invariant clean).

    Run `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check`. All fifteen non-conflict gates must pass.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/ExtensionManager.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(renderer\): ExtensionManager\.ts —' &amp;&amp; pnpm typecheck -F @vortex/renderer &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' src/renderer/src/ExtensionManager.ts` returns empty
    - `git grep -l '^<<<<<<< ' src/renderer/src/` returns empty (entire renderer bucket clean — every file from plans 28-04, 28-05, 28-06, 28-07)
    - HEAD commit title starts with `resolve(renderer): ExtensionManager.ts —`
    - HEAD commit touches exactly one file; SSH-signed
    - HEAD commit body enumerates major conflict regions + per-region stances (the most thorough commit body in Phase 28)
    - `pnpm typecheck -F @vortex/renderer` exits zero — covers plans 28-04 through 28-07 cross-file drift
    - `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
  </acceptance_criteria>
  <done>
    ExtensionManager.ts resolved last in renderer; @vortex/renderer per-bucket typecheck clean; checkpoint stays green; 45/52 hand-resolved files done.
  </done>
</task>

</tasks>

<verification>
- File conflict-clean
- One atomic commit matching `resolve(renderer): ExtensionManager.ts — <stance>`
- `pnpm typecheck -F @vortex/renderer` passes (covers 28-04..28-07)
- `scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
</verification>

<success_criteria>

- 45/52 hand-resolved Phase 28 files done
- @vortex/renderer bucket typechecks clean (final per-bucket typecheck for the renderer)
- ExtensionManager.ts last in renderer per re-export-composer idiom
- Per-bucket typecheck cadence (D-28-03) honoured for renderer

</success_criteria>

<output>
After completion, create `.planning/phases/28-renderer-main-spine/28-07-SUMMARY.md`
</output>
