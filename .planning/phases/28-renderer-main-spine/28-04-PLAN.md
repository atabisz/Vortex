---
phase: 28-renderer-main-spine
plan: 04
type: execute
wave: 5
depends_on: [28-03]
files_modified:
    - src/renderer/src/util/message.ts
    - src/renderer/src/util/migrate.ts
    - src/renderer/src/util/opn.ts
    - src/renderer/src/reducers/notifications.ts
    - src/renderer/src/hooks/windowControls.ts
    - src/renderer/src/contexts/builtInPages.ts
    - src/renderer/src/contexts/PagesContext.tsx
    - src/renderer/src/controls/Table.tsx
    - src/renderer/src/ui/components/no_results/NoResults.tsx
autonomous: true
requirements: [SYNC-07]

must_haves:
    truths:
        - "Nine renderer non-extension leaf conflict files resolved per D-28-01: util/{message,migrate,opn}.ts → reducers/notifications.ts → hooks/windowControls.ts → contexts/{builtInPages,PagesContext}.tsx → controls/Table.tsx → ui/components/no_results/NoResults.tsx"
        - "Nine atomic commits on `v8.0/config-bucket` each titled `resolve(renderer): <file> — <stance>` per D-28-00"
        - "After EACH commit, `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero — §2/§4/§8/§9 + Phase 26/27 gates stay green; §2 (webpack.config.cjs), §4 (transferPath.ts), §8 (StarterInfo.ts) are gated-target files NOT in scope this phase but verified untouched after each commit"
        - "Per-bucket typecheck for @vortex/renderer is deferred to plan 28-07 (after ExtensionManager.ts lands) per D-28-03 — runs once at the end of the renderer bucket, not per-plan within it"
    artifacts:
        - path: "src/renderer/src/util/message.ts"
          provides: "Renderer message-formatting util — leaf 1/9"
          contains: "message"
        - path: "src/renderer/src/util/migrate.ts"
          provides: "Renderer state-migration util — leaf 2/9"
          contains: "migrate"
        - path: "src/renderer/src/util/opn.ts"
          provides: "Cross-platform open() wrapper (Linux: xdg-open) — leaf 3/9"
          contains: "opn"
        - path: "src/renderer/src/reducers/notifications.ts"
          provides: "Notifications redux reducer — leaf 4/9"
          contains: "notifications"
        - path: "src/renderer/src/hooks/windowControls.ts"
          provides: "Window controls React hook — leaf 5/9"
          contains: "useWindow"
        - path: "src/renderer/src/contexts/builtInPages.ts"
          provides: "Built-in page registrations — leaf 6/9"
          contains: "builtInPages"
        - path: "src/renderer/src/contexts/PagesContext.tsx"
          provides: "Pages context provider — leaf 7/9 (depends on builtInPages.ts)"
          contains: "PagesContext"
        - path: "src/renderer/src/controls/Table.tsx"
          provides: "Generic table control component — leaf 8/9"
          contains: "Table"
        - path: "src/renderer/src/ui/components/no_results/NoResults.tsx"
          provides: "Empty-state component — leaf 9/9"
          contains: "NoResults"
    key_links:
        - from: "contexts/PagesContext.tsx"
          to: "contexts/builtInPages.ts"
          via: "Provider imports built-in page list — leaf-first ordering settles list shape before provider"
          pattern: "from './builtInPages'"
        - from: "renderer-non-extension leaves"
          to: "extensions/* (plan 28-05) and views/* (plan 28-06)"
          via: "These leaves are imported by extensions and views downstream — must settle first per D-28-01"
          pattern: "import .* from '../(util|reducers|hooks|contexts|controls|ui)/'"
---

<objective>
Resolve the 9 renderer non-extension leaf conflict files per D-28-01 sub-order: `util/{message,migrate,opn}.ts` → `reducers/notifications.ts` → `hooks/windowControls.ts` → `contexts/{builtInPages,PagesContext}.tsx` → `controls/Table.tsx` → `ui/components/no_results/NoResults.tsx`. Nine atomic commits on `v8.0/config-bucket` matching D-28-00 title format `resolve(renderer): <file> — <stance>`. Run grep-checkpoint script with `--skip-conflict-check` after each commit. Per-bucket typecheck for @vortex/renderer is deferred to plan 28-07 (after ExtensionManager.ts) per D-28-03.

Purpose: Fourth bucket per D-28-01. Renderer non-extension leaves — base infrastructure consumed by extensions (plan 28-05) and views (plan 28-06). Leaf-first within the renderer bucket so downstream plans see a stable contract.

Output: Nine commits on `v8.0/config-bucket`. After plan 28-04 lands, 20/52 hand-resolved files done.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-renderer-main-spine/28-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve util/message.ts, util/migrate.ts, util/opn.ts (leaves 1–3) and commit each</name>
  <read_first>
    - src/renderer/src/util/message.ts (every conflict region, in full)
    - src/renderer/src/util/migrate.ts (every conflict region, in full)
    - src/renderer/src/util/opn.ts (every conflict region, in full)
    - `git show f15bbabb8:src/renderer/src/util/message.ts`, `git show f15bbabb8:src/renderer/src/util/migrate.ts`, `git show f15bbabb8:src/renderer/src/util/opn.ts` (base-commit references)
    - .planning/phases/28-renderer-main-spine/28-CONTEXT.md (D-28-00 commit format; D-28-01 sub-order; D-28-06 stance defaults)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md — confirm none of these three files are named in any playbook gate (they are not). Note: `opn.ts` is the renderer wrapper around `open` / `xdg-open` — fork-side likely carries Linux-specific platform branching. Read the file before resolving.
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Resolve each file in order, ONE COMMIT PER FILE per D-28-00:

    1. `src/renderer/src/util/message.ts` → commit `resolve(renderer): util/message.ts — <stance>`. Run checkpoint `--skip-conflict-check`.
    2. `src/renderer/src/util/migrate.ts` → commit `resolve(renderer): util/migrate.ts — <stance>`. Run checkpoint `--skip-conflict-check`.
    3. `src/renderer/src/util/opn.ts` → commit `resolve(renderer): util/opn.ts — <stance>`. Run checkpoint `--skip-conflict-check`. **Pay attention** — opn.ts is the cross-platform open wrapper; if upstream removes Linux `xdg-open` handling, fork-side wins.

    For each: hand-resolve, default stance (D-28-06), watch for bluebird Promise trap on async functions. Stage only the named file. Commit. Run checkpoint. All fifteen non-conflict gates must pass after each commit.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/util/message.ts src/renderer/src/util/migrate.ts src/renderer/src/util/opn.ts; test $? -ne 0 &amp;&amp; test $(git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\(renderer\): util/(message|migrate|opn)\.ts —') -eq 3 &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - All three util/* files conflict-clean
    - Three new HEAD-region commits matching `resolve(renderer): util/<file>.ts —`
    - Each commit touches exactly one file; SSH-signed
    - Checkpoint exits zero after each commit
  </acceptance_criteria>
  <done>
    util/* leaves resolved; three atomic commits land.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolve reducers/notifications.ts and hooks/windowControls.ts (leaves 4–5) and commit each</name>
  <read_first>
    - src/renderer/src/reducers/notifications.ts (every conflict region, in full)
    - src/renderer/src/hooks/windowControls.ts (every conflict region, in full)
    - `git show f15bbabb8:src/renderer/src/reducers/notifications.ts`, `git show f15bbabb8:src/renderer/src/hooks/windowControls.ts` (base-commit references)
    - notifications redux contract — search `src/renderer/src/actions/notifications` for action creators that the reducer matches against
  </read_first>
  <action>
    Resolve each in order, one commit per file:

    1. `src/renderer/src/reducers/notifications.ts` → commit `resolve(renderer): reducers/notifications.ts — <stance>`. Run checkpoint `--skip-conflict-check`. Stance default; upstream-likely additive (new notification types).
    2. `src/renderer/src/hooks/windowControls.ts` → commit `resolve(renderer): hooks/windowControls.ts — <stance>`. Run checkpoint. Window-control hook may carry Linux-specific min/max/close behaviour — fork-side wins for any platform branch.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/reducers/notifications.ts src/renderer/src/hooks/windowControls.ts; test $? -ne 0 &amp;&amp; test $(git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\(renderer\): (reducers/notifications|hooks/windowControls)\.ts —') -eq 2 &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - Both files conflict-clean
    - Two new commits matching `resolve(renderer): (reducers/notifications|hooks/windowControls).ts —`
    - Each commit touches exactly one file; SSH-signed
    - Checkpoint exits zero after each commit
  </acceptance_criteria>
  <done>
    reducers + hooks leaves resolved; two atomic commits land.
  </done>
</task>

<task type="auto">
  <name>Task 3: Resolve contexts/builtInPages.ts then contexts/PagesContext.tsx (leaves 6–7), then controls/Table.tsx and ui/.../NoResults.tsx (leaves 8–9), and commit each</name>
  <read_first>
    - src/renderer/src/contexts/builtInPages.ts (every conflict region, in full)
    - src/renderer/src/contexts/PagesContext.tsx (every conflict region, in full)
    - src/renderer/src/controls/Table.tsx (every conflict region, in full)
    - src/renderer/src/ui/components/no_results/NoResults.tsx (every conflict region, in full)
    - `git show f15bbabb8:` versions for all four (base-commit references)
    - .planning/phases/28-renderer-main-spine/28-CONTEXT.md (D-28-01 — `builtInPages.ts` BEFORE `PagesContext.tsx` because the provider imports the page list)
  </read_first>
  <action>
    Resolve in this exact order, one commit per file:

    1. `src/renderer/src/contexts/builtInPages.ts` → commit `resolve(renderer): contexts/builtInPages.ts — <stance>`. Page-list registry; upstream-likely additive (new built-in pages).
    2. `src/renderer/src/contexts/PagesContext.tsx` → commit `resolve(renderer): contexts/PagesContext.tsx — <stance>`. Provider; depends on builtInPages.ts shape (just resolved). Run checkpoint after each.
    3. `src/renderer/src/controls/Table.tsx` → commit `resolve(renderer): controls/Table.tsx — <stance>`. Generic Table component; stance default — upstream-likely additive (new column types, new sort behaviour).
    4. `src/renderer/src/ui/components/no_results/NoResults.tsx` → commit `resolve(renderer): ui/components/no_results/NoResults.tsx — <stance>`. Empty-state UI; stance default.

    Run `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check` after EACH commit (4 runs in this task). All fifteen non-conflict gates must pass each time.

    Do NOT run per-bucket typecheck here — D-28-03 places the @vortex/renderer typecheck after ExtensionManager.ts (plan 28-07).

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/contexts/builtInPages.ts src/renderer/src/contexts/PagesContext.tsx src/renderer/src/controls/Table.tsx src/renderer/src/ui/components/no_results/NoResults.tsx; test $? -ne 0 &amp;&amp; test $(git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\(renderer\): (contexts/(builtInPages|PagesContext)\.(ts|tsx)|controls/Table\.tsx|ui/components/no_results/NoResults\.tsx) —') -eq 4 &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - All four files conflict-clean
    - Four new commits matching `resolve(renderer): (contexts/builtInPages|contexts/PagesContext|controls/Table|ui/components/no_results/NoResults).<ext> —`
    - Each commit touches exactly one file; SSH-signed
    - Checkpoint exits zero after each commit
    - `git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\(renderer\):'` returns 9 (this plan's full output)
  </acceptance_criteria>
  <done>
    All nine renderer non-extension leaves resolved; nine atomic commits land; checkpoint stays green; 20/52 hand-resolved files done.
  </done>
</task>

</tasks>

<verification>
- All nine files conflict-clean
- Nine atomic commits matching `resolve(renderer): <file> — <stance>`
- `scripts/grep-checkpoint.sh --skip-conflict-check` exits zero after each commit
- Per-bucket typecheck deferred to plan 28-07 per D-28-03
</verification>

<success_criteria>

- 20/52 hand-resolved Phase 28 files done
- Renderer non-extension leaves settled before extensions (plan 28-05) and views (plan 28-06)
- Per-region stance defaults from D-28-06 honoured

</success_criteria>

<output>
After completion, create `.planning/phases/28-renderer-main-spine/28-04-SUMMARY.md`
</output>
