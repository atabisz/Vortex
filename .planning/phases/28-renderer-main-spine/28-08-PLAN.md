---
phase: 28-renderer-main-spine
plan: 08
type: execute
wave: 9
depends_on: [28-07]
files_modified:
    - scripts/download-duckdb-extensions.ts
    - scripts/download-duckdb-extensions.test.ts
autonomous: true
requirements: [SYNC-08]

must_haves:
    truths:
        - "Two scripts/* conflict files resolved per D-28-01: download-duckdb-extensions.ts → download-duckdb-extensions.test.ts (source first, test follows)"
        - "Two atomic commits on `v8.0/config-bucket` each titled `resolve(scripts): <file> — <stance>` per D-28-00"
        - "After EACH commit, `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero"
    artifacts:
        - path: "scripts/download-duckdb-extensions.ts"
          provides: "Build-time DuckDB extension downloader — leaf 1/2"
          contains: "duckdb"
        - path: "scripts/download-duckdb-extensions.test.ts"
          provides: "Vitest suite for the downloader — leaf 2/2 (test follows source)"
          contains: "describe"
    key_links:
        - from: "scripts/download-duckdb-extensions.test.ts"
          to: "scripts/download-duckdb-extensions.ts"
          via: "Test imports the downloader — must follow source resolution"
          pattern: "from '\\./download-duckdb-extensions'"
        - from: "scripts/download-duckdb-extensions.ts"
          to: "src/main/src/store/DuckDBSingleton.ts (settled in plan 28-03)"
          via: "Both target the DuckDB family — main-side singleton may import platform-specific extension paths the script downloads"
          pattern: "duckdb"
---

<objective>
Resolve the 2 conflict files in `scripts/` per D-28-01: `download-duckdb-extensions.ts` → `download-duckdb-extensions.test.ts`. Two atomic commits on `v8.0/config-bucket` matching D-28-00 title format `resolve(scripts): <file> — <stance>`. Run grep-checkpoint script with `--skip-conflict-check` after each commit. No per-bucket typecheck — `scripts/` is not a workspace package; type-correctness surfaces in the phase-end full `pnpm typecheck` (plan 28-11).

Purpose: Eighth bucket per D-28-01. Build-time DuckDB extension downloader; same DuckDB family as main-side `DuckDBSingleton.ts` (settled in plan 28-03).

Output: Two commits on `v8.0/config-bucket`. After plan 28-08 lands, 47/52 hand-resolved files done.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-renderer-main-spine/28-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve scripts/download-duckdb-extensions.ts (leaf 1/2) and commit</name>
  <read_first>
    - scripts/download-duckdb-extensions.ts (current state — every conflict region, in full)
    - `git show f15bbabb8:scripts/download-duckdb-extensions.ts` (base-commit reference)
    - src/main/src/store/DuckDBSingleton.ts (just-resolved in plan 28-03 — DuckDB family cohesion; the script downloads what the singleton loads)
    - .planning/phases/28-renderer-main-spine/28-CONTEXT.md (D-28-00 commit format; D-28-06 stance defaults)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md — confirm scripts/download-duckdb-extensions.ts is NOT named in any playbook gate (it is not). Linux DuckDB extension URLs may differ from Windows — fork-side wins for any platform-specific URL switching.
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Open `scripts/download-duckdb-extensions.ts` and hand-resolve every conflict region. DuckDB extension download script. Stance per D-28-06: fork-side wins for any Linux-specific platform branching (URL selection, `.so` vs `.dll` extension); upstream wins for additive download targets or version bumps.

    Stage ONLY this file. Commit with title `resolve(scripts): download-duckdb-extensions.ts — <stance>` in casual developer voice.

    Run `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check`. All fifteen non-conflict gates must pass.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' scripts/download-duckdb-extensions.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(scripts\): download-duckdb-extensions\.ts —' &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - File conflict-clean
    - HEAD commit title starts with `resolve(scripts): download-duckdb-extensions.ts —`
    - HEAD commit touches exactly one file; SSH-signed
    - Checkpoint exits zero
  </acceptance_criteria>
  <done>
    Downloader script resolved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolve scripts/download-duckdb-extensions.test.ts (leaf 2/2) and commit</name>
  <read_first>
    - scripts/download-duckdb-extensions.test.ts (current state — every conflict region, in full)
    - scripts/download-duckdb-extensions.ts (just-resolved final shape from Task 1 — test imports must align)
    - `git show f15bbabb8:scripts/download-duckdb-extensions.test.ts` (base-commit reference)
  </read_first>
  <action>
    Open `scripts/download-duckdb-extensions.test.ts` and hand-resolve every conflict region. Test for the downloader. Stance default; upstream-likely additive (new test cases for new download targets).

    Stage ONLY this file. Commit with title `resolve(scripts): download-duckdb-extensions.test.ts — <stance>`.

    Run `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check`.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' scripts/download-duckdb-extensions.test.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(scripts\): download-duckdb-extensions\.test\.ts —' &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - File conflict-clean
    - HEAD commit title starts with `resolve(scripts): download-duckdb-extensions.test.ts —`
    - HEAD commit touches exactly one file; SSH-signed
    - Checkpoint exits zero
    - `git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\(scripts\):'` returns 2
  </acceptance_criteria>
  <done>
    Both scripts files resolved; 47/52 hand-resolved files done.
  </done>
</task>

</tasks>

<verification>
- Both files conflict-clean
- Two atomic commits matching `resolve(scripts): <file> — <stance>`
- `scripts/grep-checkpoint.sh --skip-conflict-check` exits zero after each commit
- No per-bucket typecheck (scripts/ is not a workspace package — full `pnpm typecheck` in plan 28-11 covers it)
</verification>

<success_criteria>

- 47/52 hand-resolved Phase 28 files done
- DuckDB family settled across main + scripts buckets

</success_criteria>

<output>
After completion, create `.planning/phases/28-renderer-main-spine/28-08-SUMMARY.md`
</output>
