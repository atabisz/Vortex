---
phase: 32-mod-management-hot-zone-v2-0-1
plan: 03
type: execute
wave: 2
depends_on:
    - 32-02
files_modified:
    - src/renderer/src/extensions/mod_management/modMerging.ts
    - src/renderer/src/extensions/mod_management/views/DeactivationButton.tsx
    - src/renderer/src/extensions/mod_management/views/Settings.tsx
    - src/renderer/src/extensions/mod_management/eventHandlers.ts
    - src/renderer/src/extensions/mod_management/views/ModList.tsx
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-03-SUMMARY.md
autonomous: true
requirements:
    - SYNC-32a
must_haves:
    truths:
        - "All 5 mid-tier files have zero conflict markers after this plan"
        - "transferPath import + 3 call sites in views/Settings.tsx preserved (playbook §4 surface; fork-wins per D-32-02)"
        - "After ModList.tsx resolution, mod_management/ non-marker typecheck error count drops from ~260 to ~0 (JSX cascades evaporate)"
        - "Each file resolution is its own SSH-signed commit per D-32-08"
        - "Harness in --skip-conflict-check mode stays GREEN after every commit"
    artifacts:
        - path: "src/renderer/src/extensions/mod_management/modMerging.ts"
          provides: "Mod merging with BACKUP_TAG re-export consumer (constant; LinkingDeployment.ts source)"
        - path: "src/renderer/src/extensions/mod_management/views/DeactivationButton.tsx"
          provides: "Deactivation button (no playbook surface)"
        - path: "src/renderer/src/extensions/mod_management/views/Settings.tsx"
          provides: "Mod settings UI with transferPath playbook §4 call sites preserved"
          contains: "transferPath"
        - path: "src/renderer/src/extensions/mod_management/eventHandlers.ts"
          provides: "Mod-management event handlers (imports InstallManager/stagingDirectory/deploy as types)"
        - path: "src/renderer/src/extensions/mod_management/views/ModList.tsx"
          provides: "Mod list React component — owns the JSX-cascade typecheck baseline"
    key_links:
        - from: "views/Settings.tsx"
          to: "transferPath helper (renderer/src/util/transferPath)"
          via: "import + 3 call sites"
          pattern: "transferPath"
---

<objective>
Resolve all 5 mid-tier conflict files in dependency-aware order per RESEARCH §3 and PATTERNS rows
8-12. The high-watermark is `views/ModList.tsx` (11 regions, owns the entire 260-error JSX-cascade
typecheck baseline) — once resolved, mod_management/ non-marker error count drops to 0. The
medium-risk file is `views/Settings.tsx` whose import-block reorganization region must keep the
fork's `transferPath` (playbook §4 surface) imports intact.

Purpose: Mid-tier finishes before playbook-heavy tier so InstallManager.ts and LinkingDeployment.ts
in Plan 04 compile against clean upstream consumers. The ModList.tsx resolution is the single
biggest typecheck-baseline reduction in the phase.

Output: 5 resolved files (zero conflict markers each), 5 SSH-signed commits on `v8.1/config-bucket`,
1 SUMMARY commit at end. After ModList.tsx is committed, mod_management/ non-marker typecheck error
count must be 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-CONTEXT.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-RESEARCH.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-PATTERNS.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-VALIDATION.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
@CLAUDE.md
@AGENTS.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-02-SUMMARY.md
</context>

<shared_per_task_workflow>
Same as Plan 02 — see `32-02-PLAN.md` `<shared_per_task_workflow>` section. Summary:

1. Capture master analog → /tmp.
2. Hand-resolve per region using D-32-02 stance hierarchy.
3. Verify zero markers (`git diff --check`).
4. Pre/post playbook grep snapshot for files with playbook surface (in this plan: `views/Settings.tsx`).
5. Run harness `--skip-conflict-check` (must exit 0).
6. Bucket-scoped typecheck filter for the file's path (must be 0 non-marker errors after commit; ModList.tsx is the exception — see Task 5 acceptance criteria).
7. SSH-signed commit (no `--no-verify`); body per D-32-09.
8. Verify signature.
9. Do NOT push.

Bluebird trap (R5): empirically clear for these files per PATTERNS metadata; if a region
introduces a bluebird import, prefer fork-side.

Per project memory `feedback_minimize_upstream_diff.md`: never reformat outside the conflict region;
keep diffs tight.
</shared_per_task_workflow>

<tasks>

<task type="auto">
  <name>Task 1: Resolve modMerging.ts (2 regions; line-wrap)</name>
  <files>src/renderer/src/extensions/mod_management/modMerging.ts</files>
  <read_first>
    - src/renderer/src/extensions/mod_management/modMerging.ts (current state)
    - Output of `git show fork/master:src/renderer/src/extensions/mod_management/modMerging.ts` saved to /tmp/modMerging.master.ts
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-PATTERNS.md (row 8 — note BACKUP_TAG import from LinkingDeployment.ts is a string constant, shape unchanged)
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-RESEARCH.md (Atomic-Commit Boundaries §4 — confirms modMerging ↔ LinkingDeployment is safe to split across plans)
  </read_first>
  <action>
    Follow shared workflow. Per PATTERNS row 8: 2 line-wrap regions, no playbook surface. Stance:
    smaller-diff for both.

    BACKUP_TAG re-export from LinkingDeployment.ts is the only import that crosses tier boundaries;
    LinkingDeployment.ts is not yet resolved at this point but BACKUP_TAG is a string constant whose
    declaration shape doesn't change between fork-side and upstream-side conflicts. This is safe per
    RESEARCH §4.

    Commit title: `resolve(mod-mgmt-v2.0.1): modMerging.ts — smaller-diff line-wraps (×2)`

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      git diff --check src/renderer/src/extensions/mod_management/modMerging.ts &amp;&amp; \
      ! grep -q '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/modMerging.ts &amp;&amp; \
      bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check &gt;/dev/null &amp;&amp; \
      git log -1 --pretty=%s | grep -q '^resolve(mod-mgmt-v2.0.1): modMerging.ts' &amp;&amp; \
      git log -1 --show-signature 2&gt;&amp;1 | grep -q 'Good "git" signature' &amp;&amp; \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - File marker-free, harness GREEN, typecheck filter for this path = 0 non-marker errors, signed commit
    - Commit body records 2 regions smaller-diff
  </acceptance_criteria>
  <done>modMerging.ts marker-free and committed.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve views/DeactivationButton.tsx (1 region; line-wrap)</name>
  <files>src/renderer/src/extensions/mod_management/views/DeactivationButton.tsx</files>
  <read_first>
    - src/renderer/src/extensions/mod_management/views/DeactivationButton.tsx (current state)
    - Output of `git show fork/master:src/renderer/src/extensions/mod_management/views/DeactivationButton.tsx` saved to /tmp/DeactivationButton.master.tsx
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-PATTERNS.md (row 9)
  </read_first>
  <action>
    Follow shared workflow. Per PATTERNS row 9: 1 line-wrap, no playbook. Stance: smaller-diff.

    Commit title: `resolve(mod-mgmt-v2.0.1): views/DeactivationButton.tsx — smaller-diff line-wrap`

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      git diff --check src/renderer/src/extensions/mod_management/views/DeactivationButton.tsx &amp;&amp; \
      ! grep -q '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/views/DeactivationButton.tsx &amp;&amp; \
      bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check &gt;/dev/null &amp;&amp; \
      git log -1 --pretty=%s | grep -q '^resolve(mod-mgmt-v2.0.1): views/DeactivationButton.tsx' &amp;&amp; \
      git log -1 --show-signature 2&gt;&amp;1 | grep -q 'Good "git" signature' &amp;&amp; \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - File marker-free, harness GREEN, typecheck filter for this path = 0 non-marker errors, signed commit
  </acceptance_criteria>
  <done>views/DeactivationButton.tsx marker-free and committed.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve views/Settings.tsx (2 regions; FORK-WINS for transferPath import block)</name>
  <files>src/renderer/src/extensions/mod_management/views/Settings.tsx</files>
  <read_first>
    - src/renderer/src/extensions/mod_management/views/Settings.tsx (current state)
    - Output of `git show fork/master:src/renderer/src/extensions/mod_management/views/Settings.tsx` saved to /tmp/Settings.master.tsx
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-PATTERNS.md (row 10 — playbook lines 58, 321, 381, 568 on master = transferPath surface)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md (§4 transferPath entry — fork-only Linux helper)
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-RESEARCH.md (Conflict-Region Shape Characterization — Settings.tsx row: "MEDIUM — hand-merge import block carefully")
  </read_first>
  <action>
    Follow shared workflow with extra care: import-block reorganization region must keep `transferPath`
    import on the fork-side per playbook §4.

    Per PATTERNS row 10: 2 regions. Region 1 = import-block reorganization → FORK-WINS for the
    `transferPath` import line; merge any new upstream imports into the fork's import block layout.
    Region 2 = line-wrap → smaller-diff.

    Pre/post grep snapshot for transferPath:
    ```
    grep -nE 'transferPath' src/renderer/src/extensions/mod_management/views/Settings.tsx > /tmp/Settings.pre.txt
    grep -nE 'transferPath' /tmp/Settings.master.tsx > /tmp/Settings.master.hits.txt
    # ... resolve ...
    grep -nE 'transferPath' src/renderer/src/extensions/mod_management/views/Settings.tsx > /tmp/Settings.post.txt
    test "$(wc -l < /tmp/Settings.post.txt)" -ge "$(wc -l < /tmp/Settings.master.hits.txt)"
    ```
    Per PATTERNS row 10, master has hits at lines 58 (import), 321, 381, 568 (call sites) — at least 4
    hits. Post-resolution must have ≥ 4 hits.

    Commit title: `resolve(mod-mgmt-v2.0.1): views/Settings.tsx — fork-wins on transferPath import block`

    Body must call out: "fork-side: 1 region (import block — preserves transferPath playbook §4
    import); smaller-diff: 1 region (line-wrap). transferPath hits: pre N → post M (master ref: 4)."

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      git diff --check src/renderer/src/extensions/mod_management/views/Settings.tsx &amp;&amp; \
      ! grep -q '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/views/Settings.tsx &amp;&amp; \
      test "$(grep -c 'transferPath' src/renderer/src/extensions/mod_management/views/Settings.tsx)" -ge 4 &amp;&amp; \
      bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check &gt;/dev/null &amp;&amp; \
      git log -1 --pretty=%s | grep -q '^resolve(mod-mgmt-v2.0.1): views/Settings.tsx' &amp;&amp; \
      git log -1 --show-signature 2&gt;&amp;1 | grep -q 'Good "git" signature' &amp;&amp; \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - File marker-free, harness GREEN, typecheck filter for this path = 0 non-marker errors, signed commit
    - `grep -c transferPath src/renderer/src/extensions/mod_management/views/Settings.tsx` ≥ 4 (matches master analog count)
    - Commit body records 1 region fork-side (import block), 1 region smaller-diff; explicitly cites VORTEX-LINUX-MERGE-PLAYBOOK §4 transferPath preservation
  </acceptance_criteria>
  <done>views/Settings.tsx marker-free, transferPath playbook §4 surface intact, committed.</done>
</task>

<task type="auto">
  <name>Task 4: Resolve eventHandlers.ts (3 regions; import block + line-wraps)</name>
  <files>src/renderer/src/extensions/mod_management/eventHandlers.ts</files>
  <read_first>
    - src/renderer/src/extensions/mod_management/eventHandlers.ts (current state)
    - Output of `git show fork/master:src/renderer/src/extensions/mod_management/eventHandlers.ts` saved to /tmp/eventHandlers.master.ts
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-PATTERNS.md (row 11 — note "imports InstallManager / stagingDirectory / deploy as types")
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-RESEARCH.md (Open Q #2 resolution: "Resolve eventHandlers before InstallManager")
  </read_first>
  <action>
    Follow shared workflow. Per PATTERNS row 11 / RESEARCH §1: 3 regions. Region 1 = import block
    addition → upstream-wins (or hand-merge). Regions 2-3 = line-wrap → smaller-diff.

    Per RESEARCH Open Q #2 resolution: eventHandlers imports `InstallManager` as a TYPE only, so
    resolving it before InstallManager.ts (which is in Plan 04) is safe — the type signatures on the
    fork-side of InstallManager already match what eventHandlers needs.

    No playbook surface in this file directly.

    Commit title: `resolve(mod-mgmt-v2.0.1): eventHandlers.ts — upstream import additions + smaller-diff line-wraps`

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      git diff --check src/renderer/src/extensions/mod_management/eventHandlers.ts &amp;&amp; \
      ! grep -q '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/eventHandlers.ts &amp;&amp; \
      bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check &gt;/dev/null &amp;&amp; \
      git log -1 --pretty=%s | grep -q '^resolve(mod-mgmt-v2.0.1): eventHandlers.ts' &amp;&amp; \
      git log -1 --show-signature 2&gt;&amp;1 | grep -q 'Good "git" signature' &amp;&amp; \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - File marker-free, harness GREEN, typecheck filter for this path = 0 non-marker errors, signed commit
    - Commit body records 1 region upstream-side (import block), 2 regions smaller-diff; notes "type-only import on InstallManager — safe to resolve before InstallManager.ts per RESEARCH Open Q #2"
  </acceptance_criteria>
  <done>eventHandlers.ts marker-free and committed.</done>
</task>

<task type="auto">
  <name>Task 5: Resolve views/ModList.tsx (11 regions; owns JSX-cascade typecheck baseline)</name>
  <files>src/renderer/src/extensions/mod_management/views/ModList.tsx</files>
  <read_first>
    - src/renderer/src/extensions/mod_management/views/ModList.tsx (current state — 11 markers)
    - Output of `git show fork/master:src/renderer/src/extensions/mod_management/views/ModList.tsx` saved to /tmp/ModList.master.tsx
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-PATTERNS.md (row 12 — note "owns the entire 260-error JSX-cascade typecheck baseline")
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-RESEARCH.md (§5 typecheck strategy + Pitfall 4 — line numbers may have drifted from v8.0 doc; trust working-tree state)
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-01-SUMMARY.md (baseline numbers from Plan 01 Step F)
  </read_first>
  <action>
    Follow shared workflow with the post-commit typecheck check being the centerpiece — this is the
    file that drives the baseline to 0.

    Per PATTERNS row 12 / RESEARCH §1: 11 regions, mostly Prettier reformat + import-block reorg, no
    direct playbook surface. Stance: smaller-diff for all 11.

    Pre-resolution measurement of mod_management/ non-marker error count (should still be ~260, all
    in this file):
    ```
    pnpm typecheck -F @vortex/renderer 2>&1 | grep "extensions/mod_management/views/ModList" | grep -v TS1185 | wc -l
    ```
    Capture the actual number.

    Hand-resolve all 11 regions. Per `feedback_minimize_upstream_diff.md`, keep diffs tight — do not
    reformat outside the regions.

    Post-resolution measurement:
    ```
    pnpm typecheck -F @vortex/renderer 2>&1 | grep "extensions/mod_management/views/ModList" | grep -v TS1185 | wc -l
    ```
    Expected: 0 (the JSX-cascade errors evaporate once markers go).

    Then full mod_management/ scope check:
    ```
    pnpm typecheck -F @vortex/renderer 2>&1 | grep "extensions/mod_management/" | grep -v TS1185 | wc -l
    ```
    Expected: 0 (since the other resolved leaf+mid files contributed 0 to the baseline; the only
    remaining markers are in InstallManager / LinkingDeployment / index.ts but those produce TS1185
    which we filter out, NOT JSX cascades).

    If post-resolution mod_management/ count is NOT 0: investigate. The file's resolution may have
    introduced a real type error. Per RESEARCH Pitfall 2: that's exactly the warning sign worth
    pausing on.

    Commit title: `resolve(mod-mgmt-v2.0.1): views/ModList.tsx — smaller-diff line-wraps + import-block reorg (11 regions)`

    Body must include the typecheck delta: "mod_management non-marker errors: pre-resolve N (~260,
    all in this file as JSX cascade) → post-resolve 0. Baseline from 32-01-SUMMARY drops to 0."

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      git diff --check src/renderer/src/extensions/mod_management/views/ModList.tsx &amp;&amp; \
      ! grep -q '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/views/ModList.tsx &amp;&amp; \
      bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check &gt;/dev/null &amp;&amp; \
      git log -1 --pretty=%s | grep -q '^resolve(mod-mgmt-v2.0.1): views/ModList.tsx' &amp;&amp; \
      git log -1 --show-signature 2&gt;&amp;1 | grep -q 'Good "git" signature' &amp;&amp; \
      MM_ERR=$(pnpm typecheck -F @vortex/renderer 2>&amp;1 | grep "extensions/mod_management/" | grep -v TS1185 | wc -l) &amp;&amp; \
      test "$MM_ERR" -eq 0 &amp;&amp; \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - File marker-free, harness GREEN, signed commit
    - Bucket-scoped typecheck for `extensions/mod_management/` returns 0 non-marker errors after this commit (the central deliverable of this task)
    - Commit body records 11 regions smaller-diff, AND records the typecheck delta (pre-resolve count, post-resolve 0)
  </acceptance_criteria>
  <done>views/ModList.tsx marker-free; mod_management non-marker baseline now 0; committed.</done>
</task>

<task type="auto">
  <name>Task 6: Plan SUMMARY commit</name>
  <files>.planning/phases/32-mod-management-hot-zone-v2-0-1/32-03-SUMMARY.md</files>
  <read_first>
    - All 5 commit messages from this plan: `git log --oneline 32-02..HEAD -- src/renderer/src/extensions/mod_management/`
    - $HOME/.claude/get-shit-done/templates/summary.md
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-VALIDATION.md (Validation Sign-Off — plan-level)
  </read_first>
  <action>
    Write `32-03-SUMMARY.md` summarizing the mid-tier resolution: 5 files done, region split per
    file, transferPath playbook §4 hits preserved in views/Settings.tsx, mod_management non-marker
    typecheck count dropped from baseline ~260 to 0 after ModList.tsx resolution.

    Required sections:
    - Outcome: 5/5 mid-tier files resolved on `v8.1/config-bucket`
    - Per-file table: file, region count, fork/upstream/smaller-diff split, commit SHA, typecheck delta (most rows = 0→0; ModList row = ~260→0)
    - Playbook gates: §4 transferPath in views/Settings.tsx preserved (4+ hits matching master)
    - Issues encountered (if any)
    - Affects: Plan 04 (playbook-heavy tier) now compiles against clean mid-tier files
    - Provides: 5 mid-tier files marker-free; mod_management non-marker baseline 0

    Add via `git add -f`. Commit title: `docs(32-03): summarize mid-tier resolution (5 files, 19 regions)`.

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      test -f .planning/phases/32-mod-management-hot-zone-v2-0-1/32-03-SUMMARY.md &amp;&amp; \
      bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check &gt;/dev/null &amp;&amp; \
      git log -1 --pretty=%s | grep -q '^docs(32-03): summarize mid-tier' &amp;&amp; \
      git log -1 --show-signature 2&gt;&amp;1 | grep -q 'Good "git" signature' &amp;&amp; \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - `32-03-SUMMARY.md` exists, committed via `git add -f`, SSH-signed
    - 5 `resolve(mod-mgmt-v2.0.1):` commits in this plan + 1 docs commit
    - Final harness skip-mode run exits 0
    - mod_management/ non-marker typecheck error count is 0 at end of plan
    - mod_management/ files remaining with conflict markers: 3 (LinkingDeployment.ts, InstallManager.ts, index.ts — all Plan 04/05 territory)
  </acceptance_criteria>
  <done>Mid-tier resolution complete; summary committed; branch ready for Plan 04 (playbook-heavy).</done>
</task>

</tasks>

<verification>
After all tasks:
- `bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check` exits 0
- `git grep -l '^<<<<<<< ' src/renderer/src/extensions/mod_management/ | wc -l` returns 3 (LinkingDeployment.ts, InstallManager.ts, index.ts)
- `pnpm typecheck -F @vortex/renderer 2>&1 | grep "extensions/mod_management/" | grep -v TS1185 | wc -l` returns 0
- views/Settings.tsx still has ≥4 `transferPath` hits (playbook §4 surface intact)
- All 5 + 1 commits SSH-signed
</verification>

<success_criteria>

- 5 mid-tier files marker-free
- 5 + 1 SSH-signed commits on `v8.1/config-bucket`
- transferPath playbook §4 surface preserved in views/Settings.tsx
- mod_management/ non-marker typecheck count drops from baseline ~260 to 0
- Plan 04 unblocked
  </success_criteria>

<output>
Create `.planning/phases/32-mod-management-hot-zone-v2-0-1/32-03-SUMMARY.md` per Task 6.
</output>
