---
phase: 32-mod-management-hot-zone-v2-0-1
plan: 04
type: execute
wave: 3
depends_on:
    - 32-03
files_modified:
    - src/renderer/src/extensions/mod_management/LinkingDeployment.ts
    - src/renderer/src/extensions/mod_management/InstallManager.ts
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-04-SUMMARY.md
autonomous: true
requirements:
    - SYNC-32a
must_haves:
    truths:
        - "LinkingDeployment.ts has zero conflict markers AND ≥3 resolvePathCase(dataPath, ...) call sites preserved (140a57217 / D-32-12 single-host invariant)"
        - "fork-only errorCodes.add(getErrorCode(err)) ?? UNKNOWN block at LinkingDeployment.ts region @212 PRESERVED"
        - "InstallManager.ts has zero conflict markers AND every playbook §6/§7a-d call site count is preserved per RESEARCH §1 dangerous-region map"
        - "Pre/post grep snapshots captured for both files per D-32-11 + Pattern S2"
        - "Each file resolution is its own SSH-signed commit per D-32-08"
        - "Harness in --skip-conflict-check mode stays GREEN with all 6 playbook gates after every commit"
    artifacts:
        - path: "src/renderer/src/extensions/mod_management/LinkingDeployment.ts"
          provides: "Hardlink/symlink deployment with externalChanges() method 140a57217 case-resolution intact"
          contains: "resolvePathCase(dataPath,"
        - path: "src/renderer/src/extensions/mod_management/InstallManager.ts"
          provides: "Mod install pipeline with §6 stagingDirHasFiles guard, §7a-d backslash/case helpers, §7d resolvePathCase(tempPath,...)"
          contains: "stagingDirHasFiles, normalizeBackslashPaths, mergeCaseConflictingDirs, resolvePathCase"
    key_links:
        - from: "LinkingDeployment.ts:externalChanges()"
          to: "resolvePathCase utility (case-folding fs wrapper, Phase 14)"
          via: "3 call sites at master lines 523/742/799 (current tree drifted to 599/818/875 — threshold is ≥3, drift is harmless)"
          pattern: "resolvePathCase\\(dataPath,"
        - from: "InstallManager.ts:doDownload"
          to: "stagingDirHasFiles (util/stagingIntegrity.ts)"
          via: "stale staging-dir guard"
          pattern: "stagingDirHasFiles"
        - from: "InstallManager.ts:simulate+install paths"
          to: "normalizeBackslashPaths + mergeCaseConflictingDirs (util)"
          via: "FOMOD pre-build call pair (lines @1165 and @3852)"
          pattern: "normalizeBackslashPaths|mergeCaseConflictingDirs"
        - from: "InstallManager.ts:extractArchive copy loop"
          to: "resolvePathCase(tempPath,...) + replaceAll backslash→slash"
          via: "case-folded copy normalization (≥1 hit)"
          pattern: "resolvePathCase\\(tempPath,"
---

<objective>
Resolve the two playbook-heavy files in dependency-aware order: `LinkingDeployment.ts` first (modMerging
imports BACKUP_TAG from it; the leaf-tier and mid-tier resolutions in plans 02-03 are stable now,
LinkingDeployment is the next rung up), then `InstallManager.ts` (largest blast radius — 40 regions,
6 dangerous per RESEARCH §1). Pre/post playbook grep snapshots are mandatory per D-32-11.

Purpose: This is where the phase earns its keep. 6 of the 97 conflict regions in this whole phase are
"dangerous" — silent playbook regressions if mis-resolved — and ALL 6 live in these two files (5 in
InstallManager, 1 in LinkingDeployment @212). The harness must stay GREEN after each commit.

Output: 2 resolved files, 2 SSH-signed commits, 1 SUMMARY commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-CONTEXT.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-RESEARCH.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-PATTERNS.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-VALIDATION.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
@CLAUDE.md
@AGENTS.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-01-SUMMARY.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-03-SUMMARY.md
</context>

<shared_per_task_workflow>
Same scaffold as Plans 02-03; key amendments for this tier:

- Bluebird trap (R5): empirically clear in both files per PATTERNS metadata BUT this is the highest-risk
  tier — `grep -n 'from .bluebird.' <file>` before resolving and again after. If a region introduces
  a bluebird import, default to fork-side for any `:Promise<void>` annotations on async fns to
  avoid TS1064.
- D-32-11 mandatory: pre/post grep snapshot for both files (not just LinkingDeployment).
- Read-confirmation step (RESEARCH §6) for LinkingDeployment.ts: after resolving, OPEN the
  externalChanges() method body and visually confirm `resolvePathCase(dataPath, ...)` call shape is
  intact (3 calls; arg names not renamed).
- Per RESEARCH "Common Pitfalls #1" and "Common Pitfalls #4": dangerous-region map drives stance
  decisions; line numbers may have drifted from v8.0 docs (LinkingDeployment master ref says 523/742/799,
  current tree is 599/818/875 — harness uses ≥3 threshold so drift is fine).
- No `--no-verify` (D-32-10) — resolution finishes before commit.

Per project memory `feedback_minimize_upstream_diff.md`: do not reformat outside conflict regions.
</shared_per_task_workflow>

<tasks>

<task type="auto">
  <name>Task 1: Resolve LinkingDeployment.ts (8 regions; FORK-WINS at @212; D-32-11 pre/post + read pattern)</name>
  <files>src/renderer/src/extensions/mod_management/LinkingDeployment.ts</files>
  <read_first>
    - src/renderer/src/extensions/mod_management/LinkingDeployment.ts (current state — 8 markers)
    - Output of `git show fork/master:src/renderer/src/extensions/mod_management/LinkingDeployment.ts` saved to /tmp/LinkingDeployment.master.ts
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-PATTERNS.md (row 13 — playbook lines 523/742/799 on master; FORK-WINS at region @212 for `errorCodes.add(getErrorCode(err)) ?? "UNKNOWN"`)
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-RESEARCH.md (§6 D-26-03a re-grep + read pattern; §7 dangerous regions)
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-CONTEXT.md (D-32-11 mandatory pre/post + read; D-32-12 single-host invariant)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md (externalChanges / 140a57217 entry)
    - The current externalChanges() method body in the file (locate first, read after resolving) — the read-confirmation step
  </read_first>
  <action>
    Step A — Pre-resolution snapshot (D-32-11):
    ```
    git grep -nE 'resolvePathCase\(dataPath,' src/renderer/src/extensions/mod_management/LinkingDeployment.ts > /tmp/linking-pre.txt
    cat /tmp/linking-pre.txt
    test "$(wc -l < /tmp/linking-pre.txt)" -ge 3
    ```
    Expected per RESEARCH §6: 3 hits at lines 599, 818, 875 (drifted from v8.0 doc's 523/742/799).
    None of these lines lie inside conflict regions on the current tree (regions are at L212, L238,
    L272, L371, L446, L539, L970, L983 per RESEARCH §6) — the executor's job is to NOT BREAK them
    while resolving the conflict regions.

    Step B — Capture master analog: `git show fork/master:src/renderer/src/extensions/mod_management/LinkingDeployment.ts > /tmp/LinkingDeployment.master.ts`

    Step C — Bluebird check: `grep -n 'from .bluebird.' src/renderer/src/extensions/mod_management/LinkingDeployment.ts` — expected 0 hits per PATTERNS metadata. Record actual.

    Step D — Hand-resolve all 8 conflict regions per D-32-02:
    - Region @212: FORK-WINS — fork-only `errorCodes.add(getErrorCode(err) ?? "UNKNOWN")` line for
      Linux error-aggregation. Upstream removed this; we keep it.
    - Regions @238, @272, @371, @446, @539, @970, @983: line-wrap → smaller-diff for each.

    Per RESEARCH "Common Pitfalls #1": no `git checkout --theirs` blanket. Hand-resolve every region.

    Step E — Verify zero markers: `git diff --check src/renderer/src/extensions/mod_management/LinkingDeployment.ts` exits 0.

    Step F — Post-resolution snapshot (D-32-11):
    ```
    git grep -nE 'resolvePathCase\(dataPath,' src/renderer/src/extensions/mod_management/LinkingDeployment.ts > /tmp/linking-post.txt
    PRE_COUNT=$(wc -l < /tmp/linking-pre.txt)
    POST_COUNT=$(wc -l < /tmp/linking-post.txt)
    test "$PRE_COUNT" -eq "$POST_COUNT"   # count unchanged
    test "$POST_COUNT" -ge 3              # ≥3 hits (140a57217 / D-32-12 invariant)
    ```

    Step G — Read-confirmation (RESEARCH §6 — required for D-32-11 belt+suspenders):
    1. Locate externalChanges method: `grep -nE 'public externalChanges|protected externalChanges|^  externalChanges\(' src/renderer/src/extensions/mod_management/LinkingDeployment.ts`
    2. Open the file at the externalChanges body (use Read tool with offset/limit around the
       reported line, ~70-line window).
    3. Visually confirm 3 `resolvePathCase(dataPath, ..., ...)` calls present with the same arg
       shape as `/tmp/LinkingDeployment.master.ts` (e.g. `resolvePathCase(dataPath, relDataPath,
       dirCache)`, `resolvePathCase(dataPath, relOutputPath, this.mReaddirCache)` × 2). If an arg has
       been renamed (e.g. `dataPath` → `targetPath`), the grep would have caught the count drop —
       but if you see something funny in the body, STOP and re-resolve.

    Step H — Bluebird re-check (R5): `grep -n 'from .bluebird.' src/renderer/src/extensions/mod_management/LinkingDeployment.ts` — should still be 0. If the resolution introduced a bluebird import (e.g. via fork-side import block), remove it unless an in-file usage needs the bluebird type.

    Step I — Run harness: `bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check; echo "exit=$?"`. Must exit 0; the gate "140a57217 resolvePathCase(dataPath, …) in LinkingDeployment.ts (≥3 hits)" must be GREEN.

    Step J — Bucket-scoped typecheck for this file's path:
    `pnpm typecheck -F @vortex/renderer 2>&1 | grep "extensions/mod_management/LinkingDeployment" | grep -v TS1185 | wc -l` — must be 0.

    Step K — Commit. Title:
    `resolve(mod-mgmt-v2.0.1): LinkingDeployment.ts — fork-wins on @212 errorCodes block; smaller-diff line-wraps (×7)`

    Body must include (per D-32-09 + D-32-11):
    - Regions resolved: 8 total (1 fork-side @212, 7 smaller-diff: @238, @272, @371, @446, @539, @970, @983)
    - Playbook gates: 140a57217 (sole host per D-32-12)
    - Playbook gates preserved: yes (resolvePathCase(dataPath, ...) hits: pre N, post N, ≥3 threshold)
    - Pre/post grep snapshot diff: counts equal
    - Read-confirmation: externalChanges() method body verified — 3 call shapes intact
    - grep-checkpoint.sh exit: 0 (gates 1-6 GREEN)
    - pnpm typecheck mm-bucket for LinkingDeployment.ts: 0 non-marker errors
    - Bluebird import status: still absent
    - --no-verify: no
    - SSH-sign per CLAUDE.md

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      git diff --check src/renderer/src/extensions/mod_management/LinkingDeployment.ts &amp;&amp; \
      ! grep -q '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/LinkingDeployment.ts &amp;&amp; \
      test "$(grep -cE 'resolvePathCase\(dataPath,' src/renderer/src/extensions/mod_management/LinkingDeployment.ts)" -ge 3 &amp;&amp; \
      bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check &gt;/dev/null &amp;&amp; \
      git log -1 --pretty=%s | grep -q '^resolve(mod-mgmt-v2.0.1): LinkingDeployment.ts' &amp;&amp; \
      git log -1 --show-signature 2&gt;&amp;1 | grep -q 'Good "git" signature' &amp;&amp; \
      MM_ERR=$(pnpm typecheck -F @vortex/renderer 2>&amp;1 | grep "extensions/mod_management/LinkingDeployment" | grep -v TS1185 | wc -l) &amp;&amp; \
      test "$MM_ERR" -eq 0 &amp;&amp; \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - File marker-free; harness `--skip-conflict-check` exit 0 (all 6 playbook gates GREEN incl. 140a57217)
    - `grep -c "resolvePathCase(dataPath," src/renderer/src/extensions/mod_management/LinkingDeployment.ts` returns ≥3
    - Post-resolution count equals pre-resolution count (no drop)
    - Pre-snapshot saved to `/tmp/linking-pre.txt`; post-snapshot saved to `/tmp/linking-post.txt`; both readable for the SUMMARY appendix
    - Read-confirmation done: executor opened externalChanges() method body and verified `resolvePathCase(dataPath, ..., ...)` call shapes match master analog
    - Bucket-scoped typecheck for LinkingDeployment.ts path: 0 non-marker errors
    - Commit is SSH-signed (`git log -1 --show-signature` shows `Good "git" signature`)
    - Commit body lists: 1 region fork-side (@212 errorCodes) + 7 regions smaller-diff (lines list); 140a57217 gate state; pre/post grep counts; read-confirmation done; harness exit 0; typecheck count 0; bluebird absent; --no-verify no
  </acceptance_criteria>
  <done>LinkingDeployment.ts marker-free; 140a57217 invariant intact; @212 fork-only errorCodes preserved; signed commit on branch.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve InstallManager.ts (40 regions; 6 dangerous per RESEARCH §1; FORK-WINS at lines 22, 1165, 3852, 6473)</name>
  <files>src/renderer/src/extensions/mod_management/InstallManager.ts</files>
  <read_first>
    - src/renderer/src/extensions/mod_management/InstallManager.ts (current state — 40 markers)
    - Output of `git show fork/master:src/renderer/src/extensions/mod_management/InstallManager.ts` saved to /tmp/InstallManager.master.ts
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-PATTERNS.md (row 14 — master playbook lines: 96 import resolvePathCase, 169 mergeCaseConflictingDirs, 172 normalizeBackslashPaths, 174 stagingDirHasFiles, 1033-1034 §7a+§7b call pair, 3621-3622 §7a+§7b call pair, 6052 §6 stagingDirHasFiles guard, 6976/7013/7015 §7c-§7d copy-loop comments, 7022 resolvePathCase(tempPath,...))
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-RESEARCH.md (§1 dangerous-region map at lines 22, 1165, 3852, 6473 of CURRENT tree; §1 pre-resolution gate counts §6=2, §7a=5, §7b=3, §7c=2, §7d=1; §7 expected line drift; Code Examples section for §6 and §7a+§7b post-resolution shapes)
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-CONTEXT.md (D-32-02 fork-wins for playbook surface; D-32-11 pre/post grep)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md (§6 stagingDirHasFiles, §7a normalizeBackslashPaths, §7b mergeCaseConflictingDirs, §7c replaceAll, §7d resolvePathCase(tempPath,...))
  </read_first>
  <action>
    This is the largest, riskiest single resolution in the entire phase. Take it carefully.

    Step A — Pre-resolution snapshots (D-32-11 + Pattern S2). Capture per-helper hit counts:
    ```
    grep -cE 'stagingDirHasFiles' src/renderer/src/extensions/mod_management/InstallManager.ts > /tmp/im-pre-stagingDirHasFiles.txt
    grep -cE 'normalizeBackslashPaths' src/renderer/src/extensions/mod_management/InstallManager.ts > /tmp/im-pre-normalizeBackslashPaths.txt
    grep -cE 'mergeCaseConflictingDirs' src/renderer/src/extensions/mod_management/InstallManager.ts > /tmp/im-pre-mergeCaseConflictingDirs.txt
    grep -cE 'resolvePathCase' src/renderer/src/extensions/mod_management/InstallManager.ts > /tmp/im-pre-resolvePathCase.txt
    grep -cE 'replaceAll\("\\\\\\\\","/"' src/renderer/src/extensions/mod_management/InstallManager.ts > /tmp/im-pre-replaceAll.txt
    ```
    Expected pre-counts per RESEARCH §1 "Pre-Resolution Playbook Gate Status":
    - stagingDirHasFiles: ≥2 (import + ≥1 call site)
    - normalizeBackslashPaths: ≥3 (import + ≥2 call sites; research observed 5 incl. commentary)
    - mergeCaseConflictingDirs: ≥3 (import + ≥2 call sites)
    - resolvePathCase: ≥1
    - replaceAll backslash→slash: ≥2 (source + destination)
    Record actual values; these are the floor for post-resolution.

    Step B — Capture master analog: `git show fork/master:src/renderer/src/extensions/mod_management/InstallManager.ts > /tmp/InstallManager.master.ts`

    Step C — Bluebird check: `grep -n 'from .bluebird.' src/renderer/src/extensions/mod_management/InstallManager.ts` — expected 0 hits per PATTERNS metadata. Record. If non-zero, R5 trap is live: prefer fork-side for `:Promise<void>` annotations on async fns.

    Step D — Hand-resolve. The 6 dangerous regions per RESEARCH §1 + PATTERNS row 14:

    | Region @line (current tree) | Stance | Reason |
    |---|---|---|
    | @22 — top-of-file import block | FORK-WINS for the 4 playbook imports (resolvePathCase, mergeCaseConflictingDirs, normalizeBackslashPaths, stagingDirHasFiles); hand-merge any new upstream imports into the fork's import block | All 4 imports must survive |
    | @1165 — `normalizeBackslashPaths(tempPath); mergeCaseConflictingDirs(tempPath);` before buildFileList (FOMOD simulate path) | FORK-WINS | Playbook §7a + §7b call pair |
    | @3852 — same call pair before buildFileList (real install path) | FORK-WINS | Playbook §7a + §7b call pair |
    | @6473 — `if (dep.mod != null && dep.mod.installationPath) { ... stagingDirHasFiles(modStagingPath) ... }` block in doDownload | FORK-WINS | Playbook §6 stagingDirHasFiles guard |

    The other ~34 conflict regions are pure Prettier line-wrap or import-block shuffling per
    RESEARCH §1 — for each, apply smaller-diff stance unless inspection reveals a fork-only line
    (e.g. a Linux-platform branch). When in doubt, default to fork-side and note in commit body.

    Per RESEARCH Code Examples: the post-resolution shape at @6473 must match the §6 stagingDirHasFiles
    guard block (full TypeScript shape in RESEARCH "Code Examples" section). The post-resolution
    shape at @1165 / @3852 must match the `await normalizeBackslashPaths(tempPath); await
    mergeCaseConflictingDirs(tempPath);` pair before `await buildFileList(tempPath);`.

    Per RESEARCH "Common Pitfalls #1": NEVER `git checkout --theirs InstallManager.ts`. The 6
    dangerous regions need explicit fork-side selection.

    Step E — Verify zero markers: `git diff --check src/renderer/src/extensions/mod_management/InstallManager.ts` exits 0.

    Step F — Post-resolution snapshot:
    ```
    grep -cE 'stagingDirHasFiles' src/renderer/src/extensions/mod_management/InstallManager.ts > /tmp/im-post-stagingDirHasFiles.txt
    grep -cE 'normalizeBackslashPaths' src/renderer/src/extensions/mod_management/InstallManager.ts > /tmp/im-post-normalizeBackslashPaths.txt
    grep -cE 'mergeCaseConflictingDirs' src/renderer/src/extensions/mod_management/InstallManager.ts > /tmp/im-post-mergeCaseConflictingDirs.txt
    grep -cE 'resolvePathCase' src/renderer/src/extensions/mod_management/InstallManager.ts > /tmp/im-post-resolvePathCase.txt
    grep -cE 'replaceAll\("\\\\\\\\","/"' src/renderer/src/extensions/mod_management/InstallManager.ts > /tmp/im-post-replaceAll.txt
    ```
    Compare each pre/post pair: post must be ≥ pre. If ANY count drops, STOP — a playbook call site
    was lost. Investigate, fix, recommit. The harness will also catch this on Step H.

    Step G — Bluebird re-check (R5): `grep -n 'from .bluebird.' src/renderer/src/extensions/mod_management/InstallManager.ts` — still 0 expected.

    Step H — Run harness: `bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check; echo "exit=$?"`. Must exit 0; gates §6, §7a, §7b, §7c, §7d, 140a57217 all PASS. If gate fails, the immediately preceding hand-resolution dropped a call site — see Step F counts to localise; fix and recommit (single commit; amend OK if not pushed).

    Step I — Bucket-scoped typecheck for this file's path:
    `pnpm typecheck -F @vortex/renderer 2>&1 | grep "extensions/mod_management/InstallManager" | grep -v TS1185 | wc -l` — must be 0.

    Step J — Commit. Title:
    `resolve(mod-mgmt-v2.0.1): InstallManager.ts — fork-wins on §6 + §7a/b call pairs (4 dangerous regions) + 36 smaller-diff line-wraps`

    Body MUST include (per D-32-09 + D-32-11):
    - Regions resolved: 40 total (4 fork-side at lines 22, 1165, 3852, 6473; ~36 smaller-diff)
    - Per-region stance breakdown for the 4 dangerous regions (line + reason)
    - Playbook gates: §6, §7a, §7b, §7c, §7d (touched by this file)
    - Playbook gates preserved: yes — pre/post counts:
      - stagingDirHasFiles: pre N → post N
      - normalizeBackslashPaths: pre N → post N
      - mergeCaseConflictingDirs: pre N → post N
      - resolvePathCase: pre N → post N
      - replaceAll backslash→slash: pre N → post N
    - grep-checkpoint.sh exit: 0 (gates 1-6 GREEN)
    - pnpm typecheck mm-bucket for InstallManager.ts: 0 non-marker errors
    - Bluebird import status: still absent (R5 dormant)
    - --no-verify: no
    - SSH-sign per CLAUDE.md

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      git diff --check src/renderer/src/extensions/mod_management/InstallManager.ts &amp;&amp; \
      ! grep -q '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/InstallManager.ts &amp;&amp; \
      test "$(grep -c 'stagingDirHasFiles' src/renderer/src/extensions/mod_management/InstallManager.ts)" -ge 1 &amp;&amp; \
      test "$(grep -c 'normalizeBackslashPaths' src/renderer/src/extensions/mod_management/InstallManager.ts)" -ge 3 &amp;&amp; \
      test "$(grep -c 'mergeCaseConflictingDirs' src/renderer/src/extensions/mod_management/InstallManager.ts)" -ge 3 &amp;&amp; \
      test "$(grep -c 'resolvePathCase' src/renderer/src/extensions/mod_management/InstallManager.ts)" -ge 1 &amp;&amp; \
      bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check &gt;/dev/null &amp;&amp; \
      git log -1 --pretty=%s | grep -q '^resolve(mod-mgmt-v2.0.1): InstallManager.ts' &amp;&amp; \
      git log -1 --show-signature 2&gt;&amp;1 | grep -q 'Good "git" signature' &amp;&amp; \
      MM_ERR=$(pnpm typecheck -F @vortex/renderer 2>&amp;1 | grep "extensions/mod_management/InstallManager" | grep -v TS1185 | wc -l) &amp;&amp; \
      test "$MM_ERR" -eq 0 &amp;&amp; \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - File marker-free; harness `--skip-conflict-check` exit 0 (all 6 playbook gates GREEN)
    - `grep -c stagingDirHasFiles InstallManager.ts` ≥1 (§6) — matches research baseline of ≥2 (import + call site)
    - `grep -c normalizeBackslashPaths InstallManager.ts` ≥3 (§7a — import + ≥2 call sites)
    - `grep -c mergeCaseConflictingDirs InstallManager.ts` ≥3 (§7b — import + ≥2 call sites)
    - `grep -c resolvePathCase InstallManager.ts` ≥1 (§7d)
    - For each helper, post-resolution count ≥ pre-resolution count (no drops; recorded in commit body)
    - Bucket-scoped typecheck for InstallManager.ts path: 0 non-marker errors
    - Commit is SSH-signed
    - Commit body lists: 4 regions fork-side at lines 22/1165/3852/6473 with reasons, ~36 regions smaller-diff, per-helper pre/post grep counts, harness gate-by-gate state, typecheck count, bluebird status, --no-verify status
  </acceptance_criteria>
  <done>InstallManager.ts marker-free; §6/§7a/§7b/§7c/§7d invariants intact and pre/post counts recorded; signed commit on branch.</done>
</task>

<task type="auto">
  <name>Task 3: Plan SUMMARY commit</name>
  <files>.planning/phases/32-mod-management-hot-zone-v2-0-1/32-04-SUMMARY.md</files>
  <read_first>
    - The 2 commit messages from this plan: `git log --oneline 32-03..HEAD -- src/renderer/src/extensions/mod_management/`
    - $HOME/.claude/get-shit-done/templates/summary.md
    - /tmp/linking-pre.txt + /tmp/linking-post.txt (140a57217 snapshots)
    - /tmp/im-pre-*.txt + /tmp/im-post-*.txt (per-helper snapshots)
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-VALIDATION.md
  </read_first>
  <action>
    Write `32-04-SUMMARY.md` summarizing the playbook-heavy resolution. Required sections:
    - Outcome: 2/2 playbook-heavy files resolved on `v8.1/config-bucket`
    - Per-file table: file, region count, fork-side region count + lines, smaller-diff count, commit SHA, harness state, typecheck count
    - Playbook gates final state (skip-mode harness output): §6 ✓, §7a ✓, §7b ✓, §7c ✓, §7d ✓, 140a57217 ✓
    - Pre/post helper counts (full table from /tmp snapshots) — both files
    - Read-confirmation status for LinkingDeployment.ts externalChanges() body (yes/no)
    - Issues encountered (if any commit needed amend; if any pre/post count temporarily dropped)
    - Affects: Plan 05 (barrel index.ts) compiles against fully-resolved sibling files
    - Provides: 2 playbook-heavy files marker-free; 4 dangerous regions in InstallManager + 1 in LinkingDeployment locked fork-side

    Add via `git add -f`. Commit title: `docs(32-04): summarize playbook-heavy resolution (2 files, 48 regions, 5 dangerous regions fork-side)`.

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      test -f .planning/phases/32-mod-management-hot-zone-v2-0-1/32-04-SUMMARY.md &amp;&amp; \
      bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check &gt;/dev/null &amp;&amp; \
      git log -1 --pretty=%s | grep -q '^docs(32-04): summarize playbook-heavy' &amp;&amp; \
      git log -1 --show-signature 2&gt;&amp;1 | grep -q 'Good "git" signature' &amp;&amp; \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - `32-04-SUMMARY.md` exists, committed via `git add -f`, SSH-signed
    - 2 `resolve(mod-mgmt-v2.0.1):` commits in this plan + 1 docs commit
    - Final harness skip-mode run exits 0 with all 6 playbook gates GREEN
    - mod_management/ files remaining with conflict markers: 1 (index.ts — Plan 05 territory)
  </acceptance_criteria>
  <done>Playbook-heavy resolution complete; summary committed; branch ready for Plan 05 (barrel).</done>
</task>

</tasks>

<verification>
After all tasks:
- `bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check` exits 0 (gates 1-6 GREEN)
- `git grep -l '^<<<<<<< ' src/renderer/src/extensions/mod_management/ | wc -l` returns 1 (only index.ts left)
- `grep -c "resolvePathCase(dataPath," src/renderer/src/extensions/mod_management/LinkingDeployment.ts` ≥3
- `grep -c stagingDirHasFiles src/renderer/src/extensions/mod_management/InstallManager.ts` ≥1
- `grep -c normalizeBackslashPaths src/renderer/src/extensions/mod_management/InstallManager.ts` ≥3
- `grep -c mergeCaseConflictingDirs src/renderer/src/extensions/mod_management/InstallManager.ts` ≥3
- `pnpm typecheck -F @vortex/renderer 2>&1 | grep "extensions/mod_management/" | grep -v TS1185 | wc -l` returns 0
- All 2+1 commits SSH-signed
</verification>

<success_criteria>

- LinkingDeployment.ts: 8 regions resolved, @212 fork-only errorCodes preserved, 140a57217 single-host invariant preserved (≥3 hits, count unchanged pre/post)
- InstallManager.ts: 40 regions resolved, 4 dangerous regions explicitly fork-side, every playbook helper count ≥ pre-resolution baseline
- 2 + 1 SSH-signed commits on `v8.1/config-bucket`
- Harness all 6 gates GREEN after every commit
- D-32-11 pre/post + read pattern executed and recorded
- Plan 05 unblocked
  </success_criteria>

<output>
Create `.planning/phases/32-mod-management-hot-zone-v2-0-1/32-04-SUMMARY.md` per Task 3.
</output>
