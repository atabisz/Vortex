---
phase: 32-mod-management-hot-zone-v2-0-1
plan: 02
type: execute
wave: 1
depends_on:
    - 32-01
files_modified:
    - src/renderer/src/extensions/mod_management/NotificationAggregator.ts
    - src/renderer/src/extensions/mod_management/util/VersionFilter.tsx
    - src/renderer/src/extensions/mod_management/util/removeMods.ts
    - src/renderer/src/extensions/mod_management/util/activationStore.ts
    - src/renderer/src/extensions/mod_management/util/externalChanges.ts
    - src/renderer/src/extensions/mod_management/util/deploy.ts
    - src/renderer/src/extensions/mod_management/stagingDirectory.ts
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-02-SUMMARY.md
autonomous: true
requirements:
    - SYNC-32a
must_haves:
    truths:
        - "All 7 leaf-tier files have zero conflict markers after this plan"
        - "Fork-only Wine-era detection block in util/activationStore.ts is intact (HEAD-side preserved per D-32-02)"
        - "Each leaf file resolution lands as its own SSH-signed commit per D-32-08"
        - "After every commit the harness in --skip-conflict-check mode exits 0 (gates 1-6 still GREEN)"
        - "After every commit the bucket-scoped typecheck error count for the resolved file's path is 0 non-marker errors (TS1185 excluded)"
    artifacts:
        - path: "src/renderer/src/extensions/mod_management/NotificationAggregator.ts"
          provides: "Notification aggregator (no playbook surface; upstream-wins import addition)"
        - path: "src/renderer/src/extensions/mod_management/util/VersionFilter.tsx"
          provides: "Version filter utility (no playbook; smaller-diff line-wrap)"
        - path: "src/renderer/src/extensions/mod_management/util/removeMods.ts"
          provides: "Remove-mods util (no playbook; smaller-diff)"
        - path: "src/renderer/src/extensions/mod_management/util/activationStore.ts"
          provides: "Activation store with fork-only Wine-era detection PRESERVED"
          contains: "isWineEraManifest"
        - path: "src/renderer/src/extensions/mod_management/util/externalChanges.ts"
          provides: "External-changes util (NOT a 140a57217 host per D-32-12)"
        - path: "src/renderer/src/extensions/mod_management/util/deploy.ts"
          provides: "Deploy util (no playbook; smaller-diff)"
        - path: "src/renderer/src/extensions/mod_management/stagingDirectory.ts"
          provides: "Staging-directory module (NOT a §6 host — that lives in InstallManager.ts:doDownload)"
    key_links:
        - from: "util/activationStore.ts"
          to: "Wine/Proton manifest detection in deployment loader"
          via: "isWineEraManifest export + queryPurgeWineEra call"
          pattern: "isWineEraManifest"
---

<objective>
Resolve all 7 leaf-tier conflict files in dependency-aware order per RESEARCH §3 and PATTERNS table
rows 1-7. Each file gets its own atomic SSH-signed commit per D-32-08, with the per-commit body
recording region split, harness exit, and bucket-scoped typecheck count per D-32-09.

Purpose: Leaves resolved before mid/playbook-heavy/barrel files so downstream tiers compile against
clean import targets. Fork-only Wine-era block in `util/activationStore.ts` is the only fork-surface
in this plan and must survive verbatim per D-32-02.

Output: 7 resolved files (zero conflict markers each), 7 SSH-signed commits on `v8.1/config-bucket`,
1 SUMMARY committed at end (alongside Task 7's commit or as the 8th commit — see Task 8).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-CONTEXT.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-RESEARCH.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-PATTERNS.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-VALIDATION.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
@CLAUDE.md
@AGENTS.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-01-SUMMARY.md
</context>

<shared_per_task_workflow>
Every leaf task follows this sequence — referenced from each task's `<action>` to keep them concise.
The shared steps are documented here once.

1. **Capture master analog:** `git show fork/master:<path> > /tmp/<basename>.master.<ext>` (Pattern S1).
2. **Read the file with markers and the master analog side-by-side.** Identify each conflict region.
   For each region apply D-32-02 stance:
    - fork-wins for any line touching playbook §6/§7/140a57217 or fork-only Linux-platform branches
    - upstream-wins for new feature scaffolding outside playbook surface
    - smaller-diff side otherwise (Prettier line-wrap; semantically equivalent)
3. **Hand-edit to remove all markers in the file** — no `git checkout --ours/--theirs` blanket per D-32-03.
4. **Verify file has zero markers:** `git diff --check src/renderer/src/extensions/mod_management/<file>` exits 0.
5. **Pre/post playbook grep snapshot (Pattern S2)** — only meaningful for files with playbook surface
   (in this plan: `util/activationStore.ts` for Wine-era; the rest have no playbook surface).
   For activationStore: `grep -nE 'isWineEraManifest|queryPurgeWineEra' src/renderer/src/extensions/mod_management/util/activationStore.ts > /tmp/activationStore.post.txt` and confirm same hits as master analog.
6. **Run harness skip-mode:** `bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check; echo "exit=$?"`. Must exit 0.
7. **Bucket-scoped typecheck count (Pattern S4):**
    ```
    pnpm typecheck -F @vortex/renderer 2>&1 | grep "extensions/mod_management/<basename>" | grep -v TS1185 | wc -l
    ```
    For non-ModList leaf files: must be 0 immediately after commit. (ModList.tsx is mid-tier — Plan 03.)
8. **Stage + commit (signed, no `--no-verify`):**
    ```
    git add <path>
    git commit -m "resolve(mod-mgmt-v2.0.1): <basename> — <one-line stance from PATTERNS row>"
    ```
    Body MUST include (D-32-09):
    - Regions resolved: total, fork-side count + lines, upstream-side count + lines, smaller-diff count
    - Playbook gates touched: list (or "none" for files with no playbook surface)
    - Playbook gates preserved: yes/no per gate touched (or "n/a")
    - grep-checkpoint.sh exit: 0
    - pnpm typecheck mm-bucket for this file's path: 0 non-marker errors
    - --no-verify: no
      Use casual project voice per `feedback_casual_voice.md` — short, direct.
9. **Verify signed:** `git log -1 --show-signature 2>&1 | grep -q 'Good "git" signature'`.
10. **DO NOT push** — operator pushes at phase end via inline SSH URL per `feedback_git_push_ssh.md`.

Bluebird Promise trap reminder (Pitfall #5 / R5): `grep -n 'from .bluebird.' <file>` — for all 7
leaf files this returns ZERO per PATTERNS verification, so the trap is dormant. If a region
unexpectedly introduces a bluebird import, flag and prefer fork-side.

Husky `--no-verify` (D-32-10): only allowed if the pre-commit hook genuinely cannot parse a
partial-marker file. Resolution finishes BEFORE commit, so this should not arise.

`.planning/` paths are gitignored — Task 8 uses `git add -f` for the SUMMARY.
</shared_per_task_workflow>

<tasks>

<task type="auto">
  <name>Task 1: Resolve NotificationAggregator.ts (1 region; pure import addition)</name>
  <files>src/renderer/src/extensions/mod_management/NotificationAggregator.ts</files>
  <read_first>
    - src/renderer/src/extensions/mod_management/NotificationAggregator.ts (current state with markers)
    - Output of `git show fork/master:src/renderer/src/extensions/mod_management/NotificationAggregator.ts` saved to /tmp/NotificationAggregator.master.ts
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-PATTERNS.md (row 1 — analog cmd, stance, risk)
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-RESEARCH.md (Conflict-Region Shape Characterization table — row "NotificationAggregator.ts")
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-CONTEXT.md (D-32-02, D-32-08, D-32-09, D-32-10)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md (sanity check — confirm no §6/§7/140a57217 surface in this file)
  </read_first>
  <action>
    Follow the shared per-task workflow above (steps 1-10).

    Per PATTERNS row 1 / RESEARCH §1: the single conflict is a pure import addition for
    `getErrorMessageOrDefault`. No playbook surface in this file. Stance: upstream-wins (or
    hand-merge import block to take both sides' imports if fork-side adds anything that upstream
    drops — read the master analog to confirm; default expectation is upstream-wins).

    Commit title: `resolve(mod-mgmt-v2.0.1): NotificationAggregator.ts — upstream import addition (getErrorMessageOrDefault)`

    Step 7 typecheck filter: `... | grep "extensions/mod_management/NotificationAggregator" | grep -v TS1185 | wc -l` must be 0.

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      git diff --check src/renderer/src/extensions/mod_management/NotificationAggregator.ts &amp;&amp; \
      ! grep -q '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/NotificationAggregator.ts &amp;&amp; \
      bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check &gt;/dev/null &amp;&amp; \
      git log -1 --pretty=%s | grep -q '^resolve(mod-mgmt-v2.0.1): NotificationAggregator.ts' &amp;&amp; \
      git log -1 --show-signature 2&gt;&amp;1 | grep -q 'Good "git" signature' &amp;&amp; \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - `git diff --check` exits 0 on this file
    - Zero `<<<<<<< `/`>>>>>>> ` markers in this file
    - harness `--skip-conflict-check` exits 0 (gates 1-6 GREEN)
    - bucket-scoped typecheck for this file's path: 0 non-marker errors (`TS1185` excluded)
    - One commit with title matching `resolve(mod-mgmt-v2.0.1): NotificationAggregator.ts —` prefix
    - Commit is SSH-signed
    - Commit body lists regions split (1 region; upstream-side or hand-merge), playbook gates ("none — no playbook surface in this file"), harness exit 0, typecheck count 0, --no-verify no
  </acceptance_criteria>
  <done>NotificationAggregator.ts is marker-free and committed.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve util/VersionFilter.tsx (1 region; Prettier line-wrap)</name>
  <files>src/renderer/src/extensions/mod_management/util/VersionFilter.tsx</files>
  <read_first>
    - src/renderer/src/extensions/mod_management/util/VersionFilter.tsx (current state with markers)
    - Output of `git show fork/master:src/renderer/src/extensions/mod_management/util/VersionFilter.tsx` saved to /tmp/VersionFilter.master.tsx
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-PATTERNS.md (row 2)
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-RESEARCH.md (Conflict-Region Shape Characterization — VersionFilter row)
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-CONTEXT.md (D-32-02 stance hierarchy)
  </read_first>
  <action>
    Follow shared per-task workflow.

    Per PATTERNS row 2 / RESEARCH §1: single conflict is pure Prettier line-wrap. Stance:
    smaller-diff (semantically equivalent either side; pick the side closer to the master analog
    to minimise diff vs `fork/master` per project memory `feedback_minimize_upstream_diff.md`).

    Commit title: `resolve(mod-mgmt-v2.0.1): util/VersionFilter.tsx — smaller-diff line-wrap`

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      git diff --check src/renderer/src/extensions/mod_management/util/VersionFilter.tsx &amp;&amp; \
      ! grep -q '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/util/VersionFilter.tsx &amp;&amp; \
      bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check &gt;/dev/null &amp;&amp; \
      git log -1 --pretty=%s | grep -q '^resolve(mod-mgmt-v2.0.1): util/VersionFilter.tsx' &amp;&amp; \
      git log -1 --show-signature 2&gt;&amp;1 | grep -q 'Good "git" signature' &amp;&amp; \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - File marker-free, harness GREEN, typecheck filter for this path = 0 non-marker errors, signed commit on branch
    - Commit body records single region as smaller-diff
  </acceptance_criteria>
  <done>util/VersionFilter.tsx marker-free and committed.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve util/removeMods.ts (1 region; line-wrap)</name>
  <files>src/renderer/src/extensions/mod_management/util/removeMods.ts</files>
  <read_first>
    - src/renderer/src/extensions/mod_management/util/removeMods.ts (current state)
    - Output of `git show fork/master:src/renderer/src/extensions/mod_management/util/removeMods.ts` saved to /tmp/removeMods.master.ts
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-PATTERNS.md (row 3)
  </read_first>
  <action>
    Follow shared workflow. Single line-wrap conflict, no playbook surface. Stance: smaller-diff.

    Commit title: `resolve(mod-mgmt-v2.0.1): util/removeMods.ts — smaller-diff line-wrap`

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      git diff --check src/renderer/src/extensions/mod_management/util/removeMods.ts &amp;&amp; \
      ! grep -q '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/util/removeMods.ts &amp;&amp; \
      bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check &gt;/dev/null &amp;&amp; \
      git log -1 --pretty=%s | grep -q '^resolve(mod-mgmt-v2.0.1): util/removeMods.ts' &amp;&amp; \
      git log -1 --show-signature 2&gt;&amp;1 | grep -q 'Good "git" signature' &amp;&amp; \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - File marker-free, harness GREEN, typecheck filter for this path = 0 non-marker errors, signed commit
    - Commit body records single region smaller-diff
  </acceptance_criteria>
  <done>util/removeMods.ts marker-free and committed.</done>
</task>

<task type="auto">
  <name>Task 4: Resolve util/activationStore.ts (3 regions; FORK-WINS for Wine-era block)</name>
  <files>src/renderer/src/extensions/mod_management/util/activationStore.ts</files>
  <read_first>
    - src/renderer/src/extensions/mod_management/util/activationStore.ts (current state with markers)
    - Output of `git show fork/master:src/renderer/src/extensions/mod_management/util/activationStore.ts` saved to /tmp/activationStore.master.ts
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-PATTERNS.md (row 4 — playbook lines 188, 192-217, 268, 492-493 on master)
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-RESEARCH.md (Conflict-Region Shape Characterization — activationStore row: "1 line-wrap + 2 surrounding fork-only Wine-era detection function")
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-CONTEXT.md (D-32-02 fork-wins for fork-only blocks)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md (Wine-era / Proton entries — fork-only Linux invariant)
  </read_first>
  <action>
    Follow shared workflow with extra care: this is the only leaf file with fork-only surface.

    Per PATTERNS row 4 / RESEARCH §1: 3 regions. The 2 regions touching the Wine-era block
    (`isWineEraManifest`, `queryPurgeWineEra`) are FORK-WINS — fork side has the Linux-only
    Wine/Proton manifest detection, upstream side does not. The 1 line-wrap region is smaller-diff.

    Pre-resolution snapshot (Pattern S2 specialised for this file):
    ```
    grep -nE 'isWineEraManifest|queryPurgeWineEra|Wine-era' src/renderer/src/extensions/mod_management/util/activationStore.ts > /tmp/activationStore.pre.txt
    grep -nE 'isWineEraManifest|queryPurgeWineEra|Wine-era' /tmp/activationStore.master.ts > /tmp/activationStore.master.hits.txt
    ```
    Hand-resolve. After resolution:
    ```
    grep -nE 'isWineEraManifest|queryPurgeWineEra|Wine-era' src/renderer/src/extensions/mod_management/util/activationStore.ts > /tmp/activationStore.post.txt
    test "$(wc -l < /tmp/activationStore.post.txt)" -ge "$(wc -l < /tmp/activationStore.master.hits.txt)"
    ```
    Hit count must be ≥ master analog count. If less, a fork-only line was lost — STOP, undo, re-resolve.

    Commit title: `resolve(mod-mgmt-v2.0.1): util/activationStore.ts — fork-wins on Wine-era detection`

    Body must call out: "fork-side: 2 regions (isWineEraManifest function block + queryPurgeWineEra
    call site) — Wine/Proton fork-only Linux invariant per VORTEX-LINUX-MERGE-PLAYBOOK; smaller-diff:
    1 region (line-wrap)."

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      git diff --check src/renderer/src/extensions/mod_management/util/activationStore.ts &amp;&amp; \
      ! grep -q '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/util/activationStore.ts &amp;&amp; \
      grep -c 'isWineEraManifest' src/renderer/src/extensions/mod_management/util/activationStore.ts | grep -qE '^[1-9]' &amp;&amp; \
      bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check &gt;/dev/null &amp;&amp; \
      git log -1 --pretty=%s | grep -q '^resolve(mod-mgmt-v2.0.1): util/activationStore.ts' &amp;&amp; \
      git log -1 --show-signature 2&gt;&amp;1 | grep -q 'Good "git" signature' &amp;&amp; \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - File marker-free, harness GREEN, typecheck filter for this path = 0 non-marker errors, signed commit
    - `grep -c isWineEraManifest src/renderer/src/extensions/mod_management/util/activationStore.ts` ≥ 1 (preferably matches master count of 2 — definition + call site)
    - `grep -c queryPurgeWineEra` returns same as master
    - Commit body records: 2 regions fork-side (Wine-era), 1 region smaller-diff (line-wrap); explicitly cites VORTEX-LINUX-MERGE-PLAYBOOK Wine-era preservation
  </acceptance_criteria>
  <done>util/activationStore.ts marker-free, Wine-era fork-only detection intact, committed.</done>
</task>

<task type="auto">
  <name>Task 5: Resolve util/externalChanges.ts (3 regions; line-wrap; NOT a 140a57217 host)</name>
  <files>src/renderer/src/extensions/mod_management/util/externalChanges.ts</files>
  <read_first>
    - src/renderer/src/extensions/mod_management/util/externalChanges.ts (current state)
    - Output of `git show fork/master:src/renderer/src/extensions/mod_management/util/externalChanges.ts` saved to /tmp/externalChanges.master.ts
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-PATTERNS.md (row 5 — explicitly NOT a 140a57217 host per D-32-12)
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-CONTEXT.md (D-32-12 single-host invariant)
  </read_first>
  <action>
    Follow shared workflow. Per PATTERNS row 5 / RESEARCH §1: 3 line-wrap regions, no playbook
    surface. Stance: smaller-diff for all 3.

    Sanity-check D-32-12: `grep -nE 'resolvePathCase\(dataPath,' src/renderer/src/extensions/mod_management/util/externalChanges.ts` MUST return zero hits both before and after resolution. If hits appear, STOP — D-32-12 invariant is violated and the harness should NOT be extended; instead surface to operator.

    Commit title: `resolve(mod-mgmt-v2.0.1): util/externalChanges.ts — smaller-diff line-wraps (×3)`

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      git diff --check src/renderer/src/extensions/mod_management/util/externalChanges.ts &amp;&amp; \
      ! grep -q '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/util/externalChanges.ts &amp;&amp; \
      test "$(grep -cE 'resolvePathCase\(dataPath,' src/renderer/src/extensions/mod_management/util/externalChanges.ts)" -eq 0 &amp;&amp; \
      bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check &gt;/dev/null &amp;&amp; \
      git log -1 --pretty=%s | grep -q '^resolve(mod-mgmt-v2.0.1): util/externalChanges.ts' &amp;&amp; \
      git log -1 --show-signature 2&gt;&amp;1 | grep -q 'Good "git" signature' &amp;&amp; \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - File marker-free, harness GREEN, typecheck filter for this path = 0 non-marker errors, signed commit
    - `resolvePathCase(dataPath,` count = 0 (D-32-12 single-host invariant maintained)
    - Commit body records 3 regions all smaller-diff; explicit "D-32-12 single-host invariant intact (no playbook surface in this file)"
  </acceptance_criteria>
  <done>util/externalChanges.ts marker-free; D-32-12 invariant verified intact; committed.</done>
</task>

<task type="auto">
  <name>Task 6: Resolve util/deploy.ts (2 regions; line-wrap)</name>
  <files>src/renderer/src/extensions/mod_management/util/deploy.ts</files>
  <read_first>
    - src/renderer/src/extensions/mod_management/util/deploy.ts (current state)
    - Output of `git show fork/master:src/renderer/src/extensions/mod_management/util/deploy.ts` saved to /tmp/deploy.master.ts
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-PATTERNS.md (row 6)
  </read_first>
  <action>
    Follow shared workflow. Per PATTERNS row 6: 2 line-wrap regions, no playbook surface. Stance:
    smaller-diff for both.

    Commit title: `resolve(mod-mgmt-v2.0.1): util/deploy.ts — smaller-diff line-wraps (×2)`

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      git diff --check src/renderer/src/extensions/mod_management/util/deploy.ts &amp;&amp; \
      ! grep -q '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/util/deploy.ts &amp;&amp; \
      bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check &gt;/dev/null &amp;&amp; \
      git log -1 --pretty=%s | grep -q '^resolve(mod-mgmt-v2.0.1): util/deploy.ts' &amp;&amp; \
      git log -1 --show-signature 2&gt;&amp;1 | grep -q 'Good "git" signature' &amp;&amp; \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - File marker-free, harness GREEN, typecheck filter for this path = 0 non-marker errors, signed commit
    - Commit body records 2 regions smaller-diff
  </acceptance_criteria>
  <done>util/deploy.ts marker-free and committed.</done>
</task>

<task type="auto">
  <name>Task 7: Resolve stagingDirectory.ts (1 region; import addition; NOT a §6 host)</name>
  <files>src/renderer/src/extensions/mod_management/stagingDirectory.ts</files>
  <read_first>
    - src/renderer/src/extensions/mod_management/stagingDirectory.ts (current state)
    - Output of `git show fork/master:src/renderer/src/extensions/mod_management/stagingDirectory.ts` saved to /tmp/stagingDirectory.master.ts
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-PATTERNS.md (row 7 — note "NOT a §6 host — that is InstallManager.ts:doDownload")
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-RESEARCH.md (Conflict-Region Shape Characterization — stagingDirectory row: "1 import addition")
  </read_first>
  <action>
    Follow shared workflow. Per PATTERNS row 7 / RESEARCH §1: 1 region, pure import addition for
    `log` from `../../logging`. No playbook surface in this file (the §6 stagingDirHasFiles host is
    `InstallManager.ts:doDownload`, resolved in Plan 04 Task 2). Stance: upstream-wins for the
    import addition (or hand-merge import block if fork-side has additional imports — read master
    analog first to confirm).

    Sanity-check: `grep -nE 'stagingDirHasFiles' src/renderer/src/extensions/mod_management/stagingDirectory.ts` should return zero hits both before and after — §6 lives in InstallManager.ts.

    Commit title: `resolve(mod-mgmt-v2.0.1): stagingDirectory.ts — upstream import addition (log)`

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      git diff --check src/renderer/src/extensions/mod_management/stagingDirectory.ts &amp;&amp; \
      ! grep -q '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/stagingDirectory.ts &amp;&amp; \
      bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check &gt;/dev/null &amp;&amp; \
      git log -1 --pretty=%s | grep -q '^resolve(mod-mgmt-v2.0.1): stagingDirectory.ts' &amp;&amp; \
      git log -1 --show-signature 2&gt;&amp;1 | grep -q 'Good "git" signature' &amp;&amp; \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - File marker-free, harness GREEN, typecheck filter for this path = 0 non-marker errors, signed commit
    - Commit body records 1 region upstream-side (or hand-merge), notes "no §6 surface here — InstallManager owns that"
  </acceptance_criteria>
  <done>stagingDirectory.ts marker-free and committed.</done>
</task>

<task type="auto">
  <name>Task 8: Plan SUMMARY commit</name>
  <files>.planning/phases/32-mod-management-hot-zone-v2-0-1/32-02-SUMMARY.md</files>
  <read_first>
    - All 7 commit messages from this plan: `git log --oneline 32-01..HEAD -- src/renderer/src/extensions/mod_management/`
    - $HOME/.claude/get-shit-done/templates/summary.md
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-VALIDATION.md (Validation Sign-Off section — plan-level updates)
  </read_first>
  <action>
    Write `32-02-SUMMARY.md` summarizing the leaf-tier resolution: 7 files done, region split per
    file (matching what each commit body recorded), aggregate harness exit (must be 0 in skip mode at
    every commit), aggregate typecheck delta (mod_management/ non-marker baseline 260 → still 260
    expected, since these files contributed 0 to the baseline; ModList.tsx is still the JSX-cascade
    source until Plan 03).

    Required sections (per template):
    - Outcome: 7/7 leaf files resolved on `v8.1/config-bucket`
    - Per-file table: file, region count, fork/upstream/smaller-diff split, commit SHA
    - Playbook gates: §6/§7a-d/140a57217 still GREEN (verify with one final harness run)
    - Issues encountered (if any)
    - Affects: dependent files (mid-tier files in Plan 03 now have clean leaf imports)
    - Provides: 7 leaf files marker-free
    - Patterns: confirms shared per-task workflow worked end-to-end

    Add SUMMARY to git via `git add -f` (`.planning/` is gitignored per project memory). Commit
    title: `docs(32-02): summarize leaf-tier resolution (7 files, 12 regions)`. Body brief:
    "Phase 32 Plan 02 complete. 7 leaf files resolved across 12 regions. All harness gates GREEN
    in --skip-conflict-check mode after every commit. Wine-era fork-only block in
    util/activationStore.ts intact. mod_management non-marker typecheck baseline unchanged at ~260
    (all in views/ModList.tsx — Plan 03 territory)."

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      test -f .planning/phases/32-mod-management-hot-zone-v2-0-1/32-02-SUMMARY.md &amp;&amp; \
      bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check &gt;/dev/null &amp;&amp; \
      test "$(git log --oneline --grep='resolve(mod-mgmt-v2.0.1):' v8.1/config-bucket~10..HEAD | wc -l)" -ge 7 &amp;&amp; \
      git log -1 --pretty=%s | grep -q '^docs(32-02): summarize leaf-tier' &amp;&amp; \
      git log -1 --show-signature 2&gt;&amp;1 | grep -q 'Good "git" signature' &amp;&amp; \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - `32-02-SUMMARY.md` exists, is committed (using `git add -f`), is SSH-signed
    - Branch HEAD has at least 7 `resolve(mod-mgmt-v2.0.1):` commits since plan 01 + 1 docs commit (8 total)
    - Final harness run in skip mode exits 0
    - mod_management/ files remaining with conflict markers: 8 (15 - 7 done)
  </acceptance_criteria>
  <done>Leaf-tier resolution complete; summary committed; branch ready for Plan 03 (mid-tier).</done>
</task>

</tasks>

<verification>
After all tasks:
- `bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check` exits 0
- `git grep -l '^<<<<<<< ' src/renderer/src/extensions/mod_management/ | wc -l` returns 8 (15 - 7 leaves resolved)
- `git log --oneline --grep='resolve(mod-mgmt-v2.0.1):' | wc -l` ≥ 7
- Every resolve commit SSH-signed (`git log --show-signature` shows `Good "git" signature` on each)
- `pnpm typecheck -F @vortex/renderer 2>&1 | grep "extensions/mod_management/" | grep -v TS1185 | wc -l` ≤ baseline (260) — note: ModList.tsx still contributes the bulk
</verification>

<success_criteria>

- 7 leaf files marker-free
- 7 + 1 SSH-signed commits on `v8.1/config-bucket`
- All commit bodies record gates / region split / harness exit / typecheck count per D-32-09
- Wine-era fork-only Linux invariant in util/activationStore.ts preserved (verified via post-resolution grep ≥ master count)
- D-32-12 single-host invariant verified for util/externalChanges.ts (zero `resolvePathCase(dataPath,` hits)
- Plan 03 unblocked
  </success_criteria>

<output>
Create `.planning/phases/32-mod-management-hot-zone-v2-0-1/32-02-SUMMARY.md` per Task 8.
</output>
