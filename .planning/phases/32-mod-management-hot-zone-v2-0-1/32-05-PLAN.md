---
phase: 32-mod-management-hot-zone-v2-0-1
plan: 05
type: execute
wave: 4
depends_on:
    - 32-04
files_modified:
    - src/renderer/src/extensions/mod_management/index.ts
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-05-SUMMARY.md
autonomous: true
requirements:
    - SYNC-32a
must_haves:
    truths:
        - "src/renderer/src/extensions/mod_management/index.ts has zero conflict markers"
        - "All 18 regions resolved with smaller-diff stance (line-wrap + import-block reorg, NOT a playbook host)"
        - "Re-exports from sibling modules (resolved in plans 02-04) remain wired — extension still registers reducers/actions/components"
        - "Bluebird import remains absent (R5 stays latent — empirically verified in PATTERNS row 15)"
        - "Harness in --skip-conflict-check stays GREEN; bucket-scoped typecheck for index.ts shows zero non-marker errors"
        - "One SSH-signed commit landed per D-32-08; SUMMARY commit landed per D-32-09 norm"
    artifacts:
        - path: "src/renderer/src/extensions/mod_management/index.ts"
          provides: "mod_management extension registration entry — re-exports + IExtensionContext init"
          contains: "registerReducer, registerMainPage"
        - path: ".planning/phases/32-mod-management-hot-zone-v2-0-1/32-05-SUMMARY.md"
          provides: "Wave 4 closeout — barrel resolution, region tally, harness/typecheck snapshots"
    key_links:
        - from: "src/renderer/src/extensions/mod_management/index.ts"
          to: "sibling modules resolved in plans 02-04 (InstallManager, LinkingDeployment, eventHandlers, util/*, views/*)"
          via: "import + re-export"
          pattern: "from '\\./"
---

<objective>
Wave 4 barrel resolution. The single file in scope, `src/renderer/src/extensions/mod_management/index.ts`,
is the extension registration entry point — it re-exports from every sibling already resolved in plans 02-04.
By landing this last, every imported symbol is stable; no chasing moving targets.

Purpose: Per PATTERNS row 15, all 18 regions are line-wrap or import-block reorg — there are NO playbook
call sites in this file (the extension `init` body wires reducers/actions/components, but the playbook
helpers themselves live in the siblings). Stance: smaller-diff for all 18 regions per D-32-02.

Output: 1 resolved file, 1 SSH-signed file commit, 1 SSH-signed SUMMARY commit on `v8.1/config-bucket`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-CONTEXT.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-RESEARCH.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-PATTERNS.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-VALIDATION.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-01-SUMMARY.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-02-SUMMARY.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-03-SUMMARY.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-04-SUMMARY.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve mod_management/index.ts (18 regions, all smaller-diff)</name>
  <files>src/renderer/src/extensions/mod_management/index.ts</files>
  <read_first>
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-PATTERNS.md (row 15 — index.ts: 18 regions, all smaller-diff, NOT a playbook host; row 8 — modMerging imports BACKUP_TAG from LinkingDeployment, already verified stable in plan 03/04)
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-RESEARCH.md (sections 1, 2, 8 R5 — bluebird trap latent in this file)
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-CONTEXT.md (D-32-02 stance hierarchy, D-32-08 atomic commit, D-32-10 husky, D-32-15 branch)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md (sanity scan only — confirm index.ts has no playbook surface)
    - The current working-tree file `src/renderer/src/extensions/mod_management/index.ts` (read in full — region count assertion expects 18 conflict regions)
    - The master analog blob (Step A captures it to /tmp)
  </read_first>
  <action>
    Step A — Capture master analog:
    `git show fork/master:src/renderer/src/extensions/mod_management/index.ts > /tmp/mm-index.master.ts`

    Step B — Pre-resolution snapshot (smaller-diff hygiene + bluebird trap guard, R5):
    ```
    F=src/renderer/src/extensions/mod_management/index.ts
    grep -c '^<<<<<<< ' "$F"             # expect: 18
    grep -nE 'bluebird|PromiseB' "$F" \
      | tee /tmp/mm-index.bluebird.pre.txt # expect: empty (per PATTERNS metadata)
    grep -nE 'resolvePathCase|stagingDirHasFiles|normalizeBackslashPaths|mergeCaseConflictingDirs' "$F" \
      | tee /tmp/mm-index.playbook.pre.txt # expect: empty (NOT a playbook host)
    diff -u /tmp/mm-index.master.ts "$F" > /tmp/mm-index.diff.pre.txt 2>&1 || true
    ```
    If region count != 18, STOP and surface — drift from PATTERNS row 15. Do NOT continue.
    If bluebird hits appear in pre-snapshot, STOP — R5 has activated; needs human review (TS1064 trap).

    Step C — Resolve all 18 regions:
    For each `<<<<<<< HEAD` … `=======` … `>>>>>>> v2.0.1` triplet, apply D-32-02 stance hierarchy:
      1. **Playbook surface check:** none in this file (PATTERNS row 15 confirmed). Skip fork-wins branch.
      2. **New scaffolding check:** if upstream side adds a NEW import from a sibling that is now wired
         (e.g. an export added during the v2.0.1 cycle that fork/master didn't have), TAKE upstream
         (upstream-wins). Cross-reference `/tmp/mm-index.master.ts` to confirm the symbol didn't exist
         on fork/master.
      3. **Default (smaller-diff):** for line-wrap and import-block reorg, take whichever side is closer
         to fork/master OR has fewer line changes. Per PATTERNS metadata virtually all 18 are this case.

    After resolving, the file should:
    - Compile against the now-stable siblings (plans 02-04 resolved them all)
    - Re-export the same public surface as master (plus any genuine v2.0.1 additions per Step C.2)
    - NOT introduce a `bluebird` import (verified post-resolve in Step D)

    Step D — Post-resolution verification:
    ```
    F=src/renderer/src/extensions/mod_management/index.ts
    grep -c '^<<<<<<< ' "$F"             # expect: 0
    grep -c '^=======$' "$F"             # expect: 0  (Note: header prose can match — if non-zero, manually confirm grep -c excludes ENV separators)
    grep -c '^>>>>>>> ' "$F"             # expect: 0
    grep -nE 'bluebird|PromiseB' "$F" \
      | tee /tmp/mm-index.bluebird.post.txt # expect: empty (R5 stays latent)
    grep -nE 'resolvePathCase|stagingDirHasFiles|normalizeBackslashPaths|mergeCaseConflictingDirs' "$F" \
      | tee /tmp/mm-index.playbook.post.txt # expect: empty
    diff /tmp/mm-index.bluebird.pre.txt /tmp/mm-index.bluebird.post.txt    # expect: no changes
    diff /tmp/mm-index.playbook.pre.txt /tmp/mm-index.playbook.post.txt    # expect: no changes
    ```
    If bluebird hits NOW present and were NOT before → STOP, R5 activated, surface for human review.

    Step E — Bucket-scoped typecheck for this file path only (per Pattern S4):
    ```
    pnpm -F @vortex/renderer typecheck 2>&1 \
      | tee /tmp/mm-index.tc.txt
    grep 'extensions/mod_management/index.ts' /tmp/mm-index.tc.txt \
      | grep -v TS1185 | wc -l
    ```
    Expected: 0 for `index.ts` itself. (Cascades elsewhere are Phase 33/34 territory — not gating here.)
    Also re-run the full bucket-scoped count and compare against plan 03/04 endpoints:
    ```
    grep 'error TS' /tmp/mm-index.tc.txt | grep 'extensions/mod_management/' | grep -v TS1185 | wc -l
    ```
    Expected: 0 (plans 02-04 already drove this to 0; this commit must NOT regress it).

    Step F — Harness check:
    ```
    bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh \
      --skip-conflict-check; echo "exit=$?"
    ```
    Expected: exit 0; gates 1-6 PASS. (Gate 7 stays SKIPPED until plan 06's final run.)

    Step G — Atomic commit per D-32-08 + commit-body template per D-32-09 (Pattern S5).
    Title (one line, ≤72 chars, casual voice per project memory):
    `resolve(mod-mgmt-v2.0.1): index.ts — barrel re-exports, line-wrap only`

    Body must include:
    - One-line stance: "Wave 4 barrel resolution. NOT a playbook host (PATTERNS row 15)."
    - Regions resolved: 18 total
        smaller-diff: 18 (line-wrap + import-block reorg)
        fork-side: 0
        upstream-side: 0 (or count if any v2.0.1 new-export imports landed via Step C.2 — note line numbers)
    - "Playbook gates affected: none (no §6/§7a-d/140a57217 sites in this file)"
    - "Playbook gates preserved: yes (no surface to disturb)"
    - "grep-checkpoint.sh exit: 0 (--skip-conflict-check; gates 1-6 PASS)"
    - "pnpm typecheck mm-bucket: 0 non-marker errors (regression-from-zero check)"
    - "bluebird import status: absent (R5 stays latent per PATTERNS metadata)"
    - "--no-verify used: no" (no partial markers in file content; husky can parse it cleanly per D-32-10)
    - "Files: src/renderer/src/extensions/mod_management/index.ts"

    SSH-signed (gpg.format=ssh, commit.gpgsign=true). Do NOT use `--no-verify`. Per project memory
    `feedback_minimize_upstream_diff.md`: don't reformat anything outside the 18 regions.
    Per `feedback_planning_gitignored.md`: this commit only touches a tracked source file — no `git add -f`
    needed for index.ts. Per `feedback_git_push_ssh.md`: do NOT push from sandbox.

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      F=src/renderer/src/extensions/mod_management/index.ts && \
      test "$(grep -c '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' "$F")" -eq 0 && \
      test "$(grep -c '^&gt;&gt;&gt;&gt;&gt;&gt;&gt; ' "$F")" -eq 0 && \
      test -z "$(grep -nE 'bluebird|PromiseB' "$F")" && \
      test -z "$(grep -nE 'resolvePathCase|stagingDirHasFiles|normalizeBackslashPaths|mergeCaseConflictingDirs' "$F")" && \
      bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check >/dev/null 2>&1 && \
      git log -1 --pretty=%s -- "$F" | grep -q '^resolve(mod-mgmt-v2.0.1): index.ts' && \
      git log -1 --show-signature -- "$F" 2>&1 | grep -q 'Good "git" signature' && \
      echo "INDEX-TS RESOLVED"
    </automated>
  </verify>
  <acceptance_criteria>
    - File has zero conflict markers (`<<<<<<< `, `=======`, `>>>>>>> ` all 0 occurrences as line-prefix matches)
    - File still has zero bluebird imports (R5 stays latent per PATTERNS metadata; pre/post `grep -nE 'bluebird|PromiseB'` are both empty)
    - File still has zero direct playbook helper hits (NOT a host — pre/post playbook greps both empty)
    - Harness `--skip-conflict-check` exits 0; gates 1-6 PASS
    - Bucket-scoped typecheck shows 0 errors for `index.ts` AND 0 non-marker errors total in `extensions/mod_management/`
    - One SSH-signed commit on `v8.1/config-bucket` with title `resolve(mod-mgmt-v2.0.1): index.ts — barrel re-exports, line-wrap only`
    - `git log -1 --show-signature` reports `Good "git" signature`
    - No `--no-verify`
    - Commit body matches Pattern S5 template; regions tallied; "Playbook gates affected: none" stated explicitly
  </acceptance_criteria>
  <done>
    index.ts resolved without disturbing the (non-existent) playbook surface; harness green; bucket
    typecheck still zero; signed commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 32-05-SUMMARY.md and commit (Wave 4 closeout)</name>
  <files>.planning/phases/32-mod-management-hot-zone-v2-0-1/32-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-04-SUMMARY.md (mirror format/structure)
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-CONTEXT.md (D-32-09 norm)
    - $HOME/.claude/get-shit-done/templates/summary.md (canonical structure)
    - The Task 1 commit (read its hash + body for inclusion)
  </read_first>
  <action>
    Write `32-05-SUMMARY.md` with these sections:

    1. **Wave 4 Outcome** — one-line: barrel resolved, all 18 regions smaller-diff, no playbook surface, harness green.
    2. **File Resolved (1)**
       - `src/renderer/src/extensions/mod_management/index.ts` — 18 regions, all smaller-diff
         - Commit SHA + signed status
         - Pre/post bluebird grep (both empty — R5 stays latent)
         - Pre/post playbook grep (both empty — NOT a host, confirms PATTERNS row 15)
         - Bucket-scoped typecheck delta: 0 → 0 (no regression)
    3. **Cumulative Phase 32 State** (forward-look for plan 06):
       - Total resolved files: 15 (NotificationAggregator, VersionFilter, removeMods, activationStore, externalChanges, deploy, stagingDirectory from plan 02 = 7) + (modMerging, DeactivationButton, Settings, eventHandlers, ModList from plan 03 = 5) + (LinkingDeployment, InstallManager from plan 04 = 2) + (index.ts from plan 05 = 1) = 15
       - Total resolved regions: 1 + 1 + 1 + 3 + 3 + 2 + 1 + 2 + 1 + 2 + 3 + 11 + 8 + 40 + 18 = 97 (matches RESEARCH §7 baseline)
       - Total resolution commits to date: 15 (one per file per D-32-08) + 4 SUMMARY commits (plans 01-04) + this commit = pending verification in plan 06
       - Single-host invariant (D-32-12): held — only LinkingDeployment.ts has resolvePathCase(dataPath,...)
       - Playbook gate status (per --skip-conflict-check harness): all 6 gates GREEN (§6, §7a, §7b, §7c, §7d, 140a57217)
       - Bucket-scoped typecheck: 0 non-marker errors in mod_management/ (down from baseline 260)
    4. **Forward-pointer to plan 06** — final phase verification:
       - Run harness WITHOUT `--skip-conflict-check`; gate 7 (no-marker) MUST pass
       - Audit all 15 `resolve(mod-mgmt-v2.0.1):` commit bodies for D-32-09 fields
       - Final bucket-scoped typecheck = 0
       - Update VALIDATION.md sign-off; write 32-PHASE-SUMMARY.md
    5. **Risks Cleared / Outstanding**
       - R1 InstallManager blast radius: mitigated in plan 04
       - R2 LinkingDeployment 140a57217 host: D-32-12 single-host invariant preserved
       - R5 bluebird trap: stayed latent across all 15 files (empirically verified)
       - Outstanding for plan 06: final marker eradication assertion (gate 7 full-mode), commit-body audit, validation sign-off

    Then commit per D-32-09 norm. Title (casual voice per project memory):
    `docs(32-05): summarize Wave 4 (barrel; index.ts, 18 regions, no playbook surface)`

    Body:
    - "Wave 4 closeout. 15/15 files resolved, 97/97 regions resolved across plans 02-05."
    - Reference Task 1's commit SHA
    - "Plan 06 next: final harness (no skip), commit-body audit, validation sign-off."

    Per project memory `feedback_planning_gitignored.md`: use `git add -f` for the SUMMARY.
    Per `feedback_git_push_ssh.md`: do NOT push from sandbox.
    SSH-signed; no `--no-verify`.

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      test -f .planning/phases/32-mod-management-hot-zone-v2-0-1/32-05-SUMMARY.md && \
      git log -1 --pretty=%s | grep -q '^docs(32-05): summarize Wave 4' && \
      git log -1 --show-signature 2>&1 | grep -q 'Good "git" signature' && \
      grep -q '15/15 files resolved' .planning/phases/32-mod-management-hot-zone-v2-0-1/32-05-SUMMARY.md && \
      grep -q '97/97 regions' .planning/phases/32-mod-management-hot-zone-v2-0-1/32-05-SUMMARY.md && \
      echo "WAVE 4 SUMMARY COMMITTED"
    </automated>
  </verify>
  <acceptance_criteria>
    - 32-05-SUMMARY.md exists with all 5 sections (Outcome, File Resolved, Cumulative State, Forward-pointer, Risks)
    - Cumulative tallies stated: 15/15 files, 97/97 regions
    - Single-host invariant restated as held
    - Bluebird R5 latent across all 15 files restated
    - One SSH-signed commit titled `docs(32-05): summarize Wave 4 (barrel; index.ts, 18 regions, no playbook surface)`
    - `git add -f` used for the gitignored `.planning/` path
  </acceptance_criteria>
  <done>
    Wave 4 documented; cumulative phase state captured; plan 06 unblocked.
  </done>
</task>

</tasks>

<verification>
After plan completes:
- `bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check` exits 0
- `git grep -c '^<<<<<<< ' src/renderer/src/extensions/mod_management/` total = 0 (all 15 files resolved)
- `git log --pretty=%s --grep='^resolve(mod-mgmt-v2.0.1):' | wc -l` = 15 (one per file per D-32-08)
- `git log --pretty=%s --grep='^docs(32-..):' | wc -l` = 5 (plans 01-05 SUMMARY commits)
- `git log -5 --show-signature 2>&1 | grep -c 'Good "git" signature'` = 5 (last 5 commits all SSH-signed)
- 32-05-SUMMARY.md exists; cumulative state captured
</verification>

<success_criteria>

- mod_management/index.ts has zero conflict markers
- All 18 regions resolved smaller-diff (no fork-wins, no upstream-wins surface in this file unless a v2.0.1 NEW export landed — explicitly tallied in commit body)
- Bluebird R5 stayed latent (pre/post grep both empty)
- Playbook gates 1-6 still GREEN via harness
- Bucket-scoped typecheck still 0 non-marker errors
- One SSH-signed file commit + one SSH-signed SUMMARY commit on `v8.1/config-bucket`
- 15/15 files resolved, 97/97 regions resolved cumulative — plan 06 ready for final verification
  </success_criteria>

<output>
Create `.planning/phases/32-mod-management-hot-zone-v2-0-1/32-05-SUMMARY.md` per Task 2.
</output>
