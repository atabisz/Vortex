---
phase: 32-mod-management-hot-zone-v2-0-1
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-01-SUMMARY.md
autonomous: true
requirements:
    - SYNC-32a
must_haves:
    truths:
        - "v8.0 harness extracted from git 7ed691f40 and placed at phase scripts/grep-checkpoint.sh"
        - "Harness is executable; dry-run reports gates 1-6 PASS on current pre-resolution tree"
        - "Pre-flight repo state matches RESEARCH §7 (branch, base SHA, 15 conflict files, 97 regions, clean tree, signing config)"
        - "Baseline mod_management/ non-marker typecheck error count captured (expected ~260, all in views/ModList.tsx)"
        - "v2.0.1 introduces zero new playbook-touching call sites — verified, recorded in summary, no gate 8 needed"
        - "D-26-03a single-host invariant verified against current tree (LinkingDeployment.ts is sole 140a57217 host)"
    artifacts:
        - path: ".planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh"
          provides: "7-gate aggregate-fail playbook harness with --skip-conflict-check flag"
          contains: "stagingDirHasFiles, normalizeBackslashPaths, mergeCaseConflictingDirs, resolvePathCase"
        - path: ".planning/phases/32-mod-management-hot-zone-v2-0-1/32-01-SUMMARY.md"
          provides: "Pre-flight snapshot, harness origin, baseline error count, v2.0.1-no-new-sites finding, single-host invariant confirmation"
    key_links:
        - from: "scripts/grep-checkpoint.sh"
          to: "src/renderer/src/extensions/mod_management/{InstallManager,LinkingDeployment}.ts"
          via: "git grep -nE patterns"
          pattern: "stagingDirHasFiles|normalizeBackslashPaths|mergeCaseConflictingDirs|resolvePathCase"
---

<objective>
Wave 0 setup. Extract the v8.0 Phase 26 harness from git history (the on-disk path
`.planning/phases/26-mod-management-hot-zone/scripts/` no longer exists per RESEARCH §7),
copy it verbatim to this phase's `scripts/` dir per D-32-04, verify pre-flight repo
state matches RESEARCH §7 snapshot, capture the bucket-scoped typecheck baseline so
later plans can compare against it, and record the v2.0.1 negative finding (no new
playbook sites) per D-32-05 so subsequent plans can re-use the existing 7 gates without
extending them.

Purpose: All subsequent plans depend on `scripts/grep-checkpoint.sh` being present and
executable. Without baseline measurement, per-file plans cannot prove regression-vs-no-regression.

Output: Executable harness in `scripts/grep-checkpoint.sh`, baseline data in `32-01-SUMMARY.md`,
one git commit (signed) — `chore(mod-mgmt-v2.0.1): scaffold phase 32 harness + baseline`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-CONTEXT.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-RESEARCH.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-PATTERNS.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-VALIDATION.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight repo-state verification</name>
  <files></files>
  <read_first>
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-RESEARCH.md (sections 7 "Pre-Flight Repo State Hazards" and "Pre-Flight Repo State Snapshot")
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-CONTEXT.md (D-32-15 branch decision)
    - CLAUDE.md (Branch Strategy, GSD Workflow Enforcement, SSH commit signing notes)
  </read_first>
  <action>
    Run the 10 pre-flight checks from RESEARCH §7 against the current working tree. No code changes;
    this is purely a guard so we abort if reality has drifted from the research snapshot.

    Required asserts (record actual values in scratch notes for inclusion in 32-01-SUMMARY.md):
    1. Branch identity is `v8.1/config-bucket`
    2. `git merge-base HEAD fork/sync/upstream-v2.0.1` equals `8054a935b6aad505798bba8a993d002718d119cb`
    3. Conflict file count under `src/renderer/src/extensions/mod_management/` is 15
    4. Total conflict region count (sum of `git grep -c '^<<<<<<< '` per file) is 97
    5. No `.git/MERGE_HEAD`, `.git/CHERRY_PICK_HEAD`, `.git/rebase-merge`, `.git/rebase-apply`
    6. Working tree clean (`git status --porcelain | grep -v '^??'` is empty)
    7. SSH signing active: `git config gpg.format` = `ssh`, `git config commit.gpgsign` = `true`,
       signing key file exists
    8. `src/renderer/src/extensions/download_management/DownloadManager.ts` has zero conflict markers
       (out-of-Phase-32 scope confirmation per CONTEXT D-32-13)
    9. `git stash list` recorded (informational; do NOT pop anything)
    10. Sentinel files exist: `src/renderer/src/extensions/mod_management/util/stagingIntegrity.ts`,
        `util/normalizeBackslashPaths.ts`, `util/mergeCaseConflictingDirs.ts`

    If ANY of 1-8 or 10 fail, STOP. Do not proceed to Task 2. Surface the discrepancy and ask the
    operator whether to re-run RESEARCH or adjust the plan. The phase is not safe to execute on a
    drifted tree.

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      test "$(git rev-parse --abbrev-ref HEAD)" = "v8.1/config-bucket" && \
      test "$(git merge-base HEAD fork/sync/upstream-v2.0.1)" = "8054a935b6aad505798bba8a993d002718d119cb" && \
      test "$(git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/ | wc -l)" -eq 15 && \
      test "$(git grep -c '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/ | awk -F: '{s+=$2} END {print s}')" -eq 97 && \
      test ! -e .git/MERGE_HEAD && test ! -e .git/CHERRY_PICK_HEAD && \
      test "$(git config --get gpg.format)" = "ssh" && \
      test "$(git config --get commit.gpgsign)" = "true" && \
      test -f src/renderer/src/extensions/mod_management/util/stagingIntegrity.ts && \
      echo "PRE-FLIGHT OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - All 10 RESEARCH §7 checks pass; recorded values match snapshot exactly for items 1, 2, 3, 4, 7, 10
    - Item 8: DownloadManager.ts conflict count is 0
    - Item 9: stash list is captured to scratch notes (informational, not gating)
    - Pre-flight summary block ready for inclusion in 32-01-SUMMARY.md
  </acceptance_criteria>
  <done>Pre-flight checks all pass and exact values recorded for the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Extract v8.0 harness, install, dry-run, capture baseline, record v2.0.1 no-new-sites finding</name>
  <files>
    .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh
    .planning/phases/32-mod-management-hot-zone-v2-0-1/32-01-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-RESEARCH.md (sections 1, 2, 5, "Pre-Resolution Playbook Gate Status", "Wave 0 Gaps")
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-PATTERNS.md (Pattern S3 — Harness invocation; Pattern S4 — Bucket-scoped typecheck baseline)
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-CONTEXT.md (D-32-04 reuse, D-32-05 inspection requirement, D-32-12 single-host invariant)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md (§6 stagingDirHasFiles, §7a-d backslash/case cluster, externalChanges/140a57217 entries)
    - First 30 lines of the source harness blob (read via `git show 7ed691f40:.planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh | head -30`) so the 7 gates encoded in it are known when writing the SUMMARY
  </read_first>
  <action>
    Step A — Extract harness verbatim from git:
    `git show 7ed691f40:.planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh > .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh`

    Verify byte content survived (e.g. `wc -l` ≈ 159 lines per RESEARCH §1 reference). DO NOT edit
    the gate logic. Per D-32-04 and the PATTERNS S3 instruction, the harness is reused VERBATIM —
    the only allowed modification is a 1-2 line header comment update. Per D-32-05 inspection (this
    task's Step C below) confirms zero new gates needed for v2.0.1, so no gate 8 lands in this phase.

    Step B — Make executable: `chmod +x .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh`

    Step C — Re-run RESEARCH §2 inspection commands (don't trust research; re-verify):
    ```
    git show f25ff55da:src/renderer/src/extensions/mod_management/InstallManager.ts \
      | grep -nE 'stagingDirHasFiles|normalizeBackslashPaths|mergeCaseConflictingDirs|resolvePathCase|caseSensitive' \
      || echo "NONE - upstream has no playbook helpers (expected)"
    git show f25ff55da:src/renderer/src/extensions/mod_management/LinkingDeployment.ts \
      | grep -nE 'resolvePathCase' || echo "NONE"
    git diff fork/master fork/sync/upstream-v2.0.1 --name-status -- 'src/renderer/src/extensions/mod_management/'
    ```
    Capture all three outputs verbatim into the SUMMARY. Per RESEARCH §1/§2 the expected result is
    NONE/NONE for the first two; record the actual outputs as audit trail.

    Step D — Verify D-32-12 single-host invariant against the CURRENT tree:
    `git grep -nE 'resolvePathCase\(dataPath,' src/renderer/src/extensions/mod_management/`
    Expected: ≥3 hits ALL inside `LinkingDeployment.ts`; ZERO hits in `util/externalChanges.ts` or
    any other file. Record actual hits in SUMMARY. If ANY hit lands outside LinkingDeployment.ts, STOP
    and surface — D-26-03a invariant has been violated and the plan structure must be re-discussed.

    Step E — Dry-run the harness on the pre-resolution tree:
    `bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check; echo "exit=$?"`
    Expected per RESEARCH "Pre-Resolution Playbook Gate Status": gates §6, §7a, §7b, §7c, §7d, 140a57217
    all PASS (6/6 with skip), exit 0. Run again WITHOUT `--skip-conflict-check`:
    `bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh; echo "exit=$?"`
    Expected: exit non-zero, gate 7 (no-marker) FAIL because 97 markers still present (this is the
    expected mid-phase state). Capture both exit codes and the per-gate result lines into SUMMARY.

    Step F — Capture bucket-scoped typecheck baseline:
    Run `pnpm typecheck -F @vortex/renderer 2>&1 > /tmp/phase32-baseline.txt; echo "tc-exit=$?"` (note:
    exit will be non-zero because Phase 33/34 errors live across the renderer; that's expected per
    RESEARCH §5). Then compute:
    - Total `error TS` lines: `grep -E 'error TS' /tmp/phase32-baseline.txt | wc -l`
    - TS1185 (marker) errors: `grep 'TS1185' /tmp/phase32-baseline.txt | wc -l`
    - mod_management/ non-marker errors: `grep 'error TS' /tmp/phase32-baseline.txt | grep 'extensions/mod_management/' | grep -v TS1185 | wc -l`
    - Per-file mod_management non-marker counts (one line per file under `extensions/mod_management/`)
    Expected per RESEARCH §5: ~1370 total, ~763 TS1185, ~260 in mod_management/ (all in views/ModList.tsx
    as JSX cascades from markers). Record actual values; if drift >20% from research snapshot, note
    in SUMMARY but proceed (drift doesn't block — strategy is comparison-vs-baseline, not absolute).

    Step G — Write `32-01-SUMMARY.md` containing:
    - Pre-flight check results (10 items from Task 1)
    - Harness origin (`git show 7ed691f40:...`), line count, executable status
    - v2.0.1 no-new-sites finding (Step C verbatim outputs); explicit statement "no gate 8 added"
    - D-32-12 single-host invariant verification (Step D output)
    - Harness dry-run results: skip-mode exit code + per-gate states; full-mode exit code (expected non-zero)
    - Baseline typecheck data (Step F numbers, per-file breakdown for mod_management/)
    - Per-tier resolution plan reference (forward-pointer to plans 02/03/04/05)

    Step H — Commit. Title:
    `chore(mod-mgmt-v2.0.1): scaffold phase 32 harness + baseline`

    Body must include (per D-32-09 norm):
    - One-line stance: "Wave 0 setup; reuses v8.0 Phase 26 harness verbatim per D-32-04."
    - Origin SHA cited (`git show 7ed691f40:...`)
    - v2.0.1 inspection finding (no new playbook sites; D-32-05 satisfied)
    - Baseline numbers (mod_management non-marker error count, per-file)
    - grep-checkpoint exit codes (skip-mode=0, full-mode=non-zero with gate 7 fail)
    - Files: scripts/grep-checkpoint.sh, 32-01-SUMMARY.md
    - SSH-sign per CLAUDE.md (commit.gpgsign=true ensures this; do NOT use `--no-verify`)

    Per project memory `feedback_planning_gitignored.md`: `.planning/` is gitignored, so use
    `git add -f` for both files. Per `feedback_git_push_ssh.md`: do not push from sandbox; the
    operator will push at end of phase.

    Per RESEARCH "Common Pitfalls #3": no `--no-verify` on this commit (no partial markers in
    files being added).

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      test -x .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh && \
      bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check >/dev/null 2>&1 && \
      test -f .planning/phases/32-mod-management-hot-zone-v2-0-1/32-01-SUMMARY.md && \
      git log -1 --pretty=%s | grep -q '^chore(mod-mgmt-v2.0.1): scaffold phase 32 harness' && \
      git log -1 --show-signature 2>&1 | grep -q 'Good "git" signature' && \
      echo "WAVE 0 COMPLETE"
    </automated>
  </verify>
  <acceptance_criteria>
    - `scripts/grep-checkpoint.sh` exists, is executable, line count within 5% of source (~159 lines)
    - Header comment cites v8.0 Phase 26 origin and v8.1 Phase 32 reuse rationale (per D-32-04)
    - Harness in `--skip-conflict-check` mode exits 0 on current tree (gates 1-6 PASS)
    - Harness in default mode exits non-zero with gate 7 FAIL (the 97 unresolved markers — this is the expected pre-resolution state, not a regression)
    - `32-01-SUMMARY.md` records pre-flight, origin, no-new-sites finding, single-host invariant verification, dry-run exits, baseline typecheck numbers
    - Step C inspection captured: zero hits for playbook helpers in upstream `f25ff55da` blobs
    - Step D verification: all `resolvePathCase(dataPath,` hits live in `LinkingDeployment.ts`; zero in any other mod_management/ file
    - Step F baseline: mod_management non-marker error count recorded (RESEARCH expects ~260, all in `views/ModList.tsx`)
    - Single SSH-signed commit on `v8.1/config-bucket` with title `chore(mod-mgmt-v2.0.1): scaffold phase 32 harness + baseline`
    - `git log -1 --show-signature` reports `Good "git" signature`
    - No `--no-verify`
    - `git add -f` used for both `.planning/` paths (file is otherwise gitignored)
  </acceptance_criteria>
  <done>
    Harness scaffolded; baseline captured; v2.0.1 no-new-sites finding recorded; single-host invariant
    re-verified; signed commit on branch. Subsequent plans can run the harness after every commit.
  </done>
</task>

</tasks>

<verification>
After plan completes:
- `bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check` exits 0
- `git log -1 --pretty=%s` matches `chore(mod-mgmt-v2.0.1): scaffold phase 32 harness + baseline`
- `git log -1 --show-signature` reports `Good "git" signature`
- `32-01-SUMMARY.md` contains: pre-flight, harness origin, v2.0.1 inspection outputs, single-host verification, dry-run exits, baseline numbers
- 15 conflict files in `src/renderer/src/extensions/mod_management/` STILL exist with markers (Plan 01 does not resolve any conflicts; that's plans 02-05's job)
</verification>

<success_criteria>

- Harness installed and verified GREEN in skip mode against pre-resolution tree
- Baseline typecheck error count documented (≈260 in mod_management/)
- v2.0.1 no-new-sites finding documented per D-32-05
- D-32-12 single-host invariant re-verified against current tree
- One signed commit on `v8.1/config-bucket`
- Plans 02-05 unblocked (depend on `scripts/grep-checkpoint.sh` existing)
  </success_criteria>

<output>
Create `.planning/phases/32-mod-management-hot-zone-v2-0-1/32-01-SUMMARY.md` per Task 2 Step G.
</output>
