---
phase: 32-mod-management-hot-zone-v2-0-1
plan: 06
type: execute
wave: 5
depends_on:
    - 32-05
files_modified:
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-VALIDATION.md
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-PHASE-SUMMARY.md
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-06-SUMMARY.md
autonomous: true
requirements:
    - SYNC-32a
must_haves:
    truths:
        - "Harness in default mode (no --skip-conflict-check) exits 0 — gate 7 (no-marker) PASSES, all 7 gates GREEN"
        - "Bucket-scoped typecheck for src/renderer/src/extensions/mod_management/ shows 0 non-marker errors"
        - "All 15 file-resolution commits exist on v8.1/config-bucket with title `resolve(mod-mgmt-v2.0.1): <file> — <stance>` per D-32-08"
        - "All 15 file-resolution commit bodies match Pattern S5 template per D-32-09 (regions tallied, gates listed, harness exit, typecheck count, --no-verify status)"
        - 'All 15 file-resolution commits are SSH-signed (git log --show-signature reports Good "git" signature)'
        - "Single-host invariant (D-32-12) re-verified at phase end: only LinkingDeployment.ts has resolvePathCase(dataPath,...)"
        - "32-VALIDATION.md updated with phase-end sign-off; 32-PHASE-SUMMARY.md written"
    artifacts:
        - path: ".planning/phases/32-mod-management-hot-zone-v2-0-1/32-VALIDATION.md"
          provides: "Updated validation sign-off — all gates passed at phase end"
          contains: "phase-end sign-off"
        - path: ".planning/phases/32-mod-management-hot-zone-v2-0-1/32-PHASE-SUMMARY.md"
          provides: "Phase 32 closeout — 15 files / 97 regions resolved, all playbook invariants preserved, fully harness-green"
          contains: "15 files, 97 regions, 7 gates"
    key_links:
        - from: "harness gate 7 (no-marker)"
          to: "src/renderer/src/extensions/mod_management/"
          via: "git grep -c '^<<<<<<< ' summed across the 15 files = 0"
          pattern: "^<<<<<<< "
        - from: "single-host invariant (D-32-12)"
          to: "src/renderer/src/extensions/mod_management/LinkingDeployment.ts"
          via: "git grep -nE 'resolvePathCase\\(dataPath,' confined to LinkingDeployment.ts"
          pattern: "resolvePathCase\\(dataPath,"
---

<objective>
Wave 5 — phase verification and sign-off. Plans 02-05 resolved all 15 files and 97 regions; this plan
proves it. The harness has been running in `--skip-conflict-check` mode after every commit; now we drop
that flag for the final assertion: gate 7 (no-marker) MUST pass. Then audit every per-file commit
body against D-32-09 + Pattern S5, run final bucket-scoped typecheck, re-verify D-32-12 single-host
invariant, update VALIDATION.md sign-off, and write 32-PHASE-SUMMARY.md.

Purpose: This is the gate. If any of the 15 commit bodies skipped a required field, if gate 7 fails,
if a stray marker survived, or if D-32-12 was violated by a sneaky region resolution — this plan
catches it before phase close. No code changes here; this is pure verification + documentation.

Output: Updated VALIDATION.md, new 32-PHASE-SUMMARY.md, 1 SSH-signed sign-off commit on `v8.1/config-bucket`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-CONTEXT.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-RESEARCH.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-PATTERNS.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-VALIDATION.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-01-SUMMARY.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-02-SUMMARY.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-03-SUMMARY.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-04-SUMMARY.md
@.planning/phases/32-mod-management-hot-zone-v2-0-1/32-05-SUMMARY.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final harness run (no skip), bucket-scoped typecheck, single-host invariant re-check</name>
  <files></files>
  <read_first>
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh (the harness itself — confirm gate 7 logic before relying on its exit code)
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-RESEARCH.md (sections 1, 5, 6, "Pre-Resolution Playbook Gate Status")
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-CONTEXT.md (D-32-04 harness reuse, D-32-06 typecheck reinterpreted, D-32-12 single-host invariant)
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-PATTERNS.md (Pattern S4 baseline expression)
    - 32-04-SUMMARY.md and 32-05-SUMMARY.md (confirm cumulative tallies match what this plan re-verifies)
  </read_first>
  <action>
    Step A — Final harness run (NO `--skip-conflict-check`):
    ```
    bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh; \
      echo "exit=$?"
    ```
    Required: exit 0; ALL 7 gates PASS (gates 1-6 = playbook surface preserved; gate 7 = zero conflict
    markers in `src/renderer/src/extensions/mod_management/`). Capture full output to
    `/tmp/phase32-final-harness.txt` for inclusion in 32-PHASE-SUMMARY.md.

    If exit non-zero, STOP. Investigate which gate failed:
    - Gate 7 fail = surviving marker somewhere → `git grep -nE '^<<<<<<< |^=======$|^>>>>>>> ' src/renderer/src/extensions/mod_management/`
    - Gate 1-6 fail = playbook surface lost during resolution → cross-reference PATTERNS row + plan 04
      pre/post snapshots → file a regression task before phase close
    - Do NOT mask the failure. Surface it.

    Step B — Final bucket-scoped typecheck (per Pattern S4 + D-32-06 reinterpreted):
    ```
    pnpm -F @vortex/renderer typecheck 2>&1 \
      | tee /tmp/phase32-final-tc.txt
    grep 'error TS' /tmp/phase32-final-tc.txt \
      | grep 'extensions/mod_management/' | grep -v TS1185 | wc -l
    ```
    Required: 0. Capture full per-file breakdown for SUMMARY:
    ```
    grep 'error TS' /tmp/phase32-final-tc.txt \
      | grep 'extensions/mod_management/' | grep -v TS1185 \
      | awk -F'(' '{print $1}' | sort | uniq -c
    ```
    Expected: empty (0 lines). If non-zero, STOP — bucket has regressed since plan 05; investigate
    which file owns the new errors.

    Note: renderer-wide non-marker error count will still be ~347 (Phase 33/34 territory per RESEARCH §5).
    That's expected and NOT gating. Only the mod_management/ slice gates this phase.

    Step C — Re-verify D-32-12 single-host invariant against the FINAL tree:
    ```
    git grep -nE 'resolvePathCase\(dataPath,' \
      src/renderer/src/extensions/mod_management/ \
      | tee /tmp/phase32-d12-final.txt
    cut -d: -f1 /tmp/phase32-d12-final.txt | sort -u
    ```
    Required:
    - Total hits ≥3 (matches plan 01 baseline; should be exactly 3 — at master lines 523/742/799 drifted
      to roughly 599/818/875 in current tree per RESEARCH)
    - All hits in `src/renderer/src/extensions/mod_management/LinkingDeployment.ts`
    - Zero hits in `util/externalChanges.ts` or any other mod_management file

    If ANY hit lands outside LinkingDeployment.ts, STOP — D-32-12 violated. This is a critical
    regression that must be fixed before phase close.

    Step D — Total marker assertion (sanity, redundant with harness gate 7 but cheap):
    ```
    git grep -c '^<<<<<<< ' src/renderer/src/extensions/mod_management/ \
      | awk -F: '{s+=$2} END {print s+0}'
    ```
    Required: 0.

    Step E — Capture all numbers + outputs to scratch notes for Task 3 (PHASE-SUMMARY.md). No commit
    yet — Task 3 is the documentation commit.

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh >/dev/null 2>&1 && \
      test "$(git grep -c '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/ 2>/dev/null | awk -F: '{s+=$2} END {print s+0}')" -eq 0 && \
      test "$(pnpm -F @vortex/renderer typecheck 2>&1 | grep 'error TS' | grep 'extensions/mod_management/' | grep -v TS1185 | wc -l)" -eq 0 && \
      test "$(git grep -nE 'resolvePathCase\(dataPath,' src/renderer/src/extensions/mod_management/ | cut -d: -f1 | sort -u | grep -v 'LinkingDeployment.ts' | wc -l)" -eq 0 && \
      echo "FINAL VERIFICATION PASSED"
    </automated>
  </verify>
  <acceptance_criteria>
    - Harness exits 0 in default mode (no --skip-conflict-check); all 7 gates PASS including gate 7 (no-marker)
    - Bucket-scoped typecheck for `extensions/mod_management/` returns 0 non-marker errors
    - Single-host invariant: all `resolvePathCase(dataPath,` hits live in LinkingDeployment.ts; zero elsewhere
    - Total `^<<<<<<< ` line-prefix count summed across mod_management/ = 0
    - All numbers captured to `/tmp/phase32-final-*.txt` for Task 3
  </acceptance_criteria>
  <done>
    All four phase-end gates passed. Phase is provably done at the harness/typecheck/invariant level.
    Ready for commit-body audit (Task 2) and final docs (Task 3).
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit-body audit (15 file-resolution commits vs D-32-09 + Pattern S5)</name>
  <files></files>
  <read_first>
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-CONTEXT.md (D-32-09 norm)
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-PATTERNS.md (Pattern S5 atomic-commit-body template)
    - $HOME/.claude/CLAUDE.md (SSH signing notes)
    - Git log of plans 02-05 resolution commits (Step A below captures them)
  </read_first>
  <action>
    Step A — Enumerate the 15 file-resolution commits:
    ```
    BASE=$(git rev-list --max-parents=0 HEAD | head -1)  # NOT used; we want plan-02 onward
    # Find the commit BEFORE plan 02's first resolve commit (i.e. plan 01's last commit, the harness scaffold):
    PLAN01_END=$(git log --pretty=%H --grep='^chore(mod-mgmt-v2.0.1): scaffold phase 32' -1)
    git log --reverse --pretty=%H --grep='^resolve(mod-mgmt-v2.0.1):' "$PLAN01_END..HEAD" \
      > /tmp/phase32-resolve-commits.txt
    wc -l /tmp/phase32-resolve-commits.txt   # expect: 15
    ```
    Required: exactly 15 commits. If <15, STOP — a file is missing a commit (D-32-08 violation).
    If >15, STOP — duplicate or stray commits; investigate.

    Step B — For each of the 15 commits, audit body fields per Pattern S5. The required fields are:
    1. Title matches `^resolve(mod-mgmt-v2.0.1): <file> — <stance>$`
    2. Body contains `Regions resolved:` followed by total + breakdown (fork-side / upstream-side / smaller-diff)
    3. Body contains `Playbook gates affected:` (even if "none" for non-host files)
    4. Body contains `Playbook gates preserved:` (yes/no per gate)
    5. Body contains `grep-checkpoint.sh exit:` with exit code
    6. Body contains `pnpm typecheck mm-bucket:` with non-marker error count
    7. Body contains `--no-verify used:` with `no` (or `yes` + reason — only D-32-10 husky-on-partial-marker exception)
    8. SSH-signed (Good "git" signature)

    Audit script:
    ```
    AUDIT_FAIL=0
    while read -r SHA; do
      BODY=$(git show -s --format=%B "$SHA")
      TITLE=$(echo "$BODY" | head -1)
      # Title check
      echo "$TITLE" | grep -qE '^resolve\(mod-mgmt-v2\.0\.1\): .+ — .+' || { echo "FAIL title: $SHA"; AUDIT_FAIL=1; }
      # Body field checks
      for field in 'Regions resolved:' 'Playbook gates affected:' 'Playbook gates preserved:' 'grep-checkpoint.sh exit:' 'pnpm typecheck mm-bucket:' '--no-verify used:'; do
        echo "$BODY" | grep -qF "$field" || { echo "FAIL field '$field': $SHA"; AUDIT_FAIL=1; }
      done
      # Signature check
      git log -1 --show-signature "$SHA" 2>&1 | grep -q 'Good "git" signature' \
        || { echo "FAIL signature: $SHA"; AUDIT_FAIL=1; }
    done < /tmp/phase32-resolve-commits.txt
    test "$AUDIT_FAIL" -eq 0 && echo "AUDIT PASS" || echo "AUDIT FAIL"
    ```
    Required: AUDIT PASS. If any commit fails, STOP and surface the offender; D-32-09 norm violation
    must be fixed (rewrite commit body via `git commit --amend` or interactive rebase) before phase close.

    Step C — Capture audit output to `/tmp/phase32-audit.txt` for Task 3.

    Step D — Also count SUMMARY commits for plan tally:
    ```
    git log --pretty=%s --grep='^docs(32-..): summarize' "$PLAN01_END..HEAD" | wc -l
    ```
    Expected: 4 (plans 02, 03, 04, 05 each emit one SUMMARY). Plan 06's SUMMARY commit is Task 3.
    If <4, surface — a SUMMARY commit is missing.

    No new commit in this task; the audit feeds Task 3.

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      PLAN01_END=$(git log --pretty=%H --grep='^chore(mod-mgmt-v2.0.1): scaffold phase 32' -1) && \
      test "$(git log --pretty=%H --grep='^resolve(mod-mgmt-v2.0.1):' "$PLAN01_END..HEAD" | wc -l)" -eq 15 && \
      test "$(git log --pretty=%H --grep='^docs(32-..): summarize' "$PLAN01_END..HEAD" | wc -l)" -ge 4 && \
      AUDIT_FAIL=0; \
      for SHA in $(git log --pretty=%H --grep='^resolve(mod-mgmt-v2.0.1):' "$PLAN01_END..HEAD"); do \
        BODY=$(git show -s --format=%B "$SHA"); \
        for f in 'Regions resolved:' 'Playbook gates affected:' 'Playbook gates preserved:' 'grep-checkpoint.sh exit:' 'pnpm typecheck mm-bucket:' '--no-verify used:'; do \
          echo "$BODY" | grep -qF "$f" || AUDIT_FAIL=1; \
        done; \
        git log -1 --show-signature "$SHA" 2>&1 | grep -q 'Good "git" signature' || AUDIT_FAIL=1; \
      done; \
      test "$AUDIT_FAIL" -eq 0 && echo "AUDIT PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - Exactly 15 `resolve(mod-mgmt-v2.0.1):` commits between plan 01's harness commit and HEAD
    - All 15 commit bodies contain the 6 required Pattern S5 fields
    - All 15 commits SSH-signed (Good "git" signature)
    - At least 4 `docs(32-..): summarize` SUMMARY commits exist (plans 02-05)
    - Audit output captured to `/tmp/phase32-audit.txt`
  </acceptance_criteria>
  <done>
    All 15 file-resolution commits conform to D-32-09 norm. Phase commit hygiene proven.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update VALIDATION.md, write 32-PHASE-SUMMARY.md + 32-06-SUMMARY.md, sign-off commit</name>
  <files>
    .planning/phases/32-mod-management-hot-zone-v2-0-1/32-VALIDATION.md
    .planning/phases/32-mod-management-hot-zone-v2-0-1/32-PHASE-SUMMARY.md
    .planning/phases/32-mod-management-hot-zone-v2-0-1/32-06-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-VALIDATION.md (existing structure — append, don't rewrite)
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-01-SUMMARY.md through 32-05-SUMMARY.md (cumulative tallies, per-wave outcomes)
    - $HOME/.claude/get-shit-done/templates/summary.md (PHASE-SUMMARY canonical structure)
    - .planning/phases/32-mod-management-hot-zone-v2-0-1/32-CONTEXT.md (D-32-09 norm)
    - Scratch notes from Tasks 1 + 2 (final harness exit, final typecheck count, single-host re-verification, audit output)
  </read_first>
  <action>
    Step A — Update `32-VALIDATION.md`. Append a "## Phase-End Sign-Off" section (do NOT rewrite earlier
    Wave 0 sections — those are historical record). Section must include:
    - Date (today): 2026-05-22 (or current date if executor runs later)
    - Branch: v8.1/config-bucket
    - Final harness exit (Task 1 Step A): 0; all 7 gates PASS
    - Final bucket-scoped typecheck count (Task 1 Step B): 0
    - Single-host invariant (D-32-12) re-verified: yes; resolvePathCase(dataPath,...) confined to LinkingDeployment.ts
    - Total marker count across mod_management/ (Task 1 Step D): 0
    - Commit-body audit (Task 2): PASS — 15/15 commits conform to D-32-09 + Pattern S5
    - SSH-signing audit: PASS — 15/15 file-resolution commits + 5/5 SUMMARY commits signed
    - Bluebird R5 status: stayed latent across all 15 files (no new bluebird imports introduced)
    - Sign-off line: "Phase 32 verified complete. Ready for STATE.md update + ROADMAP.md tick."

    Step B — Write `32-PHASE-SUMMARY.md` (the canonical phase-close artifact). Sections:
    1. **Phase 32 Outcome** — one-paragraph summary: 15 mod_management/ files resolved, 97 conflict
       regions resolved, all 6 playbook surfaces preserved, harness fully GREEN, single-host invariant
       held, no bluebird trap activations, bucket-scoped typecheck zero.
    2. **Resolution Tally**
       - Plans 01-06: harness scaffold (1) + per-tier resolutions (Wave 1: 7 leaf, Wave 2: 5 mid, Wave 3:
         2 playbook-heavy, Wave 4: 1 barrel = 15) + verification (1)
       - 15 file-resolution commits + 5 SUMMARY commits + 1 sign-off commit (this one) = 21 total commits
       - Per-region stance distribution: aggregate across plans 02-05 (capture from each wave's SUMMARY)
    3. **Playbook Surfaces Preserved**
       - §6 stagingDirHasFiles: InstallManager.ts:doDownload (≥1 site)
       - §7a normalizeBackslashPaths: InstallManager.ts FOMOD + real-install paths (≥3 sites)
       - §7b mergeCaseConflictingDirs: InstallManager.ts FOMOD + real-install paths (≥3 sites)
       - §7c-d (replaceAll backslash→slash + resolvePathCase(tempPath,...)): InstallManager.ts copy loop (≥1 site each)
       - 140a57217 resolvePathCase(dataPath,...): LinkingDeployment.ts only (3 sites — D-32-12 single-host invariant held)
       - Wine-era Proton fork-only block: activationStore.ts (D-32-02 fork-wins; preserved per plan 02)
       - transferPath import + 4 call sites: views/Settings.tsx (D-32-02 fork-wins; preserved per plan 03)
       - errorCodes.add fork-only block: LinkingDeployment.ts @212 (D-32-02 fork-wins; preserved per plan 04)
    4. **Harness + Typecheck Final Numbers**
       - Harness exit code (default mode): 0; 7/7 gates PASS
       - Bucket-scoped typecheck for `extensions/mod_management/`: 0 non-marker errors
       - Renderer-wide non-marker errors (informational, NOT gating): ≈347 in Phase 33/34 paths
    5. **Risks Cleared**
       - R1 InstallManager blast radius: mitigated (40 regions resolved, 6 dangerous fork-side, harness green)
       - R2 LinkingDeployment 140a57217 host: D-32-12 invariant held
       - R3 Husky on partial markers: D-32-10 norm — only deviated when noted in commit body; audit confirms
       - R5 Bluebird trap (TS1064): stayed latent — no new imports across all 15 files
    6. **Decisions Honored** — list D-32-01 through D-32-15 with one-line each on how each was satisfied
    7. **Forward-pointer**
       - Update STATE.md: Phase 32 → complete; current_phase → 33
       - Update ROADMAP.md: tick Phase 32 box
       - Operator action: push `v8.1/config-bucket` (sandbox cannot per `feedback_git_push_ssh.md`)
       - Phase 33 (next): per ROADMAP, scope TBD by `/gsd:discuss-phase 33`

    Step C — Write `32-06-SUMMARY.md` (this plan's own micro-SUMMARY for D-32-09 norm). Brief:
    - Wave 5 outcome: phase verified
    - Tasks 1-3 outputs (harness exit, typecheck count, audit result, single-host status)
    - Reference: PHASE-SUMMARY.md is the canonical phase-close doc; this is just the per-plan summary

    Step D — Single sign-off commit. Title:
    `docs(32-06): phase 32 verified complete (15 files / 97 regions / 7 gates green)`

    Body:
    - "Wave 5 sign-off. Final harness exit 0 (no skip flag, all 7 gates PASS)."
    - "Bucket-scoped typecheck for extensions/mod_management/: 0 non-marker errors."
    - "Single-host invariant (D-32-12) held; resolvePathCase(dataPath,...) confined to LinkingDeployment.ts (3 sites)."
    - "Commit-body audit: 15/15 file commits conform to D-32-09 + Pattern S5; all SSH-signed."
    - "VALIDATION sign-off appended; PHASE-SUMMARY written."
    - Files: 32-VALIDATION.md, 32-PHASE-SUMMARY.md, 32-06-SUMMARY.md

    Per `feedback_planning_gitignored.md`: use `git add -f` for all three `.planning/` paths.
    SSH-signed; no `--no-verify`.
    Per `feedback_git_push_ssh.md`: do NOT push from sandbox.

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      grep -q 'Phase-End Sign-Off' .planning/phases/32-mod-management-hot-zone-v2-0-1/32-VALIDATION.md && \
      test -f .planning/phases/32-mod-management-hot-zone-v2-0-1/32-PHASE-SUMMARY.md && \
      grep -q '15 mod_management' .planning/phases/32-mod-management-hot-zone-v2-0-1/32-PHASE-SUMMARY.md && \
      grep -q '97 conflict regions' .planning/phases/32-mod-management-hot-zone-v2-0-1/32-PHASE-SUMMARY.md && \
      test -f .planning/phases/32-mod-management-hot-zone-v2-0-1/32-06-SUMMARY.md && \
      git log -1 --pretty=%s | grep -q '^docs(32-06): phase 32 verified complete' && \
      git log -1 --show-signature 2>&1 | grep -q 'Good "git" signature' && \
      echo "PHASE 32 SIGN-OFF COMMITTED"
    </automated>
  </verify>
  <acceptance_criteria>
    - 32-VALIDATION.md has "## Phase-End Sign-Off" section with all 9 listed fields
    - 32-PHASE-SUMMARY.md exists with all 7 sections, "15 mod_management" and "97 conflict regions" wording present
    - 32-06-SUMMARY.md exists (per D-32-09 per-plan SUMMARY norm)
    - One SSH-signed commit on `v8.1/config-bucket` with title `docs(32-06): phase 32 verified complete (15 files / 97 regions / 7 gates green)`
    - `git log -1 --show-signature` reports `Good "git" signature`
    - `git add -f` used for all three gitignored `.planning/` paths
    - No `--no-verify`
  </acceptance_criteria>
  <done>
    Phase 32 documentation complete. STATE.md update + ROADMAP.md tick are operator-side via gsd-sdk
    `complete-phase` (next workflow), not this plan.
  </done>
</task>

</tasks>

<verification>
After plan completes:
- `bash .planning/phases/32-mod-management-hot-zone-v2-0-1/scripts/grep-checkpoint.sh` exits 0 (no skip; all 7 gates green)
- `git grep -c '^<<<<<<< ' src/renderer/src/extensions/mod_management/` summed = 0
- `pnpm -F @vortex/renderer typecheck 2>&1 | grep 'error TS' | grep 'extensions/mod_management/' | grep -v TS1185 | wc -l` = 0
- `git grep -nE 'resolvePathCase\(dataPath,' src/renderer/src/extensions/mod_management/` returns hits ONLY in LinkingDeployment.ts (3 sites)
- 32-VALIDATION.md updated with phase-end sign-off
- 32-PHASE-SUMMARY.md exists
- 32-06-SUMMARY.md exists
- Last commit titled `docs(32-06): phase 32 verified complete (15 files / 97 regions / 7 gates green)` and SSH-signed
</verification>

<success_criteria>

- All 7 harness gates GREEN in default mode (gate 7 no-marker = the one gated this whole phase)
- Bucket-scoped typecheck for mod_management/ = 0 non-marker errors
- D-32-12 single-host invariant held at phase end
- 15/15 file-resolution commits conform to D-32-09 + Pattern S5
- 15/15 commits SSH-signed; 5/5 SUMMARY commits SSH-signed; sign-off commit SSH-signed
- VALIDATION.md sign-off + PHASE-SUMMARY.md complete
- Phase 32 ready for STATE.md/ROADMAP.md tick via complete-phase workflow
  </success_criteria>

<output>
- Update `.planning/phases/32-mod-management-hot-zone-v2-0-1/32-VALIDATION.md` (append sign-off)
- Create `.planning/phases/32-mod-management-hot-zone-v2-0-1/32-PHASE-SUMMARY.md`
- Create `.planning/phases/32-mod-management-hot-zone-v2-0-1/32-06-SUMMARY.md`
</output>
