---
phase: 30-land-tag
plan: 03
subsystem: ff-merge
type: execute
wave: 4
depends_on: [30-02]
files_modified:
    - .planning/phases/30-land-tag/30-MERGE-EVIDENCE.md
autonomous: false
requirements: [SYNC-36]
tags: [linux-port, upstream-v2.0.0, phase-30, ff-merge, sync-36]

requirements_in_play: [SYNC-36]

dependency_graph:
    requires:
        - "30-02 complete (SYNC-35 PASS — Windows + Linux + Format CI green on rebased HEAD)"
        - "30-PRE-STATE.md gh CLI merge-flag verification section (decides primary vs fallback invocation)"
    provides:
        - ".planning/phases/30-land-tag/30-MERGE-EVIDENCE.md (PR #4 MERGED state, post-FF master HEAD SHA)"
        - "fork/master fast-forwarded to PR #4 head (no merge commit)"
    affects:
        - "30-04 (canonical tag is created on the post-FF master HEAD)"
        - "30-06 (cherry-pick range master..post-FF-master is the enumeration source)"

gates:
    pre_conditions:
        - "30-02 SYNC-35 evidence shows all CI legs green on PR #4 head SHA"
        - "PR #4 mergeStateStatus is CLEAN (no required-check failures, no merge conflicts surface)"
        - "Master branch protection allows the chosen merge mode (verified in 30-00)"
    post_conditions:
        - "PR #4 state = MERGED"
        - "fork/master HEAD equals the post-rebase v8.0/config-bucket HEAD that was on PR #4 (linear FF — no merge commit)"
        - "git merge-base master <post-FF-master-HEAD> == <post-FF-master-HEAD> (proves FF, not merge)"

must_haves:
    truths:
        - "PR #4 MERGED on GitHub — atabisz/Vortex#4 transitions OPEN to MERGED"
        - "fork/master is fast-forwarded to PR #4 head (no merge commit in master lineage)"
        - "Decision tree honored: try gh primary first (`gh pr merge 4 --merge=fast-forward` per D-30-01); on rejection (Pitfall 2), fall back to manual `git push fork v8.0/config-bucket:master` lease-pinned + `gh pr close 4` with explanatory comment"
        - "Post-FF master HEAD SHA captured for downstream plans (30-04 tag target, 30-06 cherry-pick range start)"
    artifacts:
        - path: ".planning/phases/30-land-tag/30-MERGE-EVIDENCE.md"
          provides: "SYNC-36 evidence — PR #4 MERGED, post-FF master HEAD recorded"
          contains: "## SYNC-36"
    key_links:
        - from: "PR #4 (atabisz/Vortex#4) head SHA"
          to: "fork/master HEAD post-FF"
          via: "FF-merge — head SHA becomes master HEAD verbatim, no merge commit"
          pattern: "git merge-base"
---

<objective>
Fast-forward merge PR #4 onto master so master HEAD becomes the post-rebase v8.0/config-bucket HEAD verbatim. Try the gh CLI primary path first; fall back to manual `git push fork` + `gh pr close` if the gh flag is rejected. Capture post-FF master HEAD SHA + PR transition to MERGED state in `30-MERGE-EVIDENCE.md`.

Purpose: This is the publish step — the resolved-merge content lands on master and PR #4 closes. Subsequent plans tag this commit (30-04) and use it as the cherry-pick range start (30-06).

Output: master HEAD == post-rebase v8.0 HEAD; PR #4 MERGED; 30-MERGE-EVIDENCE.md committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-land-tag/30-CONTEXT.md
@.planning/phases/30-land-tag/30-RESEARCH.md
@.planning/phases/30-land-tag/30-PRE-STATE.md
@.planning/phases/30-land-tag/30-CI-EVIDENCE.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3-1: Attempt gh FF-merge (primary path); fall back to manual FF push if gh rejects</name>
  <files>.planning/phases/30-land-tag/30-MERGE-EVIDENCE.md</files>
  <read_first>
    .planning/phases/30-land-tag/30-RESEARCH.md (Pattern 1 sequence step 7, Pitfall 2 + Open Questions §2 fallback path, Assumptions Log A1)
    .planning/phases/30-land-tag/30-PRE-STATE.md (gh CLI merge-flag verification section — primary vs fallback decision)
    .planning/phases/30-land-tag/30-CONTEXT.md (D-30-01 fallback note)
  </read_first>
  <what-built>
    PR #4 fast-forward merged onto master. Either via `gh pr merge --merge=fast-forward` (primary) or manual `git push fork v8.0/config-bucket:master` (fallback).
  </what-built>
  <action>
    Capture pre-merge state for the evidence file:

    1. `PRE_FF_MASTER=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1)` — should equal local master HEAD (pushed in 30-00).
    2. `PR_HEAD=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/sync/upstream-v2.0.0 | cut -f1)` — current rebased HEAD (post-30-02 docs commit).
    3. `gh pr view 4 --repo atabisz/Vortex --json state,mergeStateStatus,headRefOid,baseRefOid` — must show state=OPEN, mergeStateStatus=CLEAN.

    Pre-flight gate: state must be OPEN AND mergeStateStatus must be CLEAN. If either fails, STOP — surface and triage.

    **Primary path** (per 30-PRE-STATE.md gh-flag decision):

    ```
    gh pr merge 4 --repo atabisz/Vortex --merge=fast-forward
    ```

    (or whatever exact invocation 30-PRE-STATE.md decided on — could be `--merge` without value if the gh version requires that shape).

    Verify: `gh pr view 4 --repo atabisz/Vortex --json state -q .state` returns `MERGED`. AND `git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1` equals `$PR_HEAD` (FF-merge: master jumped to PR head verbatim, no merge commit interposed).

    **If primary fails** (gh rejects flag, or merge produces a merge commit when we wanted FF):

    Fallback path (Open Questions §2 mitigation):

    1. `LIVE_FORK_MASTER=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1)` — re-verify SHA.
    2. `git push --force-with-lease=master:$LIVE_FORK_MASTER git@github.com:atabisz/Vortex.git v8.0/config-bucket:master` — manual FF push of the rebased v8.0 head onto fork/master. Note `--force-with-lease` not `--force`: this IS a true FF (master is ancestor of v8.0 post-rebase) so the lease pin protects against concurrent pushes only.
    3. `gh pr close 4 --repo atabisz/Vortex --comment "merged via FF push to master at $(git rev-parse v8.0/config-bucket); gh pr merge --merge=fast-forward not available on this gh version"`.

    Verify either path:
    - `gh pr view 4 --repo atabisz/Vortex --json state -q .state` returns `MERGED` (primary) or `CLOSED` (fallback close-with-comment).
    - `POST_FF_MASTER=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1)`. Must equal `$PR_HEAD` (the rebased v8.0 HEAD pushed to PR #4).
    - `git fetch git@github.com:atabisz/Vortex.git master` (or whatever local fetch idiom works in sandbox); `git merge-base $POST_FF_MASTER $PR_HEAD` must equal `$POST_FF_MASTER` (FF: master is ancestor of v8.0; merge-base with PR head returns master, AND master == v8.0 head, AND merge-base of equals is itself).

    Write `.planning/phases/30-land-tag/30-MERGE-EVIDENCE.md` with:
    - `# Phase 30 Merge Evidence (SYNC-36)`
    - `## SYNC-36 — PR #4 fast-forward merged onto master`
    - **Path taken**: primary (gh) OR fallback (manual push) — name which.
    - **Pre-FF master SHA**: `$PRE_FF_MASTER`
    - **PR #4 head SHA**: `$PR_HEAD`
    - **Post-FF master SHA**: `$POST_FF_MASTER` (must equal `$PR_HEAD`)
    - **PR #4 final state**: MERGED or CLOSED
    - **Merge command**: exact invocation used
    - **FF verification**: `git merge-base` output, must equal master HEAD
    - **Result: PASS**

    Casual voice. Phase 29 done-gate section style.

  </action>
  <how-to-verify>
    1. `gh pr view 4 --repo atabisz/Vortex --json state -q .state` returns `MERGED` or `CLOSED`.
    2. `git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1` equals the PR_HEAD captured pre-merge.
    3. No merge commit in master lineage: visit https://github.com/atabisz/Vortex/commits/master in browser, or `gh api repos/atabisz/Vortex/commits/master --jq '.parents | length'` returns `1` (single parent = FF, not merge).
    4. 30-MERGE-EVIDENCE.md has `## SYNC-36` section with all SHAs captured + path taken named.
    5. `gh pr view 4 --repo atabisz/Vortex --json mergedAt -q .mergedAt` returns a timestamp (primary path) OR `gh pr view 4 --json closedAt` returns a timestamp (fallback).
  </how-to-verify>
  <acceptance_criteria>
    - `gh pr view 4 --repo atabisz/Vortex --json state -q .state` returns one of: `MERGED`, `CLOSED`.
    - `git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1` equals the PR head SHA captured pre-merge.
    - `gh api repos/atabisz/Vortex/commits/master --jq '.parents | length'` returns `1` (single-parent = FF, not merge commit).
    - `.planning/phases/30-land-tag/30-MERGE-EVIDENCE.md` exists, contains `## SYNC-36`, contains the literal `Result: PASS`, names the path taken (primary or fallback).
    - 30-MERGE-EVIDENCE.md records pre-FF master SHA, PR head SHA, post-FF master SHA — and post-FF master == PR head.
  </acceptance_criteria>
  <resume-signal>Type "approved" once PR #4 closes with FF semantics confirmed. Type "fallback" if primary failed and you used the manual path. Type "stop" if neither path landed cleanly.</resume-signal>
</task>

<task type="auto">
  <name>Task 3-2: Commit + push merge evidence</name>
  <files>.planning/phases/30-land-tag/30-MERGE-EVIDENCE.md</files>
  <read_first>
    .planning/phases/30-land-tag/30-MERGE-EVIDENCE.md (the file we're committing)
  </read_first>
  <action>
    Standard atomic commit pattern (Phase 29 §Verify-script-rules):

    1. `git status --short` — expect only the new evidence file.
    2. `git add -f .planning/phases/30-land-tag/30-MERGE-EVIDENCE.md`.
    3. `git commit -m 'docs(30-03): SYNC-36 — PR #4 fast-forward merged onto master'`.
    4. `git log -1 --show-signature` — confirm SSH-signed.
    5. Push to fork: standard inline-SSH-URL lease-pin idiom against sync/upstream-v2.0.0. After 30-03's FF, the PR #4 ref `sync/upstream-v2.0.0` may or may not still exist — verify with `git ls-remote git@github.com:atabisz/Vortex.git refs/heads/sync/upstream-v2.0.0`. If the branch is deleted (gh's default after merge), push to `master` instead since v8.0/config-bucket is now ancestor of master.

    Sequence:
    ```
    LIVE_SYNC=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/sync/upstream-v2.0.0 | cut -f1)
    if [ -n "$LIVE_SYNC" ]; then
      git push --force-with-lease=sync/upstream-v2.0.0:$LIVE_SYNC git@github.com:atabisz/Vortex.git v8.0/config-bucket:sync/upstream-v2.0.0
    fi
    LIVE_MASTER=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1)
    git push --force-with-lease=master:$LIVE_MASTER git@github.com:atabisz/Vortex.git v8.0/config-bucket:master
    ```

    Note: pushing the docs commit to fork/master ADVANCES master one commit past the FF-merge point. That's fine — Phase 30 docs commits live on master alongside the milestone artifacts. The canonical tag in 30-04 will be placed on this advanced master HEAD (which contains the docs commit), not the bare FF point.

    Decision: tag placement is on the post-30-03 master HEAD (includes 30-03 evidence commit). Documented here so 30-04 doesn't get confused.

  </action>
  <verify>
    <automated>git log --oneline v8.0/config-bucket -1 | grep -q "docs(30-03): SYNC-36" && git log -1 --show-signature 2>&1 | grep -q 'Good "git" signature' && POST=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1) && test "$POST" = "$(git rev-parse v8.0/config-bucket)"</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline v8.0/config-bucket -1` first line starts with `docs(30-03): SYNC-36`.
    - `git log -1 --show-signature 2>&1` contains `Good "git" signature`.
    - `git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1` equals `git rev-parse v8.0/config-bucket` (master advanced to include the docs commit).
  </acceptance_criteria>
  <done>30-MERGE-EVIDENCE.md committed + pushed; master advanced with the evidence commit; SSH-signed.</done>
</task>

</tasks>

<verification>
- PR #4 MERGED (or CLOSED with explanatory comment in fallback path)
- master HEAD == post-rebase v8.0/config-bucket HEAD (modulo the 30-03 docs commit, which is +1 ahead)
- No merge commit on master (single-parent FF semantics confirmed)
- 30-MERGE-EVIDENCE.md captures all SHAs + path taken + Result: PASS
</verification>

<success_criteria>
SYNC-36 PASS: PR #4 fast-forward merged. master is ready to be tagged in 30-04.
</success_criteria>

<output>
30-MERGE-EVIDENCE.md remains in place — 30-08 done-gate cites it. No SUMMARY.md needed.
</output>
