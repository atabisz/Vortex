---
phase: 30-land-tag
plan: 05
subsystem: deferred-evidence
type: execute
wave: 6
depends_on: [30-04]
files_modified:
    - .planning/phases/30-land-tag/30-CANONICAL-SMOKE-EVIDENCE.md
    - .planning/phases/30-land-tag/screenshots/
autonomous: false
requirements: [SYNC-33, SYNC-34]
tags: [linux-port, upstream-v2.0.0, phase-30, deferred, sync-33, sync-34, carry-forward]

requirements_in_play: [SYNC-33, SYNC-34]

dependency_graph:
    requires:
        - "30-04 complete (canonical tag pushed, release-linux.yml green, canonical AppImage + .deb downloaded to ~/Downloads/vortex-canonical/)"
    provides:
        - ".planning/phases/30-land-tag/30-CANONICAL-SMOKE-EVIDENCE.md (carry-forward Phase 29 SYNC-33-C + SYNC-34 evidence vs canonical tag artefacts)"
        - "6 screenshots: 1 AppImage boot + 1 .deb desktop launch + 4 Skyrim walkthrough"
    affects:
        - "30-08 done-gate (rolls up SYNC-33-C + SYNC-34 carry-forward)"

gates:
    pre_conditions:
        - "Canonical AppImage + .deb downloaded to ~/Downloads/vortex-canonical/ (from 30-04 Task 4-5)"
        - "Phase 29 SYNC-33 part A + part B already PASS (29-DONE-GATE.md SYNC-33 sections)"
        - "Steam + Proton installed (Skyrim SE daily-driver setup)"
    post_conditions:
        - "30-CANONICAL-SMOKE-EVIDENCE.md captures: canonical AppImage SHA256 boot evidence (chmod+x → launch → screenshot); canonical .deb sudo apt install + desktop-entry launch screenshot; 4-screenshot Skyrim SE walkthrough (game detection, NXM mod install, deploy w/ LOOT autosort, Proton launch w/ tray)"
        - "All 6 PNGs exist under .planning/phases/30-land-tag/screenshots/ with naming convention 30-<artefact>-<step>.png"
        - "Both Phase 29 deferred items closed against canonical tag artefacts"

must_haves:
    truths:
        - "SYNC-33 part C (carry-forward): canonical AppImage boots when chmod+x'd; canonical .deb installs via sudo apt and launches via desktop entry"
        - "SYNC-34 (carry-forward): 4-screenshot Skyrim SE walkthrough on canonical AppImage covering D-29-03 4 checkpoints"
        - "Evidence file matches Phase 29 SMOKE-EVIDENCE.md shape — section per acceptance gate, PASS/FAIL verdict, screenshot paths"
        - "If canonical AppImage fails to boot, plan FAILS — done-gate cannot close (R-30-05 contingency: don't roll Phase 30 to complete with this failing; diagnose, fix, re-tag canonical -rc2 or revised)"
        - "libfuse2 install path documented if AppImage requires it on Ubuntu 24.04 (Assumption A5 mitigation)"
        - "Screenshots use casual filenames matching Phase 29 precedent: `30-appimage-boot.png`, `30-deb-desktop-launch.png`, `30-skyrim-01-detection.png` etc."
    artifacts:
        - path: ".planning/phases/30-land-tag/30-CANONICAL-SMOKE-EVIDENCE.md"
          provides: "SYNC-33-C + SYNC-34 carry-forward evidence vs canonical tag"
          contains: "## SYNC-33"
        - path: ".planning/phases/30-land-tag/screenshots/"
          provides: "6 PNG files: AppImage boot, .deb desktop launch, 4 Skyrim checkpoints"
          contains: "30-appimage-boot.png"
    key_links:
        - from: "v2.0.0-linux-rebased canonical AppImage SHA256"
          to: "30-CANONICAL-SMOKE-EVIDENCE.md SYNC-33 part C section"
          via: "Same SHA captured in 30-TAG-EVIDENCE.md cross-referenced as evidence-of-binding"
          pattern: "v2.0.0-linux-rebased"
---

<objective>
Close out the two Phase 29 deferred items against canonical tag (v2.0.0-linux-rebased) artefacts:
- SYNC-33 part C: chmod+x AppImage launch + sudo apt install .deb + desktop-entry launch (2 screenshots).
- SYNC-34: 4-screenshot Skyrim SE walkthrough (game detection, NXM mod install, deploy w/ LOOT autosort, Proton launch w/ tray).

Purpose: Phase 29 split SYNC-33 into parts A/B/C and deferred part C + SYNC-34 to Phase 30 because they require canonical (non-RC) tag artefacts. ROADMAP carry-forward: Phase 30 closes both gates against the v2.0.0-linux-rebased release.

Output: 30-CANONICAL-SMOKE-EVIDENCE.md + 6 PNGs under screenshots/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-land-tag/30-CONTEXT.md
@.planning/phases/30-land-tag/30-RESEARCH.md
@.planning/phases/30-land-tag/30-TAG-EVIDENCE.md
@.planning/phases/29-build-verification/29-SMOKE-EVIDENCE.md
@.planning/phases/29-build-verification/29-DONE-GATE.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5-1: Canonical AppImage chmod+x boot + screenshot (SYNC-33-C part 1)</name>
  <files>
    .planning/phases/30-land-tag/30-CANONICAL-SMOKE-EVIDENCE.md
    .planning/phases/30-land-tag/screenshots/30-appimage-boot.png
  </files>
  <read_first>
    .planning/phases/30-land-tag/30-RESEARCH.md (Code Example 3, Assumption A5 libfuse2, Environment Availability table)
    .planning/phases/29-build-verification/29-SMOKE-EVIDENCE.md (Phase 29 SYNC-33 part A from-source boot screenshot for shape reference)
    .planning/phases/30-land-tag/30-TAG-EVIDENCE.md (canonical SHA256 to cross-reference)
  </read_first>
  <what-built>
    Canonical AppImage boots on Linux desktop. Window screenshot captures the running app.
  </what-built>
  <action>
    Setup screenshot dir: `mkdir -p .planning/phases/30-land-tag/screenshots`.

    Initialize 30-CANONICAL-SMOKE-EVIDENCE.md with header (timestamp, Linux distro version, kernel, env). Add `# Phase 30 Canonical Smoke Evidence (carry-forward SYNC-33-C + SYNC-34)`.

    Boot the canonical AppImage:
    1. `cd ~/Downloads/vortex-canonical`.
    2. `ls *.AppImage` — confirm AppImage filename. Capture exact filename for evidence.
    3. `chmod +x *.AppImage`.
    4. `./vortex*.AppImage &` — launch in background.
    5. If AppImage refuses with "AppImages require FUSE" (Assumption A5): `sudo apt install libfuse2`, retry. Document the libfuse2 install in evidence file.
    6. Wait ~10s for window to appear. Take a screenshot of the running app window: `gnome-screenshot -w -f .planning/phases/30-land-tag/screenshots/30-appimage-boot.png` (or use `flameshot gui --path .planning/phases/30-land-tag/screenshots/ --filename 30-appimage-boot` if gnome-screenshot unavailable). Show the main Vortex window with at least the title bar + version visible.
    7. Confirm version in window title or About dialog matches the canonical tag: should show 1.16.8 or similar v2.0.0-aligned label.
    8. Close the running AppImage cleanly.

    Append `## SYNC-33 part C (canonical) — AppImage chmod+x launch` section to 30-CANONICAL-SMOKE-EVIDENCE.md:
    - Canonical AppImage filename + SHA256 (cross-reference 30-TAG-EVIDENCE.md)
    - chmod + launch commands
    - libfuse2 install (if needed)
    - Screenshot path: `.planning/phases/30-land-tag/screenshots/30-appimage-boot.png`
    - Version observed in app
    - **Result: PASS**

    Casual voice. Use `git add -f` for the screenshot when committing in 5-4.

  </action>
  <how-to-verify>
    1. `test -f .planning/phases/30-land-tag/screenshots/30-appimage-boot.png && file .planning/phases/30-land-tag/screenshots/30-appimage-boot.png | grep -q "PNG image"` (file is a valid PNG).
    2. Eyeball the screenshot: shows the running Vortex AppImage window with visible title bar.
    3. 30-CANONICAL-SMOKE-EVIDENCE.md `## SYNC-33 part C (canonical) — AppImage chmod+x launch` section captures filename, SHA256, screenshot path, version, Result: PASS.
  </how-to-verify>
  <acceptance_criteria>
    - File `.planning/phases/30-land-tag/screenshots/30-appimage-boot.png` exists.
    - `file .planning/phases/30-land-tag/screenshots/30-appimage-boot.png` output contains literal `PNG image`.
    - `wc -c < .planning/phases/30-land-tag/screenshots/30-appimage-boot.png` returns > 10000 (non-trivial PNG, not stub).
    - 30-CANONICAL-SMOKE-EVIDENCE.md contains literal text `## SYNC-33 part C (canonical) — AppImage chmod+x launch`.
    - That section contains literal text `Result: PASS`.
    - Section cross-references the canonical AppImage SHA256 captured in 30-TAG-EVIDENCE.md.
  </acceptance_criteria>
  <resume-signal>Type "approved" once AppImage boots and screenshot captured. Type "stop" if AppImage fails to boot — do NOT proceed; surface to user (R-30-05 — likely blocks done-gate).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5-2: Canonical .deb sudo apt install + desktop-entry launch screenshot (SYNC-33-C part 2)</name>
  <files>
    .planning/phases/30-land-tag/30-CANONICAL-SMOKE-EVIDENCE.md
    .planning/phases/30-land-tag/screenshots/30-deb-desktop-launch.png
  </files>
  <read_first>
    .planning/phases/30-land-tag/30-RESEARCH.md (Code Example 3 — .deb install path, Environment Availability)
    .planning/phases/29-build-verification/29-SMOKE-EVIDENCE.md (Phase 29 SYNC-33 part C deferral text)
  </read_first>
  <what-built>
    Canonical .deb installed via sudo apt; Vortex launches via desktop entry (Activities or `gtk-launch vortex`).
  </what-built>
  <action>
    1. `cd ~/Downloads/vortex-canonical`.
    2. `ls *.deb` — confirm .deb filename.
    3. Install: `sudo apt install ./vortex_*.deb` (or `sudo dpkg -i ./vortex_*.deb && sudo apt-get install -f` if apt rejects local file).
    4. Verify install: `dpkg -l | grep -i vortex` — should show installed package.
    5. Verify desktop entry: `ls /usr/share/applications/ | grep -i vortex` — should list a `.desktop` file.
    6. Launch via desktop entry. Either:
        - GUI: open Activities, search "Vortex", click. OR
        - CLI: `gtk-launch vortex` (where `vortex` matches the .desktop file basename without `.desktop` extension).
    7. Wait ~10s for window. Screenshot: `gnome-screenshot -w -f .planning/phases/30-land-tag/screenshots/30-deb-desktop-launch.png`.
    8. Close the running app.

    **Cleanup decision**: do NOT uninstall the .deb — it's the daily-driver path now. Note in evidence file.

    Append `## SYNC-33 part C (canonical) — .deb install + desktop-entry launch` section to 30-CANONICAL-SMOKE-EVIDENCE.md:
    - Canonical .deb filename + SHA256 (cross-reference 30-TAG-EVIDENCE.md)
    - sudo apt install command + output snippet (last 10 lines)
    - dpkg -l verification line
    - Desktop entry path (/usr/share/applications/vortex.desktop or similar)
    - Launch command used (Activities or gtk-launch)
    - Screenshot path: `.planning/phases/30-land-tag/screenshots/30-deb-desktop-launch.png`
    - **Result: PASS**

    Casual voice.

  </action>
  <how-to-verify>
    1. `dpkg -l | grep -i vortex` shows installed package version matching canonical.
    2. `test -f .planning/phases/30-land-tag/screenshots/30-deb-desktop-launch.png && file .planning/phases/30-land-tag/screenshots/30-deb-desktop-launch.png | grep -q "PNG image"`.
    3. Eyeball: screenshot shows running Vortex window launched via desktop entry.
    4. 30-CANONICAL-SMOKE-EVIDENCE.md `## SYNC-33 part C (canonical) — .deb install + desktop-entry launch` section present with Result: PASS.
  </how-to-verify>
  <acceptance_criteria>
    - File `.planning/phases/30-land-tag/screenshots/30-deb-desktop-launch.png` exists and is valid PNG (`file` command output contains `PNG image`).
    - `dpkg -l | grep -i vortex | wc -l` returns >= 1 (Vortex installed).
    - `ls /usr/share/applications/ | grep -ic vortex` returns >= 1 (desktop entry exists).
    - 30-CANONICAL-SMOKE-EVIDENCE.md contains literal text `## SYNC-33 part C (canonical) — .deb install + desktop-entry launch`.
    - That section contains literal text `Result: PASS`.
  </acceptance_criteria>
  <resume-signal>Type "approved" once .deb installed and desktop-entry launch screenshot captured. Type "stop" if install or launch fails — surface to user (R-30-05).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5-3: 4-screenshot Skyrim SE walkthrough (SYNC-34)</name>
  <files>
    .planning/phases/30-land-tag/30-CANONICAL-SMOKE-EVIDENCE.md
    .planning/phases/30-land-tag/screenshots/30-skyrim-01-detection.png
    .planning/phases/30-land-tag/screenshots/30-skyrim-02-nxm.png
    .planning/phases/30-land-tag/screenshots/30-skyrim-03-deploy.png
    .planning/phases/30-land-tag/screenshots/30-skyrim-04-proton.png
  </files>
  <read_first>
    .planning/phases/30-land-tag/30-CONTEXT.md (D-29-03 4 checkpoints carry-forward, Phase 29 SYNC-34 deferral text)
    .planning/phases/30-land-tag/30-RESEARCH.md (Open Questions §3 — filename convention, Environment Availability — Steam + Proton + Nexus login)
    .planning/phases/29-build-verification/29-SMOKE-EVIDENCE.md (any prior Skyrim screenshot precedent)
  </read_first>
  <what-built>
    4 screenshots covering the D-29-03 carry-forward checklist: game detection, NXM mod install, deploy w/ LOOT autosort, Proton launch w/ tray. All against canonical AppImage launched in Task 5-1 (re-launch fresh for this task).
  </what-built>
  <action>
    Launch canonical Vortex (use the AppImage from Task 5-1 OR the .deb-installed Vortex from Task 5-2 — either is canonical artefact). Run through 4 checkpoints with screenshot at each:

    **Checkpoint 1 — Game detection**: Open Vortex's Games tab. Confirm Skyrim Special Edition is auto-detected (Steam library scan picks it up). Screenshot the Games tab with Skyrim SE listed/managed.
    - Screenshot: `gnome-screenshot -w -f .planning/phases/30-land-tag/screenshots/30-skyrim-01-detection.png`.

    **Checkpoint 2 — NXM mod install**: Click an NXM-protocol link from Nexus Mods (any Skyrim SE mod — pick a small popular one for speed). Vortex catches the protocol, shows the install dialog, mod downloads + installs to Mods list. Screenshot the Mods list with the freshly-installed mod visible.
    - Screenshot: `gnome-screenshot -w -f .planning/phases/30-land-tag/screenshots/30-skyrim-02-nxm.png`.

    **Checkpoint 3 — Deploy with LOOT autosort**: Enable the mod, click Deploy. Confirm LOOT runs (Plugins tab shows autosort happened). Screenshot Plugins tab post-deploy with the new plugin sorted in load order.
    - Screenshot: `gnome-screenshot -w -f .planning/phases/30-land-tag/screenshots/30-skyrim-03-deploy.png`.

    **Checkpoint 4 — Proton launch with tray**: Click Vortex's Launch button for Skyrim SE. Steam launches the game via Proton. Confirm the Vortex tray icon appears (system tray / status area) AND the Skyrim SE main menu loads. Screenshot Skyrim main menu with Vortex tray icon visible — if you can fit both in one screenshot, great; if not, use two and document.
    - Screenshot: `gnome-screenshot -w -f .planning/phases/30-land-tag/screenshots/30-skyrim-04-proton.png`.

    Close Skyrim. Cleanup: leave the test mod installed (no need to uninstall).

    Append `## SYNC-34 (canonical) — 4-screenshot Skyrim SE walkthrough` section to 30-CANONICAL-SMOKE-EVIDENCE.md with:
    - Vortex artefact used (AppImage or .deb-installed)
    - Per-checkpoint subsection: checkpoint name (Game detection / NXM mod install / Deploy w/ LOOT autosort / Proton launch w/ tray), action description (1-2 sentences), screenshot path
    - Mod name used in checkpoint 2 (for reproducibility)
    - **Result: PASS** if all 4 checkpoints pass; FAIL if any block

    Casual voice. If any checkpoint fails (e.g., NXM handler not registered, LOOT autosort doesn't run, Proton launch hangs), STOP and surface — that's a regression vs Phase 29 SYNC-33 part A (which proved the from-source build did all of this). R-30-05 path: triage, fix, re-tag.

  </action>
  <how-to-verify>
    1. All 4 PNG files exist: `for f in 30-skyrim-01-detection.png 30-skyrim-02-nxm.png 30-skyrim-03-deploy.png 30-skyrim-04-proton.png; do test -f .planning/phases/30-land-tag/screenshots/$f && file .planning/phases/30-land-tag/screenshots/$f | grep -q "PNG image" || echo "MISSING: $f"; done`.
    2. Eyeball each: shows the expected state (Skyrim SE detected, mod in list, plugin sorted, Proton-launched game with tray).
    3. 30-CANONICAL-SMOKE-EVIDENCE.md `## SYNC-34 (canonical) — 4-screenshot Skyrim SE walkthrough` section present with 4 checkpoint subsections + Result: PASS.
  </how-to-verify>
  <acceptance_criteria>
    - All 4 files exist: `.planning/phases/30-land-tag/screenshots/30-skyrim-01-detection.png`, `30-skyrim-02-nxm.png`, `30-skyrim-03-deploy.png`, `30-skyrim-04-proton.png`.
    - Each PNG passes `file ... | grep -q "PNG image"`.
    - Each PNG `wc -c < ...` returns > 10000.
    - 30-CANONICAL-SMOKE-EVIDENCE.md contains literal text `## SYNC-34 (canonical) — 4-screenshot Skyrim SE walkthrough`.
    - Section contains 4 checkpoint subsections (one per screenshot).
    - Section contains literal text `Result: PASS`.
  </acceptance_criteria>
  <resume-signal>Type "approved" once all 4 Skyrim screenshots captured and walkthrough completed cleanly. Type "stop" + describe failure if any checkpoint blocks (regression vs Phase 29 from-source build).</resume-signal>
</task>

<task type="auto">
  <name>Task 5-4: Commit + push canonical smoke evidence + screenshots</name>
  <files>
    .planning/phases/30-land-tag/30-CANONICAL-SMOKE-EVIDENCE.md
    .planning/phases/30-land-tag/screenshots/
  </files>
  <read_first>
    .planning/phases/30-land-tag/30-CANONICAL-SMOKE-EVIDENCE.md (final review of evidence file)
  </read_first>
  <action>
    Verify all 6 files present:
    ```
    ls -la .planning/phases/30-land-tag/screenshots/
    test -f .planning/phases/30-land-tag/30-CANONICAL-SMOKE-EVIDENCE.md
    ls .planning/phases/30-land-tag/screenshots/ | wc -l   # expect 6
    ```

    Atomic commit (Phase 29 §Verify-script-rules):
    1. `git add -f .planning/phases/30-land-tag/30-CANONICAL-SMOKE-EVIDENCE.md .planning/phases/30-land-tag/screenshots/*.png` (use `-f` because `.planning/` is gitignored).
    2. `git status --short` — confirm only the 7 expected files staged (1 md + 6 PNG).
    3. `git commit -m 'docs(30-05): SYNC-33-C + SYNC-34 carry-forward — canonical smoke evidence'`.
    4. `git log -1 --show-signature` — confirm SSH-signed.
    5. Push to fork master: `LIVE=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1); git push --force-with-lease=master:$LIVE git@github.com:atabisz/Vortex.git v8.0/config-bucket:master`.
    6. Verify post-push: `POST=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1); test "$POST" = "$(git rev-parse v8.0/config-bucket)"`.

  </action>
  <verify>
    <automated>git log --oneline v8.0/config-bucket -1 | grep -q "docs(30-05): SYNC-33-C + SYNC-34" && git log -1 --show-signature 2>&1 | grep -q 'Good "git" signature' && [ $(ls .planning/phases/30-land-tag/screenshots/*.png 2>/dev/null | wc -l) -ge 6 ] && POST=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1) && test "$POST" = "$(git rev-parse v8.0/config-bucket)"</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline v8.0/config-bucket -1` first line starts with `docs(30-05): SYNC-33-C + SYNC-34`.
    - `git log -1 --show-signature 2>&1` contains `Good "git" signature`.
    - `git log -1 --stat` shows 1 markdown file + 6 PNGs added (or modified) under `.planning/phases/30-land-tag/`.
    - `git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1` equals `git rev-parse v8.0/config-bucket`.
  </acceptance_criteria>
  <done>30-CANONICAL-SMOKE-EVIDENCE.md + 6 screenshots committed + pushed; SSH-signed.</done>
</task>

</tasks>

<verification>
- AppImage boots cleanly via chmod+x; screenshot captured
- .deb installs via sudo apt + desktop-entry launch; screenshot captured
- 4 Skyrim SE checkpoints (detection, NXM, deploy w/ LOOT, Proton w/ tray) all PASS with screenshots
- 6 PNGs under screenshots/ directory
- 30-CANONICAL-SMOKE-EVIDENCE.md captures all evidence with Result: PASS for both SYNC-33 part C and SYNC-34
- Single signed commit landed
</verification>

<success_criteria>
Phase 29 deferred items closed against canonical tag artefacts. SYNC-33-C + SYNC-34 carry-forward PASS evidence in place for done-gate roll-up.
</success_criteria>

<output>
30-CANONICAL-SMOKE-EVIDENCE.md + screenshots remain in place for 30-08 done-gate to cite. No SUMMARY.md needed.
</output>
