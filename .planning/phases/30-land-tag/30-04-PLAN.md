---
phase: 30-land-tag
plan: 04
subsystem: tag-and-release
type: execute
wave: 5
depends_on: [30-03]
files_modified:
    - .planning/phases/30-land-tag/30-TAG-EVIDENCE.md
autonomous: false
requirements: [SYNC-37]
tags: [linux-port, upstream-v2.0.0, phase-30, tag, release, sync-37]

requirements_in_play: [SYNC-37]

dependency_graph:
    requires:
        - "30-03 complete (PR #4 MERGED, master FF'd to post-rebase head, 30-03 evidence commit landed)"
    provides:
        - ".planning/phases/30-land-tag/30-TAG-EVIDENCE.md (RC tag deletion log + canonical tag SHA + release-linux.yml conclusion + canonical AppImage/.deb SHA256s)"
        - "Annotated SSH-signed tag v2.0.0-linux-rebased on post-FF master HEAD; pushed to fork + origin"
        - "GitHub Release Linux Beta v2.0.0-linux-rebased with canonical-named AppImage + .deb assets"
    affects:
        - "30-05 (consumes canonical-tag artefacts for SYNC-33-C and SYNC-34 deferred evidence)"

gates:
    pre_conditions:
        - "30-03 complete; master HEAD on fork is the post-FF point (plus 30-03 docs commit)"
        - "Local working tree clean"
        - "RC tag v2.0.0-linux-rebased-rc1 still exists on fork remote (per 30-PRE-STATE.md baseline)"
    post_conditions:
        - "RC tag v2.0.0-linux-rebased-rc1 deleted from local + fork remote"
        - "Canonical tag v2.0.0-linux-rebased exists locally, annotated, SSH-signed; `git tag -v v2.0.0-linux-rebased` exit 0"
        - "Canonical tag pushed to fork (CI-triggering side) AND origin (informational; failure non-blocking)"
        - "release-linux.yml run on canonical tag concludes success; AppImage + .deb published"
        - "30-TAG-EVIDENCE.md captures all of the above + canonical AppImage/.deb SHA256s"

must_haves:
    truths:
        - "RC tag deletion ordering honored (D-30-02): local deletion first (`git tag -d`), then fork remote (`git push :refs/tags/...`), THEN canonical tag created — avoids window where both exist"
        - "Canonical tag annotated + SSH-signed (Pattern 2): `git tag -a -s` (or just `-a` since tag.gpgsign=true is configured)"
        - "Tag placed on post-30-03 master HEAD (the docs commit IS part of milestone history per 30-03 task 3-2 decision)"
        - "Push to fork FIRST (Pitfall 5: CI-triggering side, where we have write); origin second (informational)"
        - "release-linux.yml run completes success; canonical AppImage + .deb captured with sha256sum"
        - "Canonical tag annotation matches D-30-02 shape: milestone summary + Phase 29/30 evidence pointers; casual voice"
        - "GitHub Release for RC tag (v2.0.0-linux-rebased-rc1) NOT deleted (Assumption A2 — keeps Phase 29 evidence URLs alive)"
    artifacts:
        - path: ".planning/phases/30-land-tag/30-TAG-EVIDENCE.md"
          provides: "SYNC-37 evidence — tag created, signed, pushed, release-linux.yml green, canonical asset SHA256s"
          contains: "## SYNC-37"
    key_links:
        - from: "v2.0.0-linux-rebased tag push to fork"
          to: "release-linux.yml workflow run"
          via: "push: tags: ['v*'] trigger in workflow YAML"
          pattern: "release-linux.yml"
        - from: "release-linux.yml run output"
          to: "GitHub Release Linux Beta v2.0.0-linux-rebased"
          via: "softprops/action-gh-release@v2 publishes AppImage + .deb"
          pattern: "v2.0.0-linux-rebased"
---

<objective>
Delete the RC tag (v2.0.0-linux-rebased-rc1, local + fork remote), then create + sign + push the canonical tag (v2.0.0-linux-rebased) on the post-30-03 master HEAD. Wait for `release-linux.yml` to republish canonical-named AppImage + .deb. Capture all SHAs, the workflow conclusion, and asset SHA256s in `30-TAG-EVIDENCE.md`.

Purpose: This is the milestone publish event. The canonical tag is what consumers (and the Phase 29 deferred SYNC-33-C / SYNC-34 evidence in 30-05) will reference.

Output: canonical tag pushed to both remotes; canonical Linux Beta release published; 30-TAG-EVIDENCE.md committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-land-tag/30-CONTEXT.md
@.planning/phases/30-land-tag/30-RESEARCH.md
@.planning/phases/30-land-tag/30-PRE-STATE.md
@.planning/phases/30-land-tag/30-MERGE-EVIDENCE.md
@.planning/phases/29-build-verification/29-DONE-GATE.md
@.planning/phases/29-build-verification/29-SMOKE-EVIDENCE.md
@.github/workflows/release-linux.yml
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 4-1: Delete RC tag (local + fork remote)</name>
  <files>.planning/phases/30-land-tag/30-TAG-EVIDENCE.md</files>
  <read_first>
    .planning/phases/30-land-tag/30-RESEARCH.md (Pattern 2 RC cleanup ordering, Pitfall 4 RC tag SHA confusion, Open Questions §5 local-then-remote, Runtime State §GitHub Releases auxiliary state)
    .planning/phases/30-land-tag/30-CONTEXT.md (D-30-02 RC cleanup ordering)
    .planning/phases/30-land-tag/30-PRE-STATE.md (RC tag SHA captured at setup)
  </read_first>
  <action>
    Initialize `.planning/phases/30-land-tag/30-TAG-EVIDENCE.md` with header (timestamp, post-FF master SHA from 30-MERGE-EVIDENCE.md, target tag name) and a `## RC tag cleanup` section. Then run cleanup in this order (local first per Open Questions §5):

    1. Capture RC state pre-deletion: `git ls-remote git@github.com:atabisz/Vortex.git refs/tags/v2.0.0-linux-rebased-rc1` — record output.
    2. `git tag -d v2.0.0-linux-rebased-rc1` (local). If "tag not found", that's fine — the tag was already only on remote.
    3. `git push git@github.com:atabisz/Vortex.git :refs/tags/v2.0.0-linux-rebased-rc1` (fork remote deletion via inline SSH URL).
    4. Verify deletion: `git ls-remote git@github.com:atabisz/Vortex.git refs/tags/v2.0.0-linux-rebased-rc1` — output should be empty.
    5. Sanity check: `gh release view v2.0.0-linux-rebased-rc1 --repo atabisz/Vortex --json name,tagName,assets 2>&1` — record whether the GitHub Release for the RC tag is still present (Assumption A2). Do NOT delete the release — Phase 29 SYNC-33 part B evidence URLs cite it.

    Append all five outputs to the `## RC tag cleanup` section. Casual voice.

    No commit yet — 30-TAG-EVIDENCE.md is built up across 4-1 → 4-2 → 4-3 → 4-4 and committed in 4-5.

  </action>
  <verify>
    <automated>RC=$(git ls-remote git@github.com:atabisz/Vortex.git refs/tags/v2.0.0-linux-rebased-rc1); test -z "$RC" && grep -c "^## RC tag cleanup" .planning/phases/30-land-tag/30-TAG-EVIDENCE.md | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-remote git@github.com:atabisz/Vortex.git refs/tags/v2.0.0-linux-rebased-rc1` returns empty output.
    - `git tag -l v2.0.0-linux-rebased-rc1` returns empty.
    - `30-TAG-EVIDENCE.md` exists with one `## RC tag cleanup` section recording the pre-deletion SHA, the local + remote deletion commands, and the post-deletion ls-remote empty result.
    - GitHub Release for RC tag was NOT deleted (Phase 29 evidence link preserved): `gh release view v2.0.0-linux-rebased-rc1 --repo atabisz/Vortex --json tagName -q .tagName 2>/dev/null` returns `v2.0.0-linux-rebased-rc1` OR exits non-zero (release auto-deleted by GitHub when tag deleted; A2 risk realized — note in evidence file).
  </acceptance_criteria>
  <done>RC tag deleted from local + fork remote; ls-remote confirms; release deletion status recorded.</done>
</task>

<task type="auto">
  <name>Task 4-2: Create canonical tag (annotated, SSH-signed)</name>
  <files>.planning/phases/30-land-tag/30-TAG-EVIDENCE.md</files>
  <read_first>
    .planning/phases/30-land-tag/30-RESEARCH.md (Pattern 2 — canonical tag annotation shape, casual voice; git config verified gpg.format=ssh + tag.gpgsign=true)
    .planning/phases/30-land-tag/30-CONTEXT.md (D-30-02 tag annotation shape)
    .planning/phases/29-build-verification/29-DONE-GATE.md (path to reference in tag annotation)
  </read_first>
  <action>
    Capture target SHA: `TAG_TARGET=$(git rev-parse v8.0/config-bucket); echo $TAG_TARGET`. Should equal current local master HEAD post-30-03 (the 30-03 docs commit landed on top of the FF point).

    Verify SSH signing config:
    ```
    git config --get user.signingkey   # expect /home/alex/.ssh/id_ed25519.pub
    git config --get gpg.format        # expect ssh
    git config --get tag.gpgsign       # expect true
    ```
    If any of these aren't set, STOP and surface — tag won't auto-sign.

    Create the tag with annotated body. Casual voice; reference Phase 29 + 30 done-gate paths. Single command (the body is multi-line — use heredoc or `-m` per line):

    ```
    git tag -a v2.0.0-linux-rebased $TAG_TARGET -m 'Vortex v2.0.0 Linux rebased — milestone v8.0 close.

Resolves upstream v2.0.0 sync (PR #4) onto Linux fork. 374 commits replayed
onto master HEAD via rebase, conflicts resolved fork-side by default;
downloader.test.ts taken upstream-side as Phase 25 SYNC-14 restoration pickup.

Phase 29 evidence: .planning/phases/29-build-verification/29-DONE-GATE.md
Phase 30 evidence: .planning/phases/30-land-tag/30-DONE-GATE.md'
```

    (The exact text — do NOT change words, line breaks, or punctuation. Casual voice locked per memory `feedback_casual_voice.md`.)

    Verify the tag exists locally + is signed:
    ```
    git tag -l v2.0.0-linux-rebased   # expect: v2.0.0-linux-rebased
    git tag -v v2.0.0-linux-rebased   # expect exit 0; output ends with "Good ssh signature for ..."
    git cat-file tag v2.0.0-linux-rebased   # show full tag object — annotation + signature visible
    ```

    Append `## Canonical tag creation` section to 30-TAG-EVIDENCE.md with: TAG_TARGET SHA, full `git cat-file tag` output, exit code from `git tag -v`, and signature key fingerprint.

    No push yet — Task 4-3 handles fork push, 4-4 handles origin push, 4-5 watches release-linux.yml.

  </action>
  <verify>
    <automated>git tag -l v2.0.0-linux-rebased | grep -q "^v2.0.0-linux-rebased$" && git tag -v v2.0.0-linux-rebased 2>&1 | grep -q "Good .* signature" && grep -c "^## Canonical tag creation" .planning/phases/30-land-tag/30-TAG-EVIDENCE.md | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `git tag -l v2.0.0-linux-rebased` returns the literal tag name.
    - `git tag -v v2.0.0-linux-rebased 2>&1` exit 0 and output contains `Good "git" signature` or `Good ssh signature`.
    - `git cat-file -t v2.0.0-linux-rebased` returns `tag` (annotated, not lightweight).
    - `git rev-parse v2.0.0-linux-rebased^{commit}` equals the TAG_TARGET SHA captured pre-creation.
    - 30-TAG-EVIDENCE.md has a `## Canonical tag creation` section with the cat-file output.
    - Tag annotation contains literal text "Vortex v2.0.0 Linux rebased — milestone v8.0 close" and references both `29-DONE-GATE.md` and `30-DONE-GATE.md`.
  </acceptance_criteria>
  <done>Canonical tag exists locally, annotated, SSH-signed; verified via tag -v.</done>
</task>

<task type="auto">
  <name>Task 4-3: Push canonical tag to fork (CI trigger)</name>
  <files>.planning/phases/30-land-tag/30-TAG-EVIDENCE.md</files>
  <read_first>
    .planning/phases/30-land-tag/30-RESEARCH.md (Pattern 2 push to fork first, Pitfall 5 wrong-remote ordering, Pitfall 6 v* pattern)
    .planning/phases/30-land-tag/30-CONTEXT.md (D-30-02 fork push triggers CI)
    .github/workflows/release-linux.yml (verify push:tags:['v*'] trigger still present)
  </read_first>
  <action>
    Push canonical tag to FORK FIRST (per Pitfall 5 — CI-triggering side, where we have write):

    ```
    git push git@github.com:atabisz/Vortex.git v2.0.0-linux-rebased
    ```

    Tag pushes are additive (no lease pin needed — they don't rewrite refs). If this push fails, STOP and surface — canonical tag must reach fork or release-linux.yml never runs.

    Verify post-push:
    ```
    git ls-remote git@github.com:atabisz/Vortex.git refs/tags/v2.0.0-linux-rebased   # expect: <SHA>\trefs/tags/v2.0.0-linux-rebased
    ```

    Capture the run that the push triggered. Wait briefly (~30s) for GitHub to register the trigger, then:
    ```
    RELEASE_RUN=$(gh run list --repo atabisz/Vortex --workflow=release-linux.yml --limit 5 --json databaseId,headBranch,event,status -q '.[] | select(.event == "push") | .databaseId' | head -1)
    echo "release-linux.yml run: $RELEASE_RUN"
    ```

    Append `## Canonical tag push to fork` section to 30-TAG-EVIDENCE.md: push command output, post-push ls-remote SHA, triggered RELEASE_RUN ID + URL.

  </action>
  <verify>
    <automated>SHA=$(git ls-remote git@github.com:atabisz/Vortex.git refs/tags/v2.0.0-linux-rebased | cut -f1); test -n "$SHA" && grep -c "^## Canonical tag push to fork" .planning/phases/30-land-tag/30-TAG-EVIDENCE.md | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-remote git@github.com:atabisz/Vortex.git refs/tags/v2.0.0-linux-rebased | cut -f1` returns a non-empty SHA matching local `git rev-parse v2.0.0-linux-rebased`.
    - 30-TAG-EVIDENCE.md has `## Canonical tag push to fork` section with the run ID.
    - `gh run list --repo atabisz/Vortex --workflow=release-linux.yml --limit 1 --json status -q '.[0].status'` returns one of `queued`/`in_progress`/`completed` (CI triggered).
  </acceptance_criteria>
  <done>Canonical tag on fork remote; release-linux.yml run kicked.</done>
</task>

<task type="auto">
  <name>Task 4-4: Push canonical tag to origin (Nexus-Mods/Vortex; informational, non-blocking)</name>
  <files>.planning/phases/30-land-tag/30-TAG-EVIDENCE.md</files>
  <read_first>
    .planning/phases/30-land-tag/30-RESEARCH.md (Pattern 2 dual-remote push, Assumptions Log A3 — origin push may fail without breaking milestone, Project Constraints upstream-PR policy)
  </read_first>
  <action>
    Push canonical tag to origin (Nexus-Mods/Vortex). This is informational only per memory `project_upstream_pr_policy.md` — Nexus-Mods doesn't consume our PRs but the tag keeps fork story legible if they ever look. Failure here is non-blocking.

    Try dry-run first to surface permission issues without blocking:
    ```
    git push --dry-run git@github.com:Nexus-Mods/Vortex.git v2.0.0-linux-rebased 2>&1
    ```

    If dry-run shows "Permission denied" or "remote rejected", document and skip:
    - Append `## Canonical tag push to origin` section to 30-TAG-EVIDENCE.md noting the rejection + reason. PASS-the-plan because origin push is non-blocking per Pitfall 5 + A3.

    If dry-run succeeds, run the real push:
    ```
    git push git@github.com:Nexus-Mods/Vortex.git v2.0.0-linux-rebased
    ```
    Verify: `git ls-remote git@github.com:Nexus-Mods/Vortex.git refs/tags/v2.0.0-linux-rebased | cut -f1` returns the canonical SHA.

    Append `## Canonical tag push to origin` section either way: push command, output (success or rejection), final state.

  </action>
  <verify>
    <automated>grep -c "^## Canonical tag push to origin" .planning/phases/30-land-tag/30-TAG-EVIDENCE.md | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - 30-TAG-EVIDENCE.md has `## Canonical tag push to origin` section with one of: success (origin tag SHA recorded) OR rejection (permission denied / remote rejected, documented + plan still passes).
    - If origin push succeeded: `git ls-remote git@github.com:Nexus-Mods/Vortex.git refs/tags/v2.0.0-linux-rebased` returns non-empty.
    - If origin push rejected: section contains the literal rejection text from git output.
  </acceptance_criteria>
  <done>Origin tag push attempted; success or documented rejection in 30-TAG-EVIDENCE.md (non-blocking).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4-5: Watch release-linux.yml + capture canonical asset SHA256s</name>
  <files>.planning/phases/30-land-tag/30-TAG-EVIDENCE.md</files>
  <read_first>
    .planning/phases/30-land-tag/30-RESEARCH.md (Pitfall 9 pnpm node-gyp chmod retry, R-30-04 retry policy, Code Example 3 asset capture, Don't Hand-Roll table — gh release download)
    .planning/phases/29-build-verification/29-SMOKE-EVIDENCE.md (RC release URL + asset SHA256s for comparison)
  </read_first>
  <what-built>
    release-linux.yml run triggered by canonical tag push. Plan watches to completion, downloads canonical AppImage + .deb, captures SHA256s.
  </what-built>
  <action>
    Watch the release-linux.yml run kicked in Task 4-3:

    ```
    gh run watch $RELEASE_RUN --repo atabisz/Vortex --exit-status
    ```

    Expect ~10–15 min wall-clock (Phase 29 RC was 10m58s). On flake (Pitfall 9 — pnpm node-gyp chmod), retry once: `gh run rerun $RELEASE_RUN --repo atabisz/Vortex` then watch again. Allow up to 2 retries before escalating per R-30-04.

    On success, download canonical assets and capture SHA256s:
    ```
    mkdir -p ~/Downloads/vortex-canonical
    cd ~/Downloads/vortex-canonical
    gh release download v2.0.0-linux-rebased --repo atabisz/Vortex --skip-existing
    ls -la
    sha256sum *.AppImage *.deb 2>/dev/null > canonical-sha256.txt
    cat canonical-sha256.txt
    ```

    Append `## release-linux.yml run + canonical assets` section to 30-TAG-EVIDENCE.md with:
    - run ID, URL, conclusion, wall-clock duration
    - any retry IDs + reasons
    - `gh release view v2.0.0-linux-rebased --repo atabisz/Vortex --json name,publishedAt,assets` JSON output (asset names + sizes)
    - SHA256s for each asset (full output of canonical-sha256.txt as fenced block)

    Compare against Phase 29 RC SHAs (29-SMOKE-EVIDENCE.md): if SHA256s differ from RC (expected — different commit history; canonical was built from rebased-then-FF'd master, RC was built from pre-rebase v8.0), note the difference. If SHA256s are identical to RC, that's surprising — investigate (might mean release-linux.yml didn't actually rebuild). Casual voice on the comparison note.

  </action>
  <how-to-verify>
    1. `gh run view $RELEASE_RUN --repo atabisz/Vortex --json conclusion -q .conclusion` returns `success`.
    2. `gh release view v2.0.0-linux-rebased --repo atabisz/Vortex --json assets -q '.assets | length'` returns `>= 2` (at least AppImage + .deb).
    3. `~/Downloads/vortex-canonical/canonical-sha256.txt` exists and contains 2+ lines (AppImage + .deb).
    4. 30-TAG-EVIDENCE.md `## release-linux.yml run + canonical assets` section captures conclusion + SHA256s + asset URLs.
    5. Visit `https://github.com/atabisz/Vortex/releases/tag/v2.0.0-linux-rebased` in browser — confirm release page loads with assets listed.
  </how-to-verify>
  <acceptance_criteria>
    - `gh run view $RELEASE_RUN --repo atabisz/Vortex --json conclusion -q .conclusion` returns `success`.
    - `gh release view v2.0.0-linux-rebased --repo atabisz/Vortex --json assets -q '[.assets[].name]'` includes both an AppImage and a .deb filename.
    - `test -f ~/Downloads/vortex-canonical/canonical-sha256.txt && [ $(wc -l < ~/Downloads/vortex-canonical/canonical-sha256.txt) -ge 2 ]` (file exists with 2+ SHA256 lines).
    - 30-TAG-EVIDENCE.md has `## release-linux.yml run + canonical assets` section containing both SHA256 hex strings.
  </acceptance_criteria>
  <resume-signal>Type "approved" once release-linux.yml concluded success and canonical AppImage + .deb SHA256s are in 30-TAG-EVIDENCE.md. Type "retry" if a flake recovered. Type "stop" if persistent failure — surface to user.</resume-signal>
</task>

<task type="auto">
  <name>Task 4-6: Commit + push tag evidence</name>
  <files>.planning/phases/30-land-tag/30-TAG-EVIDENCE.md</files>
  <read_first>
    .planning/phases/30-land-tag/30-TAG-EVIDENCE.md (the file we're committing)
  </read_first>
  <action>
    Roll up the four tag-related sections (RC cleanup, canonical creation, fork push, origin push, release-linux.yml + assets) into final `## SYNC-37 — canonical tag created + Linux Beta release published` section at top of 30-TAG-EVIDENCE.md (Phase 29 done-gate style: ## SYNC-N section per requirement, evidence quoted, Result: PASS).

    Then commit:
    1. `git add -f .planning/phases/30-land-tag/30-TAG-EVIDENCE.md`.
    2. `git commit -m 'docs(30-04): SYNC-37 — canonical tag v2.0.0-linux-rebased + AppImage/.deb published'`.
    3. `git log -1 --show-signature` — confirm SSH-signed.
    4. Push to fork master (current branch v8.0/config-bucket → master): `LIVE=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1); git push --force-with-lease=master:$LIVE git@github.com:atabisz/Vortex.git v8.0/config-bucket:master`.
    5. Verify post-push: `POST=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1); test "$POST" = "$(git rev-parse v8.0/config-bucket)"`.

  </action>
  <verify>
    <automated>git log --oneline v8.0/config-bucket -1 | grep -q "docs(30-04): SYNC-37" && git log -1 --show-signature 2>&1 | grep -q 'Good "git" signature' && grep -c "^## SYNC-37" .planning/phases/30-land-tag/30-TAG-EVIDENCE.md | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline v8.0/config-bucket -1` first line starts with `docs(30-04): SYNC-37`.
    - `git log -1 --show-signature 2>&1` contains `Good "git" signature`.
    - 30-TAG-EVIDENCE.md has `## SYNC-37` heading at top + 4–5 detail subsections beneath.
    - 30-TAG-EVIDENCE.md contains literal text `Result: PASS`.
  </acceptance_criteria>
  <done>30-TAG-EVIDENCE.md committed + pushed; canonical tag + release published; SSH-signed.</done>
</task>

</tasks>

<verification>
- RC tag deleted (local + fork remote)
- Canonical tag v2.0.0-linux-rebased exists locally, annotated, SSH-signed
- Canonical tag pushed to fork (CI triggered) AND origin (success or documented rejection)
- release-linux.yml run on canonical tag concluded success
- Canonical AppImage + .deb published; SHA256s captured
- 30-TAG-EVIDENCE.md committed + pushed
</verification>

<success_criteria>
SYNC-37 PASS: canonical tag exists on the post-FF master HEAD, both AppImage + .deb published under canonical names. Ready for 30-05 deferred-evidence capture.
</success_criteria>

<output>
30-TAG-EVIDENCE.md remains in place — 30-08 done-gate cites it. Canonical AppImage + .deb stay in `~/Downloads/vortex-canonical/` for 30-05 to consume. No SUMMARY.md needed.
</output>
