---
phase: 30-land-tag
plan: 02
subsystem: ci-verification
type: execute
wave: 3
depends_on: [30-01]
files_modified:
    - .planning/phases/30-land-tag/30-CI-EVIDENCE.md
autonomous: false
requirements: [SYNC-35]
tags: [linux-port, upstream-v2.0.0, phase-30, ci, sync-35]

requirements_in_play: [SYNC-35]

dependency_graph:
    requires:
        - "30-01 complete (rebased HEAD pushed to fork/sync/upstream-v2.0.0)"
        - "Main + Format CI runs queued/in-progress on the rebased SHA"
    provides:
        - ".planning/phases/30-land-tag/30-CI-EVIDENCE.md (Windows + Linux + Format conclusions on rebased HEAD)"
    affects:
        - "30-03 FF-merge plan (cannot merge until CI green per ROADMAP success criterion #1)"

gates:
    pre_conditions:
        - "30-01 commits live on fork/sync/upstream-v2.0.0"
        - "PR #4 head SHA matches post-rebase v8.0/config-bucket HEAD (push took)"
    post_conditions:
        - "Main workflow run on PR #4 head SHA: conclusion = success on BOTH windows-latest AND ubuntu-latest matrix legs"
        - "Format workflow run on PR #4 head SHA: conclusion = success"
        - "30-CI-EVIDENCE.md captures: run IDs, conclusions, wall-clock per leg, link to each run"

must_haves:
    truths:
        - "Windows CI (main.yml windows-latest leg) green on the rebased v8.0/config-bucket HEAD — this is SYNC-35 verbatim"
        - "Linux CI leg also green (sanity — Phase 29 already verified Linux build, but the rebase pulled in master's +20 commits including Phase 25 SYNC-14 restore which is fresh content)"
        - "Format CI green (rebase didn't introduce oxfmt drift outside change scope per Pitfall 8)"
        - "If a flake occurs, retry up to 2x (R-30-04 contingency); if persistent failure, STOP and surface — do NOT proceed to FF-merge with red CI"
    artifacts:
        - path: ".planning/phases/30-land-tag/30-CI-EVIDENCE.md"
          provides: "SYNC-35 evidence — Windows + Linux + Format CI green on rebased HEAD"
          contains: "## SYNC-35"
    key_links:
        - from: "fork/sync/upstream-v2.0.0 rebased HEAD SHA"
          to: "gh run list output filtered by --branch=sync/upstream-v2.0.0"
          via: "`gh run list --workflow=main.yml --branch=sync/upstream-v2.0.0 --limit 5 --json headSha,conclusion,status`"
          pattern: "conclusion=success"
---

<objective>
Wait for the Main + Format CI runs triggered by 30-01's rebased force-push to complete. Verify all matrix legs (windows-latest, ubuntu-latest, format) return `success`. Capture conclusions, run IDs, and wall-clock to `30-CI-EVIDENCE.md` as SYNC-35 evidence.

Purpose: SYNC-35 is the explicit ROADMAP success criterion #1 ("Windows CI green on the resolved merge commit"). Without this gate, FF-merge in 30-03 lands red CI on master.

Output: `30-CI-EVIDENCE.md` with three success conclusions; commit landed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-land-tag/30-CONTEXT.md
@.planning/phases/30-land-tag/30-RESEARCH.md
@.planning/phases/30-land-tag/30-PRE-STATE.md
@.planning/phases/30-land-tag/30-REBASE-NOTES.md
@.github/workflows/main.yml
@.github/workflows/format.yml
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2-1: Watch Main + Format CI on rebased HEAD</name>
  <files>.planning/phases/30-land-tag/30-CI-EVIDENCE.md</files>
  <read_first>
    .planning/phases/30-land-tag/30-RESEARCH.md (Pitfall 9 pnpm node-gyp chmod, R-30-02 + R-30-04 retry policy, Phase Requirements Test Map)
    .planning/phases/30-land-tag/30-CONTEXT.md (R-30-02, success criteria #1)
    .github/workflows/main.yml (verify the matrix shape — windows-latest + ubuntu-latest legs)
    .github/workflows/format.yml (verify the format job)
  </read_first>
  <what-built>
    Two CI workflow runs triggered by 30-01 force-push. Plan watches them to completion and records conclusions.
  </what-built>
  <action>
    Identify the in-flight runs (Main + Format) on the rebased HEAD:

    1. `REBASED_HEAD=$(git rev-parse v8.0/config-bucket)`.
    2. `MAIN_RUN=$(gh run list --repo atabisz/Vortex --workflow=main.yml --branch=sync/upstream-v2.0.0 --limit 5 --json databaseId,headSha,status,conclusion -q ".[] | select(.headSha == \"$REBASED_HEAD\") | .databaseId" | head -1)`.
    3. `FORMAT_RUN=$(gh run list --repo atabisz/Vortex --workflow=format.yml --branch=sync/upstream-v2.0.0 --limit 5 --json databaseId,headSha,status,conclusion -q ".[] | select(.headSha == \"$REBASED_HEAD\") | .databaseId" | head -1)`.

    If either is empty, the rebased push didn't trigger that workflow. Re-check the workflow's `on:` trigger and surface — STOP, do not invent a run ID.

    Watch each to completion: `gh run watch $MAIN_RUN --repo atabisz/Vortex --exit-status` and `gh run watch $FORMAT_RUN --repo atabisz/Vortex --exit-status`. The `--exit-status` flag returns non-zero if the run concludes failure. Expect ~10-15 min wall-clock based on Phase 29 RC build (10m58s).

    On flake (R-30-04): if a run concludes `failure` AND inspection shows transient error (Pitfall 9 pnpm node-gyp chmod, runner network blip), retry once: `gh run rerun $RUN_ID --repo atabisz/Vortex` then `gh run watch $RUN_ID --repo atabisz/Vortex --exit-status` again. Allow up to 2 retries before escalating.

    On real failure (test/build/lint regression): STOP. Surface the failing step + log tail. Do NOT proceed to commit CI-EVIDENCE.md as PASS. Triage: regression introduced by a v8.0 commit (cycle back to fix in v8.0 + re-rebase) or by a master commit landed via the +20 (push fix to master). Document deviation.

    On all-green, write `.planning/phases/30-land-tag/30-CI-EVIDENCE.md` with:

    - `# Phase 30 CI Evidence (SYNC-35)`
    - `## SYNC-35 — Windows CI green on rebased HEAD`
    - Rebased HEAD SHA
    - Main workflow run ID + URL (https://github.com/atabisz/Vortex/actions/runs/$MAIN_RUN) + conclusion `success` + per-leg breakdown via `gh run view $MAIN_RUN --repo atabisz/Vortex --json jobs -q '.jobs[] | "\(.name): \(.conclusion) (\(.startedAt) -> \(.completedAt))"'`
    - Format workflow run ID + URL + conclusion + duration
    - Retries (if any): original run ID, retry run ID(s), reason
    - `Result: PASS` line with each leg's conclusion quoted

    Casual voice. Phase 29 done-gate section style.

  </action>
  <how-to-verify>
    1. `gh run view $MAIN_RUN --repo atabisz/Vortex --json conclusion -q .conclusion` returns `success`.
    2. `gh run view $FORMAT_RUN --repo atabisz/Vortex --json conclusion -q .conclusion` returns `success`.
    3. Per-leg: `gh run view $MAIN_RUN --repo atabisz/Vortex --json jobs -q '.jobs[] | select(.name | contains("windows")) | .conclusion'` returns `success`. Same for `ubuntu`.
    4. `.planning/phases/30-land-tag/30-CI-EVIDENCE.md` has `## SYNC-35` section with each leg's conclusion quoted.
    5. PR #4 status: `gh pr checks 4 --repo atabisz/Vortex` shows all checks green.
  </how-to-verify>
  <acceptance_criteria>
    - `gh run view $MAIN_RUN --repo atabisz/Vortex --json conclusion -q .conclusion` returns `success`.
    - `gh run view $FORMAT_RUN --repo atabisz/Vortex --json conclusion -q .conclusion` returns `success`.
    - `gh run view $MAIN_RUN --repo atabisz/Vortex --json jobs -q '[.jobs[] | select(.name | contains("windows")) | .conclusion] | unique'` returns `["success"]` (windows leg green).
    - `gh run view $MAIN_RUN --repo atabisz/Vortex --json jobs -q '[.jobs[] | select(.name | contains("ubuntu")) | .conclusion] | unique'` returns `["success"]` (linux leg green).
    - `.planning/phases/30-land-tag/30-CI-EVIDENCE.md` exists, contains `## SYNC-35` heading, contains literal text `Result: PASS`, and contains both run IDs.
    - `gh pr checks 4 --repo atabisz/Vortex` shows zero failing checks.
  </acceptance_criteria>
  <resume-signal>Type "approved" once Main + Format both green and 30-CI-EVIDENCE.md captures both conclusions. Type "retry" if a flake happened and the retry is now green. Type "stop" + describe the failure if persistent red.</resume-signal>
</task>

<task type="auto">
  <name>Task 2-2: Commit + push CI evidence</name>
  <files>.planning/phases/30-land-tag/30-CI-EVIDENCE.md</files>
  <read_first>
    .planning/phases/30-land-tag/30-CI-EVIDENCE.md (the file we're committing — re-verify before commit)
  </read_first>
  <action>
    Commit + push:

    1. `git status --short` — must be empty except for the new 30-CI-EVIDENCE.md.
    2. `git add -f .planning/phases/30-land-tag/30-CI-EVIDENCE.md`.
    3. `git commit -m 'docs(30-02): SYNC-35 — windows + linux CI green on rebased head'` (casual voice).
    4. `git log -1 --show-signature` — confirm SSH-signed.
    5. `LIVE=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/sync/upstream-v2.0.0 | cut -f1)` — verify live SHA.
    6. `git push --force-with-lease=sync/upstream-v2.0.0:$LIVE git@github.com:atabisz/Vortex.git v8.0/config-bucket:sync/upstream-v2.0.0`.
    7. Verify post-push: `POST=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/sync/upstream-v2.0.0 | cut -f1); test "$POST" = "$(git rev-parse v8.0/config-bucket)"`.

    Note: this push retriggers Main + Format CI on the new HEAD (the docs commit lands on top of the rebased HEAD). That's expected — the new runs aren't gating; they're informational. 30-03's FF-merge consumes the EARLIER (rebased-HEAD) runs as SYNC-35 evidence. Document in 30-CI-EVIDENCE.md if convenient.

  </action>
  <verify>
    <automated>git log --oneline v8.0/config-bucket -1 | grep -q "docs(30-02): SYNC-35" && git log -1 --show-signature 2>&1 | grep -q 'Good "git" signature' && POST=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/sync/upstream-v2.0.0 | cut -f1) && test "$POST" = "$(git rev-parse v8.0/config-bucket)"</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline v8.0/config-bucket -1` first line starts with `docs(30-02): SYNC-35`.
    - `git log -1 --show-signature 2>&1` contains `Good "git" signature`.
    - `git ls-remote git@github.com:atabisz/Vortex.git refs/heads/sync/upstream-v2.0.0 | cut -f1` equals `git rev-parse v8.0/config-bucket`.
  </acceptance_criteria>
  <done>30-CI-EVIDENCE.md committed + pushed; SSH-signed; remote sync/upstream-v2.0.0 advanced.</done>
</task>

</tasks>

<verification>
- 30-CI-EVIDENCE.md captures windows + linux + format CI green on rebased HEAD
- Both Main + Format runs concluded success
- Commit landed + SSH-signed + pushed
- SYNC-35 evidence ready for done-gate roll-up
</verification>

<success_criteria>
SYNC-35 PASS: Windows + Linux CI green on the rebased HEAD that PR #4 will FF-merge from. Cleared to proceed with FF-merge in 30-03.
</success_criteria>

<output>
30-CI-EVIDENCE.md remains in place — 30-08 done-gate cites it. No SUMMARY.md needed.
</output>
