---
phase: 30-land-tag
plan: 08
subsystem: done-gate
type: execute
wave: 9
depends_on: [30-07]
files_modified:
    - .planning/phases/30-land-tag/30-DONE-GATE.md
    - .planning/phases/30-land-tag/30-08-SUMMARY.md
    - .planning/STATE.md
    - .planning/ROADMAP.md
autonomous: false
requirements: [SYNC-35, SYNC-36, SYNC-37, SYNC-38, SYNC-39, SYNC-33, SYNC-34]
tags:
    [
        linux-port,
        upstream-v2.0.0,
        phase-30,
        done-gate,
        sync-35,
        sync-36,
        sync-37,
        sync-38,
        sync-39,
        sync-33,
        sync-34,
        carry-forward,
    ]

requirements_in_play: [SYNC-35, SYNC-36, SYNC-37, SYNC-38, SYNC-39, SYNC-33, SYNC-34]

dependency_graph:
    requires:
        - "30-00..30-07 complete with PASS verdicts captured in 30-PRE-STATE.md, 30-REBASE-NOTES.md, 30-CI-EVIDENCE.md, 30-MERGE-EVIDENCE.md, 30-TAG-EVIDENCE.md, 30-CANONICAL-SMOKE-EVIDENCE.md, 30-CHERRY-PICK-NOTES.md, VORTEX-LINUX-MERGE-PLAYBOOK.md"
    provides:
        - ".planning/phases/30-land-tag/30-DONE-GATE.md (rolls up SYNC-35/36/37/38/39 + carry-forward SYNC-33-C/34)"
        - "STATE.md updated: Phase 30 complete; v8.0 milestone closed"
        - "ROADMAP.md updated: Phase 30 row checkbox flipped, all 5 success criteria checked, v8.0 milestone done"
        - ".planning/phases/30-land-tag/30-08-SUMMARY.md matching 29-10-SUMMARY.md shape"
    affects:
        - "v8.0 milestone status (this is the milestone-close step)"
        - "Whatever phase comes next (likely v8.1 backlog if any, or roadmap rollover)"

gates:
    pre_conditions:
        - "30-00..30-07 commits all landed + SSH-signed"
        - "All evidence files exist with PASS verdicts: 30-CI-EVIDENCE.md, 30-MERGE-EVIDENCE.md, 30-TAG-EVIDENCE.md, 30-CANONICAL-SMOKE-EVIDENCE.md, 30-CHERRY-PICK-NOTES.md (all from prior plans)"
        - "Playbook commit `docs(playbook): v8.0 milestone post-mortem` is current HEAD"
        - "Working tree clean: git status --short empty"
    post_conditions:
        - "30-DONE-GATE.md exists with one section per SYNC requirement; evidence quoted; all 7 PASS (5 native + 2 carry-forward)"
        - "STATE.md frontmatter updated: completed_phases=7, completed_plans+=9 (Phase 30 produced 9 plans 30-00..30-08), stopped_at='Phase 30 complete; v8.0 milestone done'"
        - "ROADMAP.md Phase 30 entry: checkbox `[x]`, all 5 success criteria checked"
        - "Two commits land: `docs(30-08): phase 30 done-gate evidence` + `docs(30): mark phase 30 complete in roadmap + state` (Phase 28/29 two-commit landing precedent)"
        - "Both commits SSH-signed; pushed to fork master"
        - "30-08-SUMMARY.md written matching 29-10-SUMMARY.md shape"

must_haves:
    truths:
        - "30-DONE-GATE.md has one ## section per SYNC requirement (7 total: SYNC-35, SYNC-36, SYNC-37, SYNC-38, SYNC-39 native + SYNC-33-C, SYNC-34 carry-forward). Each section quotes evidence verbatim from the upstream evidence file, marks PASS, cites originating plan."
        - "Done-gate template matches Phase 29 `29-DONE-GATE.md` shape — section per check, evidence quoted, requirements-satisfied table at bottom, timestamped status line"
        - "Carry-forward sections (SYNC-33-C, SYNC-34) explicitly labeled as carry-forward from Phase 29 with cross-reference to 29-DONE-GATE.md"
        - "STATE.md frontmatter updated: completed_phases 6→7, completed_plans 59→68 (Phase 30 produced 9 plans 30-00..30-08), stopped_at='Phase 30 complete; v8.0 milestone done', last_updated bumped, last_activity bumped"
        - "ROADMAP.md Phase 30 entry: checkbox `[ ]` → `[x]`, append `(complete YYYY-MM-DD)` after the title, all 5 ROADMAP success criteria flipped to checked. Plans line: 9 plans listed with objectives."
        - "Two commits in order: `docs(30-08): phase 30 done-gate evidence` (paired with 30-DONE-GATE.md + 30-08-SUMMARY.md) then `docs(30): mark phase 30 complete in roadmap + state` (paired with STATE.md + ROADMAP.md). Same two-commit landing pattern as Phase 28 + 29."
        - "Both commits SSH-signed; both pushed to fork master; pure additions on top of playbook commit (no force-rewrite)"
        - "Phase 30 final summary 30-08-SUMMARY.md matching 29-10-SUMMARY.md shape"
    artifacts:
        - path: ".planning/phases/30-land-tag/30-DONE-GATE.md"
          provides: "Done-gate evidence record — captures PASS evidence for all 7 requirements (5 native + 2 carry-forward) for milestone audit trail"
          contains: "## Phase 30 Done Gate"
        - path: ".planning/phases/30-land-tag/30-08-SUMMARY.md"
          provides: "Phase summary — what worked, what was inefficient, patterns established"
          contains: "## Phase 30 summary"
        - path: ".planning/STATE.md"
          provides: "Phase 30 marked complete in frontmatter"
          contains: "stopped_at: Phase 30 complete"
        - path: ".planning/ROADMAP.md"
          provides: "Phase 30 row checkbox flipped to [x]"
          contains: "[x] **Phase 30"
    key_links:
        - from: "30-DONE-GATE.md"
          to: "ROADMAP.md Phase 30 row"
          via: "Evidence that all 5 ROADMAP success criteria + 5 SYNC native + 2 SYNC carry-forward satisfied"
          pattern: "Phase 30"
        - from: "v8.0/config-bucket post-30 HEAD"
          to: "v8.0 milestone close"
          via: "Phase 30 is the milestone-close step; subsequent work moves to v8.1+ backlog"
          pattern: "v2.0.0-linux-rebased"
---

<objective>
Roll up all Phase 30 evidence into `30-DONE-GATE.md`. Update STATE.md + ROADMAP.md to mark Phase 30 (and v8.0 milestone) complete. Two-commit landing (Phase 28/29 precedent): docs(30-08) for the done-gate + summary, then docs(30) for the STATE + ROADMAP flips.

Purpose: Single point where Phase 30 declares done. Catches any drift between 30-07 and now (uncommitted edits, missing evidence sections). Writes the audit-trail record that future-you reads to confirm the milestone actually closed.

This is a checkpoint:human-verify plan because the final phase-complete declaration + ROADMAP/STATE writes are best gated on a human eyeball over the rolled-up evidence.

Output: 30-DONE-GATE.md + 30-08-SUMMARY.md + STATE.md + ROADMAP.md updates; two signed commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-land-tag/30-CONTEXT.md
@.planning/phases/30-land-tag/30-PRE-STATE.md
@.planning/phases/30-land-tag/30-REBASE-NOTES.md
@.planning/phases/30-land-tag/30-CI-EVIDENCE.md
@.planning/phases/30-land-tag/30-MERGE-EVIDENCE.md
@.planning/phases/30-land-tag/30-TAG-EVIDENCE.md
@.planning/phases/30-land-tag/30-CANONICAL-SMOKE-EVIDENCE.md
@.planning/phases/30-land-tag/30-CHERRY-PICK-NOTES.md
@.planning/phases/29-build-verification/29-DONE-GATE.md
@.planning/phases/29-build-verification/29-10-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 8-1: Confirm starting state + sanity-check evidence files</name>
  <files></files>
  <read_first>
    .planning/phases/30-land-tag/30-CI-EVIDENCE.md (must have ## SYNC-35 + Result: PASS)
    .planning/phases/30-land-tag/30-MERGE-EVIDENCE.md (must have ## SYNC-36 + Result: PASS)
    .planning/phases/30-land-tag/30-TAG-EVIDENCE.md (must have ## SYNC-37 + Result: PASS)
    .planning/phases/30-land-tag/30-CANONICAL-SMOKE-EVIDENCE.md (must have ## SYNC-33 + ## SYNC-34 + Result: PASS for both)
    .planning/phases/30-land-tag/30-CHERRY-PICK-NOTES.md (must have ## SYNC-38 + Result: PASS)
    VORTEX-LINUX-MERGE-PLAYBOOK.md (must have v2.0.0-linux-rebased + 5 deltas)
  </read_first>
  <action>
    Verify clean working tree + current branch:
    ```
    git rev-parse --abbrev-ref HEAD       # expect: v8.0/config-bucket
    git status --short                    # expect: empty
    ```

    Verify upstream evidence files have PASS sections populated:
    ```
    grep -E '## SYNC-35' .planning/phases/30-land-tag/30-CI-EVIDENCE.md
    grep -E '## SYNC-36' .planning/phases/30-land-tag/30-MERGE-EVIDENCE.md
    grep -E '## SYNC-37' .planning/phases/30-land-tag/30-TAG-EVIDENCE.md
    grep -E '## SYNC-33' .planning/phases/30-land-tag/30-CANONICAL-SMOKE-EVIDENCE.md
    grep -E '## SYNC-34' .planning/phases/30-land-tag/30-CANONICAL-SMOKE-EVIDENCE.md
    grep -E '## SYNC-38' .planning/phases/30-land-tag/30-CHERRY-PICK-NOTES.md
    grep -c "Result: PASS" .planning/phases/30-land-tag/30-CI-EVIDENCE.md .planning/phases/30-land-tag/30-MERGE-EVIDENCE.md .planning/phases/30-land-tag/30-TAG-EVIDENCE.md .planning/phases/30-land-tag/30-CANONICAL-SMOKE-EVIDENCE.md .planning/phases/30-land-tag/30-CHERRY-PICK-NOTES.md   # expect total >= 6 (5 evidence files; canonical-smoke has 2 sections)
    ```

    Verify Phase 30 commit shape:
    ```
    git log --oneline v8.0/config-bucket --not f746a0767 | head -20   # f746a0767 = Phase 29 close HEAD per 29-DONE-GATE.md; expect commits 30-00..30-07 in order
    ```

    Expected commit pattern (in order):
    - `docs(30-00): phase 30 setup — pre-rebase state captured`
    - `docs(30-01): rebase v8.0 onto master — conflict log`
    - `docs(30-02): SYNC-35 — windows + linux CI green on rebased head`
    - `docs(30-03): SYNC-36 — PR #4 fast-forward merged onto master`
    - `docs(30-04): SYNC-37 — canonical tag v2.0.0-linux-rebased + AppImage/.deb published`
    - `docs(30-05): SYNC-33-C + SYNC-34 carry-forward — canonical smoke evidence`
    - `docs(30-06): SYNC-38 — linux-port cherry-pick complete`
    - `docs(playbook): v8.0 milestone post-mortem`

    If any expected section is missing, marked FAIL, or any commit out of order, STOP and surface — done-gate cannot close until upstream is solid.

  </action>
  <verify>
    <automated>git status --short | wc -l | grep -q "^0$" && grep -q "## SYNC-35" .planning/phases/30-land-tag/30-CI-EVIDENCE.md && grep -q "## SYNC-36" .planning/phases/30-land-tag/30-MERGE-EVIDENCE.md && grep -q "## SYNC-37" .planning/phases/30-land-tag/30-TAG-EVIDENCE.md && grep -q "## SYNC-38" .planning/phases/30-land-tag/30-CHERRY-PICK-NOTES.md && grep -q "## SYNC-33" .planning/phases/30-land-tag/30-CANONICAL-SMOKE-EVIDENCE.md && grep -q "## SYNC-34" .planning/phases/30-land-tag/30-CANONICAL-SMOKE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `git status --short` returns empty.
    - `git rev-parse --abbrev-ref HEAD` returns `v8.0/config-bucket`.
    - All 6 evidence sections grep-confirmed (## SYNC-33, 34, 35, 36, 37, 38 across the 5 evidence files).
    - `git log --oneline v8.0/config-bucket --not f746a0767 | wc -l` returns 8 (or 7 if Phase 29's done-gate paired commits are already pruned from the comparison; the count is precisely the Phase 30 commits — adjust the `--not` reference if needed).
    - Total `Result: PASS` occurrences across the 5 evidence files >= 6.
  </acceptance_criteria>
  <done>Pre-conditions confirmed; ready to write done-gate.</done>
</task>

<task type="auto">
  <name>Task 8-2: Write 30-DONE-GATE.md (roll up all 7 SYNC sections)</name>
  <files>.planning/phases/30-land-tag/30-DONE-GATE.md</files>
  <read_first>
    .planning/phases/29-build-verification/29-DONE-GATE.md (template shape — section per check, evidence quoted, requirements table)
    .planning/phases/30-land-tag/30-CI-EVIDENCE.md (SYNC-35 source)
    .planning/phases/30-land-tag/30-MERGE-EVIDENCE.md (SYNC-36 source)
    .planning/phases/30-land-tag/30-TAG-EVIDENCE.md (SYNC-37 source)
    .planning/phases/30-land-tag/30-CANONICAL-SMOKE-EVIDENCE.md (SYNC-33-C + SYNC-34 carry-forward source)
    .planning/phases/30-land-tag/30-CHERRY-PICK-NOTES.md (SYNC-38 source)
    VORTEX-LINUX-MERGE-PLAYBOOK.md (SYNC-39 source — verify the 5 deltas + commit-index row landed)
  </read_first>
  <action>
    Write `.planning/phases/30-land-tag/30-DONE-GATE.md` matching `29-DONE-GATE.md` shape verbatim. Sections:

    1. `# Phase 30 Done Gate` (header: branch HEAD SHA, ISO-8601 timestamp, environment line — distro, kernel, Node, pnpm, Electron versions like 29-DONE-GATE.md).
    2. `## Phase 30 Done Gate` (subheader for the body, matching 29 pattern).
    3. `### SYNC-35 — Windows CI green on rebased head` (originating plan: 30-02). Quote evidence verbatim from 30-CI-EVIDENCE.md: run IDs, conclusions per leg (windows-latest + ubuntu-latest), Result: PASS.
    4. `### SYNC-36 — PR #4 fast-forward merged onto master` (originating plan: 30-03). Quote post-FF master SHA, PR head SHA equality, FF parents-count = 1, path taken (primary or fallback), Result: PASS.
    5. `### SYNC-37 — canonical tag v2.0.0-linux-rebased + AppImage/.deb published` (originating plan: 30-04). Quote: RC tag deletion confirmed; canonical tag SHA + signature verified; release-linux.yml run conclusion; canonical asset SHA256s; Result: PASS.
    6. `### SYNC-38 — linux-port cherry-pick complete` (originating plan: 30-06). Quote: linux-port baseline SHA, post-cherry-pick SHA, applied count, dropped count, total count, grep-checkpoint exit 0, Result: PASS.
    7. `### SYNC-39 — VORTEX-LINUX-MERGE-PLAYBOOK.md updated` (originating plan: 30-07). Quote: 5 delta titles landed, commit-index table refreshed, single signed commit, Result: PASS.
    8. `### SYNC-33 part C (carry-forward from Phase 29) — canonical AppImage + .deb local boot` (originating plan: 30-05; carry-forward marker explicit). Cross-reference 29-DONE-GATE.md SYNC-33 section that deferred this. Quote: AppImage chmod+x boot screenshot path, .deb sudo apt + desktop-entry launch screenshot path, libfuse2 install (if applicable), Result: PASS.
    9. `### SYNC-34 (carry-forward from Phase 29) — Skyrim SE 4-screenshot walkthrough` (originating plan: 30-05; carry-forward marker explicit). Cross-reference 29-DONE-GATE.md SYNC-34 section that deferred this. Quote: 4 checkpoint screenshot paths, mod name used, Result: PASS.
    10. `## Requirements satisfied` — table:

    ```
    | Req ID | Source | Plan | Result |
    |--------|--------|------|--------|
    | SYNC-35 | 30-CI-EVIDENCE.md | 30-02 | PASS |
    | SYNC-36 | 30-MERGE-EVIDENCE.md | 30-03 | PASS |
    | SYNC-37 | 30-TAG-EVIDENCE.md | 30-04 | PASS |
    | SYNC-38 | 30-CHERRY-PICK-NOTES.md | 30-06 | PASS |
    | SYNC-39 | VORTEX-LINUX-MERGE-PLAYBOOK.md | 30-07 | PASS |
    | SYNC-33-C (carry-fwd) | 30-CANONICAL-SMOKE-EVIDENCE.md | 30-05 | PASS |
    | SYNC-34 (carry-fwd) | 30-CANONICAL-SMOKE-EVIDENCE.md | 30-05 | PASS |
    ```

    11. `## Phase 30 status: COMPLETE` (timestamped per system clock, ISO-8601). Note: also marks v8.0 milestone closure.

    Casual voice. Quote evidence verbatim — don't paraphrase.

  </action>
  <verify>
    <automated>test -f .planning/phases/30-land-tag/30-DONE-GATE.md && grep -c "^### SYNC-" .planning/phases/30-land-tag/30-DONE-GATE.md | awk '$1 >= 7 {exit 0} {exit 1}' && grep -q "Phase 30 status: COMPLETE" .planning/phases/30-land-tag/30-DONE-GATE.md && grep -c "PASS" .planning/phases/30-land-tag/30-DONE-GATE.md | awk '$1 >= 7 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/30-land-tag/30-DONE-GATE.md` exists.
    - File has at least 7 `### SYNC-` subsections (SYNC-33-C, SYNC-34, SYNC-35, SYNC-36, SYNC-37, SYNC-38, SYNC-39).
    - File contains `## Requirements satisfied` table with 7 rows all marked PASS.
    - File contains literal text `## Phase 30 status: COMPLETE`.
    - At least 7 `PASS` occurrences in the file (one per SYNC requirement).
    - SYNC-33 part C and SYNC-34 sections explicitly include the literal text "carry-forward" or "carry forward".
  </acceptance_criteria>
  <done>30-DONE-GATE.md written with 7 SYNC sections + requirements table + COMPLETE status line.</done>
</task>

<task type="auto">
  <name>Task 8-3: Write 30-08-SUMMARY.md matching 29-10-SUMMARY.md shape</name>
  <files>.planning/phases/30-land-tag/30-08-SUMMARY.md</files>
  <read_first>
    .planning/phases/29-build-verification/29-10-SUMMARY.md (template shape — section per SYNC requirement with evidence pointers + What worked / What was inefficient / Patterns trailing blocks)
    .planning/phases/30-land-tag/30-DONE-GATE.md (just-written — pull section pointers)
  </read_first>
  <action>
    Write `.planning/phases/30-land-tag/30-08-SUMMARY.md` matching `29-10-SUMMARY.md` shape exactly. Sections:

    1. `# Phase 30 summary` (header: phase title, completion date, status COMPLETE).
    2. `## Phase 30 summary` (body subheader).
    3. One subsection per SYNC requirement with pointer to evidence file (not full quote — just the file + section): `### SYNC-35 — see 30-CI-EVIDENCE.md ## SYNC-35`.
    4. `## What worked` — 3-5 bullets. Examples to consider:
        - "Pre-rebase 30-PRE-STATE.md captured live SHAs — saved at least one drift surprise during rebase"
        - "Rebase --ours/--theirs inversion documented in RESEARCH.md — Code Example 2 prevented picking the wrong side on downloader.test.ts"
        - "Two-commit landing pattern (28+29 precedent) made done-gate close clean"
        - "Path-filtered cherry-pick enumeration was faithful to D-30-03 — N applied, M dropped with rationale"
        - "Single playbook commit per D-30-04 kept post-mortem coherent"
    5. `## What was inefficient` — 2-4 bullets. Examples to consider:
        - "Live state delta from CONTEXT to RESEARCH (4d35b1bec vs 355158381) cost a re-verify pass at every push step"
        - "gh pr merge --merge=fast-forward flag behavior verification ate 30-00 cycles"
        - "Cherry-pick of 216 commits is N hours of human-loop work — opportunity for v8.1 to script the path-filter + run unattended with checkpoints"
    6. `## Patterns established / extended` — 3-5 bullets. Examples:
        - "Inline-SSH-URL force-with-lease idiom now playbook-canonical (Delta 4)"
        - "DEFERRED-not-skipped pattern across phase boundaries (Phase 29 SYNC-33-C + SYNC-34 → Phase 30 close)"
        - "Two-commit landing for done-gate + ROADMAP/STATE flip"
        - "Annotated SSH-signed milestone tag with .planning evidence pointers in annotation"
    7. `## Cost / time delta` — bulleted: per-plan rough wall-clock + context cost (consult Phase 29 summary for shape).
    8. `## Followups / deferred to v8.1+`:
        - AppImage update channel for Linux (Nexus-Mods upstream `latest-linux.yml` 404 surface)
        - GitHub-Actions step bumps (Node-20 deprecated)
        - `@vortex/api` regen as scheduled chore
        - ROADMAP Phase 28 progress-table row drift at line 291

    Casual voice.

  </action>
  <verify>
    <automated>test -f .planning/phases/30-land-tag/30-08-SUMMARY.md && grep -c "^## " .planning/phases/30-land-tag/30-08-SUMMARY.md | awk '$1 >= 5 {exit 0} {exit 1}' && grep -q "What worked" .planning/phases/30-land-tag/30-08-SUMMARY.md && grep -q "What was inefficient" .planning/phases/30-land-tag/30-08-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/30-land-tag/30-08-SUMMARY.md` exists.
    - File has at least 5 `## ` H2 sections (Phase 30 summary, What worked, What was inefficient, Patterns established, Followups).
    - File contains literal text `What worked` and `What was inefficient` and `Patterns established` (or `Patterns established / extended`).
    - File has SYNC-* pointer entries for at least 5 SYNC requirements.
  </acceptance_criteria>
  <done>30-08-SUMMARY.md written matching 29-10-SUMMARY.md shape.</done>
</task>

<task type="auto">
  <name>Task 8-4: Stage + commit done-gate + summary (Commit A of two-commit landing)</name>
  <files>
    .planning/phases/30-land-tag/30-DONE-GATE.md
    .planning/phases/30-land-tag/30-08-SUMMARY.md
  </files>
  <read_first>
    .planning/phases/29-build-verification/29-10-PLAN.md (Commit A pattern from Phase 29 done-gate — verbatim shape)
  </read_first>
  <action>
    First commit of two-commit landing pattern:

    1. `git add -f .planning/phases/30-land-tag/30-DONE-GATE.md .planning/phases/30-land-tag/30-08-SUMMARY.md`.
    2. `git commit -m 'docs(30-08): phase 30 done-gate evidence' -m 'Roll up all 5 native SYNC requirement PASS verdicts (35/36/37/38/39) plus 2 carry-forward (33-C/34) into 30-DONE-GATE.md per Phase 29 done-gate template (extended to 7 items including the carry-forwards from Phase 29).' -m 'Evidence quoted from 30-CI-EVIDENCE.md, 30-MERGE-EVIDENCE.md, 30-TAG-EVIDENCE.md, 30-CANONICAL-SMOKE-EVIDENCE.md, 30-CHERRY-PICK-NOTES.md, VORTEX-LINUX-MERGE-PLAYBOOK.md.'`.
    3. `git log -1 --show-signature` — confirm SSH-signed.
    4. Push: `LIVE=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1); git push --force-with-lease=master:$LIVE git@github.com:atabisz/Vortex.git v8.0/config-bucket:master`.
    5. Verify post-push: `POST=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1); test "$POST" = "$(git rev-parse v8.0/config-bucket)"`.

  </action>
  <verify>
    <automated>git log --oneline v8.0/config-bucket -1 | grep -q "docs(30-08): phase 30 done-gate" && git log -1 --show-signature 2>&1 | grep -q 'Good "git" signature' && POST=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1) && test "$POST" = "$(git rev-parse v8.0/config-bucket)"</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline v8.0/config-bucket -1` first line starts with `docs(30-08): phase 30 done-gate`.
    - `git log -1 --show-signature 2>&1` contains `Good "git" signature`.
    - `git log -1 --stat` shows 2 files added: `.planning/phases/30-land-tag/30-DONE-GATE.md` + `.planning/phases/30-land-tag/30-08-SUMMARY.md`.
    - `git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1` equals local `git rev-parse v8.0/config-bucket`.
  </acceptance_criteria>
  <done>Commit A landed: docs(30-08) — done-gate + summary signed + pushed.</done>
</task>

<task type="auto">
  <name>Task 8-5: Update STATE.md + ROADMAP.md (Commit B of two-commit landing)</name>
  <files>
    .planning/STATE.md
    .planning/ROADMAP.md
  </files>
  <read_first>
    .planning/STATE.md (current frontmatter to update incrementally)
    .planning/ROADMAP.md (Phase 30 row at line 89, Phase Details section at line 224, Progress table)
    .planning/phases/29-build-verification/29-10-PLAN.md (steps 5-7 — incremental-update pattern verbatim)
  </read_first>
  <action>
    **Update STATE.md frontmatter:**
    - `completed_phases: 7` (was 6)
    - `completed_plans: 68` (was 59; Phase 30 produced 9 plans: 30-00 through 30-08)
    - `stopped_at: Phase 30 complete; v8.0 milestone done`
    - `last_updated: <current ISO timestamp>`
    - `last_activity: <today YYYY-MM-DD>`
    - Recompute `percent` if auto-derived; otherwise leave alone.
    - Update the Current Position narrative block to reflect Phase 30 close + v8.0 milestone close.

    **Update ROADMAP.md:**
    1. Line ~89 (Phase 30 row in the milestone summary list): `- [ ] **Phase 30: Land + tag**` → `- [x] **Phase 30: Land + tag** (complete <today YYYY-MM-DD>)`.
    2. Phase Details section at line ~224 (`### Phase 30: Land + tag`): flip each of the 5 ROADMAP success criteria checkboxes to checked (if individually-marked). If `Plans: TBD` is still there, replace with the 9-plan list:

    ```
    Plans:
    - [x] 30-00 Phase setup — pre-rebase state captured
    - [x] 30-01 Rebase v8.0 onto master — conflict log
    - [x] 30-02 SYNC-35 — windows + linux CI green on rebased head
    - [x] 30-03 SYNC-36 — PR #4 fast-forward merged onto master
    - [x] 30-04 SYNC-37 — canonical tag v2.0.0-linux-rebased + AppImage/.deb published
    - [x] 30-05 SYNC-33-C + SYNC-34 carry-forward — canonical smoke evidence
    - [x] 30-06 SYNC-38 — linux-port cherry-pick complete
    - [x] 30-07 SYNC-39 — VORTEX-LINUX-MERGE-PLAYBOOK.md milestone post-mortem
    - [x] 30-08 Phase 30 done-gate
    ```

    3. Progress table near line 262: add a row for Phase 30 (`| 30. Land + tag | v8.0 | 9/9 | Complete | <today> |`).

    Commit B:

    1. `git add -f .planning/STATE.md .planning/ROADMAP.md`.
    2. `git commit -m 'docs(30): mark phase 30 complete in roadmap + state' -m 'Phase 30 (land + tag) complete. v8.0 milestone closed at canonical tag v2.0.0-linux-rebased.' -m 'ROADMAP.md Phase 30 row → [x] (complete <date>). STATE.md → completed_phases 7, stopped_at=Phase 30 complete; v8.0 milestone done.'`.
    3. `git log -1 --show-signature` — confirm SSH-signed.
    4. Push: `LIVE=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1); git push --force-with-lease=master:$LIVE git@github.com:atabisz/Vortex.git v8.0/config-bucket:master`.
    5. Final verify: `POST=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1); test "$POST" = "$(git rev-parse v8.0/config-bucket)"`.

  </action>
  <verify>
    <automated>git log --oneline v8.0/config-bucket -1 | grep -q "docs(30): mark phase 30 complete" && git log -1 --show-signature 2>&1 | grep -q 'Good "git" signature' && grep -q "\[x\] \*\*Phase 30" .planning/ROADMAP.md && grep -q "stopped_at.*Phase 30 complete" .planning/STATE.md && POST=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1) && test "$POST" = "$(git rev-parse v8.0/config-bucket)"</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline v8.0/config-bucket -1` first line starts with `docs(30): mark phase 30 complete`.
    - `git log -1 --show-signature 2>&1` contains `Good "git" signature`.
    - `git log -1 --stat` shows STATE.md + ROADMAP.md as the only modified files.
    - `grep -q "\[x\] \*\*Phase 30" .planning/ROADMAP.md` exit 0 (phase 30 row flipped).
    - `grep -q "stopped_at.*Phase 30 complete" .planning/STATE.md` exit 0 (state stopped_at flipped).
    - `git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1` equals local `git rev-parse v8.0/config-bucket`.
  </acceptance_criteria>
  <done>Commit B landed: docs(30) — STATE + ROADMAP flipped, signed, pushed. Two-commit landing complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8-6: Final remote-sync sanity check + milestone-close declaration</name>
  <files></files>
  <read_first>
    .planning/STATE.md (verify post-flip frontmatter)
    .planning/ROADMAP.md (verify post-flip Phase 30 row)
    .planning/phases/30-land-tag/30-DONE-GATE.md (final gate file)
  </read_first>
  <what-built>
    Phase 30 close declared. Both commits landed + pushed. v8.0 milestone closed.
  </what-built>
  <action>
    Final sanity-check pass:

    1. `LOCAL_HEAD=$(git rev-parse HEAD); REMOTE_HEAD=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1); test "$LOCAL_HEAD" = "$REMOTE_HEAD"` — local and remote master in sync.
    2. `git log -2 --show-signature` — confirm BOTH last two commits SSH-signed (Commit A docs(30-08) + Commit B docs(30)).
    3. `git ls-remote git@github.com:atabisz/Vortex.git refs/tags/v2.0.0-linux-rebased | cut -f1` — confirm canonical tag still on fork remote.
    4. `gh release view v2.0.0-linux-rebased --repo atabisz/Vortex --json name,publishedAt,assets` — confirm canonical Linux Beta release still published.
    5. `git ls-remote git@github.com:atabisz/Vortex.git refs/heads/linux-port | cut -f1` — confirm linux-port at post-cherry-pick HEAD (matches 30-CHERRY-PICK-NOTES.md).
    6. `gh pr view 4 --repo atabisz/Vortex --json state -q .state` — confirm `MERGED` (or CLOSED if fallback path was used).
    7. `git status --short` — empty.

    If any check fails, surface — done-gate cannot truly close until all post-conditions hold.

    On all-green: declare Phase 30 + v8.0 milestone CLOSED.

  </action>
  <how-to-verify>
    1. Local + remote master HEAD match (no drift).
    2. Last two commits both SSH-signed (`git log -2 --show-signature` shows two `Good "git" signature` blocks).
    3. Canonical tag on fork remote.
    4. Canonical Linux Beta release published with assets.
    5. linux-port HEAD on fork matches local linux-port post-cherry-pick.
    6. PR #4 MERGED or CLOSED.
    7. Working tree clean.
    8. ROADMAP Phase 30 row checkboxed; STATE Phase 30 marked complete.
  </how-to-verify>
  <acceptance_criteria>
    - `git rev-parse HEAD` equals `git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1`.
    - `git log -2 --show-signature 2>&1 | grep -c 'Good "git" signature'` returns >= 2.
    - `git ls-remote git@github.com:atabisz/Vortex.git refs/tags/v2.0.0-linux-rebased | cut -f1` is non-empty.
    - `gh release view v2.0.0-linux-rebased --repo atabisz/Vortex --json name -q .name` returns a non-empty release name.
    - `gh pr view 4 --repo atabisz/Vortex --json state -q .state` returns `MERGED` or `CLOSED`.
    - `git status --short` returns empty.
    - `grep -q "\[x\] \*\*Phase 30" .planning/ROADMAP.md` exit 0.
  </acceptance_criteria>
  <resume-signal>Type "approved" to declare Phase 30 + v8.0 milestone CLOSED. Type "stop" + describe issue if any sanity check fails.</resume-signal>
</task>

</tasks>

<verification>
- 30-DONE-GATE.md has 7 SYNC sections + requirements table + COMPLETE status line
- 30-08-SUMMARY.md matches 29-10-SUMMARY.md shape
- STATE.md frontmatter reflects 7 completed_phases, 68 completed_plans, Phase 30 done
- ROADMAP.md Phase 30 row marked [x] with completion date; success criteria checked; Plans listed
- Two done-gate commits SSH-signed (`git log -2 --show-signature` shows two Good signature blocks)
- Remote master matches local HEAD
- Canonical tag still on fork; canonical release still published
- linux-port HEAD on fork matches post-cherry-pick state
- PR #4 MERGED or CLOSED
</verification>

<success_criteria>
Phase 30 + v8.0 milestone CLOSED. All 5 native SYNC requirements (35/36/37/38/39) PASS. Both Phase 29 carry-forward items (SYNC-33-C, SYNC-34) PASS against canonical tag artefacts. ROADMAP and STATE reflect milestone closure.
</success_criteria>

<output>
After completion: 30-DONE-GATE.md and 30-08-SUMMARY.md are the durable artefacts. Phase 30 closes the v8.0 (upstream-v2.0.0 sync) milestone. Next work moves to v8.1+ backlog (AppImage update channel, GitHub-Actions step bumps, `@vortex/api` regen-as-chore, etc., per 30-08-SUMMARY.md Followups list).
</output>
