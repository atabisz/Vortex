---
phase: 38-config-bucket-v2-0-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [SYNC-38a, SYNC-38b]
must_haves:
    truths:
        - "Local branch v8.2/sync-upstream-v2.0.2 exists and is checked out at fork/master HEAD (D-38-01)"
        - "Branch base SHA is recorded in 38-01-SUMMARY (current master HEAD ea21358a4 — NOT CONTEXT's stale 855fb3e1a per RESEARCH R4)"
        - "All 8 in-scope hand-resolved Bucket A files contain conflict markers on fork/sync/upstream-v2.0.2 (matches RESEARCH authoritative table)"
        - "package.json + vitest.config.ts + docker/windows/Dockerfile + .vscode/extensions.json have NO conflict markers and NO diff vs fork/master (RESEARCH §Surprise 1+2 — no commits needed)"
        - "extensions/copy-native.mjs + rolldown.base.mjs are explicitly out-of-scope on this branch (RESEARCH R3 — defer to Phase 40)"
        - "R1 verify: packages/paths/package.json + packages/paths-node/package.json present on fork/sync/upstream-v2.0.2 (RESEARCH R1 — restore from fork/master if absent)"
        - "Default resolution stance is hand-resolve per file (D-38-05) — no blanket pick-ours/pick-theirs"
        - "R2 (Jest __mocks__/ scaffolding) deferred to Phase 41 — recorded in 38-01-SUMMARY"
    artifacts:
        - path: ".git/refs/heads/v8.2/sync-upstream-v2.0.2"
          provides: "Local working branch for Phase 38 resolutions"
    key_links:
        - from: "v8.2/sync-upstream-v2.0.2"
          to: "fork/master at ea21358a4"
          via: "git switch -c v8.2/sync-upstream-v2.0.2 fork/master"
          pattern: "branch tip equals fork/master tip at branch creation time"
---

<objective>
Bootstrap the Phase 38 working branch from current `fork/master` HEAD, capture a baseline conflict-marker inventory, verify the R1 carry-forward state (packages/paths{,-node}), and record the v2.0.2-specific deltas vs the v8.1 prior-art template.

Purpose: Subsequent plans (38-02..38-06) commit one resolution per file onto this branch. The baseline inventory acts as a sanity check that the conflict surface matches RESEARCH.md's authoritative table. Drift here would invalidate the per-file stances locked in D-38-06..D-38-15.

The critical v8.2 difference vs v8.1: only 8 hand-resolved files (vs 9), 4 v8.1 files auto-merged clean (no commits needed), 2 new-vs-v8.1 files enter Bucket A (`launch.json`, `renderer/tsconfig.json`), 2 .mjs files carry markers but defer to Phase 40 (`copy-native.mjs`, `rolldown.base.mjs`). RESEARCH R4 also moves the branch base from CONTEXT's stale `855fb3e1a` to current master HEAD `ea21358a4`.

Output: Local branch `v8.2/sync-upstream-v2.0.2` checked out at current `fork/master`; baseline inventory captured; R1 + R2 + R3 risks surfaced in 38-01-SUMMARY.md with explicit deferred-phase routing.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-config-bucket-v2-0-2/38-CONTEXT.md
@.planning/phases/38-config-bucket-v2-0-2/38-RESEARCH.md
@.planning/phases/38-config-bucket-v2-0-2/PATTERNS.md
@.planning/phases/31-config-bucket/31-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch fork remote and create v8.2/sync-upstream-v2.0.2 branch from current master HEAD (D-38-01 + R4)</name>
  <files>.git/refs/heads/v8.2/sync-upstream-v2.0.2</files>
  <read_first>
    - .planning/phases/38-config-bucket-v2-0-2/38-CONTEXT.md (D-38-01: branch v8.2/sync-upstream-v2.0.2 from fork/master)
    - .planning/phases/38-config-bucket-v2-0-2/38-RESEARCH.md (R4: master HEAD drift — CONTEXT cites 855fb3e1a but current is ea21358a4; branch from CURRENT master HEAD)
  </read_first>
  <action>
    Per D-38-01 + RESEARCH R4: create branch `v8.2/sync-upstream-v2.0.2` from current `fork/master` HEAD. The CONTEXT.md cites `855fb3e1a` but current master HEAD is `ea21358a4` (RESEARCH R4 — two `.planning/` commits added since CONTEXT was written, none touching Bucket A). Branch from current.

    Steps:
    1. `git fetch fork master sync/upstream-v2.0.2` — refresh both remote refs before branching.
    2. Capture and record SHAs for the SUMMARY:
       - `git rev-parse fork/master` (expect `ea21358a4`; if different, log and continue).
       - `git rev-parse fork/sync/upstream-v2.0.2` (expect `314ca807c`; if different, the daily `rebase-upstream.yml` cron updated the PR head — re-run conflict enumeration in Task 2 against the new SHA).
    3. `git switch -c v8.2/sync-upstream-v2.0.2 fork/master` — creates and checks out the local branch at current master tip. Use `git switch -C` to force-recreate if the branch exists from a prior aborted run.
    4. Verify HEAD: `git rev-parse HEAD` matches `git rev-parse fork/master`.

    Do NOT commit anything in this task. No file modifications. The branch creation is the only state change. Note the merge state will be clean here — conflict markers live on `fork/sync/upstream-v2.0.2`, not on master. Plans 38-02..38-06 will pull resolved file content from `fork/sync/upstream-v2.0.2` and resolve the markers in place.

    Critical implementation detail: the conflict markers on `fork/sync/upstream-v2.0.2` are committed-in by PR #6's auto-merge — there is no live merge state. To get the conflict-marker file content into the working tree on `v8.2/sync-upstream-v2.0.2`, Task 5 captures the in-scope file list, and each downstream plan (38-02..38-05) starts by `git checkout fork/sync/upstream-v2.0.2 -- <file>` to pull the marker-bearing version, then hand-edits to resolve.

  </action>
  <verify>
    <automated>test "$(git rev-parse HEAD)" = "$(git rev-parse fork/master)" &amp;&amp; test "$(git rev-parse --abbrev-ref HEAD)" = "v8.2/sync-upstream-v2.0.2"</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse --abbrev-ref HEAD` outputs `v8.2/sync-upstream-v2.0.2`
    - `git rev-parse HEAD` equals `git rev-parse fork/master` at branch-creation time
    - Working tree clean (no uncommitted changes); the branch base SHA is logged
  </acceptance_criteria>
  <done>Branch `v8.2/sync-upstream-v2.0.2` checked out at current fork/master HEAD; branch base SHA captured for SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Authoritative conflict enumeration on fork/sync/upstream-v2.0.2 (RESEARCH §Conflict File Enumeration)</name>
  <files>(no file modifications — verification only)</files>
  <read_first>
    - .planning/phases/38-config-bucket-v2-0-2/38-RESEARCH.md (Conflict File Enumeration table — 8 in-scope hand-resolved + 1 lockfile + 2 out-of-scope .mjs)
  </read_first>
  <action>
    Sanity-check the conflict surface on `fork/sync/upstream-v2.0.2` matches RESEARCH's authoritative table.

    1. Run the full Bucket A enumeration against the remote ref:
       ```
       git grep -l '^<<<<<<< ' fork/sync/upstream-v2.0.2 -- '*' | grep -vE '\.planning/|\.github/'
       ```
       Expected output (10 paths total):
       - `pnpm-workspace.yaml`
       - `src/main/eslint.config.mjs`
       - `src/preload/eslint.config.mjs`
       - `src/renderer/eslint.config.mjs`
       - `src/shared/eslint.config.mjs`
       - `src/main/prepare-dist-package.mjs`
       - `src/renderer/tsconfig.json`
       - `.vscode/launch.json`
       - `pnpm-lock.yaml`
       - `extensions/copy-native.mjs` (out-of-scope, Phase 40)
       - `rolldown.base.mjs` (out-of-scope, Phase 40)

       If the count or paths differ, the daily `rebase-upstream.yml` cron may have rewritten `fork/sync/upstream-v2.0.2` since RESEARCH was authored — STOP and report drift.

    2. Confirm the 4 v8.1 prior-art files have NO conflict markers AND NO diff (RESEARCH §Surprise 1+2):
       ```
       for f in package.json vitest.config.ts docker/windows/Dockerfile .vscode/extensions.json; do
         echo "=== $f ==="
         git show fork/sync/upstream-v2.0.2:"$f" | grep -c '^<<<<<<< ' || true
         git diff --stat fork/master..fork/sync/upstream-v2.0.2 -- "$f"
       done
       ```
       Expected: zero conflict markers AND zero diff lines for all four. If any has either, the v8.2 plan partition is wrong — STOP.

    3. Per-file conflict-region count sanity (matches RESEARCH table):
       ```
       for f in pnpm-workspace.yaml src/main/eslint.config.mjs src/preload/eslint.config.mjs \
                src/renderer/eslint.config.mjs src/shared/eslint.config.mjs \
                src/main/prepare-dist-package.mjs src/renderer/tsconfig.json \
                .vscode/launch.json; do
         echo "$f $(git show fork/sync/upstream-v2.0.2:"$f" | grep -c '^<<<<<<< ')"
       done
       ```
       Expected counts (per RESEARCH): `pnpm-workspace.yaml 1`, `src/main/eslint.config.mjs 2`, `src/preload/eslint.config.mjs 1`, `src/renderer/eslint.config.mjs 2`, `src/shared/eslint.config.mjs 2`, `src/main/prepare-dist-package.mjs 1`, `src/renderer/tsconfig.json 1`, `.vscode/launch.json 3`. Total 13 regions across the 8 in-scope files (+1 marker on lockfile = 14).

    No commits, no edits. Pure guard against a stale RESEARCH.md.

  </action>
  <verify>
    <automated>test "$(git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' fork/sync/upstream-v2.0.2 -- pnpm-workspace.yaml 'src/main/eslint.config.mjs' 'src/preload/eslint.config.mjs' 'src/renderer/eslint.config.mjs' 'src/shared/eslint.config.mjs' 'src/main/prepare-dist-package.mjs' 'src/renderer/tsconfig.json' '.vscode/launch.json' pnpm-lock.yaml | wc -l)" = "9" &amp;&amp; test "$(git show fork/sync/upstream-v2.0.2:package.json | grep -c '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ')" = "0" &amp;&amp; test "$(git show fork/sync/upstream-v2.0.2:vitest.config.ts | grep -c '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - 9 in-scope conflict-bearing paths listed (8 hand-resolved + 1 lockfile)
    - `package.json`, `vitest.config.ts`, `docker/windows/Dockerfile`, `.vscode/extensions.json` all have zero conflict markers and zero diff vs `fork/master`
    - Per-file region counts match RESEARCH table (13 regions across the 8 in-scope files)
  </acceptance_criteria>
  <done>Baseline inventory matches RESEARCH authoritative table. Plans 38-02..38-06 may proceed.</done>
</task>

<task type="auto">
  <name>Task 3: R1 verify — packages/paths{,-node}/package.json present on fork/sync/upstream-v2.0.2</name>
  <files>(no file modifications — verification only)</files>
  <read_first>
    - .planning/phases/38-config-bucket-v2-0-2/38-RESEARCH.md (R1 — packages/paths and packages/paths-node orphan package.json carry-forward from v8.1; verify present on v2.0.2 sync ref)
    - .planning/phases/31-config-bucket/31-01-PLAN.md (v8.1 R1 mitigation pattern: restore from fork/master if absent)
  </read_first>
  <action>
    Confirm the v8.1 Phase 31 R1 restoration of `packages/paths/package.json` and `packages/paths-node/package.json` survived through the v2.0.2 auto-merge. If absent, restore from `fork/master` as an additional first commit on `v8.2/sync-upstream-v2.0.2` (mirrors v8.1 31-01 Task 4).

    Steps:
    1. Probe the sync branch for both files:
       ```
       git cat-file -e fork/sync/upstream-v2.0.2:packages/paths/package.json && echo "paths/package.json: present" || echo "paths/package.json: ABSENT"
       git cat-file -e fork/sync/upstream-v2.0.2:packages/paths-node/package.json && echo "paths-node/package.json: present" || echo "paths-node/package.json: ABSENT"
       ```

    2. **If both present:** R1 is benign for v8.2. Record in 38-01-SUMMARY: "R1 carry-forward verified — packages/paths{,-node}/package.json survived v2.0.2 auto-merge. No restore commit needed." Skip step 3.

    3. **If either absent:** restore from `fork/master` as a fix-up commit on the working branch:
       ```
       git checkout fork/master -- packages/paths/package.json packages/paths-node/package.json
       # Optional: pull build configs too if they're missing
       for f in packages/paths/tsconfig.json packages/paths/tsdown.config.ts \
                packages/paths-node/tsconfig.json packages/paths-node/tsconfig.build.json \
                packages/paths-node/vitest.config.ts; do
         test -e "$f" || git checkout fork/master -- "$f" 2>/dev/null || true
       done
       node -e "JSON.parse(require('fs').readFileSync('packages/paths/package.json','utf8'))"
       node -e "JSON.parse(require('fs').readFileSync('packages/paths-node/package.json','utf8'))"
       git add packages/paths packages/paths-node
       git commit -m "restore(workspaces): packages/paths{,-node} package.json (v8.1 R1 carry-forward to v8.2)" \
         -m "v2.0.2 auto-merge dropped packages/paths{,-node}/package.json again. Restoring from fork/master so pnpm install in 38-06 succeeds (pnpm-workspace.yaml globs ./packages/* with failIfNoMatch: true). Mirrors v8.1 Phase 31 R1 mitigation."
       ```
       Note: this commit is fork-internal (working tree on the local branch), not affecting the source PR #6. SSH signing is on per memory `feedback_ssh_signing.md` — do NOT pass `--no-gpg-sign`.

    4. Whichever branch is taken, document in 38-01-SUMMARY:
       - Pre-state: present / absent for each file
       - Action taken: none / restore commit SHA

  </action>
  <verify>
    <automated>git cat-file -e fork/sync/upstream-v2.0.2:packages/paths/package.json &amp;&amp; git cat-file -e fork/sync/upstream-v2.0.2:packages/paths-node/package.json</automated>
  </verify>
  <acceptance_criteria>
    - Both `packages/paths/package.json` and `packages/paths-node/package.json` resolve on `fork/sync/upstream-v2.0.2` (R1 benign), OR
    - Restore commit landed on the working branch with title `restore(workspaces): packages/paths{,-node} package.json (v8.1 R1 carry-forward to v8.2)` and both files parse as valid JSON
    - 38-01-SUMMARY records pre-state + action taken
  </acceptance_criteria>
  <done>R1 risk closed for v8.2 — either confirmed benign or remediated with a restore commit.</done>
</task>

<task type="auto">
  <name>Task 4: Confirm out-of-scope .mjs files (R3) and pre-flight package.json conflict scan (RESEARCH R1 + Plan 38-06 precondition)</name>
  <files>(no file modifications — verification only)</files>
  <read_first>
    - .planning/phases/38-config-bucket-v2-0-2/38-RESEARCH.md (R3 — extensions/copy-native.mjs + rolldown.base.mjs defer to Phase 40; PATTERNS Plan 38-06 note about workspace package.json precondition)
  </read_first>
  <action>
    Two pre-flight checks before resolution plans run:

    1. **R3 out-of-scope confirmation:** the two .mjs files carry conflict markers but belong to Phase 40 (gamebryo + per-game extensions, mirroring v8.1 Phase 33). Plans 38-02..38-06 must NOT touch them.
       ```
       git show fork/sync/upstream-v2.0.2:extensions/copy-native.mjs | grep -c '^<<<<<<< '
       git show fork/sync/upstream-v2.0.2:rolldown.base.mjs | grep -c '^<<<<<<< '
       ```
       Expected: each returns ≥1 (still conflict-bearing). Record SHA references in SUMMARY for the Phase 40 hand-off.

    2. **Workspace package.json precondition for Plan 38-06:** v8.1 Phase 31-07 was blocked by an unrelated conflict in `extensions/games/game-baldursgate3/package.json` that prevented `pnpm install` until atomically resolved. Verify v8.2 has no analogous in-flight workspace package.json conflict:
       ```
       git grep -l '^<<<<<<< ' fork/sync/upstream-v2.0.2 -- '**/package.json' 2>/dev/null
       ```
       Expected: empty output. If any workspace `package.json` is listed, log it in the SUMMARY as a Plan 38-06 precondition that needs surgical resolution before lockfile regen (Plan 38-06 will reference this finding).

    No commits, no edits.

  </action>
  <verify>
    <automated>test "$(git show fork/sync/upstream-v2.0.2:extensions/copy-native.mjs | grep -c '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ')" -ge "1" &amp;&amp; test "$(git show fork/sync/upstream-v2.0.2:rolldown.base.mjs | grep -c '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ')" -ge "1"</automated>
  </verify>
  <acceptance_criteria>
    - `extensions/copy-native.mjs` and `rolldown.base.mjs` confirmed conflict-bearing (and out-of-scope) on the sync branch
    - Workspace `**/package.json` scan returns empty (no Plan 38-06 precondition) — OR any hits are logged in 38-01-SUMMARY for surgical handling
    - Both findings documented in 38-01-SUMMARY
  </acceptance_criteria>
  <done>R3 deferral confirmed. Plan 38-06 lockfile regen has no known precondition blockers (or those are surfaced for 38-06 to handle).</done>
</task>

<task type="auto">
  <name>Task 5: Write 38-01-SUMMARY.md with baseline state, drift, and deferred-risk routing</name>
  <files>.planning/phases/38-config-bucket-v2-0-2/38-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/38-config-bucket-v2-0-2/38-RESEARCH.md (R1, R2, R3, R4, R5 sections)
    - .planning/phases/38-config-bucket-v2-0-2/PATTERNS.md (Plan 38-01 analog)
  </read_first>
  <action>
    Record the Phase 38 baseline state for downstream plan inheritance. Use the standard SUMMARY template (`@/home/alex/.claude/get-shit-done/templates/summary.md`).

    Required content:

    1. **Branch SHAs**
       - `git rev-parse v8.2/sync-upstream-v2.0.2` (branch tip)
       - `git rev-parse fork/master` (branch base — should match the tip on first creation)
       - `git rev-parse fork/sync/upstream-v2.0.2` (PR #6 head reference for resolution lookups)

    2. **Master HEAD drift (R4)**
       - CONTEXT cited `855fb3e1a`; current is `ea21358a4`. Drift is two `.planning/` commits seeding REQUIREMENTS/ROADMAP — none touch Bucket A. Branch was cut from current master HEAD per RESEARCH R4 recommendation.

    3. **In-scope conflict files (8 hand-resolved + 1 lockfile = 9 resolution targets)**
       List each path with its region count from RESEARCH table.

    4. **No-op vs v8.1 prior art (4 files)**
       `package.json`, `vitest.config.ts`, `docker/windows/Dockerfile`, `.vscode/extensions.json` — each has zero markers AND zero diff vs `fork/master`. No commits planned.

    5. **NEW vs v8.1 (2 files)**
       `.vscode/launch.json` (3 regions, all D-38-13 `build/` vs `out/`) and `src/renderer/tsconfig.json` (1 region, fork's test-glob excludes). Both bundled into Plan 38-02.

    6. **R1 result** — present / absent for `packages/paths{,-node}/package.json`. Restore commit SHA if applied.

    7. **R2 deferred to Phase 41** — Jest `__mocks__/` scaffolding may have been re-introduced by v2.0.1/v2.0.2; banned by VORTEX-LINUX-MERGE-PLAYBOOK.md §11. Phase 41 (per SYNC-41c) cleans this up. Recommended commit when handled: `chore(playbook): remove reintroduced src/renderer/src/__mocks__/ scaffolding (§11 v2.0.2 carry-forward)`. Does NOT block Phase 38 done-gate.

    8. **R3 out-of-scope (deferred to Phase 40)** — `extensions/copy-native.mjs` + `rolldown.base.mjs` carry conflict markers on `fork/sync/upstream-v2.0.2` but belong to Phase 40 (gamebryo + per-game extensions, mirroring v8.1 Phase 33). Plans 38-02..38-06 must NOT touch them. Phase 38 done-gate (D-38-17 item 1 — zero conflict markers in **Bucket A**) is unaffected.

    9. **R5 husky note** — only `pnpm-workspace.yaml` is YAML-conflicted in v8.2; no companion `package.json` conflict. Plans should run hooks normally without `--no-verify`. Sequencing: 38-05 (workspace.yaml) before 38-06 (lockfile) recommended; the other plans are wave-2-parallel and the hooks tolerate any order (oxfmt only parses files it formats).

    10. **Plan 38-06 precondition status** — workspace `**/package.json` scan result from Task 4. If clean, document "no precondition blocker."

    11. **Decision references** — D-38-01 (branch), D-38-02 (push), D-38-03 (atomic commits), D-38-17 (done-gate).

    Note: `.planning/` is gitignored (per memory `feedback_planning_gitignored.md`); use `git add -f` if committing this file. Phase 38 plans intentionally do not commit SUMMARYs — they're written for the orchestrator's consumption only.

  </action>
  <verify>
    <automated>test -f .planning/phases/38-config-bucket-v2-0-2/38-01-SUMMARY.md &amp;&amp; grep -q 'R1' .planning/phases/38-config-bucket-v2-0-2/38-01-SUMMARY.md &amp;&amp; grep -q 'R3' .planning/phases/38-config-bucket-v2-0-2/38-01-SUMMARY.md &amp;&amp; grep -q 'R4' .planning/phases/38-config-bucket-v2-0-2/38-01-SUMMARY.md &amp;&amp; grep -q 'launch.json' .planning/phases/38-config-bucket-v2-0-2/38-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `38-01-SUMMARY.md` exists and contains all 11 sections above
    - Branch tip + base + PR-head SHAs all recorded
    - R1, R2, R3, R4, R5 each addressed with explicit deferred-phase routing where applicable
    - 4 no-op files, 2 NEW-vs-v8.1 files, 8 in-scope files, 2 out-of-scope .mjs files all enumerated
  </acceptance_criteria>
  <done>Baseline state and deferred risks captured for downstream Phase 38 plan handoff and Phase 40/41 pickup.</done>
</task>

</tasks>

<verification>
- `git rev-parse --abbrev-ref HEAD` is `v8.2/sync-upstream-v2.0.2`
- 9 in-scope conflict-bearing paths listed by `git grep -l '^<<<<<<< ' fork/sync/upstream-v2.0.2 -- <bucket A>` (8 hand-resolved + lockfile)
- 4 v8.1 prior-art files (`package.json`, `vitest.config.ts`, Dockerfile, extensions.json) have zero markers and zero diff
- `packages/paths{,-node}/package.json` confirmed present on the sync branch (or restored)
- `.planning/phases/38-config-bucket-v2-0-2/38-01-SUMMARY.md` records branch SHAs, drift summary, R1/R2/R3/R4/R5 dispositions
</verification>

<success_criteria>
Phase 38 working branch ready for hand-resolution work. R1 risk closed (verified or remediated). R2/R3 deferred with explicit pickup instructions for Phases 40/41. RESEARCH per-file stances (D-38-06..D-38-15) match the actual conflict surface on `fork/sync/upstream-v2.0.2`. Branch base correctly set to current master HEAD `ea21358a4` per R4 (not CONTEXT's stale `855fb3e1a`).
</success_criteria>

<output>
Create `.planning/phases/38-config-bucket-v2-0-2/38-01-SUMMARY.md` recording: branch tip SHA + master HEAD base SHA + PR-head SHA, master HEAD drift summary (R4), the 8 in-scope conflict files with region counts, the 4 no-op files (Surprise 1+2), the 2 NEW-vs-v8.1 files (launch.json, renderer/tsconfig.json), R1 result, R2 deferred-to-Phase-41, R3 out-of-scope-Phase-40, R5 husky note, Plan 38-06 precondition status.
</output>
