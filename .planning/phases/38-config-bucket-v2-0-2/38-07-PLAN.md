---
phase: 38-config-bucket-v2-0-2
plan: 07
type: execute
wave: 4
depends_on: [06]
files_modified: []
autonomous: false
requirements: [SYNC-38a, SYNC-38b]
must_haves:
    truths:
        - "Local v8.2/sync-upstream-v2.0.2 branch is fast-forward-only ahead of fork/sync/upstream-v2.0.2 OR a clean rebase has been performed if the cron raced"
        - "git push --force-with-lease succeeds and updates fork/sync/upstream-v2.0.2 to v8.2/sync-upstream-v2.0.2's HEAD (D-38-02 — defends against rebase-upstream.yml cron via lease check)"
        - "PR #6 head reflects the resolved tree"
        - "Phase 38 atomic-commit history (8 file resolutions + 1 lockfile = 9 commits per D-38-03 + D-38-16 cadence; +1 if R1 restore was needed) preserved on remote"
    artifacts:
        - path: "(remote ref) fork/sync/upstream-v2.0.2"
          provides: "Updated PR #6 head with all 9 in-scope Bucket A conflicts resolved"
    key_links:
        - from: "v8.2/sync-upstream-v2.0.2 (local)"
          to: "fork/sync/upstream-v2.0.2 (remote)"
          via: "git push --force-with-lease with inline SSH URL"
          pattern: "lease check refuses if rebase-upstream.yml cron raced"
---

<objective>
Push the resolved `v8.2/sync-upstream-v2.0.2` branch to the fork as `sync/upstream-v2.0.2` using `--force-with-lease` per D-38-02. PR #6's review thread continues. Daily `rebase-upstream.yml` cron is defended against by the lease check.

Purpose: This plan is **autonomous: false** because force-pushing to a shared remote branch is a high-blast-radius destructive operation per CLAUDE.md operational rules:

- `--force-with-lease` will refuse the push if the `rebase-upstream.yml` cron has updated `fork/sync/upstream-v2.0.2` since Plan 38-01 created the local branch — that race requires human inspection (rebase vs replan).
- Final visible state on PR #6 is shared with reviewers — human confirmation that the local branch is in the desired state before pushing matches the GSD checkpoint:human-verify pattern.
- Per memory `feedback_git_push_ssh.md`: sandbox blocks `.git/config` rewrites, so the push must use the inline SSH URL `git@github.com:atabisz/Vortex.git`.

Difference vs v8.1 31-08: v8.1 was first-push (no remote ref existed; `--force-with-lease` was no-op). v8.2 pushes to the **existing** `sync/upstream-v2.0.2` branch (PR #6 head at `314ca807c`) — the lease check is real this time and defends against the daily cron.

Output: Remote `fork/sync/upstream-v2.0.2` updated to point at the resolved tree. PR #6 reflects the resolution. Phase 38 fully landed.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/38-config-bucket-v2-0-2/38-CONTEXT.md
@.planning/phases/38-config-bucket-v2-0-2/38-RESEARCH.md
@.planning/phases/38-config-bucket-v2-0-2/PATTERNS.md
@.planning/phases/31-config-bucket/31-08-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-push sanity check — verify local branch state matches Phase 38 expectations</name>
  <files>(no file modifications — verification only)</files>
  <read_first>
    - .planning/phases/38-config-bucket-v2-0-2/38-CONTEXT.md (D-38-02: push to fork/sync/upstream-v2.0.2 once at phase end with --force-with-lease; D-38-03: atomic commits — one per file + 1 lockfile)
  </read_first>
  <action>
    Verify local `v8.2/sync-upstream-v2.0.2` is in expected state before any remote interaction.

    1. Confirm checked-out branch:
       ```
       git rev-parse --abbrev-ref HEAD
       ```
       Expected: `v8.2/sync-upstream-v2.0.2`.

    2. Confirm working tree clean:
       ```
       git status --porcelain
       ```
       Expected: empty (no uncommitted changes, no untracked files in tracked-paths set).

    3. Count atomic commits since branch base. Phase 38 expected commit count:
       - 1 commit for `.vscode/launch.json` (Plan 38-02 Task 1)
       - 1 commit for `src/renderer/tsconfig.json` (Plan 38-02 Task 2)
       - 4 commits for the eslint.config.mjs files (Plan 38-03 Tasks 1-4)
       - 1 commit for `src/main/prepare-dist-package.mjs` (Plan 38-04)
       - 1 commit for `pnpm-workspace.yaml` (Plan 38-05)
       - 1 commit for `pnpm-lock.yaml` regen (Plan 38-06)
       - **Total: 9 commits** (or 10 if Plan 38-01 R1 restore commit was needed; check 38-01-SUMMARY for "Restore commit SHA").
       ```
       git log --oneline fork/master..v8.2/sync-upstream-v2.0.2 | wc -l
       ```
       Expected: 9 or 10. Anything else suggests a missing or extra commit and warrants investigation — log the actual count in SUMMARY before proceeding.

    4. Confirm zero conflict markers across the resolved Bucket A scope (other source files outside Bucket A still have markers — those are Phases 39-41 scope; both `extensions/copy-native.mjs` and `rolldown.base.mjs` should still be marker-bearing on this branch since we did NOT touch them):
       ```
       git grep -l '^<<<<<<< ' -- \
         pnpm-workspace.yaml pnpm-lock.yaml \
         'src/main/eslint.config.mjs' 'src/preload/eslint.config.mjs' \
         'src/renderer/eslint.config.mjs' 'src/shared/eslint.config.mjs' \
         'src/main/prepare-dist-package.mjs' \
         'src/renderer/tsconfig.json' '.vscode/launch.json'
       ```
       Expected: empty.

    5. Re-run frozen-lockfile sanity from Plan 38-06:
       ```
       pnpm install --frozen-lockfile
       ```
       Expected: exit 0.

    6. R1 verify — confirm packages/paths{,-node}/package.json present:
       ```
       test -f packages/paths/package.json && test -f packages/paths-node/package.json
       ```

  </action>
  <verify>
    <automated>test "$(git rev-parse --abbrev-ref HEAD)" = "v8.2/sync-upstream-v2.0.2" &amp;&amp; test -z "$(git status --porcelain)" &amp;&amp; test -z "$(git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- pnpm-workspace.yaml pnpm-lock.yaml 'src/main/eslint.config.mjs' 'src/preload/eslint.config.mjs' 'src/renderer/eslint.config.mjs' 'src/shared/eslint.config.mjs' 'src/main/prepare-dist-package.mjs' 'src/renderer/tsconfig.json' '.vscode/launch.json' 2&gt;/dev/null)" &amp;&amp; test -f packages/paths/package.json &amp;&amp; test -f packages/paths-node/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse --abbrev-ref HEAD` returns `v8.2/sync-upstream-v2.0.2`
    - `git status --porcelain` returns empty
    - The Bucket A `git grep '^<<<<<<< '` check returns zero files
    - Commit count 9 (or 10 if R1 restore was needed) — actual count logged in SUMMARY
    - `packages/paths/package.json` and `packages/paths-node/package.json` both exist (R1 intact)
    - `pnpm install --frozen-lockfile` exits 0
  </acceptance_criteria>
  <done>Local branch state matches Phase 38 expectations; safe to attempt the lease-check fetch.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh remote ref and detect cron race</name>
  <files>(no file modifications — fetch only)</files>
  <read_first>
    - .planning/phases/38-config-bucket-v2-0-2/38-CONTEXT.md (D-38-02: --force-with-lease defends against rebase-upstream.yml cron clobber)
  </read_first>
  <action>
    Refresh the remote reference and detect whether `fork/sync/upstream-v2.0.2` has been updated since Plan 38-01 created the local branch (cron race detection).

    1. `git fetch fork sync/upstream-v2.0.2` — refresh remote ref.

    2. Compute branch base:
       ```
       BRANCH_BASE=$(git merge-base v8.2/sync-upstream-v2.0.2 fork/sync/upstream-v2.0.2)
       ```
       Note: this returns the most recent common ancestor. For v8.2, this should be the merge-tree commit `a918d52ef` (PR #6's auto-merge commit) OR earlier — depending on exactly how the local branch interacts with the sync ref. The race-detection logic compares current remote tip vs the value Plan 38-01 saw at branch creation.

    3. Compute current remote tip:
       ```
       REMOTE_HEAD=$(git rev-parse fork/sync/upstream-v2.0.2)
       ```
       Expected (per RESEARCH date): `314ca807c1da7fb0f227c25f8d69d948b60f3fed`. If different, the cron raced.

    4. Cross-reference against 38-01-SUMMARY's recorded "PR-head SHA":
       - `RECORDED=$(grep -E 'fork/sync/upstream-v2.0.2.*[a-f0-9]{40}' .planning/phases/38-config-bucket-v2-0-2/38-01-SUMMARY.md | head -1)` (heuristic — adjust to the SUMMARY's actual format).
       - If `REMOTE_HEAD` differs from the SHA recorded by Plan 38-01, the cron raced.

    5. Print results for human reviewer:
       ```
       echo "BRANCH_BASE=$BRANCH_BASE"
       echo "REMOTE_HEAD=$REMOTE_HEAD"
       echo "RECORDED in 38-01: $RECORDED"
       echo "Race detected: $([[ "$REMOTE_HEAD" == "314ca807c1da7fb0f227c25f8d69d948b60f3fed" ]] || [[ "$REMOTE_HEAD" == "$RECORDED" ]] && echo no || echo YES)"
       ```

    6. If race detected, capture the new commits the cron added:
       ```
       git log v8.2/sync-upstream-v2.0.2..fork/sync/upstream-v2.0.2 --oneline > /tmp/cron-race-commits.txt
       cat /tmp/cron-race-commits.txt
       ```
       This output feeds Task 3's checkpoint decision.

  </action>
  <verify>
    <automated>git fetch fork sync/upstream-v2.0.2 &amp;&amp; git rev-parse fork/sync/upstream-v2.0.2 &gt; /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `git fetch fork sync/upstream-v2.0.2` exits 0
    - Race-detection result (no-race vs YES-race) logged for Task 3 checkpoint
    - If race: list of cron-introduced commits captured in `/tmp/cron-race-commits.txt`
  </acceptance_criteria>
  <done>Remote state refreshed; cron-race status known and recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-confirm push (force-with-lease) — fallback path if cron raced</name>
  <what-built>
    Plans 38-01 through 38-06 produced a local branch `v8.2/sync-upstream-v2.0.2` that resolves all 9 in-scope Bucket A conflict-bearing files on `fork/sync/upstream-v2.0.2` (8 hand-resolved + lockfile regenerated). Done-gate D-38-17 (all 5 items) satisfied per Plan 38-06 Task 5. Tasks 1+2 confirmed local state and refreshed the remote ref.

    Out-of-scope on this branch (still marker-bearing — Phase 40 territory): `extensions/copy-native.mjs`, `rolldown.base.mjs`. Push will carry these forward unchanged — that's intentional.

    What remains: pushing `v8.2/sync-upstream-v2.0.2` to `fork/sync/upstream-v2.0.2` with `--force-with-lease` so PR #6 reflects the resolution.

    Why this requires human approval (autonomous: false):
    - Force-push to a shared remote is high-blast-radius per CLAUDE.md.
    - PR #6 review thread is shared — final tree state needs sign-off before going live.
    - `--force-with-lease` may refuse if cron raced; the rebase-vs-replan decision is human-judgment-grade.

  </what-built>
  <how-to-verify>
    1. Review cron-race status logged by Task 2 (`Race detected: no` vs `Race detected: YES`).

    2. **If `Race detected: no`:** approve the push. The lease form pins to the SHA Plan 38-01 saw:
       ```
       BRANCH_BASE=$(git merge-base v8.2/sync-upstream-v2.0.2 fork/sync/upstream-v2.0.2)
       git push --force-with-lease=sync/upstream-v2.0.2:$BRANCH_BASE \
         git@github.com:atabisz/Vortex.git \
         v8.2/sync-upstream-v2.0.2:sync/upstream-v2.0.2
       ```
       Per memory `feedback_git_push_ssh.md`: sandbox blocks `.git/config`, so push uses the inline SSH URL `git@github.com:atabisz/Vortex.git` rather than a remote name shortcut.

       The lease form `--force-with-lease=<branch>:<expected-sha>` pins the lease to the SHA the local branch was based on (BRANCH_BASE from Task 2).

       Expected: push succeeds, remote updated, PR #6 head reflects the resolved tree.

    3. **If `Race detected: YES`:** DO NOT push directly. Fallback path:
       a. Inspect new remote commits: `cat /tmp/cron-race-commits.txt` (captured by Task 2).
       b. Decide:
          - **Rebase** (if cron commits are mechanical / non-conflicting with our Bucket A resolutions): proceed to (c).
          - **Abort + replan** (if cron-introduced changes overlap Bucket A files): report the cron diff scope back to planning; Phase 38 needs a follow-up plan or partial replan.
       c. If rebasing: `git rebase fork/sync/upstream-v2.0.2` on `v8.2/sync-upstream-v2.0.2`. Resolve any rebase conflicts following CONTEXT D-38-05..D-38-15 stances per file. Re-run `pnpm install --frozen-lockfile` to confirm lockfile still consistent post-rebase. Re-run Task 2 to refresh the lease, then re-run this checkpoint with the updated `BRANCH_BASE`.

    4. Confirm push landed by inspecting GitHub:
       ```
       gh pr view 6 --repo atabisz/Vortex --json headRefName,headRefOid,statusCheckRollup
       ```
       Use `GH_TOKEN` env var per memory `reference_github_token.md` if `gh auth` is expired.
       Expected: `headRefOid` matches `git rev-parse v8.2/sync-upstream-v2.0.2` (local).

    5. Optional: post a brief comment on PR #6 noting Phase 38 closeout. Casual voice per memory `feedback_casual_voice.md` — something like "Phase 38 config bucket resolved. 8 hand-resolutions + lockfile regen. Source-side conflicts (Phases 39-41) still ahead." Not formal/ops-review.

  </how-to-verify>
  <resume-signal>
    - Type "approved — pushed clean" if the no-race push succeeded
    - Type "approved — rebased and pushed" if the rebase-then-push fallback was used
    - Describe the issue and STOP if the rebase surfaces conflicts requiring replan
  </resume-signal>
</task>

</tasks>

<verification>
- `git rev-parse fork/sync/upstream-v2.0.2` matches `git rev-parse v8.2/sync-upstream-v2.0.2` after the push (refetch first: `git fetch fork sync/upstream-v2.0.2`)
- PR #6 (`atabisz/Vortex#6`) `headRefOid` matches local branch HEAD
- All 9 (or 10 with R1 restore) atomic commits land on remote in order

## Phase 38 Done Gate (D-38-17, all five items)

After Task 3's push lands, the phase-level done gate is fully satisfied:

1. `git grep '^<<<<<<< '` zero hits across all 9 in-scope Bucket A files (proven Plan 38-06 Task 5).
2. `pnpm install` succeeds (proven Plan 38-06 Task 3).
3. `pnpm install --frozen-lockfile` succeeds (proven Plan 38-06 Task 4).
4. IDE/TypeScript server loads without parser errors and resolves all workspace packages — automated proxy via `pnpm nx run-many -t typecheck --projects=@vortex/shared` (Plan 38-06 Task 4); full typecheck is Phase 42 work since Phases 39-41 still resolve source-level conflicts.
5. Lockfile drift summary recorded in the chore(deps) commit body (proven Plan 38-06 Task 4).
   </verification>

<success_criteria>
Remote `fork/sync/upstream-v2.0.2` updated to the resolved tree. PR #6 review thread continues. Phase 38 work visible on the shared branch — foundation for Phases 39-43.
</success_criteria>

<rollback>
If lease-checked push is rejected (cron raced and rebase fallback unsuitable): local branch `v8.2/sync-upstream-v2.0.2` remains intact locally; report cron diff scope to planning; Phase 38 needs a follow-up plan to either rebase-and-retry or surgically integrate the cron commits.
If push lands but PR #6 status checks immediately fail (e.g. CI catches a regression that local frozen-lockfile didn't): local branch is still the truth; investigate the CI delta, plan a surgical follow-up commit, push again with a fresh `--force-with-lease`. Don't roll back the push (would clobber the resolved state on remote and lose work for reviewers).
</rollback>

<output>
Create `.planning/phases/38-config-bucket-v2-0-2/38-07-SUMMARY.md` recording: race-detection result; whether direct push or rebase-then-push was used; final remote SHA; PR #6 status check result; final commit count on the branch (expected: 9 or 10). If rebase fallback used, also document: which cron-introduced commits were rebased over; whether any rebase conflicts surfaced; if any did, which D-38-NN stance was applied.
</output>
