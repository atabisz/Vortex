---
phase: 38-config-bucket-v2-0-2
plan: 06
type: execute
wave: 3
depends_on: [02, 03, 04, 05]
files_modified:
    - pnpm-lock.yaml
autonomous: true
requirements: [SYNC-38a, SYNC-38b]
must_haves:
    truths:
        - "All 8 hand-resolved Bucket A files are conflict-free before lockfile regen"
        - "pnpm-lock.yaml is freshly generated by `pnpm install` (D-38-16)"
        - "`pnpm install` exits 0 (D-38-17 gate 2)"
        - "`pnpm install --frozen-lockfile` exits 0 on a second run (D-38-17 gate 3)"
        - "Lockfile drift summary against fork/sync/upstream-v2.0.2:pnpm-lock.yaml is recorded in the lockfile commit body (D-38-17 gate 5 / D-38-18)"
        - "Workspace package resolution succeeds — partial nx typecheck on @vortex/shared exits 0 as automated proxy for D-38-17 gate 4"
        - "Atomic commit titled per D-38-16 — `chore(deps): regenerate pnpm-lock.yaml after v2.0.2 sync`"
    artifacts:
        - path: "pnpm-lock.yaml"
          provides: "Regenerated lockfile consistent with all package.json files in the workspace and pnpm-workspace.yaml catalog (D-38-16)"
    key_links:
        - from: "pnpm-lock.yaml"
          to: "pnpm-workspace.yaml catalog (Plan 38-05) + workspace package.json files"
          via: "pnpm install resolution"
          pattern: "pnpm install --frozen-lockfile passes (lockfile internally consistent with both inputs)"
        - from: "pnpm install"
          to: "packages/paths{,-node}/package.json (Plan 38-01 R1 verify)"
          via: "pnpm-workspace.yaml `./packages/*` glob with failIfNoMatch: true"
          pattern: "pnpm install does not error on workspace dirs because Plan 38-01 verified packages/paths{,-node}/package.json present"
---

<objective>
Regenerate `pnpm-lock.yaml` per D-38-16 (`rm pnpm-lock.yaml && pnpm install`), then run the Phase 38 done-gate (D-38-17): `pnpm install` succeeds, `pnpm install --frozen-lockfile` succeeds on a second run, and a lockfile drift summary is captured in the commit body. Smoke-test workspace package resolution via a partial nx typecheck.

Purpose: Hand-merging the lockfile is brittle and produces non-deterministic results in pnpm strict catalog mode. Fresh regen produces a deterministic lockfile from the resolved package.json + pnpm-workspace.yaml. Plan 38-01's R1 verify confirms `packages/paths{,-node}/package.json` are present so `pnpm install` doesn't trip on `failIfNoMatch: true`.

Per RESEARCH §Lockfile Baseline: drift is tiny (37 lines / ~0.16% of lockfile), expected `pnpm install` runtime 3-8 minutes (vs v8.1's 5-15). Drift summary will be short — bulleted prose works fine, table is overkill.

Output: One atomic lockfile commit with D-38-16 title and drift summary in the body. Phase 38 done-gate satisfied.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/38-config-bucket-v2-0-2/38-CONTEXT.md
@.planning/phases/38-config-bucket-v2-0-2/38-RESEARCH.md
@.planning/phases/38-config-bucket-v2-0-2/PATTERNS.md
@.planning/phases/31-config-bucket/31-07-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight check — verify all 8 hand-resolved files are conflict-free and committed; check workspace package.json conditions</name>
  <files>(no file modifications — verification only)</files>
  <read_first>
    - .planning/phases/38-config-bucket-v2-0-2/38-CONTEXT.md (D-38-17 done-gate prerequisites)
    - .planning/phases/38-config-bucket-v2-0-2/38-RESEARCH.md (Conflict File Enumeration: 8 in-scope hand-resolved files)
    - .planning/phases/38-config-bucket-v2-0-2/38-01-SUMMARY.md (Plan 38-06 precondition status from 38-01 Task 4)
  </read_first>
  <action>
    Before regenerating the lockfile, verify Plans 38-02..38-05 each resolved their files cleanly. If any conflict markers remain in the 8 hand-resolved files, STOP and re-run the responsible plan — `pnpm install` would error on conflict markers in `pnpm-workspace.yaml`.

    Steps:

    1. Confirm zero conflict markers in the 8 in-scope Bucket A files:
       ```
       git grep -l '^<<<<<<< ' -- \
         pnpm-workspace.yaml \
         'src/main/eslint.config.mjs' \
         'src/preload/eslint.config.mjs' \
         'src/renderer/eslint.config.mjs' \
         'src/shared/eslint.config.mjs' \
         'src/main/prepare-dist-package.mjs' \
         'src/renderer/tsconfig.json' \
         '.vscode/launch.json'
       ```
       Expected: zero output. If any file is listed, the corresponding resolution plan didn't complete cleanly — return to that plan first.

    2. Confirm working tree clean for those 8 files (all changes committed):
       ```
       git status --porcelain -- \
         pnpm-workspace.yaml \
         'src/main/eslint.config.mjs' 'src/preload/eslint.config.mjs' \
         'src/renderer/eslint.config.mjs' 'src/shared/eslint.config.mjs' \
         'src/main/prepare-dist-package.mjs' \
         'src/renderer/tsconfig.json' '.vscode/launch.json'
       ```
       Expected: empty.

    3. R1 verify — `packages/paths{,-node}/package.json` exist (per Plan 38-01 outcome):
       ```
       test -f packages/paths/package.json && test -f packages/paths-node/package.json
       ```
       Expected: exit 0.

    4. Workspace package.json precondition (per Plan 38-01 Task 4 finding) — confirm no in-flight conflict markers in any workspace package.json that would block `pnpm install`:
       ```
       git grep -l '^<<<<<<< ' -- '**/package.json' 'package.json' 2>/dev/null
       ```
       Expected: empty output. If any path is listed, STOP and surgically resolve the affected workspace package.json (likely keep-HEAD per v8.1 31-07 BG3 precondition pattern). Re-run this Task 1 after the surgical resolution.

  </action>
  <verify>
    <automated>test -z "$(git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- pnpm-workspace.yaml 'src/main/eslint.config.mjs' 'src/preload/eslint.config.mjs' 'src/renderer/eslint.config.mjs' 'src/shared/eslint.config.mjs' 'src/main/prepare-dist-package.mjs' 'src/renderer/tsconfig.json' '.vscode/launch.json' 2&gt;/dev/null)" &amp;&amp; test -z "$(git status --porcelain -- pnpm-workspace.yaml 'src/main/eslint.config.mjs' 'src/preload/eslint.config.mjs' 'src/renderer/eslint.config.mjs' 'src/shared/eslint.config.mjs' 'src/main/prepare-dist-package.mjs' 'src/renderer/tsconfig.json' '.vscode/launch.json')" &amp;&amp; test -f packages/paths/package.json &amp;&amp; test -f packages/paths-node/package.json &amp;&amp; test -z "$(git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- '**/package.json' 'package.json' 2&gt;/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - The 8-file conflict-marker grep returns zero files
    - `git status --porcelain` for the 8 files returns empty (all committed)
    - `packages/paths/package.json` and `packages/paths-node/package.json` both exist
    - No workspace `**/package.json` is conflict-marker-bearing
  </acceptance_criteria>
  <done>All 8 hand-resolved Bucket A files conflict-free and committed. R1 intact. No workspace package.json precondition. Safe to regenerate the lockfile.</done>
</task>

<task type="auto">
  <name>Task 2: Capture pre-regeneration lockfile reference for drift comparison</name>
  <files>/tmp/upstream-pre-merge-lockfile.yaml (transient — not committed)</files>
  <read_first>
    - .planning/phases/38-config-bucket-v2-0-2/38-RESEARCH.md (Lockfile Baseline: drift comparison target is `fork/sync/upstream-v2.0.2:pnpm-lock.yaml`)
  </read_first>
  <action>
    Save a copy of `fork/sync/upstream-v2.0.2:pnpm-lock.yaml` (the auto-merged-with-1-conflict-marker lockfile that PR #6 produced) for later drift comparison per D-38-17 gate 5.

    Steps:
    1. `git show fork/sync/upstream-v2.0.2:pnpm-lock.yaml > /tmp/upstream-pre-merge-lockfile.yaml`
    2. Verify non-empty: `test -s /tmp/upstream-pre-merge-lockfile.yaml`.
    3. Note: the reference contains 1 conflict marker per RESEARCH — that's expected. The drift summary in Task 4 compares high-level structure, not byte-equality.

    Do NOT commit `/tmp/upstream-pre-merge-lockfile.yaml`. It's transient.

  </action>
  <verify>
    <automated>test -s /tmp/upstream-pre-merge-lockfile.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/upstream-pre-merge-lockfile.yaml` exists and is non-empty
    - Content equals `git show fork/sync/upstream-v2.0.2:pnpm-lock.yaml`
  </acceptance_criteria>
  <done>Pre-merge lockfile saved for drift comparison.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate pnpm-lock.yaml (D-38-16: rm + pnpm install)</name>
  <files>pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/38-config-bucket-v2-0-2/38-CONTEXT.md (D-38-16: rm pnpm-lock.yaml + pnpm install; D-38-17 gate 2: pnpm install must succeed)
    - .planning/phases/38-config-bucket-v2-0-2/38-RESEARCH.md (Lockfile Baseline: estimated runtime 3-8 minutes; native build guards: allowBuilds correctly contains @electron/rebuild after Plan 38-05)
  </read_first>
  <action>
    Per D-38-16: regenerate `pnpm-lock.yaml` from scratch.

    Steps:

    1. `rm pnpm-lock.yaml` — delete the auto-merged-with-1-conflict-marker lockfile.

    2. `pnpm install` — let pnpm resolve and write a fresh lockfile from package.json + pnpm-workspace.yaml. Exit code MUST be 0. This is D-38-17 gate 2.
       - Expected runtime per RESEARCH: 3-8 minutes (catalog only adds 1 entry — `native-errors` — and bumps 1 SHA).
       - Native builds may run for: bsatk, core-js, drivelist, electron, esbuild, font-scanner, gamebryo-savegame, leveldown, loot, protobufjs, unrs-resolver, winapi-bindings, xxhash-addon, @parcel/watcher, @nexusmods/fomod-installer-native — these are gated by allowBuilds (Plan 38-05 verified `@electron/rebuild: true`).

    3. Confirm `pnpm-lock.yaml` exists, is non-empty, and has no conflict markers:
       ```
       test -s pnpm-lock.yaml
       git grep '^<<<<<<< ' -- pnpm-lock.yaml; test $? -eq 1
       ```

    If `pnpm install` fails:
    - Read the error output. Common failure modes:
      * `ENOLOCAL packages/paths` — Plan 38-01 R1 verify missed something. Re-run Plan 38-01.
      * Catalog version conflict — package.json or pnpm-workspace.yaml inconsistency. Investigate which input is wrong, fix via `git commit --amend` on the relevant resolution plan's commit (or a small follow-up commit), retry.
      * Native build failure (e.g. xxhash-addon, leveldown) — Linux/Windows toolchain issue, not a Phase 38 logic issue. Capture in SUMMARY and proceed if the lockfile was still written; build failure is Phase 42 territory.
    - Document the failure mode in 38-06-SUMMARY.md regardless.

    Do NOT commit yet — Task 4 captures the drift summary for the commit body.

  </action>
  <verify>
    <automated>rm -f pnpm-lock.yaml &amp;&amp; pnpm install &amp;&amp; test -s pnpm-lock.yaml &amp;&amp; (git grep '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- pnpm-lock.yaml; test $? -eq 1)</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install` exits 0 (D-38-17 gate 2 / SYNC-38a)
    - `pnpm-lock.yaml` exists and non-empty after install
    - `git grep '^<<<<<<< ' -- pnpm-lock.yaml` returns no output (regenerated lockfile is conflict-marker-free by construction)
  </acceptance_criteria>
  <done>pnpm-lock.yaml freshly generated by pnpm. D-38-17 gate 2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 4: Capture drift summary, commit lockfile, run frozen-lockfile validation, smoke partial typecheck</name>
  <files>pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/38-config-bucket-v2-0-2/38-CONTEXT.md (D-38-17 gates 3+4+5; D-38-18: drift handling — document non-trivial deltas and accept)
    - .planning/phases/38-config-bucket-v2-0-2/38-RESEARCH.md (Lockfile Baseline: 22 ins / 15 del across 1 file, ~0.16% drift, 1 catalog add `native-errors`, 1 SHA bump on `nexus-api`)
  </read_first>
  <action>
    Capture drift summary, commit lockfile per D-38-16 atomic-commit cadence, run frozen-lockfile validation (D-38-17 gate 3), then smoke-test workspace resolution (D-38-17 gate 4 proxy).

    Steps:

    1. **Drift summary** (D-38-17 gate 5 / D-38-18):
       - Compare regenerated `pnpm-lock.yaml` against `/tmp/upstream-pre-merge-lockfile.yaml`.
       - Useful diff strategies:
         * Size delta: `wc -l pnpm-lock.yaml /tmp/upstream-pre-merge-lockfile.yaml`
         * Direct-dep version differences (top-level `dependencies:` and `devDependencies:` sections)
         * Major-version bumps (scan for any package whose major changed)
         * New transitive packages (informational)
         * Confirm catalog-driven additions: `native-errors` appears in regenerated lockfile (came from Plan 38-05)
         * Confirm SHA bump propagated: `nexus-api` references `2d92fd2bdc` (not `4dd3460c2d`)
         * `leveldown` / `levelup` should appear in both (auto-merged-clean preserved)
       - Format the drift summary as a markdown bulleted list (D-38-18 discretion clause; bulleted prose is fine for ~37 lines of drift). Save to `/tmp/lockfile-drift-summary.md`.
       - Required content (4 items minimum):
         (a) lockfile size delta
         (b) any direct-dep major-version bump (or "none" if none)
         (c) confirmation `native-errors` added + `nexus-api` SHA bumped to `2d92fd2bdc`
         (d) "transitive drift accepted per D-38-18" line

    2. **Commit the lockfile with drift summary in the body:**
       Assemble the message in `/tmp/lockfile-commit-msg.txt`:
       ```
       cat > /tmp/lockfile-commit-msg.txt <<'HEAD_EOF'

chore(deps): regenerate pnpm-lock.yaml after v2.0.2 sync

Phase 38 / D-38-16 / D-38-17 — fresh lockfile from rm + pnpm install.
Replaces the auto-merged-with-1-conflict-marker lockfile from PR #6.

## Drift summary (D-38-17 gate 5)

HEAD_EOF
cat /tmp/lockfile-drift-summary.md >> /tmp/lockfile-commit-msg.txt
cat >> /tmp/lockfile-commit-msg.txt <<'TAIL_EOF'

Transitive drift accepted per D-38-18 (catalog mode pins direct deps in
pnpm-workspace.yaml; transitive surface is non-load-bearing for Phase 38
gates). Real impact, if any, surfaces at Phase 42 build verification.
TAIL_EOF
git add pnpm-lock.yaml
git commit -F /tmp/lockfile-commit-msg.txt
```       Hooks run normally — no`--no-verify`(R5: low risk). SSH signing on per memory`feedback_ssh_signing.md`.

    3. **Run frozen-lockfile validation** (D-38-17 gate 3):
       - `pnpm install --frozen-lockfile` — must exit 0. Proves committed lockfile is internally consistent with package.json + pnpm-workspace.yaml.
       - If this fails, the regenerated lockfile drifted from package.json on second pass — investigate before declaring Phase 38 done.

    4. **Workspace package resolution smoke** (D-38-17 gate 4 proxy):
       - `pnpm nx run-many -t typecheck --projects=@vortex/shared` — small, fast, validates nx + workspace resolution.
       - If `@vortex/shared` typechecks, IDE/TS-server will too (gate 4 satisfied automated).
       - If it fails for source-level reasons (Phase 39+ source conflicts not yet resolved), note in 38-06-SUMMARY: "Phase 39+ resolves source conflicts that block full typecheck; partial nx typecheck on @vortex/shared confirms workspace package resolution works." That's still gate 4 satisfied — gate 4 is about workspace resolution, not full typecheck.
       - If it fails for **package-resolution** reasons (e.g. `Cannot find module '@vortex/paths'`), that IS in scope: investigate and fix before declaring Phase 38 done.

    5. Update `38-06-SUMMARY.md` with: commit SHA, drift summary excerpt, frozen-lockfile result, partial typecheck result.

  </action>
  <verify>
    <automated>git grep '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- pnpm-lock.yaml; test $? -eq 1 &amp;&amp; pnpm install --frozen-lockfile &amp;&amp; git log -1 --pretty=format:'%s' -- pnpm-lock.yaml | grep -q '^chore(deps): regenerate pnpm-lock.yaml after v2.0.2 sync'</automated>
  </verify>
  <acceptance_criteria>
    - Commit titled `chore(deps): regenerate pnpm-lock.yaml after v2.0.2 sync` (D-38-16 exact title)
    - Commit body contains a "Drift summary" section with the 4 required observations
    - `pnpm install --frozen-lockfile` exits 0 (D-38-17 gate 3)
    - `git grep '^<<<<<<< ' -- pnpm-lock.yaml` returns no output
    - Partial nx typecheck on `@vortex/shared` result documented in 38-06-SUMMARY (pass = D-38-17 gate 4 automated; fail-for-source-reasons = noted with explicit Phase 39+ deferral)
  </acceptance_criteria>
  <done>Lockfile committed atomically with drift summary in body. D-38-17 gates 2, 3, 5 satisfied. Gate 4 satisfied or documented.</done>
</task>

<task type="auto">
  <name>Task 5: Final phase done-gate verification — assert all five D-38-17 items</name>
  <files>(no file modifications — verification only)</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 38 success criteria)
    - .planning/phases/38-config-bucket-v2-0-2/38-CONTEXT.md (D-38-17 done-gate, all 5 items)
  </read_first>
  <action>
    Run all five D-38-17 done-gate assertions. All must pass before Phase 38 can be declared complete.

    1. **Zero conflict markers across Bucket A** (D-38-17 gate 1):
       ```
       git grep -l '^<<<<<<< ' -- \
         pnpm-workspace.yaml pnpm-lock.yaml \
         'src/main/eslint.config.mjs' 'src/preload/eslint.config.mjs' \
         'src/renderer/eslint.config.mjs' 'src/shared/eslint.config.mjs' \
         'src/main/prepare-dist-package.mjs' \
         'src/renderer/tsconfig.json' '.vscode/launch.json'
       ```
       Expected: empty. The 9 conflict-bearing Bucket A files (8 hand-resolved + 1 lockfile) are all conflict-free.

    2. **`pnpm install` succeeds** (D-38-17 gate 2): proven Task 3. Re-confirm idempotent: `pnpm install` exits 0 even when `pnpm-lock.yaml` exists.

    3. **`pnpm install --frozen-lockfile` succeeds** (D-38-17 gate 3): proven Task 4. Re-confirm.

    4. **IDE/TypeScript server loads tree without parse errors** (D-38-17 gate 4):
       - Automated proxy from Task 4: `pnpm nx run-many -t typecheck --projects=@vortex/shared` exited 0 (or comparable result documented).
       - Strong proxy: workspace package resolution works; full-typecheck failures from source-level conflicts in Phases 39-41 are out of scope per the gate definition.

    5. **Lockfile drift summary in commit body** (D-38-17 gate 5):
       - `git log -1 --pretty=format:'%b' pnpm-lock.yaml` (or HEAD body) contains the drift summary with the 4 required observations.

    Capture each assertion's result in `38-06-SUMMARY.md`. If any of 1-3 fail, Phase 38 is not done. If 4 fails for source reasons (Phases 39-41), document and proceed.

  </action>
  <verify>
    <automated>test -z "$(git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- pnpm-workspace.yaml pnpm-lock.yaml 'src/main/eslint.config.mjs' 'src/preload/eslint.config.mjs' 'src/renderer/eslint.config.mjs' 'src/shared/eslint.config.mjs' 'src/main/prepare-dist-package.mjs' 'src/renderer/tsconfig.json' '.vscode/launch.json' 2&gt;/dev/null)" &amp;&amp; pnpm install --frozen-lockfile</automated>
  </verify>
  <acceptance_criteria>
    - D-38-17 gate 1: `git grep '^<<<<<<< ' -- <bucket-A>` returns zero output across all 9 conflict-bearing in-scope files
    - D-38-17 gate 2: `pnpm install` exits 0 (idempotent re-run)
    - D-38-17 gate 3: `pnpm install --frozen-lockfile` exits 0
    - D-38-17 gate 4: documented as automated (partial nx typecheck passed) OR noted with Phase 39+ deferral if partial typecheck fails for source reasons
    - D-38-17 gate 5: drift summary present in lockfile commit body
    - `38-06-SUMMARY.md` records pass/fail for each gate
  </acceptance_criteria>
  <done>All five D-38-17 gates asserted and recorded. Phase 38 complete; Plan 38-07 may push to remote.</done>
</task>

</tasks>

<verification>
- All 9 conflict-bearing Bucket A files have zero conflict markers (8 hand-resolved + 1 lockfile)
- `pnpm install` exits 0 (D-38-17 gate 2 / SYNC-38a)
- `pnpm install --frozen-lockfile` exits 0 (D-38-17 gate 3)
- Lockfile commit body contains drift summary with the 4 required observations (D-38-17 gate 5)
- Partial nx typecheck on `@vortex/shared` result recorded in SUMMARY (D-38-17 gate 4 proxy)
</verification>

<success_criteria>
Phase 38 done-gate (D-38-17, all 5 items) fully satisfied. Lockfile consistent with package.json + pnpm-workspace.yaml. Atomic lockfile commit recorded with drift summary in body. Phase 38 ready for Plan 38-07 push.
</success_criteria>

<rollback>
Task 3 (regen) failure: investigate the underlying package.json / pnpm-workspace.yaml issue (likely amend a prior plan's commit with `git commit --amend` since this is local-only), then re-run.
Task 4 (commit/frozen-lockfile) failure: if commit landed but `--frozen-lockfile` fails, the lockfile drifted from inputs on second pass. `git reset --soft HEAD^` to drop, investigate (likely non-deterministic upstream registry lookup), re-run `pnpm install`, re-commit.
Task 5 gate failure: if Gate 1-3 fail, Phase 38 is not done — return to relevant plan. If only Gate 4 fails for source reasons (Phases 39-41), document and proceed (per ROADMAP fallback).
</rollback>

<output>
Create `.planning/phases/38-config-bucket-v2-0-2/38-06-SUMMARY.md` recording: lockfile commit SHA; drift summary highlights (size delta, `native-errors` add confirmed, `nexus-api` SHA bump confirmed, transitive-drift acknowledgment); pnpm install + frozen-lockfile results; partial nx typecheck result; explicit pass/fail for each of the 5 D-38-17 gates.
</output>
