---
phase: 38-config-bucket-v2-0-2
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
    - pnpm-workspace.yaml
autonomous: true
requirements: [SYNC-38a]
must_haves:
    truths:
        - "pnpm-workspace.yaml contains no conflict markers"
        - "pnpm-workspace.yaml `allowBuilds` block contains `@electron/rebuild: true` (D-38-07 sanity, A3 verification)"
        - "pnpm-workspace.yaml `catalog:` block adds upstream's `native-errors` git+SHA entry"
        - "pnpm-workspace.yaml `catalog:` block bumps `nexus-api` git+SHA from `4dd3460c2d` to `2d92fd2bdc`"
        - "pnpm-workspace.yaml `catalogMode: strict` is preserved"
        - "pnpm-workspace.yaml `nx: ^22.7.1` catalog entry is preserved (D-38-14)"
        - "pnpm-workspace.yaml `leveldown` + `levelup` catalog entries preserved (auto-merged clean per RESEARCH; not in the conflict region this phase)"
        - "pnpm-workspace.yaml file parses as valid YAML"
        - "Atomic commit per D-38-03 cadence"
    artifacts:
        - path: "pnpm-workspace.yaml"
          provides: "pnpm workspace + catalog + allowBuilds — take-upstream on the single conflict region (catalog `nexus-api` SHA bump + `native-errors` add); rest auto-merged clean"
    key_links:
        - from: "pnpm-workspace.yaml allowBuilds"
          to: "auto-merged-clean state (no conflict, A3 verification only)"
          via: "RESEARCH-confirmed @electron/rebuild: true preserved by auto-merge"
          pattern: "yaml allowBuilds block contains '@electron/rebuild' key with value true"
        - from: "pnpm-workspace.yaml catalog nexus-api entry"
          to: "node-nexus-api SHA 2d92fd2bdc"
          via: "take upstream side on the conflict region (real upstream movement, additive native-errors entry)"
          pattern: "catalog yaml contains exactly one 'nexus-api' key with #2d92fd2bdc SHA"
---

<objective>
Resolve `pnpm-workspace.yaml` per RESEARCH §Per-File Analysis: take **upstream side** on the single conflict region (catalog additions — `native-errors` line + `nexus-api` SHA bump from `4dd3460c2d` → `2d92fd2bdc`). The first line of the catalog (`@nexusmods/nexus-api`) ALSO bumps to the new SHA but auto-merged cleanly outside the conflict region; both `nexus-api`-shaped entries should reference the new SHA after this resolution.

Sanity check (RESEARCH A3): the `allowBuilds` block has NO conflict in v2.0.2 (auto-merged clean — `@electron/rebuild: true` survived). Confirm before committing — this is not a resolution decision, it's a verification of the auto-merge result.

Note: D-38-07 says "keep HEAD on allowBuilds" but allowBuilds isn't conflicted in v2.0.2. The ONE conflict region is purely a catalog additive on the upstream side. The resolution stance for this region is take-upstream (consistent with v8.1 D-31-07 catalog-additions stance — 31-06 regions 2/3/5 all took upstream).

Output: Single file conflict-free, single atomic commit matching D-38-03 title format. v8.1 31-06's "keep HEAD" stance does NOT apply here — v8.2 has different conflict shape.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/38-config-bucket-v2-0-2/38-CONTEXT.md
@.planning/phases/38-config-bucket-v2-0-2/38-RESEARCH.md
@.planning/phases/38-config-bucket-v2-0-2/PATTERNS.md
@.planning/phases/31-config-bucket/31-06-PLAN.md
</context>

<interfaces>
<!-- The single conflict region (from RESEARCH §Per-File Analysis): -->

```
<<<<<<< HEAD
  nexus-api: git+https://github.com/Nexus-Mods/node-nexus-api#4dd3460c2d02d93ba8f1bbeeeb2c5fa9af039a67
=======
  native-errors: git+https://github.com/Nexus-Mods/node-native-errors#51913db07e4c9b68a96ba7fcf741b32796758f18
  nexus-api: git+https://github.com/Nexus-Mods/node-nexus-api#2d92fd2bdc4aa6b9813a1e7043d412e13f4aa1d7
>>>>>>> v2.0.2
```

Stance: take **upstream side** (additive `native-errors` + `nexus-api` SHA bump).

Mechanic note: this is a hand-edit, not a `git checkout fork/master --` (which would lose the upstream additions and leave us with the old SHA). Pull the marker-bearing file from `fork/sync/upstream-v2.0.2` and hand-edit via Edit tool to keep upstream's two lines and drop the `<<<<<<< HEAD` / `=======` / `>>>>>>>` markers.

Auto-merged-clean items to verify (RESEARCH A3 sanity — these are NOT decisions, they're checks):

- `allowBuilds:` block contains `@electron/rebuild: true` (D-38-07 default already preserved)
- `catalog:` block contains `nx: ^22.7.1` (D-38-14)
- `catalog:` block contains `leveldown` and `levelup` (CLAUDE.md tech stack)
- `catalogMode: strict` retained
- The OTHER `@nexusmods/nexus-api` catalog entry already bumped to `2d92fd2bdc` outside the conflict region
  </interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Resolve pnpm-workspace.yaml — take upstream on catalog conflict region; verify A3 sanity items</name>
  <files>pnpm-workspace.yaml</files>
  <read_first>
    - .planning/phases/38-config-bucket-v2-0-2/38-RESEARCH.md (Per-File Analysis: pnpm-workspace.yaml — single region, take upstream; A3 sanity items: allowBuilds @electron/rebuild + catalog nx/leveldown/levelup auto-merged clean)
    - .planning/phases/38-config-bucket-v2-0-2/38-CONTEXT.md (D-38-07: keep HEAD on allowBuilds — but allowBuilds isn't conflicted in v8.2, so this is just an A3 sanity check; D-38-14: nx orchestration; D-38-15: drop new upstream scripts — moot here, that's a package.json concern, no conflict in v8.2)
  </read_first>
  <action>
    Targeted hand-resolution of `pnpm-workspace.yaml`. One real decision (take upstream on the catalog region) plus four sanity checks against the auto-merged-clean content.

    Steps:

    1. Pull the marker-bearing file from the sync branch:
       ```
       git checkout fork/sync/upstream-v2.0.2 -- pnpm-workspace.yaml
       grep -c '^<<<<<<< ' pnpm-workspace.yaml
       ```
       Expected: `1`.

    2. Hand-edit via Edit tool. Replace the conflict block:
       ```
       <<<<<<< HEAD
         nexus-api: git+https://github.com/Nexus-Mods/node-nexus-api#4dd3460c2d02d93ba8f1bbeeeb2c5fa9af039a67
       =======
         native-errors: git+https://github.com/Nexus-Mods/node-native-errors#51913db07e4c9b68a96ba7fcf741b32796758f18
         nexus-api: git+https://github.com/Nexus-Mods/node-nexus-api#2d92fd2bdc4aa6b9813a1e7043d412e13f4aa1d7
       >>>>>>> v2.0.2
       ```
       with the upstream content only:
       ```
         native-errors: git+https://github.com/Nexus-Mods/node-native-errors#51913db07e4c9b68a96ba7fcf741b32796758f18
         nexus-api: git+https://github.com/Nexus-Mods/node-nexus-api#2d92fd2bdc4aa6b9813a1e7043d412e13f4aa1d7
       ```
       Preserve original indentation (2 spaces — pnpm-workspace.yaml uses 2-space indentation throughout).

    3. Verify zero conflict markers and YAML parses:
       - `git grep '^<<<<<<< ' -- pnpm-workspace.yaml` returns no output (exit 1).
       - YAML parses (preferred python3): `python3 -c "import yaml; yaml.safe_load(open('pnpm-workspace.yaml'))"` exits 0. If python3-yaml unavailable, the lockfile regen in Plan 38-06 will reject malformed YAML — record the missing tool in SUMMARY but rely on 38-06 as backstop.

    4. **A3 sanity checks** (auto-merged-clean items the resolution must preserve):
       - `grep -E '^\s*"@electron/rebuild":\s*true' pnpm-workspace.yaml || grep -E "'@electron/rebuild':\s*true" pnpm-workspace.yaml` — at least one match (D-38-07).
       - `grep -E '^\s*nx:\s*\^?22\.' pnpm-workspace.yaml` — at least one match (D-38-14).
       - `grep -F 'leveldown' pnpm-workspace.yaml && grep -F 'levelup' pnpm-workspace.yaml` — both at least one match.
       - `grep -E 'catalogMode:\s*strict' pnpm-workspace.yaml` — at least one match.

       If any A3 sanity check fails: STOP, do not commit. The auto-merge dropped something it shouldn't have, which is a bigger problem than a single conflict region — investigate and report.

    5. **Resolution result checks:**
       - `grep -F 'native-errors' pnpm-workspace.yaml` — at least 1 match (upstream addition landed).
       - `grep -c 'nexus-api' pnpm-workspace.yaml` — at least 2 (both `@nexusmods/nexus-api` and `nexus-api` entries present).
       - `grep -c '4dd3460c2d' pnpm-workspace.yaml` — exactly 0 (old SHA fully removed; if any reference to the old SHA remains, the auto-merge state on the OTHER `@nexusmods/nexus-api` line was wrong — STOP).
       - `grep -c '2d92fd2bdc' pnpm-workspace.yaml` — at least 2 (both entries reference the new SHA).

    6. Stage and commit. R5: hooks run normally — no `--no-verify` needed (RESEARCH R5: hook risk low for v8.2 because no companion package.json conflict).
       ```
       git add pnpm-workspace.yaml
       git commit -m "resolve(config): pnpm-workspace.yaml — take upstream on catalog (native-errors + nexus-api SHA bump)" \
         -m "Phase 38 / D-38-03 / D-38-07. Single conflict region in the catalog: take upstream side (additive \`native-errors\` git+SHA + \`nexus-api\` SHA bump 4dd3460c2d→2d92fd2bdc). The other \`@nexusmods/nexus-api\` catalog entry also bumped to 2d92fd2bdc auto-merged clean outside the conflict region; both entries now reference the new SHA. allowBuilds block (\`@electron/rebuild: true\` per D-38-07) auto-merged clean — A3 sanity verified. catalogMode: strict + nx: ^22.7.1 (D-38-14) + leveldown/levelup also preserved by auto-merge."
       ```

  </action>
  <verify>
    <automated>git grep '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- pnpm-workspace.yaml; test $? -eq 1 &amp;&amp; grep -qF 'native-errors' pnpm-workspace.yaml &amp;&amp; test "$(grep -c '4dd3460c2d' pnpm-workspace.yaml)" = "0" &amp;&amp; test "$(grep -c '2d92fd2bdc' pnpm-workspace.yaml)" -ge "2" &amp;&amp; grep -qE 'catalogMode:\s*strict' pnpm-workspace.yaml &amp;&amp; grep -qF 'leveldown' pnpm-workspace.yaml &amp;&amp; grep -qF 'levelup' pnpm-workspace.yaml &amp;&amp; grep -qE 'nx:\s*\^?22\.' pnpm-workspace.yaml &amp;&amp; git log -1 --pretty=format:'%s' -- pnpm-workspace.yaml | grep -q '^resolve(config): pnpm-workspace.yaml'</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' -- pnpm-workspace.yaml` returns no output (exit 1)
    - `grep -F 'native-errors' pnpm-workspace.yaml` returns at least one match (upstream addition)
    - `grep -c '4dd3460c2d' pnpm-workspace.yaml` returns 0 (old `nexus-api` SHA fully removed)
    - `grep -c '2d92fd2bdc' pnpm-workspace.yaml` returns at least 2 (both `nexus-api` entries on new SHA)
    - A3 sanity: `@electron/rebuild: true`, `nx: ^22.7.1`, `leveldown`, `levelup`, `catalogMode: strict` all present
    - YAML parses with python3-yaml (or backstop documented if unavailable)
    - Commit titled `resolve(config): pnpm-workspace.yaml — take upstream on catalog (native-errors + nexus-api SHA bump)`
    - Commit body documents D-38-03 + D-38-07 + A3 sanity result
  </acceptance_criteria>
  <done>pnpm-workspace.yaml is conflict-free, retains all 5 auto-merged-clean items, incorporates upstream catalog additions, and is committed atomically.</done>
</task>

</tasks>

<verification>
- `git grep '^<<<<<<< ' -- pnpm-workspace.yaml` returns no output
- All A3 sanity items present: `@electron/rebuild: true`, `catalogMode: strict`, `leveldown`, `levelup`, `nx: ^22.7.1`
- Catalog state: `native-errors` present, `nexus-api` references new SHA `2d92fd2bdc` (2+ occurrences), old SHA `4dd3460c2d` absent (0 occurrences)
- One atomic commit on `v8.2/sync-upstream-v2.0.2` with D-38-03 title format
</verification>

<success_criteria>
pnpm-workspace.yaml resolved take-upstream on the single catalog region per RESEARCH stance. allowBuilds + catalogMode strict + nx + leveldown/levelup all preserved by auto-merge (A3 sanity verified). Catalog additions from upstream merged: `native-errors` + `nexus-api` SHA bump.
</success_criteria>

<rollback>
If A3 sanity fails: STOP, do not commit. The auto-merge dropped something material — escalate to plan-phase replan. Don't try to "fix" the auto-merge by resurrecting items — that would mask whatever else might be missing.
If verification fails post-commit (markers remain or YAML invalid): `git reset --soft HEAD^` drops the commit. Hand-edit to correct, then re-commit. If hopelessly tangled, `git checkout fork/sync/upstream-v2.0.2 -- pnpm-workspace.yaml` and start over from markers.
</rollback>

<output>
Create `.planning/phases/38-config-bucket-v2-0-2/38-05-SUMMARY.md` recording: commit SHA, A3 sanity-check results (all 5 items confirmed), the catalog state delta (native-errors added, nexus-api SHA bumped to 2d92fd2bdc on both occurrences, old SHA fully absent), R5 footnote (no `--no-verify` used).
</output>
