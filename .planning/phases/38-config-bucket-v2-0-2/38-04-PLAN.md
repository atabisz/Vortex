---
phase: 38-config-bucket-v2-0-2
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
    - src/main/prepare-dist-package.mjs
autonomous: true
requirements: [SYNC-38a]
must_haves:
    truths:
        - "src/main/prepare-dist-package.mjs contains no conflict markers"
        - "Fork's `packagesSection` block emission for workspace:* resolution survives intact (D-38-12)"
        - "File parses via `node --check`"
        - "Atomic commit per D-38-03 cadence"
    artifacts:
        - path: "src/main/prepare-dist-package.mjs"
          provides: "Dist package preparation script (keep HEAD per D-38-12 — fork's packagesSection workspace:* resolution preserved)"
    key_links:
        - from: "src/main/prepare-dist-package.mjs"
          to: "fork's dist build flow (workspace:* deps in packaged tree)"
          via: "keep HEAD's packagesSection block (15 fork-side lines)"
          pattern: "post-resolve file contains `packagesSection` variable assignment + `neededWorkspaceDirs` reference"
---

<objective>
Resolve `src/main/prepare-dist-package.mjs` per D-38-12 by keeping HEAD wholesale on the single conflict region. The HEAD side preserves fork's `packagesSection` block — 15 lines emitting a `packages:` YAML section so pnpm can resolve `workspace:*` references in `file:` deps in the dist context. Upstream's vanilla one-liner lacks this addition.

Per RESEARCH §Per-File Analysis: v2.0.1→v2.0.2 was pure whitespace/oxfmt formatting churn for this file (14 ins / 101 dels, formatting-only). Fork's HEAD divergence is 101 lines beyond v2.0.2 baseline; the conflict region is just one of several fork-side additions, but it's the only one with a marker.

Output: Single file conflict-free, single atomic commit matching D-38-03 title format.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/38-config-bucket-v2-0-2/38-CONTEXT.md
@.planning/phases/38-config-bucket-v2-0-2/38-RESEARCH.md
@.planning/phases/38-config-bucket-v2-0-2/PATTERNS.md
@.planning/phases/31-config-bucket/31-04-PLAN.md
</context>

<interfaces>
<!-- Conflict region (single, from RESEARCH §Per-File Analysis): -->

```
<<<<<<< HEAD
  // Emit a packages: section so pnpm can resolve workspace:* refs in file: deps.
  let packagesSection = "";
  if (neededWorkspaceDirs && neededWorkspaceDirs.size > 0) {
    const lines = [...neededWorkspaceDirs]
      .map((absPath) => `  - ${relative(DIST_DIR, absPath)}`)
      .join("\n");
    packagesSection = `packages:\n${lines}\n\n`;
  }
  const minimalYaml =
    packagesSection + (overrides ? overrides + "\n" : "") + catalog + "\n" + allowBuilds + "\n";
=======
  const minimalYaml = (overrides ? overrides + "\n" : "") + catalog + "\n" + allowBuilds + "\n";
>>>>>>> v2.0.2
```

Stance: keep HEAD (D-38-12). The mechanic — same as Plan 38-03 — pick-HEAD-wholesale on a file means `git checkout fork/master -- <file>` retrieves the resolved keep-HEAD shape directly, bypassing the marker-bearing version on `fork/sync/upstream-v2.0.2`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Resolve src/main/prepare-dist-package.mjs — keep HEAD packagesSection block (D-38-12)</name>
  <files>src/main/prepare-dist-package.mjs</files>
  <read_first>
    - .planning/phases/38-config-bucket-v2-0-2/38-RESEARCH.md (Per-File Analysis: src/main/prepare-dist-package.mjs — single region, fork's packagesSection block emission for workspace:* resolution; v2.0.1→v2.0.2 delta is pure whitespace/oxfmt churn)
    - .planning/phases/38-config-bucket-v2-0-2/38-CONTEXT.md (D-38-12: tsconfig*.json + prepare-dist-package.mjs — hand-resolve; preserve fork's vortex-api .d.ts generation flow)
  </read_first>
  <action>
    Per D-38-12: keep HEAD wholesale on the single conflict region in `src/main/prepare-dist-package.mjs`. Fork's `packagesSection` block is necessary for the dist build flow (pnpm needs to resolve `workspace:*` deps when packaging).

    Steps:

    1. Pick HEAD wholesale via fork/master content (the resolved keep-HEAD shape). v2.0.1→v2.0.2 was formatting churn only — fork/master's content already includes the `packagesSection` block and the fork-side oxfmt formatting:
       ```
       git checkout fork/master -- src/main/prepare-dist-package.mjs
       ```

    2. Verify:
       - `git grep '^<<<<<<< ' -- src/main/prepare-dist-package.mjs` returns no output (exit 1).
       - `node --check src/main/prepare-dist-package.mjs` exits 0.
       - Fork's `packagesSection` block survived: `grep -F 'packagesSection' src/main/prepare-dist-package.mjs` returns at least 1 match.
       - `neededWorkspaceDirs` reference present: `grep -F 'neededWorkspaceDirs' src/main/prepare-dist-package.mjs` returns at least 1 match.

    3. Stage and commit:
       ```
       git add src/main/prepare-dist-package.mjs
       git commit -m "resolve(config): src/main/prepare-dist-package.mjs — keep HEAD packagesSection block per D-38-12" \
         -m "Phase 38 / D-38-03 / D-38-12. Single conflict region: HEAD emits a 15-line \`packagesSection\` block (a YAML \`packages:\` section listing workspace dirs, so pnpm can resolve \`workspace:*\` refs in \`file:\` deps within the dist context). Upstream's vanilla one-liner lacks this. Fork's addition is necessary for the dist build flow. v2.0.1→v2.0.2 was pure formatting churn (14 ins / 101 dels, whitespace-only); fork's HEAD divergence is 101 lines beyond v2.0.2 baseline (oxfmt + the packagesSection addition)."
       ```
       Hooks run normally — no `--no-verify` (RESEARCH R5).

  </action>
  <verify>
    <automated>git grep '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- src/main/prepare-dist-package.mjs; test $? -eq 1 &amp;&amp; node --check src/main/prepare-dist-package.mjs &amp;&amp; grep -qF 'packagesSection' src/main/prepare-dist-package.mjs &amp;&amp; grep -qF 'neededWorkspaceDirs' src/main/prepare-dist-package.mjs &amp;&amp; git log -1 --pretty=format:'%s' -- src/main/prepare-dist-package.mjs | grep -q '^resolve(config): src/main/prepare-dist-package.mjs'</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' -- src/main/prepare-dist-package.mjs` returns no output (exit 1)
    - `node --check src/main/prepare-dist-package.mjs` exits 0
    - `grep -F 'packagesSection' src/main/prepare-dist-package.mjs` returns at least 1 match
    - `grep -F 'neededWorkspaceDirs' src/main/prepare-dist-package.mjs` returns at least 1 match
    - Commit titled `resolve(config): src/main/prepare-dist-package.mjs — keep HEAD packagesSection block per D-38-12`
    - Commit body explains v2.0.1→v2.0.2 was formatting-only churn
  </acceptance_criteria>
  <done>src/main/prepare-dist-package.mjs conflict-free, packagesSection block intact, atomic commit landed.</done>
</task>

</tasks>

<verification>
- `git grep '^<<<<<<< ' -- src/main/prepare-dist-package.mjs` returns no output
- One atomic commit on `v8.2/sync-upstream-v2.0.2` with D-38-03 title format
- `node --check` passes
- `packagesSection` and `neededWorkspaceDirs` both present in resolved file
</verification>

<success_criteria>
src/main/prepare-dist-package.mjs resolved keep-HEAD per D-38-12. Fork's workspace:\* resolution machinery preserved. Atomic commit landed without `--no-verify`.
</success_criteria>

<rollback>
If verification fails: `git reset --soft HEAD^` drops the commit and leaves the file in working tree for re-attempt. If `node --check` fails on master's version (unexpected — would indicate master is broken), STOP and report — Phase 38 cannot proceed without a syntactically valid prepare-dist-package.mjs.
</rollback>

<output>
Create `.planning/phases/38-config-bucket-v2-0-2/38-04-SUMMARY.md` recording: commit SHA, confirmation that `packagesSection` block + `neededWorkspaceDirs` reference are present in the resolved file, R5 footnote (no `--no-verify` used), note that v2.0.1→v2.0.2 was formatting-only churn.
</output>
