---
phase: 38-config-bucket-v2-0-2
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
    - src/main/eslint.config.mjs
    - src/preload/eslint.config.mjs
    - src/renderer/eslint.config.mjs
    - src/shared/eslint.config.mjs
autonomous: true
requirements: [SYNC-38a]
must_haves:
    truths:
        - "All four eslint.config.mjs files contain no conflict markers"
        - "Each file's HEAD-side delegation to eslint.config.base.mjs survives intact"
        - "Renderer's HEAD-only sections outside the conflict regions (vortex/no-bluebird-*, react-x, better-tailwindcss, sort-jsx-props, @eslint-react) survive untouched"
        - "Each file passes `node --check`"
        - "Four atomic commits per D-38-03 cadence — one per file with the same revised D-38-10 title format"
    artifacts:
        - path: "src/main/eslint.config.mjs"
          provides: "Main process ESLint config (pick HEAD per revised D-38-10 — upstream side duplicates eslint.config.base.mjs)"
        - path: "src/preload/eslint.config.mjs"
          provides: "Preload process ESLint config (pick HEAD per revised D-38-10)"
        - path: "src/renderer/eslint.config.mjs"
          provides: "Renderer process ESLint config (pick HEAD per revised D-38-10)"
        - path: "src/shared/eslint.config.mjs"
          provides: "Shared types ESLint config (pick HEAD per revised D-38-10)"
    key_links:
        - from: "all four eslint.config.mjs files"
          to: "eslint.config.base.mjs (already merged-in upstream-clean by v2.0.2)"
          via: "pick HEAD wholesale per revised D-38-10 (RESEARCH verified base file already contains every rule the upstream conflict-side proposes — v2.0.1→v2.0.2 only reduced inline rules further)"
          pattern: "post-resolve files contain `defineConfig([...baseConfig(import.meta.dirname)` or equivalent base-extends pattern"
---

<objective>
Resolve the four `eslint.config.mjs` workspace-tier configs (main + preload + renderer + shared) by picking HEAD wholesale on every conflict region. RESEARCH verified the upstream conflict-side adds in v2.0.2 are 100% duplicates of `eslint.config.base.mjs` content — the base extraction is upstream-clean. v2.0.1→v2.0.2 only reduced inline rules further (more base extraction).

Stance per RESEARCH §Per-File Analysis: pick HEAD wholesale on all four files. Renderer's significant HEAD-only tail content (`vortex/no-bluebird-*`, `react-x`, `better-tailwindcss`, `sort-jsx-props`, `@eslint-react`, stylistic plugin) sits outside the conflict regions and survives automatically.

Output: Four files conflict-free. Four atomic commits matching D-38-03 title format. Each commit body documents that upstream-side adds are duplicates of `eslint.config.base.mjs` and references RESEARCH's verification.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/38-config-bucket-v2-0-2/38-CONTEXT.md
@.planning/phases/38-config-bucket-v2-0-2/38-RESEARCH.md
@.planning/phases/38-config-bucket-v2-0-2/PATTERNS.md
@.planning/phases/31-config-bucket/31-03-PLAN.md
</context>

<interfaces>
<!-- Conflict-marker mechanic: markers are committed-in from PR #6's auto-merge, no live merge
     state. `git checkout --ours` does NOT work. Pull the marker-bearing file from
     fork/sync/upstream-v2.0.2, then hand-edit (or for whole-file pick-HEAD: `git checkout
     fork/master -- <file>` directly bypasses the marker-bearing version since HEAD on master
     IS the resolved keep-HEAD shape we want). The simpler mechanic for THIS plan: use
     `git checkout fork/master -- <file>` since pick-HEAD wholesale means we want fork/master's
     content verbatim. Document the mechanic in each commit body. -->

From eslint.config.base.mjs on `fork/sync/upstream-v2.0.2` (already merged in upstream-clean):

- exports `baseConfig(importMetaDirname)` function
- supplies: parserOptions.projectService, parserOptions.tsconfigRootDir, plugins.perfectionist, recommended/recommendedTypeChecked, sort-imports/sort-exports, consistent-type-imports error, no-unused-vars argsIgnorePattern: "^\_", separate `.mjs` glob block

HEAD-side post-resolve shape per file:

- src/preload/eslint.config.mjs: single delegation line `export default defineConfig([...baseConfig(import.meta.dirname)]);`
- src/main/eslint.config.mjs: HEAD's conflict regions are empty (file extends base; nothing to take from HEAD inside the conflicts)
- src/renderer/eslint.config.mjs: conflict regions only span imports + parserOptions block; renderer's significant tail (vortex/no-bluebird-\*, react-x, better-tailwindcss, sort-jsx-props, @eslint-react, stylistic plugin) is HEAD-side OUTSIDE the conflicts and survives automatically
- src/shared/eslint.config.mjs: HEAD has `files: ["src/**/*.ts"]` followed by `rules:` block delegating to base via existing extends
  </interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Pick HEAD on src/preload/eslint.config.mjs (revised D-38-10)</name>
  <files>src/preload/eslint.config.mjs</files>
  <read_first>
    - .planning/phases/38-config-bucket-v2-0-2/38-RESEARCH.md (Per-File Analysis: src/preload/eslint.config.mjs — single large region; HEAD is one-line baseConfig delegation; upstream side is ~50 lines of inline rules already in base)
    - eslint.config.base.mjs (read to confirm base contains the rules upstream proposes)
  </read_first>
  <action>
    Per revised D-38-10: pick HEAD wholesale on src/preload/eslint.config.mjs.

    Steps:

    1. Sanity-check base coverage. Read `eslint.config.base.mjs` on the working tree (which equals `fork/master`'s content here — it has no conflict). Confirm the file contains: `recommended`, `recommendedTypeChecked`, `perfectionist`, `consistent-type-imports`, `no-unused-vars`, separate `.mjs` glob block.
       ```
       grep -E '(recommendedTypeChecked|perfectionist|consistent-type-imports|"\*\.mjs")' eslint.config.base.mjs
       ```
       Expected: at least 4 matches across those terms. If any required rule is missing on the merge tree, the pick-HEAD-wholesale stance breaks down and a hand-merge is required for that one rule — note in commit body and add the missing rule explicitly.

    2. Pick HEAD wholesale by checking out fork/master's version of the file (which is the resolved keep-HEAD shape):
       ```
       git checkout fork/master -- src/preload/eslint.config.mjs
       ```
       (Mechanic note: `fork/master`'s content of this file is exactly what HEAD-side resolution produces. The marker-bearing version on `fork/sync/upstream-v2.0.2` includes upstream's duplicate adds; we're discarding those by using master's content directly.)

    3. Verify zero conflict markers and syntactic validity:
       - `git grep '^<<<<<<< ' -- src/preload/eslint.config.mjs` returns no output (exit 1).
       - `node --check src/preload/eslint.config.mjs` exits 0.
       - Base-extension pattern survived: `grep -E 'baseConfig\(import\.meta\.dirname\)' src/preload/eslint.config.mjs` returns at least 1 match.

    4. Stage and commit:
       ```
       git add src/preload/eslint.config.mjs
       git commit -m "resolve(config): src/preload/eslint.config.mjs — pick HEAD per D-38-10 (upstream-side duplicates eslint.config.base.mjs)" \
         -m "Phase 38 / D-38-03 / D-38-10. HEAD is the post-base-extraction shape (one-line baseConfig delegation). Upstream conflict-side adds parserOptions.projectService, parserOptions.tsconfigRootDir, plugins.perfectionist, recommended, recommendedTypeChecked, sort-imports/sort-exports, consistent-type-imports error, no-unused-vars custom, separate \`*.mjs\` glob block — all already present in eslint.config.base.mjs (verified). v2.0.1→v2.0.2 only reduced upstream-side inline rules further; stance unchanged."
       ```
       Hooks run normally — no `--no-verify`.

  </action>
  <verify>
    <automated>git grep '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- src/preload/eslint.config.mjs; test $? -eq 1 &amp;&amp; node --check src/preload/eslint.config.mjs &amp;&amp; grep -qE 'baseConfig\(import\.meta\.dirname\)' src/preload/eslint.config.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' -- src/preload/eslint.config.mjs` returns no output (exit 1)
    - `node --check src/preload/eslint.config.mjs` exits 0
    - `grep -E 'baseConfig\(import\.meta\.dirname\)' src/preload/eslint.config.mjs` returns at least 1 match
    - Commit titled `resolve(config): src/preload/eslint.config.mjs — pick HEAD per D-38-10 (upstream-side duplicates eslint.config.base.mjs)`
  </acceptance_criteria>
  <done>src/preload/eslint.config.mjs conflict-free, base-delegation pattern intact, atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 2: Pick HEAD on src/main/eslint.config.mjs (revised D-38-10)</name>
  <files>src/main/eslint.config.mjs</files>
  <read_first>
    - .planning/phases/38-config-bucket-v2-0-2/38-RESEARCH.md (Per-File Analysis: src/main/eslint.config.mjs — 2 regions; HEAD empty; upstream adds extends + recommendedTypeChecked + prettierConfig and *.mjs block, all already in base)
  </read_first>
  <action>
    Per revised D-38-10: pick HEAD wholesale on src/main/eslint.config.mjs.

    Same protocol as Task 1:

    1. Sanity-check base coverage (already done in Task 1; one-time check is sufficient — but if `eslint.config.base.mjs` was re-read, cite the result).

    2. `git checkout fork/master -- src/main/eslint.config.mjs`

    3. Verify:
       - `git grep '^<<<<<<< ' -- src/main/eslint.config.mjs` returns no output (exit 1).
       - `node --check src/main/eslint.config.mjs` exits 0.

    4. Stage and commit:
       ```
       git add src/main/eslint.config.mjs
       git commit -m "resolve(config): src/main/eslint.config.mjs — pick HEAD per D-38-10 (upstream-side duplicates eslint.config.base.mjs)" \
         -m "Phase 38 / D-38-03 / D-38-10. Two regions: HEAD empty in both, upstream adds extends array and *.mjs block — both already in eslint.config.base.mjs. v2.0.1→v2.0.2 reduced inline rules by 12 lines (further base extraction); stance unchanged."
       ```

  </action>
  <verify>
    <automated>git grep '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- src/main/eslint.config.mjs; test $? -eq 1 &amp;&amp; node --check src/main/eslint.config.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' -- src/main/eslint.config.mjs` returns no output (exit 1)
    - `node --check src/main/eslint.config.mjs` exits 0
    - Commit titled `resolve(config): src/main/eslint.config.mjs — pick HEAD per D-38-10 (upstream-side duplicates eslint.config.base.mjs)`
  </acceptance_criteria>
  <done>src/main/eslint.config.mjs conflict-free, atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 3: Pick HEAD on src/renderer/eslint.config.mjs (revised D-38-10) — verify HEAD-only tail survives</name>
  <files>src/renderer/eslint.config.mjs</files>
  <read_first>
    - .planning/phases/38-config-bucket-v2-0-2/38-RESEARCH.md (Per-File Analysis: src/renderer/eslint.config.mjs — 2 regions; renderer's significant rule content vortex/no-bluebird-*, react-x, better-tailwindcss, perfectionist sort-jsx-props, stylistic plugin is HEAD-side outside the conflicts and survives automatically)
  </read_first>
  <action>
    Per revised D-38-10: pick HEAD wholesale on src/renderer/eslint.config.mjs. Critical assertion: the renderer file has the most HEAD-side content OUTSIDE the conflict regions; verify those sections are intact post-resolution.

    Same protocol as Task 1:

    1. `git checkout fork/master -- src/renderer/eslint.config.mjs`

    2. Verify:
       - `git grep '^<<<<<<< ' -- src/renderer/eslint.config.mjs` returns no output (exit 1).
       - `node --check src/renderer/eslint.config.mjs` exits 0.
       - HEAD-only tail content survived (sanity grep — the master version IS the resolved shape, so these grep checks confirm we got master's content, not the marker-bearing sync-branch content):
         - `grep -F 'no-bluebird' src/renderer/eslint.config.mjs` returns at least 1 match.
         - `grep -F 'better-tailwindcss' src/renderer/eslint.config.mjs` returns at least 1 match.
         - `grep -F 'react-x' src/renderer/eslint.config.mjs || grep -F '@eslint-react' src/renderer/eslint.config.mjs` returns at least 1 match (renderer-specific React rules).

    3. Stage and commit:
       ```
       git add src/renderer/eslint.config.mjs
       git commit -m "resolve(config): src/renderer/eslint.config.mjs — pick HEAD per D-38-10 (upstream-side duplicates eslint.config.base.mjs)" \
         -m "Phase 38 / D-38-03 / D-38-10. Two regions: imports list (upstream adds eslint-config-prettier + perfectionist imports already in base) and config block (upstream adds *.mjs block already in base). Renderer-specific rules tail (vortex/no-bluebird-*, react-x, better-tailwindcss, sort-jsx-props, @eslint-react, stylistic plugin) sits HEAD-side outside the conflict regions and survives automatically."
       ```

  </action>
  <verify>
    <automated>git grep '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- src/renderer/eslint.config.mjs; test $? -eq 1 &amp;&amp; node --check src/renderer/eslint.config.mjs &amp;&amp; grep -qF 'no-bluebird' src/renderer/eslint.config.mjs &amp;&amp; grep -qF 'better-tailwindcss' src/renderer/eslint.config.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' -- src/renderer/eslint.config.mjs` returns no output (exit 1)
    - `node --check src/renderer/eslint.config.mjs` exits 0
    - HEAD-only tail content present: `no-bluebird` and `better-tailwindcss` both grep-hit
    - Commit titled `resolve(config): src/renderer/eslint.config.mjs — pick HEAD per D-38-10 (upstream-side duplicates eslint.config.base.mjs)`
  </acceptance_criteria>
  <done>src/renderer/eslint.config.mjs conflict-free, renderer-specific rules tail intact, atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 4: Pick HEAD on src/shared/eslint.config.mjs (revised D-38-10)</name>
  <files>src/shared/eslint.config.mjs</files>
  <read_first>
    - .planning/phases/38-config-bucket-v2-0-2/38-RESEARCH.md (Per-File Analysis: src/shared/eslint.config.mjs — 2 regions; HEAD empty in both; upstream adds extends + parserOptions block and *.mjs block already in base)
  </read_first>
  <action>
    Per revised D-38-10: pick HEAD wholesale on src/shared/eslint.config.mjs.

    Same protocol as Task 1:

    1. `git checkout fork/master -- src/shared/eslint.config.mjs`

    2. Verify:
       - `git grep '^<<<<<<< ' -- src/shared/eslint.config.mjs` returns no output (exit 1).
       - `node --check src/shared/eslint.config.mjs` exits 0.

    3. Stage and commit:
       ```
       git add src/shared/eslint.config.mjs
       git commit -m "resolve(config): src/shared/eslint.config.mjs — pick HEAD per D-38-10 (upstream-side duplicates eslint.config.base.mjs)" \
         -m "Phase 38 / D-38-03 / D-38-10. Two regions: HEAD empty in both; upstream adds extends/languageOptions.parserOptions.projectService block and *.mjs block — both already in eslint.config.base.mjs."
       ```

  </action>
  <verify>
    <automated>git grep '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' -- src/shared/eslint.config.mjs; test $? -eq 1 &amp;&amp; node --check src/shared/eslint.config.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' -- src/shared/eslint.config.mjs` returns no output (exit 1)
    - `node --check src/shared/eslint.config.mjs` exits 0
    - Commit titled `resolve(config): src/shared/eslint.config.mjs — pick HEAD per D-38-10 (upstream-side duplicates eslint.config.base.mjs)`
  </acceptance_criteria>
  <done>src/shared/eslint.config.mjs conflict-free, atomic commit landed.</done>
</task>

</tasks>

<verification>
- `git grep '^<<<<<<< ' -- 'src/main/eslint.config.mjs' 'src/preload/eslint.config.mjs' 'src/renderer/eslint.config.mjs' 'src/shared/eslint.config.mjs'` returns no output
- Four atomic commits exist on `v8.2/sync-upstream-v2.0.2` (one per file) with revised D-38-10 title format
- All four files pass `node --check`
- Renderer file retains `no-bluebird` and `better-tailwindcss` rule references (HEAD-side tail survived)
</verification>

<success_criteria>
All four ESLint configs resolved via pick-HEAD per revised D-38-10. eslint.config.base.mjs delegation pattern preserved. v2.0.2's further base-extraction reduction documented in commit bodies. Renderer's HEAD-only tail intact. No `--no-verify` needed (RESEARCH R5).
</success_criteria>

<rollback>
Per-task: if verification fails for one file, `git reset --soft HEAD^` drops that commit and restores the file in working tree for re-attempt with hand-merge fallback.
Plan-level: if eslint.config.base.mjs is missing a rule the upstream side genuinely added in v2.0.2 (rare), hand-edit the affected file to add the missing rule and `git commit --amend --no-edit`.
</rollback>

<output>
Create `.planning/phases/38-config-bucket-v2-0-2/38-03-SUMMARY.md` recording: four commit SHAs, sanity-check confirmation that `eslint.config.base.mjs` covered every upstream-side rule (Task 1 step 1), confirmation that renderer's HEAD-only tail rules (no-bluebird, better-tailwindcss, react-x) survived (Task 3 grep results), R5 footnote (no `--no-verify` used).
</output>
