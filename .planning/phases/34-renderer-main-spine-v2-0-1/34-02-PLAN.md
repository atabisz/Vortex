---
phase: 34
plan: 02
type: execute
wave: B
status: ready
requires:
    - 34-00
    - 34-01
depends_on:
    - 34-00
    - 34-01
files_modified:
    - src/preload/src/index.ts
    - .planning/phases/34-renderer-main-spine-v2-0-1/34-02-SUMMARY.md
autonomous: true
requirements:
    - SYNC-34a
must_haves:
    truths:
        - "src/preload/src/index.ts marker-free; IPC bridge surface preserved per D-34-02 6-tier"
        - "1 SSH-signed `resolve(preload): src/index.ts — <stance>` commit on `v8.1/config-bucket`"
        - "Wave-B-end bucket-scoped typecheck (D-34-06): `pnpm tsc -p src/preload/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l` returns 0"
        - "Harness skip-mode (13 gates) stays GREEN after the commit"
        - "1 SUMMARY commit with region tally + bucket-typecheck count"
    artifacts:
        - path: "src/preload/src/index.ts"
          provides: "Typed IPC bridge between renderer and main process; preload context-bridge surface"
          contains: "contextBridge"
    key_links:
        - from: "src/preload/src/index.ts"
          to: "src/shared/src/types/ipc"
          via: "import"
          pattern: "from.*shared.*ipc|from.*shared.*types"
---

<objective>
Wave B. Resolve the single preload spine file `src/preload/src/index.ts`. This is the typed IPC bridge between renderer and main — depends on shared (Wave A) IPC channel types. Single-file wave; small wave by design (preload bucket has only this conflicted file).

Default stance per D-34-02: hand-resolve per region using the 6-tier stance hierarchy. Most regions expected to be Rule-1 (HEAD-empty dup-import after upstream split single-line imports) or smaller-diff. Any IPC channel name added in v2.0.1 = upstream-wins (channel set is feature-additive). Any HEAD-only Linux platform guard around an Electron IPC method = Linux-guard fork-wins.

Purpose: preload is on the critical path for the boot sequence — Application → preload → renderer needs the bridge clean before Wave C touches the main spine that constructs the BrowserWindow.

Output: 1 SSH-signed `resolve(preload): src/index.ts — <stance>` commit + 1 SUMMARY commit. Bucket-scoped typecheck = 0 non-marker errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-renderer-main-spine-v2-0-1/34-CONTEXT.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-RESEARCH.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-VALIDATION.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-00-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-01-SUMMARY.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
@CLAUDE.md
@AGENTS.md
</context>

<read_first>

- `.planning/phases/34-renderer-main-spine-v2-0-1/34-CONTEXT.md` — D-34-02 stance hierarchy, D-34-06 per-bucket typecheck cadence, D-34-08 commit body format, D-34-09 no `--no-verify`
- `.planning/phases/34-renderer-main-spine-v2-0-1/34-RESEARCH.md` — Wave-B section if any IPC-channel changes flagged
- `.planning/phases/34-renderer-main-spine-v2-0-1/34-00-SUMMARY.md` — harness state (13 gates extended, gate-13 spec)
- `.planning/phases/34-renderer-main-spine-v2-0-1/34-01-SUMMARY.md` — shared bucket already 0-error; preload imports from there
- `VORTEX-LINUX-MERGE-PLAYBOOK.md` — §3 Linux-guard fork-wins discipline
- `src/preload/src/index.ts` (the conflicted file itself before resolution)
- `src/preload/tsconfig.json` (bucket scope for D-34-06 typecheck)
  </read_first>

<tasks>

<task type="auto">
  <name>Task B1: resolve(preload): src/index.ts</name>
  <files>src/preload/src/index.ts</files>
  <action>
Resolve all conflict regions in `src/preload/src/index.ts` per D-34-02 6-tier stance hierarchy:

1. Run `git diff --check src/preload/src/index.ts` to enumerate region count for the commit body tally.
2. For each region, classify by 6-tier order: §1 playbook fork-wins → §3 Linux-guard fork-wins → v2.0.1 feature upstream-wins (new IPC channel names) → Rule-1 dup-import HEAD-empty (most likely on import block) → Rule-2 D1-carryover prevention → smaller-diff default.
3. Hand-resolve each region. Do NOT reformat outside conflict regions (per `feedback_minimize_upstream_diff.md`).
4. After resolution, verify markers gone: `git grep -nE '^(<{7}|={7}|>{7})( |$)' src/preload/src/index.ts` returns nothing.
5. Run harness in skip-mode against the file's bucket: `bash .planning/phases/34-renderer-main-spine-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check` — must exit 0 (13/13 GREEN).
6. Stage with `git add src/preload/src/index.ts` and commit with title `resolve(preload): src/index.ts — <stance>` (stance = predominant tier among regions).
7. Pattern S5 commit body MUST include: regions tally (e.g. "regions: 4 total — Rule-1 dup-import: 2, smaller-diff: 1, Linux-guard fork-wins: 1"), gate exits (`13/13 GREEN`), harness exit (`0`), typecheck status (`deferred to wave end`).
8. Verify SSH signature: `git cat-file -p HEAD | grep -c '^gpgsig '` returns >= 1.
   </action>
   <verify>
   <automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/preload/src/index.ts | wc -l</automated>
   </verify>
   <done>

- File marker-free
- Atomic SSH-signed `resolve(preload): src/index.ts — <stance>` commit on `v8.1/config-bucket`
- Pattern S5 body: regions tally + 13/13 GREEN + harness exit 0 + typecheck deferred
- `git cat-file -p HEAD | grep -c '^gpgsig '` >= 1
  </done>
  </task>

<task type="auto">
  <name>Task B2: wave-end bucket typecheck + SUMMARY</name>
  <files>.planning/phases/34-renderer-main-spine-v2-0-1/34-02-SUMMARY.md</files>
  <action>
1. Run the wave-end bucket-scoped typecheck per D-34-06: `pnpm tsc -p src/preload/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l` — MUST return `0`. If non-zero, surface the errors and STOP (do not write SUMMARY); orchestrator will route to a follow-up resolve-pass.
2. Write `.planning/phases/34-renderer-main-spine-v2-0-1/34-02-SUMMARY.md` covering:
   - Files resolved (1: src/preload/src/index.ts) with the per-file region tally
   - Stance distribution (e.g. "Rule-1: 2, smaller-diff: 1, Linux-guard fork-wins: 1")
   - Bucket-scoped typecheck output (`0 errors`)
   - Harness state (13/13 GREEN at wave end)
   - Atomic-commit list (1 resolve commit + this SUMMARY commit) with short SHAs
   - Any planner-flagged risks observed during resolution (none expected for preload)
3. Stage with `git add -f .planning/phases/34-renderer-main-spine-v2-0-1/34-02-SUMMARY.md` (`.planning/` is gitignored per project memory).
4. Commit with title `docs(phase-34): Wave B (preload) summary — 1 file, 0 typecheck errors`.
5. Verify SSH signature on the SUMMARY commit (`git cat-file -p HEAD | grep -c '^gpgsig '` >= 1).
  </action>
  <verify>
    <automated>pnpm tsc -p src/preload/tsconfig.json --noEmit 2>&amp;1 | grep -v TS1185 | wc -l</automated>
  </verify>
  <done>
- `pnpm tsc -p src/preload/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l` returns `0`
- 34-02-SUMMARY.md exists at `.planning/phases/34-renderer-main-spine-v2-0-1/34-02-SUMMARY.md`
- SUMMARY committed (force-added) with SSH-signed `docs(phase-34): Wave B (preload) summary — 1 file, 0 typecheck errors`
- Wave B closed; downstream Wave C may proceed
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                                                                         |
| ------------------------- | --------------------------------------------------------------------------------------------------- |
| renderer→preload→main IPC | Preload is the contextBridge surface; corruption here = broken IPC contract for the entire renderer |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                      | Disposition | Mitigation Plan                                                                                                                                             |
| ---------- | ---------------------- | ---------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-34-02-01 | Tampering              | src/preload/src/index.ts contextBridge surface | mitigate    | D-34-02 stance hierarchy preserves Linux-guard fork-wins; Wave-end bucket typecheck (D-34-06) catches any IPC type drift before downstream waves consume it |
| T-34-02-02 | Information Disclosure | preload exposes Electron APIs to renderer      | accept      | Phase 34 is merge-resolution only; surface composition unchanged from HEAD; security audit of contextBridge exposure scoped to Phase 999.x backlog          |
| T-34-02-SC | Tampering              | npm/pip/cargo installs                         | n/a         | No new package installs in Phase 34 — RESEARCH.md Package Legitimacy Audit confirms zero install tasks                                                      |

</threat_model>

<verification>
- L1 markers: `git grep -nE '^(<{7}|={7}|>{7})( |$)' src/preload/` returns 0 lines
- L2 harness: `bash .planning/phases/34-renderer-main-spine-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check` exits 0 (13/13 GREEN)
- L3 bucket typecheck: `pnpm tsc -p src/preload/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l` returns 0
- All commits SSH-signed: `git log --format='%H %G?' v8.1/config-bucket~2..HEAD | grep -v ' G$'` returns nothing
</verification>

<success_criteria>

- 1 atomic resolve commit + 1 SUMMARY commit on `v8.1/config-bucket`
- Bucket typecheck 0 non-marker errors
- Wave B closed; Wave C unblocked
  </success_criteria>

<output>
Write `.planning/phases/34-renderer-main-spine-v2-0-1/34-02-SUMMARY.md` capturing region tally, typecheck output, harness state, and commit SHAs.
</output>
