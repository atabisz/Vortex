---
phase: 34
plan: 05
type: execute
wave: E
status: ready
requires:
    - 34-00
    - 34-01
    - 34-02
    - 34-03
    - 34-04
depends_on:
    - 34-00
    - 34-01
    - 34-02
    - 34-03
    - 34-04
files_modified:
    - src/renderer/src/extensions/nexus_integration/eventHandlers.ts
    - src/renderer/src/extensions/nexus_integration/index.tsx
    - src/renderer/src/extensions/nexus_integration/selectors.test.ts
    - src/renderer/src/extensions/nexus_integration/types/IValidateKeyData.ts
    - src/renderer/src/extensions/nexus_integration/util.ts
    - src/renderer/src/extensions/nexus_integration/util/UIDs.ts
    - src/renderer/src/extensions/nexus_integration/util/oauth.ts
    - src/renderer/src/extensions/nexus_integration/views/FreeUserDLDialog.tsx
    - src/renderer/src/extensions/nexus_integration/views/GoPremiumDashlet.tsx
    - src/renderer/src/extensions/health_check/api/triggers.ts
    - src/renderer/src/extensions/health_check/checks/modRequirementsCheck.ts
    - src/renderer/src/extensions/health_check/components/mod_requirement/ModRequirement.tsx
    - src/renderer/src/extensions/health_check/types.ts
    - src/renderer/src/extensions/health_check/views/HealthCheckDetailPage.tsx
    - src/renderer/src/extensions/health_check/views/HealthCheckPage.tsx
    - src/renderer/src/extensions/profile_management/index.ts
    - src/renderer/src/extensions/profile_management/selectors.ts
    - src/renderer/src/extensions/profile_management/views/ProfileView.tsx
    - src/renderer/src/extensions/gamemode_management/index.ts
    - src/renderer/src/extensions/gamemode_management/views/GameRow.tsx
    - src/renderer/src/extensions/starter_dashlet/Tools.tsx
    - src/renderer/src/extensions/starter_dashlet/actions.ts
    - src/renderer/src/extensions/installer_fomod_ipc/utils/VortexIPCConnection.ts
    - src/renderer/src/extensions/installer_fomod_native/installer.ts
    - src/renderer/src/extensions/browse_nexus/views/BrowseNexusPage.tsx
    - src/renderer/src/extensions/category_management/index.ts
    - src/renderer/src/extensions/extension_manager/installExtension.ts
    - src/renderer/src/extensions/file_based_loadorder/UpdateSet.ts
    - src/renderer/src/extensions/hardlink_activator/index.ts
    - src/renderer/src/extensions/symlink_activator_elevate/index.ts
    - .planning/phases/34-renderer-main-spine-v2-0-1/34-05-SUMMARY.md
autonomous: true
requirements:
    - SYNC-34a
must_haves:
    truths:
        - "All 30 renderer extension files marker-free, resolved in three sub-batches per CONTEXT D-34-01: E1 (nexus_integration cluster — 9 sequential leaf-first) → E2 (health_check cluster — 6 sequential leaf-first) → E3 (15 remaining extension files across smaller clusters + singletons, parallel-capable per D-34-12)"
        - "30 atomic SSH-signed `resolve(renderer-extensions): <relative-path> — <stance>` commits on `v8.1/config-bucket`"
        - "Wave-E-end bucket-scoped typecheck (D-34-06) — same renderer bucket as Wave D — filtered against still-conflicted Wave-F renderer files: 0 errors against Wave-D + Wave-E-resolved files"
        - "Bluebird trap audit (project memory) clean for the 4 risk files in this wave: `nexus_integration/eventHandlers.ts`, `nexus_integration/util.ts`, `hardlink_activator/index.ts`, `symlink_activator_elevate/index.ts`"
        - "No blanket `git checkout --ours` / `--theirs` per D-34-03 — every region hand-classified"
        - "Mid-wave files-still-bearing-markers filter passes: `git grep -lE '^(<{7}|={7}|>{7})( |$)' src/renderer/` shrinks by exactly 30 entries by wave end"
        - "Harness skip-mode (13 gates) stays GREEN after every commit"
        - "1 SUMMARY commit closes the wave with per-file region tally + bluebird-trap audit + bucket-typecheck count"
    artifacts:
        - path: "src/renderer/src/extensions/nexus_integration/eventHandlers.ts"
          provides: "Nexus event handler bridge — bluebird-trap risk file"
          contains: "Promise"
        - path: "src/renderer/src/extensions/symlink_activator_elevate/index.ts"
          provides: "Elevated symlink deployment extension — bluebird-trap risk + §3 Linux-guard candidate"
          contains: "Promise"
        - path: "src/renderer/src/extensions/hardlink_activator/index.ts"
          provides: "Hardlink deployment extension — bluebird-trap risk + §3 Linux-guard candidate"
          contains: "Promise"
    key_links:
        - from: "src/renderer/src/extensions/nexus_integration/index.tsx"
          to: "src/renderer/src/extensions/nexus_integration/eventHandlers.ts"
          via: "import"
          pattern: "from.*eventHandlers"
        - from: "src/renderer/src/extensions/nexus_integration/eventHandlers.ts"
          to: "src/renderer/src/extensions/nexus_integration/util.ts"
          via: "import"
          pattern: "from.*\\./util"
        - from: "src/renderer/src/extensions/health_check/views/HealthCheckPage.tsx"
          to: "src/renderer/src/extensions/health_check/types.ts"
          via: "import"
          pattern: "from.*\\./types"
---

<objective>
Wave E. Resolve all 30 renderer extension files in three sub-batches per CONTEXT D-34-01:

- **E1 — nexus_integration cluster (9 files, sequential leaf-first)**: types/IValidateKeyData → util/UIDs → util/oauth → util.ts → eventHandlers.ts → views/FreeUserDLDialog → views/GoPremiumDashlet → selectors.test.ts → index.tsx.
- **E2 — health_check cluster (6 files, sequential leaf-first)**: types.ts → api/triggers.ts → checks/modRequirementsCheck.ts → components/mod_requirement/ModRequirement.tsx → views/HealthCheckDetailPage.tsx → views/HealthCheckPage.tsx.
- **E3 — remaining 15 extension files (parallel-capable per D-34-12)** across smaller clusters and singletons:
    - profile_management (3): selectors → views/ProfileView → index
    - gamemode_management (2): views/GameRow → index
    - starter_dashlet (2): actions → Tools
    - installer*fomod*\* (2 singletons): installer_fomod_ipc/utils/VortexIPCConnection, installer_fomod_native/installer
    - 6 singletons: browse_nexus/views/BrowseNexusPage, category_management/index, extension_manager/installExtension, file_based_loadorder/UpdateSet, hardlink_activator/index (bluebird risk), symlink_activator_elevate/index (bluebird risk)

Default stance per D-34-02 6-tier hierarchy:

- §3 Linux-guard fork-wins for `symlink_activator_elevate/index.ts`, `hardlink_activator/index.ts` (deployment paths often Linux-guarded), `gamemode_management/index.ts` (Steam path Linux branch), `installer_fomod_native/installer.ts` (FOMOD .NET 9 Linux path)
- v2.0.1 feature upstream-wins for new health-check probe types, new oauth scopes, new dashlet props
- Rule-1 dup-import HEAD-empty (very common across extension files — split-import-block pattern)
- smaller-diff default for whitespace/comment-only regions
- D-34-03: no blanket `git checkout --ours` / `--theirs` — every region hand-classified

**Bluebird-trap risk files in this wave (per RESEARCH "Bluebird Promise Trap")**: `nexus_integration/eventHandlers.ts`, `nexus_integration/util.ts`, `hardlink_activator/index.ts`, `symlink_activator_elevate/index.ts`. Per-task audit pattern (same as Plan 04):

1. After resolving the file, run `grep -E ':\s*Promise<void>' <file>`.
2. Cross-check `grep -E '^import.*[Pp]romise.*from.*bluebird' <file>` — if bluebird is imported AND the annotation came from upstream side, REVERT in same commit.
3. Commit body MUST contain "bluebird-trap audit" line per Plan 04 protocol.

Per D-34-06: bucket-scoped typecheck runs at WAVE END only (Task E16). Filter pattern: exclude errors originating from files still bearing conflict markers (Wave F renderer files at this point). Wave-E-end typecheck covers Wave D + Wave E resolved files.

Mid-wave verification: `git grep -lE '^(<{7}|={7}|>{7})( |$)' src/renderer/` shrinks by exactly 30 entries from start of E1 to end of E3.

Output: 30 SSH-signed `resolve(renderer-extensions): ...` commits in E1→E2→E3 order + 1 SUMMARY commit. All 13 harness gates GREEN at end. Filtered bucket-scoped typecheck = 0 non-marker errors against Wave D + E-resolved files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-renderer-main-spine-v2-0-1/34-CONTEXT.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-RESEARCH.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-VALIDATION.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-00-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-01-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-02-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-03-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-04-SUMMARY.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
@CLAUDE.md
@AGENTS.md
</context>

<read_first>

- `.planning/phases/34-renderer-main-spine-v2-0-1/34-CONTEXT.md` — D-34-01 sub-batches, D-34-02 6-tier, D-34-03 (no `--theirs`/`--ours` blanket), D-34-12 (parallelism)
- `.planning/phases/34-renderer-main-spine-v2-0-1/34-RESEARCH.md` — Bluebird Promise Trap section (4 risk files this wave)
- `~/.claude/projects/-home-alex-src-Vortex/memory/feedback_bluebird_promise_trap.md`
- Each conflicted file before resolution
  </read_first>

<shared_per_task_workflow>
For every Task E1.x / E2.x / E3.x:

1. `git diff --check <file>` to enumerate region count.
2. Classify each region by D-34-02 6-tier order. NEVER use blanket `--theirs`/`--ours` (D-34-03).
3. Hand-resolve. Do NOT reformat outside conflict regions.
4. **Bluebird-trap audit (if file is in risk list — see objective)**: include audit line in commit body per Plan 04 protocol.
5. Markers gone: `git grep -nE '^(<{7}|={7}|>{7})( |$)' <file>` returns nothing.
6. Harness skip-mode: 13/13 GREEN.
7. Mid-wave files-still-bearing-markers count: `git grep -lE '^(<{7}|={7}|>{7})( |$)' src/renderer/ | wc -l` — record value, must monotonically decrease.
8. `git add <file>`; commit `resolve(renderer-extensions): <relative-path> — <stance>`.
9. Pattern S5 body (per D-34-08): regions tally per stance tier, bluebird-trap audit (if applicable), Linux-guard preservation (if applicable), `13/13 GREEN`, harness `0`, typecheck `deferred to wave end (D-34-06)`.
10. SSH signature verified.
    </shared_per_task_workflow>

<tasks>

<!-- E1: nexus_integration cluster (9 files, sequential leaf-first) -->

<task type="auto"><name>Task E1.1: resolve(renderer-extensions): nexus_integration/types/IValidateKeyData.ts</name><files>src/renderer/src/extensions/nexus_integration/types/IValidateKeyData.ts</files><action>Apply shared_per_task_workflow. Leaf — no Wave-E intra-deps. v2.0.1 may add new fields → upstream-wins.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/nexus_integration/types/IValidateKeyData.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task E1.2: resolve(renderer-extensions): nexus_integration/util/UIDs.ts</name><files>src/renderer/src/extensions/nexus_integration/util/UIDs.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/nexus_integration/util/UIDs.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task E1.3: resolve(renderer-extensions): nexus_integration/util/oauth.ts</name><files>src/renderer/src/extensions/nexus_integration/util/oauth.ts</files><action>Apply shared_per_task_workflow. v2.0.1 may add new oauth scopes → upstream-wins.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/nexus_integration/util/oauth.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task E1.4: resolve(renderer-extensions): nexus_integration/util.ts (bluebird-trap risk)</name><files>src/renderer/src/extensions/nexus_integration/util.ts</files><action>Apply shared_per_task_workflow. **Bluebird-trap risk file** — audit step REQUIRED per Plan 04 protocol.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/nexus_integration/util.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN; bluebird-trap audit line in body.</done></task>

<task type="auto"><name>Task E1.5: resolve(renderer-extensions): nexus_integration/eventHandlers.ts (bluebird-trap risk)</name><files>src/renderer/src/extensions/nexus_integration/eventHandlers.ts</files><action>Apply shared_per_task_workflow. **Bluebird-trap risk file** — audit step REQUIRED per Plan 04 protocol. Imports util.ts (E1.4).</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/nexus_integration/eventHandlers.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN; bluebird-trap audit line in body.</done></task>

<task type="auto"><name>Task E1.6: resolve(renderer-extensions): nexus_integration/views/FreeUserDLDialog.tsx</name><files>src/renderer/src/extensions/nexus_integration/views/FreeUserDLDialog.tsx</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/nexus_integration/views/FreeUserDLDialog.tsx | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task E1.7: resolve(renderer-extensions): nexus_integration/views/GoPremiumDashlet.tsx</name><files>src/renderer/src/extensions/nexus_integration/views/GoPremiumDashlet.tsx</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/nexus_integration/views/GoPremiumDashlet.tsx | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task E1.8: resolve(renderer-extensions): nexus_integration/selectors.test.ts</name><files>src/renderer/src/extensions/nexus_integration/selectors.test.ts</files><action>Apply shared_per_task_workflow. Test file.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/nexus_integration/selectors.test.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task E1.9: resolve(renderer-extensions): nexus_integration/index.tsx</name><files>src/renderer/src/extensions/nexus_integration/index.tsx</files><action>Apply shared_per_task_workflow. Imports util/eventHandlers/views — closes E1 sub-batch.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/nexus_integration/index.tsx | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<!-- E2: health_check cluster (6 files, sequential leaf-first) -->

<task type="auto"><name>Task E2.1: resolve(renderer-extensions): health_check/types.ts</name><files>src/renderer/src/extensions/health_check/types.ts</files><action>Apply shared_per_task_workflow. v2.0.1 may add new probe types → upstream-wins.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/health_check/types.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task E2.2: resolve(renderer-extensions): health_check/api/triggers.ts</name><files>src/renderer/src/extensions/health_check/api/triggers.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/health_check/api/triggers.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task E2.3: resolve(renderer-extensions): health_check/checks/modRequirementsCheck.ts</name><files>src/renderer/src/extensions/health_check/checks/modRequirementsCheck.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/health_check/checks/modRequirementsCheck.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task E2.4: resolve(renderer-extensions): health_check/components/mod_requirement/ModRequirement.tsx</name><files>src/renderer/src/extensions/health_check/components/mod_requirement/ModRequirement.tsx</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/health_check/components/mod_requirement/ModRequirement.tsx | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task E2.5: resolve(renderer-extensions): health_check/views/HealthCheckDetailPage.tsx</name><files>src/renderer/src/extensions/health_check/views/HealthCheckDetailPage.tsx</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/health_check/views/HealthCheckDetailPage.tsx | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task E2.6: resolve(renderer-extensions): health_check/views/HealthCheckPage.tsx</name><files>src/renderer/src/extensions/health_check/views/HealthCheckPage.tsx</files><action>Apply shared_per_task_workflow. Imports types (E2.1). Closes E2 sub-batch.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/health_check/views/HealthCheckPage.tsx | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<!-- E3: remaining 15 extension files — parallel-capable across small clusters per D-34-12 -->

<!-- E3 cluster: profile_management (3 files, leaf-first) -->

<task type="auto"><name>Task E3.1: resolve(renderer-extensions): profile_management/selectors.ts</name><files>src/renderer/src/extensions/profile_management/selectors.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/profile_management/selectors.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task E3.2: resolve(renderer-extensions): profile_management/views/ProfileView.tsx</name><files>src/renderer/src/extensions/profile_management/views/ProfileView.tsx</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/profile_management/views/ProfileView.tsx | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task E3.3: resolve(renderer-extensions): profile_management/index.ts</name><files>src/renderer/src/extensions/profile_management/index.ts</files><action>Apply shared_per_task_workflow. Imports selectors (E3.1) + views/ProfileView (E3.2).</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/profile_management/index.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<!-- E3 cluster: gamemode_management (2 files, leaf-first) -->

<task type="auto"><name>Task E3.4: resolve(renderer-extensions): gamemode_management/views/GameRow.tsx</name><files>src/renderer/src/extensions/gamemode_management/views/GameRow.tsx</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/gamemode_management/views/GameRow.tsx | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task E3.5: resolve(renderer-extensions): gamemode_management/index.ts</name><files>src/renderer/src/extensions/gamemode_management/index.ts</files><action>Apply shared_per_task_workflow. §3 Linux-guard likely (Steam path on Linux). Imports views/GameRow (E3.4).</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/gamemode_management/index.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN; Linux-guard preservation if any.</done></task>

<!-- E3 cluster: starter_dashlet (2 files, leaf-first) -->

<task type="auto"><name>Task E3.6: resolve(renderer-extensions): starter_dashlet/actions.ts</name><files>src/renderer/src/extensions/starter_dashlet/actions.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/starter_dashlet/actions.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task E3.7: resolve(renderer-extensions): starter_dashlet/Tools.tsx</name><files>src/renderer/src/extensions/starter_dashlet/Tools.tsx</files><action>Apply shared_per_task_workflow. Imports actions (E3.6).</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/starter_dashlet/Tools.tsx | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<!-- E3 cluster: installer_fomod_* (2 singletons) -->

<task type="auto"><name>Task E3.8: resolve(renderer-extensions): installer_fomod_ipc/utils/VortexIPCConnection.ts</name><files>src/renderer/src/extensions/installer_fomod_ipc/utils/VortexIPCConnection.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/installer_fomod_ipc/utils/VortexIPCConnection.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task E3.9: resolve(renderer-extensions): installer_fomod_native/installer.ts</name><files>src/renderer/src/extensions/installer_fomod_native/installer.ts</files><action>Apply shared_per_task_workflow. §3 Linux-guard likely (FOMOD .NET 9 Linux path).</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/installer_fomod_native/installer.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN; Linux-guard preservation if any.</done></task>

<!-- E3 singletons (6 files): -->

<task type="auto"><name>Task E3.10: resolve(renderer-extensions): browse_nexus/views/BrowseNexusPage.tsx</name><files>src/renderer/src/extensions/browse_nexus/views/BrowseNexusPage.tsx</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/browse_nexus/views/BrowseNexusPage.tsx | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task E3.11: resolve(renderer-extensions): category_management/index.ts</name><files>src/renderer/src/extensions/category_management/index.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/category_management/index.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task E3.12: resolve(renderer-extensions): extension_manager/installExtension.ts</name><files>src/renderer/src/extensions/extension_manager/installExtension.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/extension_manager/installExtension.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task E3.13: resolve(renderer-extensions): file_based_loadorder/UpdateSet.ts</name><files>src/renderer/src/extensions/file_based_loadorder/UpdateSet.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/file_based_loadorder/UpdateSet.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task E3.14: resolve(renderer-extensions): hardlink_activator/index.ts (bluebird-trap risk + §3 Linux-guard)</name><files>src/renderer/src/extensions/hardlink_activator/index.ts</files><action>Apply shared_per_task_workflow. **Bluebird-trap risk file** — audit step REQUIRED per Plan 04 protocol. §3 Linux-guard likely (deployment path).</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/hardlink_activator/index.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN; bluebird-trap audit + Linux-guard preservation lines in body.</done></task>

<task type="auto"><name>Task E3.15: resolve(renderer-extensions): symlink_activator_elevate/index.ts (bluebird-trap risk + §3 Linux-guard)</name><files>src/renderer/src/extensions/symlink_activator_elevate/index.ts</files><action>Apply shared_per_task_workflow. **Bluebird-trap risk file** — audit step REQUIRED per Plan 04 protocol. §3 Linux-guard fork-wins very likely (Linux symlink elevation path). Closes E3 / Wave E.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/extensions/symlink_activator_elevate/index.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN; bluebird-trap audit + Linux-guard preservation lines in body.</done></task>

<task type="auto">
  <name>Task E16: wave-end bucket typecheck + SUMMARY</name>
  <files>.planning/phases/34-renderer-main-spine-v2-0-1/34-05-SUMMARY.md</files>
  <action>
1. Filtered wave-end bucket-scoped typecheck (Wave-F renderer files still conflicted): `pnpm tsc -p src/renderer/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | grep -vFf <(git grep -lE '^(<{7}|={7}|>{7})( |$)' src/renderer/) | wc -l` — MUST return `0`.
2. Mid-wave-end count of files-still-bearing-markers in `src/renderer/`: `git grep -lE '^(<{7}|={7}|>{7})( |$)' src/renderer/ | wc -l` — record value (must equal pre-Wave-E count minus 30).
3. Harness skip-mode: 13/13 GREEN.
4. Write `.planning/phases/34-renderer-main-spine-v2-0-1/34-05-SUMMARY.md`:
   - Files resolved (30 total: E1=9, E2=6, E3=15) with per-file region tally + stance distribution
   - Bluebird-trap audit summary (4 risk files: outcome per file)
   - Linux-guard surfaces preserved (named — likely `symlink_activator_elevate`, `hardlink_activator`, `gamemode_management`, `installer_fomod_native`)
   - Filtered bucket-typecheck output (`0 errors against Wave-D + Wave-E-resolved files`)
   - Pre/post Wave-E files-still-bearing-markers count (delta = -30)
   - Harness state (13/13 GREEN at wave end)
   - Atomic-commit list (30 resolves + this SUMMARY) with short SHAs
5. `git add -f`; commit `docs(phase-34): Wave E (renderer extensions) summary — 30 files, 0 typecheck errors`.
6. Verify SSH signature.
  </action>
  <verify><automated>pnpm tsc -p src/renderer/tsconfig.json --noEmit 2>&amp;1 | grep -v TS1185 | grep -vFf &lt;(git grep -lE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/ || true) | wc -l</automated></verify>
  <done>
- Filtered bucket typecheck returns `0`
- 34-05-SUMMARY.md exists with all required sections (esp. bluebird-trap audit + Linux-guard preservation)
- SUMMARY committed (force-added) and SSH-signed
- Wave E closed; downstream Wave F may proceed
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                           |
| -------------------------- | --------------------------------------------------------------------- |
| bluebird vs native Promise | TS1064 trap (4 risk files this wave); project memory invariant        |
| Linux platform guard       | HEAD-only Linux paths in deployment activators + Steam + FOMOD .NET 9 |

## STRIDE Threat Register

| Threat ID  | Category    | Component                                                    | Disposition | Mitigation Plan                                                            |
| ---------- | ----------- | ------------------------------------------------------------ | ----------- | -------------------------------------------------------------------------- |
| T-34-05-01 | Tampering   | bluebird-importing async fns in 4 risk files                 | mitigate    | Bluebird-trap audit line REQUIRED per risk file (E1.4, E1.5, E3.14, E3.15) |
| T-34-05-02 | Tampering   | Linux-guard surfaces (deployment paths, FOMOD .NET 9, Steam) | mitigate    | §3 Linux-guard fork-wins; preserved surfaces named in commit bodies        |
| T-34-05-03 | Repudiation | 31 atomic commits this wave                                  | mitigate    | SSH signature verified                                                     |
| T-34-05-04 | Tampering   | Blanket `--theirs`/`--ours` regression risk (D-34-03)        | mitigate    | Per-region hand-classification per D-34-02; no blanket checkout            |
| T-34-05-SC | Tampering   | npm/pip/cargo installs                                       | n/a         | No new package installs in this wave                                       |

</threat_model>

<verification>
- L1 markers: `git grep -nE '^(<{7}|={7}|>{7})( |$)' src/renderer/src/extensions/` returns 0 lines for the 30 Wave-E files
- L2 harness: `bash .planning/phases/34-renderer-main-spine-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check` exits 0 (13/13 GREEN)
- L3 filtered bucket typecheck: 0 errors against Wave-D + Wave-E-resolved files
- Mid-wave files-still-bearing-markers count drops by exactly 30 in `src/renderer/`
- All 31 commits SSH-signed
- 4 bluebird-trap audit lines present in commit bodies
</verification>

<success_criteria>

- 30 atomic resolve commits (E1=9, E2=6, E3=15) + 1 SUMMARY commit on `v8.1/config-bucket`
- Filtered bucket typecheck 0 non-marker errors
- Bluebird-trap audit clean for 4 risk files
- Wave E closed; Wave F unblocked
  </success_criteria>

<output>
Write `.planning/phases/34-renderer-main-spine-v2-0-1/34-05-SUMMARY.md` capturing E1/E2/E3 sub-batch order, per-file region tally, bluebird-trap audit, Linux-guard preservation, filtered typecheck output, harness state, and commit SHAs.
</output>
