---
phase: 34
plan: 08
type: execute
wave: H
status: ready
requires:
    - 34-00
    - 34-01
    - 34-02
    - 34-03
    - 34-04
    - 34-05
    - 34-06
    - 34-07
depends_on:
    - 34-00
    - 34-01
    - 34-02
    - 34-03
    - 34-04
    - 34-05
    - 34-06
    - 34-07
files_modified:
    - src/renderer/src/__mocks__/ # entire directory removed via git rm -r
    - .planning/REQUIREMENTS.md
    - .planning/phases/34-renderer-main-spine-v2-0-1/34-08-SUMMARY.md
autonomous: true
requirements:
    - SYNC-34b
must_haves:
    truths:
        - "src/renderer/src/__mocks__/ directory removed entirely via `git rm -r` (R2 DROP per CONTEXT D-34-15 (DROP decision); ordering after Wave G per D-34-16)"
        - "Pre-deletion safety verified: `grep -r '__mocks__' src/renderer/src/ --include='*.ts*' | grep -v '^src/renderer/src/__mocks__/' | wc -l == 0` (no live references)"
        - "1 atomic SSH-signed `chore(renderer): drop dead Jest __mocks__/ tree (R2)` commit on `v8.1/config-bucket`"
        - "SYNC-34b documented as completed in `.planning/REQUIREMENTS.md` — checkbox flipped to `[x]` with brief evidence"
        - "1 SUMMARY commit closes the wave"
    artifacts:
        - path: ".planning/REQUIREMENTS.md"
          provides: "SYNC-34b requirement marked done with evidence"
          contains: "SYNC-34b"
    key_links: []
---

<objective>
Wave H. Two-step closeout:

1. **R2 DROP (CONTEXT D-34-15 — DROP decision; D-34-16 — ordering after Wave G)**: remove the dead `src/renderer/src/__mocks__/` Jest tree. RESEARCH §R2 Pre-Audit confirmed 0 references to `__mocks__/` remain in renderer test files (`grep -r '__mocks__' src/renderer/src/ --include='*.ts*' | grep -v '^src/renderer/src/__mocks__/' | wc -l == 0`); the directory is dead code. Single `git rm -r` commit titled `chore(renderer): drop dead Jest __mocks__/ tree (R2)`.

2. **SYNC-34b documentation**: flip the checkbox in `.planning/REQUIREMENTS.md` for SYNC-34b ("R2 carry-forward documented") to `[x]` with one-line evidence (R2 DROP commit short-SHA + bucket-typecheck status).

Default stance: this is not a merge-resolution wave — it's a deferred-cleanup wave. No D-34-02 6-tier classification applies; the deletion is decided by D-34-15 (DROP decision) with timing per D-34-16 (R2 lands after all resolution waves so test surface is unchanged during heavy resolution).

Per D-34-06: no bucket typecheck on the renderer bucket here — Wave F closed it at 0. But Task H1 MUST run an unfiltered renderer bucket typecheck after the deletion to confirm no surprise references emerged from elsewhere: `pnpm tsc -p src/renderer/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l == 0`.

Output: 1 SSH-signed `chore(renderer): drop dead Jest __mocks__/ tree (R2)` commit + 1 docs commit (REQUIREMENTS.md update + SUMMARY) on `v8.1/config-bucket`. All 13 harness gates GREEN at end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-renderer-main-spine-v2-0-1/34-CONTEXT.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-RESEARCH.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-VALIDATION.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-00-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-06-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-07-SUMMARY.md
@.planning/REQUIREMENTS.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
@CLAUDE.md
@AGENTS.md
</context>

<read_first>

- `.planning/phases/34-renderer-main-spine-v2-0-1/34-CONTEXT.md` — D-34-15 (R2 DROP decision) + D-34-16 (R2 ordering after Wave G)
- `.planning/phases/34-renderer-main-spine-v2-0-1/34-RESEARCH.md` — R2 Pre-Audit section
- `.planning/REQUIREMENTS.md` — find SYNC-34b checkbox row
- `src/renderer/src/__mocks__/` (the directory to be removed)
  </read_first>

<tasks>

<task type="auto">
  <name>Task H1: chore(renderer): drop dead Jest __mocks__/ tree (R2)</name>
  <files>src/renderer/src/__mocks__/</files>
  <action>
1. **Re-verify R2 safety** (RESEARCH §R2 Pre-Audit was cached — re-check now in case Wave D/E/F resolution accidentally added a reference):
   ```
   grep -r '__mocks__' src/renderer/src/ --include='*.ts*' --include='*.tsx' | grep -v '^src/renderer/src/__mocks__/' | wc -l
   ```
   MUST return `0`. If non-zero, STOP and surface the surprise reference to orchestrator (do NOT delete blindly).
2. Remove the directory: `git rm -r src/renderer/src/__mocks__/`.
3. Verify deletion: `[ ! -d src/renderer/src/__mocks__ ] && echo OK`.
4. Run renderer bucket typecheck (post-deletion safety check): `pnpm tsc -p src/renderer/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l` — MUST return `0`. If non-zero, STOP — restore via `git checkout HEAD -- src/renderer/src/__mocks__/` and surface.
5. Run harness in skip-mode: `bash .planning/phases/34-renderer-main-spine-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check` — must exit 0 (13/13 GREEN).
6. Stage all deletions: `git add -A src/renderer/src/__mocks__/`.
7. Commit with title `chore(renderer): drop dead Jest __mocks__/ tree (R2)`.
8. Pattern S5 commit body MUST include:
   - "R2 disposition: dead Jest mocks tree removed per D-34-15 (DROP) + D-34-16 (post-Wave-G ordering)"
   - "pre-deletion grep: 0 live references to __mocks__/ outside the directory itself"
   - "post-deletion renderer bucket typecheck: 0 errors"
   - file count deleted (e.g. "files removed: N")
   - `13/13 GREEN`, harness `0`
9. Verify SSH signature: `git cat-file -p HEAD | grep -c '^gpgsig '` >= 1.
  </action>
  <verify>
    <automated>[ ! -d src/renderer/src/__mocks__ ] &amp;&amp; pnpm tsc -p src/renderer/tsconfig.json --noEmit 2>&amp;1 | grep -v TS1185 | wc -l</automated>
  </verify>
  <done>
- src/renderer/src/__mocks__/ does not exist
- Renderer bucket typecheck returns `0`
- SSH-signed `chore(renderer): drop dead Jest __mocks__/ tree (R2)` commit on `v8.1/config-bucket`
- Pattern S5 body has R2 disposition + grep evidence + post-deletion typecheck status + file count
  </done>
</task>

<task type="auto">
  <name>Task H2: SYNC-34b doc update + Wave H SUMMARY</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/phases/34-renderer-main-spine-v2-0-1/34-08-SUMMARY.md
  </files>
  <action>
1. Edit `.planning/REQUIREMENTS.md`:
   - Locate the SYNC-34b row (find via `grep -n 'SYNC-34b' .planning/REQUIREMENTS.md`).
   - Flip the checkbox from `[ ]` to `[x]`.
   - Append one-line evidence: `done in 34-08 (R2 DROP commit <short-sha>, renderer bucket typecheck 0 errors)`.
2. Verify the documentation: `grep -q 'SYNC-34b.*\[x\]' .planning/REQUIREMENTS.md && echo OK` (per VALIDATION Wave-H row).
3. Write `.planning/phases/34-renderer-main-spine-v2-0-1/34-08-SUMMARY.md`:
   - R2 disposition (deleted via `git rm -r`)
   - Pre-deletion safety check evidence (`grep` returned 0 live references)
   - Post-deletion bucket typecheck (`0 errors`)
   - File count deleted
   - SYNC-34b checkbox flipped to `[x]` (with H1 SHA + REQUIREMENTS.md edit SHA)
   - Harness state (13/13 GREEN at wave end)
   - Atomic-commit list (1 R2 DROP commit + this SUMMARY) with short SHAs
4. Stage with `git add -f .planning/REQUIREMENTS.md .planning/phases/34-renderer-main-spine-v2-0-1/34-08-SUMMARY.md` (`.planning/` is gitignored).
5. Commit with title `docs(phase-34): Wave H (R2 DROP) summary + SYNC-34b done`.
6. Verify SSH signature on the SUMMARY commit.
  </action>
  <verify>
    <automated>grep -q 'SYNC-34b.*\[x\]' .planning/REQUIREMENTS.md &amp;&amp; test -f .planning/phases/34-renderer-main-spine-v2-0-1/34-08-SUMMARY.md</automated>
  </verify>
  <done>
- REQUIREMENTS.md SYNC-34b row flipped to `[x]` with evidence line
- 34-08-SUMMARY.md exists with all required sections
- SUMMARY committed (force-added) and SSH-signed
- Wave H closed; downstream Wave 9 (done-gate) may proceed
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary          | Description                                                                  |
| ----------------- | ---------------------------------------------------------------------------- |
| dead-code removal | R2 DROP deletes a tree; if any live reference exists, deletion = build break |

## STRIDE Threat Register

| Threat ID  | Category          | Component                                    | Disposition | Mitigation Plan                                                                                                                 |
| ---------- | ----------------- | -------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------- |
| T-34-08-01 | Denial of Service | accidental deletion of live test scaffolding | mitigate    | Pre-deletion grep MUST return 0 live references; post-deletion bucket typecheck MUST return 0; restore-on-fail recipe specified |
| T-34-08-02 | Repudiation       | atomic R2 DROP commit signing                | mitigate    | SSH signature verified                                                                                                          |
| T-34-08-03 | Tampering         | REQUIREMENTS.md doc update accuracy          | mitigate    | grep validates checkbox flip; evidence line names commit SHA                                                                    |
| T-34-08-SC | Tampering         | npm/pip/cargo installs                       | n/a         | No package installs in Wave H                                                                                                   |

</threat_model>

<verification>
- `[ ! -d src/renderer/src/__mocks__ ]` true
- Renderer bucket typecheck: `pnpm tsc -p src/renderer/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l == 0`
- L2 harness: 13/13 GREEN
- `grep -q 'SYNC-34b.*\[x\]' .planning/REQUIREMENTS.md` returns true
- Both commits SSH-signed
</verification>

<success_criteria>

- 1 R2 DROP commit + 1 SUMMARY commit on `v8.1/config-bucket`
- src/renderer/src/**mocks**/ removed
- Renderer bucket typecheck still 0 errors
- SYNC-34b marked done in REQUIREMENTS.md
- Wave H closed; done-gate (Wave 9) unblocked
  </success_criteria>

<output>
Write `.planning/phases/34-renderer-main-spine-v2-0-1/34-08-SUMMARY.md` capturing R2 disposition, safety evidence, post-deletion typecheck, SYNC-34b doc flip, harness state, and commit SHAs.
</output>
