---
phase: 34
plan: 09
type: execute
wave: 9
status: ready
requires:
    - 34-00
    - 34-01
    - 34-02
    - 34-03
    - 34-04
    - 34-05
    - 34-06
    - 34-07
    - 34-08
depends_on:
    - 34-00
    - 34-01
    - 34-02
    - 34-03
    - 34-04
    - 34-05
    - 34-06
    - 34-07
    - 34-08
files_modified:
    - .planning/STATE.md
    - .planning/ROADMAP.md
    - .planning/phases/34-renderer-main-spine-v2-0-1/34-09-SUMMARY.md
autonomous: true
requirements:
    - SYNC-34a
    - SYNC-34b
must_haves:
    truths:
        - "All 7 D-34-14 done-gate criteria verified GREEN: zero markers + 13/13 harness skip-mode + 6 bucket typechecks (shared/preload/main/renderer/fingerprints/e2e) all 0 + commit accounting matches plan + R2 documented + STATE.md updated + ROADMAP.md updated"
        - "1 combined SSH-signed `docs(phase-34): close-out — STATE + ROADMAP` commit on `v8.1/config-bucket`"
        - "1 master closeout SUMMARY commit `docs(phase-34): close-out summary` on `v8.1/config-bucket`"
        - "Phase 34 marked COMPLETE in STATE.md (decisions, commits, blockers, next-phase)"
        - "Phase 34 row in ROADMAP.md flipped to `[x]` with milestone progress (7/8 v8.1 phases or matching count)"
        - "No `--no-verify` used anywhere in Phase 34 (D-34-09)"
        - "All Phase 34 commits SSH-signed (`git log --format='%H %G?' v8.1/config-bucket~N..HEAD | grep -v ' G$' | wc -l == 0` for the Phase 34 commit range)"
    artifacts:
        - path: ".planning/STATE.md"
          provides: "Phase 34 closeout block (decisions, commits, blockers, next-phase)"
          contains: "Phase 34"
        - path: ".planning/ROADMAP.md"
          provides: "Milestone v8.1 progress with Phase 34 marked done"
          contains: "[x] Phase 34"
    key_links: []
---

<objective>
Wave 9 (done-gate). Verify all 7 D-34-14 done-gate criteria are GREEN, then update `.planning/STATE.md` + `.planning/ROADMAP.md` in a single combined commit, then write the master closeout SUMMARY.

D-34-14 done-gate criteria (all 7 must be GREEN to close Phase 34):

1. **L1 — zero markers outside `.planning/`**: `git grep -nE '^(<{7}|={7}|>{7})( |$)' -- ':!.planning'` returns 0 lines.
2. **L2 — harness 13-gate skip-mode**: `bash .planning/phases/34-renderer-main-spine-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check` exits 0 with 13/13 GREEN.
3. **L3 — six bucket typechecks all 0 errors**:
    - `pnpm tsc -p src/shared/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l == 0`
    - `pnpm tsc -p src/preload/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l == 0`
    - `pnpm tsc -p src/main/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l == 0`
    - `pnpm tsc -p src/renderer/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l == 0`
    - `pnpm tsc -p .github/actions/fingerprints/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l == 0`
    - `pnpm tsc -p packages/e2e/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l == 0`
4. **Commit accounting matches plan**: target ~119 unique work commits (117 file-resolves + 1 R2 DROP + 1 dist regen) + 9 wave SUMMARYs + 2 docs commits (REQUIREMENTS.md update folded into 34-08, plus this wave's STATE/ROADMAP + closeout SUMMARY) = ~131 ± few commits. Verify via `git log --format=%s v8.1/config-bucket | grep -E '^(resolve|chore|regen|docs)\(' | wc -l` and cross-check by Pattern S5 commit body counts.
5. **R2 documented**: `grep -q 'SYNC-34b.*\[x\]' .planning/REQUIREMENTS.md` returns true (confirmed by Wave H Task H2).
6. **STATE.md updated**: this task adds the Phase 34 closeout block.
7. **ROADMAP.md updated**: this task flips the Phase 34 checkbox to `[x]` and updates milestone progress.

Per D-34-09: no `--no-verify` was used in Phase 34. Verify in this task.

Per project memory `feedback_ssh_signing.md`: every Phase 34 commit SSH-signed. Verify the entire commit range.

Output: 1 combined `docs(phase-34): close-out — STATE + ROADMAP` commit + 1 master closeout SUMMARY commit on `v8.1/config-bucket`. All 7 D-34-14 criteria GREEN.

**No push from sandbox** (per project memory `feedback_git_push_ssh.md`): Phase 36 owns push + FF-merge. This wave only commits locally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-renderer-main-spine-v2-0-1/34-CONTEXT.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-RESEARCH.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-VALIDATION.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-00-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-01-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-02-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-03-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-04-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-05-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-06-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-07-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-08-SUMMARY.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
@CLAUDE.md
@AGENTS.md
</context>

<read_first>

- All Wave 0–H SUMMARYs (already in context above)
- `.planning/STATE.md` — current phase counter, decisions block, last-phase summary block format
- `.planning/ROADMAP.md` — current Phase 34 row, milestone progress format
- `.planning/REQUIREMENTS.md` — confirm SYNC-34a + SYNC-34b are checkboxes ready to flip
  </read_first>

<commit_accounting_recipe>
Target distribution (verify in Task 1):

- Wave 0 (harness): 1 commit (`resolve(checkpoint): scripts/grep-checkpoint.sh — extend with gate-13`) + 1 SUMMARY
- Wave A (shared): 5 resolve + 1 SUMMARY
- Wave B (preload): 1 resolve + 1 SUMMARY
- Wave C (main): 9 resolve + 1 SUMMARY
- Wave D (renderer leaves): 21 resolve + 1 SUMMARY
- Wave E (renderer extensions): 30 resolve + 1 SUMMARY
- Wave F (renderer views/pages + heaviest): 17 resolve + 1 SUMMARY (F3.3 may carry pnpm-workspace.yaml + pnpm-lock.yaml in same commit)
- Wave G (repo-wide): 33 resolve + 1 dist regen + 1 SUMMARY (G2 was no-commit; G3.10 is the dist regen; G2 lockfile may have folded into G3.1)
- Wave H (R2 DROP): 1 R2 chore + 1 SUMMARY (combines REQUIREMENTS.md update)
- Wave 9 (this wave): 1 combined STATE/ROADMAP + 1 closeout SUMMARY

Total: 117 file-resolves + 1 R2 DROP + 1 dist regen = 119 work commits, plus 9 wave SUMMARYs + 1 STATE/ROADMAP + 1 closeout = ~131 commits.

Cross-check command:

```
git log --oneline v8.1/config-bucket~131..HEAD | wc -l
git log --format=%s v8.1/config-bucket~131..HEAD | grep -cE '^(resolve|chore|regen)\('
git log --format=%s v8.1/config-bucket~131..HEAD | grep -cE '^docs\(phase-34\):'
```

</commit_accounting_recipe>

<state_update_recipe>
Append the Phase 34 closeout block to `.planning/STATE.md`:

```markdown
## Phase 34 — renderer + main spine merge resolution v2.0.1

**Status:** COMPLETE @ <YYYY-MM-DD>
**Branch:** v8.1/config-bucket
**Commits:** ~131 (117 file-resolves + 1 R2 DROP + 1 dist regen + 9 wave SUMMARYs + 2 docs)

**Decisions exercised (D-34-00..D-34-20):** all 21 locked decisions honored. Notable:

- D-34-12: dist/index.js regenerated via ncc build (NOT hand-merged 915 regions)
- D-34-13: R2 **mocks**/ tree removed (Wave H)
- D-34-17: native-errors trigger evaluation in F3.3 — <branch taken>
- D-34-20: full pnpm test/lint/build deferred to Phase 35

**Linux-guard surfaces preserved:**

- Tray API (TrayIcon.ts)
- autoupdater Linux disposition (extensions/autoupdater.ts)
- native error handler tier (errorReporting.ts)
- elevated path (util/elevated.ts)
- fs path-handling (util/fs.ts)
- symlink_activator_elevate Linux branch
- gamemode_management Steam path
- installer_fomod FOMOD .NET 9 Linux path
- Tools.tsx Linux launcher disposition
- flatpak/com.nexusmods.Vortex.yml
- scripts/build-natives.js Linux branch

**Validation:**

- L1 markers (outside .planning/): 0
- L2 harness skip-mode: 13/13 GREEN
- L3 bucket typechecks (shared/preload/main/renderer/fingerprints/e2e): all 0 errors

**Blockers:** none
**Next phase:** 35 (full pnpm test/lint/build/CI green) per D-34-20
```

Also update the milestone counter at the top of STATE.md if applicable (e.g. `v8.1: 7/8 phases COMPLETE`).
</state_update_recipe>

<roadmap_update_recipe>
Edit `.planning/ROADMAP.md`:

1. Find the `[ ] Phase 34 — renderer + main spine merge resolution v2.0.1` row (or similar wording).
2. Flip checkbox to `[x]`.
3. Append closeout date and commit count.
4. Update milestone v8.1 progress counter (e.g. `v8.1 milestone: 7/8 COMPLETE`).
   </roadmap_update_recipe>

<tasks>

<task type="auto">
  <name>Task 1: D-34-14 done-gate verification (7 criteria)</name>
  <files>(no file edits — verification only)</files>
  <action>
Run all 7 D-34-14 criteria. STOP at first failure and surface to orchestrator. Capture all outputs for use in Task 3 SUMMARY.

**Criterion 1 — L1 markers outside .planning/:**

```
git grep -nE '^(<{7}|={7}|>{7})( |$)' -- ':!.planning' | wc -l
```

Expected: `0`.

**Criterion 2 — L2 harness skip-mode (13/13):**

```
bash .planning/phases/34-renderer-main-spine-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check
echo "exit=$?"
```

Expected: `exit=0` with 13/13 GREEN in output.

**Criterion 3 — L3 six bucket typechecks (all 0):**

```
pnpm tsc -p src/shared/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l
pnpm tsc -p src/preload/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l
pnpm tsc -p src/main/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l
pnpm tsc -p src/renderer/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l
pnpm tsc -p .github/actions/fingerprints/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l
pnpm tsc -p packages/e2e/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l
```

Expected: all 6 commands output `0`.

**Criterion 4 — Commit accounting (per commit_accounting_recipe):**

```
git log --oneline v8.1/config-bucket~131..HEAD | wc -l
git log --format=%s v8.1/config-bucket~131..HEAD | grep -cE '^(resolve|chore|regen)\('
git log --format=%s v8.1/config-bucket~131..HEAD | grep -cE '^docs\(phase-34\):'
```

Expected: ~131 total, ~119 work commits, ~12 docs commits (9 wave SUMMARYs + 2 in this wave + 1 leftover variance allowance).

**Criterion 5 — R2 documented:**

```
grep -q 'SYNC-34b.*\[x\]' .planning/REQUIREMENTS.md && echo OK
```

Expected: `OK`.

**Criterion 6 — STATE.md updated** (this is Task 2 below; mark as PENDING in this verification task).

**Criterion 7 — ROADMAP.md updated** (this is Task 2 below; mark as PENDING in this verification task).

**Bonus — D-34-09 no `--no-verify`:**

```
git log --format=%B v8.1/config-bucket~131..HEAD | grep -ic 'no-verify'
```

Expected: `0` (or only references to a NOT-using-no-verify rationale; review manually if non-zero).

**Bonus — All commits SSH-signed:**

```
git log --format='%H %G?' v8.1/config-bucket~131..HEAD | grep -v ' G$' | wc -l
```

Expected: `0` (every commit verified-signed).

**Halt-on-fail:** if ANY of criteria 1–5 fail, STOP and report to orchestrator. Do NOT proceed to Task 2.
</action>
<verify>
<automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' -- ':!.planning' | wc -l</automated>
</verify>
<done>

- All 7 criteria evaluated; criteria 1–5 all GREEN (criteria 6 + 7 PENDING for Task 2)
- D-34-09 no-verify check: 0 occurrences
- SSH-signing check: 0 unsigned commits in Phase 34 range
- All output captured for SUMMARY use in Task 3
  </done>
  </task>

<task type="auto">
  <name>Task 2: STATE + ROADMAP combined update commit</name>
  <files>
    .planning/STATE.md
    .planning/ROADMAP.md
  </files>
  <action>
1. Apply state_update_recipe to `.planning/STATE.md`:
   - Append the Phase 34 closeout block (template above; fill in concrete decision references — e.g. D-34-17 trigger branch as recorded in 34-06-SUMMARY.md)
   - Update milestone v8.1 progress counter at top of STATE.md
2. Apply roadmap_update_recipe to `.planning/ROADMAP.md`:
   - Flip Phase 34 checkbox to `[x]`
   - Append closeout date + commit count
   - Update milestone counter
3. Stage with `git add -f .planning/STATE.md .planning/ROADMAP.md` (`.planning/` is gitignored).
4. Commit with title `docs(phase-34): close-out — STATE + ROADMAP`.
5. Pattern S5 commit body MUST include:
   - "Phase 34 closeout: STATE.md + ROADMAP.md updated"
   - Cross-reference to D-34-14 done-gate criteria (5/7 GREEN at this point; criteria 6 + 7 satisfied by THIS commit)
   - Commit-accounting tally from Task 1
   - `13/13 GREEN` harness state
6. Verify SSH signature.
  </action>
  <verify>
    <automated>grep -q '\[x\] Phase 34' .planning/ROADMAP.md &amp;&amp; grep -q 'Phase 34.*COMPLETE' .planning/STATE.md</automated>
  </verify>
  <done>
- STATE.md has Phase 34 closeout block with all required sub-sections (status, branch, commits, decisions, Linux-guard surfaces, validation, blockers, next-phase)
- ROADMAP.md has `[x] Phase 34` and milestone counter updated
- SSH-signed `docs(phase-34): close-out — STATE + ROADMAP` commit on `v8.1/config-bucket`
- D-34-14 criteria 6 + 7 now satisfied
  </done>
</task>

<task type="auto">
  <name>Task 3: master closeout SUMMARY</name>
  <files>.planning/phases/34-renderer-main-spine-v2-0-1/34-09-SUMMARY.md</files>
  <action>
Write `.planning/phases/34-renderer-main-spine-v2-0-1/34-09-SUMMARY.md` as the master closeout for Phase 34. Sections:

1. **Header**: Phase 34 — renderer + main spine merge resolution v2.0.1; status: COMPLETE; branch: v8.1/config-bucket; closeout date.
2. **D-34-14 done-gate evidence**: paste the 7-criterion verification output from Task 1 (markers / harness exit / 6 typecheck counts / commit accounting / SYNC-34b grep / STATE.md grep / ROADMAP.md grep).
3. **Wave-by-wave commit accounting**: 0/A/B/C/D/E/F/G/H/9 — files resolved, atomic commits, sub-batch typecheck status, Linux-guard surfaces preserved per wave.
4. **D-34-17 trigger decision** (from 34-06-SUMMARY.md).
5. **Open Question 2 catalog re-add disposition** (from 34-06-SUMMARY.md).
6. **R2 DROP disposition** (from 34-08-SUMMARY.md).
7. **Dist regen disposition** (from 34-07-SUMMARY.md).
8. **Bluebird-trap audit summary** (7 risk files: util/errorHandling, util/elevated, util/fs, hardlink_activator/index, symlink_activator_elevate/index, nexus_integration/eventHandlers, nexus_integration/util — outcome per file).
9. **Linux-guard surfaces preserved (full inventory)**.
10. **D-34-09 + signing audits**: no-verify count 0, unsigned-commit count 0.
11. **Blockers carried forward**: none.
12. **Next-phase handoff**: Phase 35 (full pnpm test/lint/build/CI) per D-34-20; Phase 36 (push + FF-merge) per project memory `feedback_git_push_ssh.md`.
13. **Atomic-commit list**: full list of all Phase 34 commit short-SHAs grouped by wave (use `git log --oneline --no-merges v8.1/config-bucket~131..HEAD`).

Stage with `git add -f`.
Commit with title `docs(phase-34): close-out summary`.
Pattern S5 commit body: "Phase 34 closeout summary; all 7 D-34-14 criteria GREEN; ready for Phase 35 (full test/lint/build) and Phase 36 (push + FF-merge)."
Verify SSH signature.
</action>
<verify>
<automated>test -f .planning/phases/34-renderer-main-spine-v2-0-1/34-09-SUMMARY.md &amp;&amp; git log --format=%s -1 HEAD | grep -q 'close-out summary'</automated>
</verify>
<done>

- 34-09-SUMMARY.md exists with all 13 sections
- SSH-signed `docs(phase-34): close-out summary` commit on `v8.1/config-bucket`
- Phase 34 fully closed; orchestrator may transition to Phase 35
- No push performed (Phase 36 owns push)
  </done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                | Description                                                              |
| ----------------------- | ------------------------------------------------------------------------ |
| done-gate verification  | If any criterion is fudged, downstream phases inherit broken assumptions |
| state-record durability | STATE.md + ROADMAP.md are the source of truth for phase progress         |

## STRIDE Threat Register

| Threat ID  | Category    | Component                         | Disposition | Mitigation Plan                                                                                                                          |
| ---------- | ----------- | --------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ------------- | -------------------------- |
| T-34-09-01 | Repudiation | done-gate verification evidence   | mitigate    | All 7 criteria evaluated with concrete commands; outputs captured in SUMMARY; halt-on-fail prevents premature closure                    |
| T-34-09-02 | Tampering   | STATE.md + ROADMAP.md updates     | mitigate    | Combined commit with specific recipe; verified via grep before commit                                                                    |
| T-34-09-03 | Repudiation | Phase 34 commit SSH-signing audit | mitigate    | `git log --format='%H %G?' ...                                                                                                           | grep -v ' G$' | wc -l == 0` runs in Task 1 |
| T-34-09-04 | Spoofing    | accidental push from sandbox      | mitigate    | Project memory `feedback_git_push_ssh.md` invariant: no push from sandbox; Phase 36 owns push + FF-merge; this wave commits locally only |
| T-34-09-SC | Tampering   | npm/pip/cargo installs            | n/a         | No package installs in Wave 9                                                                                                            |

</threat_model>

<verification>
- All 7 D-34-14 criteria GREEN (markers / harness / 6 typechecks / commits / R2 / STATE / ROADMAP)
- D-34-09 no-verify count: 0
- All Phase 34 commits SSH-signed
- 34-09-SUMMARY.md exists with all required sections
- 2 commits in this wave (STATE/ROADMAP + closeout SUMMARY) SSH-signed
- No push attempted from sandbox
</verification>

<success_criteria>

- All 7 D-34-14 done-gate criteria GREEN
- STATE.md + ROADMAP.md updated and committed
- Master closeout SUMMARY written and committed
- Phase 34 COMPLETE on `v8.1/config-bucket` (local only — Phase 36 owns push)
- v8.1 milestone progress counter updated
- Orchestrator may transition to Phase 35
  </success_criteria>

<output>
Write `.planning/phases/34-renderer-main-spine-v2-0-1/34-09-SUMMARY.md` as the master Phase 34 closeout document with all 13 sections (header, done-gate evidence, wave-by-wave accounting, D-34-17 / Open Question 2 / R2 / dist regen dispositions, bluebird audit, Linux-guard inventory, no-verify+signing audits, blockers, next-phase handoff, atomic-commit list).
</output>
