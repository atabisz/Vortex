---
phase: 34
plan: 04
type: execute
wave: D
status: ready
requires:
    - 34-00
    - 34-01
    - 34-02
    - 34-03
depends_on:
    - 34-00
    - 34-01
    - 34-02
    - 34-03
files_modified:
    - src/renderer/src/contexts/PagesContext.tsx
    - src/renderer/src/contexts/builtInPages.ts
    - src/renderer/src/hooks/windowControls.ts
    - src/renderer/src/reducers/notifications.ts
    - src/renderer/src/reducers/verify.test.ts
    - src/renderer/src/store/stateDiff.test.ts
    - src/renderer/src/store/stateDiff.ts
    - src/renderer/src/telemetry/selectors.ts
    - src/renderer/src/ui/README.md
    - src/renderer/src/ui/components/form/formfield/FormField.tsx
    - src/renderer/src/ui/components/form/input/Input.tsx
    - src/renderer/src/ui/components/no_results/NoResults.tsx
    - src/renderer/src/util/elevated.ts
    - src/renderer/src/util/errorHandling.ts
    - src/renderer/src/util/message.ts
    - src/renderer/src/util/migrate.ts
    - src/renderer/src/util/opn.ts
    - src/renderer/src/util/startupSettings.ts
    - src/renderer/src/util/util.objDiff.test.ts
    - src/renderer/src/util/walk.ts
    - .planning/phases/34-renderer-main-spine-v2-0-1/34-04-SUMMARY.md
autonomous: true
requirements:
    - SYNC-34a
must_haves:
    truths:
        - "All 20 renderer-leaf files (non-extensions, non-views, non-controls/Table, non-ExtensionManager.ts, non-renderer.tsx) marker-free, resolved in three sub-batches per CONTEXT D-34-01: D1 (util/* — 8 files) → D2 (reducers/* + hooks/* + store/* + telemetry/* — 6 files) → D3 (contexts/* + ui/* — 6 files)"
        - "20 atomic SSH-signed `resolve(renderer-leaves): <relative-path> — <stance>` commits on `v8.1/config-bucket`"
        - "Wave-D-end bucket-scoped typecheck (D-34-06) — filtered against still-conflicted Wave E/F renderer files: 0 errors against Wave-D-resolved files"
        - "Bluebird trap audit (project memory `feedback_bluebird_promise_trap.md`) clean for the 2 risk files in this wave: `util/errorHandling.ts`, `util/elevated.ts`"
        - "No blanket `git checkout --ours` / `--theirs` per D-34-03 — every region hand-classified per D-34-02 6-tier"
        - "Mid-wave files-still-bearing-markers filter passes: `git grep -lE '^(<{7}|={7}|>{7})( |$)' src/renderer/` shrinks by exactly 20 entries by wave end"
        - "Harness skip-mode (13 gates) stays GREEN after every commit"
        - "1 SUMMARY commit closes the wave with per-file region tally + bluebird-trap audit + bucket-typecheck count"
    artifacts:
        - path: "src/renderer/src/util/errorHandling.ts"
          provides: "Renderer-side error handling; bluebird Promise trap risk file"
          contains: "Promise"
        - path: "src/renderer/src/util/elevated.ts"
          provides: "Renderer-side elevated process invocation; bluebird trap risk file; §3 Linux-guard candidate"
          contains: "Promise"
        - path: "src/renderer/src/store/stateDiff.ts"
          provides: "Renderer state-diff helper consumed by reducers + persistor"
          contains: "stateDiff"
    key_links:
        - from: "src/renderer/src/util/errorHandling.ts"
          to: "bluebird Promise import"
          via: "TS1064 trap site — `:Promise<void>` annotations on async fns"
          pattern: "import.*[Pp]romise.*from.*bluebird"
        - from: "src/renderer/src/util/elevated.ts"
          to: "bluebird Promise import"
          via: "TS1064 trap site — `:Promise<void>` annotations on async fns"
          pattern: "import.*[Pp]romise.*from.*bluebird"
        - from: "src/renderer/src/contexts/builtInPages.ts"
          to: "src/renderer/src/contexts/PagesContext.tsx"
          via: "import"
          pattern: "from.*PagesContext"
---

<objective>
Wave D. Resolve all 20 renderer-leaf files (no extensions, no views, no `controls/Table.tsx`, no `ExtensionManager.ts`, no `renderer.tsx`) in three sub-batches per CONTEXT D-34-01:

- **D1 — util/\* (8 files)**: util/{elevated, errorHandling, message, migrate, opn, startupSettings, walk, util.objDiff.test} — leaf-first by intra-D1 import depth. The two `*.test.ts` files (util.objDiff.test) sit at the leaf-end (only consumes nothing).
- **D2 — reducers/_ + hooks/_ + store/_ + telemetry/_ (6 files)**: reducers/{notifications, verify.test} (2) + hooks/windowControls (1) + store/{stateDiff, stateDiff.test} (2) + telemetry/selectors (1).
- **D3 — contexts/_ + ui/_ + ui/README (6 files)**: contexts/{PagesContext, builtInPages} (2) + ui/{README.md, components/form/formfield/FormField, components/form/input/Input, components/no_results/NoResults} (4).

Default stance per D-34-02 6-tier hierarchy:

- §3 Linux-guard fork-wins for any HEAD-only Linux platform branches in `util/elevated.ts` (HEAD-only Linux elevation path is the canonical example)
- v2.0.1 feature upstream-wins for new ui component props or new reducer cases
- Rule-1 dup-import HEAD-empty for split-import-block conflicts (very common across renderer leaves)
- smaller-diff default for whitespace/comment-only regions
- D-34-03: no blanket `git checkout --ours` / `--theirs` — every region hand-classified

**Bluebird trap (project memory `feedback_bluebird_promise_trap.md`):** files importing bluebird Promise where upstream added `:Promise<void>` annotation on an async fn — TS1064. Risk files in this wave (per RESEARCH "Bluebird Promise Trap"): `util/errorHandling.ts`, `util/elevated.ts`. Per-task audit pattern:

1. After resolving the file, run `grep -E ':\s*Promise<void>' <file>` to enumerate annotations.
2. Cross-check `grep -E '^import.*[Pp]romise.*from.*bluebird' <file>` — if bluebird is imported AND the annotation came from upstream side of a region, REVERT that annotation to HEAD's typing in the same commit.
3. Commit body MUST include a "bluebird-trap audit" line either: (a) "no upstream `:Promise<void>` taken on bluebird-importing async fn" or (b) "upstream annotation reverted on lines X,Y,Z (bluebird import retained)".

Per D-34-06: bucket-scoped typecheck runs at WAVE END only (Task D21). Renderer is a single bucket; mid-wave typechecks would fail because reducers/hooks/contexts/ui have intra-bucket import chains, plus Wave E/F files are still conflicted. Filter pattern: exclude errors originating from files still bearing conflict markers.

Mid-wave verification: `git grep -lE '^(<{7}|={7}|>{7})( |$)' src/renderer/` shrinks by exactly 20 entries from start of D1 to end of D3.

Purpose: renderer leaves are the dependees of Wave E (extensions) and Wave F (views/pages + the three heaviest). Without them clean, Wave E import-chain typechecks would fail and Wave F cannot evaluate the D-34-17 native-errors trigger.

Output: 20 SSH-signed `resolve(renderer-leaves): ...` commits in D1→D2→D3 order + 1 SUMMARY commit. All 13 harness gates GREEN at end. Filtered bucket-scoped typecheck = 0 non-marker errors against Wave-D-resolved files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-renderer-main-spine-v2-0-1/34-CONTEXT.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-RESEARCH.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-VALIDATION.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-00-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-01-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-02-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-03-SUMMARY.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
@CLAUDE.md
@AGENTS.md
</context>

<read_first>

- `.planning/phases/34-renderer-main-spine-v2-0-1/34-CONTEXT.md` — D-34-01 (sub-batches), D-34-02 6-tier, D-34-03 (no `--theirs`/`--ours` blanket), D-34-06 wave-end typecheck, D-34-08 commit body, D-34-12 (parallelism within sub-batches)
- `.planning/phases/34-renderer-main-spine-v2-0-1/34-RESEARCH.md` — Bluebird Promise Trap section (2 risk files this wave)
- `~/.claude/projects/-home-alex-src-Vortex/memory/feedback_bluebird_promise_trap.md` (the rule)
- `src/renderer/tsconfig.json` (bucket scope)
- Each conflicted file before resolution
  </read_first>

<shared_per_task_workflow>
For every Task D1.x / D2.x / D3.x (atomic per-file resolves):

1. Run `git diff --check <file>` to enumerate region count.
2. For each region, classify by D-34-02 6-tier order. NEVER use `git checkout --theirs`/`--ours` (D-34-03).
3. Hand-resolve. Do NOT reformat outside conflict regions.
4. **Bluebird-trap audit (when applicable — `util/errorHandling.ts` + `util/elevated.ts`)**:
    - `grep -E '^import.*[Pp]romise.*from.*bluebird' <file>` — if bluebird is imported, scan resolved file for `grep -E ':\s*Promise<void>' <file>`.
    - If any `:Promise<void>` annotation came from upstream side of a region AND the file imports bluebird, REVERT the annotation in the same commit.
    - Commit body MUST contain a "bluebird-trap audit" line stating either "no upstream `:Promise<void>` taken on bluebird-importing async fn" or "upstream annotation reverted on lines X,Y,Z (bluebird import retained)".
5. Markers verified gone: `git grep -nE '^(<{7}|={7}|>{7})( |$)' <file>` returns nothing.
6. Harness skip-mode: `bash .planning/phases/34-renderer-main-spine-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check` exits 0 (13/13 GREEN).
7. Mid-wave files-still-bearing-markers count: `git grep -lE '^(<{7}|={7}|>{7})( |$)' src/renderer/ | wc -l` — record value, must monotonically decrease across D1→D3.
8. Stage with `git add <file>`; commit with title `resolve(renderer-leaves): <relative-path> — <stance>`.
9. Pattern S5 commit body (per D-34-08): regions tally per stance tier, bluebird-trap audit line (if applicable), Linux-guard preservation (if applicable), gate exits (`13/13 GREEN`), harness exit (`0`), typecheck (`deferred to wave end (D-34-06)`).
10. Verify SSH signature.
    </shared_per_task_workflow>

<tasks>

<!-- D1: util/* (8 files) — leaf-first; tests last in batch -->

<task type="auto"><name>Task D1.1: resolve(renderer-leaves): util/opn.ts</name><files>src/renderer/src/util/opn.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/util/opn.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task D1.2: resolve(renderer-leaves): util/walk.ts</name><files>src/renderer/src/util/walk.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/util/walk.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task D1.3: resolve(renderer-leaves): util/startupSettings.ts</name><files>src/renderer/src/util/startupSettings.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/util/startupSettings.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task D1.4: resolve(renderer-leaves): util/migrate.ts</name><files>src/renderer/src/util/migrate.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/util/migrate.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task D1.5: resolve(renderer-leaves): util/message.ts</name><files>src/renderer/src/util/message.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/util/message.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task D1.6: resolve(renderer-leaves): util/errorHandling.ts (bluebird-trap risk)</name><files>src/renderer/src/util/errorHandling.ts</files><action>Apply shared_per_task_workflow. **Bluebird-trap risk file** — audit step REQUIRED. Run the bluebird-trap audit per workflow step 4. Commit body MUST include the bluebird-trap audit line.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/util/errorHandling.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN; bluebird-trap audit line present in body.</done></task>

<task type="auto"><name>Task D1.7: resolve(renderer-leaves): util/elevated.ts (bluebird-trap risk + §3 Linux-guard)</name><files>src/renderer/src/util/elevated.ts</files><action>Apply shared_per_task_workflow. **Bluebird-trap risk file** — audit step REQUIRED. §3 Linux-guard fork-wins likely (HEAD-only Linux elevation path). Commit body MUST include both the bluebird-trap audit line AND a "§3 Linux-guard preserved: &lt;named-surface-or-none&gt;" line.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/util/elevated.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN; bluebird-trap audit line + Linux-guard preservation line both present.</done></task>

<task type="auto"><name>Task D1.8: resolve(renderer-leaves): util/util.objDiff.test.ts</name><files>src/renderer/src/util/util.objDiff.test.ts</files><action>Apply shared_per_task_workflow. Test file — closes D1 sub-batch.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/util/util.objDiff.test.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<!-- D2: reducers/* + hooks/* + store/* + telemetry/* (6 files) -->

<task type="auto"><name>Task D2.1: resolve(renderer-leaves): reducers/notifications.ts</name><files>src/renderer/src/reducers/notifications.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/reducers/notifications.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task D2.2: resolve(renderer-leaves): reducers/verify.test.ts</name><files>src/renderer/src/reducers/verify.test.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/reducers/verify.test.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task D2.3: resolve(renderer-leaves): hooks/windowControls.ts</name><files>src/renderer/src/hooks/windowControls.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/hooks/windowControls.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task D2.4: resolve(renderer-leaves): store/stateDiff.ts</name><files>src/renderer/src/store/stateDiff.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/store/stateDiff.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task D2.5: resolve(renderer-leaves): store/stateDiff.test.ts</name><files>src/renderer/src/store/stateDiff.test.ts</files><action>Apply shared_per_task_workflow. Imports stateDiff (D2.4).</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/store/stateDiff.test.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task D2.6: resolve(renderer-leaves): telemetry/selectors.ts</name><files>src/renderer/src/telemetry/selectors.ts</files><action>Apply shared_per_task_workflow. Closes D2 sub-batch.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/telemetry/selectors.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<!-- D3: contexts/* + ui/* + ui/README (6 files) -->

<task type="auto"><name>Task D3.1: resolve(renderer-leaves): contexts/PagesContext.tsx</name><files>src/renderer/src/contexts/PagesContext.tsx</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/contexts/PagesContext.tsx | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task D3.2: resolve(renderer-leaves): contexts/builtInPages.ts</name><files>src/renderer/src/contexts/builtInPages.ts</files><action>Apply shared_per_task_workflow. Imports PagesContext (D3.1).</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/contexts/builtInPages.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task D3.3: resolve(renderer-leaves): ui/README.md</name><files>src/renderer/src/ui/README.md</files><action>Apply shared_per_task_workflow. Doc-only file; smaller-diff default expected.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/ui/README.md | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task D3.4: resolve(renderer-leaves): ui/components/form/input/Input.tsx</name><files>src/renderer/src/ui/components/form/input/Input.tsx</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/ui/components/form/input/Input.tsx | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task D3.5: resolve(renderer-leaves): ui/components/form/formfield/FormField.tsx</name><files>src/renderer/src/ui/components/form/formfield/FormField.tsx</files><action>Apply shared_per_task_workflow. May import Input (D3.4).</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/ui/components/form/formfield/FormField.tsx | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task D3.6: resolve(renderer-leaves): ui/components/no_results/NoResults.tsx</name><files>src/renderer/src/ui/components/no_results/NoResults.tsx</files><action>Apply shared_per_task_workflow. Closes D3 sub-batch and Wave D file resolution.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/ui/components/no_results/NoResults.tsx | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto">
  <name>Task D21: wave-end bucket typecheck + SUMMARY</name>
  <files>.planning/phases/34-renderer-main-spine-v2-0-1/34-04-SUMMARY.md</files>
  <action>
1. Wave-end filtered bucket-scoped typecheck per D-34-06 (Wave E + F renderer files still conflicted): `pnpm tsc -p src/renderer/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | grep -vFf <(git grep -lE '^(<{7}|={7}|>{7})( |$)' src/renderer/) | wc -l` — MUST return `0`. If non-zero, surface errors and STOP.
2. Mid-wave-end count of files-still-bearing-markers in `src/renderer/`: `git grep -lE '^(<{7}|={7}|>{7})( |$)' src/renderer/ | wc -l` — record value (must equal pre-Wave-D count minus 20).
3. Harness skip-mode: 13/13 GREEN.
4. Write `.planning/phases/34-renderer-main-spine-v2-0-1/34-04-SUMMARY.md`:
   - Files resolved (20 total: D1=8, D2=6, D3=6) with per-file region tally + stance distribution
   - Bluebird-trap audit summary (2 risk files: `util/errorHandling.ts`, `util/elevated.ts` — outcome per file)
   - Linux-guard surfaces preserved (named — likely `util/elevated.ts` Linux elevation path)
   - Filtered bucket-typecheck output (`0 errors against Wave-D-resolved files`)
   - Pre/post Wave-D files-still-bearing-markers count (delta = -20)
   - Harness state (13/13 GREEN at wave end)
   - Atomic-commit list (20 resolves + this SUMMARY) with short SHAs
5. `git add -f`; commit `docs(phase-34): Wave D (renderer leaves) summary — 20 files, 0 typecheck errors`.
6. Verify SSH signature.
  </action>
  <verify><automated>pnpm tsc -p src/renderer/tsconfig.json --noEmit 2>&amp;1 | grep -v TS1185 | grep -vFf &lt;(git grep -lE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/ || true) | wc -l</automated></verify>
  <done>
- Filtered bucket typecheck returns `0`
- 34-04-SUMMARY.md exists with all required sections (esp. bluebird-trap audit + Linux-guard preservation)
- SUMMARY committed (force-added) and SSH-signed
- Wave D closed; downstream Wave E may proceed
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                |
| -------------------------- | -------------------------------------------------------------------------- |
| bluebird vs native Promise | TS1064 trap; runtime errors masked by type-check; project memory invariant |
| Linux platform guard       | HEAD-only Linux paths (esp. `util/elevated.ts`) must survive resolution    |

## STRIDE Threat Register

| Threat ID  | Category    | Component                                                                            | Disposition | Mitigation Plan                                                                                                                                  |
| ---------- | ----------- | ------------------------------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-34-04-01 | Tampering   | bluebird-importing async fn signatures (`util/errorHandling.ts`, `util/elevated.ts`) | mitigate    | Bluebird-trap audit line REQUIRED in commit bodies; explicit `:Promise<void>` revert when upstream side adds annotation to bluebird-importing fn |
| T-34-04-02 | Tampering   | Linux-guard branches in `util/elevated.ts`                                           | mitigate    | §3 Linux-guard fork-wins; preserved surfaces named in commit body                                                                                |
| T-34-04-03 | Repudiation | 21 atomic commits this wave                                                          | mitigate    | SSH signature verified per commit                                                                                                                |
| T-34-04-04 | Tampering   | Blanket `--theirs`/`--ours` regression risk (D-34-03)                                | mitigate    | Per-region hand-classification per D-34-02; no blanket checkout                                                                                  |
| T-34-04-SC | Tampering   | npm/pip/cargo installs                                                               | n/a         | No new package installs in this wave                                                                                                             |

</threat_model>

<verification>
- L1 markers: `git grep -nE '^(<{7}|={7}|>{7})( |$)' <each-file>` returns 0 lines for all 20 files
- L2 harness: `bash .planning/phases/34-renderer-main-spine-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check` exits 0 (13/13 GREEN)
- L3 bucket typecheck (filtered): 0 errors against Wave-D-resolved files
- Mid-wave files-still-bearing-markers count drops by exactly 20 in `src/renderer/`
- All 21 commits SSH-signed
- Bluebird-trap audit lines present in 2 risk-file commit bodies
</verification>

<success_criteria>

- 20 atomic resolve commits (D1=8, D2=6, D3=6) + 1 SUMMARY commit on `v8.1/config-bucket`
- Filtered bucket typecheck 0 non-marker errors
- Bluebird-trap audit clean for the 2 risk files
- Wave D closed; Wave E unblocked
  </success_criteria>

<output>
Write `.planning/phases/34-renderer-main-spine-v2-0-1/34-04-SUMMARY.md` capturing D1/D2/D3 sub-batch order, per-file region tally, bluebird-trap audit, Linux-guard preservation, filtered typecheck output, harness state, and commit SHAs.
</output>
