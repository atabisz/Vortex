---
phase: 34
plan: 01
type: execute
wave: A
status: ready
requires:
    - 34-00
depends_on:
    - 34-00
files_modified:
    - src/shared/src/types/errors.ts
    - src/shared/src/types/state.ts
    - src/shared/src/errors.ts
    - src/shared/src/telemetry/spans.ts
    - src/shared/src/errors.test.ts
    - .planning/phases/34-renderer-main-spine-v2-0-1/34-01-SUMMARY.md
autonomous: true
requirements:
    - SYNC-34a
must_haves:
    truths:
        - "All 5 shared spine files marker-free in leaf-first order: types/errors.ts → types/state.ts → errors.ts → telemetry/spans.ts → errors.test.ts"
        - "5 atomic SSH-signed `resolve(shared): <file> — <stance>` commits on `v8.1/config-bucket`"
        - "Wave-A-end bucket-scoped typecheck (D-34-06): `pnpm tsc -p src/shared/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l` returns 0"
        - "Harness skip-mode (13 gates) stays GREEN after every commit"
        - "HEAD-only fork-wins error classes `AlreadyDownloaded` and `DownloadIsHTML` preserved in src/shared/src/types/errors.ts (RESEARCH §Marker Scope Verification — Dominant change pattern)"
        - "All other named error classes (UserCanceled, TimeoutError, etc.) preserved across errors.ts and errors.test.ts (gate-15 skip rationale relies on commit-body discipline)"
        - "1 SUMMARY commit closes the wave with per-file region tally (fork-wins / upstream-wins / smaller-diff / Rule-1 dup-import) and bucket-typecheck count"
    artifacts:
        - path: "src/shared/src/types/errors.ts"
          provides: "IPC error type contract; HEAD-only fork-wins classes AlreadyDownloaded + DownloadIsHTML preserved"
          contains: "AlreadyDownloaded"
        - path: "src/shared/src/errors.ts"
          provides: "Sanitize/fingerprint helpers for error reporting; smaller-diff (HEAD = single-line, v2.0.1 = wrapped multi-line)"
          contains: "UserCanceled"
        - path: "src/shared/src/errors.test.ts"
          provides: "Vitest coverage for error sanitize/fingerprint behaviour; 6 conflict regions resolved with named-class invariants preserved"
          contains: "describe"
    key_links:
        - from: "src/shared/src/telemetry/spans.ts"
          to: "src/shared/src/errors.ts"
          via: "import"
          pattern: "from.*errors"
        - from: "src/shared/src/errors.test.ts"
          to: "src/shared/src/errors.ts"
          via: "vitest test import"
          pattern: "from.*errors"
        - from: "src/shared/src/types/state.ts"
          to: "src/shared/src/types/ipc"
          via: "import"
          pattern: "from.*ipc"
---

<objective>
Wave A. Resolve all 5 shared spine files leaf-first per RESEARCH §Wave-A Coupling. Shared is the foundational dependees of preload/main/renderer — get it clean and bucket-typechecking GREEN before any other wave touches IPC channel types or error classes.

Default stance per RESEARCH §Marker Scope Verification + D-34-02 6-tier hierarchy:

- `types/errors.ts`: **fork-wins** (preserves HEAD-only `AlreadyDownloaded` + `DownloadIsHTML` classes that v2.0.1 deletes — these are consumed by at least one downstream site identified in `nexus_integration/eventHandlers.ts` per RESEARCH §Marker Scope Verification — Dominant change pattern)
- `types/state.ts`: hand-resolve per region under D-34-02 stance hierarchy
- `errors.ts`, `telemetry/spans.ts`, `errors.test.ts`: smaller-diff (HEAD = single-line, v2.0.1 = wrapped multi-line per RESEARCH sample) UNLESS a region carries a named-class string assertion or sanitize/fingerprint behaviour change — in which case fork-wins for the test-side class-name invariant.

Purpose: shared spine is the dependees of all other Phase 34 waves. v8.0 Phase 28 precedent runs shared first so renderer + main + preload all see consistent type contracts. Gate-15 (error-class preservation) was skipped from the harness extension per RESEARCH §Validation Architecture; commit-body discipline + L1 zero-marker assertion + D-34-08 region tally is the substitute coverage. Every commit body in this wave MUST list which named classes were preserved.

Output: 5 SSH-signed `resolve(shared): ...` commits + 1 docs SUMMARY commit. All 13 harness gates GREEN at end. Bucket-scoped typecheck = 0 non-marker errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-renderer-main-spine-v2-0-1/34-CONTEXT.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-RESEARCH.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-VALIDATION.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-00-SUMMARY.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
@CLAUDE.md
@AGENTS.md
</context>

<files_in_scope>
| Order | File | Regions (approx) | Stance | Notes |
|-------|------|------------------|--------|-------|
| 1 | src/shared/src/types/errors.ts | ~1 | fork-wins | HEAD-only `AlreadyDownloaded` + `DownloadIsHTML` classes; v2.0.1 deletes; preserve. RESEARCH §Validation Architecture confirms classes live HERE (NOT in `src/shared/src/errors.ts` — researcher correction to D-34-04 / CONTEXT.md mention) |
| 2 | src/shared/src/types/state.ts | ~1 | smaller-diff | Per-region D-34-02; depends on `./ipc` (no marker) |
| 3 | src/shared/src/errors.ts | ~2 | smaller-diff | Sanitize/fingerprint helpers; HEAD = single-line, v2.0.1 = wrapped per RESEARCH sample |
| 4 | src/shared/src/telemetry/spans.ts | ~1 | smaller-diff | OpenTelemetry span definitions; consumer of `../errors` |
| 5 | src/shared/src/errors.test.ts | 6 | smaller-diff with named-class invariant fork-wins | Heaviest in shared; vitest test of `errors.ts`; any region with class-name string assertion → fork-wins |

**Total:** 5 files, ~10 regions per RESEARCH §"Region totals per bucket".

**Bucket typecheck command (D-34-06):** `pnpm tsc -p src/shared/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l` — must return 0 at wave end.

**Gate-15 skip substitute:** Each commit body MUST list which named error classes the resolution touched/preserved. The L1 zero-marker assertion + D-34-08 region tally is the coverage replacement (RESEARCH §Validation Architecture).
</files_in_scope>

<active_gates>
Per RESEARCH §Validation Architecture: 0 of 13 gates active during Wave A resolution (all 13 are passive). Gate-15 was the candidate for "named error class preservation" but was SKIPPED — coverage comes from commit-body discipline + L1.

- All harness assertions are post-commit only.
- D-34-10 single-host invariant: no `resolvePathCase(dataPath,…)` lines in shared; gate stays passively green.
  </active_gates>

<stance_application_order>
Per D-34-02 (6-tier hierarchy):

1. **Playbook-surface (§1/§3/§4–§5/§6/§7a–d/§10/140a57217):** fork-wins — N/A in Wave A.
2. **Linux platform guard (`process.platform === 'win32'` etc.):** fork-wins — none expected in shared.
3. **New v2.0.1 feature scaffolding outside playbook surface:** upstream-wins — possible in `errors.ts` sanitize-helper additions; per-region judgement.
4. **Rule-1 dup-import avoidance:** when upstream side merely duplicates an import already in HEAD → HEAD-empty.
5. **Rule-2 D1-carryover prevention:** none of the 5 shared files import bluebird per RESEARCH §Bluebird Promise Trap (none of the 7 risk files are shared/) — Rule-2 not active in Wave A.
6. **Default — smaller-diff (formatter reflow):** dominant pattern per RESEARCH samples.

**Special case for `types/errors.ts`:** the HEAD side carries fork-only error classes (`AlreadyDownloaded`, `DownloadIsHTML`) that v2.0.1 deletes. Per RESEARCH §Marker Scope Verification — Dominant change pattern, this is "fork-wins — preserves error contract used by 1+ consumer". Tier-1-equivalent (semantic preservation); the executor must NOT take the upstream side which deletes the classes.
</stance_application_order>

<shared_per_task_workflow>
For each file:

1. **Capture master analog** (Pattern P1):

    ```bash
    F=<full path>
    git show fork/master:$F > /tmp/$(basename $F).master 2>/dev/null || echo "(file new on master path)"
    ```

2. **Read current file + master analog side-by-side** to understand the conflict shape.

3. **Hand-resolve all regions** per the stance hierarchy. NO blanket `git checkout --ours/--theirs` (D-34-03).

4. **Verify zero markers**:

    ```bash
    git diff --check $F
    ! grep -q '^<<<<<<< ' $F
    ```

5. **Run harness** (all 13 gates):

    ```bash
    bash .planning/phases/34-renderer-main-spine-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check
    echo "exit=$?"
    ```

    Must exit 0.

6. **Bucket-scoped typecheck — DEFERRED to wave closeout commit** per D-34-06. On intermediate file commits, body records "deferred to wave closeout commit". On the LAST file (errors.test.ts), run:

    ```bash
    pnpm tsc -p src/shared/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l
    ```

    Must be 0. Record count in commit body.

7. **Stage + commit signed**:

    ```bash
    git add $F
    git commit -m "resolve(shared): $(basename $F) — <one-line stance>"
    ```

    Body per Pattern S5 / D-34-08: regions tallied (fork-wins / upstream-wins / smaller-diff / Rule-1 / Rule-2), playbook gates affected (none in Wave A), gates preserved (yes — list named error classes touched), grep-checkpoint.sh exit (0), bucket-typecheck status ("deferred" or actual count on wave closeout). SSH-sign per `feedback_ssh_signing.md`.

8. **Verify signed**: `git cat-file -p HEAD | grep -c '^gpgsig '` >= 1.

9. **DO NOT push** — `feedback_git_push_ssh.md`.

Per `feedback_minimize_upstream_diff.md`: do not reformat outside conflict regions.
Per `feedback_casual_voice.md`: casual project voice in commit body.
</shared_per_task_workflow>

<tasks>

<task type="auto">
  <name>Task 1: Resolve src/shared/src/types/errors.ts (fork-wins; preserve AlreadyDownloaded + DownloadIsHTML)</name>
  <files>src/shared/src/types/errors.ts</files>
  <read_first>
    - src/shared/src/types/errors.ts (current state with markers)
    - `git show fork/master:src/shared/src/types/errors.ts` (HEAD baseline)
    - `git show fork/sync/upstream-v2.0.1:src/shared/src/types/errors.ts` (upstream baseline — confirm it deletes the 2 classes)
    - .planning/phases/34-renderer-main-spine-v2-0-1/34-RESEARCH.md (§Marker Scope Verification — "Dominant change pattern" row for shared/types/errors.ts)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md (no playbook surface, but record "none" in commit body)
  </read_first>
  <action>
    Apply shared_per_task_workflow steps 1–9. Stance per RESEARCH §Marker Scope Verification: fork-wins — preserves the HEAD-only `AlreadyDownloaded` and `DownloadIsHTML` classes that v2.0.1 deletes. Confirm via `git show fork/sync/upstream-v2.0.1:<path>` that the upstream side does NOT contain these two class declarations; if upstream DOES contain them (researcher's reading was wrong), fall back to per-region D-34-02.

    Title: `resolve(shared): types/errors.ts — fork-wins (preserve AlreadyDownloaded + DownloadIsHTML classes deleted upstream)`

    Body must explicitly list:
    - Regions: 1 (or actual count)
    - Stance: fork-wins
    - Named classes preserved: AlreadyDownloaded, DownloadIsHTML, plus any other class names present in this file
    - Playbook gates: none affected (no §1/§3/§4–§5/§6/§7/§10/140a57217 surface in this file)
    - Bucket typecheck: deferred to wave closeout commit (errors.test.ts)
    - grep-checkpoint.sh skip-mode exit: 0

    SSH-signed; --no-verify NOT used.

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      ! grep -q '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/shared/src/types/errors.ts && \
      grep -q '^export class AlreadyDownloaded' src/shared/src/types/errors.ts && \
      grep -q '^export class DownloadIsHTML' src/shared/src/types/errors.ts && \
      bash .planning/phases/34-renderer-main-spine-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check &gt;/dev/null && \
      git log -1 --pretty=%s | grep -q '^resolve(shared): types/errors.ts' && \
      git cat-file -p HEAD | grep -q '^gpgsig ' && \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - File marker-free
    - AlreadyDownloaded and DownloadIsHTML classes still present (verified by grep)
    - Single SSH-signed `resolve(shared): types/errors.ts — ...` commit
    - Body lists fork-wins stance + named classes preserved + gates affected (none) + bucket typecheck deferred
    - Harness skip-mode exit 0
  </acceptance_criteria>
  <done>types/errors.ts resolved with fork-only classes preserved; 1 signed commit on branch.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve src/shared/src/types/state.ts</name>
  <files>src/shared/src/types/state.ts</files>
  <read_first>
    - src/shared/src/types/state.ts (current with markers)
    - `git show fork/master:src/shared/src/types/state.ts`
    - `git show fork/sync/upstream-v2.0.1:src/shared/src/types/state.ts`
    - src/shared/src/types/ipc.ts (referenced via `./ipc`; verify no marker present)
  </read_first>
  <action>
    Apply shared_per_task_workflow steps 1–9. Stance: per-region D-34-02 hierarchy. Most likely smaller-diff (HEAD-wins for line-wrap reflows). If a region adds a state-shape field that is part of v2.0.1 feature scaffolding, take upstream-wins (tier-3) for that region.

    Title: `resolve(shared): types/state.ts — <one-line stance per dominant region>`

    Body lists region tally, gates affected (none), bucket typecheck deferred. SSH-signed.

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      ! grep -q '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/shared/src/types/state.ts && \
      bash .planning/phases/34-renderer-main-spine-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check &gt;/dev/null && \
      git log -1 --pretty=%s | grep -q '^resolve(shared): types/state.ts' && \
      git cat-file -p HEAD | grep -q '^gpgsig ' && \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - File marker-free
    - 1 SSH-signed `resolve(shared): types/state.ts — ...` commit
    - Body documents per-region stance with tally
    - Harness skip-mode exit 0
  </acceptance_criteria>
  <done>types/state.ts resolved; 1 signed commit on branch.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve src/shared/src/errors.ts (smaller-diff; preserve named classes)</name>
  <files>src/shared/src/errors.ts</files>
  <read_first>
    - src/shared/src/errors.ts (current with markers)
    - `git show fork/master:src/shared/src/errors.ts`
    - `git show fork/sync/upstream-v2.0.1:src/shared/src/errors.ts`
    - .planning/phases/34-renderer-main-spine-v2-0-1/34-RESEARCH.md (§Marker Scope Verification "Dominant change pattern" — HEAD = single-line, v2.0.1 = wrapped multi-line)
  </read_first>
  <action>
    Apply shared_per_task_workflow steps 1–9. Default stance: smaller-diff (HEAD-wins) for formatter reflow regions per RESEARCH sample.

    Verify via grep that the file's named classes (`UserCanceled`, `TimeoutError`, plus any others) are still present after resolution. List them in commit body (gate-15 skip substitute).

    Title: `resolve(shared): errors.ts — smaller-diff on formatter reflow regions`

    Body lists region tally, named classes preserved (gate-15 skip substitute), gates affected (none), bucket typecheck deferred. SSH-signed.

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      ! grep -q '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/shared/src/errors.ts && \
      bash .planning/phases/34-renderer-main-spine-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check &gt;/dev/null && \
      git log -1 --pretty=%s | grep -q '^resolve(shared): errors.ts' && \
      git cat-file -p HEAD | grep -q '^gpgsig ' && \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - File marker-free
    - 1 SSH-signed `resolve(shared): errors.ts — ...` commit
    - Body lists named classes preserved + region tally
    - Harness skip-mode exit 0
  </acceptance_criteria>
  <done>errors.ts resolved; 1 signed commit on branch.</done>
</task>

<task type="auto">
  <name>Task 4: Resolve src/shared/src/telemetry/spans.ts</name>
  <files>src/shared/src/telemetry/spans.ts</files>
  <read_first>
    - src/shared/src/telemetry/spans.ts (current with markers)
    - `git show fork/master:src/shared/src/telemetry/spans.ts`
    - `git show fork/sync/upstream-v2.0.1:src/shared/src/telemetry/spans.ts`
    - src/shared/src/errors.ts (just-resolved; spans.ts imports from here)
  </read_first>
  <action>
    Apply shared_per_task_workflow steps 1–9. Stance: smaller-diff default. If a region adds a new span name that is part of v2.0.1 feature scaffolding, upstream-wins (tier-3).

    Title: `resolve(shared): telemetry/spans.ts — <one-line stance>`

    Body lists region tally, gates affected (none), bucket typecheck deferred. SSH-signed.

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      ! grep -q '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/shared/src/telemetry/spans.ts && \
      bash .planning/phases/34-renderer-main-spine-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check &gt;/dev/null && \
      git log -1 --pretty=%s | grep -q '^resolve(shared): telemetry/spans.ts' && \
      git cat-file -p HEAD | grep -q '^gpgsig ' && \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - File marker-free
    - 1 SSH-signed `resolve(shared): telemetry/spans.ts — ...` commit
    - Body documents region tally, bucket typecheck deferred
    - Harness skip-mode exit 0
  </acceptance_criteria>
  <done>telemetry/spans.ts resolved; 1 signed commit on branch.</done>
</task>

<task type="auto">
  <name>Task 5: Resolve src/shared/src/errors.test.ts (last; runs Wave-A bucket typecheck closeout)</name>
  <files>src/shared/src/errors.test.ts</files>
  <read_first>
    - src/shared/src/errors.test.ts (current with markers; 6 regions — heaviest in shared)
    - `git show fork/master:src/shared/src/errors.test.ts`
    - `git show fork/sync/upstream-v2.0.1:src/shared/src/errors.test.ts`
    - src/shared/src/errors.ts (just-resolved; test target)
  </read_first>
  <action>
    Apply shared_per_task_workflow steps 1–9. 6 regions per RESEARCH §"Top region density". Per-region stance:
    - **Class-name string assertions (any region with a `expect(...).toBe('UserCanceled')` or similar):** fork-wins (preserve test invariant against HEAD-only class names)
    - **Other regions:** smaller-diff (formatter reflow)

    **Wave-A bucket typecheck closeout (D-34-06):**
    ```bash
    pnpm tsc -p src/shared/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l
    ```
    Must be 0. Record exact count in commit body. If non-zero, investigate (likely a region was mis-resolved); fix and amend. If errors are pre-existing (not introduced by Phase 34), document as "Pre-existing — not introduced by Phase 34" and proceed (Phase 28 / Phase 33 done-gate precedent).

    Title: `resolve(shared): errors.test.ts — smaller-diff with named-class invariant fork-wins (Wave A closeout, bucket typecheck = 0)`

    Body lists 6-region tally, named classes preserved across the test, gates affected (none), bucket typecheck count (0). SSH-signed.

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      ! grep -q '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/shared/src/errors.test.ts && \
      ! git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/shared/ && \
      bash .planning/phases/34-renderer-main-spine-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check &gt;/dev/null && \
      TC=$(pnpm tsc -p src/shared/tsconfig.json --noEmit 2&gt;&amp;1 | grep -v TS1185 | wc -l) && \
      test "$TC" -eq 0 && \
      git log -1 --pretty=%s | grep -q '^resolve(shared): errors.test.ts' && \
      git cat-file -p HEAD | grep -q '^gpgsig ' && \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - File marker-free
    - Zero remaining markers anywhere under src/shared/
    - 1 SSH-signed `resolve(shared): errors.test.ts — ...` commit (Wave-A closeout)
    - Body documents 6-region tally + named-class preservation + bucket typecheck count = 0
    - Harness skip-mode exit 0
    - `pnpm tsc -p src/shared/tsconfig.json --noEmit | grep -v TS1185` returns 0
  </acceptance_criteria>
  <done>errors.test.ts resolved + Wave-A bucket typecheck GREEN; 1 signed commit on branch. Wave A complete (5/5 files).</done>
</task>

<task type="auto">
  <name>Task 6: Wave A SUMMARY commit</name>
  <files>.planning/phases/34-renderer-main-spine-v2-0-1/34-01-SUMMARY.md</files>
  <read_first>
    - All 5 commit messages: `git log --oneline 34-00..HEAD -- src/shared/`
    - $HOME/.claude/get-shit-done/templates/summary.md
    - .planning/phases/34-renderer-main-spine-v2-0-1/34-00-SUMMARY.md (Wave 0 baseline)
  </read_first>
  <action>
    Write `34-01-SUMMARY.md` summarising Wave A. Required sections:
    - **Outcome:** 5/5 shared spine files resolved; 5 atomic resolution commits + 1 docs commit on `v8.1/config-bucket`
    - **Per-file table:** path, regions, fork-wins count, upstream-wins count, smaller-diff count, Rule-1 dup-import count, named classes preserved
    - **Bucket typecheck:** `pnpm tsc -p src/shared/tsconfig.json --noEmit | grep -v TS1185 | wc -l` = 0 at errors.test.ts commit
    - **Harness state:** all 13 gates GREEN in skip-mode after the 5th commit; gate-15 skip substitute exercised via per-file class-preservation entries in commit bodies
    - **Affects:** Wave B (preload IPC bridge consumes shared/types/ipc), Wave C (main spine consumes shared/errors + shared/telemetry/spans), Wave D–F (renderer consumes everything)
    - **Provides:** 5 fully resolved shared spine files; AlreadyDownloaded + DownloadIsHTML classes preserved for downstream Wave-E consumer in nexus_integration/eventHandlers.ts
    - **Issues encountered:** any commit needing amend, any bucket-typecheck pre-existing errors

    Add via `git add -f`. Title: `docs(34-01): summarize Wave A shared spine resolution (5 files, 5 commits, bucket typecheck = 0)`.

  </action>
  <verify>
    <automated>
      cd /home/alex/src/Vortex && \
      test -f .planning/phases/34-renderer-main-spine-v2-0-1/34-01-SUMMARY.md && \
      bash .planning/phases/34-renderer-main-spine-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check &gt;/dev/null && \
      git log -1 --pretty=%s | grep -q '^docs(34-01): summarize Wave A' && \
      git cat-file -p HEAD | grep -q '^gpgsig ' && \
      echo OK
    </automated>
  </verify>
  <acceptance_criteria>
    - 34-01-SUMMARY.md exists, committed via `git add -f`, SSH-signed
    - 5 resolve commits + 1 docs commit on Wave A
    - Final harness skip-mode exits 0 with 13/13 gates GREEN
    - Conflict-marker files in src/shared/: 0
  </acceptance_criteria>
  <done>Wave A complete; summary committed; branch ready for Wave B.</done>
</task>

</tasks>

<verification>
After all tasks:
- `git grep -l '^<<<<<<< ' src/shared/` returns empty
- `bash .planning/phases/34-renderer-main-spine-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check` exits 0
- `pnpm tsc -p src/shared/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l` returns 0
- 5 + 1 SSH-signed commits on `v8.1/config-bucket`
- AlreadyDownloaded + DownloadIsHTML classes still present in src/shared/src/types/errors.ts
</verification>

<success_criteria>

- 5 shared spine files fully resolved
- 5 SSH-signed `resolve(shared): ...` commits + 1 SUMMARY commit
- Harness 13/13 gates GREEN
- Bucket typecheck = 0 non-marker errors
- Wave B unblocked (preload)

</success_criteria>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                         |
| ------------------------------------ | ------------------------------------------------------------------- |
| HEAD ↔ v2.0.1                        | Conflict regions; per-region stance hierarchy is the integrity gate |
| shared types → renderer/main/preload | Type contract; downstream waves rely on the resolved shape          |

## STRIDE Threat Register

| Threat ID  | Category    | Component                      | Disposition | Mitigation Plan                                                                                                         |
| ---------- | ----------- | ------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------- |
| T-34-01-01 | Tampering   | src/shared/src/types/errors.ts | mitigate    | fork-wins to preserve AlreadyDownloaded + DownloadIsHTML classes; verified by post-resolve grep + commit body           |
| T-34-01-02 | Tampering   | src/shared/src/errors.test.ts  | mitigate    | named-class invariants preserved via fork-wins on assertion regions; bucket typecheck = 0 confirms no test/source drift |
| T-34-01-03 | Repudiation | atomic commits                 | mitigate    | Each file = 1 SSH-signed commit per D-34-07; verify via `git cat-file -p HEAD \| grep -c '^gpgsig '` >= 1               |

</threat_model>

<output>
Create `.planning/phases/34-renderer-main-spine-v2-0-1/34-01-SUMMARY.md` per Task 6.
</output>
</content>
</invoke>
