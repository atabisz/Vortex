---
phase: 34
plan: 06
type: execute
wave: F
status: ready
requires:
    - 34-00
    - 34-01
    - 34-02
    - 34-03
    - 34-04
    - 34-05
depends_on:
    - 34-00
    - 34-01
    - 34-02
    - 34-03
    - 34-04
    - 34-05
files_modified:
    - src/renderer/src/views/components/Header/IconButton.tsx
    - src/renderer/src/views/components/Header/Notifications/useNotificationFiltering.ts
    - src/renderer/src/views/components/Menu/DownloadsMenuContent.tsx
    - src/renderer/src/views/components/Menu/ToolsSection.tsx
    - src/renderer/src/views/components/Menu/useTools.ts
    - src/renderer/src/views/components/Spine/GameButton.tsx
    - src/renderer/src/views/components/Spine/SpineContext.tsx
    - src/renderer/src/views/components/Spine/index.tsx
    - src/renderer/src/views/components/Spine/utils.ts
    - src/renderer/src/views/layout/ToastContainer.tsx
    - src/renderer/src/views/pages/Tools/ToolRow.tsx
    - src/renderer/src/views/pages/Tools/index.tsx
    - src/renderer/src/views/pages/Tools/toolStarters.ts
    - src/renderer/src/views/pages/Tools/useToolsData.ts
    - src/renderer/src/views/pages/Tools/useToolsPage.ts
    - src/renderer/src/controls/Table.tsx
    - src/renderer/src/ExtensionManager.ts
    - src/renderer/src/renderer.tsx
    - pnpm-workspace.yaml
    - .planning/phases/34-renderer-main-spine-v2-0-1/34-06-SUMMARY.md
autonomous: true
requirements:
    - SYNC-34a
must_haves:
    truths:
        - "All 18 renderer views/pages + the three heaviest renderer-bucket files marker-free, resolved in three sub-batches per CONTEXT D-34-01 + D-34-12: F1 (views/components — 10 parallel-safe across siblings) → F2 (views/pages/Tools/ — 5 sequential leaf-first) → F3 (controls/Table.tsx → ExtensionManager.ts → renderer.tsx, sequential heaviest)"
        - "18 atomic SSH-signed `resolve(renderer-views): <relative-path> — <stance>` commits on `v8.1/config-bucket`"
        - "renderer.tsx is LAST in F3 because the D-34-17 native-errors trigger evaluation depends on its import-block resolution. Three-branch decision tree on `nativeErr` import resolution: (a) HEAD-empty (upstream import dropped) → no `pnpm-workspace.yaml` edit needed; (b) upstream-wins (`nativeErr` survives import block) → `pnpm-workspace.yaml` `catalog:` re-add of `native-errors` lands in same commit per executor judgment; (c) split into a follow-up `chore(workspace): re-add native-errors catalog entry` commit if (b) AND `pnpm install --frozen-lockfile=false` regen produces a noisy lockfile diff. Decision recorded in F3.3 commit body."
        - "Bluebird trap audit (project memory) clean for the 2 risk files in this wave: `ExtensionManager.ts`, `renderer.tsx`"
        - "No blanket `git checkout --ours` / `--theirs` per D-34-03 — every region hand-classified"
        - "Wave-F-end bucket-scoped typecheck (D-34-06) — Wave F closes the renderer bucket; UNFILTERED `pnpm tsc -p src/renderer/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l` returns 0"
        - "Mid-wave files-still-bearing-markers filter passes: `git grep -lE '^(<{7}|={7}|>{7})( |$)' src/renderer/` reaches 0 by wave end"
        - "Harness skip-mode (13 gates) stays GREEN after every commit"
        - "1 SUMMARY commit closes the wave with per-file region tally + D-34-17 trigger decision + UNFILTERED bucket-typecheck count"
    artifacts:
        - path: "src/renderer/src/renderer.tsx"
          provides: "Renderer process entrypoint — D-34-17 native-errors trigger site"
          contains: "ReactDOM"
        - path: "src/renderer/src/ExtensionManager.ts"
          provides: "Extension loader / lifecycle owner — bluebird-trap risk file"
          contains: "Promise"
        - path: "src/renderer/src/controls/Table.tsx"
          provides: "Table primitive consumed by all *Page.tsx surfaces"
          contains: "class Table"
    key_links:
        - from: "src/renderer/src/renderer.tsx"
          to: "src/renderer/src/ExtensionManager.ts"
          via: "import"
          pattern: "from.*ExtensionManager"
        - from: "src/renderer/src/renderer.tsx"
          to: "native-errors workspace catalog (pnpm-workspace.yaml)"
          via: "import * as nativeErr — D-34-17 trigger"
          pattern: "import.*nativeErr.*native-errors"
        - from: "src/renderer/src/views/components/Spine/index.tsx"
          to: "src/renderer/src/views/components/Spine/SpineContext.tsx"
          via: "import"
          pattern: "from.*SpineContext"
---

<objective>
Wave F. Resolve all 18 renderer views/pages files + the three heaviest renderer-bucket files in three sub-batches per CONTEXT D-34-01 + D-34-12:

- **F1 — views/components (10 files, parallel-safe across siblings per D-34-12)**:
    - views/components/Header/IconButton.tsx
    - views/components/Header/Notifications/useNotificationFiltering.ts
    - views/components/Menu/{DownloadsMenuContent.tsx, ToolsSection.tsx, useTools.ts} (3)
    - views/components/Spine/{GameButton.tsx, SpineContext.tsx, index.tsx, utils.ts} (4 — leaf-first within Spine: utils → SpineContext → GameButton → index)
    - views/layout/ToastContainer.tsx
- **F2 — views/pages/Tools/ (5 files, sequential leaf-first within Tools/)**: toolStarters.ts → useToolsData.ts → useToolsPage.ts → ToolRow.tsx → index.tsx.
- **F3 — three heaviest sequential**: controls/Table.tsx → ExtensionManager.ts → renderer.tsx. **renderer.tsx is LAST** because D-34-17 native-errors trigger evaluation depends on its import-block resolution.

Default stance per D-34-02 6-tier hierarchy:

- §3 Linux-guard fork-wins for any HEAD-only Linux platform branches in renderer.tsx (boot path), ExtensionManager.ts (extension loader)
- v2.0.1 feature upstream-wins for new view component props, new menu sections, new Tools page layouts
- Rule-1 dup-import HEAD-empty (very common across views — esp. controls/Table.tsx is the heaviest by region count)
- smaller-diff default for whitespace/comment-only regions
- D-34-03: no blanket `git checkout --ours` / `--theirs` — every region hand-classified

**Bluebird-trap risk files in this wave (per RESEARCH "Bluebird Promise Trap")**: `ExtensionManager.ts`, `renderer.tsx`. Per-task audit pattern (same as Plan 04):

1. After resolving the file, run `grep -E ':\s*Promise<void>' <file>`.
2. Cross-check `grep -E '^import.*[Pp]romise.*from.*bluebird' <file>` — if bluebird is imported AND the annotation came from upstream side, REVERT in same commit.
3. Commit body MUST contain "bluebird-trap audit" line per Plan 04 protocol.

**D-34-17 native-errors trigger (encoded in F3.3, `renderer.tsx`)**:

`renderer.tsx` contains a deferred `import * as nativeErr from "native-errors"` site (CONTEXT D-34-17, RESEARCH §"Phase 33-F Deferral"). On resolving this file's import block, the executor takes one of three branches:

- **(a) HEAD-empty (upstream side dropped the import)**: no `pnpm-workspace.yaml` edit. Commit body notes "D-34-17: nativeErr import dropped — no catalog re-add needed".
- **(b) upstream-wins (`nativeErr` survives the import block)**: `pnpm-workspace.yaml` `catalog:` block gets `native-errors` re-added in the SAME commit as `renderer.tsx`. Run `pnpm install --frozen-lockfile=false` and stage `pnpm-lock.yaml` regen if needed. Commit body notes "D-34-17: nativeErr survived — catalog re-added in same commit".
- **(c) upstream-wins BUT lockfile regen is noisy**: split into a follow-up `chore(workspace): re-add native-errors catalog entry` commit landing immediately after F3.3. Commit body notes "D-34-17: nativeErr survived — catalog re-add deferred to follow-up commit".

Per D-34-06: bucket-scoped typecheck runs at WAVE END (Task F19). Wave F closes the renderer bucket — UNFILTERED typecheck `pnpm tsc -p src/renderer/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l` MUST return 0 (no marker-bearing files left in `src/renderer/` at this point).

Mid-wave verification: `git grep -lE '^(<{7}|={7}|>{7})( |$)' src/renderer/` reaches 0 by wave end.

Output: 18 SSH-signed `resolve(renderer-views): ...` commits in F1→F2→F3 order (+ optional follow-up `chore(workspace)` commit if branch (c)) + 1 SUMMARY commit. All 13 harness gates GREEN at end. UNFILTERED bucket-scoped typecheck = 0 errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-renderer-main-spine-v2-0-1/34-CONTEXT.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-RESEARCH.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-VALIDATION.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-00-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-01-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-02-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-03-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-04-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-05-SUMMARY.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
@CLAUDE.md
@AGENTS.md
</context>

<read_first>

- `.planning/phases/34-renderer-main-spine-v2-0-1/34-CONTEXT.md` — D-34-01 sub-batches, D-34-02 6-tier, D-34-03, D-34-12 parallelism, D-34-17 native-errors trigger
- `.planning/phases/34-renderer-main-spine-v2-0-1/34-RESEARCH.md` — Bluebird Promise Trap section (2 risk files this wave) + Phase 33-F deferral notes
- `pnpm-workspace.yaml` — `catalog:` block (D-34-17 candidate edit site)
- `~/.claude/projects/-home-alex-src-Vortex/memory/feedback_bluebird_promise_trap.md`
- Each conflicted file before resolution
  </read_first>

<shared_per_task_workflow>
For every Task F1.x / F2.x / F3.x:

1. `git diff --check <file>` to enumerate region count.
2. Classify each region by D-34-02 6-tier order. NEVER use blanket `--theirs`/`--ours` (D-34-03).
3. Hand-resolve. Do NOT reformat outside conflict regions.
4. **Bluebird-trap audit (if file is in risk list — `ExtensionManager.ts`, `renderer.tsx`)**: include audit line in commit body per Plan 04 protocol.
5. Markers gone: `git grep -nE '^(<{7}|={7}|>{7})( |$)' <file>` returns nothing.
6. Harness skip-mode: 13/13 GREEN.
7. Mid-wave files-still-bearing-markers count: `git grep -lE '^(<{7}|={7}|>{7})( |$)' src/renderer/ | wc -l` — record value, must monotonically decrease to 0.
8. `git add <file>`; commit `resolve(renderer-views): <relative-path> — <stance>`.
9. Pattern S5 body (per D-34-08): regions tally per stance tier, bluebird-trap audit (if applicable), Linux-guard preservation (if applicable), `13/13 GREEN`, harness `0`, typecheck `deferred to wave end (D-34-06)`.
10. SSH signature verified.
    </shared_per_task_workflow>

<tasks>

<!-- F1: views/components (10 files, parallel-safe across siblings) -->

<task type="auto"><name>Task F1.1: resolve(renderer-views): views/components/Header/IconButton.tsx</name><files>src/renderer/src/views/components/Header/IconButton.tsx</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/views/components/Header/IconButton.tsx | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task F1.2: resolve(renderer-views): views/components/Header/Notifications/useNotificationFiltering.ts</name><files>src/renderer/src/views/components/Header/Notifications/useNotificationFiltering.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/views/components/Header/Notifications/useNotificationFiltering.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task F1.3: resolve(renderer-views): views/components/Menu/useTools.ts</name><files>src/renderer/src/views/components/Menu/useTools.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/views/components/Menu/useTools.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task F1.4: resolve(renderer-views): views/components/Menu/ToolsSection.tsx</name><files>src/renderer/src/views/components/Menu/ToolsSection.tsx</files><action>Apply shared_per_task_workflow. Imports useTools (F1.3).</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/views/components/Menu/ToolsSection.tsx | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task F1.5: resolve(renderer-views): views/components/Menu/DownloadsMenuContent.tsx</name><files>src/renderer/src/views/components/Menu/DownloadsMenuContent.tsx</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/views/components/Menu/DownloadsMenuContent.tsx | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task F1.6: resolve(renderer-views): views/components/Spine/utils.ts</name><files>src/renderer/src/views/components/Spine/utils.ts</files><action>Apply shared_per_task_workflow. Spine sub-cluster leaf.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/views/components/Spine/utils.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task F1.7: resolve(renderer-views): views/components/Spine/SpineContext.tsx</name><files>src/renderer/src/views/components/Spine/SpineContext.tsx</files><action>Apply shared_per_task_workflow. Imports utils (F1.6).</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/views/components/Spine/SpineContext.tsx | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task F1.8: resolve(renderer-views): views/components/Spine/GameButton.tsx</name><files>src/renderer/src/views/components/Spine/GameButton.tsx</files><action>Apply shared_per_task_workflow. May import SpineContext (F1.7).</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/views/components/Spine/GameButton.tsx | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task F1.9: resolve(renderer-views): views/components/Spine/index.tsx</name><files>src/renderer/src/views/components/Spine/index.tsx</files><action>Apply shared_per_task_workflow. Imports SpineContext (F1.7) + GameButton (F1.8). Closes Spine sub-cluster.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/views/components/Spine/index.tsx | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task F1.10: resolve(renderer-views): views/layout/ToastContainer.tsx</name><files>src/renderer/src/views/layout/ToastContainer.tsx</files><action>Apply shared_per_task_workflow. Closes F1 sub-batch.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/views/layout/ToastContainer.tsx | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<!-- F2: views/pages/Tools/ (5 files, sequential leaf-first) -->

<task type="auto"><name>Task F2.1: resolve(renderer-views): views/pages/Tools/toolStarters.ts</name><files>src/renderer/src/views/pages/Tools/toolStarters.ts</files><action>Apply shared_per_task_workflow. Tools/ leaf.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/views/pages/Tools/toolStarters.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task F2.2: resolve(renderer-views): views/pages/Tools/useToolsData.ts</name><files>src/renderer/src/views/pages/Tools/useToolsData.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/views/pages/Tools/useToolsData.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task F2.3: resolve(renderer-views): views/pages/Tools/useToolsPage.ts</name><files>src/renderer/src/views/pages/Tools/useToolsPage.ts</files><action>Apply shared_per_task_workflow. Imports useToolsData (F2.2).</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/views/pages/Tools/useToolsPage.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task F2.4: resolve(renderer-views): views/pages/Tools/ToolRow.tsx</name><files>src/renderer/src/views/pages/Tools/ToolRow.tsx</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/views/pages/Tools/ToolRow.tsx | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task F2.5: resolve(renderer-views): views/pages/Tools/index.tsx</name><files>src/renderer/src/views/pages/Tools/index.tsx</files><action>Apply shared_per_task_workflow. Imports useToolsPage (F2.3) + ToolRow (F2.4). Closes F2 sub-batch.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/views/pages/Tools/index.tsx | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<!-- F3: three heaviest sequential — Table → ExtensionManager → renderer.tsx (renderer.tsx LAST per D-34-17) -->

<task type="auto"><name>Task F3.1: resolve(renderer-views): controls/Table.tsx</name><files>src/renderer/src/controls/Table.tsx</files><action>Apply shared_per_task_workflow. Heaviest views file by region count — leaf-first within F3 (consumed by Tools/index and other \*Page.tsx surfaces).</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/controls/Table.tsx | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task F3.2: resolve(renderer-views): ExtensionManager.ts (bluebird-trap risk)</name><files>src/renderer/src/ExtensionManager.ts</files><action>Apply shared_per_task_workflow. **Bluebird-trap risk file** — audit step REQUIRED per Plan 04 protocol. §3 Linux-guard preservation possible (extension loader path).</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/ExtensionManager.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN; bluebird-trap audit line in body.</done></task>

<task type="auto">
  <name>Task F3.3: resolve(renderer-views): renderer.tsx (bluebird-trap risk + D-34-17 native-errors trigger)</name>
  <files>
    src/renderer/src/renderer.tsx
    pnpm-workspace.yaml
  </files>
  <action>
1. Apply shared_per_task_workflow on `renderer.tsx`. **Bluebird-trap risk file** — audit step REQUIRED per Plan 04 protocol.
2. **D-34-17 trigger evaluation** — when resolving the import block region containing `import * as nativeErr from "native-errors"`, take ONE of three branches:
   - **(a) HEAD-empty branch**: HEAD side already drops the import (Phase 33-F deferred this). Resolve as HEAD-empty. NO `pnpm-workspace.yaml` edit. Commit body MUST contain: "D-34-17: nativeErr import dropped (HEAD-empty) — no catalog re-add needed".
   - **(b) upstream-wins same-commit**: upstream side keeps `nativeErr` import; the import survives resolution. Edit `pnpm-workspace.yaml` `catalog:` block to re-add `native-errors` entry (mirror Phase 31 surgical-edit pattern from CONTEXT). Run `pnpm install --frozen-lockfile=false`. If `pnpm-lock.yaml` diff is small/clean, stage it in the SAME commit. Commit body MUST contain: "D-34-17: nativeErr survived — catalog re-added in same commit (lockfile regen included)".
   - **(c) upstream-wins follow-up commit**: same as (b) but lockfile regen is noisy. Resolve `renderer.tsx` alone in this commit. Commit body MUST contain: "D-34-17: nativeErr survived — catalog re-add deferred to follow-up". Then create a SECOND commit titled `chore(workspace): re-add native-errors catalog entry` containing the `pnpm-workspace.yaml` + `pnpm-lock.yaml` edits.
3. After resolution + (optional) catalog re-add: harness skip-mode 13/13 GREEN; markers gone in `renderer.tsx` AND any edited config files.
4. Stage with `git add src/renderer/src/renderer.tsx [pnpm-workspace.yaml pnpm-lock.yaml]` per chosen branch.
5. Commit `resolve(renderer-views): renderer.tsx — &lt;stance&gt;` with the D-34-17 decision line in body. If branch (c), follow with the second `chore(workspace):` commit.
6. SSH signature verified on each commit.
  </action>
  <verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/renderer/src/renderer.tsx pnpm-workspace.yaml | wc -l</automated></verify>
  <done>
- `renderer.tsx` marker-free
- `pnpm-workspace.yaml` edited (or not) per chosen D-34-17 branch
- Commit body records the D-34-17 decision branch (a/b/c) explicitly
- SSH-signed commit(s); 13/13 GREEN
  </done>
</task>

<task type="auto">
  <name>Task F19: wave-end UNFILTERED bucket typecheck + SUMMARY</name>
  <files>.planning/phases/34-renderer-main-spine-v2-0-1/34-06-SUMMARY.md</files>
  <action>
1. Wave-end UNFILTERED renderer bucket-scoped typecheck per D-34-06 (Wave F closes the renderer bucket — no marker-bearing files left in `src/renderer/`): `pnpm tsc -p src/renderer/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l` — MUST return `0`. If non-zero, surface errors and STOP.
2. Confirm `git grep -lE '^(<{7}|={7}|>{7})( |$)' src/renderer/ | wc -l == 0` — renderer bucket fully marker-free.
3. Harness skip-mode: 13/13 GREEN.
4. Write `.planning/phases/34-renderer-main-spine-v2-0-1/34-06-SUMMARY.md`:
   - Files resolved (18 total: F1=10, F2=5, F3=3) with per-file region tally + stance distribution
   - D-34-17 trigger decision (which of (a)/(b)/(c) was taken; whether `pnpm-workspace.yaml` was edited; lockfile regen line if any)
   - Bluebird-trap audit summary (2 risk files: `ExtensionManager.ts`, `renderer.tsx` — outcome per file)
   - Linux-guard surfaces preserved
   - UNFILTERED bucket-typecheck output (`0 errors`)
   - Renderer bucket marker count (`0 files`)
   - Harness state (13/13 GREEN at wave end)
   - Atomic-commit list (18 resolves + optional `chore(workspace)` if branch (c) + this SUMMARY) with short SHAs
5. `git add -f`; commit `docs(phase-34): Wave F (renderer views/pages + heaviest) summary — 18 files, 0 typecheck errors`.
6. Verify SSH signature.
  </action>
  <verify><automated>pnpm tsc -p src/renderer/tsconfig.json --noEmit 2>&amp;1 | grep -v TS1185 | wc -l</automated></verify>
  <done>
- UNFILTERED bucket typecheck returns `0`
- `src/renderer/` is fully marker-free
- 34-06-SUMMARY.md exists with all required sections (esp. D-34-17 decision branch + bluebird-trap audit)
- SUMMARY committed (force-added) and SSH-signed
- Wave F closed; downstream Wave G may proceed
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                                   |
| ----------------------------------------- | ----------------------------------------------------------------------------- |
| renderer process boot path (renderer.tsx) | D-34-17 native-errors import resolution gates workspace catalog state         |
| bluebird vs native Promise                | TS1064 trap (2 risk files this wave); project memory invariant                |
| Linux platform guard                      | HEAD-only Linux paths in renderer.tsx + ExtensionManager.ts boot/loader paths |

## STRIDE Threat Register

| Threat ID  | Category    | Component                                                                              | Disposition | Mitigation Plan                                                                                                                                                       |
| ---------- | ----------- | -------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-34-06-01 | Tampering   | D-34-17 native-errors import resolution in renderer.tsx                                | mitigate    | Three-branch decision tree encoded in F3.3; commit body records the chosen branch and any pnpm-workspace.yaml edits                                                   |
| T-34-06-02 | Tampering   | bluebird-importing async fns in 2 risk files                                           | mitigate    | Bluebird-trap audit line REQUIRED per risk file (F3.2, F3.3)                                                                                                          |
| T-34-06-03 | Tampering   | Linux-guard surfaces in renderer.tsx + ExtensionManager.ts                             | mitigate    | §3 Linux-guard fork-wins; preserved surfaces named in commit bodies                                                                                                   |
| T-34-06-04 | Repudiation | 19+ atomic commits this wave                                                           | mitigate    | SSH signature verified per commit                                                                                                                                     |
| T-34-06-05 | Tampering   | Blanket `--theirs`/`--ours` regression risk (D-34-03)                                  | mitigate    | Per-region hand-classification per D-34-02; no blanket checkout                                                                                                       |
| T-34-06-SC | Tampering   | npm/pip/cargo installs (`pnpm install --frozen-lockfile=false` in F3.3 branch (b)/(c)) | mitigate    | Install runs only against existing pnpm-workspace.yaml edits; no new packages introduced — `native-errors` was previously catalogued (Phase 31) and is being re-added |

</threat_model>

<verification>
- L1 markers: `git grep -nE '^(<{7}|={7}|>{7})( |$)' src/renderer/` returns 0 lines
- L2 harness: `bash .planning/phases/34-renderer-main-spine-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check` exits 0 (13/13 GREEN)
- L3 UNFILTERED bucket typecheck: 0 errors
- All 19+ commits SSH-signed
- 2 bluebird-trap audit lines + 1 D-34-17 decision line present in commit bodies
</verification>

<success_criteria>

- 18 atomic resolve commits (F1=10, F2=5, F3=3) + (optional) 1 `chore(workspace)` commit if D-34-17 branch (c) + 1 SUMMARY commit on `v8.1/config-bucket`
- UNFILTERED bucket typecheck 0 errors
- Bluebird-trap audit clean for 2 risk files
- D-34-17 decision recorded
- Wave F closed; Wave G unblocked
  </success_criteria>

<output>
Write `.planning/phases/34-renderer-main-spine-v2-0-1/34-06-SUMMARY.md` capturing F1/F2/F3 sub-batch order, per-file region tally, D-34-17 decision branch, bluebird-trap audit, Linux-guard preservation, UNFILTERED typecheck output, harness state, and commit SHAs.
</output>
