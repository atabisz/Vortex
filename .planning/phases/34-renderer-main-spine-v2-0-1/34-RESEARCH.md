# Phase 34: Renderer + main spine (v2.0.1) — Research

**Researched:** 2026-05-23
**Domain:** Brownfield merge-conflict resolution across renderer/main/preload/shared spine + repo-wide leaves under v8.1/config-bucket
**Confidence:** HIGH

## Summary

Phase 34 resolves every remaining `<<<<<<< HEAD ... >>>>>>> v2.0.1` marker in the repository outside `.planning/` and `extensions/` — exactly **117 files / 1203 conflict regions** (matching CONTEXT.md scope, with the caveat that **915 of those regions are inside a single built artifact**, `.github/actions/fingerprints/dist/index.js`, which is regenerated by `ncc build src/index.ts -o dist`). The "real" hand-resolution surface is **288 regions across 116 files**.

The dominant change pattern across the entire 117-file set is a **mechanical formatter reflow on the upstream side**: HEAD = single-line statements / fork-original import order; v2.0.1 = Prettier-wrapped multi-line / re-grouped imports. Every file I sampled (Application.ts, Table.tsx, cli.ts, eventHandlers.ts, preload/index.ts, e2e/smoke.spec.ts, etc.) confirms this. The Phase 32/33 D-32-02/D-33-02 smaller-diff stance covers the vast majority — but **a handful of files carry real semantic changes that must be hand-resolved with playbook fidelity**: `renderer.tsx` (the `native-errors` import — D-34-17 trigger), `shared/types/errors.ts` (HEAD-only error classes `AlreadyDownloaded`/`DownloadIsHTML` that v2.0.1 deletes), `nexus_integration/eventHandlers.ts` (NXM/§4–§5 surface), and the four "main spine" files cli.ts/Application.ts/errorReporting.ts/main.ts.

**Primary recommendation:** Execute as planned (D-34-01 8-wave order, D-34-02 6-tier stance hierarchy, D-34-06 per-bucket typecheck). Add **one** new harness gate (gate 13: single-host `getIPCPath`) — gates 14/15/16 are not needed (see §"Validation Architecture"). The R2 DROP decision (D-34-15) is verified safe by pre-audit. The bluebird trap risk is contained to **7 specific files** that need post-resolve `pnpm tsc -p` per D-34-02 tier-5 Rule-2.

## Architectural Responsibility Map

| Capability                                      | Primary Tier                                               | Secondary Tier            | Rationale                                                                                                              |
| ----------------------------------------------- | ---------------------------------------------------------- | ------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| Conflict-marker discovery + per-file resolution | Workstation/git                                            | —                         | Pure source-tree text edits; no runtime tier involvement                                                               |
| Per-file atomic SSH-signed commits              | Workstation/git                                            | —                         | Per D-34-00, D-34-07; matches Phase 32/33 idiom                                                                        |
| Per-bucket typecheck                            | Build / `pnpm tsc -p <ws>/tsconfig.json --noEmit`          | —                         | D-34-06; tsconfig-per-bucket invocation, not workspace `pnpm typecheck`                                                |
| Harness verification                            | Workstation/bash                                           | CI (deferred to Phase 35) | `scripts/grep-checkpoint.sh` (Phase 33 12-gate, Phase 34 +1 candidate); CI promotion deferred                          |
| Lockfile/catalog regen (D-34-17 conditional)    | Workspace tooling (`pnpm install --frozen-lockfile=false`) | —                         | Triggered only if `nativeErr` survives `renderer.tsx` resolution                                                       |
| Built-artifact regeneration                     | `.github/actions/fingerprints/` ncc rebuild                | Phase 35                  | `dist/index.js` (915 regions) regenerates from `src/`; resolve source first, accept dist drift OR rebuild after Wave G |

## User Constraints (from CONTEXT.md)

### Locked Decisions

- **D-34-00:** Continue work on `v8.1/config-bucket`. Per-file atomic commits; title format `resolve(<bucket-slug>): <file> — <one-line stance>`. SSH-signed. No push from sandbox (Phase 36 owns push + FF-merge).
- **D-34-01:** Per-file atomic commits, grouped by bucket in execution order, ordered by dependency depth (dependees first), leaf-first inside each bucket. **8 waves: A (shared) → B (preload) → C (main spine) → D (renderer leaves) → E (renderer extensions) → F (renderer views/pages incl. heaviest leaves) → G (repo-wide leaves) → H (R2 + done-gate).**
- **D-34-02:** 6-tier per-region stance hierarchy: (1) playbook-surface fork-wins; (2) Linux platform-guard fork-wins; (3) v2.0.1 feature outside playbook upstream-wins; (4) Rule-1 dup-import HEAD-empty; (5) Rule-2 D1-carryover prevention (`tsc --noEmit` or `node --check` before commit); (6) smaller-diff default.
- **D-34-03:** No blanket `git checkout --ours` / `--theirs` (v8.0 Phase 30 cascading-drift incident proof).
- **D-34-04:** Re-use Phase 33 12-gate `grep-checkpoint.sh`. Copy under `.planning/phases/34-renderer-main-spine-v2-0-1/scripts/`. Extend with up to 4 candidate gates IF this researcher confirms necessity.
- **D-34-05:** Researcher inspects v2.0.1 diff for new playbook-touching surface; documents harness extensions.
- **D-34-06:** Per-bucket typecheck after each wave: `pnpm tsc -p <ws>/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l`. Lint deferred to Phase 35.
- **D-34-07:** One commit per resolved file, SSH-signed, casual project voice.
- **D-34-08:** Commit body: which playbook gates affected/preserved, region tally per side, harness exit, bucket-typecheck status.
- **D-34-09:** No `--no-verify` unless husky cannot parse. `.planning/` paths use `git add -f`.
- **D-34-10:** Single-host invariant carries: `LinkingDeployment.ts` is the only `resolvePathCase(dataPath, …)` host in code.
- **D-34-11:** Pre/post grep + read pattern for sentinel preservation.
- **D-34-12:** Wave parallelism allowed via `Agent(subagent_type="Engineer", run_in_background=true)` for independent files.
- **D-34-13:** Scope expansion: ALL conflict markers outside `.planning/` and `extensions/` (117 files / ~217 regions documented; **actual = 117 files / 1203 regions, 915 inside dist/index.js artifact**).
- **D-34-14:** 7-criterion done-gate (zero markers outside .planning/, harness GREEN, all bucket typechecks 0 non-marker errors, full-repo typecheck deferred to Phase 35, ~117 atomic commits + R2 commit, R2 documented + SYNC-34b checked, STATE.md + ROADMAP.md updated).
- **D-34-15:** R2 DROP `src/renderer/src/__mocks__/`. Recommended decision; researcher pre-audit confirms (see §"R2 Pre-Audit Confirmation").
- **D-34-16:** R2 commit lands AFTER all 117 resolution commits (Wave H).
- **D-34-17:** `native-errors` catalog re-add trigger-conditional on `nativeErr` survival in `renderer.tsx` — see §"native-errors Carry-over: trigger condition probe".
- **D-34-18:** R3 `electron-builder.config.json` deferred to Phase 35.
- **D-34-19:** Manual hardware UAT deferred to Phase 999.1 / 37.
- **D-34-20:** Full `pnpm run {typecheck,lint,test,build}` deferred to Phase 35.

### Claude's Discretion

- Per-region resolution outcomes inside each file (executor judgement under D-34-02 tier hierarchy).
- Exact wave parallelism count and agent batching for Wave G.
- Whether to extend the harness with v2.0.1-specific gates (this researcher's recommendation: **add gate 13 only**; see §"Validation Architecture").
- Whether `R2 drop` is single-file-rm or `git rm -r` of the directory plus tsconfig adjustments.
- Whether `native-errors` catalog re-add lands in same commit as `renderer.tsx` or follow-up.
- Final `bucket-slug` mapping for Wave G.

### Deferred Ideas (OUT OF SCOPE)

- Phase 35: full `pnpm run {typecheck,lint,test,build}` exit 0; lint baseline-parity; R3 carry-forward; `dist/index.js` rebuild verification.
- Phase 36: rebase + FF-merge + tag + cherry-pick to `linux-port` + `release-linux.yml` AppImage/.deb.
- Phase 37: SYNC-33-C, SYNC-34, SYNC-39 carry-forward UAT; playbook updates discovered during v8.1.
- Phase 999.1: manual hardware UAT.
- Restoring renderer Jest test runner (rejected D-34-15 alternative).
- Promoting `grep-checkpoint.sh` to `release-linux.yml` CI (re-deferred to Phase 35).

## Phase Requirements

| ID           | Description                                                                                                                                                                                                                                                                    | Research Support                                                                                                                                                                                                                             |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **SYNC-34a** | Renderer + main + shared spine resolved (`ExtensionManager`, `controls/Table`, `Application`, `cli`, `errorReporting`, `autoupdater`, `TrayIcon`, `store/{LevelPersist,DuckDBSingleton}`, `preload/index`, `shared/{errors,errors.test,telemetry/spans}`, `nexus_integration`) | Verified scope (117 files / 1203 regions); per-file dependency graph documented (§"Wave-A Coupling", §"Wave-C Coupling", §"Wave-E Nexus Coupling"); per-bucket tsconfig invocations verified to exist (§"Per-Bucket Typecheck Verification") |
| **SYNC-34b** | R2 carry-forward — Jest `__mocks__/` reintroduction decision documented                                                                                                                                                                                                        | Pre-audit (§"R2 Pre-Audit Confirmation"): 23 mock files, **0 references** in any `*.test.ts*` under `src/renderer/src/`, **no `src/renderer/jest.config.*`** present (only `src/renderer/vitest.config.mts`). DROP is verified safe.         |

## Marker Scope Verification

```bash
git grep -l '^<<<<<<< ' | grep -v ^.planning/ | grep -v ^extensions/ | sort | wc -l
# → 117 ✓ matches CONTEXT.md
```

### Region totals per bucket (verified)

| Bucket                              | Files   | Regions  | Note                                                                                             |
| ----------------------------------- | ------- | -------- | ------------------------------------------------------------------------------------------------ |
| `src/renderer/src/`                 | 68      | 174      | Matches CONTEXT.md                                                                               |
| `src/main/src/`                     | 9       | 22       | Matches CONTEXT.md (DuckDBSingleton **not** in marker set — drop from CONTEXT.md domain wording) |
| `src/preload/src/`                  | 1       | 1        | Matches CONTEXT.md                                                                               |
| `src/shared/src/`                   | 5       | 10       | Matches CONTEXT.md                                                                               |
| `packages/e2e/`                     | 11      | 30       | Matches CONTEXT.md                                                                               |
| `.github/actions/fingerprints/`     | 10      | 943      | **Of which 915 are inside `dist/index.js`** (built artifact)                                     |
| `scripts/`                          | 2       | ~8       | `download-duckdb-extensions{,.test}.ts`                                                          |
| Top-level docs                      | 4       | ~6       | CHANGELOG/CLAUDE/CONTRIBUTE/README                                                               |
| `docs/etc/flatpak/tools/__tests__/` | 7       | ~9       | misc leaves                                                                                      |
| **TOTAL**                           | **117** | **1203** | **288 excluding dist/index.js artifact**                                                         |

### Top region density (heaviest hitters)

```
915 .github/actions/fingerprints/dist/index.js   ← built artifact; resolve via rebuild
 11 src/renderer/src/extensions/nexus_integration/eventHandlers.ts  ← NXM hot file
 11 src/main/src/Application.ts                                     ← main spine heaviest
 10 src/renderer/src/controls/Table.tsx                             ← line-wrap reflows
  9 src/renderer/src/views/pages/Tools/useToolsPage.ts
  9 src/renderer/src/views/pages/Tools/useToolsData.ts
  8 src/renderer/src/extensions/extension_manager/installExtension.ts
  8 packages/e2e/tests/dashboard.spec.ts
  7 src/renderer/src/extensions/nexus_integration/index.tsx
  6 src/shared/src/errors.test.ts
  6 src/renderer/src/views/pages/Tools/{ToolRow,index}.tsx
  6 src/renderer/src/util/util.objDiff.test.ts
  6 src/renderer/src/extensions/nexus_integration/util.ts
```

[VERIFIED: `git grep -c '^<<<<<<<' <file>`]

### Dominant change pattern (sampled across 8 buckets)

| File                                                        | Sampled Pattern                                                                                                  | Stance Class                                                       |
| ----------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| `Application.ts` (main, 11 regions)                         | HEAD = single-line; v2.0.1 = wrapped multi-line                                                                  | **smaller-diff (HEAD)** — D-32-02 default                          |
| `cli.ts` (main, 1 region)                                   | Import-block reorg (HEAD-empty vs upstream new imports already present elsewhere)                                | **Rule-1 dup-import HEAD-empty** OR upstream-wins; needs hand-read |
| `controls/Table.tsx` (renderer, 10 regions)                 | Pure line-wrap reflows                                                                                           | **smaller-diff (HEAD)** uniform                                    |
| `nexus_integration/eventHandlers.ts` (renderer, 11 regions) | Mix: HEAD has `getErrorCode`, `AlreadyDownloaded`, `DownloadIsHTML` imports v2.0.1 dropped; type-import grouping | **fork-wins for §4–§5 surface; smaller-diff for the rest**         |
| `shared/types/errors.ts` (5 files, 1 region in this file)   | HEAD has `AlreadyDownloaded` + `DownloadIsHTML` classes that v2.0.1 deletes                                      | **fork-wins** — preserves error contract used by 1+ consumer       |
| `preload/src/index.ts` (preload, 1 region)                  | Single line-wrap                                                                                                 | **smaller-diff (HEAD)**                                            |
| `packages/e2e/tests/smoke.spec.ts` (e2e, 5 regions)         | Pure formatter reflow                                                                                            | **smaller-diff (HEAD)** uniform                                    |
| `CLAUDE.md` (top-level, 1 region: lines 2–391)              | ENTIRE fork GSD/Branch Strategy block on HEAD; v2.0.1 has none                                                   | **fork-wins** — strict (390 lines of fork-only content)            |

[VERIFIED: `git diff fork/master -- <file>` and direct `sed -n` reads]

## Wave-A Coupling (shared)

Imports relevant to the 5 shared files:

```
src/shared/src/types/errors.ts     ← no shared/* imports (depends only on @opentelemetry/api types via `cause?:unknown`)
src/shared/src/types/state.ts      ← imports `./ipc` only (no other Phase 34 shared file)
src/shared/src/errors.ts           ← no shared/* imports (self-contained)
src/shared/src/telemetry/spans.ts  ← imports `../errors` (=> errors.ts)
src/shared/src/errors.test.ts      ← imports `./errors` via vitest test
```

**Leaf-first commit order (verified):**

1. `types/errors.ts` (foundational; HEAD-side has fork-only error classes — fork-wins)
2. `types/state.ts` (depends on `./ipc` — `./ipc` has no marker per audit)
3. `errors.ts` (foundational sanitize/fingerprint helpers; HEAD = single-line, v2.0.1 = wrapped)
4. `telemetry/spans.ts` (consumer of `errors.ts`)
5. `errors.test.ts` (consumer of `errors.ts`; 6 regions)

Per-bucket typecheck after Wave A: `pnpm tsc -p src/shared/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l` (verified to fail today only on TS1185 conflict-marker errors — exact D-34-06 sentinel pattern). [VERIFIED]

## Wave-C Coupling (main spine)

```
src/main/src/main.ts                     ← imports Application, cli, errorReporting (entry-point; commit LAST)
src/main/src/Application.ts              ← imports cli, errorReporting (heaviest, 11 regions; commits before main.ts)
src/main/src/cli.ts                      ← imports getVortexPath, logging only (leaf within main spine)
src/main/src/errorReporting.ts           ← imports telemetry/* (leaf within main spine)
src/main/src/extensions/autoupdater.ts   ← imports ../ipc, ../logging (leaf)
src/main/src/TrayIcon.ts                 ← imports getVortexPath, logging (leaf)
src/main/src/store/SubPersistor.ts       ← no local imports detected (leaf)
src/main/src/store/LevelPersist.ts       ← imports ../getVortexPath, ../logging, ./DuckDBSingleton (depends on store leaf)
src/main/src/store/ReduxPersistorIPC.ts  ← imports ../errorHandling, ../logging, **type LevelPersist** (depends on LevelPersist)
```

**Leaf-first commit order:**

1. `store/SubPersistor.ts` (leaf)
2. `store/LevelPersist.ts` (depends on DuckDBSingleton, but DuckDBSingleton has NO marker)
3. `store/ReduxPersistorIPC.ts` (depends on LevelPersist via type-only import)
4. `errorReporting.ts` (leaf within spine)
5. `cli.ts` (leaf within spine)
6. `extensions/autoupdater.ts` (leaf)
7. `TrayIcon.ts` (leaf)
8. `Application.ts` (depends on all above)
9. `main.ts` (entry-point, depends on Application + cli + errorReporting)

**Note for planner: D-34-06 typecheck for Wave C should run AFTER all 9 commits land** (mid-wave intermediates will fail because Application.ts imports from still-unresolved cli.ts during the wave; commit-then-defer-typecheck-to-wave-end is the matching pattern from Phase 32 D-32-08).

## Wave-E Nexus Coupling

The 9-file nexus extension leaf-first order from CONTEXT.md is **verified accurate**:

```
types/IValidateKeyData.ts   ← @nexusmods/nexus-api type-only import (leaf)
util/UIDs.ts                ← imports ../../mod_management/types (cross-extension type-only; not in P34 marker set; safe)
util/oauth.ts               ← imports ../../../util/CustomErrors, ../constants (constants has no marker)
util.ts                     ← imports types/IValidateKeyData, types/IJWTAccessToken (leaf inside extension)
views/FreeUserDLDialog.tsx  ← imports ../../controls/Modal, util/opn (cross-renderer, not in marker set)
views/GoPremiumDashlet.tsx  ← imports ../../controls/* (cross-renderer, not in marker set)
selectors.test.ts           ← test consumer; commit before eventHandlers
eventHandlers.ts            ← 11 regions; consumer of util.ts + types
index.tsx                   ← 7 regions; entry, depends on eventHandlers + util + views
```

[VERIFIED: per-file `grep '^import' <file>` audit]

## native-errors Carry-over: trigger condition probe

**File:** `src/renderer/src/renderer.tsx` lines 65–71.

**State of the conflict region:**

```
65: <<<<<<< HEAD
66: import { ipcRenderer, webFrame } from "electron";
67: =======
68: import type crashDumpT from "crash-dump";
69: import { ipcRenderer, webFrame } from "electron";
70: import * as nativeErr from "native-errors";
71: >>>>>>> v2.0.1
```

**Reading:** `nativeErr` is on the **upstream-v2.0.1 side**. HEAD has dropped it. So D-34-17's "if `nativeErr` survives" condition resolves as follows:

- If executor takes upstream-wins (preserves `nativeErr` import) → **catalog re-add of `native-errors` REQUIRED**, plus `pnpm install --frozen-lockfile=false` to regen `pnpm-lock.yaml`. Single combined commit recommended.
- If executor takes HEAD-wins (drops `nativeErr`) → SYNC-33b's deferral upgrades to permanent "no consumer" record matching the other 3 already-resolved packages.

**Recommended planner stance:** This is the single highest-stakes conflict in Phase 34. The decision depends on whether `nativeErr` is consumed elsewhere in the file (lines 127–141 hold the second `renderer.tsx` conflict region — needs hand-read at execution time to confirm). Plan-phase planner should flag this for solo-pass execution by the most senior agent and explicitly call out the trigger-conditional logic in the task body.

[VERIFIED: direct `sed -n '50,110p' src/renderer/src/renderer.tsx`]

## R2 Pre-Audit Confirmation (D-34-15)

```bash
find src/renderer/src/__mocks__ -type f | wc -l
# → 23

grep -rn "__mocks__" src/renderer/src --include='*.test.ts*' --include='*.test.js*'
# → (empty — zero matches)

grep -rn "vi.mock.*__mocks__\|jest.mock.*__mocks__" src/renderer/src
# → (empty — zero matches)

ls src/renderer/jest.config.*
# → exit 2 (none exist)

ls src/renderer/vitest.config.mts
# → src/renderer/vitest.config.mts (only Vitest configured)
```

**Conclusion:** `src/renderer/src/__mocks__/` is dead infrastructure. Zero test files reference it. No Jest config exists in the renderer. Vitest does not autoload `__mocks__/` directories. **DROP is safe per D-34-15.** Wave H single commit pattern works.

[VERIFIED: filesystem grep + ls audit on current branch HEAD]

## Per-Bucket Typecheck Verification (D-34-06)

| Wave                                                   | Command                                                                                                                                                                               | tsconfig exists?                                                       |
| ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| A (shared)                                             | `pnpm tsc -p src/shared/tsconfig.json --noEmit 2>&1 \| grep -v TS1185 \| wc -l`                                                                                                       | ✓ VERIFIED — currently fails only on TS1185 (matches D-34-06 sentinel) |
| B (preload)                                            | `pnpm tsc -p src/preload/tsconfig.json --noEmit ...`                                                                                                                                  | ✓                                                                      |
| C (main)                                               | `pnpm tsc -p src/main/tsconfig.json --noEmit ...`                                                                                                                                     | ✓                                                                      |
| D / E / F (renderer)                                   | `pnpm tsc -p src/renderer/tsconfig.json --noEmit ...`                                                                                                                                 | ✓ (single tsconfig spans the renderer)                                 |
| G — fingerprints                                       | `cd .github/actions/fingerprints && pnpm exec tsc --noEmit`                                                                                                                           | ✓                                                                      |
| G — e2e                                                | `cd packages/e2e && pnpm exec tsc --noEmit`                                                                                                                                           | ✓                                                                      |
| G — scripts                                            | **No `scripts/tsconfig.json`** — fallback: `pnpm exec tsc --noEmit scripts/download-duckdb-extensions.ts scripts/download-duckdb-extensions.test.ts` (or `node --check` post-compile) | ✗ — fallback documented                                                |
| G — top-level docs / etc / flatpak / tools / **tests** | No typecheck (`.md`, `.json`, `.html`, `.test.js`)                                                                                                                                    | n/a                                                                    |
| G — `__tests__/reducers.download_management.test.js`   | `node --check` (matches Phase 33 Route 3 pattern)                                                                                                                                     | n/a                                                                    |

[VERIFIED: `ls <ws>/tsconfig.json` for all 6 named workspaces; manual `pnpm tsc -p src/shared/tsconfig.json --noEmit` returned only TS1185 errors]

## Wave-G Repo-Wide Leaves Classification

### `.github/actions/fingerprints/` (10 files, 943 regions)

| File                                  | Regions | Type                                              | Recommended stance                                                                                                                                                                                                                                                                                                               |
| ------------------------------------- | ------- | ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `dist/index.js`                       | **915** | Built artifact (`ncc build src/index.ts -o dist`) | **REBUILD after src resolution.** Cleanest path: resolve all 9 src files in Wave G, then `cd .github/actions/fingerprints && pnpm install && pnpm run build`, then `git add dist/index.js -A` as a single follow-up commit. Marker-resolution-by-rebuild — strictly safer than hand-resolving 915 regions of webpack-bundled JS. |
| `src/index.ts`                        | 1       | source                                            | smaller-diff                                                                                                                                                                                                                                                                                                                     |
| `src/types.ts`                        | 1       | source                                            | smaller-diff                                                                                                                                                                                                                                                                                                                     |
| `src/clickhouse.ts`                   | ~3      | source                                            | smaller-diff                                                                                                                                                                                                                                                                                                                     |
| `src/collect-input.ts` + `.test.ts`   | ~5      | source + test                                     | smaller-diff                                                                                                                                                                                                                                                                                                                     |
| `src/collect-pr.ts` + `.test.ts`      | ~7      | source + test                                     | smaller-diff                                                                                                                                                                                                                                                                                                                     |
| `src/collect-release.ts` + `.test.ts` | ~9      | source + test                                     | smaller-diff                                                                                                                                                                                                                                                                                                                     |

`fingerprints` is fork-CI-only and not part of the runtime build. The `dist/index.js` file in the marker set is **stale** (last built `2026-05-22 19:36`); rebuilding from the resolved source is the correct stance. If executor cannot rebuild in-sandbox (network or build-tool restrictions), the **fallback** is to take whichever side of `dist/index.js` corresponds to the resolved source state and flag for Phase 35 rebuild verification.

### `packages/e2e/` (11 files, 30 regions)

Sample (`smoke.spec.ts`): pure formatter reflow (HEAD = single-line param destructure / inline import order; v2.0.1 = wrapped multi-line / Prettier-grouped imports). **Stance: smaller-diff (HEAD) uniform.** Tests don't run on Linux CI (Phase 22/27 deferred); resolution only — no test-suite execution. Per-file typecheck against `packages/e2e/tsconfig.json` after wave commit.

### Top-level docs (CHANGELOG, CLAUDE, CONTRIBUTE, README) — 4 files

- **CLAUDE.md** (lines 2–391): ENTIRE fork-side block holds 389 lines of GSD `Project / Technology Stack / Branch Strategy / GSD Workflow Enforcement / Developer Profile` content that v2.0.1 has none of. **Stance: fork-wins** (drop the upstream side entirely, keep the `>>>>>>> v2.0.1` marker line so the planner can run `git restore --source=HEAD` paths if needed). Casual project voice per memory `feedback_casual_voice.md`.
- **CHANGELOG.md / CONTRIBUTE.md / README.md**: similar conflict pattern likely — fork's notes (Linux port, build instructions, fork-specific contributor guidelines) live on HEAD; v2.0.1 has upstream-only release notes. Per-file hand-read; stance is **fork-wins for fork-specific sections, upstream-wins for upstream wording reflows**. Sample these at execution time before final stance call.

### `scripts/`, `docs/`, `etc/`, `flatpak/`, `tools/`, `__tests__/` — 9 misc leaves

- `scripts/download-duckdb-extensions{,.test}.ts` — TS leaves; smaller-diff stance; fallback typecheck via `pnpm exec tsc --noEmit <file>`.
- `docs/flatpak/maintenance.md`, `docs/native-node-module-management.md` — fork docs; smaller-diff per region; fork-wins for any Linux/flatpak-specific content.
- `etc/Dependency Report.md`, `etc/vortex.api.md` — generated API doc references (the `vortex.api.md` mentions `resolvePathCase` non-binding); smaller-diff.
- `flatpak/generated-sources.json` — generated; smaller-diff stance OR regenerate (defer regen to Phase 35 if non-trivial).
- `tools/addicons/index.html` — single-host fork tool; smaller-diff; fork-wins for any Linux content.
- `__tests__/reducers.download_management.test.js` — legacy test file; smaller-diff; `node --check` post-resolve.

## Bluebird Promise Trap (per memory `feedback_bluebird_promise_trap.md`)

Files in Phase 34 that import `Promise` from bluebird AND have `:Promise<…>` annotations in conflict regions — **risk for TS1064 if upstream side adds annotations to async functions:**

```
src/renderer/src/ExtensionManager.ts
src/renderer/src/extensions/hardlink_activator/index.ts
src/renderer/src/extensions/nexus_integration/eventHandlers.ts
src/renderer/src/extensions/nexus_integration/util.ts
src/renderer/src/extensions/symlink_activator_elevate/index.ts
src/renderer/src/renderer.tsx
src/renderer/src/util/errorHandling.ts
```

**Recommendation:** Apply D-34-02 tier-5 Rule-2 strictly to these 7 files: run `pnpm tsc -p src/renderer/tsconfig.json --noEmit` after each commit (or batch them at sub-wave end) and explicitly check for TS1064 errors before proceeding to next file in the wave. Memory `feedback_bluebird_promise_trap.md` says: **don't take upstream `:Promise<void>` annotations on async functions when the file imports bluebird Promise — TS1064 will fire.**

[VERIFIED: `grep -E "from .bluebird.|require\(.bluebird.\)" <file>` AND `grep -E ":\s*Promise<" <file>` for all 117 P34 files; 7 hits]

## Validation Architecture

### Test Framework

| Property                       | Value                                                                                                                               |
| ------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------- |
| Framework                      | Vitest 4.1.0 (renderer/main/shared) + Jest 29.7.0 (legacy `__tests__/` + extension-side mocks)                                      |
| Config files                   | `vitest.config.ts` (root project array), `src/renderer/vitest.config.mts`, `src/main/vitest.config.*`, `src/shared/vitest.config.*` |
| Quick run command (per-bucket) | `pnpm tsc -p <ws>/tsconfig.json --noEmit 2>&1 \| grep -v TS1185 \| wc -l`                                                           |
| Full suite command             | Deferred to Phase 35 per D-34-20 (`pnpm run typecheck`, `pnpm run lint`, `pnpm run test`, `pnpm run build`)                         |

### Phase 34 Validation = Three-Layer Gate

For a brownfield merge-resolution phase, validation is binary at three layers:

| Layer                         | Sentinel                                                                         | Pass condition                                                                    |
| ----------------------------- | -------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| **L1 — Conflict markers**     | `git grep -l '^<<<<<<< ' \| grep -v ^.planning/`                                 | Returns empty (D-34-14 criterion 1)                                               |
| **L2 — Harness gates**        | `bash .planning/phases/34-renderer-main-spine-v2-0-1/scripts/grep-checkpoint.sh` | Exit 0; all 12+ inherited gates GREEN; gate 13 (new) GREEN (D-34-14 criterion 2)  |
| **L3 — Per-bucket typecheck** | `pnpm tsc -p <ws>/tsconfig.json --noEmit 2>&1 \| grep -v TS1185 \| wc -l`        | Returns 0 for shared/preload/main/renderer/fingerprints/e2e (D-34-14 criterion 3) |

### Harness Gates — Add gate 13, skip gates 14/15/16

| Candidate                                | Decision                                       | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ---------------------------------------- | ---------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Gate 13 — `getIPCPath` single-host**   | **ADD**                                        | Verified: `getIPCPath` is exported once from `src/renderer/src/util/ipc.ts:9` and **imported** at 5 sites: `ExtensionManager.ts`, `extensions/symlink_activator_elevate/index.ts`, `util/elevated.ts`, `util/fs.ts`, `util/elevated.test.ts`. Two of these (`ExtensionManager.ts` and `symlink_activator_elevate/index.ts`) are in the Phase 34 marker set. Single-host invariant must be preserved per D-04 IPC serialisation trap memory. **Gate 13 sentinel:** `grep -c '^export function getIPCPath' src/renderer/src/util/ipc.ts == 1` AND `grep -c 'import.*getIPCPath.*from.*ipc' src/renderer/src/{ExtensionManager,extensions/symlink_activator_elevate/index,util/{elevated,fs}}.ts >= 4`. |
| **Gate 14 — Linux argv slice in cli.ts** | **SKIP**                                       | Already settled; the conflict region in `cli.ts` is at lines 1–7 (import block), NOT at line 56 (`argv.slice`). The argv-slice logic is `argv.slice(1, firstArgumentIdx - 1)` and is unchanged by either side of the merge — already cross-platform per quick task `260407-grv` `d9986f6`. No new gate needed.                                                                                                                                                                                                                                                                                                                                                                                       |
| **Gate 15 — error-class preservation**   | **SKIP** (covered by L1 + per-file body audit) | Error classes (`UserCanceled`, `TimeoutError`, `AlreadyDownloaded`, `DownloadIsHTML`, etc.) live in `src/shared/src/types/errors.ts` (NOT `src/shared/src/errors.ts` — researcher correction to D-34-04). HEAD-only fork-wins classes `AlreadyDownloaded` and `DownloadIsHTML` will be flagged automatically by L1 (the conflict marker around them surfaces in any post-resolution grep) AND by D-34-08 commit-body discipline (executor must list class preservation in body for `shared/types/errors.ts`). A separate gate adds noise without coverage gain.                                                                                                                                      |
| **Gate 16 — `__mocks__/` shape**         | **SKIP** (R2 DROP makes this trivial)          | After D-34-15 DROP commit, the directory ceases to exist. Gate would assert `[ ! -d src/renderer/src/__mocks__ ]` — trivial; covered by L1 zero-marker assertion plus the explicit Wave H R2 commit. Adding the gate is harness bloat for a one-shot check.                                                                                                                                                                                                                                                                                                                                                                                                                                          |

### Phase 34 Requirements → Test Map

| Req ID   | Behavior                                | Test Type                  | Automated Command                                                                                                                                                                                                                                                                      | File Exists?               |
| -------- | --------------------------------------- | -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- |
| SYNC-34a | Renderer + main + shared spine resolved | L1 + L2 + L3 (binary gate) | `git grep -l '^<<<<<<< ' \| grep -v ^.planning/` returns empty AND `bash .planning/phases/34-renderer-main-spine-v2-0-1/scripts/grep-checkpoint.sh` exits 0 AND `pnpm tsc -p {shared,preload,main,renderer}/tsconfig.json --noEmit 2>&1 \| grep -v TS1185 \| wc -l` returns 0 for each | ✓ existing                 |
| SYNC-34b | R2 carry-forward documented             | doc-level                  | `[ ! -d src/renderer/src/__mocks__ ]` AND `grep -q 'SYNC-34b.*\\[x\\]' .planning/REQUIREMENTS.md` AND R2 decision present in `34-PHASE-SUMMARY.md`                                                                                                                                     | ✓ once Wave H commit lands |

### Sampling Rate

- **Per task commit:** No per-file typecheck; defer to wave end per D-34-06.
- **Per wave commit (closing the wave):** Run that wave's bucket-scoped typecheck; harness in `--skip-conflict-check` mode.
- **Phase gate (D-34-14):** L1 + L2 (full mode, no `--skip-conflict-check`) + L3 across all six TS workspaces.

### Wave 0 Gaps

- [x] Phase 33 harness exists at `.planning/phases/33-gamebryo-per-game-extensions-v2-0-1/scripts/grep-checkpoint.sh` — **copy to `.planning/phases/34-renderer-main-spine-v2-0-1/scripts/grep-checkpoint.sh`** (D-34-04).
- [ ] Add gate 13 (single-host `getIPCPath`) — append to copied harness; renumber existing gate 12 conflict-marker check to gate 14 OR keep at 12 and append new gate as 13 (matches Phase 33 → 34 numbering convention).
- [ ] No framework install needed — Vitest already in place.

## Source Hierarchy

| Level | Sources                                                                                                                                      | Used For                                                                                           |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| HIGH  | `git grep`/`git diff` against working tree, `cat`/`sed` direct file reads, `ls` filesystem audit                                             | Marker counts, bucket totals, dependency graph, conflict-region samples — **all primary findings** |
| HIGH  | `.planning/phases/33-*/scripts/grep-checkpoint.sh` execution (`--skip-conflict-check` mode → 11 gates GREEN today)                           | Harness baseline for D-34-04                                                                       |
| HIGH  | `pnpm tsc -p src/shared/tsconfig.json --noEmit` actual run                                                                                   | Per-bucket typecheck command verification                                                          |
| HIGH  | Project memory `feedback_bluebird_promise_trap.md`, `feedback_casual_voice.md`, `feedback_ssh_signing.md`, `feedback_planning_gitignored.md` | Operational constraints                                                                            |
| HIGH  | `VORTEX-LINUX-MERGE-PLAYBOOK.md` §1/§3/§4–§5/§6/§7a–d/§10/140a57217 (referenced in CONTEXT.md `<canonical_refs>`)                            | Playbook surface                                                                                   |

No web sources used — all findings are from the local working tree, prior phase artifacts, and project memory.

## Assumptions Log

| #   | Claim                                                                                                                       | Section                  | Risk if Wrong                                                                                                                                                                                                                                                |
| --- | --------------------------------------------------------------------------------------------------------------------------- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| A1  | The 915 regions inside `dist/index.js` are recoverable by `ncc build src/index.ts -o dist` from the resolved 9 source files | Wave-G fingerprints      | If sandbox cannot run the ncc build, executor falls back to "take whichever side matches resolved source state" and flags for Phase 35 rebuild — fallback documented, no execution block.                                                                    |
| A2  | The second `renderer.tsx` conflict region (lines 127–141) doesn't contain a hidden second consumer of `nativeErr`           | native-errors carry-over | Mitigation: D-34-17 spec already requires the executor to hand-read the full `renderer.tsx` resolution — the second region will be visible at execution time and the trigger condition is evaluated AFTER both regions resolve, not before. No risk to plan. |
| A3  | The `CLAUDE.md` 389-line fork block at lines 2–391 is fully fork-only with no upstream content embedded inside              | Wave-G top-level docs    | Mitigation: hand-read at execution time; if upstream content is interleaved, executor falls back to per-section smaller-diff.                                                                                                                                |
| A4  | The bluebird trap risk list of 7 files is complete (no false negatives via dynamic `import("bluebird")`)                    | Bluebird Promise Trap    | Mitigation: `pnpm tsc -p src/renderer/tsconfig.json --noEmit` at Wave D/E/F end will surface any TS1064 not caught by static grep — defense in depth.                                                                                                        |

## Open Questions

1. **`packages/e2e/` typecheck after wave G — does it pass cleanly?**
    - What we know: `packages/e2e/tsconfig.json` exists; `packages/e2e/package.json` has Playwright deps; no marker in tsconfig itself.
    - What's unclear: whether v2.0.1 added new `@playwright/test` types that fork-master doesn't have via lockfile — the typecheck depends on `pnpm install` happening first.
    - Recommendation: planner adds a `pnpm install` step before Wave G fingerprints + e2e typecheck (matches Phase 33 wave-end pattern).

2. **`flatpak/generated-sources.json` regeneration**
    - What we know: This is a generated lockfile-derived file used by the Flatpak build pipeline.
    - What's unclear: whether the generator script needs to be re-run post-resolve, or whether smaller-diff is acceptable.
    - Recommendation: defer regeneration to Phase 35 (matches D-34-20); apply smaller-diff in Wave G unless executor sees clear contradictions.

3. **Whether `__tests__/reducers.download_management.test.js` is still consumed.**
    - What we know: It's a legacy `.js` test in the repo root `__tests__/` (not Vitest's typical `*.test.ts` shape).
    - What's unclear: which test runner picks it up. Smaller-diff + `node --check` is the safe stance regardless.

## Project Constraints (from CLAUDE.md / AGENTS.md)

| Directive                                                                            | Source                                       | Application to Phase 34                                                                   |
| ------------------------------------------------------------------------------------ | -------------------------------------------- | ----------------------------------------------------------------------------------------- |
| Use `pnpm run` for repo commands                                                     | AGENTS.md                                    | All Phase 34 commands use `pnpm` (no `npm`/`yarn`)                                        |
| After code changes, run `build`, `test`, `lint`, `format` on affected package        | AGENTS.md                                    | Phase 34 defers full build/test/lint to Phase 35 per D-34-20; per-bucket typecheck only   |
| Never reformat files outside the scope of a change                                   | memory `feedback_minimize_upstream_diff.md`  | Resolution-only stance — no re-flow of unmarked regions                                   |
| Casual developer voice in docs/commits/PR/issue comments                             | memory `feedback_casual_voice.md`            | All commit messages and SUMMARY docs                                                      |
| GH_TOKEN env var for atabisz when gh auth expired                                    | memory `reference_github_token.md`           | Use as env var if needed in Phase 36; not in Phase 34                                     |
| `git push git@github.com:atabisz/Vortex.git <branch>` (sandbox blocks .git/config)   | memory `feedback_git_push_ssh.md`            | **No push from sandbox** during Phase 34 — Phase 36 owns push                             |
| All commits SSH-signed                                                               | memory `feedback_ssh_signing.md`             | D-34-07 enforces; verify via `git cat-file -p <sha> \| grep -c '^gpgsig '` ≥1             |
| Upstream Nexus-Mods/Vortex not accepting fork PRs                                    | memory `project_upstream_pr_policy.md`       | Out of scope for Phase 34 — applies to Phase 36/37                                        |
| After upstream merge, verify gamebryo platform guards + skip-on-windows.mjs survived | memory `project_upstream_merge_checklist.md` | Phase 33 already verified; Phase 34 harness gate 7 (§1 extension build guards) re-asserts |
| Bluebird Promise TS1064 trap                                                         | memory `feedback_bluebird_promise_trap.md`   | 7 files at risk (§"Bluebird Promise Trap") — D-34-02 tier-5 Rule-2 mandatory              |
| `.planning/` is gitignored                                                           | memory `feedback_planning_gitignored.md`     | All Phase 34 planning-doc commits use `git add -f`                                        |
| Use `/gsd:execute-phase` for planned phase work                                      | global CLAUDE.md GSD enforcement             | Phase 34 execution will run via `/gsd:execute-phase`                                      |

## Sources

### Primary (HIGH confidence)

- Local git working tree on branch `v8.1/config-bucket` at HEAD `c2c725801` (current as of 2026-05-23):
    - `git grep -l '^<<<<<<< '` for marker discovery
    - `git grep -c '^<<<<<<<'` per file for region counts
    - `git diff fork/master -- <file>` for stance classification
    - Direct `cat` / `sed -n` reads for sample regions
    - `ls`, `find`, `grep -rn` for R2 pre-audit
- `.planning/phases/33-gamebryo-per-game-extensions-v2-0-1/scripts/grep-checkpoint.sh` — harness baseline; ran today in `--skip-conflict-check` mode → 11 gates GREEN
- `.planning/phases/34-renderer-main-spine-v2-0-1/34-CONTEXT.md` — locked decisions D-34-00..D-34-20
- `.planning/REQUIREMENTS.md` — SYNC-34a, SYNC-34b
- `.planning/STATE.md` — milestone position; Phase 33 closeout receipts (193 commits, 11/11 harness gates)
- Project memory under `~/.claude/projects/-home-alex-src-Vortex/memory/`

### Secondary (HIGH confidence — referenced not verified by tool execution this session)

- `VORTEX-LINUX-MERGE-PLAYBOOK.md` §1/§3/§4–§5/§6/§7a–d/§10/140a57217 — pre-existing playbook, gates 1–6 + 7–10 already proven by Phase 32/33 harness runs
- v8.0 Phase 28 archive (extracted from git d56c45cea) — D-28-XX precedent decisions

### Tertiary

- None — no WebSearch / WebFetch was needed; this is a closed-system brownfield merge phase with all primary sources on the local filesystem.

## Metadata

**Confidence breakdown:**

- Marker scope (117 files / 1203 regions / per-bucket totals): **HIGH** — verified by direct `git grep` execution
- Wave-A/C/E coupling and leaf-first order: **HIGH** — verified by per-file `grep '^import'` audit
- Per-bucket typecheck commands: **HIGH** — `pnpm tsc -p src/shared/tsconfig.json --noEmit` executed and returned exactly the expected TS1185 sentinel pattern
- Harness reuse + gate 13 recommendation: **HIGH** — `getIPCPath` single-host verified by full `git grep` of 5 import sites + 1 export host
- R2 DROP safety: **HIGH** — three independent grep audits returned zero references
- D-34-17 trigger condition: **HIGH** for region 1 (lines 65–71) — direct file read; **MEDIUM** for region 2 (lines 127–141) since not separately read; mitigation: executor reads at resolution time
- Wave-G fingerprints `dist/index.js` rebuild path: **MEDIUM** — `package.json scripts.build` reads `ncc build src/index.ts`, but a clean rebuild was not run in this session; risk mitigated by Phase 35 deferral fallback
- Bluebird trap file list: **HIGH** — verified by combined grep of bluebird import + `:Promise<` annotation across all 117 P34 files

**Research date:** 2026-05-23
**Valid until:** 2026-05-30 (7 days — fast-moving brownfield merge state)
