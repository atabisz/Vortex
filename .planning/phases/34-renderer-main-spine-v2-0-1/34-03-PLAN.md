---
phase: 34
plan: 03
type: execute
wave: C
status: ready
requires:
    - 34-00
    - 34-01
    - 34-02
depends_on:
    - 34-00
    - 34-01
    - 34-02
files_modified:
    - src/main/src/store/SubPersistor.ts
    - src/main/src/store/LevelPersist.ts
    - src/main/src/store/ReduxPersistorIPC.ts
    - src/main/src/errorReporting.ts
    - src/main/src/cli.ts
    - src/main/src/extensions/autoupdater.ts
    - src/main/src/TrayIcon.ts
    - src/main/src/Application.ts
    - src/main/src/main.ts
    - .planning/phases/34-renderer-main-spine-v2-0-1/34-03-SUMMARY.md
autonomous: true
requirements:
    - SYNC-34a
must_haves:
    truths:
        - "All 9 main spine files marker-free in leaf-first order: store/SubPersistor → store/LevelPersist → store/ReduxPersistorIPC → errorReporting → cli → extensions/autoupdater → TrayIcon → Application → main"
        - "9 atomic SSH-signed `resolve(main): <file> — <stance>` commits on `v8.1/config-bucket`"
        - "Wave-C-end bucket-scoped typecheck (D-34-06): `pnpm tsc -p src/main/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l` returns 0"
        - "Harness skip-mode (13 gates) stays GREEN after every commit"
        - "Linux platform guards preserved per §3 Linux-guard fork-wins (e.g. errorReporting native-handler tier, TrayIcon, autoupdater Linux disposition)"
        - "Application.ts (11 conflict regions, heaviest in wave) resolved as the penultimate file because it imports from cli + autoupdater + TrayIcon + ReduxPersistorIPC + errorReporting"
        - "1 SUMMARY commit closes the wave with per-file region tally + bucket-typecheck count + Linux-guard preservation evidence"
    artifacts:
        - path: "src/main/src/Application.ts"
          provides: "Main process lifecycle orchestrator (init → ready → shutdown)"
          contains: "class Application"
        - path: "src/main/src/main.ts"
          provides: "Main process entry point — instantiates Application"
          contains: "Application"
        - path: "src/main/src/errorReporting.ts"
          provides: "Crash reporter + native error handler tier; HEAD-only Linux-guard surface preserved"
          contains: "errorHandler"
        - path: "src/main/src/TrayIcon.ts"
          provides: "System tray surface; HEAD-only Linux-guard around Tray API preserved"
          contains: "Tray"
        - path: "src/main/src/extensions/autoupdater.ts"
          provides: "electron-updater integration; Linux disposition preserved"
          contains: "autoUpdater"
    key_links:
        - from: "src/main/src/Application.ts"
          to: "src/main/src/cli.ts"
          via: "import"
          pattern: "from.*cli"
        - from: "src/main/src/Application.ts"
          to: "src/main/src/extensions/autoupdater.ts"
          via: "import"
          pattern: "from.*autoupdater"
        - from: "src/main/src/Application.ts"
          to: "src/main/src/TrayIcon.ts"
          via: "import"
          pattern: "from.*TrayIcon"
        - from: "src/main/src/main.ts"
          to: "src/main/src/Application.ts"
          via: "import"
          pattern: "from.*Application"
        - from: "src/main/src/store/ReduxPersistorIPC.ts"
          to: "src/main/src/store/LevelPersist.ts"
          via: "import"
          pattern: "from.*LevelPersist"
        - from: "src/main/src/store/LevelPersist.ts"
          to: "src/main/src/store/SubPersistor.ts"
          via: "import"
          pattern: "from.*SubPersistor"
---

<objective>
Wave C. Resolve all 9 main spine files leaf-first per RESEARCH §Wave-C Coupling. Order: `store/SubPersistor → store/LevelPersist → store/ReduxPersistorIPC → errorReporting → cli → extensions/autoupdater → TrayIcon → Application → main`. Application.ts is the heaviest (11 conflict regions per RESEARCH) and lands at position 8 because it imports from cli + autoupdater + TrayIcon + ReduxPersistorIPC + errorReporting; main.ts lands last because it imports from Application.

Default stance per D-34-02 6-tier hierarchy:

- §3 Linux-guard fork-wins is the dominant tier in this wave — `errorReporting.ts` (native error handler tier), `TrayIcon.ts` (Tray API guard), `extensions/autoupdater.ts` (Linux disposition), `Application.ts` (boot path Linux guards) all carry Linux platform-specific code that v2.0.1 has no equivalent for and must NOT be reverted to upstream
- v2.0.1 feature upstream-wins for new boot-time features (e.g. new IPC channel registrations in Application.ts, new persistence hooks in store/\*)
- Rule-1 dup-import HEAD-empty for split-import-block conflicts
- smaller-diff default for whitespace/comment-only regions

Per D-34-06: bucket-scoped typecheck `pnpm tsc -p src/main/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l` runs at WAVE END (Task C10) — NOT after each file. Application imports from cli during the wave, so per-file typecheck would fail mid-wave; closeout typecheck at the SUMMARY task is the cadence.

Purpose: main spine boots the renderer + brokers all OS integration. Without a clean main bucket no Phase 34 wave downstream of C can verify their work end-to-end. Linux platform guards are non-negotiable per VORTEX-LINUX-MERGE-PLAYBOOK §3 — every commit body must list which Linux-guard surfaces (Tray, autoupdater Linux branch, native error handler tier) were preserved.

Output: 9 SSH-signed `resolve(main): ...` commits in leaf-first order + 1 SUMMARY commit. All 13 harness gates GREEN at end. Bucket-scoped typecheck = 0 non-marker errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-renderer-main-spine-v2-0-1/34-CONTEXT.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-RESEARCH.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-VALIDATION.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-00-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-01-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-02-SUMMARY.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
@CLAUDE.md
@AGENTS.md
</context>

<read_first>

- `.planning/phases/34-renderer-main-spine-v2-0-1/34-CONTEXT.md` — D-34-02 stance hierarchy (especially §3 Linux-guard fork-wins), D-34-06 per-bucket typecheck (wave-end for Wave C), D-34-08 commit body format, D-34-09 no `--no-verify`
- `.planning/phases/34-renderer-main-spine-v2-0-1/34-RESEARCH.md` — Wave-C Coupling table (leaf-first order), Application.ts 11-region heaviness flag, Linux-guard surface inventory
- `VORTEX-LINUX-MERGE-PLAYBOOK.md` — §3 Linux-guard fork-wins discipline, §4–§5 errorReporting + TrayIcon precedent, §7a–d autoupdater disposition
- `src/main/tsconfig.json` (bucket scope for D-34-06 wave-end typecheck)
- Each conflicted file before resolution
  </read_first>

<shared_per_task_workflow>
For every Task C1–C9 (atomic per-file resolves):

1. Run `git diff --check <file>` to enumerate region count for the commit body tally.
2. For each region, classify by D-34-02 6-tier order (§1 playbook → §3 Linux-guard → v2.0.1 upstream-wins → Rule-1 → Rule-2 → smaller-diff).
3. Hand-resolve. Do NOT reformat outside conflict regions.
4. After resolution, verify markers gone: `git grep -nE '^(<{7}|={7}|>{7})( |$)' <file>` returns nothing.
5. Run harness in skip-mode: `bash .planning/phases/34-renderer-main-spine-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check` — must exit 0 (13/13 GREEN).
6. Stage with `git add <file>` and commit with title `resolve(main): <relative-path> — <stance>`.
7. Pattern S5 commit body MUST include: regions tally per stance tier, Linux-guard surfaces preserved (named — e.g. "TrayIcon Linux branch", "errorReporting native-handler tier", "autoupdater Linux disposition") if the file carries any, gate exits (`13/13 GREEN`), harness exit (`0`), typecheck status (`deferred to wave end (D-34-06)`).
8. Verify SSH signature: `git cat-file -p HEAD | grep -c '^gpgsig '` returns >= 1.
   </shared_per_task_workflow>

<tasks>

<task type="auto">
  <name>Task C1: resolve(main): store/SubPersistor.ts</name>
  <files>src/main/src/store/SubPersistor.ts</files>
  <action>Apply shared_per_task_workflow. Leaf of the store sub-cluster — no inbound deps from earlier C-tasks. Stance expected: smaller-diff or Rule-1 dup-import.</action>
  <verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/main/src/store/SubPersistor.ts | wc -l</automated></verify>
  <done>File marker-free; SSH-signed `resolve(main): store/SubPersistor.ts — <stance>` commit; 13/13 GREEN; Pattern S5 body present.</done>
</task>

<task type="auto">
  <name>Task C2: resolve(main): store/LevelPersist.ts</name>
  <files>src/main/src/store/LevelPersist.ts</files>
  <action>Apply shared_per_task_workflow. Imports SubPersistor (resolved in C1). Watch for HEAD-only LevelDB Linux path handling — preserve any Linux-guard branch.</action>
  <verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/main/src/store/LevelPersist.ts | wc -l</automated></verify>
  <done>File marker-free; SSH-signed commit; 13/13 GREEN; Pattern S5 body lists any Linux-guard surfaces preserved.</done>
</task>

<task type="auto">
  <name>Task C3: resolve(main): store/ReduxPersistorIPC.ts</name>
  <files>src/main/src/store/ReduxPersistorIPC.ts</files>
  <action>Apply shared_per_task_workflow. Imports LevelPersist (C2). Watch for new v2.0.1 IPC persistence hooks → upstream-wins. Closes the store sub-cluster.</action>
  <verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/main/src/store/ReduxPersistorIPC.ts | wc -l</automated></verify>
  <done>File marker-free; SSH-signed commit; 13/13 GREEN; Pattern S5 body present.</done>
</task>

<task type="auto">
  <name>Task C4: resolve(main): errorReporting.ts</name>
  <files>src/main/src/errorReporting.ts</files>
  <action>Apply shared_per_task_workflow. Linux-guard heavy: HEAD carries native error handler tier for Linux platform. **§3 Linux-guard fork-wins** is the dominant stance — preserve native handler branch even if v2.0.1 simplified the upstream side. Commit body MUST name "native error handler tier preserved".</action>
  <verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/main/src/errorReporting.ts | wc -l</automated></verify>
  <done>File marker-free; SSH-signed commit; 13/13 GREEN; commit body explicitly lists "native error handler tier preserved" (§3 Linux-guard fork-wins).</done>
</task>

<task type="auto">
  <name>Task C5: resolve(main): cli.ts</name>
  <files>src/main/src/cli.ts</files>
  <action>Apply shared_per_task_workflow. CLI argv parsing; downstream consumed by Application (C8). Stance expected: mostly smaller-diff or Rule-1; any new v2.0.1 CLI flags = upstream-wins.</action>
  <verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/main/src/cli.ts | wc -l</automated></verify>
  <done>File marker-free; SSH-signed commit; 13/13 GREEN; Pattern S5 body present.</done>
</task>

<task type="auto">
  <name>Task C6: resolve(main): extensions/autoupdater.ts</name>
  <files>src/main/src/extensions/autoupdater.ts</files>
  <action>Apply shared_per_task_workflow. **§3 Linux-guard fork-wins** dominant: HEAD carries Linux-disposition branch (per VORTEX-LINUX-MERGE-PLAYBOOK §7a–d) for AppImage/.deb update flow that v2.0.1 has no equivalent for. Commit body MUST name "autoupdater Linux disposition preserved".</action>
  <verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/main/src/extensions/autoupdater.ts | wc -l</automated></verify>
  <done>File marker-free; SSH-signed commit; 13/13 GREEN; commit body explicitly lists "autoupdater Linux disposition preserved" (§3 Linux-guard fork-wins).</done>
</task>

<task type="auto">
  <name>Task C7: resolve(main): TrayIcon.ts</name>
  <files>src/main/src/TrayIcon.ts</files>
  <action>Apply shared_per_task_workflow. **§3 Linux-guard fork-wins** dominant: HEAD carries Linux Tray API guard. Commit body MUST name "TrayIcon Linux branch preserved".</action>
  <verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/main/src/TrayIcon.ts | wc -l</automated></verify>
  <done>File marker-free; SSH-signed commit; 13/13 GREEN; commit body explicitly lists "TrayIcon Linux branch preserved" (§3 Linux-guard fork-wins).</done>
</task>

<task type="auto">
  <name>Task C8: resolve(main): Application.ts (11 regions, heaviest)</name>
  <files>src/main/src/Application.ts</files>
  <action>Apply shared_per_task_workflow. **HEAVIEST file in Wave C**: 11 conflict regions per RESEARCH. Imports from cli (C5), autoupdater (C6), TrayIcon (C7), ReduxPersistorIPC (C3), errorReporting (C4) — all resolved earlier in the wave. Multiple Linux-guard surfaces likely. Run a final `git diff --check` after resolution to confirm no leftover regions. Commit body MUST list every Linux-guard surface preserved + the per-stance region tally for all 11 regions.</action>
  <verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/main/src/Application.ts | wc -l</automated></verify>
  <done>File marker-free; all 11 regions resolved; SSH-signed `resolve(main): Application.ts — <stance>` commit; 13/13 GREEN; Pattern S5 body has full per-stance tally + every Linux-guard surface named.</done>
</task>

<task type="auto">
  <name>Task C9: resolve(main): main.ts</name>
  <files>src/main/src/main.ts</files>
  <action>Apply shared_per_task_workflow. Imports Application (C8). Last main-bucket file. Stance expected: mostly smaller-diff (boot entry is short).</action>
  <verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' src/main/src/main.ts | wc -l</automated></verify>
  <done>File marker-free; SSH-signed commit; 13/13 GREEN; Pattern S5 body present.</done>
</task>

<task type="auto">
  <name>Task C10: wave-end bucket typecheck + SUMMARY</name>
  <files>.planning/phases/34-renderer-main-spine-v2-0-1/34-03-SUMMARY.md</files>
  <action>
1. Run wave-end bucket-scoped typecheck per D-34-06: `pnpm tsc -p src/main/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l` — MUST return `0`. If non-zero, surface errors and STOP — do not write SUMMARY.
2. Write `.planning/phases/34-renderer-main-spine-v2-0-1/34-03-SUMMARY.md` covering:
   - Files resolved (9, leaf-first order with per-file region count + stance distribution)
   - Linux-guard surfaces preserved (named: native error handler tier, TrayIcon Linux branch, autoupdater Linux disposition, Application boot-path guards)
   - Bucket-scoped typecheck output (`0 errors`)
   - Harness state (13/13 GREEN at wave end)
   - Atomic-commit list (9 resolves + this SUMMARY) with short SHAs
3. Stage with `git add -f` (`.planning/` is gitignored).
4. Commit `docs(phase-34): Wave C (main) summary — 9 files, 0 typecheck errors`.
5. Verify SSH signature on the SUMMARY commit.
  </action>
  <verify><automated>pnpm tsc -p src/main/tsconfig.json --noEmit 2>&amp;1 | grep -v TS1185 | wc -l</automated></verify>
  <done>
- `pnpm tsc -p src/main/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l` returns `0`
- 34-03-SUMMARY.md exists with all required sections (esp. Linux-guard preservation evidence)
- SUMMARY committed (force-added) and SSH-signed
- Wave C closed; downstream Wave D may proceed
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                                                                               |
| ------------------------- | --------------------------------------------------------------------------------------------------------- |
| OS process boundary       | Main owns child-process spawn, native module access, IPC orchestration — corrupt main = unbootable Vortex |
| upstream→fork integration | v2.0.1 changes to boot order may collide with HEAD Linux-guard branches                                   |

## STRIDE Threat Register

| Threat ID  | Category          | Component                                                                | Disposition | Mitigation Plan                                                                                                                                                                                                        |
| ---------- | ----------------- | ------------------------------------------------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-34-03-01 | Tampering         | Linux platform guards in errorReporting/TrayIcon/autoupdater/Application | mitigate    | Each Linux-guard-bearing commit body MUST name the preserved surface; D-34-02 §3 Linux-guard fork-wins is invariant; bucket typecheck catches accidental removal of guarded code paths that downstream code references |
| T-34-03-02 | Denial of Service | Application.ts boot path                                                 | mitigate    | Application is the heaviest file (11 regions); leaf-first ordering ensures all imports resolve cleanly first; wave-end typecheck verifies the full main bucket compiles                                                |
| T-34-03-03 | Repudiation       | Atomic commit signing                                                    | mitigate    | Every commit verified via `git cat-file -p HEAD \| grep -c '^gpgsig '` >= 1 (per project memory `feedback_ssh_signing.md`)                                                                                             |
| T-34-03-SC | Tampering         | npm/pip/cargo installs                                                   | n/a         | No new package installs in Phase 34                                                                                                                                                                                    |

</threat_model>

<verification>
- L1 markers: `git grep -nE '^(<{7}|={7}|>{7})( |$)' src/main/` returns 0 lines
- L2 harness: `bash .planning/phases/34-renderer-main-spine-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check` exits 0 (13/13 GREEN)
- L3 bucket typecheck: `pnpm tsc -p src/main/tsconfig.json --noEmit 2>&1 | grep -v TS1185 | wc -l` returns 0
- All 10 commits SSH-signed
- SUMMARY commit body lists every Linux-guard surface preserved
</verification>

<success_criteria>

- 9 atomic resolve commits + 1 SUMMARY commit on `v8.1/config-bucket`
- Bucket typecheck 0 non-marker errors
- Linux-guard surfaces (Tray / autoupdater / native errors / Application boot path) explicitly preserved and named in commit bodies
- Wave C closed; Wave D unblocked
  </success_criteria>

<output>
Write `.planning/phases/34-renderer-main-spine-v2-0-1/34-03-SUMMARY.md` capturing leaf-first resolution order, per-file region tally, Linux-guard preservation evidence, typecheck output, harness state, and commit SHAs.
</output>
