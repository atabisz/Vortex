---
phase: 34
plan: 07
type: execute
wave: G
status: ready
requires:
    - 34-00
    - 34-01
    - 34-02
    - 34-03
    - 34-04
    - 34-05
    - 34-06
depends_on:
    - 34-00
    - 34-01
    - 34-02
    - 34-03
    - 34-04
    - 34-05
    - 34-06
files_modified:
    - CHANGELOG.md
    - CLAUDE.md
    - CONTRIBUTE.md
    - README.md
    - etc/Dependency Report.md
    - etc/vortex.api.md
    - .github/actions/fingerprints/src/clickhouse.ts
    - .github/actions/fingerprints/src/collect-input.ts
    - .github/actions/fingerprints/src/collect-input.test.ts
    - .github/actions/fingerprints/src/collect-pr.ts
    - .github/actions/fingerprints/src/collect-pr.test.ts
    - .github/actions/fingerprints/src/collect-release.ts
    - .github/actions/fingerprints/src/collect-release.test.ts
    - .github/actions/fingerprints/src/index.ts
    - .github/actions/fingerprints/src/types.ts
    - .github/actions/fingerprints/dist/index.js
    - packages/e2e/README.md
    - packages/e2e/fixtures/game-setup/fake-game.ts
    - packages/e2e/fixtures/vortex-app.ts
    - packages/e2e/helpers/navigation.ts
    - packages/e2e/selectors/navbar.ts
    - packages/e2e/selectors/settings.ts
    - packages/e2e/tests/smoke.spec.ts
    - packages/e2e/tests/dashboard.spec.ts
    - packages/e2e/tests/login.spec.ts
    - packages/e2e/tests/game-management.spec.ts
    - packages/e2e/tests/settings.spec.ts
    - scripts/download-duckdb-extensions.ts
    - scripts/download-duckdb-extensions.test.ts
    - tools/addicons/index.html
    - __tests__/reducers.download_management.test.js
    - docs/flatpak/maintenance.md
    - docs/native-node-module-management.md
    - flatpak/generated-sources.json
    - .planning/phases/34-renderer-main-spine-v2-0-1/34-07-SUMMARY.md
autonomous: true
requirements:
    - SYNC-34a
must_haves:
    truths:
        - "All 34 repo-wide leaf files (outside src/, .planning/, extensions/) marker-free, resolved in five sub-batches per CONTEXT D-34-12: G1 (top-level docs + etc/* — 6 parallel-safe) → G2 (`pnpm install --frozen-lockfile=false` — no-commit prep step; any lockfile drift folded into G3.1's commit) → G3 (.github/actions/fingerprints/* — 10 files including dist/index.js regen via ncc) → G4 (packages/e2e/* — 11 sequential leaf-first) → G5 (misc leaves — 7 parallel-safe)"
        - "34 atomic SSH-signed commits on `v8.1/config-bucket`. Per-file slug per D-34-08 Pattern S5 freedom: `resolve(fingerprints): ...`, `resolve(e2e): ...`, `resolve(docs): ...`, `resolve(scripts): ...`, `resolve(tools): ...`, `resolve(repo-leaves): ...` (top-level docs and miscellany)"
        - "G3 fingerprints `dist/index.js` rebuilt deterministically via `pnpm --filter ./.github/actions/fingerprints exec ncc build src/index.ts -o dist`. If ncc unavailable, fallback to smaller-diff per-region resolve with explicit note in commit body — NEVER `git checkout --theirs` (D-34-03)"
        - "G2 `pnpm install --frozen-lockfile=false` runs ONCE before G3 + G4 to populate node_modules. Any `pnpm-lock.yaml` drift gets folded into the G3.1 (first fingerprints) commit so we don't add a bare lockfile commit"
        - "G3 fingerprints typecheck after dist regen: `pnpm tsc -p .github/actions/fingerprints/tsconfig.json --noEmit | grep -v TS1185 | wc -l == 0`"
        - "G4 packages/e2e typecheck after wave: `pnpm tsc -p packages/e2e/tsconfig.json --noEmit | grep -v TS1185 | wc -l == 0`"
        - "G5 scripts validated via `pnpm tsc -p scripts/tsconfig.json --noEmit` (if scripts has its own tsconfig) OR `node --check` for plain JS"
        - "Mid-wave files-still-bearing-markers filter passes: `git grep -lE '^(<{7}|={7}|>{7})( |$)' -- ':!.planning' | wc -l` reaches 0 by wave end"
        - "Harness skip-mode (13 gates) stays GREEN after every commit"
        - "1 SUMMARY commit closes the wave with per-bucket region tally + dist regen evidence + e2e typecheck + scripts validation"
    artifacts:
        - path: ".github/actions/fingerprints/dist/index.js"
          provides: "ncc-bundled GitHub Actions fingerprint compute artifact (rebuilt not hand-resolved per D-34-03 + D-34-12)"
          contains: "module.exports"
        - path: "packages/e2e/tsconfig.json"
          provides: "Playwright e2e bucket scope; depends on pnpm install having run (G2)"
          contains: "compilerOptions"
        - path: ".github/actions/fingerprints/tsconfig.json"
          provides: "Fingerprints action bucket scope"
          contains: "compilerOptions"
    key_links:
        - from: ".github/actions/fingerprints/src/index.ts"
          to: ".github/actions/fingerprints/dist/index.js"
          via: "ncc build pipeline (regenerate, do not hand-resolve dist)"
          pattern: "module.exports"
        - from: "packages/e2e/tests/*.spec.ts"
          to: "packages/e2e/{fixtures,helpers,selectors}/*"
          via: "Playwright fixtures + selectors imports"
          pattern: "from.*(fixtures|helpers|selectors)"
---

<objective>
Wave G. Resolve all 34 repo-wide leaf files (outside `src/`, `.planning/`, `extensions/`) in five sub-batches per CONTEXT D-34-12 + D-34-13:

- **G1 — top-level docs + etc/\* (6 files, parallel-safe)**: CHANGELOG.md, CLAUDE.md, CONTRIBUTE.md, README.md, etc/Dependency Report.md, etc/vortex.api.md.
- **G2 — `pnpm install --frozen-lockfile=false` (no-commit prep step)**: populates `node_modules` for ncc build (G3) and `tsc` against `packages/e2e/tsconfig.json` (G4). Any `pnpm-lock.yaml` drift gets STAGED in this step but COMMITTED as part of G3.1's first fingerprints commit (so the wave doesn't have a bare-lockfile commit). If lockfile drift is large/noisy, surface as planner-discretion note in SUMMARY.
- **G3 — .github/actions/fingerprints/\* (10 files)**: 9 src/\* hand-resolved leaf-first (types → clickhouse → collect-input → collect-input.test → collect-pr → collect-pr.test → collect-release → collect-release.test → index) + 1 `dist/index.js` REGENERATED via `pnpm --filter ./.github/actions/fingerprints exec ncc build src/index.ts -o dist`. If ncc unavailable, fallback to smaller-diff per-region resolve in `dist/index.js` with explicit note in commit body (NOT `git checkout --theirs` — D-34-03).
- **G4 — packages/e2e/\* (11 files, sequential leaf-first within Playwright fixture/selector graph)**: README.md → fixtures/game-setup/fake-game.ts → fixtures/vortex-app.ts → helpers/navigation.ts → selectors/navbar.ts → selectors/settings.ts → tests/smoke.spec.ts → tests/dashboard.spec.ts → tests/login.spec.ts → tests/game-management.spec.ts → tests/settings.spec.ts. Wave-end runs `pnpm tsc -p packages/e2e/tsconfig.json --noEmit`.
- **G5 — misc leaves (7 files, parallel-safe)**: scripts/download-duckdb-extensions.ts, scripts/download-duckdb-extensions.test.ts, tools/addicons/index.html, **tests**/reducers.download_management.test.js, docs/flatpak/maintenance.md, docs/native-node-module-management.md, flatpak/generated-sources.json.

**File count note (planner discretion)**: the deterministic inventory `git grep -l '^<<<<<<< ' -- ':!.planning'` for repo-wide leaves yields 34 entries (10 fingerprints + 6 G1 + 11 e2e + 7 G5). The original gap-closure spec said "33"; this is a count-of-1 discrepancy that surfaces from including `.github/actions/fingerprints/src/types.ts` as a sibling rather than a header. Plan covers all 34 to leave the bucket marker-free; SUMMARY will document the +1 vs spec.

Default stance per D-34-02 6-tier hierarchy:

- v2.0.1 feature upstream-wins for new e2e selectors, new fingerprints columns, new doc sections
- Rule-1 dup-import HEAD-empty for split-import-block conflicts in fingerprints/src/_ and e2e/_
- smaller-diff default for whitespace/comment-only regions in docs and tests
- §3 Linux-guard fork-wins likely in `flatpak/generated-sources.json` (Linux packaging) and `docs/flatpak/maintenance.md` (Linux-only doc)
- D-34-03: no blanket `git checkout --ours` / `--theirs` — every region hand-classified. The G3 dist/index.js fallback (when ncc unavailable) uses smaller-diff per-region resolve, NOT `git checkout --theirs`.

Per D-34-06: per-bucket typecheck runs at sub-batch boundaries within Wave G:

- G3 closes with `pnpm tsc -p .github/actions/fingerprints/tsconfig.json --noEmit | grep -v TS1185 | wc -l == 0`
- G4 closes with `pnpm tsc -p packages/e2e/tsconfig.json --noEmit | grep -v TS1185 | wc -l == 0`
- G5 closes with `node --check` (or `tsc --noEmit`) per-file for scripts/tests where applicable
- G1 (docs) and `tools/addicons/index.html` are non-typecheckable; verification is marker-free + harness GREEN

Mid-wave verification: `git grep -lE '^(<{7}|={7}|>{7})( |$)' -- ':!.planning' | wc -l` reaches 0 by wave end.

Output: 34 SSH-signed commits in G1→G2(prep)→G3→G4→G5 order + 1 SUMMARY commit. All 13 harness gates GREEN at end. All bucket-scoped typechecks for G3/G4 = 0 errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-renderer-main-spine-v2-0-1/34-CONTEXT.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-RESEARCH.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-VALIDATION.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-04-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-05-SUMMARY.md
@.planning/phases/34-renderer-main-spine-v2-0-1/34-06-SUMMARY.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
@CLAUDE.md
@AGENTS.md
</context>

<read_first>

- `.planning/phases/34-renderer-main-spine-v2-0-1/34-CONTEXT.md` — D-34-02 6-tier, D-34-03 (no blanket `--theirs`/`--ours`), D-34-08 commit body, D-34-12 (sub-batches + parallelism), D-34-13 (scope expansion to repo-wide leaves)
- `.github/actions/fingerprints/tsconfig.json` (G3 bucket scope)
- `packages/e2e/tsconfig.json` (G4 bucket scope)
- `pnpm-workspace.yaml` (workspace filter for ncc invocation)
- Each conflicted file before resolution
  </read_first>

<shared_per_task_workflow>
For every Task G1.x / G3.x / G4.x / G5.x (atomic per-file resolves):

1. `git diff --check <file>` to enumerate region count.
2. Classify each region by D-34-02 6-tier order. NEVER use blanket `--theirs`/`--ours` (D-34-03).
3. Hand-resolve. Do NOT reformat outside conflict regions.
4. Markers gone: `git grep -nE '^(<{7}|={7}|>{7})( |$)' <file>` returns nothing.
5. Harness skip-mode: 13/13 GREEN.
6. Mid-wave files-still-bearing-markers count: `git grep -lE '^(<{7}|={7}|>{7})( |$)' -- ':!.planning' | wc -l` — record value, must monotonically decrease to 0.
7. `git add <file>`; commit with title slug per D-34-08 Pattern S5 freedom: `resolve(<bucket-slug>): <relative-path> — <stance>` where bucket-slug ∈ {`repo-leaves`, `fingerprints`, `e2e`, `docs`, `scripts`, `tools`}.
8. Pattern S5 body: regions tally per stance tier, `13/13 GREEN`, harness `0`, sub-batch typecheck status (`deferred to sub-batch end (G3.10/G4.11)` for G3/G4 mid-files; `n/a — non-typecheckable` for G1 docs and tools/addicons; `node --check 0` for G5 scripts/tests where applicable).
9. SSH signature verified.
   </shared_per_task_workflow>

<tasks>

<!-- G1: top-level docs + etc/* (6 files, parallel-safe) -->

<task type="auto"><name>Task G1.1: resolve(repo-leaves): CHANGELOG.md</name><files>CHANGELOG.md</files><action>Apply shared_per_task_workflow. Doc-only; smaller-diff default expected.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' CHANGELOG.md | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G1.2: resolve(repo-leaves): CLAUDE.md</name><files>CLAUDE.md</files><action>Apply shared_per_task_workflow. Doc-only; smaller-diff default. May contain HEAD-only fork-specific GSD wording — preserve fork side for fork-only sections.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' CLAUDE.md | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G1.3: resolve(repo-leaves): CONTRIBUTE.md</name><files>CONTRIBUTE.md</files><action>Apply shared_per_task_workflow. Doc-only.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' CONTRIBUTE.md | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G1.4: resolve(repo-leaves): README.md</name><files>README.md</files><action>Apply shared_per_task_workflow. Doc-only.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' README.md | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G1.5: resolve(repo-leaves): etc/Dependency Report.md</name><files>etc/Dependency Report.md</files><action>Apply shared_per_task_workflow. Generated/manual hybrid doc; v2.0.1 dependency bumps likely → upstream-wins for changed lines, smaller-diff default for header.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' "etc/Dependency Report.md" | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G1.6: resolve(repo-leaves): etc/vortex.api.md</name><files>etc/vortex.api.md</files><action>Apply shared_per_task_workflow. API Extractor output — likely upstream-wins (regenerated artifact). Closes G1.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' etc/vortex.api.md | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<!-- G2: pnpm install prep (no commit) -->

<task type="auto">
  <name>Task G2: pnpm install prep (no commit — drift folded into G3.1)</name>
  <files>node_modules/  (transient — not committed)</files>
  <action>
1. Run `pnpm install --frozen-lockfile=false` to populate `node_modules/` for G3 ncc build and G4 e2e typecheck.
2. Capture `pnpm-lock.yaml` drift (if any): `git diff --stat pnpm-lock.yaml`. If small/clean (typical: 0 bytes if catalog stable from F3.3 D-34-17 branch), STAGE pnpm-lock.yaml so it lands in G3.1's commit. If large/noisy, surface as planner-discretion note in SUMMARY.
3. NO commit at this step. Verification: `node_modules/.modules.yaml` exists; `which ncc || pnpm --filter ./.github/actions/fingerprints exec which ncc` resolves.
  </action>
  <verify><automated>test -f node_modules/.modules.yaml &amp;&amp; pnpm --filter ./.github/actions/fingerprints exec ncc --version</automated></verify>
  <done>
- `node_modules/` populated
- ncc resolvable in fingerprints workspace
- `pnpm-lock.yaml` drift (if any) staged for G3.1 fold-in
  </done>
</task>

<!-- G3: .github/actions/fingerprints/* (10 files) — 9 src hand-resolved + 1 dist regen -->

<task type="auto"><name>Task G3.1: resolve(fingerprints): src/types.ts (+ optional staged pnpm-lock.yaml from G2)</name><files>.github/actions/fingerprints/src/types.ts, pnpm-lock.yaml</files><action>Apply shared_per_task_workflow. Leaf — no fingerprints intra-deps. If G2 staged a small pnpm-lock.yaml drift, FOLD it into this commit (note in body: "pnpm-lock.yaml drift from G2 install folded in"). If no drift, just `src/types.ts`.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' .github/actions/fingerprints/src/types.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN; pnpm-lock fold-in noted (or "no drift") in body.</done></task>

<task type="auto"><name>Task G3.2: resolve(fingerprints): src/clickhouse.ts</name><files>.github/actions/fingerprints/src/clickhouse.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' .github/actions/fingerprints/src/clickhouse.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G3.3: resolve(fingerprints): src/collect-input.ts</name><files>.github/actions/fingerprints/src/collect-input.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' .github/actions/fingerprints/src/collect-input.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G3.4: resolve(fingerprints): src/collect-input.test.ts</name><files>.github/actions/fingerprints/src/collect-input.test.ts</files><action>Apply shared_per_task_workflow. Imports collect-input (G3.3).</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' .github/actions/fingerprints/src/collect-input.test.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G3.5: resolve(fingerprints): src/collect-pr.ts</name><files>.github/actions/fingerprints/src/collect-pr.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' .github/actions/fingerprints/src/collect-pr.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G3.6: resolve(fingerprints): src/collect-pr.test.ts</name><files>.github/actions/fingerprints/src/collect-pr.test.ts</files><action>Apply shared_per_task_workflow. Imports collect-pr (G3.5).</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' .github/actions/fingerprints/src/collect-pr.test.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G3.7: resolve(fingerprints): src/collect-release.ts</name><files>.github/actions/fingerprints/src/collect-release.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' .github/actions/fingerprints/src/collect-release.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G3.8: resolve(fingerprints): src/collect-release.test.ts</name><files>.github/actions/fingerprints/src/collect-release.test.ts</files><action>Apply shared_per_task_workflow. Imports collect-release (G3.7).</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' .github/actions/fingerprints/src/collect-release.test.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G3.9: resolve(fingerprints): src/index.ts</name><files>.github/actions/fingerprints/src/index.ts</files><action>Apply shared_per_task_workflow. Closes fingerprints src — imports types/clickhouse/collect-\* (G3.1-G3.8). Run `pnpm tsc -p .github/actions/fingerprints/tsconfig.json --noEmit | grep -v TS1185 | wc -l` after this commit; MUST return 0 (sub-batch typecheck checkpoint).</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' .github/actions/fingerprints/src/index.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN; fingerprints tsc returns 0.</done></task>

<task type="auto">
  <name>Task G3.10: regen(fingerprints): dist/index.js via ncc build</name>
  <files>.github/actions/fingerprints/dist/index.js</files>
  <action>
1. Regenerate `dist/index.js` deterministically: `pnpm --filter ./.github/actions/fingerprints exec ncc build src/index.ts -o dist`.
2. Verify markers gone: `git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' .github/actions/fingerprints/dist/index.js | wc -l == 0`.
3. **Fallback if ncc unavailable** (D-34-03 reminder: NEVER `git checkout --theirs`): hand-resolve `dist/index.js` per-region using smaller-diff stance for the bundled output. Note explicitly in commit body: "ncc unavailable — dist/index.js resolved per-region with smaller-diff; will be regenerated by next CI build".
4. Harness skip-mode 13/13 GREEN.
5. `git add .github/actions/fingerprints/dist/index.js`; commit `regen(fingerprints): dist/index.js — ncc build` (or `resolve(fingerprints): dist/index.js — smaller-diff per-region (ncc unavailable)` if fallback).
6. SSH signature verified.
  </action>
  <verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' .github/actions/fingerprints/dist/index.js | wc -l</automated></verify>
  <done>
- dist/index.js marker-free
- SSH-signed commit explicitly identifies regen path vs fallback
- 13/13 GREEN
- G3 closed
  </done>
</task>

<!-- G4: packages/e2e/* (11 files, sequential leaf-first) -->

<task type="auto"><name>Task G4.1: resolve(e2e): README.md</name><files>packages/e2e/README.md</files><action>Apply shared_per_task_workflow. Doc-only leaf.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' packages/e2e/README.md | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G4.2: resolve(e2e): fixtures/game-setup/fake-game.ts</name><files>packages/e2e/fixtures/game-setup/fake-game.ts</files><action>Apply shared_per_task_workflow. Fixture leaf.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' packages/e2e/fixtures/game-setup/fake-game.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G4.3: resolve(e2e): fixtures/vortex-app.ts</name><files>packages/e2e/fixtures/vortex-app.ts</files><action>Apply shared_per_task_workflow. May import fake-game (G4.2).</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' packages/e2e/fixtures/vortex-app.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G4.4: resolve(e2e): helpers/navigation.ts</name><files>packages/e2e/helpers/navigation.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' packages/e2e/helpers/navigation.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G4.5: resolve(e2e): selectors/navbar.ts</name><files>packages/e2e/selectors/navbar.ts</files><action>Apply shared_per_task_workflow. v2.0.1 may add new selectors → upstream-wins.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' packages/e2e/selectors/navbar.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G4.6: resolve(e2e): selectors/settings.ts</name><files>packages/e2e/selectors/settings.ts</files><action>Apply shared_per_task_workflow. v2.0.1 may add new selectors → upstream-wins.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' packages/e2e/selectors/settings.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G4.7: resolve(e2e): tests/smoke.spec.ts</name><files>packages/e2e/tests/smoke.spec.ts</files><action>Apply shared_per_task_workflow. Imports fixtures/helpers (G4.2-G4.4) + selectors (G4.5-G4.6).</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' packages/e2e/tests/smoke.spec.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G4.8: resolve(e2e): tests/dashboard.spec.ts</name><files>packages/e2e/tests/dashboard.spec.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' packages/e2e/tests/dashboard.spec.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G4.9: resolve(e2e): tests/login.spec.ts</name><files>packages/e2e/tests/login.spec.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' packages/e2e/tests/login.spec.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G4.10: resolve(e2e): tests/game-management.spec.ts</name><files>packages/e2e/tests/game-management.spec.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' packages/e2e/tests/game-management.spec.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G4.11: resolve(e2e): tests/settings.spec.ts (closes G4 — run e2e tsc)</name><files>packages/e2e/tests/settings.spec.ts</files><action>Apply shared_per_task_workflow. Last G4 file. Run `pnpm tsc -p packages/e2e/tsconfig.json --noEmit | grep -v TS1185 | wc -l` after this commit; MUST return 0 (sub-batch typecheck checkpoint).</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' packages/e2e/tests/settings.spec.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN; e2e tsc returns 0.</done></task>

<!-- G5: misc leaves (7 files, parallel-safe) -->

<task type="auto"><name>Task G5.1: resolve(scripts): download-duckdb-extensions.ts</name><files>scripts/download-duckdb-extensions.ts</files><action>Apply shared_per_task_workflow.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' scripts/download-duckdb-extensions.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G5.2: resolve(scripts): download-duckdb-extensions.test.ts</name><files>scripts/download-duckdb-extensions.test.ts</files><action>Apply shared_per_task_workflow. Imports download-duckdb-extensions (G5.1).</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' scripts/download-duckdb-extensions.test.ts | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G5.3: resolve(tools): tools/addicons/index.html</name><files>tools/addicons/index.html</files><action>Apply shared_per_task_workflow. HTML — non-typecheckable.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' tools/addicons/index.html | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G5.4: resolve(repo-leaves): **tests**/reducers.download_management.test.js</name><files>**tests**/reducers.download_management.test.js</files><action>Apply shared_per_task_workflow. Plain JS — verify with `node --check __tests__/reducers.download_management.test.js`.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' **tests**/reducers.download_management.test.js | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN; `node --check` passes.</done></task>

<task type="auto"><name>Task G5.5: resolve(docs): docs/flatpak/maintenance.md</name><files>docs/flatpak/maintenance.md</files><action>Apply shared_per_task_workflow. Linux-only doc — §3 Linux-guard fork-wins likely for HEAD-only fork-specific maintenance content.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' docs/flatpak/maintenance.md | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G5.6: resolve(docs): docs/native-node-module-management.md</name><files>docs/native-node-module-management.md</files><action>Apply shared_per_task_workflow. Doc-only.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' docs/native-node-module-management.md | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN.</done></task>

<task type="auto"><name>Task G5.7: resolve(repo-leaves): flatpak/generated-sources.json</name><files>flatpak/generated-sources.json</files><action>Apply shared_per_task_workflow. Linux flatpak generated artifact — likely smaller-diff or HEAD-fork-wins for fork-specific source list. Verify JSON validity with `python3 -c 'import json; json.load(open("flatpak/generated-sources.json"))'` or `node -e 'JSON.parse(require("fs").readFileSync("flatpak/generated-sources.json","utf8"))'`. Closes G5 / Wave G.</action><verify><automated>git grep -nE '^(&lt;{7}|={7}|&gt;{7})( |$)' flatpak/generated-sources.json | wc -l</automated></verify><done>marker-free; SSH-signed commit; 13/13 GREEN; JSON parses.</done></task>

<task type="auto">
  <name>Task G33: wave-end SUMMARY</name>
  <files>.planning/phases/34-renderer-main-spine-v2-0-1/34-07-SUMMARY.md</files>
  <action>
1. Confirm `git grep -lE '^(<{7}|={7}|>{7})( |$)' -- ':!.planning' | wc -l == 0` — entire repo (outside .planning/) is marker-free.
2. Sub-batch typecheck re-run (defensive):
   - G3 fingerprints: `pnpm tsc -p .github/actions/fingerprints/tsconfig.json --noEmit | grep -v TS1185 | wc -l == 0`
   - G4 e2e: `pnpm tsc -p packages/e2e/tsconfig.json --noEmit | grep -v TS1185 | wc -l == 0`
3. Harness skip-mode: 13/13 GREEN.
4. Write `.planning/phases/34-renderer-main-spine-v2-0-1/34-07-SUMMARY.md`:
   - Files resolved (34 total: G1=6, G3=10, G4=11, G5=7)
   - G2 prep step outcome (`pnpm-lock.yaml` drift summary; folded into G3.1 if any)
   - G3 dist regen path taken (ncc build success vs fallback per-region resolve)
   - G3 fingerprints typecheck output (`0 errors`)
   - G4 e2e typecheck output (`0 errors`)
   - G5 scripts/tests validation (`node --check` results)
   - File count discrepancy note: deterministic inventory yielded 34 (gap-spec said 33 — +1 surfaces from including `src/types.ts`)
   - Repo-wide marker count (`0 outside .planning/`)
   - Harness state (13/13 GREEN at wave end)
   - Atomic-commit list (34 resolves + this SUMMARY) with short SHAs
5. `git add -f`; commit `docs(phase-34): Wave G (repo-wide leaves) summary — 34 files, 0 typecheck errors`.
6. Verify SSH signature.
  </action>
  <verify><automated>git grep -lE '^(&lt;{7}|={7}|&gt;{7})( |$)' -- ':!.planning' | wc -l</automated></verify>
  <done>
- Repo (outside .planning/) is marker-free
- G3 + G4 typechecks return 0
- 34-07-SUMMARY.md exists with all required sections (esp. dist regen path + 33-vs-34 count note + sub-batch typecheck statuses)
- SUMMARY committed (force-added) and SSH-signed
- Wave G closed; downstream Wave H may proceed
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                     |
| ---------------------------- | ------------------------------------------------------------------------------- |
| dist/index.js bundled output | ncc regen vs hand-resolve: drift between source and bundle = silent CI breakage |
| repo-wide marker hygiene     | leaves block FF-merge if any markers survive                                    |
| Linux platform guard         | flatpak/_ + docs/flatpak/_ contain HEAD-only fork-specific Linux content        |

## STRIDE Threat Register

| Threat ID  | Category    | Component                                                             | Disposition | Mitigation Plan                                                                                                               |
| ---------- | ----------- | --------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------- |
| T-34-07-01 | Tampering   | fingerprints/dist/index.js bundled output drift                       | mitigate    | Regenerate via ncc build; fallback path explicitly forbids `git checkout --theirs` (D-34-03) and uses smaller-diff per-region |
| T-34-07-02 | Tampering   | Linux-guard surfaces in flatpak/_ + docs/flatpak/_                    | mitigate    | §3 Linux-guard fork-wins; preserved surfaces named in commit bodies                                                           |
| T-34-07-03 | Repudiation | 35 atomic commits this wave                                           | mitigate    | SSH signature verified per commit                                                                                             |
| T-34-07-04 | Tampering   | Blanket `--theirs`/`--ours` regression risk (D-34-03)                 | mitigate    | Per-region hand-classification per D-34-02; G3.10 fallback explicitly named as smaller-diff per-region, not blanket checkout  |
| T-34-07-05 | Tampering   | flatpak/generated-sources.json JSON validity                          | mitigate    | Post-resolve JSON parse check in G5.7 verify step                                                                             |
| T-34-07-SC | Tampering   | npm/pip/cargo installs (`pnpm install --frozen-lockfile=false` in G2) | mitigate    | Install runs against existing pnpm-workspace.yaml — no new packages introduced; lockfile drift surfaced and folded into G3.1  |

</threat_model>

<verification>
- L1 markers: `git grep -lE '^(<{7}|={7}|>{7})( |$)' -- ':!.planning' | wc -l == 0`
- L2 harness: `bash .planning/phases/34-renderer-main-spine-v2-0-1/scripts/grep-checkpoint.sh --skip-conflict-check` exits 0 (13/13 GREEN)
- L3 G3 fingerprints typecheck: 0 errors
- L3 G4 e2e typecheck: 0 errors
- All 35 commits SSH-signed
- dist/index.js path documented (regen vs fallback)
</verification>

<success_criteria>

- 34 atomic resolve/regen commits (G1=6, G3=10, G4=11, G5=7) + 1 SUMMARY commit on `v8.1/config-bucket`
- Repo (outside .planning/) is fully marker-free
- G3 + G4 typechecks 0 errors
- Wave G closed; Wave H unblocked
  </success_criteria>

<output>
Write `.planning/phases/34-renderer-main-spine-v2-0-1/34-07-SUMMARY.md` capturing G1/G3/G4/G5 sub-batch order, per-file region tally, dist regen evidence, sub-batch typecheck statuses, file-count discrepancy note, harness state, and commit SHAs.
</output>
