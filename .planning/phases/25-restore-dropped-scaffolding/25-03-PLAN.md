---
phase: 25-restore-dropped-scaffolding
plan: 03
type: execute
wave: 3
depends_on: [25-02]
files_modified:
  - extensions/gamebryo-ba2-support/**
  - pnpm-workspace.yaml
  - pnpm-lock.yaml
  - .github/workflows/release-linux.yml
  - scripts/verify-addons.cjs
autonomous: true
requirements: [SYNC-13]

must_haves:
  truths:
    - "extensions/gamebryo-ba2-support/ exists with named-script Linux guard form per D-25-08 (no inline process.platform checks in package.json)"
    - "pnpm-workspace.yaml catalog has a ba2tk entry (fork git URL OR upstream npm version per Plan 01's discovery decision)"
    - "release-linux.yml has a ba2tk native-rebuild step modeled on the existing bsatk step"
    - "scripts/verify-addons.cjs asserts ba2tk loads"
    - "pnpm install succeeds; ba2-support workspace + ba2tk catalog entry resolve"
  artifacts:
    - path: "extensions/gamebryo-ba2-support/package.json"
      provides: "ba2-support workspace manifest in named-script Linux-guard form"
      contains: "skip-on-windows.mjs"
    - path: "pnpm-workspace.yaml"
      provides: "Catalog entry for ba2tk"
      contains: "ba2tk"
    - path: ".github/workflows/release-linux.yml"
      provides: "CI native-rebuild step for ba2tk"
      contains: "Rebuild ba2tk for Linux"
    - path: "scripts/verify-addons.cjs"
      provides: "Native-addon load assertion list now includes ba2tk"
      contains: "ba2tk"
  key_links:
    - from: "extensions/gamebryo-ba2-support/package.json"
      to: "extensions/skip-on-windows.mjs"
      via: "build/dist scripts call `node ../skip-on-windows.mjs && pnpm run _build`"
      pattern: "skip-on-windows\\.mjs"
    - from: ".github/workflows/release-linux.yml ba2tk step"
      to: "scripts/verify-addons.cjs"
      via: "AppImage build fails loudly if ba2tk rebuild produces no usable .node"
      pattern: "ba2tk"
---

<objective>
Land Phase 25 commit 2 per D-25-04: restore `extensions/gamebryo-ba2-support/` byte-for-byte from `8b5a9f675`, then REWRITE its `package.json` scripts region to the fork's named-script Linux-guard form per D-25-08 (Playbook §1 prohibits inline `process.platform` guards). Add `ba2tk` to `pnpm-workspace.yaml` catalog using the version source decided in Plan 01's `25-DISCOVERY-RESULT.md` (fork git URL if vendored; upstream npm version if not — per D-25-10). Append a ba2tk native-rebuild step to `.github/workflows/release-linux.yml` modeled on the existing bsatk step (D-25-09). Extend `scripts/verify-addons.cjs` with a `ba2tk` assertion (D-25-09). Run `pnpm install` per D-25-06.

Purpose: SYNC-13. Linux fork must build ba2-support without trying to run upstream's Windows-only inline guards, and CI must rebuild ba2tk natively for Linux at AppImage time. The named-script form is the same shape as the existing `gamebryo-bsa-support` package.json; the CI step is the same shape as the existing bsatk rebuild step. This is one atomic commit (D-25-04 commit 2) — the only catalog/lockfile-touching commit in Phase 25.

Output: New extension workspace + catalog entry + CI rebuild + verify-addons assertion in one signed commit titled `restore(extensions): gamebryo-ba2-support + ba2tk catalog entry + CI rebuild`.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-restore-dropped-scaffolding/25-CONTEXT.md
@.planning/phases/25-restore-dropped-scaffolding/25-DISCOVERY-RESULT.md
@extensions/gamebryo-bsa-support/package.json
@extensions/skip-on-windows.mjs
@.github/workflows/release-linux.yml
@scripts/verify-addons.cjs
@pnpm-workspace.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore ba2-support, rewrite its package.json to named-script form, and add ba2tk catalog entry</name>
  <read_first>
    - .planning/phases/25-restore-dropped-scaffolding/25-CONTEXT.md (D-25-08 named-script form, D-25-10 catalog source decision, D-25-15 condition 1 byte-for-byte assertion — note: package.json is intentionally NOT byte-for-byte after rewrite; D-25-15 condition 1 applies to the initial checkout, the post-rewrite package.json is the form to ship)
    - .planning/phases/25-restore-dropped-scaffolding/25-DISCOVERY-RESULT.md `## ba2tk Version Source` (decided in Plan 01)
    - extensions/gamebryo-bsa-support/package.json (closest analog — shape of the named-script form to model)
    - extensions/skip-on-windows.mjs (helper invoked by the rewritten build/dist scripts)
    - pnpm-workspace.yaml (current catalog structure to extend)
  </read_first>
  <action>
    Run `git checkout 8b5a9f675 -- extensions/gamebryo-ba2-support` to land the restored extension. Then REWRITE only `extensions/gamebryo-ba2-support/package.json`'s `scripts` block to match D-25-08's exact form (see CONTEXT.md `<specifics>` "ba2-support package.json — exact form per D-25-08" block — copy that scripts block verbatim, preserving the rest of the package.json from upstream — name, version, dependencies, devDependencies, etc., as restored from `8b5a9f675`). The result must NOT contain any `node -e \"if(process.platform==='win32')\"` or similar inline guard string anywhere in the file.

    Edit `pnpm-workspace.yaml` to add a `ba2tk` entry to the catalog. Source per Plan 01's `## ba2tk Version Source` line in `25-DISCOVERY-RESULT.md`:
    - If `## node-ba2tk Grep Result` recorded a fork-vendored version: catalog entry uses the fork git URL (the same form other custom-fork deps in `pnpm-workspace.yaml` already use — read existing entries to mirror the shape).
    - If grep produced no fork hits: catalog entry pins the exact upstream npm `ba2tk` version recorded in `## ba2tk Version Source` (read from upstream's `pnpm-lock.yaml` at SHA `8b5a9f675`).

    Run `pnpm install` per D-25-06. Lockfile must update with the new `ba2tk` resolution and the new `gamebryo-ba2-support` workspace link. Do not stage anything yet — Task 2 adds the CI step before the single atomic commit.
  </action>
  <verify>
    <automated>test -d extensions/gamebryo-ba2-support && grep -q 'skip-on-windows\.mjs' extensions/gamebryo-ba2-support/package.json && ! grep -E 'process\.platform.*win32|node -e.*platform' extensions/gamebryo-ba2-support/package.json && grep -q 'ba2tk' pnpm-workspace.yaml && pnpm install --reporter=silent 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `extensions/gamebryo-ba2-support/` exists; rest of files (build.mjs, src/**, etc.) byte-for-byte match `8b5a9f675` per `git diff 8b5a9f675 -- extensions/gamebryo-ba2-support ':!extensions/gamebryo-ba2-support/package.json'` producing empty output
    - `extensions/gamebryo-ba2-support/package.json` `scripts` block matches D-25-08 verbatim (verifiable by reading the file and comparing against the CONTEXT.md `<specifics>` block)
    - No inline platform guards anywhere in the file: `! grep -E 'process\\.platform|platform.*win32' extensions/gamebryo-ba2-support/package.json` exits 0
    - `pnpm-workspace.yaml` contains a `ba2tk` catalog entry sourced per `25-DISCOVERY-RESULT.md`'s decision
    - `pnpm install` exits 0; `pnpm-lock.yaml` shows new `ba2tk` resolution
  </acceptance_criteria>
  <done>
    Working tree carries the rewritten extension + catalog entry; install green; ready for the CI step in Task 2 before the atomic commit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append ba2tk rebuild step to release-linux.yml, extend verify-addons.cjs, then atomic commit + frozen-lockfile validation</name>
  <read_first>
    - .planning/phases/25-restore-dropped-scaffolding/25-CONTEXT.md (D-25-09 CI step shape — matches CONTEXT.md `<specifics>` "release-linux.yml ba2tk rebuild step" block; D-25-04 commit 2 title; D-25-05 commit-message format; D-25-06 install discipline)
    - .github/workflows/release-linux.yml (existing bsatk rebuild step — locate it via `grep -n 'bsatk' .github/workflows/release-linux.yml` so the ba2tk step lands immediately after, mirroring shape)
    - scripts/verify-addons.cjs (current addon assertion list — find the existing addon entries and add `ba2tk` alongside)
  </read_first>
  <action>
    Edit `.github/workflows/release-linux.yml`: locate the existing `Rebuild bsatk for Linux` step (grep for `bsatk`), then append a new step immediately after it with the exact YAML shape from CONTEXT.md `<specifics>` "release-linux.yml ba2tk rebuild step" block (the step name is `Rebuild ba2tk for Linux`; the `BA2TK_DIR` resolution uses `require.resolve('ba2tk/package.json', {paths: ['extensions/gamebryo-ba2-support/node_modules', 'node_modules']})`; the `node-gyp rebuild` flags use `--target=39.8.0 --arch=x64 --dist-url=https://electronjs.org/headers`). Indentation must match the existing step's indentation (read the surrounding context).

    Edit `scripts/verify-addons.cjs`: find the existing addon assertion list (likely an array of module names or an object keyed by addon name) and add `ba2tk` to it in the same shape as the existing entries. Per Claude's discretion in CONTEXT.md `<decisions>`, if this turns into more than a one-liner (e.g., the script needs structural changes to handle ba2tk's load path), keep it part of this commit anyway — D-25-04 says commit 2 includes "(D-25-09 if accepted)" extension to verify-addons.

    Run `pnpm install` once more to confirm nothing in the YAML/CJS edits invalidated the workspace graph (it shouldn't, but per D-25-06 each pre-commit install must succeed).

    Stage `extensions/gamebryo-ba2-support`, `pnpm-workspace.yaml`, `pnpm-lock.yaml`, `.github/workflows/release-linux.yml`, `scripts/verify-addons.cjs`. Commit with title `restore(extensions): gamebryo-ba2-support + ba2tk catalog entry + CI rebuild` per D-25-04 commit 2. Body must cite: (a) restoration command `git checkout 8b5a9f675 -- extensions/gamebryo-ba2-support`; (b) the post-restore rewrite of `package.json` to the named-script form per D-25-08 with reason (Playbook §1 prohibits inline platform guards); (c) catalog source decision from `25-DISCOVERY-RESULT.md`; (d) CI step modeled on bsatk per D-25-09. Sign with SSH.

    After commit, run `pnpm install --frozen-lockfile` as a sanity check (not the phase done-gate yet — that runs in Plan 04 — but a useful early signal that catalog stability is intact post-commit, per D-25-15 condition 3 logic).
  </action>
  <verify>
    <automated>git diff HEAD~1 HEAD --name-only | sort | tr '\n' ' ' | grep -qE 'extensions/gamebryo-ba2-support.*pnpm-lock\.yaml.*pnpm-workspace\.yaml.*release-linux\.yml.*verify-addons\.cjs' || git diff HEAD~1 HEAD --name-only | sort && grep -q 'Rebuild ba2tk for Linux' .github/workflows/release-linux.yml && grep -q 'ba2tk' scripts/verify-addons.cjs && pnpm install --frozen-lockfile --reporter=silent</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/release-linux.yml` contains the literal string `Rebuild ba2tk for Linux` and the `node-gyp rebuild --target=39.8.0` flag (verifiable: `grep -c 'Rebuild ba2tk for Linux' .github/workflows/release-linux.yml` returns 1)
    - The new step appears immediately after the existing `Rebuild bsatk for Linux` step (verify with `grep -n -A20 'Rebuild bsatk for Linux' .github/workflows/release-linux.yml | grep -m1 'Rebuild ba2tk for Linux'`)
    - `scripts/verify-addons.cjs` has a `ba2tk` reference where the assertion list lives (mirror existing addon entries)
    - HEAD commit title is exactly `restore(extensions): gamebryo-ba2-support + ba2tk catalog entry + CI rebuild`
    - HEAD commit body cites SHA `8b5a9f675`, references D-25-08 named-script rewrite reason, cites the catalog source decision
    - HEAD commit is signed
    - HEAD commit modifies exactly the five paths in `files_modified` (no others)
    - `pnpm install --frozen-lockfile` exits 0 post-commit (D-25-15 condition 3 reachability)
  </acceptance_criteria>
  <done>
    Commit 2 of 5 landed; ba2-support workspace + catalog + CI + addon assertion all in one atomic, signed commit; frozen-lockfile install green.
  </done>
</task>

</tasks>

<verification>
- Named-script form, no inline platform guards
- Catalog entry resolves; lockfile internally consistent (`--frozen-lockfile` green)
- CI step shape matches existing bsatk step
</verification>

<success_criteria>
SYNC-13 satisfied; one atomic signed commit added; catalog stable for the rest of Phase 25.
</success_criteria>

<output>
After completion, create `.planning/phases/25-restore-dropped-scaffolding/25-03-SUMMARY.md`. Cite the commit SHA, the catalog source chosen, the line ranges modified in `release-linux.yml` and `verify-addons.cjs`, and the frozen-lockfile install result.
</output>
