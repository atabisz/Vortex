---
phase: 37-carry-forward-uat-v2-0-1
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
    - .planning/phases/37-carry-forward-uat-v2-0-1/37-CANONICAL-SMOKE-EVIDENCE.md
    - .planning/phases/37-carry-forward-uat-v2-0-1/screenshots/
autonomous: false
requirements: [SYNC-37a]

must_haves:
    truths:
        - "AppImage boots from ~/Downloads/ to first window with no fatal/unhandled errors"
        - ".deb installs via apt, registers /usr/bin/vortex shim, launches to first window"
        - "Skyrim SE 4-checkpoint walkthrough captured (real-usage roll-up table OR 4 PNG screenshots)"
        - "37-CANONICAL-SMOKE-EVIDENCE.md committed to git with all three SYNC-37a sections marked PASS"
    artifacts:
        - path: ".planning/phases/37-carry-forward-uat-v2-0-1/37-CANONICAL-SMOKE-EVIDENCE.md"
          provides: "Complete SYNC-37a evidence (3 sections all marked PASS, boot logs cited, Skyrim walkthrough filled)"
          contains: "Result: PASS"
        - path: ".planning/phases/37-carry-forward-uat-v2-0-1/screenshots/"
          provides: "Optional PNG screenshots if contrived-walkthrough fallback used (per D-37-02 fallback)"
    key_links:
        - from: ".planning/phases/37-carry-forward-uat-v2-0-1/37-CANONICAL-SMOKE-EVIDENCE.md"
          to: "~/Downloads/vortex-setup.AppImage + ~/Downloads/vortex_amd64.deb (Wave 1 outputs)"
          via: "verbatim sha256sum cross-check + boot log evidence"
          pattern: "Result: PASS"
---

<objective>
Wave 2 SYNC-37a UAT: operator-driven local-boot of both canonical artefacts (AppImage + .deb) on the daily-driver Linux laptop, plus 4-checkpoint Skyrim SE walkthrough (default = real-usage roll-up per D-37-02; fallback = 4 PNG screenshots). Fill in the Wave 1 evidence-file scaffold with PASS verdicts + boot evidence and commit it under `git add -f`.

Purpose: gates done-gate criteria 1, 2, and 3 from D-37-10. Without operator-confirmed PASS verdicts in `37-CANONICAL-SMOKE-EVIDENCE.md`, SYNC-37a cannot tick.
Output: one updated evidence file (3 sections all PASS), optional `screenshots/*.png` if contrived-walkthrough fallback chosen, one SSH-signed commit on master.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-carry-forward-uat-v2-0-1/37-CONTEXT.md
@.planning/phases/37-carry-forward-uat-v2-0-1/37-RESEARCH.md
@.planning/phases/36-land-tag-cherry-pick-v2-0-1/36-DONE-GATE.md
@.planning/milestones/v8.0-phases/29-build-verification/29-SMOKE-EVIDENCE.md
@.planning/phases/37-carry-forward-uat-v2-0-1/37-CANONICAL-SMOKE-EVIDENCE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2.1: AppImage local-boot UAT — operator runs canonical AppImage and confirms boot invariants</name>
  <files>.planning/phases/37-carry-forward-uat-v2-0-1/37-CANONICAL-SMOKE-EVIDENCE.md, /tmp/boot-37-appimage.log</files>
  <read_first>
    - .planning/phases/37-carry-forward-uat-v2-0-1/37-RESEARCH.md (## Local-boot UAT mechanics → AppImage local-boot section — 6-step recipe; ## Common Pitfalls → libfuse2 fallback)
    - .planning/phases/37-carry-forward-uat-v2-0-1/37-CANONICAL-SMOKE-EVIDENCE.md (Wave 1 scaffold — fill the AppImage section in place)
    - .planning/milestones/v8.0-phases/29-build-verification/29-SMOKE-EVIDENCE.md ([ERRO] triage table — known-benign categories: auto-updater 404, Windows-only-game guards)
  </read_first>
  <action>
    Operator-driven AppImage local-boot UAT against the canonical artefact downloaded in Wave 1. Claude's role here is to (a) present the operator with the exact reproduction steps (see `<how-to-verify>`), (b) parse the operator's PASS/FAIL response and any boot-log paths cited, and (c) update `## SYNC-37a — Local-boot AppImage` section in `37-CANONICAL-SMOKE-EVIDENCE.md` with the boot timestamp, Result PASS verdict, window evidence (wmctrl line + screenshot path `screenshots/appimage-about.png`), extension count (expect 132 per Phase 35 floor 130), [ERRO] triage table (rows must categorise into known-benign per `29-SMOKE-EVIDENCE.md`), boot tail showing `gamemode-activated` for `skyrimse`, and Verdict paragraph. If operator reports a new ERRO category, append the row to `## Findings — queued to Phase 999.1` per D-37-04 (don't expand Phase 37 scope). Do NOT commit yet — Task 2.3 commits the evidence file after all 3 sections fill.
  </action>
  <what-built>
    Claude pre-built (Wave 1): canonical AppImage downloaded to `~/Downloads/vortex-setup.AppImage`, executable bit set, SHA256 verified `13aa29288e8936a4dd7cdc3c9f3f669d15c7c65d3d416efee8ab2ba957059c9b`. Evidence file scaffold ready at `.planning/phases/37-carry-forward-uat-v2-0-1/37-CANONICAL-SMOKE-EVIDENCE.md` with the AppImage section's command/trigger and SHA256 block pre-filled.
  </what-built>
  <how-to-verify>
    1. In a terminal, run: `~/Downloads/vortex-setup.AppImage 2>&1 | tee /tmp/boot-37-appimage.log` (per D-37-01).
    2. If launch fails with FUSE error (e.g. "AppImages require FUSE to run"), fall back to `~/Downloads/vortex-setup.AppImage --appimage-extract-and-run 2>&1 | tee /tmp/boot-37-appimage.log` and note the fallback in the evidence file (per RESEARCH.md `libfuse2` note).
    3. Confirm the main Vortex window opens (`wmctrl -l | grep -i Vortex` shows a row).
    4. Confirm extension count: `grep -c 'loaded extension' /tmp/boot-37-appimage.log` returns ≥ 130 (Phase 35 floor; expected 132).
    5. Confirm zero `fatal` / `unhandled` / `crashed` lines: `grep -E -i 'fatal|unhandled|crashed' /tmp/boot-37-appimage.log` returns 0 lines.
    6. Triage `[ERRO]` lines: `grep -c '\[ERRO\]' /tmp/boot-37-appimage.log` — every ERRO line must fall into a known-benign category (auto-updater 404, Windows-only-game guards) per `29-SMOKE-EVIDENCE.md`. Any new category = file as a finding under `## Findings — queued to Phase 999.1` in the evidence file.
    7. Confirm boot tail shows `gamemode-activated` for `skyrimse` (or other expected game): `tail -50 /tmp/boot-37-appimage.log | grep gamemode-activated`.
    8. Capture screenshot of About dialog or window title showing version `v2.0.1-linux-rebased` and/or electron-builder internal `1.16.202605230443`. Save under `.planning/phases/37-carry-forward-uat-v2-0-1/screenshots/appimage-about.png` (PNG only, no HEIC/JPEG per code_context). Tool is operator's discretion (Wayland: `gnome-screenshot` or `grim`; X11: `gnome-screenshot` or `ksnip`).
    9. Update `37-CANONICAL-SMOKE-EVIDENCE.md` `## SYNC-37a — Local-boot AppImage` section in place: fill boot timestamp + wall-clock to first render, `Result: PASS`, window evidence (wmctrl line + screenshot path), extension count (132), [ERRO] triage table (all rows known-benign), boot tail (last 5 lines including `gamemode-activated`), Verdict paragraph.
  </how-to-verify>
  <verify>
    <automated>grep -q '^## SYNC-37a — Local-boot AppImage' .planning/phases/37-carry-forward-uat-v2-0-1/37-CANONICAL-SMOKE-EVIDENCE.md &amp;&amp; awk '/^## SYNC-37a — Local-boot AppImage/,/^## /' .planning/phases/37-carry-forward-uat-v2-0-1/37-CANONICAL-SMOKE-EVIDENCE.md | grep -q -i 'Result.*PASS'</automated>
  </verify>
  <acceptance_criteria>
    - `## SYNC-37a — Local-boot AppImage` section in `37-CANONICAL-SMOKE-EVIDENCE.md` contains a `Result: PASS` line.
    - Section cites a wmctrl-style window-evidence line (e.g. `0x... Vortex` or screenshot path `screenshots/appimage-about.png`).
    - Section cites extension count ≥ 130 (expected 132).
    - Section cites zero fatal/unhandled/crashed lines.
    - Section's [ERRO] triage table has all rows categorised into known-benign categories matching `29-SMOKE-EVIDENCE.md` taxonomy, OR any new category recorded under `## Findings — queued to Phase 999.1`.
    - Section cites boot tail showing `gamemode-activated` for `skyrimse`.
    - Screenshot file `.planning/phases/37-carry-forward-uat-v2-0-1/screenshots/appimage-about.png` exists OR section explains why screenshot was skipped (e.g. version visible in window title only).
    - SHA256 cross-check block in this section already shows `13aa29288e8936a4dd7cdc3c9f3f669d15c7c65d3d416efee8ab2ba957059c9b` (pre-filled in Wave 1).
  </acceptance_criteria>
  <resume-signal>Type "approved" with PASS verdict, OR describe any boot failure / new ERRO category for triage.</resume-signal>
  <done>AppImage section of evidence file shows Result: PASS with boot evidence; ready for Task 2.2 to fill .deb section.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2.2: .deb local-boot UAT — operator installs canonical .deb via apt and confirms launch via desktop entry</name>
  <files>.planning/phases/37-carry-forward-uat-v2-0-1/37-CANONICAL-SMOKE-EVIDENCE.md, /tmp/boot-37-deb.log</files>
  <read_first>
    - .planning/phases/37-carry-forward-uat-v2-0-1/37-RESEARCH.md (## Local-boot UAT mechanics → .deb local-boot section — 6-step recipe)
    - .planning/phases/37-carry-forward-uat-v2-0-1/37-CANONICAL-SMOKE-EVIDENCE.md (Wave 1 scaffold — fill the .deb section in place; AppImage section already complete from Task 2.1)
  </read_first>
  <action>
    Operator-driven .deb local-boot UAT. Claude's role: (a) present the apt-install + launch reproduction steps to the operator, (b) parse the operator's PASS/FAIL response, (c) update `## SYNC-37a — Local-boot .deb` section in `37-CANONICAL-SMOKE-EVIDENCE.md` with apt dependency-resolution log lines, boot timestamp, Result PASS verdict, window evidence + screenshot paths (`screenshots/deb-window.png` and `screenshots/deb-menu.png`), extension count, [ERRO] triage, boot tail showing `gamemode-activated` for `skyrimse`, and Verdict paragraph. apt is preferred over `dpkg -i` per D-37-01 because apt resolves dependencies + registers the desktop entry. Do NOT commit yet — Task 2.3 commits the evidence file.
  </action>
  <what-built>
    Claude pre-built (Wave 1): `~/Downloads/vortex_amd64.deb` downloaded, SHA256 verified `3d82353963d3625865bcd9281862172ede2a6f860812cc52579f1c1d7b22f3a6`. Evidence file `.deb` section's command/trigger and SHA256 block pre-filled.
  </what-built>
  <how-to-verify>
    1. Install via apt (per D-37-01 — preferred over `dpkg -i` because apt resolves dependencies + registers desktop entry): `sudo apt install ~/Downloads/vortex_amd64.deb`. Capture the apt prompt + dependency list (paste into evidence file).
    2. Verify desktop entry registered: `test -f /usr/share/applications/vortex.desktop && echo OK`.
    3. Verify shim installed: `test -x /usr/bin/vortex && echo OK`.
    4. Launch via either: (a) Click "Vortex" from Activities/Applications launcher, OR (b) `vortex 2>&1 | tee /tmp/boot-37-deb.log` from a terminal. Same boot invariants as AppImage path apply: window opens, extension count ≥ 130, zero fatal/unhandled/crashed, [ERRO] lines all known-benign, boot tail shows `gamemode-activated` for `skyrimse`.
    5. Capture screenshot of: (a) the launched Vortex window, AND (b) the desktop entry showing Vortex in the system menu. Save as `.planning/phases/37-carry-forward-uat-v2-0-1/screenshots/deb-window.png` and `screenshots/deb-menu.png` (PNG only).
    6. Update `37-CANONICAL-SMOKE-EVIDENCE.md` `## SYNC-37a — Local-boot .deb` section in place with same fields as Task 2.1 (boot timestamp, Result PASS, window evidence, extension count, ERRO triage, boot tail, Verdict). Cite the apt-install dependency-resolution log lines.
    7. After UAT PASS, the .deb install can stay or be removed via `sudo apt remove vortex` — operator's choice (RESEARCH.md "Runtime State Inventory" notes this is intentional UAT-induced state).
  </how-to-verify>
  <verify>
    <automated>grep -q '^## SYNC-37a — Local-boot .deb' .planning/phases/37-carry-forward-uat-v2-0-1/37-CANONICAL-SMOKE-EVIDENCE.md &amp;&amp; awk '/^## SYNC-37a — Local-boot .deb/,/^## /' .planning/phases/37-carry-forward-uat-v2-0-1/37-CANONICAL-SMOKE-EVIDENCE.md | grep -q -i 'Result.*PASS' &amp;&amp; awk '/^## SYNC-37a — Local-boot .deb/,/^## /' .planning/phases/37-carry-forward-uat-v2-0-1/37-CANONICAL-SMOKE-EVIDENCE.md | grep -q '/usr/bin/vortex'</automated>
  </verify>
  <acceptance_criteria>
    - `## SYNC-37a — Local-boot .deb` section in `37-CANONICAL-SMOKE-EVIDENCE.md` contains a `Result: PASS` line.
    - Section cites `/usr/bin/vortex` shim presence and `/usr/share/applications/vortex.desktop` registration.
    - Section cites apt dependency-resolution log lines.
    - Section cites extension count ≥ 130 and zero fatal/unhandled/crashed lines.
    - Section cites boot tail showing `gamemode-activated` for `skyrimse`.
    - Screenshot files `screenshots/deb-window.png` and `screenshots/deb-menu.png` exist OR section explains why screenshots were skipped.
    - SHA256 cross-check block in this section already shows `3d82353963d3625865bcd9281862172ede2a6f860812cc52579f1c1d7b22f3a6` (pre-filled in Wave 1).
  </acceptance_criteria>
  <resume-signal>Type "approved" with PASS verdict, OR describe any install/launch failure for triage.</resume-signal>
  <done>.deb section of evidence file shows Result: PASS; ready for Task 2.3 to fill Skyrim section + commit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2.3: Skyrim SE 4-checkpoint walkthrough (real-usage roll-up DEFAULT, contrived 5-min smoke FALLBACK) + commit evidence file</name>
  <files>.planning/phases/37-carry-forward-uat-v2-0-1/37-CANONICAL-SMOKE-EVIDENCE.md, .planning/phases/37-carry-forward-uat-v2-0-1/screenshots/skyrim-*.png</files>
  <read_first>
    - .planning/phases/37-carry-forward-uat-v2-0-1/37-CONTEXT.md (`<specifics>` 4 Skyrim checkpoints with playbook section cross-refs)
    - .planning/phases/37-carry-forward-uat-v2-0-1/37-RESEARCH.md (## Local-boot UAT mechanics → Skyrim SE walkthrough — D-37-02 default vs fallback; ## Common Pitfalls → Pitfall 6 cite session timestamps after `799ad300f`)
    - .planning/milestones/v8.0-phases/29-build-verification/29-SMOKE-EVIDENCE.md (SYNC-34 Real-usage evidence table — 4-row format to clone for the Skyrim section)
    - .planning/phases/37-carry-forward-uat-v2-0-1/37-CANONICAL-SMOKE-EVIDENCE.md (fills the Skyrim SE section)
  </read_first>
  <action>
    Operator-driven Skyrim SE walkthrough — Path A (real-usage roll-up) is the DEFAULT per D-37-02; Path B (contrived 5-min smoke with 4 PNG screenshots) is the FALLBACK only if real-usage roll-up isn't available within the day. Claude's role: (a) present both paths to the operator, (b) parse choice + PASS/FAIL response, (c) update `## SYNC-37a — Skyrim SE walkthrough` section with the chosen path's evidence (4-row table filled with prose for Path A, OR 4 screenshot paths cited for Path B); set `Result: PASS` (or `PASS-WITH-FINDING` if a regression surfaced — append the finding to `## Findings — queued to Phase 999.1` per D-37-04 + RESEARCH.md Open Question 4), (d) `git add -f` the evidence file (and `screenshots/*.png` if Path B) per D-37-12 + Pitfall 2, (e) commit SSH-signed (no `--no-verify` per D-37-13 — set up corepack PATH first per Pitfall 4 if `pnpm` not on PATH), (f) verify SSH signature `git log -1 --format='%G?'` returns `G`. Do NOT push yet. Wave 3's push (Plan 37-03 Task 3.2) lands on master and carries this Wave 2 evidence commit forward to fork/master as part of the SYNC-37b push. Plans 37-02 and 37-03 are sequential (Wave 2 before Wave 3), so by the time Wave 3 pushes, this commit is already in master's local history and ships with it.
  </action>
  <what-built>
    Claude pre-built (Wave 1): evidence file scaffold's Skyrim section already contains the 4-row table skeleton with the D-37-02 checkpoints. AppImage and .deb sections completed in Tasks 2.1 + 2.2.
  </what-built>
  <how-to-verify>
    Operator chooses ONE of two paths per D-37-02:

    **Path A — Real-usage roll-up (DEFAULT).** Use today-or-later sessions on the daily-driver Skyrim SE workflow (per Pitfall 6 — only sessions after Phase 36 close commit `799ad300f` push timestamp). Fill the 4-row table with prose evidence:
    | D-37-02 smoke step | Evidence | Playbook section | Confirms |
    |---|---|---|---|
    | Game detection | `gamemode-activated` for skyrimse in boot log; `gamePath`/`stagingPath` resolved to `/media/alex/intel/Vortex/SkyrimSE` (or equivalent) | §9 findAllLinuxSteamPaths | SYNC-25 |
    | NXM mod install + staging populated | Active staging populated; backslash-paths cluster invariants hold | §6/§7a-d staging integrity, backslash-paths | SYNC-22/23 |
    | Hardlink deploy + LOOT autosort | Deploy/purge sessions clean; gamebryo native binaries loaded; no "ghost file" errors (playbook §3 LOOT casing rule active) | §3 LOOT autosort + §4 testPathTransfer + §10 cross-compiled gamebryo | SYNC-19/20/26 |
    | Proton launch + tray-icon visible | Steam → Proton launch + Vortex tray hides per §8 hide-on-spawn invariant | §8 StarterInfo Proton helpers + hide-on-spawn | SYNC-24 |
    No screenshots required for Path A — prose evidence with timestamps suffices.

    **Path B — Contrived 5-min smoke (FALLBACK only if real-usage roll-up unavailable within the day).** Run a clean Skyrim SE Steam session against the canonical AppImage installed in Task 2.1. Capture 4 PNG screenshots (one per checkpoint) under `.planning/phases/37-carry-forward-uat-v2-0-1/screenshots/`:
    - `skyrim-01-detection.png` — Vortex dashboard showing Skyrim SE detected with version visible.
    - `skyrim-02-staging.png` — Mods page after NXM install (or manual install) with staging directory populated.
    - `skyrim-03-deploy-loot.png` — post-Deploy view with LOOT autosort completed and plugin order updated.
    - `skyrim-04-proton-tray.png` — Vortex tray icon visible while Skyrim SE launches via Proton.
    Cite all 4 paths in the evidence file's Skyrim section.

    After ONE path completes:
    1. Update `37-CANONICAL-SMOKE-EVIDENCE.md` `## SYNC-37a — Skyrim SE walkthrough` section with the chosen path's evidence (table filled with prose for Path A, OR 4 screenshot paths cited for Path B). Set `Result: PASS` (or `PASS-WITH-FINDING` per RESEARCH.md Open Question 4 if a regression surfaced; queue to Phase 999.1 in the `## Findings` section).
    2. Set up corepack PATH if needed: `export PATH="/home/alex/.local/share/pnpm/nodejs/22.14.0/lib/node_modules/corepack/shims:$PATH"` (per D-37-13 + Pitfall 4).
    3. `git add -f .planning/phases/37-carry-forward-uat-v2-0-1/37-CANONICAL-SMOKE-EVIDENCE.md` (gitignored path per D-37-12 + Pitfall 2). If Path B chosen, also `git add -f .planning/phases/37-carry-forward-uat-v2-0-1/screenshots/*.png`.
    4. Commit SSH-signed (no `--no-verify` per D-37-13): `git commit -m 'docs(37): canonical smoke evidence — SYNC-37a AppImage + .deb local-boot + Skyrim walkthrough PASS'` with body citing AppImage SHA `13aa29288...` matched, .deb SHA `3d82353963...` matched, walkthrough path used (real-usage roll-up OR contrived 4-screenshot), and tag `v2.0.1-linux-rebased` Phase 36 release URL `https://github.com/atabisz/Vortex/releases/tag/v2.0.1-linux-rebased`.
    5. Verify SSH signature: `git log -1 --format='%G?'` MUST return `G`.
    6. Stay on master; do NOT push from Wave 2. Wave 3's push (Plan 37-03 Task 3.2) lands on master and carries this Wave 2 evidence commit forward to fork/master as part of the SYNC-37b push. Plans 37-02 and 37-03 are sequential (37-03 depends on 37-02), so this commit is guaranteed to be in master's local history before Wave 3 pushes.

  </how-to-verify>
  <verify>
    <automated>grep -q '^## SYNC-37a — Skyrim SE walkthrough' .planning/phases/37-carry-forward-uat-v2-0-1/37-CANONICAL-SMOKE-EVIDENCE.md &amp;&amp; awk '/^## SYNC-37a — Skyrim SE walkthrough/,/^## /' .planning/phases/37-carry-forward-uat-v2-0-1/37-CANONICAL-SMOKE-EVIDENCE.md | grep -q -i 'Result.*PASS' &amp;&amp; git log -1 --format='%s' | grep -q 'canonical smoke evidence' &amp;&amp; git log -1 --format='%G?' | grep -q '^G$' &amp;&amp; git log -1 --name-only --format='' | grep -q '37-CANONICAL-SMOKE-EVIDENCE.md'</automated>
  </verify>
  <acceptance_criteria>
    - `## SYNC-37a — Skyrim SE walkthrough` section contains `Result: PASS` (or `PASS-WITH-FINDING` with finding logged).
    - Path A chosen: 4-row table filled with prose evidence including the literal phrases "gamemode-activated" (Game detection row) and "Proton" (last row).
    - Path B chosen: 4 PNG paths cited (`screenshots/skyrim-01-detection.png` … `screenshots/skyrim-04-proton-tray.png`) and corresponding files exist on disk.
    - Commit subject contains `canonical smoke evidence`.
    - Commit body cites both AppImage SHA `13aa29288...` and .deb SHA `3d82353963...` matched.
    - Commit touches `.planning/phases/37-carry-forward-uat-v2-0-1/37-CANONICAL-SMOKE-EVIDENCE.md` (and optionally `screenshots/*.png` if Path B).
    - `git log -1 --format='%G?'` returns `G` (SSH signature good).
    - Zero `--no-verify` invocations.
    - Commit NOT yet pushed from Wave 2 (Wave 3's push in Plan 37-03 Task 3.2 carries this commit to fork/master).
  </acceptance_criteria>
  <resume-signal>Type "approved" with PASS verdict + path chosen (real-usage OR contrived) + commit SHA, OR describe any walkthrough failure.</resume-signal>
  <done>All 3 SYNC-37a sections in evidence file marked PASS; SSH-signed commit lands on master locally; Wave 3's push will carry this commit to fork/master alongside SYNC-37b.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                                                                                                                             |
| ---------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| .deb install → system PATH         | apt registers `/usr/bin/vortex` shim and `/usr/share/applications/vortex.desktop`; trust boundary is package signing (Phase 36's release-linux.yml run; no further signing in Phase 37) |
| AppImage runtime → user filesystem | AppImage runs as user; reads/writes `~/.config/Vortex` and staging path                                                                                                                 |

## STRIDE Threat Register

| Threat ID | Category    | Component                                                                                  | Disposition | Mitigation Plan                                                                                                                                                                 |
| --------- | ----------- | ------------------------------------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-37-03   | Tampering   | UAT-induced regression in canonical AppImage that didn't surface in Phase 36 release-smoke | mitigate    | Triage any new `[ERRO]` category; queue regressions to Phase 999.1 per D-37-04 (don't expand Phase 37 scope)                                                                    |
| T-37-04   | Repudiation | Evidence file claims PASS but operator skipped a step                                      | mitigate    | Verbatim acceptance text per D-37-03 — every section requires Result + Window evidence + Extension count + boot tail; checkpoint:human-verify gate forces operator confirmation |

</threat_model>

<verification>
- All three `## SYNC-37a — *` sections in `37-CANONICAL-SMOKE-EVIDENCE.md` show `Result: PASS` (or `PASS-WITH-FINDING` with finding logged).
- AppImage boot log shows extension count ≥ 130 and zero fatal/unhandled/crashed lines.
- .deb installed via apt; `/usr/bin/vortex` shim present; `/usr/share/applications/vortex.desktop` present.
- Skyrim section filled via Path A (real-usage table) or Path B (4 PNG screenshots cited).
- Evidence file committed via `git add -f` + SSH-signed (`git log -1 --format='%G?'` = `G`).
</verification>

<success_criteria>

- Wave 2 commit lands on master, SSH-signed, evidence file shows 3 SYNC-37a sections all PASS.
- Done-gate criteria 1, 2, 3 (D-37-10) all satisfiable from this evidence file.
  </success_criteria>

<output>
Create `.planning/phases/37-carry-forward-uat-v2-0-1/37-02-SUMMARY.md` when done.
</output>
