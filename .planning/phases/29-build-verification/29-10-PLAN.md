---
phase: 29-build-verification
plan: 10
subsystem: done-gate
type: execute
wave: 11
depends_on: [29-09]
files_modified:
    - .planning/phases/29-build-verification/29-DONE-GATE.md
    - .planning/STATE.md
    - .planning/ROADMAP.md
autonomous: false
requirements: [SYNC-01, SYNC-21, SYNC-28, SYNC-29, SYNC-30, SYNC-31, SYNC-32, SYNC-33, SYNC-34]
tags:
    [
        linux-port,
        upstream-v2.0.0,
        phase-29,
        done-gate,
        sync-01,
        sync-21,
        sync-28,
        sync-29,
        sync-30,
        sync-31,
        sync-32,
        sync-33,
        sync-34,
    ]

requirements_in_play:
    [SYNC-01, SYNC-21, SYNC-28, SYNC-29, SYNC-30, SYNC-31, SYNC-32, SYNC-33, SYNC-34]

dependency_graph:
    requires:
        - "29-00..29-09 complete with PASS verdicts captured in 29-VERIFY-RESULTS.md, 29-LINT-BASELINE.md, 29-SMOKE-EVIDENCE.md"
    provides:
        - ".planning/phases/29-build-verification/29-DONE-GATE.md (rolls up all 9 SYNC requirements)"
        - "STATE.md updated: Phase 29 complete; ready for Phase 30 (land + tag)"
        - "ROADMAP.md updated: Phase 29 row checkbox flipped, success criteria checked"
    affects:
        - "Phase 30 (gates the milestone tag — 30 cannot start until this gate is closed)"

gates:
    pre_conditions:
        - "29-09 complete (SYNC-34 PASS)"
        - "29-VERIFY-RESULTS.md has PASS sections for SYNC-01, SYNC-28, SYNC-29, SYNC-30, SYNC-21, SYNC-31, SYNC-32"
        - "29-SMOKE-EVIDENCE.md has PASS sections for SYNC-33 (parts A/B/C) and SYNC-34"
        - "29-LINT-BASELINE.md has full delta table + PASS verdict for SYNC-32"
        - "Working tree clean: git status --short empty"
    post_conditions:
        - "29-DONE-GATE.md exists with one section per SYNC requirement; evidence quoted; all 9 PASS"
        - "STATE.md frontmatter updated: completed_phases=6, completed_plans+=11, stopped_at='Phase 29 complete; ready for Phase 30 land + tag'"
        - "ROADMAP.md Phase 29 entry: checkbox `[x]`, all 5 success criteria checked"
        - "Two commits land: `docs(29-10): phase 29 done-gate evidence` + `docs(29): mark phase 29 complete in roadmap + state`"
        - "Both commits SSH-signed; pushed to fork remote"

commands:
    - "git rev-parse --abbrev-ref HEAD"
    - "git status --short"
    - "git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | head -20"
    - "git ls-remote git@github.com:atabisz/Vortex.git refs/heads/sync/upstream-v2.0.0"
    - "git add -f .planning/phases/29-build-verification/29-DONE-GATE.md"
    - "git commit -m 'docs(29-10): phase 29 done-gate evidence'"
    - "git log -1 --show-signature"
    - "git push git@github.com:atabisz/Vortex.git v8.0/config-bucket:sync/upstream-v2.0.0"
    - "git add -f .planning/STATE.md .planning/ROADMAP.md"
    - "git commit -m 'docs(29): mark phase 29 complete in roadmap + state'"
    - "git log -1 --show-signature"
    - "git push git@github.com:atabisz/Vortex.git v8.0/config-bucket:sync/upstream-v2.0.0"

verification:
    - "29-DONE-GATE.md has 9 sections (one per SYNC requirement) plus a phase-status line and a final timestamped 'Phase 29 status: COMPLETE'"
    - "STATE.md and ROADMAP.md updates landed and pushed"
    - "Both commits SSH-signed"
    - "Remote sync/upstream-v2.0.0 HEAD matches local v8.0/config-bucket HEAD"

must_haves:
    truths:
        - "29-DONE-GATE.md exists with one section per SYNC requirement (9 total): SYNC-01, SYNC-21, SYNC-28, SYNC-29, SYNC-30, SYNC-31, SYNC-32, SYNC-33, SYNC-34. Each section quotes the relevant evidence verbatim from 29-VERIFY-RESULTS.md / 29-LINT-BASELINE.md / 29-SMOKE-EVIDENCE.md, marks PASS, cites the originating plan number."
        - "Done-gate template matches Phase 28's `28-DONE-GATE.md` shape — section per check, evidence quoted, requirements-satisfied table at bottom, timestamped status line"
        - "STATE.md frontmatter updated: completed_phases 5→6, completed_plans 48→59 (Phase 29 produced 11 plans: 29-00 through 29-10), stopped_at='Phase 29 complete; ready for Phase 30 land + tag', last_updated bumped"
        - "ROADMAP.md Phase 29 entry: checkbox `[ ]` → `[x]`, append `(complete YYYY-MM-DD)` after the title, all 5 ROADMAP success criteria flipped to checked"
        - "Two commits, in order: `docs(29-10): phase 29 done-gate evidence` (paired with 29-DONE-GATE.md) then `docs(29): mark phase 29 complete in roadmap + state` (paired with STATE.md + ROADMAP.md)"
        - "Both commits SSH-signed; both pushed to fork remote sync/upstream-v2.0.0; force-push NOT applicable — Phase 29 commits are pure additions on top of Phase 28's HEAD da282aaf4"
        - "Phase 29 final summary 29-10-SUMMARY.md created matching the 28-11-SUMMARY.md shape"
    artifacts:
        - path: ".planning/phases/29-build-verification/29-DONE-GATE.md"
          provides: "Done-gate evidence record — captures PASS evidence for all 9 SYNC requirements + final phase-status timestamped line, for milestone audit trail"
          contains: "## Phase 29 Done Gate"
        - path: ".planning/STATE.md"
          provides: "Phase 29 marked complete in frontmatter"
          contains: "stopped_at: Phase 29 complete"
        - path: ".planning/ROADMAP.md"
          provides: "Phase 29 row checkbox flipped to [x] with completion date"
          contains: "[x] Phase 29"
    key_links:
        - from: "29-DONE-GATE.md"
          to: "ROADMAP.md Phase 29 row"
          via: "Evidence that all 5 ROADMAP success criteria + all 9 SYNC requirements satisfied"
          pattern: "Phase 29"
        - from: "v8.0/config-bucket post-29 HEAD"
          to: "Phase 30 plan inputs"
          via: "Phase 30 (land + tag) takes this branch state as input — done-gate is the handoff"
          pattern: "fork/sync/upstream-v2.0.0"
---

<objective>
Run the Phase 29 done-gate: roll up all 9 SYNC requirement PASS verdicts into one evidence file, update STATE.md + ROADMAP.md, push both commits. Single point where the phase declares done. Same shape as Phase 28's `28-DONE-GATE.md`, extended to 9 SYNC items.

Purpose: Catches any drift between 29-09 and now (uncommitted edits, missing evidence sections). Writes the audit-trail record Phase 30 reads to confirm 29 actually closed before tagging the milestone.

This is a checkpoint:human-verify plan because the final phase-complete declaration + ROADMAP/STATE writes are best gated on a human eyeball over the rolled-up evidence.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-build-verification/29-CONTEXT.md
@.planning/phases/29-build-verification/29-VERIFY-RESULTS.md
@.planning/phases/29-build-verification/29-LINT-BASELINE.md
@.planning/phases/29-build-verification/29-SMOKE-EVIDENCE.md
@.planning/phases/28-renderer-main-spine/28-DONE-GATE.md
@CLAUDE.md
</context>

## Goal

`29-DONE-GATE.md` rolls up all 9 SYNC requirements with PASS evidence; STATE.md + ROADMAP.md flipped; two signed docs commits pushed.

## Steps

1. **Confirm starting state.**

    ```
    git rev-parse --abbrev-ref HEAD       # expect: v8.0/config-bucket
    git status --short                    # expect: empty
    ```

    Sanity-check that all upstream evidence files have their PASS sections populated:

    ```
    grep -E '## SYNC-(01|28|29|30|31|32)' .planning/phases/29-build-verification/29-VERIFY-RESULTS.md
    grep -E '## SYNC-21' .planning/phases/29-build-verification/29-VERIFY-RESULTS.md
    grep -E '## SYNC-32' .planning/phases/29-build-verification/29-LINT-BASELINE.md
    grep -E '## SYNC-33' .planning/phases/29-build-verification/29-SMOKE-EVIDENCE.md
    grep -E '## SYNC-34' .planning/phases/29-build-verification/29-SMOKE-EVIDENCE.md
    grep -E 'PASS' .planning/phases/29-build-verification/29-VERIFY-RESULTS.md | wc -l    # ≥ 7
    grep -E 'PASS' .planning/phases/29-build-verification/29-SMOKE-EVIDENCE.md | wc -l    # ≥ 4
    ```

    If any expected section is missing or marked FAIL, STOP and surface — done-gate cannot close.

2. **Confirm Phase 29 commit shape.**

    ```
    git log --oneline v8.0/config-bucket --not da282aaf4
    ```

    Expected pattern (in order, plus possibly one `fix(29-04): regenerate lockfile post-merge` if R1 contingency hit):
    - `docs(29-00): phase 29 setup — lint:ci master baseline captured`
    - `docs(29-01): SYNC-01 + SYNC-28 — repo-wide zero conflicts + pnpm typecheck`
    - `docs(29-02): SYNC-29 — pnpm build`
    - `docs(29-03): SYNC-30 + SYNC-21 — pnpm build:extensions + bundledPlugins count`
    - `docs(29-04): SYNC-31 — pnpm test (Vitest)`
    - `docs(29-05): SYNC-32 — pnpm lint:ci diff vs master`
    - `docs(29-06): SYNC-33 part A — pnpm run start boots from source`
    - `docs(29-07): SYNC-33 part B — RC tag triggers AppImage+deb CI build`
    - `docs(29-08): SYNC-33 part C — AppImage + .deb local boot evidence`
    - `docs(29-09): SYNC-34 — Skyrim SE 5-min Steam smoke`

3. **Write `29-DONE-GATE.md`** matching `28-DONE-GATE.md` shape verbatim (Phase 28 template). Sections:
    - `# Phase 29 Done Gate` (header: branch HEAD SHA, timestamp, environment)
    - `## SYNC-01 — repo-wide zero conflict markers` (originating plan: 29-01) — quote evidence verbatim from 29-VERIFY-RESULTS.md, mark PASS.
    - `## SYNC-21 — bundledPlugins count` (29-03) — quote count + sample, PASS.
    - `## SYNC-28 — pnpm typecheck` (29-01) — quote summary line + exit code, PASS.
    - `## SYNC-29 — pnpm build` (29-02) — quote per-workspace markers, PASS.
    - `## SYNC-30 — pnpm build:extensions` (29-03) — quote exit code + log tail, PASS.
    - `## SYNC-31 — pnpm test (Vitest)` (29-04) — quote final summary line, note any R1 contingency, PASS.
    - `## SYNC-32 — pnpm lint:ci diff vs master` (29-05) — quote delta table + verdict, PASS.
    - `## SYNC-33 — AppImage from release-linux.yml + pnpm run start` (29-06/07/08) — three subsections (part A from-source, part B CI build, part C local boot) with screenshot references + SHA256s, PASS.
    - `## SYNC-34 — Skyrim SE 5-min Steam smoke` (29-09) — walkthrough mapping + screenshot references, PASS.
    - `## Requirements satisfied` — table mapping all 9 SYNC IDs to their evidence section + PASS.
    - `## Phase 29 status: COMPLETE` (timestamped per system clock, ISO-8601).

4. **Stage + commit done-gate.**

    ```
    git add -f .planning/phases/29-build-verification/29-DONE-GATE.md
    git commit -m 'docs(29-10): phase 29 done-gate evidence'
    git log -1 --show-signature
    git push git@github.com:atabisz/Vortex.git v8.0/config-bucket:sync/upstream-v2.0.0
    ```

5. **Update `STATE.md` frontmatter.** Increment counters and set status:
    - `completed_phases: 6` (was 5)
    - `completed_plans: 59` (was 48; Phase 29 produced 11 plans: 29-00..29-10)
    - `stopped_at: Phase 29 complete; ready for Phase 30 land + tag`
    - `last_updated: <today ISO timestamp>`
    - `last_activity: <today YYYY-MM-DD>`
    - Recompute `percent` if it's auto-derived; otherwise leave alone.
      Update the Current Position narrative block to reflect Phase 29 close.

6. **Update `ROADMAP.md` Phase 29 entry.**
    - Flip the row checkbox from `[ ]` to `[x]` and append `(complete <today YYYY-MM-DD>)` after `### Phase 29: Build verification`.
    - Flip each of the 5 ROADMAP success-criteria checkboxes (if they're individually-marked) to checked.
    - Update the `Plans:` line if it's a `TBD` placeholder — write out the 11 plans with their objectives.

7. **Stage + commit STATE + ROADMAP.**

    ```
    git add -f .planning/STATE.md .planning/ROADMAP.md
    git commit -m 'docs(29): mark phase 29 complete in roadmap + state'
    git log -1 --show-signature
    git push git@github.com:atabisz/Vortex.git v8.0/config-bucket:sync/upstream-v2.0.0
    ```

8. **Final remote-sync sanity check.**
    ```
    LOCAL_HEAD=$(git rev-parse HEAD)
    REMOTE_HEAD=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/sync/upstream-v2.0.0 | awk '{print $1}')
    test "$LOCAL_HEAD" = "$REMOTE_HEAD"
    ```

## Verification

- `29-DONE-GATE.md` exists with all 9 SYNC sections + requirements table + COMPLETE status line
- `git log --oneline v8.0/config-bucket --not da282aaf4 | head -15` shows the expected Phase 29 commit sequence ending with the two done-gate commits
- `STATE.md` frontmatter reflects 6 completed_phases, 59 completed_plans, Phase 30 next
- `ROADMAP.md` Phase 29 row marked `[x]` with completion date
- Both done-gate commits SSH-signed (`git log -2 --show-signature` shows two `Good "git" signature` blocks)
- Remote `sync/upstream-v2.0.0` matches local HEAD
- `29-10-SUMMARY.md` written matching `28-11-SUMMARY.md` shape

## Commit

Two commits, in order:

**Commit A** — `docs(29-10): phase 29 done-gate evidence`
Body:

```
Roll up all 9 SYNC requirement PASS verdicts into 29-DONE-GATE.md per Phase 28
done-gate template (extended from 8 items to 9 SYNC items).

SYNC-01, SYNC-21, SYNC-28, SYNC-29, SYNC-30, SYNC-31, SYNC-32, SYNC-33, SYNC-34
all PASS. Evidence quoted from 29-VERIFY-RESULTS.md, 29-LINT-BASELINE.md,
29-SMOKE-EVIDENCE.md.
```

**Commit B** — `docs(29): mark phase 29 complete in roadmap + state`
Body:

```
Phase 29 (build verification) complete. Branch v8.0/config-bucket pushed to
fork remote sync/upstream-v2.0.0. Ready for Phase 30 (land + tag).

ROADMAP.md Phase 29 row → [x] (complete <date>).
STATE.md → completed_phases 6, stopped_at='Phase 29 complete; ready for Phase 30'.
```

Both signed via SSH per CLAUDE.md memory. Both pushed (no force-push — pure additions over Phase 28 HEAD da282aaf4).

## Deviations

If 29-DONE-GATE.md cannot be closed because some upstream evidence section is missing/FAIL, STOP — that means the upstream plan didn't actually finish. The fix is to re-run that plan, not patch the done-gate around the gap.

If STATE.md or ROADMAP.md schema has changed since Phase 28 (frontmatter shape, success-criteria-checkbox idiom), match the current schema rather than blindly copying Phase 28's exact format — surface deltas to user before commit.

<output>
After completion, create `.planning/phases/29-build-verification/29-10-SUMMARY.md` matching `.planning/phases/28-renderer-main-spine/28-11-SUMMARY.md` shape — one section per SYNC requirement with the same evidence pointers, plus the standard "What worked / What was inefficient / Patterns" trailing blocks.
</output>
