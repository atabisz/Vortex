---
phase: 29-build-verification
plan: 02
subsystem: build
type: execute
wave: 3
depends_on: [29-01]
files_modified:
    - .planning/phases/29-build-verification/29-VERIFY-RESULTS.md
autonomous: true
requirements: [SYNC-29]
tags: [linux-port, upstream-v2.0.0, phase-29, sync-29, build]

requirements_in_play: [SYNC-29]

dependency_graph:
    requires:
        - "29-01 complete (SYNC-01 + SYNC-28 PASS recorded in 29-VERIFY-RESULTS.md)"
    provides:
        - "29-VERIFY-RESULTS.md (SYNC-29 section appended)"
        - "Built artefacts in src/main/build/, src/renderer/dist/, src/preload/dist/, src/shared/dist/ (or equivalent webpack/tsc outputs) — consumed by 29-06 pnpm run start boot test"
    affects:
        - "29-03..29-05 cascade"
        - "29-06 (boot from source — needs the build outputs)"
        - "29-10 (rolls up SYNC-29 evidence)"

gates:
    pre_conditions:
        - "29-01 PASS (SYNC-01 + SYNC-28)"
        - "Working tree clean: git status --short empty"
    post_conditions:
        - "pnpm build exits 0; per-workspace successes captured"
        - "29-VERIFY-RESULTS.md SYNC-29 section appended PASS"
        - "HEAD commit title is `docs(29-02): SYNC-29 — pnpm build`"
        - "HEAD commit SSH-signed; pushed to fork remote"

commands:
    - "git status --short"
    - "pnpm build 2>&1 | tee /tmp/build.log"
    - "ls -la src/main/build/ src/renderer/dist/ 2>/dev/null || true"
    - "git add -f .planning/phases/29-build-verification/29-VERIFY-RESULTS.md"
    - "git commit -m 'docs(29-02): SYNC-29 — pnpm build'"
    - "git log -1 --show-signature"
    - "git push git@github.com:atabisz/Vortex.git v8.0/config-bucket:sync/upstream-v2.0.0"

verification:
    - "pnpm build exits 0; positive per-workspace signals captured (main, renderer, preload, shared)"
    - "Build outputs exist on disk (at least src/main/build and the renderer bundle)"
    - "29-VERIFY-RESULTS.md SYNC-29 section reads PASS with exit code + tail of stdout"

must_haves:
    truths:
        - "`pnpm build` exits 0 — main + renderer + preload + shared chain all green per SYNC-29"
        - "Per-workspace build success signals captured (e.g. nx summary or individual compile-success lines for each workspace)"
        - "On non-zero exit, STOP and surface — D-29-01 cascade halts (29-03 et seq do not run)"
        - "Single docs commit `docs(29-02): SYNC-29 — pnpm build` lands on v8.0/config-bucket and is pushed"
    artifacts:
        - path: ".planning/phases/29-build-verification/29-VERIFY-RESULTS.md"
          provides: "Aggregate verification results — SYNC-29 section appended (PASS/FAIL + exit code + log tail)"
          contains: "## SYNC-29"
    key_links:
        - from: "29-VERIFY-RESULTS.md SYNC-29 section"
          to: "29-10-DONE-GATE.md"
          via: "Done-gate quotes SYNC-29 evidence verbatim"
          pattern: "SYNC-29"
---

<objective>
Run `pnpm build` end-to-end (main + renderer + preload + shared chain). Capture exit code and per-workspace success signals into `29-VERIFY-RESULTS.md` SYNC-29 section. Halt the cascade on non-zero exit.

Purpose: SYNC-29 — the merged tree must compile and bundle. Phase 28 only ran typecheck per-bucket; SYNC-29 is the first time the full webpack/electron-builder build chain runs on the resolved tree.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-build-verification/29-CONTEXT.md
@.planning/phases/29-build-verification/29-VERIFY-RESULTS.md
@CLAUDE.md
</context>

## Goal

`pnpm build` exit 0; SYNC-29 section in `29-VERIFY-RESULTS.md` marked PASS with evidence; one signed docs commit pushed.

## Steps

1. **Confirm starting state.**

    ```
    git rev-parse --abbrev-ref HEAD       # expect: v8.0/config-bucket
    git status --short                    # expect: empty
    ```

2. **Run `pnpm build`** (the root `build` script — NOT `build:all` or `build:extensions`; per CONTEXT D-29-01 each script gets its own plan).

    ```
    time pnpm build 2>&1 | tee /tmp/build.log
    echo "exit=${PIPESTATUS[0]}"
    ```

    Capture: exit code, wall-clock time, tail of stdout (final nx summary or equivalent), per-workspace success markers (look for "Successfully ran target build" lines, or webpack "compiled successfully" markers, or tsc clean exits per bucket).

3. **Inspect build outputs** as a sanity check (these aren't formal SYNC-29 acceptance, just a "yes, things actually emitted" check for evidence).

    ```
    ls -la src/main/build/ 2>/dev/null | head
    ls -la src/renderer/dist/ 2>/dev/null | head
    ls -la src/preload/dist/ 2>/dev/null | head
    ls -la src/shared/dist/ 2>/dev/null | head
    ```

    Note any directories that didn't materialise (some workspaces may not have a dist/ pattern; look at `package.json` build scripts to confirm what's expected).

4. **Append SYNC-29 section to `29-VERIFY-RESULTS.md`** replacing the placeholder. Include:
    - Command run, exit code, wall-clock time
    - Per-workspace success markers (quoted from log)
    - Last 30 lines of stdout in a fenced block
    - PASS/FAIL verdict
    - Note any output-dir absences as informational only (not failures unless build itself failed)

5. **Commit and push.**
    ```
    git add -f .planning/phases/29-build-verification/29-VERIFY-RESULTS.md
    git commit -m 'docs(29-02): SYNC-29 — pnpm build'
    git log -1 --show-signature
    git push git@github.com:atabisz/Vortex.git v8.0/config-bucket:sync/upstream-v2.0.0
    ```

## Verification

- `pnpm build` exited 0
- 29-VERIFY-RESULTS.md SYNC-29 section: PASS with exit code, time, tail, per-workspace markers
- HEAD commit title `docs(29-02): SYNC-29 — pnpm build` exact match
- `git log -1 --show-signature` shows valid SSH signature
- Remote `sync/upstream-v2.0.0` matches local HEAD

## Commit

Title: `docs(29-02): SYNC-29 — pnpm build`

Body:

```
pnpm build → exit 0 across main + renderer + preload + shared.
Per D-29-01 sequential cascade; evidence in 29-VERIFY-RESULTS.md SYNC-29.
```

Signing: SSH per CLAUDE.md memory.

## Deviations

None expected. If build fails for a real bug, halt the cascade — do not patch in this plan unless the fix is one-line and obvious (e.g. lockfile drift, in which case the fix is its own narrowly-scoped `fix(29-02): ...` commit before the docs commit).

<output>
After completion, create `.planning/phases/29-build-verification/29-02-SUMMARY.md`.
</output>
