---
phase: 27-gamebryo-per-game-extensions
plan: 01
type: execute
wave: 2
depends_on: [27-00]
files_modified:
    - extensions/gamebryo-savegame-management/src/actions/session.ts
    - extensions/gamebryo-savegame-management/src/index.ts
autonomous: true
requirements: [SYNC-05]

must_haves:
    truths:
        - "Both gamebryo-savegame-management conflict files resolved in leaf-first order: actions/session.ts first, then index.ts"
        - "Two atomic commits on v8.0/config-bucket each titled `resolve(savegame-mgmt): <file> — <stance>` per D-27-00"
        - "After the second commit, `pnpm typecheck -F gamebryo-savegame-management` exits zero (D-27-04 per-extension cadence)"
        - "After EACH commit, `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero — the new §1/§3/§10/BG3/Morrowind gates plus the Phase 26 §6/§7a–d/140a57217 gates all stay green"
    artifacts:
        - path: "extensions/gamebryo-savegame-management/src/actions/session.ts"
          provides: "Save-game session redux actions — leaf 1/2 in D-27-01 sub-order"
          contains: "session"
        - path: "extensions/gamebryo-savegame-management/src/index.ts"
          provides: "Extension barrel — leaf 2/2 (re-exports settle last)"
          contains: "init"
    key_links:
        - from: "actions/session.ts"
          to: "index.ts"
          via: "Leaf-first ordering — action shapes settle before barrel re-exports them"
          pattern: "from './actions/session'"
---

<objective>
Resolve the 2 conflict files in `extensions/gamebryo-savegame-management/src/` per D-27-01 leaf-first sub-order: `actions/session.ts` first, then `index.ts`. Two atomic commits on `v8.0/config-bucket` matching D-27-00 title format `resolve(savegame-mgmt): <file> — <stance>`. Per-extension typecheck after the second commit per D-27-04. Run grep-checkpoint script with `--skip-conflict-check` after each commit.

Purpose: First extension in execution order per D-27-01 (no extension-internal deps on others in scope). Settles first so any merge-driver artefact patterns surface here before touching heavier extensions.

Output: Two commits on `v8.0/config-bucket`. After plan 27-01 lands, only 23 of the 25 Phase 27 conflict files remain.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md
@.planning/phases/26-mod-management-hot-zone/26-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve actions/session.ts (leaf 1/2) and commit</name>
  <read_first>
    - extensions/gamebryo-savegame-management/src/actions/session.ts (current state — read in full so executor sees every conflict region, not assumptions)
    - `git show f15bbabb8:extensions/gamebryo-savegame-management/src/actions/session.ts` (base-commit reference for fork-side action shapes)
    - .planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md (D-27-00 commit format; D-27-01 leaf-first; "Claude's Discretion" — per-region stance defaults)
    - .planning/phases/24-config-bucket/24-CONTEXT.md (D-05 hand-resolve default; fork-side wins for Linux fixes; upstream wins for new feature scaffolding that doesn't touch playbook items)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md — confirm `gamebryo-savegame-management` is NOT named in §1, §3, §6, §7a–d, §10, BG3, or Morrowind gates (it is not — but read so executor verifies, not assumes)
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket` (`git rev-parse --abbrev-ref HEAD`). Open `extensions/gamebryo-savegame-management/src/actions/session.ts` and hand-resolve every `<<<<<<<`/`=======`/`>>>>>>>` region. Default stance per CONTEXT D-27 Claude's Discretion: hand-resolve, fork-side wins for Linux fixes, upstream wins for new feature scaffolding that doesn't touch playbook items. This file is pure redux-act action creators — no playbook items live here, so per-region calls are routine.

    For each region: run `git log --oneline -5 extensions/gamebryo-savegame-management/src/actions/session.ts` to see recent history; if unsure, `git diff HEAD <upstream-merge-parent> -- <file>`.

    After all markers are resolved, run `git diff extensions/gamebryo-savegame-management/src/actions/session.ts` and confirm no remaining conflict markers (`grep -c '^<<<<<<< ' extensions/gamebryo-savegame-management/src/actions/session.ts` must equal 0). Stage ONLY this file. Commit with title `resolve(savegame-mgmt): actions/session.ts — <stance>` where `<stance>` is a one-line summary of per-region decisions. Body: list each conflict region resolved and which side won.

    Run `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check`. ALL eleven non-conflict gates must still pass (this file does not touch any of them — passing here proves nothing broke adjacent).

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/gamebryo-savegame-management/src/actions/session.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(savegame-mgmt\): actions/session\.ts —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/gamebryo-savegame-management/src/actions/session.ts` returns empty (exit code 1)
    - HEAD commit title starts with `resolve(savegame-mgmt): actions/session.ts —`
    - HEAD commit touches exactly one file: `extensions/gamebryo-savegame-management/src/actions/session.ts`
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
  </acceptance_criteria>
  <done>
    First savegame-mgmt leaf resolved; one atomic commit lands; checkpoint stays green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolve index.ts (leaf 2/2), commit, and run per-extension typecheck</name>
  <read_first>
    - extensions/gamebryo-savegame-management/src/index.ts (current state — every conflict region, in full)
    - `git show f15bbabb8:extensions/gamebryo-savegame-management/src/index.ts` (base-commit reference)
    - extensions/gamebryo-savegame-management/src/actions/session.ts (just-committed final shape — barrel re-exports must align)
    - .planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md (D-27-04 per-extension typecheck cadence; "Integration Points" — extension barrels re-export internals; same idiom as Phase 26 D-26-01 for `index.ts` last)
    - .planning/phases/26-mod-management-hot-zone/26-CONTEXT.md (D-26-01 — `index.ts` last in sub-order rationale; same applies here)
  </read_first>
  <action>
    Open `extensions/gamebryo-savegame-management/src/index.ts` and hand-resolve every conflict region. Per D-27-01 this is the barrel — re-exports and `init()` registration. Stance default: hand-resolve, fork-side wins for any Linux-port import targeting paths/`@vortex/paths`, upstream wins for new-feature scaffolding registrations that don't touch playbook items.

    Pay attention to the import block — if Phase 24/25 scaffolding (e.g. `@vortex/paths`) was added to this file, fork-side wins. If upstream added new redux-thunk action registrations or new `context.register*` calls, upstream wins unless they conflict with a fork addition.

    After all markers resolved, run `git diff extensions/gamebryo-savegame-management/src/index.ts`. Confirm zero conflict markers. Stage ONLY this file. Commit with title `resolve(savegame-mgmt): index.ts — <stance>`. Body: list resolved regions + which side won.

    Run `pnpm typecheck -F gamebryo-savegame-management` per D-27-04. Filter is the bare package name (NOT `@vortex/<ext>` — extensions in this repo do not carry the `@vortex/` scope; confirm via `grep '"name"' extensions/gamebryo-savegame-management/package.json` — name is `gamebryo-savegame-management`). Exit zero required.

    If typecheck fails: amend the index.ts commit with the fix (single commit per D-27-00 — use `git commit --amend --no-edit` after staging the fix) before running the checkpoint script.

    Run `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check`. All eleven non-conflict gates must pass.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/gamebryo-savegame-management/src/index.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(savegame-mgmt\): index\.ts —' &amp;&amp; pnpm typecheck -F gamebryo-savegame-management &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/gamebryo-savegame-management/src/index.ts` returns empty
    - `git grep -l '^<<<<<<< ' extensions/gamebryo-savegame-management/` returns empty (entire extension clean)
    - HEAD commit title starts with `resolve(savegame-mgmt): index.ts —`
    - HEAD commit touches exactly one file
    - `pnpm typecheck -F gamebryo-savegame-management` exits zero
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
    - `git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\(savegame-mgmt\):'` returns 2
  </acceptance_criteria>
  <done>
    Both savegame-mgmt files resolved; per-extension typecheck clean; checkpoint stays green; 2/25 phase files done.
  </done>
</task>

</tasks>

<verification>
- Both files conflict-clean
- Two atomic commits matching `resolve(savegame-mgmt): <file> — <stance>`
- `pnpm typecheck -F gamebryo-savegame-management` passes
- `scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
</verification>

<success_criteria>

- 2 of 25 Phase 27 files resolved
- gamebryo-savegame-management extension typechecks clean
- Per-extension typecheck cadence (D-27-04) honoured

</success_criteria>

<output>
After completion, create `.planning/phases/27-gamebryo-per-game-extensions/27-01-SUMMARY.md`
</output>
