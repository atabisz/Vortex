---
phase: 27-gamebryo-per-game-extensions
plan: 03
type: execute
wave: 4
depends_on: [27-02]
files_modified:
    - extensions/modtype-bepinex/src/bepInExDownloader.ts
    - extensions/modtype-bepinex/src/common.ts
    - extensions/modtype-bepinex/src/index.ts
autonomous: true
requirements: [SYNC-06]

must_haves:
    truths:
        - "All 3 modtype-bepinex conflict files resolved in leaf-first order: bepInExDownloader.ts, common.ts, index.ts"
        - "Three atomic commits on v8.0/config-bucket each titled `resolve(bepinex): <file> — <stance>` per D-27-00"
        - "After the third commit, `pnpm typecheck -F modtype-bepinex` exits zero (D-27-04)"
        - "After EACH commit, `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero"
    artifacts:
        - path: "extensions/modtype-bepinex/src/bepInExDownloader.ts"
          provides: "BepInEx fetcher — leaf 1/3"
          contains: "download"
        - path: "extensions/modtype-bepinex/src/common.ts"
          provides: "BepInEx common helpers — leaf 2/3"
          contains: "common"
        - path: "extensions/modtype-bepinex/src/index.ts"
          provides: "Extension barrel — last"
          contains: "init"
    key_links:
        - from: "common.ts + bepInExDownloader.ts"
          to: "index.ts"
          via: "Barrel re-exports settle last"
          pattern: "from './common'"
---

<objective>
Resolve the 3 conflict files in `extensions/modtype-bepinex/src/` per D-27-01 leaf-first sub-order: `bepInExDownloader.ts` → `common.ts` → `index.ts`. Three atomic commits matching `resolve(bepinex): <file> — <stance>`. Per-extension typecheck after the third commit.

Purpose: Independent extension per D-27-01 (no extension-internal deps on other in-scope extensions). Settles before collections (which doesn't depend on bepinex but goes after by extension-order rule).

Output: Three commits on `v8.0/config-bucket`. After plan 27-03 lands, 16 of 25 phase files remain.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve bepInExDownloader.ts (leaf 1/3) and commit</name>
  <read_first>
    - extensions/modtype-bepinex/src/bepInExDownloader.ts (current state, in full)
    - `git show f15bbabb8:extensions/modtype-bepinex/src/bepInExDownloader.ts` (base-commit reference)
    - .planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md (D-27-00, D-27-01, Discretion)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md — confirm modtype-bepinex is NOT named in any §-numbered gate (it isn't)
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Open `extensions/modtype-bepinex/src/bepInExDownloader.ts` and hand-resolve every conflict region. Stance per CONTEXT D-27 Discretion: fork-side wins for Linux-port imports, upstream wins for new feature scaffolding not touching playbook items.

    Watch for OS-conditional download URLs or unzip paths. If a region toggles between Windows and platform-agnostic logic, fork-side wins (BepInEx itself runs cross-platform via Mono — Linux Vortex must be able to fetch and unpack it).

    After resolution: zero conflict markers. Stage only this file. Commit `resolve(bepinex): bepInExDownloader.ts — <stance>`.

    Run grep-checkpoint with `--skip-conflict-check`. All gates pass.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/modtype-bepinex/src/bepInExDownloader.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(bepinex\): bepInExDownloader\.ts —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/modtype-bepinex/src/bepInExDownloader.ts` returns empty
    - HEAD commit title starts with `resolve(bepinex): bepInExDownloader.ts —`
    - HEAD commit touches exactly one file
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
  </acceptance_criteria>
  <done>
    Leaf 1/3 resolved; checkpoint clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolve common.ts (leaf 2/3) and commit</name>
  <read_first>
    - extensions/modtype-bepinex/src/common.ts (current state, in full)
    - `git show f15bbabb8:extensions/modtype-bepinex/src/common.ts` (base-commit reference)
    - extensions/modtype-bepinex/src/bepInExDownloader.ts (just-committed shape — common.ts may share types)
  </read_first>
  <action>
    Open `extensions/modtype-bepinex/src/common.ts` and hand-resolve every conflict region. Stance defaults apply.

    After resolution: zero conflict markers. Stage only this file. Commit `resolve(bepinex): common.ts — <stance>`.

    Run grep-checkpoint with `--skip-conflict-check`. All gates pass.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/modtype-bepinex/src/common.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(bepinex\): common\.ts —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/modtype-bepinex/src/common.ts` returns empty
    - HEAD commit title starts with `resolve(bepinex): common.ts —`
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
  </acceptance_criteria>
  <done>
    Leaf 2/3 resolved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Resolve index.ts (leaf 3/3), commit, run per-extension typecheck</name>
  <read_first>
    - extensions/modtype-bepinex/src/index.ts (current state, in full)
    - `git show f15bbabb8:extensions/modtype-bepinex/src/index.ts` (base-commit reference)
    - extensions/modtype-bepinex/src/bepInExDownloader.ts and common.ts (just-committed final shapes)
    - .planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md (D-27-04 typecheck cadence)
  </read_first>
  <action>
    Open `extensions/modtype-bepinex/src/index.ts` and hand-resolve every conflict region. Same idiom — barrel last per Phase 26 D-26-01.

    After resolution: zero conflict markers. Stage only this file. Commit `resolve(bepinex): index.ts — <stance>`.

    Run `pnpm typecheck -F modtype-bepinex` per D-27-04 (bare name; this extension has a typecheck script). If failure, amend the index.ts commit before checkpoint.

    Run grep-checkpoint with `--skip-conflict-check`. All gates pass.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/modtype-bepinex/; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(bepinex\): index\.ts —' &amp;&amp; pnpm typecheck -F modtype-bepinex &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/modtype-bepinex/src/index.ts` returns empty
    - `git grep -l '^<<<<<<< ' extensions/modtype-bepinex/` returns empty (entire extension clean)
    - HEAD commit title starts with `resolve(bepinex): index.ts —`
    - `pnpm typecheck -F modtype-bepinex` exits zero
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
    - `git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\(bepinex\):'` returns 3
  </acceptance_criteria>
  <done>
    All 3 bepinex files resolved; per-extension typecheck clean; 9/25 phase files done.
  </done>
</task>

</tasks>

<verification>
- All 3 files conflict-clean
- Three atomic commits matching `resolve(bepinex): <file> — <stance>`
- `pnpm typecheck -F modtype-bepinex` passes
- `scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
</verification>

<success_criteria>

- 9 of 25 Phase 27 files resolved (cumulative)
- modtype-bepinex extension typechecks clean

</success_criteria>

<output>
After completion, create `.planning/phases/27-gamebryo-per-game-extensions/27-03-SUMMARY.md`
</output>
