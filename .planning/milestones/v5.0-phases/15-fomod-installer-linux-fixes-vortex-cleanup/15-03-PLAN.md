---
phase: 15-fomod-installer-linux-fixes-vortex-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/vortex-api/lib/api.d.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [FOMD-15-06, FOMD-15-07]

must_haves:
  truths:
    - "vortex-api declarations include resolvePathCase export"
    - "REQUIREMENTS.md contains all FOMD-15-xx entries"
  artifacts:
    - path: "packages/vortex-api/lib/api.d.ts"
      provides: "Regenerated API declarations with resolvePathCase"
      contains: "resolvePathCase"
    - path: ".planning/REQUIREMENTS.md"
      provides: "FOMD requirement definitions"
      contains: "FOMD-15-01"
  key_links:
    - from: "packages/vortex-api/lib/api.d.ts"
      to: "src/renderer/src/util/"
      via: "API Extractor declaration generation"
      pattern: "resolvePathCase"
---

<objective>
Regenerate vortex-api type declarations to include resolvePathCase (a utility added
during v4.0 that's missing from the generated .d.ts), and create the REQUIREMENTS.md
entries for FOMD-15-xx requirement IDs.

Purpose: The stale api.d.ts means extensions consuming vortex-api can't see
resolvePathCase in their type definitions. The REQUIREMENTS.md update is a planning
artifact to close the requirement tracking loop.

Output: Regenerated api.d.ts and new REQUIREMENTS.md file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

Branch strategy: Both changes are master-only.
- api.d.ts regeneration is a build artifact, not upstream-eligible
- REQUIREMENTS.md is a planning artifact

Do NOT commit to linux-port branch.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate vortex-api declarations</name>
  <files>packages/vortex-api/lib/api.d.ts</files>
  <read_first>
    - packages/vortex-api/package.json (build scripts)
    - packages/vortex-api/tsconfig.json (compilation config)
  </read_first>
  <action>
Run the vortex-api build to regenerate api.d.ts via API Extractor.

```bash
cd /home/alex/src/Vortex
pnpm --filter vortex-api run build
```

If the build script is not named `build`, check package.json for the correct script name
(could be `build-api`, `declarations`, `api-extractor`, etc.).

After the build completes, verify that `resolvePathCase` appears in the regenerated
`packages/vortex-api/lib/api.d.ts`. This function was added to the renderer util
during Phase 14 (Linux Case-Folding fs Wrapper) and should be picked up by the
declaration generator.

If `pnpm --filter vortex-api run build` fails, try building the renderer first since
vortex-api extracts types from the renderer build output:
```bash
pnpm --filter @vortex/renderer run build
pnpm --filter vortex-api run build
```

If API Extractor is a separate step, look for scripts like `api-extractor` or
`extract-api` in the package.json.
  </action>
  <verify>
    <automated>grep -c "resolvePathCase" /home/alex/src/Vortex/packages/vortex-api/lib/api.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/vortex-api/lib/api.d.ts contains at least 1 occurrence of `resolvePathCase`
    - The file was regenerated (modification timestamp updated)
    - No TypeScript compilation errors during build
  </acceptance_criteria>
  <done>vortex-api declarations regenerated with resolvePathCase included in the public API surface</done>
</task>

<task type="auto">
  <name>Task 2: Create FOMD requirement entries in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md (phase 15 requirements list)
  </read_first>
  <action>
Create `.planning/REQUIREMENTS.md` with the FOMD-15-xx requirement definitions.
This file does not exist yet — FOMD-15-07 specifically calls for its creation.

Write the file with this content:

```markdown
# Requirements

## Phase 15: fomod-installer Linux Fixes + Vortex Cleanup

| ID | Description | Status |
|----|-------------|--------|
| FOMD-15-01 | CSharpScript runtime OS guard — wrap CSharpScriptType registration with RuntimeInformation.IsOSPlatform(OSPlatform.Windows) in ModFormatManager.cs | Complete (pre-existing) |
| FOMD-15-02 | XmlScriptInstaller source path normalization — apply TextUtil.NormalizePath to matchedFiles[0] for consistent lowercase forward-slash paths | Planned |
| FOMD-15-03 | CI Linux IPC build pipeline — build-packages.yml matrix includes linux-x64 dotnet publish for ModInstallerIPC | Complete (pre-existing) |
| FOMD-15-04 | C# script unsupported-instruction warning — Linux-specific notification in reportUnsupported when source === CSharpScript | Planned |
| FOMD-15-05 | Remove redundant replaceAll workaround — source path replaceAll("\\\\", "/") no longer needed after Parser10 upstream normalization | Planned |
| FOMD-15-06 | Regenerate vortex-api declarations — api.d.ts missing resolvePathCase export | Planned |
| FOMD-15-07 | Add FOMD requirements to REQUIREMENTS.md — planning artifact for requirement tracking | Planned |
```
  </action>
  <verify>
    <automated>grep -c "FOMD-15-0" /home/alex/src/Vortex/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md exists
    - File contains all 7 requirement IDs: FOMD-15-01 through FOMD-15-07
    - Each requirement has an ID, description, and status
  </acceptance_criteria>
  <done>REQUIREMENTS.md created with all FOMD-15-xx requirement definitions for phase tracking</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — this plan only regenerates type declarations and creates a planning document.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | I (Information Disclosure) | api.d.ts | accept | Public type declarations; no secrets |
</threat_model>

<verification>
1. `grep resolvePathCase packages/vortex-api/lib/api.d.ts` returns at least one match
2. `.planning/REQUIREMENTS.md` contains all 7 FOMD-15-xx IDs
</verification>

<success_criteria>
- api.d.ts regenerated with resolvePathCase visible in declarations
- REQUIREMENTS.md created with complete FOMD-15-xx requirement table
- Both committed on master branch only
</success_criteria>

<output>
After completion, create `.planning/phases/15-fomod-installer-linux-fixes-vortex-cleanup/15-03-SUMMARY.md`
</output>
