---
phase: 15-fomod-installer-linux-fixes-vortex-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/alex/src/fomod-installer/src/InstallScripting/XmlScript/XmlScriptInstaller.cs
autonomous: true
requirements: [FOMD-15-01, FOMD-15-02, FOMD-15-03]

must_haves:
  truths:
    - "XmlScriptInstaller emits lowercased forward-slash source paths from matchedFiles"
    - "CSharpScript OS guard prevents registration on non-Windows platforms"
    - "CI build pipeline produces linux-x64 ModInstallerIPC binary"
  artifacts:
    - path: "/home/alex/src/fomod-installer/src/InstallScripting/XmlScript/XmlScriptInstaller.cs"
      provides: "Source path normalization via TextUtil.NormalizePath"
      contains: "TextUtil.NormalizePath(matchedFiles[0]"
  key_links:
    - from: "XmlScriptInstaller.cs"
      to: "TextUtil.NormalizePath"
      via: "direct call on matchedFiles[0]"
      pattern: "TextUtil\\.NormalizePath\\(matchedFiles\\[0\\]"
---

<objective>
Apply source-path normalization in fomod-installer's XmlScriptInstaller so that file-copy
source paths emitted by the XML script engine are lowercased with forward slashes,
consistent with destination paths that already go through Parser10 normalization.

Also verify that the two previously-completed items (CSharpScript OS guard, CI Linux IPC
build) are in place and passing.

Purpose: Eliminates case mismatch between source and destination paths on Linux. While
Vortex's resolvePathCase safety net catches this, the fix belongs upstream in the installer
for clean behavior.

Output: Modified XmlScriptInstaller.cs in the fomod-installer fork.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

NOTE: This plan operates on the fomod-installer repo at /home/alex/src/fomod-installer,
NOT the Vortex repo. All file paths are absolute to that repo.

<interfaces>
From /home/alex/src/fomod-installer/src/Utils/TextUtil.cs:
```csharp
public static string NormalizePath(string path, bool dirTerminate = false, bool alternateSeparators = false, bool toLower = true)
```
Called as `TextUtil.NormalizePath(value, false, true)`:
- dirTerminate=false (no trailing separator)
- alternateSeparators=true (use Path.AltDirectorySeparatorChar = `/`)
- toLower=true (default, lowercases the path)

From /home/alex/src/fomod-installer/src/InstallScripting/XmlScript/XmlScriptInstaller.cs:
```csharp
private static string NormalizeSeparators(string path)  // existing local helper
IList<string> matchedFiles = ModArchive.GetFileList(strSourceXml, true, false);
string strSource = matchedFiles[0];  // <-- line 139, needs normalization
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize source path in XmlScriptInstaller.cs</name>
  <files>/home/alex/src/fomod-installer/src/InstallScripting/XmlScript/XmlScriptInstaller.cs</files>
  <read_first>
    - /home/alex/src/fomod-installer/src/InstallScripting/XmlScript/XmlScriptInstaller.cs
    - /home/alex/src/fomod-installer/src/Utils/TextUtil.cs (NormalizePath signature)
  </read_first>
  <action>
In XmlScriptInstaller.cs, modify line 139 to normalize the source path returned by
GetFileList. The destination path (line 140) already goes through NormalizeSeparators,
but the source path from matchedFiles[0] retains archive-original case.

Change line 139 from:
```csharp
string strSource = matchedFiles[0];
```
to:
```csharp
string strSource = TextUtil.NormalizePath(matchedFiles[0], false, true);
```

This requires adding `using Utils;` at the top of the file if not already present.
Check the existing using statements first — `Utils` may already be imported since
TextUtil is used elsewhere in the codebase.

The call `TextUtil.NormalizePath(value, false, true)` means:
- dirTerminate=false (no trailing slash)
- alternateSeparators=true (forward slashes via Path.AltDirectorySeparatorChar)
- toLower=true (default, lowercases)

This makes the source path consistent with destination paths that already go through
Parser10's NormalizePath at XML parse time.
  </action>
  <verify>
    <automated>cd /home/alex/src/fomod-installer && grep -n "TextUtil.NormalizePath(matchedFiles\[0\]" src/InstallScripting/XmlScript/XmlScriptInstaller.cs</automated>
  </verify>
  <acceptance_criteria>
    - XmlScriptInstaller.cs line ~139 contains `TextUtil.NormalizePath(matchedFiles[0], false, true)`
    - File has `using Utils;` in its using block
    - No other lines in the method are changed
  </acceptance_criteria>
  <done>matchedFiles[0] source path is normalized to lowercase forward-slash via TextUtil.NormalizePath before being passed to InstallFileFromMod</done>
</task>

<task type="auto">
  <name>Task 2: Verify existing FOMD-15-01 and FOMD-15-03 implementations</name>
  <files>
    /home/alex/src/fomod-installer/src/ModInstaller.Adaptor.Dynamic/ModFormatManager.cs (read-only)
    /home/alex/src/fomod-installer/.github/workflows/build-packages.yml (read-only)
    /home/alex/src/fomod-installer/src/ModInstaller.IPC.TypeScript/test/verify-warning.spec.ts (read-only)
  </files>
  <read_first>
    - /home/alex/src/fomod-installer/src/ModInstaller.Adaptor.Dynamic/ModFormatManager.cs
    - /home/alex/src/fomod-installer/.github/workflows/build-packages.yml
    - /home/alex/src/fomod-installer/src/ModInstaller.IPC.TypeScript/test/verify-warning.spec.ts
  </read_first>
  <action>
Verify that the following two items are already in place (no code changes needed):

FOMD-15-01 (CSharpScript OS guard):
- In ModFormatManager.cs, confirm both GetRequirements() and GetScriptType() methods
  wrap `CSharpScriptType` registration inside:
  ```csharp
  #if USE_CSHARP_SCRIPT
      if (RuntimeInformation.IsOSPlatform(OSPlatform.Windows))
      {
          CurrentScriptTypeRegistry.RegisterType(new CSharpScriptType());
      }
  #endif
  ```
- Confirm `using System.Runtime.InteropServices;` is present in the file.
- Confirm verify-warning.spec.ts exists with test for UnsupportedFunctionalityWarning.

FOMD-15-03 (CI Linux IPC build):
- In build-packages.yml, confirm the build-ipc job matrix includes `ubuntu-22.04` / `linux-x64`.
- Confirm the Linux step runs `dotnet publish` targeting `linux-x64` self-contained.
- Confirm the package-ipc job downloads both win32-x64 and linux-x64 artifacts.

Log verification results. No code changes — these are already complete.
  </action>
  <verify>
    <automated>cd /home/alex/src/fomod-installer && grep -c "IsOSPlatform(OSPlatform.Windows)" src/ModInstaller.Adaptor.Dynamic/ModFormatManager.cs && grep -c "linux-x64" .github/workflows/build-packages.yml</automated>
  </verify>
  <acceptance_criteria>
    - ModFormatManager.cs contains at least 2 occurrences of `IsOSPlatform(OSPlatform.Windows)` (one per method)
    - build-packages.yml contains at least 3 occurrences of `linux-x64` (matrix entry + publish + artifact download)
    - verify-warning.spec.ts exists and contains `UnsupportedFunctionalityWarning`
  </acceptance_criteria>
  <done>FOMD-15-01 (OS guard) and FOMD-15-03 (CI pipeline) confirmed in place with no changes needed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Archive file paths -> installer | Untrusted mod archive paths parsed by XML script engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | T (Tampering) | XmlScriptInstaller source path | mitigate | NormalizePath lowercases and forward-slashes source paths; Vortex resolvePathCase is safety net |
| T-15-02 | I (Information Disclosure) | Path casing | accept | Path lowercasing only affects install-time copy operations within user's own mod staging directory |
</threat_model>

<verification>
1. XmlScriptInstaller.cs contains `TextUtil.NormalizePath(matchedFiles[0], false, true)` on the source path assignment
2. ModFormatManager.cs has OS platform guard wrapping CSharpScriptType registration (2 locations)
3. build-packages.yml has linux-x64 in IPC build matrix
</verification>

<success_criteria>
- Source path normalization applied in XmlScriptInstaller.cs
- FOMD-15-01 and FOMD-15-03 verified as already complete
- Changes committed in fomod-installer repo
</success_criteria>

<output>
After completion, create `.planning/phases/15-fomod-installer-linux-fixes-vortex-cleanup/15-01-SUMMARY.md`
</output>
