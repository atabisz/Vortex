---
phase: 09-native-addon-fix-elevation-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - patches/gamebryo-savegame@2.1.2.patch
  - pnpm-workspace.yaml
  - .github/workflows/main.yml
  - src/main/electron-builder.config.cjs
autonomous: true
requirements: [SAVE-01]
must_haves:
  truths:
    - "gamebryo-savegame.node compiles on Linux without linker errors"
    - "pnpm install applies the gamebryo-savegame patch without warnings"
    - "deb package declares lz4 and zlib runtime dependencies"
  artifacts:
    - path: "patches/gamebryo-savegame@2.1.2.patch"
      provides: "C++ MoreInfoException fix + binding.gyp Linux condition"
      contains: "std::runtime_error"
    - path: "pnpm-workspace.yaml"
      provides: "patchedDependencies entry for gamebryo-savegame"
      contains: "gamebryo-savegame@2.1.2"
    - path: ".github/workflows/main.yml"
      provides: "zlib1g-dev in CI apt step"
      contains: "zlib1g-dev"
    - path: "src/main/electron-builder.config.cjs"
      provides: "deb runtime depends for lz4 and zlib"
      contains: "liblz4-1"
  key_links:
    - from: "pnpm-workspace.yaml"
      to: "patches/gamebryo-savegame@2.1.2.patch"
      via: "patchedDependencies key"
      pattern: "gamebryo-savegame@2.1.2: patches/gamebryo-savegame@2.1.2.patch"
    - from: ".github/workflows/main.yml"
      to: "gamebryo-savegame native build"
      via: "apt-get install provides headers"
      pattern: "liblz4-dev zlib1g-dev"
---

<objective>
Create a pnpm patch for `gamebryo-savegame` that fixes the MSVC-only C++ exception constructor and adds Linux linker flags, then update CI and packaging to support the native addon on Linux.

Purpose: SAVE-01 requires `gamebryo-savegame.node` to compile and load on Linux CI. The addon currently fails on GCC due to an MSVC-only `std::exception` constructor and missing `-llz4 -lz` linker flags.
Output: Patch file, updated pnpm-workspace.yaml, updated CI apt step, updated deb depends.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-native-addon-fix-elevation-foundation/09-CONTEXT.md
@.planning/phases/09-native-addon-fix-elevation-foundation/09-RESEARCH.md

<interfaces>
<!-- Existing pnpm patch format reference -->
From pnpm-workspace.yaml (lines 315-317):
```yaml
patchedDependencies:
  playwright-core@1.58.2: patches/playwright-core@1.58.2.patch
  loot@6.2.1: patches/loot@6.2.1.patch
```

From patches/loot@6.2.1.patch (binding.gyp section):
```
["OS=='linux'", {
  "libraries": [
    "-L../loot_api",
    "-llibloot"
  ],
  "ldflags": [
    "-Wl,-rpath,'$$ORIGIN/../../loot_api'"
  ]
}]
```

From electron-builder.config.cjs (line 65-68):
```js
deb: {
  depends: ["xdg-utils", "libasound2"],
  artifactName: "vortex_amd64.deb",
},
```

From .github/workflows/main.yml (line 50):
```yaml
run: sudo apt-get update && sudo apt-get install -y libfontconfig1-dev cmake liblz4-dev
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gamebryo-savegame pnpm patch and register it</name>
  <files>patches/gamebryo-savegame@2.1.2.patch, pnpm-workspace.yaml</files>
  <read_first>
    - patches/loot@6.2.1.patch (patch format reference)
    - pnpm-workspace.yaml (patchedDependencies section at end, catalog entry for gamebryo-savegame)
    - node_modules/.pnpm/gamebryo-savegame@https+++codeload.github.com+Nexus-Mods+node-gamebryo-savegames+tar.gz+f060df62b04aa53e0d2fa7e6de0c9df423bbd44c/node_modules/gamebryo-savegame/src/gamebryosavegame.cpp (current MoreInfoException at line 23-31)
    - node_modules/.pnpm/gamebryo-savegame@https+++codeload.github.com+Nexus-Mods+node-gamebryo-savegames+tar.gz+f060df62b04aa53e0d2fa7e6de0c9df423bbd44c/node_modules/gamebryo-savegame/binding.gyp (current conditions block)
  </read_first>
  <action>
Create the pnpm patch for gamebryo-savegame containing two fixes (per D-04):

**Fix 1: MoreInfoException C++ base class** in `src/gamebryosavegame.cpp`:
- Change `class MoreInfoException : public std::exception {` to `class MoreInfoException : public std::runtime_error {`
- Change the constructor initializer from `std::exception(std::runtime_error(message))` to `std::runtime_error(message)`
- This is the ONLY change in the .cpp file. The rest of the class body is unchanged.

**Fix 2: binding.gyp Linux condition**:
- Add an `OS=="linux"` condition alongside the existing `OS=="win"` condition in the `conditions` array
- The Linux condition contains only: `"libraries": ["-llz4", "-lz"]`
- No RPATH needed (system libraries, not bundled). No cflags changes needed.

**Create the patch using `pnpm patch`:**
1. Run `pnpm patch gamebryo-savegame` to extract the package to a temp directory
2. Apply the two fixes to the extracted files
3. Run `pnpm patch-commit <temp-dir>` to generate `patches/gamebryo-savegame@2.1.2.patch`

If `pnpm patch-commit` does not auto-register the patch, manually add the entry to `pnpm-workspace.yaml` under `patchedDependencies`:
```yaml
patchedDependencies:
  playwright-core@1.58.2: patches/playwright-core@1.58.2.patch
  loot@6.2.1: patches/loot@6.2.1.patch
  gamebryo-savegame@2.1.2: patches/gamebryo-savegame@2.1.2.patch
```

Per D-05, verify the package version is `2.1.2` (check `package.json` in the extracted dir). The catalog entry is a git URL — pnpm uses the package's internal version field for patch matching.

After patch creation, run `pnpm install` to apply it and verify no warnings about the patch.
  </action>
  <verify>
    <automated>grep -c "std::runtime_error" patches/gamebryo-savegame@2.1.2.patch && grep -c "gamebryo-savegame@2.1.2" pnpm-workspace.yaml && pnpm install 2>&1 | grep -i "gamebryo-savegame" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - patches/gamebryo-savegame@2.1.2.patch exists and contains `std::runtime_error(message)` (the fixed base class)
    - patches/gamebryo-savegame@2.1.2.patch contains `OS=='linux'` (the new condition)
    - patches/gamebryo-savegame@2.1.2.patch contains `"-llz4"` and `"-lz"` (linker flags)
    - pnpm-workspace.yaml contains `gamebryo-savegame@2.1.2: patches/gamebryo-savegame@2.1.2.patch`
    - `pnpm install` completes without patch-related warnings
    - The patched source in node_modules contains `std::runtime_error` not `std::exception(std::runtime_error`
  </acceptance_criteria>
  <done>Patch file created, registered in pnpm-workspace.yaml, and applied successfully by pnpm install</done>
</task>

<task type="auto">
  <name>Task 2: Update CI apt step and deb depends for lz4/zlib</name>
  <files>.github/workflows/main.yml, src/main/electron-builder.config.cjs</files>
  <read_first>
    - .github/workflows/main.yml (line 48-50: Linux build dependencies step)
    - src/main/electron-builder.config.cjs (line 65-68: deb.depends array)
  </read_first>
  <action>
**CI apt step** (per D-02) — `.github/workflows/main.yml` line 50:
Change:
```yaml
run: sudo apt-get update && sudo apt-get install -y libfontconfig1-dev cmake liblz4-dev
```
To:
```yaml
run: sudo apt-get update && sudo apt-get install -y libfontconfig1-dev cmake liblz4-dev zlib1g-dev
```
Add `zlib1g-dev` to the end of the existing apt-get install line. No new step. `liblz4-dev` is already present (added in Phase 3).

**deb depends** (per D-03) — `src/main/electron-builder.config.cjs` line 66:
Change:
```js
depends: ["xdg-utils", "libasound2"],
```
To:
```js
depends: ["xdg-utils", "libasound2", "liblz4-1", "zlib1g"],
```
Append `"liblz4-1"` and `"zlib1g"` to the existing array. These are the runtime shared libraries that `gamebryo-savegame.node` links against.
  </action>
  <verify>
    <automated>grep "zlib1g-dev" .github/workflows/main.yml && grep "liblz4-1" src/main/electron-builder.config.cjs && grep "zlib1g" src/main/electron-builder.config.cjs</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/main.yml line 50 contains `zlib1g-dev` alongside `liblz4-dev`
    - .github/workflows/main.yml still has ONE apt-get install line (no new step added)
    - electron-builder.config.cjs deb.depends array contains `"liblz4-1"` and `"zlib1g"`
    - electron-builder.config.cjs deb.depends array still contains `"xdg-utils"` and `"libasound2"` (not replaced)
  </acceptance_criteria>
  <done>CI apt step includes zlib1g-dev for compile-time headers; deb depends includes liblz4-1 and zlib1g for runtime loading</done>
</task>

</tasks>

<verification>
1. `patches/gamebryo-savegame@2.1.2.patch` exists with both C++ and binding.gyp fixes
2. `pnpm install` applies the patch without warnings
3. `grep "std::runtime_error" node_modules/.pnpm/gamebryo-savegame*/node_modules/gamebryo-savegame/src/gamebryosavegame.cpp` shows the fix applied
4. `grep "zlib1g-dev" .github/workflows/main.yml` confirms CI has the header package
5. `grep "liblz4-1" src/main/electron-builder.config.cjs` confirms deb runtime dep
</verification>

<success_criteria>
- gamebryo-savegame pnpm patch created and registered; `pnpm install` applies it cleanly
- CI apt-get step includes both `liblz4-dev` and `zlib1g-dev`
- deb.depends includes both `liblz4-1` and `zlib1g` runtime libraries
- No existing Windows build configuration is altered
</success_criteria>

<output>
After completion, create `.planning/phases/09-native-addon-fix-elevation-foundation/09-01-SUMMARY.md`
</output>
