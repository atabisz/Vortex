---
phase: 27-gamebryo-per-game-extensions
plan: 07
type: execute
wave: 6
depends_on: [27-04]
files_modified:
    - extensions/games/game-witcher3/src/installers.ts
    - extensions/games/game-witcher3/src/index.ts
autonomous: true
requirements: [SYNC-19]

must_haves:
    truths:
        - "Both Witcher 3 conflict files resolved in leaf-first order: installers.ts → index.ts"
        - "Two atomic commits on v8.0/config-bucket each titled `resolve(witcher3): <file> — <stance>` per D-27-00"
        - "After the second commit, Witcher3 typecheck passes — game-witcher3 has no `typecheck` script in package.json, so the executor runs `pnpm exec tsc --noEmit -p extensions/games/game-witcher3` (or build-as-typecheck) — see action"
        - "After EACH commit, `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero"
    artifacts:
        - path: "extensions/games/game-witcher3/src/installers.ts"
          provides: "Witcher 3 mod installer logic — leaf 1/2"
          contains: "installer"
        - path: "extensions/games/game-witcher3/src/index.ts"
          provides: "Extension entry — last (registers Witcher 3 game definition)"
          contains: "init"
    key_links:
        - from: "installers.ts"
          to: "index.ts"
          via: "Index registers installer; installer types settle first"
          pattern: "from './installers'"
---

<objective>
Resolve the 2 conflict files in `extensions/games/game-witcher3/src/` per D-27-01 leaf-first sub-order: `installers.ts` → `index.ts`. Two atomic commits matching `resolve(witcher3): <file> — <stance>`. Per-extension typecheck (with caveat: no typecheck script — see action).

Purpose: Independent of plans 27-05 and 27-06 — wave 6 runs all three game extensions in parallel.

Output: Two commits on `v8.0/config-bucket`. After plan 27-07 lands, all 25 of 25 phase files are conflict-clean.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve installers.ts (leaf 1/2) and commit</name>
  <read_first>
    - extensions/games/game-witcher3/src/installers.ts (current state, in full)
    - `git show f15bbabb8:extensions/games/game-witcher3/src/installers.ts` (base-commit reference)
    - .planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md (D-27-00, D-27-01, Discretion)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md — confirm Witcher 3 is NOT named in any §-numbered gate (it isn't — Witcher 3 is plain script-merger / mod-installer territory, no playbook protections)
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Open `extensions/games/game-witcher3/src/installers.ts` and hand-resolve every conflict region. Stance defaults: fork-side wins for Linux-port (path-casing, fs-extra branches), upstream wins for new mod-installer features.

    After resolution: zero conflict markers. Stage only this file. Commit `resolve(witcher3): installers.ts — <stance>`.

    Run grep-checkpoint with `--skip-conflict-check`.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/games/game-witcher3/src/installers.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(witcher3\): installers\.ts —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/games/game-witcher3/src/installers.ts` returns empty
    - HEAD commit title starts with `resolve(witcher3): installers.ts —`
    - HEAD commit touches exactly one file
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
  </acceptance_criteria>
  <done>
    Leaf 1/2 resolved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolve index.ts (leaf 2/2), commit, run typecheck</name>
  <read_first>
    - extensions/games/game-witcher3/src/index.ts (current state, in full)
    - `git show f15bbabb8:extensions/games/game-witcher3/src/index.ts` (base-commit reference)
    - extensions/games/game-witcher3/src/installers.ts (just-committed shape)
    - extensions/games/game-witcher3/package.json (confirm: NO `typecheck` script — same caveat as BG3)
    - .planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md (D-27-04 typecheck cadence)
  </read_first>
  <action>
    Open `extensions/games/game-witcher3/src/index.ts` and hand-resolve every conflict region. Extension entry — registers Witcher 3 game definition + installer wiring. Stance defaults: fork-side wins for Linux-port imports / Proton-aware paths, upstream wins for new feature scaffolding.

    After resolution: zero conflict markers. Stage only this file. Commit `resolve(witcher3): index.ts — <stance>`.

    **Per-extension typecheck (D-27-04 deviation, same as BG3):** game-witcher3 has no `typecheck` script. Acceptable executor options:

    1. **tsc --noEmit (preferred):** `pnpm exec tsc --noEmit --project extensions/games/game-witcher3` — if no per-extension tsconfig.json exists, fall back to:
       ```
       pnpm exec tsc --noEmit --target es2020 --module commonjs --jsx react --esModuleInterop --skipLibCheck \
         extensions/games/game-witcher3/src/installers.ts \
         extensions/games/game-witcher3/src/index.ts
       ```
    2. **Build-as-typecheck:** `pnpm -F game-witcher3 build` — rolldown bundler refuses to bundle on errors.

    Either is acceptable. Confirm exit zero. If failure, amend the index.ts commit before checkpoint.

    Run `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check`. All gates pass.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/games/game-witcher3/; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(witcher3\): index\.ts —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/games/game-witcher3/src/index.ts` returns empty
    - `git grep -l '^<<<<<<< ' extensions/games/game-witcher3/` returns empty (entire extension clean)
    - HEAD commit title starts with `resolve(witcher3): index.ts —`
    - Per-extension typecheck (`pnpm exec tsc --noEmit -p extensions/games/game-witcher3` OR build-as-typecheck) exits zero
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
    - `git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\(witcher3\):'` returns 2
  </acceptance_criteria>
  <done>
    Both Witcher 3 files resolved; per-extension typecheck clean; 25/25 phase files done.
  </done>
</task>

</tasks>

<verification>
- Both files conflict-clean
- Two atomic commits matching `resolve(witcher3): <file> — <stance>`
- Witcher3 typecheck (`tsc --noEmit -p`) passes
- `scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
</verification>

<success_criteria>

- 25 of 25 Phase 27 files resolved (cumulative)
- Witcher3 extension typechecks via tsc --noEmit

</success_criteria>

<output>
After completion, create `.planning/phases/27-gamebryo-per-game-extensions/27-07-SUMMARY.md`
</output>
