---
phase: 27-gamebryo-per-game-extensions
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
    - .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh
autonomous: true
requirements: [SYNC-05, SYNC-06]

must_haves:
    truths:
        - "scripts/grep-checkpoint.sh extends with §1 (extension build guards), §3 (LOOT casing in autosort.ts), §10 (cross-compiled native binaries), BG3 4-class divine error gate, and Morrowind migrate103 warning gate"
        - "All five new gates exit zero on the current pre-resolution v8.0/config-bucket tree (§1/§3/§10 + BG3 + Morrowind invariants pre-date this merge)"
        - "Pre-existing Phase 26 gates (§6, §7a–d, 140a57217, no-conflict-marker) still pass with --skip-conflict-check"
        - "Single atomic commit on v8.0/config-bucket titled `resolve(checkpoint): scripts/grep-checkpoint.sh — extend with §1 + §3 + §10 + BG3 + Morrowind gates`"
        - "--skip-conflict-check flag still honoured (no-conflict-marker gate is the ONLY gate gated by that flag; the new BG3/Morrowind/§1/§3/§10 gates always run)"
    artifacts:
        - path: ".planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh"
          provides: "Extended re-grep harness — five new gates added on top of the seven Phase 26 gates"
          contains: "DivineExecMissing"
    key_links:
        - from: "scripts/grep-checkpoint.sh"
          to: "Plans 27-01..27-07 acceptance criteria"
          via: "Each per-file resolution plan invokes the script after its commit lands; the new gates surface upstream regressions immediately"
          pattern: "scripts/grep-checkpoint.sh"
        - from: "scripts/grep-checkpoint.sh"
          to: "Plan 27-08 done-gate"
          via: "Done-gate runs the full script (no flags) as item #2 of D-27-05"
          pattern: "grep-checkpoint.sh"
---

<objective>
Extend `.planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh` per D-27-03 and D-27-02. Five new gates added in-place: §1 extension build guards, §3 LOOT call-site casing in `autosort.ts`, §10 cross-compiled native binaries (existence), BG3 four-class divine error preservation (count ≥ 4), Morrowind `migrate103` warning preservation (count ≥ 1). The script remains a single shared harness — Phase 27 extends, does not rewrite, per CONTEXT D-27-03 and Phase 26 D-26-03 reuse pattern.

Purpose: Plans 27-01..27-07 invoke this harness after each per-file commit. Without the new gates, an upstream-side stance on `divineCore.ts` (or any package.json with an inline guard) would silently land and only surface at runtime in Phase 29. With the gates, regressions surface in-line.

Output: Extended `scripts/grep-checkpoint.sh` (12 gates total: 7 from Phase 26 + 5 new) and one atomic commit on `v8.0/config-bucket`.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md
@.planning/phases/26-mod-management-hot-zone/26-CONTEXT.md
@.planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend grep-checkpoint.sh with §1 + §3 + §10 + BG3 + Morrowind gates</name>
  <read_first>
    - .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh (current 7-gate body — read ALL of it; the existing structure is the template the new gates plug into: same `pass`/`fail` helpers, same `failures` counter, same `--skip-conflict-check` flag, NO `set -e`)
    - .planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md (D-27-02 — exact BG3 + Morrowind grep commands; D-27-03 — exact §1/§3/§10 grep commands; "Reusable Assets" — script lives at Phase 26's path, extended in-place per executor's call)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md §1 lines 21–48 — extension build guards, named-script form, sentinel files (skip-on-windows.mjs / skip-on-linux.mjs)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md §3 lines 67–73 — LOOT call-site casing, `path.basename(pluginList[id].filePath)` shape
    - VORTEX-LINUX-MERGE-PLAYBOOK.md §10 lines 163–179 — cross-compiled native binaries, file paths
    - extensions/games/game-baldursgate3/src/divineCore.ts at base commit f15bbabb8 — confirm 4 named error classes (DivineExecMissing, DivineMissingDotNet, DivineTimedOut, DivineAborted) all extend Error. Read via `git show f15bbabb8:extensions/games/game-baldursgate3/src/divineCore.ts | grep -nE 'class (DivineExecMissing|DivineMissingDotNet|DivineTimedOut|DivineAborted)\b extends Error'`
    - extensions/games/game-morrowind/src/migrations.js at base commit f15bbabb8 — confirm `'morrowind migrate103: mod directory missing'` warning string. Read via `git show f15bbabb8:extensions/games/game-morrowind/src/migrations.js | grep -n 'morrowind migrate103: mod directory missing'`
  </read_first>
  <action>
    Edit `.planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh` in place. Add five new gates AFTER the existing gate 6 (140a57217) and BEFORE the conflict-marker gate (currently gate 7). Renumber the conflict-marker gate to 12. Use the same `pass`/`fail`/`failures` pattern as the existing gates.

    Update the header comment block to add the five new gate descriptions and cite playbook §1/§3/§10 + D-27-02 (BG3) + D-27-02 (Morrowind). Update the usage-count comment near the bottom (`%d gate(s) clean`) — when `--skip-conflict-check` is on, total runs is now 11; without the flag, 12.

    **Gate 7 — §1 extension build guards (CONTEXT D-27-03):**
    - Negative check: `grep -l "node -e.*process.platform" extensions/*/package.json extensions/games/*/package.json 2>/dev/null | grep -v 'gamestore-xbox' | head -1` must produce zero output. If any line is produced, that's a non-gamestore-xbox extension carrying an inline `process.platform` guard — playbook §1 requires named-script form (skip-on-windows.mjs / skip-on-linux.mjs).
    - Positive existence: `test -f extensions/skip-on-windows.mjs && test -f extensions/skip-on-linux.mjs` must succeed.
    - Positive existence: `grep -q "skip-on-linux.mjs" extensions/gamestore-xbox/package.json` must succeed (gamestore-xbox is the one expected user of the named-script form).
    - All three sub-checks must pass for the gate to pass; report which sub-check failed in the FAIL line.

    **Gate 8 — §3 LOOT call-site casing in autosort.ts (CONTEXT D-27-03):**
    - Negative check: `git grep -nE 'pluginName\.toLowerCase' extensions/gamebryo-plugin-management/src/autosort.ts | grep -E '(loadPluginsAsync|getPluginMetadataAsync|getPluginAsync|sortPluginsAsync)' | head -1` must produce zero output. (LOOT call sites must NOT use `pluginName.toLowerCase` as the basename arg.)
    - Positive existence: `git grep -cE 'path\.basename\(pluginList\[' extensions/gamebryo-plugin-management/src/autosort.ts` must return ≥ 4 (one per LOOT call site). NOTE: `grep -c` counts comment-only matches too; if the file ever grows comment lines containing the pattern this can become self-invalidating. Initial count expected to be exactly 4 (no comments). If executor finds count > 4 due to legitimate growth, increment threshold; do not lower below 4.

    **Gate 9 — §10 cross-compiled native binaries on disk (CONTEXT D-27-03):**
    - All four `test -f` checks must pass:
      - `extensions/gamebryo-plugin-management/dist/node-loot.node`
      - `extensions/gamebryo-plugin-management/dist/libloot.so.0`
      - `extensions/gamebryo-plugin-management/dist/libloot_wstring_stub.so`
      - `extensions/gamebryo-bsa-support/dist/bsatk.node`
    - Phase 27 only edits `gamebryo-plugin-management/src/**`, but a hand-resolution that accidentally restages or git-removes any `dist/**` would surface here. Same gate is durable for v8.1+ syncs.

    **Gate 10 — BG3 4-class divine error preservation (CONTEXT D-27-02):**
    - `git grep -nE 'class (DivineExecMissing|DivineMissingDotNet|DivineTimedOut|DivineAborted)\b extends Error' extensions/games/game-baldursgate3/src/divineCore.ts | wc -l` must return ≥ 4.
    - Detects: upstream removing or renaming any of the 4 error classes; partial reverts where a subset survives.
    - This file IS in scope this phase (resolved by plan 27-05). Gate is allowed to fail before plan 27-05 lands its commit IF the conflict region containing the error classes is still in the file with conflict markers — the regex won't match `class DivineExecMissing` if it's wrapped in conflict-marker noise. Document that at plan 27-05 commit time the gate must flip to clean. Until then, the gate noise is informational only on this specific file. Do NOT special-case the gate to suppress — let the failing line tell the executor.

    **Gate 11 — Morrowind migrate103 warning preservation (CONTEXT D-27-02):**
    - `git grep -n 'morrowind migrate103: mod directory missing' extensions/games/game-morrowind/src/migrations.js | wc -l` must return ≥ 1.
    - Detects: upstream replacing the warning string with a new copy that doesn't match the prefix-anchored substring.
    - This file IS in scope this phase (resolved by plan 27-06). Same caveat as gate 10 — may noise-fail if the warning region is inside a conflict block. Plan 27-06 must flip it clean.

    **Gate 12 (was 7): no conflict markers** — broaden the path list. Replace `MOD_MGMT_DIR` single-path check with the seven Phase 27 directories AND the original mod_management dir (we must continue protecting Phase 26's invariant). New path list:
    - `src/renderer/src/extensions/mod_management/` (Phase 26 — must remain clean)
    - `extensions/gamebryo-plugin-management/`
    - `extensions/gamebryo-savegame-management/`
    - `extensions/collections/`
    - `extensions/modtype-bepinex/`
    - `extensions/games/game-baldursgate3/`
    - `extensions/games/game-morrowind/`
    - `extensions/games/game-witcher3/`

    Update the gate label and `marker_files` invocation accordingly. The `--skip-conflict-check` flag continues to gate ONLY this last gate.

    After editing, run the script with `--skip-conflict-check`. ALL eleven non-conflict gates must pass on the unmodified `v8.0/config-bucket` tree because §1/§3/§10 + BG3 + Morrowind invariants pre-date this merge and the BG3/Morrowind files DO have conflict markers but the protected regions (4 error classes; warning string) are EXPECTED to still match because conflict regions wrap added/removed lines, not the unchanged protected lines. If any gate fails on baseline, STOP and surface to the user — that means an invariant is already eaten, and Phase 27's premise ("resolve, don't re-apply") is wrong for that area.

    Then run the script WITHOUT `--skip-conflict-check`. It must exit non-zero (because the 25 Phase 27 conflict files still exist). Confirm the failure line names the conflict-marker gate and lists files including (as a sanity check) some of the 25 files in scope.

    Stage only the script and commit with title `resolve(checkpoint): scripts/grep-checkpoint.sh — extend with §1 + §3 + §10 + BG3 + Morrowind gates`. Body: list the five new gates by name (§1, §3, §10, BG3 4-class, Morrowind migrate103) and the playbook section / CONTEXT decision each one cites.

    Do NOT push. D-27-00 specifies one push at phase end via plan 27-08.

  </action>
  <verify>
    <automated>test -x .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(checkpoint\): scripts/grep-checkpoint\.sh — extend with §1 \+ §3 \+ §10 \+ BG3 \+ Morrowind gates$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^# Gate' .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh` returns ≥ 12 (or equivalent — the script has 12 labelled gates after editing)
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero (all 11 non-conflict gates pass on pre-resolution tree)
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh` exits non-zero (conflict-marker gate trips on the 25 unresolved files)
    - Header comment names §1, §3, §10, "BG3 4-class divine errors", "Morrowind migrate103" by exact label
    - Conflict-marker gate path list includes all 7 Phase 27 directories plus `src/renderer/src/extensions/mod_management/` (Phase 26 path retained)
    - HEAD commit title matches exactly `resolve(checkpoint): scripts/grep-checkpoint.sh — extend with §1 + §3 + §10 + BG3 + Morrowind gates`
    - HEAD commit touches exactly one file: `.planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh`
    - No push has happened (push is plan 27-08 territory)
  </acceptance_criteria>
  <done>
    Script extended with five new durable gates; baseline-clean modulo expected conflict-marker failure; ready for plans 27-01..27-07 to invoke after each per-file commit.
  </done>
</task>

</tasks>

<verification>
- `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
- HEAD commit body cites §1, §3, §10, BG3 D-27-02, Morrowind D-27-02
- Single-file commit on v8.0/config-bucket
</verification>

<success_criteria>

- Five new gates added (12 total)
- Phase 26 gates untouched
- Baseline run clean modulo expected conflict-marker failure
- Commit 0 of Phase 27 lands on v8.0/config-bucket

</success_criteria>

<output>
After completion, create `.planning/phases/27-gamebryo-per-game-extensions/27-00-SUMMARY.md`
</output>
