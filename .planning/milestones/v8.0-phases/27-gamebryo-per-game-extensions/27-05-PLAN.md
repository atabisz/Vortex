---
phase: 27-gamebryo-per-game-extensions
plan: 05
type: execute
wave: 6
depends_on: [27-04]
files_modified:
    - extensions/games/game-baldursgate3/src/cache.ts
    - extensions/games/game-baldursgate3/src/util.ts
    - extensions/games/game-baldursgate3/src/divineCore.ts
    - extensions/games/game-baldursgate3/src/divineWrapper.ts
    - extensions/games/game-baldursgate3/src/divineCore.test.ts
    - extensions/games/game-baldursgate3/src/loadOrder.ts
    - extensions/games/game-baldursgate3/src/index.tsx
autonomous: true
requirements: [SYNC-19]

must_haves:
    truths:
        - "All 7 BG3 conflict files resolved in leaf-first order: cache.ts → util.ts → divineCore.ts → divineWrapper.ts → divineCore.test.ts → loadOrder.ts → index.tsx"
        - "Seven atomic commits on v8.0/config-bucket each titled `resolve(bg3): <file> — <stance>` per D-27-00"
        - "After divineCore.ts commits, gate 10 (BG3 4-class) of grep-checkpoint exits clean: `git grep -nE 'class (DivineExecMissing|DivineMissingDotNet|DivineTimedOut|DivineAborted)\\b extends Error' extensions/games/game-baldursgate3/src/divineCore.ts | wc -l` returns ≥ 4"
        - "After the seventh commit, BG3 typecheck passes — game-baldursgate3 has no `typecheck` script in package.json, so the executor runs `pnpm exec tsc --noEmit -p extensions/games/game-baldursgate3` (or equivalent project-config-aware tsc invocation) and confirms exit zero"
        - "After EACH commit, `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero"
    artifacts:
        - path: "extensions/games/game-baldursgate3/src/divineCore.ts"
          provides: "Divine.exe IPC core — heaviest single file in scope per CONTEXT 'Integration Points'; hosts the 4 named error classes that gate 10 protects"
          contains: "DivineExecMissing"
    key_links:
        - from: "divineCore.ts"
          to: "divineWrapper.ts + divineCore.test.ts + loadOrder.ts + index.tsx"
          via: "Source before consumers; the 4 error classes are imported / re-thrown downstream"
          pattern: "from './divineCore'"
        - from: "all 6 above"
          to: "index.tsx"
          via: "Extension entry — registers the BG3 game definition last"
          pattern: "init"
---

<objective>
Resolve the 7 conflict files in `extensions/games/game-baldursgate3/src/` per D-27-01 leaf-first sub-order: `cache.ts` → `util.ts` → `divineCore.ts` → `divineWrapper.ts` → `divineCore.test.ts` → `loadOrder.ts` → `index.tsx`. Seven atomic commits matching `resolve(bg3): <file> — <stance>`. Per-extension typecheck after the seventh commit (via `tsc --noEmit -p` because BG3 has no typecheck script — see action below).

**Critical preservation:** `divineCore.ts` MUST retain its 4 named error classes (DivineExecMissing, DivineMissingDotNet, DivineTimedOut, DivineAborted, each `extends Error`). Gate 10 of the extended grep-checkpoint script enforces this with the prefix-anchored regex `class (Divine…)\b extends Error` count ≥ 4 per D-27-02. The base commit `f15bbabb8` confirms all 4 classes exist with this exact shape.

Purpose: Independent of plans 27-06 and 27-07 — wave 6 runs all three game extensions in parallel. BG3 is the heaviest of the three (7 files, including the playbook-protected `divineCore.ts`).

Output: Seven commits on `v8.0/config-bucket`. After plan 27-05 lands, 3 of 25 phase files remain (Morrowind 1 + Witcher3 2).
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve cache.ts (leaf 1/7) and commit</name>
  <read_first>
    - extensions/games/game-baldursgate3/src/cache.ts (current state, in full)
    - `git show f15bbabb8:extensions/games/game-baldursgate3/src/cache.ts` (base-commit reference)
    - .planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md (D-27-01 sub-order; D-27-02 BG3 gate)
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Open `extensions/games/game-baldursgate3/src/cache.ts` and hand-resolve every conflict region. Stance defaults: fork-side wins for Linux-port imports, upstream wins for new feature scaffolding.

    After resolution: zero conflict markers. Stage only this file. Commit `resolve(bg3): cache.ts — <stance>`.

    Run grep-checkpoint with `--skip-conflict-check`. Note: gate 10 (BG3 4-class) may already pass or fail depending on whether divineCore.ts conflict regions wrap the error class definitions. The gate state at this point is informational — it must flip to clean by the end of task 3.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/games/game-baldursgate3/src/cache.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(bg3\): cache\.ts —'</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/games/game-baldursgate3/src/cache.ts` returns empty
    - HEAD commit title starts with `resolve(bg3): cache.ts —`
    - HEAD commit touches exactly one file
  </acceptance_criteria>
  <done>
    Leaf 1/7 resolved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolve util.ts (leaf 2/7) and commit</name>
  <read_first>
    - extensions/games/game-baldursgate3/src/util.ts (current state, in full)
    - `git show f15bbabb8:extensions/games/game-baldursgate3/src/util.ts` (base-commit reference)
    - extensions/games/game-baldursgate3/src/cache.ts (just-committed shape)
  </read_first>
  <action>
    Open `extensions/games/game-baldursgate3/src/util.ts` and hand-resolve every conflict region. Stance defaults apply. Watch for path-handling conflicts — BG3 has heavy filesystem interaction (loadorder.lsx, etc.); fork-side wins for any case-folding or path-normalisation logic.

    After resolution: zero conflict markers. Stage only this file. Commit `resolve(bg3): util.ts — <stance>`.

    Run grep-checkpoint with `--skip-conflict-check`.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/games/game-baldursgate3/src/util.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(bg3\): util\.ts —'</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/games/game-baldursgate3/src/util.ts` returns empty
    - HEAD commit title starts with `resolve(bg3): util.ts —`
  </acceptance_criteria>
  <done>
    Leaf 2/7 resolved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Resolve divineCore.ts (leaf 3/7) — PRESERVATION-CRITICAL — and commit</name>
  <read_first>
    - extensions/games/game-baldursgate3/src/divineCore.ts (current state, in full — read every conflict region carefully)
    - `git show f15bbabb8:extensions/games/game-baldursgate3/src/divineCore.ts` (base-commit reference; confirm the 4 error classes shape: each `class DivineExecMissing extends Error { constructor(msg: string) { super(msg); this.name = 'DivineExecMissing'; } }` etc.)
    - .planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md D-27-02 (BG3 gate spec — exact regex `class (DivineExecMissing|DivineMissingDotNet|DivineTimedOut|DivineAborted)\b extends Error`, count ≥ 4)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md — confirm BG3 divine errors are NOT in the §-numbered playbook (they're a CONTEXT D-27-02 add)
    - .planning/phases/26-mod-management-hot-zone/26-CONTEXT.md D-26-03 (count-threshold gate idiom — same defensiveness shape applied here per CONTEXT "Specific Ideas")
  </read_first>
  <action>
    **HEAVIEST FILE IN SCOPE per CONTEXT "Integration Points".** Hand-read EVERY conflict region carefully — this file hosts the 4 named error classes that gate 10 protects.

    Open `extensions/games/game-baldursgate3/src/divineCore.ts`. For each conflict region:
    1. Identify whether the region touches any of the 4 classes (`DivineExecMissing`, `DivineMissingDotNet`, `DivineTimedOut`, `DivineAborted`).
    2. If yes — fork-side wins by default; the base commit `f15bbabb8` is the authoritative shape. If upstream genuinely improved the class (e.g. richer error context) and the improvement preserves all 4 names + `extends Error` shape, take upstream — but verify gate 10 still passes after.
    3. If no (region is unrelated — divine.exe spawn logic, IPC framing, etc.): default stance (fork-side for Linux fixes — particularly the `which divine` lookup vs `divine.exe` Windows path, the `process.platform` branches, the Wine/native dispatch — all fork-side; upstream-side for new feature scaffolding).

    After all markers resolved, run the gate 10 grep BEFORE staging:
    ```
    git grep -nE 'class (DivineExecMissing|DivineMissingDotNet|DivineTimedOut|DivineAborted)\b extends Error' extensions/games/game-baldursgate3/src/divineCore.ts | wc -l
    ```
    Must return ≥ 4. If less, STOP — the resolution ate one or more error classes. Re-open the file, re-resolve, re-check.

    Stage only this file. Commit `resolve(bg3): divineCore.ts — <stance> (preserves 4 divine error classes per D-27-02)`. Body: list each conflict region resolved + which side won + explicit confirmation that all 4 error classes survive.

    Run `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check`. **Gate 10 (BG3 4-class) MUST pass here.** If it fails, amend the commit with the fix before proceeding to task 4.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/games/game-baldursgate3/src/divineCore.ts; test $? -ne 0 &amp;&amp; test $(git grep -nE 'class (DivineExecMissing|DivineMissingDotNet|DivineTimedOut|DivineAborted)\b extends Error' extensions/games/game-baldursgate3/src/divineCore.ts | wc -l) -ge 4 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(bg3\): divineCore\.ts —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/games/game-baldursgate3/src/divineCore.ts` returns empty
    - **Gate 10 — `git grep -nE 'class (DivineExecMissing|DivineMissingDotNet|DivineTimedOut|DivineAborted)\b extends Error' extensions/games/game-baldursgate3/src/divineCore.ts | wc -l` returns ≥ 4** (all four error classes preserved)
    - HEAD commit title starts with `resolve(bg3): divineCore.ts —`
    - HEAD commit body explicitly mentions D-27-02 / 4 divine error classes preserved
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero (gate 10 specifically passes)
  </acceptance_criteria>
  <done>
    divineCore.ts resolved with all 4 named error classes intact; gate 10 flips to clean.
  </done>
</task>

<task type="auto">
  <name>Task 4: Resolve divineWrapper.ts (leaf 4/7) and commit</name>
  <read_first>
    - extensions/games/game-baldursgate3/src/divineWrapper.ts (current state, in full)
    - `git show f15bbabb8:extensions/games/game-baldursgate3/src/divineWrapper.ts` (base-commit reference)
    - extensions/games/game-baldursgate3/src/divineCore.ts (just-committed shape — wrapper imports the 4 error classes)
  </read_first>
  <action>
    Open `extensions/games/game-baldursgate3/src/divineWrapper.ts` and hand-resolve every conflict region. Stance defaults apply. If the wrapper imports/throws any of the 4 error classes, ensure those imports survive resolution (wrapper without `DivineExecMissing` etc. would type-error against the just-resolved divineCore.ts).

    After resolution: zero conflict markers. Stage only this file. Commit `resolve(bg3): divineWrapper.ts — <stance>`.

    Run grep-checkpoint with `--skip-conflict-check`. Gate 10 stays clean (divineCore.ts unchanged here).

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/games/game-baldursgate3/src/divineWrapper.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(bg3\): divineWrapper\.ts —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/games/game-baldursgate3/src/divineWrapper.ts` returns empty
    - HEAD commit title starts with `resolve(bg3): divineWrapper.ts —`
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero (gate 10 still passes)
  </acceptance_criteria>
  <done>
    Leaf 4/7 resolved.
  </done>
</task>

<task type="auto">
  <name>Task 5: Resolve divineCore.test.ts (leaf 5/7) and commit</name>
  <read_first>
    - extensions/games/game-baldursgate3/src/divineCore.test.ts (current state, in full)
    - `git show f15bbabb8:extensions/games/game-baldursgate3/src/divineCore.test.ts` (base-commit reference)
    - extensions/games/game-baldursgate3/src/divineCore.ts (just-resolved shape — test asserts class names + behaviours)
    - .planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md (Discretion — D-27-01 suggests divineCore.ts before divineCore.test.ts; this plan follows that order. The test file may also assert the 4 error class names)
  </read_first>
  <action>
    Open `extensions/games/game-baldursgate3/src/divineCore.test.ts` and hand-resolve every conflict region. The test should assert the 4 error class names (`expect(err).toBeInstanceOf(DivineExecMissing)` etc.) — preserve those assertions; if upstream removed them, fork-side wins.

    After resolution: zero conflict markers. Stage only this file. Commit `resolve(bg3): divineCore.test.ts — <stance>`.

    Run grep-checkpoint with `--skip-conflict-check`.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/games/game-baldursgate3/src/divineCore.test.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(bg3\): divineCore\.test\.ts —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/games/game-baldursgate3/src/divineCore.test.ts` returns empty
    - HEAD commit title starts with `resolve(bg3): divineCore.test.ts —`
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
  </acceptance_criteria>
  <done>
    Leaf 5/7 resolved.
  </done>
</task>

<task type="auto">
  <name>Task 6: Resolve loadOrder.ts (leaf 6/7) and commit</name>
  <read_first>
    - extensions/games/game-baldursgate3/src/loadOrder.ts (current state, in full)
    - `git show f15bbabb8:extensions/games/game-baldursgate3/src/loadOrder.ts` (base-commit reference)
    - extensions/games/game-baldursgate3/src/divineWrapper.ts (just-committed shape — loadOrder may invoke divine via wrapper)
  </read_first>
  <action>
    Open `extensions/games/game-baldursgate3/src/loadOrder.ts` and hand-resolve every conflict region. BG3 modsettings.lsx serialisation logic; stance defaults apply. Watch for path-casing logic — fork-side wins for any case-folding handling.

    After resolution: zero conflict markers. Stage only this file. Commit `resolve(bg3): loadOrder.ts — <stance>`.

    Run grep-checkpoint with `--skip-conflict-check`.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/games/game-baldursgate3/src/loadOrder.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(bg3\): loadOrder\.ts —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/games/game-baldursgate3/src/loadOrder.ts` returns empty
    - HEAD commit title starts with `resolve(bg3): loadOrder.ts —`
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
  </acceptance_criteria>
  <done>
    Leaf 6/7 resolved.
  </done>
</task>

<task type="auto">
  <name>Task 7: Resolve index.tsx (leaf 7/7), commit, run BG3 typecheck</name>
  <read_first>
    - extensions/games/game-baldursgate3/src/index.tsx (current state, in full)
    - `git show f15bbabb8:extensions/games/game-baldursgate3/src/index.tsx` (base-commit reference)
    - All 6 just-committed sibling files
    - extensions/games/game-baldursgate3/package.json (confirm: NO `typecheck` script — BG3 builds via rolldown bundler; executor must use `pnpm exec tsc --noEmit` directly)
    - .planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md (D-27-04 — typecheck command "executor confirms exact filter names against pnpm-workspace.yaml / each package.json `name` field at plan time")
  </read_first>
  <action>
    Open `extensions/games/game-baldursgate3/src/index.tsx` and hand-resolve every conflict region. Extension entry — registers the BG3 game definition + load-order page + divine wiring. Stance defaults: fork-side wins for any Linux-port (`@vortex/paths`, Proton-aware game.exe path), upstream wins for new feature scaffolding.

    After resolution: zero conflict markers. Stage only this file. Commit `resolve(bg3): index.tsx — <stance>`.

    **Per-extension typecheck (D-27-04 deviation):** game-baldursgate3 has no `typecheck` script in `package.json`. Run typecheck directly:
    ```
    pnpm exec tsc --noEmit --project extensions/games/game-baldursgate3
    ```
    or, if no per-extension tsconfig.json exists:
    ```
    pnpm exec tsc --noEmit --target es2020 --module commonjs --jsx react --esModuleInterop --skipLibCheck \
      extensions/games/game-baldursgate3/src/cache.ts \
      extensions/games/game-baldursgate3/src/util.ts \
      extensions/games/game-baldursgate3/src/divineCore.ts \
      extensions/games/game-baldursgate3/src/divineWrapper.ts \
      extensions/games/game-baldursgate3/src/divineCore.test.ts \
      extensions/games/game-baldursgate3/src/loadOrder.ts \
      extensions/games/game-baldursgate3/src/index.tsx
    ```
    Confirm exit zero. If typecheck fails, amend the index.tsx commit before checkpoint.

    Alternative acceptable executor stance: `pnpm -F game-baldursgate3 build` (rolldown bundler — produces `dist/index.cjs`; bundler errors surface here). Build-as-typecheck is acceptable for BG3 because esbuild/rolldown will refuse to bundle on syntax/resolution errors. Note: build-as-typecheck does NOT catch all TypeScript errors (only syntax + resolution); prefer `tsc --noEmit` if available.

    Run grep-checkpoint with `--skip-conflict-check`. **Gate 10 must still pass.**

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/games/game-baldursgate3/; test $? -ne 0 &amp;&amp; test $(git grep -nE 'class (DivineExecMissing|DivineMissingDotNet|DivineTimedOut|DivineAborted)\b extends Error' extensions/games/game-baldursgate3/src/divineCore.ts | wc -l) -ge 4 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(bg3\): index\.tsx —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/games/game-baldursgate3/src/index.tsx` returns empty
    - `git grep -l '^<<<<<<< ' extensions/games/game-baldursgate3/` returns empty (entire extension clean)
    - HEAD commit title starts with `resolve(bg3): index.tsx —`
    - `git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\(bg3\):'` returns 7
    - **Gate 10 (BG3 4-class divine error preservation) passes** — `git grep -nE 'class (DivineExecMissing|DivineMissingDotNet|DivineTimedOut|DivineAborted)\b extends Error' extensions/games/game-baldursgate3/src/divineCore.ts | wc -l` returns ≥ 4
    - Per-extension typecheck (`pnpm exec tsc --noEmit -p extensions/games/game-baldursgate3` OR equivalent build-as-typecheck) exits zero
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
  </acceptance_criteria>
  <done>
    All 7 BG3 files resolved; 4 divine error classes preserved (gate 10 confirms); per-extension typecheck clean; 22/25 phase files done.
  </done>
</task>

</tasks>

<verification>
- All 7 files conflict-clean
- Seven atomic commits matching `resolve(bg3): <file> — <stance>`
- Gate 10 (BG3 4-class) passes after divineCore.ts commit and stays clean
- BG3 typecheck (`tsc --noEmit -p`) passes
- `scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
</verification>

<success_criteria>

- 22 of 25 Phase 27 files resolved (cumulative)
- BG3 4-class divine error preservation invariant intact
- BG3 extension typechecks via tsc --noEmit

</success_criteria>

<output>
After completion, create `.planning/phases/27-gamebryo-per-game-extensions/27-05-SUMMARY.md`
</output>
