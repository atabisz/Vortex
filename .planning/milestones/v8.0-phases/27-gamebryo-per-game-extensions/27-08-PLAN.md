---
phase: 27-gamebryo-per-game-extensions
plan: 08
type: execute
wave: 7
depends_on: [27-01, 27-02, 27-03, 27-04, 27-05, 27-06, 27-07]
files_modified: []
autonomous: true
requirements: [SYNC-05, SYNC-06, SYNC-17, SYNC-19]

must_haves:
    truths:
        - "All six D-27-05 done-gate items pass: zero conflict markers across 7 phase directories, grep-checkpoint.sh exits zero (full run, no flags), each touched extension typechecks clean, full-repo `pnpm typecheck` exits zero, 25 atomic commits matching the per-extension title format are present (+1 setup commit = 26 total), force-with-lease push to fork/sync/upstream-v2.0.0 succeeds"
        - "All 4 phase requirement IDs (SYNC-05, SYNC-06, SYNC-17, SYNC-19) verifiable as satisfied via the done-gate evidence"
        - "Branch v8.0/config-bucket is pushed to fork remote with --force-with-lease per D-27-00"
    artifacts:
        - path: ".planning/phases/27-gamebryo-per-game-extensions/27-DONE-GATE.md"
          provides: "Done-gate evidence record — captures the output of each of the 6 D-27-05 checks plus the push confirmation, for milestone audit trail"
          contains: "## D-27-05 Done Gate"
    key_links:
        - from: "27-DONE-GATE.md"
          to: "ROADMAP.md Phase 27 row"
          via: "Evidence that success criteria 1–5 in the ROADMAP Phase 27 entry plus CONTEXT D-27-05's added 6th item are met"
          pattern: "Phase 27"
        - from: "v8.0/config-bucket"
          to: "fork remote (atabisz/Vortex)"
          via: "--force-with-lease push to sync/upstream-v2.0.0"
          pattern: "fork/sync/upstream-v2.0.0"
---

<objective>
Run the full D-27-05 done-gate — six checks — and push `v8.0/config-bucket` to the fork remote with `--force-with-lease` per D-27-00. Capture evidence in `.planning/phases/27-gamebryo-per-game-extensions/27-DONE-GATE.md` for milestone audit trail. Update STATE.md and ROADMAP.md to mark Phase 27 complete.

Purpose: Single point where the phase declares done. Catches any regression that may have crept in between plan 27-07 and now (uncommitted edits, drift). Pushes ONLY after all six checks pass. Same pattern as Phase 26 plan 26-10.

Output: Pushed branch on fork remote, evidence file `27-DONE-GATE.md`, ROADMAP.md Phase 27 row marked complete.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md
@.planning/phases/26-mod-management-hot-zone/26-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all six D-27-05 done-gate checks and write evidence file</name>
  <read_first>
    - .planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md (D-27-05 — six-item done-gate; D-27-00 — commit title format and force-with-lease push policy; D-27-04 — phase-end full typecheck cadence)
    - .planning/phases/24-config-bucket/24-CONTEXT.md (D-02 push policy — force-with-lease to fork remote at phase end)
    - .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh (extended harness — runs without flags here for the conflict-marker assertion)
    - .planning/phases/26-mod-management-hot-zone/26-10-PLAN.md (template for done-gate evidence file shape)
    - CLAUDE.md memory note "Git push SSH URL" — sandbox blocks `.git/config`; push with inline SSH URL `git push git@github.com:atabisz/Vortex.git <branch>` if standard `fork` remote push fails with permissions error
    - CLAUDE.md memory note "GitHub auth token" — `GH_TOKEN` env var available for atabisz when gh auth is expired
    - CLAUDE.md memory note "SSH commit signing" — all commits signed with SSH (~/.ssh/id_ed25519)
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Run each D-27-05 check and capture output verbatim:

    1. **Zero conflict markers across the 7 phase directories (D-27-05 #1):**
       ```
       git grep '^<<<<<<< ' \
         extensions/gamebryo-plugin-management/ \
         extensions/gamebryo-savegame-management/ \
         extensions/collections/ \
         extensions/modtype-bepinex/ \
         extensions/games/game-baldursgate3/ \
         extensions/games/game-morrowind/ \
         extensions/games/game-witcher3/
       ```
       Expect non-zero exit (no matches).

    2. **scripts/grep-checkpoint.sh exits zero — FULL run, NO `--skip-conflict-check` (D-27-05 #2):**
       ```
       bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh
       ```
       Capture full stdout/stderr. All twelve gates must pass: §6, §7a, §7b, §7c, §7d, 140a57217, §1, §3, §10, BG3 4-class, Morrowind migrate103, conflict-marker. Mod-management invariants from Phase 26 still hold.

    3. **Per-extension typecheck per D-27-05 #3:** Re-run each extension's typecheck filter. For extensions WITH a typecheck script:
       ```
       pnpm typecheck -F gamebryo-savegame-management
       pnpm typecheck -F gamebryo-plugin-management
       pnpm typecheck -F modtype-bepinex
       pnpm typecheck -F collections
       ```
       For extensions WITHOUT a typecheck script (BG3, Morrowind, Witcher3) — same fallback as in plans 27-05/06/07:
       ```
       pnpm exec tsc --noEmit -p extensions/games/game-baldursgate3 || \
         pnpm -F game-baldursgate3 build
       pnpm -F game-morrowind build  # .js — bundler-as-syntax-check
       pnpm exec tsc --noEmit -p extensions/games/game-witcher3 || \
         pnpm -F game-witcher3 build
       ```
       All must exit zero. Capture exit codes per extension.

    4. **Phase-end full-repo `pnpm typecheck` (D-27-05 #4 — added by CONTEXT D-27-05 as the 6th gate):**
       ```
       pnpm typecheck
       ```
       (or `pnpm -r typecheck` depending on root package.json — confirm at execution time. Phase 26's done-gate ran `pnpm typecheck -F @vortex/renderer` only; Phase 27 broadens to full-repo per CONTEXT D-27-05's added item.)
       Capture stdout tail + exit code. Must exit zero. This is the cross-extension drift check — catches anything per-extension typechecks miss.

    5. **25 atomic commits + 1 setup commit (D-27-05 #5):**
       ```
       git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | head -50
       ```
       Filter for the per-extension scopes:
       ```
       git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 \
         | grep -cE '^[0-9a-f]+ resolve\((savegame-mgmt|plugin-mgmt|bepinex|collections|bg3|morrowind|witcher3)\):'
       ```
       Must return 25 (2 + 4 + 3 + 6 + 7 + 1 + 2 = 25 per CONTEXT D-27-00).

       Plus the setup commit (Plan 27-00's `resolve(checkpoint): scripts/grep-checkpoint.sh — extend …`):
       ```
       git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 \
         | grep -cE '^[0-9a-f]+ resolve\(checkpoint\): scripts/grep-checkpoint\.sh —'
       ```
       Must return 1.

       Total Phase 27 commits over `origin/sync/upstream-v2.0.0`: 26 (25 resolve commits + 1 setup) + however many Phase 26 already added when phase 26 pushed (Phase 26 pushed at plan 26-10, so origin already has Phase 26's commits — the 26 number is Phase 27's increment).

       Validate: `git rev-list --count origin/sync/upstream-v2.0.0..v8.0/config-bucket` returns ≥ 26 (typically exactly 26 + any plan-summary docs commits if those were pushed by execute-phase, but those land separately and are non-blocking).

    6. **Force-with-lease push (D-27-05 #6):**
       ```
       git push --force-with-lease fork v8.0/config-bucket:sync/upstream-v2.0.0
       ```
       If `fork` remote push fails with permissions/sandbox error, fall back to inline SSH URL:
       ```
       git push --force-with-lease git@github.com:atabisz/Vortex.git v8.0/config-bucket:sync/upstream-v2.0.0
       ```
       Capture the push output (the new remote SHA).

    Checks 1–5 must ALL pass before check 6 (the push). If any of 1–5 fails, STOP and surface to the user — no push happens.

    Write `.planning/phases/27-gamebryo-per-game-extensions/27-DONE-GATE.md` with sections:
    - `## D-27-05 Done Gate`
    - `### 1. Zero conflict markers across 7 phase directories` (echo of `git grep` exit code + the path list grepped)
    - `### 2. grep-checkpoint.sh full run` (full stdout/stderr — should show 12 OK lines)
    - `### 3. Per-extension typecheck` (table: extension name → exit code → command used, including which fallback was used for BG3/Morrowind/Witcher3)
    - `### 4. Phase-end full-repo pnpm typecheck` (stdout tail + exit code)
    - `### 5. 26 atomic commits` (the filtered `git log --oneline` output; per-extension counts: savegame-mgmt 2, plugin-mgmt 4, bepinex 3, collections 6, bg3 7, morrowind 1, witcher3 2 = 25 + checkpoint setup 1 = 26)
    - `### 6. Force-with-lease push` (remote SHA before + after, push command used, fallback if any)
    - `## Requirements satisfied` — table mapping SYNC-05, SYNC-06, SYNC-17, SYNC-19 to the D-27-05 evidence that proves each one
    - `## Phase 27 status: COMPLETE` (timestamped per system clock)

    Stage `.planning/phases/27-gamebryo-per-game-extensions/27-DONE-GATE.md` and commit with title `docs(phase-27): done-gate evidence`. Push that commit too (`.planning/` doc lives on `master` / `v8.0/config-bucket` per CLAUDE.md branch policy — never cherry-picked to `linux-port`).

    Update `.planning/ROADMAP.md` Phase 27 row: change checkbox `[ ]` → `[x]`, add `(complete YYYY-MM-DD)` after the title; commit as `docs(roadmap): mark Phase 27 complete`. Push.

    Update `.planning/STATE.md` `current_position` and `progress` blocks: increment completed_phases to 4, increment completed_plans by the 9 plan summaries Phase 27 produced (27-00 through 27-08), set `stopped_at: Phase 27 complete; ready for Phase 28 plan`. Commit as `docs(state): mark Phase 27 complete; ready for Phase 28`. Push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/gamebryo-plugin-management/ extensions/gamebryo-savegame-management/ extensions/collections/ extensions/modtype-bepinex/ extensions/games/game-baldursgate3/ extensions/games/game-morrowind/ extensions/games/game-witcher3/; test $? -ne 0 &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh &amp;&amp; pnpm typecheck -F gamebryo-savegame-management &amp;&amp; pnpm typecheck -F gamebryo-plugin-management &amp;&amp; pnpm typecheck -F modtype-bepinex &amp;&amp; pnpm typecheck -F collections &amp;&amp; test $(git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\((savegame-mgmt|plugin-mgmt|bepinex|collections|bg3|morrowind|witcher3)\):') -eq 25 &amp;&amp; test -f .planning/phases/27-gamebryo-per-game-extensions/27-DONE-GATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' <7 phase directories>` returns non-zero (no matching files)
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh` (NO --skip-conflict-check) exits zero — all 12 gates pass
    - All four extensions with typecheck scripts (savegame-mgmt, plugin-mgmt, bepinex, collections) exit zero on `pnpm typecheck -F <name>`
    - All three game extensions (BG3, Morrowind, Witcher3) exit zero on tsc --noEmit OR build (whichever fallback the executor used; documented in the evidence file)
    - Phase-end full-repo `pnpm typecheck` exits zero
    - `git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\((savegame-mgmt|plugin-mgmt|bepinex|collections|bg3|morrowind|witcher3)\):'` returns exactly 25
    - `git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\(checkpoint\): scripts/grep-checkpoint\.sh —'` returns 1 (the Plan 27-00 setup commit)
    - `git ls-remote fork sync/upstream-v2.0.0 | grep -qE '^[0-9a-f]+'` returns the expected post-push SHA (force-with-lease succeeded)
    - `.planning/phases/27-gamebryo-per-game-extensions/27-DONE-GATE.md` exists with all six gate sections + requirements table + COMPLETE status line
    - `.planning/ROADMAP.md` Phase 27 row shows `[x]` and `(complete <date>)`
    - `.planning/STATE.md` reflects Phase 27 complete; ready for Phase 28
    - **Gate 10 (BG3 4-class) and Gate 11 (Morrowind migrate103) both pass** in the full grep-checkpoint run — explicitly verified
    - **Gate 6 (Phase 26 140a57217) still passes** — Phase 26's invariants survived Phase 27 untouched
  </acceptance_criteria>
  <done>
    Phase 27 done-gate evidence captured; force-with-lease push succeeded; ROADMAP and STATE updated; ready for Phase 28.
  </done>
</task>

</tasks>

<verification>
- All six D-27-05 done-gate items satisfied
- grep-checkpoint.sh full run exits zero (12 gates clean)
- Phase-end full-repo `pnpm typecheck` passes
- 25 + 1 atomic commits land on `v8.0/config-bucket`
- Force-with-lease push to fork/sync/upstream-v2.0.0 succeeds
- `.planning/phases/27-gamebryo-per-game-extensions/27-DONE-GATE.md` written
- ROADMAP.md + STATE.md updated
</verification>

<success_criteria>

- Phase 27 marked complete in ROADMAP and STATE
- All 4 phase requirement IDs (SYNC-05, SYNC-06, SYNC-17, SYNC-19) verifiable via done-gate
- BG3 4-class divine error preservation invariant + Morrowind migrate103 invariant + §1/§3/§10 + Phase 26 §6/§7a–d/140a57217 all green
- Branch on fork remote ready for Phase 28 (renderer + main spine) to build on

</success_criteria>

<output>
After completion, create `.planning/phases/27-gamebryo-per-game-extensions/27-08-SUMMARY.md`
</output>
