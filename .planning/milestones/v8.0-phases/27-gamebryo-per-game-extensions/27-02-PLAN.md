---
phase: 27-gamebryo-per-game-extensions
plan: 02
type: execute
wave: 3
depends_on: [27-01]
files_modified:
    - extensions/gamebryo-plugin-management/src/util/gameSupport.ts
    - extensions/gamebryo-plugin-management/src/util/PluginPersistor.ts
    - extensions/gamebryo-plugin-management/src/views/PluginList.tsx
    - extensions/gamebryo-plugin-management/src/index.ts
autonomous: true
requirements: [SYNC-05]

must_haves:
    truths:
        - "All 4 gamebryo-plugin-management conflict files resolved in leaf-first order: util/gameSupport.ts, util/PluginPersistor.ts, views/PluginList.tsx, index.ts"
        - "Four atomic commits on v8.0/config-bucket each titled `resolve(plugin-mgmt): <file> — <stance>` per D-27-00"
        - "After the fourth commit, `pnpm typecheck -F gamebryo-plugin-management` exits zero (D-27-04)"
        - "After EACH commit, `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero — §3 LOOT casing in autosort.ts (untouched in this plan but gated) stays clean, §10 cross-compiled native binaries stay on disk"
        - "Phase 27 file `extensions/gamebryo-plugin-management/src/autosort.ts` is NOT edited — it's the §3 gate target only (currently clean per CONTEXT.md 'Source files NOT edited but gated')"
    artifacts:
        - path: "extensions/gamebryo-plugin-management/src/util/gameSupport.ts"
          provides: "Plugin-mgmt game-support util — leaf 1/4"
          contains: "gameSupport"
        - path: "extensions/gamebryo-plugin-management/src/util/PluginPersistor.ts"
          provides: "LOOT plugin persistence util — leaf 2/4"
          contains: "PluginPersistor"
        - path: "extensions/gamebryo-plugin-management/src/views/PluginList.tsx"
          provides: "Plugin list React view — leaf 3/4"
          contains: "PluginList"
        - path: "extensions/gamebryo-plugin-management/src/index.ts"
          provides: "Extension barrel — last (re-exports settle after util/views)"
          contains: "init"
    key_links:
        - from: "util/gameSupport.ts + util/PluginPersistor.ts"
          to: "views/PluginList.tsx"
          via: "View imports util — utils settle first"
          pattern: "import.*util/"
        - from: "all three above"
          to: "index.ts"
          via: "Barrel re-exports settle last per Phase 26 D-26-01 idiom"
          pattern: "export"
---

<objective>
Resolve the 4 conflict files in `extensions/gamebryo-plugin-management/src/` per D-27-01 leaf-first sub-order: `util/gameSupport.ts` → `util/PluginPersistor.ts` → `views/PluginList.tsx` → `index.ts`. Four atomic commits matching `resolve(plugin-mgmt): <file> — <stance>`. Per-extension typecheck after the fourth commit. Run grep-checkpoint with `--skip-conflict-check` after each commit — §3 (autosort.ts LOOT casing, untouched in this plan) and §10 (native binaries) gates stay green throughout.

Purpose: Plugin-management is foundational — `collections` (plan 27-04) imports plugin-mgmt types indirectly per CONTEXT D-27-01. Settle this extension before collections opens.

Output: Four commits on `v8.0/config-bucket`. After plan 27-02 lands, 19 of 25 phase files remain.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md
@.planning/phases/26-mod-management-hot-zone/26-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve util/gameSupport.ts (leaf 1/4) and commit</name>
  <read_first>
    - extensions/gamebryo-plugin-management/src/util/gameSupport.ts (current state — every conflict region in full)
    - `git show f15bbabb8:extensions/gamebryo-plugin-management/src/util/gameSupport.ts` (base-commit reference)
    - .planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md (D-27-00 commit format; D-27-01 leaf-first)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md §1 lines 21–48 — `gamebryo-plugin-management/package.json` guard already removed (commit ba23aee71); CI pattern documented. THIS file is `src/util/gameSupport.ts`, not `package.json` — but read so executor knows the package.json is gated by §1 and must NOT be touched by this resolution
    - VORTEX-LINUX-MERGE-PLAYBOOK.md §3 lines 67–73 — `autosort.ts` is the §3 gate target; `util/gameSupport.ts` is NOT a §3 site, but read to confirm
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Open `extensions/gamebryo-plugin-management/src/util/gameSupport.ts` and hand-resolve every conflict region. Stance per CONTEXT D-27 Discretion: hand-resolve, fork-side wins for Linux fixes, upstream wins for new feature scaffolding that doesn't touch playbook items. This file is game-id → support-data tables — no playbook items live here directly.

    For each region: check history with `git log --oneline -5 -- extensions/gamebryo-plugin-management/src/util/gameSupport.ts`. If unsure of fork-side intent, `git show f15bbabb8:extensions/gamebryo-plugin-management/src/util/gameSupport.ts | diff -u - extensions/gamebryo-plugin-management/src/util/gameSupport.ts`.

    After resolution, run `grep -c '^<<<<<<< ' extensions/gamebryo-plugin-management/src/util/gameSupport.ts` — must equal 0. Stage only this file. Commit `resolve(plugin-mgmt): util/gameSupport.ts — <stance>`.

    Run `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check`. All gates pass.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/gamebryo-plugin-management/src/util/gameSupport.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(plugin-mgmt\): util/gameSupport\.ts —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/gamebryo-plugin-management/src/util/gameSupport.ts` returns empty
    - HEAD commit title starts with `resolve(plugin-mgmt): util/gameSupport.ts —`
    - HEAD commit touches exactly one file
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
  </acceptance_criteria>
  <done>
    Leaf 1/4 resolved; checkpoint clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolve util/PluginPersistor.ts (leaf 2/4) and commit</name>
  <read_first>
    - extensions/gamebryo-plugin-management/src/util/PluginPersistor.ts (current state, in full)
    - `git show f15bbabb8:extensions/gamebryo-plugin-management/src/util/PluginPersistor.ts` (base-commit reference)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md §3 — for awareness; PluginPersistor is the LOOT plugin metadata persistor. While §3 LOOT call sites live in `autosort.ts` (NOT this file), the persistor may also call LOOT — if you find a `loadPluginsAsync`/`getPluginMetadataAsync`/`getPluginAsync`/`sortPluginsAsync` call inside a conflict region in PluginPersistor.ts, it MUST use `path.basename(pluginList[id].filePath)` per §3, not `pluginName.toLowerCase()`. Confirm the resolved version of any such region.
    - .planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md (Discretion + D-27-01 sub-order)
  </read_first>
  <action>
    Open `extensions/gamebryo-plugin-management/src/util/PluginPersistor.ts` and hand-resolve every conflict region. Same stance defaults.

    Special vigilance: if any conflict region contains a LOOT call (regex `(loadPluginsAsync|getPluginMetadataAsync|getPluginAsync|sortPluginsAsync)`), confirm the resolved version uses `path.basename(...)` form, NOT `pluginName.toLowerCase()`. Record in commit body which side of each such region won and why.

    After resolution: zero conflict markers. Stage only this file. Commit `resolve(plugin-mgmt): util/PluginPersistor.ts — <stance>`.

    Run grep-checkpoint with `--skip-conflict-check`. The §3 gate (autosort.ts) MUST stay clean — it's the canonical site, but if PluginPersistor's resolution leaked a `pluginName.toLowerCase` into the file via cut-and-paste, the gate's negative grep would still target only autosort.ts. Sanity-check this file separately: `git grep -E 'pluginName\.toLowerCase' extensions/gamebryo-plugin-management/src/util/PluginPersistor.ts` should produce zero output (no LOOT-arg lowercasing).

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/gamebryo-plugin-management/src/util/PluginPersistor.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(plugin-mgmt\): util/PluginPersistor\.ts —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check &amp;&amp; ! git grep -qE 'pluginName\.toLowerCase' extensions/gamebryo-plugin-management/src/util/PluginPersistor.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/gamebryo-plugin-management/src/util/PluginPersistor.ts` returns empty
    - HEAD commit title starts with `resolve(plugin-mgmt): util/PluginPersistor.ts —`
    - `git grep -E 'pluginName\.toLowerCase' extensions/gamebryo-plugin-management/src/util/PluginPersistor.ts` returns empty (no LOOT-arg lowercasing introduced)
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero (§3 gate target autosort.ts unaffected)
  </acceptance_criteria>
  <done>
    Leaf 2/4 resolved; §3 LOOT-casing discipline preserved at this site.
  </done>
</task>

<task type="auto">
  <name>Task 3: Resolve views/PluginList.tsx (leaf 3/4) and commit</name>
  <read_first>
    - extensions/gamebryo-plugin-management/src/views/PluginList.tsx (current state, in full)
    - `git show f15bbabb8:extensions/gamebryo-plugin-management/src/views/PluginList.tsx` (base-commit reference)
    - extensions/gamebryo-plugin-management/src/util/gameSupport.ts (just-committed shape — view imports may need to align)
    - extensions/gamebryo-plugin-management/src/util/PluginPersistor.ts (just-committed shape — view imports may need to align)
    - .planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md (Discretion)
  </read_first>
  <action>
    Open `extensions/gamebryo-plugin-management/src/views/PluginList.tsx` and hand-resolve every conflict region. Pure React view; stance defaults apply. If the conflict touches imports from `util/gameSupport` or `util/PluginPersistor`, align with the just-committed shapes from tasks 1–2.

    After resolution: zero conflict markers. Stage only this file. Commit `resolve(plugin-mgmt): views/PluginList.tsx — <stance>`.

    Run grep-checkpoint with `--skip-conflict-check`. All gates pass.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/gamebryo-plugin-management/src/views/PluginList.tsx; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(plugin-mgmt\): views/PluginList\.tsx —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/gamebryo-plugin-management/src/views/PluginList.tsx` returns empty
    - HEAD commit title starts with `resolve(plugin-mgmt): views/PluginList.tsx —`
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
  </acceptance_criteria>
  <done>
    Leaf 3/4 resolved.
  </done>
</task>

<task type="auto">
  <name>Task 4: Resolve index.ts (leaf 4/4), commit, run per-extension typecheck</name>
  <read_first>
    - extensions/gamebryo-plugin-management/src/index.ts (current state, in full — barrel + extension init)
    - `git show f15bbabb8:extensions/gamebryo-plugin-management/src/index.ts` (base-commit reference)
    - extensions/gamebryo-plugin-management/src/util/gameSupport.ts and util/PluginPersistor.ts and views/PluginList.tsx (just-committed final shapes)
    - .planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md (D-27-04 typecheck cadence; "Integration Points" — plugin-mgmt index re-exports plugin-mgmt internals; same idiom as Phase 26 D-26-01 for `index.ts` last)
  </read_first>
  <action>
    Open `extensions/gamebryo-plugin-management/src/index.ts` and hand-resolve every conflict region. Re-exports + extension `init()`. Stance defaults: fork-side wins for Linux-port imports (`@vortex/paths` etc.), upstream wins for new feature scaffolding registrations not touching playbook items.

    After resolution: zero conflict markers. Stage only this file. Commit `resolve(plugin-mgmt): index.ts — <stance>`.

    Run `pnpm typecheck -F gamebryo-plugin-management` per D-27-04 (bare package name; this extension has its own typecheck script per `extensions/gamebryo-plugin-management/package.json`). Exit zero required. If typecheck fails, amend the index.ts commit (`git commit --amend --no-edit` after staging the fix); single commit per D-27-00.

    Run grep-checkpoint with `--skip-conflict-check`. All eleven non-conflict gates pass — particularly §3 (autosort.ts untouched, 4 LOOT call sites still use `path.basename`) and §10 (native binaries on disk).

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/gamebryo-plugin-management/; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(plugin-mgmt\): index\.ts —' &amp;&amp; pnpm typecheck -F gamebryo-plugin-management &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/gamebryo-plugin-management/src/index.ts` returns empty
    - `git grep -l '^<<<<<<< ' extensions/gamebryo-plugin-management/` returns empty (entire extension clean)
    - HEAD commit title starts with `resolve(plugin-mgmt): index.ts —`
    - `pnpm typecheck -F gamebryo-plugin-management` exits zero
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
    - `git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\(plugin-mgmt\):'` returns 4
    - §3 gate: `git grep -cE 'path\.basename\(pluginList\[' extensions/gamebryo-plugin-management/src/autosort.ts` returns ≥ 4 (autosort.ts untouched, still has 4 LOOT call sites with correct basename arg)
  </acceptance_criteria>
  <done>
    All 4 plugin-mgmt files resolved; per-extension typecheck clean; §3 invariant preserved; 6/25 phase files done.
  </done>
</task>

</tasks>

<verification>
- All 4 files conflict-clean
- Four atomic commits matching `resolve(plugin-mgmt): <file> — <stance>`
- `pnpm typecheck -F gamebryo-plugin-management` passes
- `scripts/grep-checkpoint.sh --skip-conflict-check` exits zero (§3 + §10 gates green)
</verification>

<success_criteria>

- 6 of 25 Phase 27 files resolved (cumulative)
- gamebryo-plugin-management extension typechecks clean
- §3 LOOT casing preserved (autosort.ts untouched but verified)

</success_criteria>

<output>
After completion, create `.planning/phases/27-gamebryo-per-game-extensions/27-02-SUMMARY.md`
</output>
