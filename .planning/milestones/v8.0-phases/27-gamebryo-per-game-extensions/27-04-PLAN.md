---
phase: 27-gamebryo-per-game-extensions
plan: 04
type: execute
wave: 5
depends_on: [27-02, 27-03]
files_modified:
    - extensions/collections/src/util/gameSupport/gamebryo.tsx
    - extensions/collections/src/eventHandlers.ts
    - extensions/collections/src/views/CollectionPageEdit/Instructions.tsx
    - extensions/collections/src/views/InstallDialog/InstallStartDialog.tsx
    - extensions/collections/src/views/CollectionList/index.tsx
    - extensions/collections/src/index.ts
autonomous: true
requirements: [SYNC-17]

must_haves:
    truths:
        - "All 6 collections conflict files resolved in leaf-first order: util/gameSupport/gamebryo.tsx → eventHandlers.ts → views/CollectionPageEdit/Instructions.tsx → views/InstallDialog/InstallStartDialog.tsx → views/CollectionList/index.tsx → src/index.ts"
        - "Six atomic commits on v8.0/config-bucket each titled `resolve(collections): <file> — <stance>` per D-27-00"
        - "After the sixth commit, `pnpm typecheck -F collections` exits zero (D-27-04)"
        - "After EACH commit, `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero"
    artifacts:
        - path: "extensions/collections/src/util/gameSupport/gamebryo.tsx"
          provides: "Gamebryo support for collections — leaf 1/6 (depends on plugin-mgmt types from plan 27-02)"
          contains: "gamebryo"
        - path: "extensions/collections/src/eventHandlers.ts"
          provides: "Collection event handlers — leaf 2/6"
          contains: "eventHandlers"
        - path: "extensions/collections/src/views/CollectionPageEdit/Instructions.tsx"
          provides: "Collection edit instructions view — leaf 3/6"
          contains: "Instructions"
        - path: "extensions/collections/src/views/InstallDialog/InstallStartDialog.tsx"
          provides: "Install start dialog — leaf 4/6"
          contains: "InstallStartDialog"
        - path: "extensions/collections/src/views/CollectionList/index.tsx"
          provides: "Collection list view — leaf 5/6"
          contains: "CollectionList"
        - path: "extensions/collections/src/index.ts"
          provides: "Extension barrel — last (per CONTEXT 'Integration Points': re-exports settle after the other 5)"
          contains: "init"
    key_links:
        - from: "util/gameSupport/gamebryo.tsx"
          to: "extensions/gamebryo-plugin-management (resolved in plan 27-02)"
          via: "Indirectly via shared types per CONTEXT D-27-01"
          pattern: "import"
        - from: "all 5 above"
          to: "src/index.ts"
          via: "Barrel last per CONTEXT 'Integration Points'"
          pattern: "export"
---

<objective>
Resolve the 6 conflict files in `extensions/collections/src/` per D-27-01 leaf-first sub-order: `util/gameSupport/gamebryo.tsx` → `eventHandlers.ts` → `views/CollectionPageEdit/Instructions.tsx` → `views/InstallDialog/InstallStartDialog.tsx` → `views/CollectionList/index.tsx` → `src/index.ts`. Six atomic commits matching `resolve(collections): <file> — <stance>`. Per-extension typecheck after the sixth commit.

Purpose: Per CONTEXT D-27-01, collections imports from gamebryo-plugin-management indirectly via shared types — must run AFTER plan 27-02. The 6-file size makes this the heaviest extension by file count after BG3.

Output: Six commits on `v8.0/config-bucket`. After plan 27-04 lands, 10 of 25 phase files remain.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve util/gameSupport/gamebryo.tsx (leaf 1/6) and commit</name>
  <read_first>
    - extensions/collections/src/util/gameSupport/gamebryo.tsx (current state, in full)
    - `git show f15bbabb8:extensions/collections/src/util/gameSupport/gamebryo.tsx` (base-commit reference)
    - extensions/gamebryo-plugin-management/src/util/gameSupport.ts (resolved in plan 27-02 — types this file may consume)
    - .planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md (D-27-01 sub-order; "Integration Points" — collections-gamebryo bridge)
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Open `extensions/collections/src/util/gameSupport/gamebryo.tsx` and hand-resolve every conflict region. Stance defaults: fork-side wins for any Linux-port import, upstream wins for new collections feature scaffolding (collections is a heavily-iterated upstream feature). If a region touches plugin-mgmt-shared types, align with the shape committed in plan 27-02 task 1.

    After resolution: zero conflict markers. Stage only this file. Commit `resolve(collections): util/gameSupport/gamebryo.tsx — <stance>`.

    Run grep-checkpoint with `--skip-conflict-check`. All gates pass.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/collections/src/util/gameSupport/gamebryo.tsx; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(collections\): util/gameSupport/gamebryo\.tsx —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/collections/src/util/gameSupport/gamebryo.tsx` returns empty
    - HEAD commit title starts with `resolve(collections): util/gameSupport/gamebryo.tsx —`
    - HEAD commit touches exactly one file
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
  </acceptance_criteria>
  <done>
    Leaf 1/6 resolved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolve eventHandlers.ts (leaf 2/6) and commit</name>
  <read_first>
    - extensions/collections/src/eventHandlers.ts (current state, in full)
    - `git show f15bbabb8:extensions/collections/src/eventHandlers.ts` (base-commit reference)
    - .planning/phases/26-mod-management-hot-zone/26-CONTEXT.md (precedent — `mod_management/eventHandlers.ts` was resolved in plan 26-03; collections eventHandlers has the same idiom)
  </read_first>
  <action>
    Open `extensions/collections/src/eventHandlers.ts` and hand-resolve every conflict region. Stance defaults apply.

    After resolution: zero conflict markers. Stage only this file. Commit `resolve(collections): eventHandlers.ts — <stance>`.

    Run grep-checkpoint with `--skip-conflict-check`.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/collections/src/eventHandlers.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(collections\): eventHandlers\.ts —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/collections/src/eventHandlers.ts` returns empty
    - HEAD commit title starts with `resolve(collections): eventHandlers.ts —`
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
  </acceptance_criteria>
  <done>
    Leaf 2/6 resolved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Resolve views/CollectionPageEdit/Instructions.tsx (leaf 3/6) and commit</name>
  <read_first>
    - extensions/collections/src/views/CollectionPageEdit/Instructions.tsx (current state, in full)
    - `git show f15bbabb8:extensions/collections/src/views/CollectionPageEdit/Instructions.tsx` (base-commit reference)
  </read_first>
  <action>
    Open `extensions/collections/src/views/CollectionPageEdit/Instructions.tsx` and hand-resolve every conflict region. React view; stance defaults apply.

    After resolution: zero conflict markers. Stage only this file. Commit `resolve(collections): views/CollectionPageEdit/Instructions.tsx — <stance>`.

    Run grep-checkpoint with `--skip-conflict-check`.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/collections/src/views/CollectionPageEdit/Instructions.tsx; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(collections\): views/CollectionPageEdit/Instructions\.tsx —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/collections/src/views/CollectionPageEdit/Instructions.tsx` returns empty
    - HEAD commit title starts with `resolve(collections): views/CollectionPageEdit/Instructions.tsx —`
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
  </acceptance_criteria>
  <done>
    Leaf 3/6 resolved.
  </done>
</task>

<task type="auto">
  <name>Task 4: Resolve views/InstallDialog/InstallStartDialog.tsx (leaf 4/6) and commit</name>
  <read_first>
    - extensions/collections/src/views/InstallDialog/InstallStartDialog.tsx (current state, in full)
    - `git show f15bbabb8:extensions/collections/src/views/InstallDialog/InstallStartDialog.tsx` (base-commit reference)
  </read_first>
  <action>
    Open `extensions/collections/src/views/InstallDialog/InstallStartDialog.tsx` and hand-resolve every conflict region. React dialog view; stance defaults apply.

    After resolution: zero conflict markers. Stage only this file. Commit `resolve(collections): views/InstallDialog/InstallStartDialog.tsx — <stance>`.

    Run grep-checkpoint with `--skip-conflict-check`.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/collections/src/views/InstallDialog/InstallStartDialog.tsx; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(collections\): views/InstallDialog/InstallStartDialog\.tsx —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/collections/src/views/InstallDialog/InstallStartDialog.tsx` returns empty
    - HEAD commit title starts with `resolve(collections): views/InstallDialog/InstallStartDialog.tsx —`
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
  </acceptance_criteria>
  <done>
    Leaf 4/6 resolved.
  </done>
</task>

<task type="auto">
  <name>Task 5: Resolve views/CollectionList/index.tsx (leaf 5/6) and commit</name>
  <read_first>
    - extensions/collections/src/views/CollectionList/index.tsx (current state, in full)
    - `git show f15bbabb8:extensions/collections/src/views/CollectionList/index.tsx` (base-commit reference)
    - extensions/collections/src/eventHandlers.ts (just-committed shape — view may import handlers)
  </read_first>
  <action>
    Open `extensions/collections/src/views/CollectionList/index.tsx` and hand-resolve every conflict region. NOTE: this is a view-internal `index.tsx` (the CollectionList directory's barrel) — distinct from the extension barrel `extensions/collections/src/index.ts` resolved in task 6. Stance defaults apply.

    After resolution: zero conflict markers. Stage only this file. Commit `resolve(collections): views/CollectionList/index.tsx — <stance>`.

    Run grep-checkpoint with `--skip-conflict-check`.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/collections/src/views/CollectionList/index.tsx; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(collections\): views/CollectionList/index\.tsx —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/collections/src/views/CollectionList/index.tsx` returns empty
    - HEAD commit title starts with `resolve(collections): views/CollectionList/index.tsx —`
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
  </acceptance_criteria>
  <done>
    Leaf 5/6 resolved.
  </done>
</task>

<task type="auto">
  <name>Task 6: Resolve src/index.ts (leaf 6/6), commit, run per-extension typecheck</name>
  <read_first>
    - extensions/collections/src/index.ts (current state, in full)
    - `git show f15bbabb8:extensions/collections/src/index.ts` (base-commit reference)
    - All 5 just-committed sibling files (re-exports must align)
    - .planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md ("Integration Points": collections/src/index.ts re-exports depend on the other 5 files' final symbol shapes; D-27-04 typecheck)
  </read_first>
  <action>
    Open `extensions/collections/src/index.ts` and hand-resolve every conflict region. Extension barrel + `init()` registration. Stance defaults: fork-side wins for any Linux-port import, upstream wins for new feature scaffolding registrations.

    After resolution: zero conflict markers. Stage only this file. Commit `resolve(collections): index.ts — <stance>`.

    Run `pnpm typecheck -F collections` per D-27-04 (bare name; collections has its own typecheck script). If failure, amend the index.ts commit before checkpoint.

    Run grep-checkpoint with `--skip-conflict-check`. All gates pass.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/collections/; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(collections\): index\.ts —' &amp;&amp; pnpm typecheck -F collections &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/collections/src/index.ts` returns empty
    - `git grep -l '^<<<<<<< ' extensions/collections/` returns empty (entire extension clean)
    - HEAD commit title starts with `resolve(collections): index.ts —`
    - `pnpm typecheck -F collections` exits zero
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
    - `git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\(collections\):'` returns 6
  </acceptance_criteria>
  <done>
    All 6 collections files resolved; per-extension typecheck clean; 15/25 phase files done.
  </done>
</task>

</tasks>

<verification>
- All 6 files conflict-clean
- Six atomic commits matching `resolve(collections): <file> — <stance>`
- `pnpm typecheck -F collections` passes
- `scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
</verification>

<success_criteria>

- 15 of 25 Phase 27 files resolved (cumulative)
- collections extension typechecks clean

</success_criteria>

<output>
After completion, create `.planning/phases/27-gamebryo-per-game-extensions/27-04-SUMMARY.md`
</output>
