---
phase: 27-gamebryo-per-game-extensions
plan: 06
type: execute
wave: 6
depends_on: [27-04]
files_modified:
    - extensions/games/game-morrowind/src/migrations.js
autonomous: true
requirements: [SYNC-19]

must_haves:
    truths:
        - "extensions/games/game-morrowind/src/migrations.js conflict-clean — single file in this extension"
        - "One atomic commit on v8.0/config-bucket titled `resolve(morrowind): migrations.js — <stance>` per D-27-00"
        - "After the commit, gate 11 of grep-checkpoint exits clean: `git grep -n 'morrowind migrate103: mod directory missing' extensions/games/game-morrowind/src/migrations.js | wc -l` returns ≥ 1 — the warning string survived per D-27-02"
        - "Morrowind has no `typecheck` script in package.json — executor runs `pnpm exec tsc --noEmit --allowJs --checkJs=false` against the file (or accepts build-as-typecheck) — see action"
        - "After the commit, `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero"
    artifacts:
        - path: "extensions/games/game-morrowind/src/migrations.js"
          provides: "Morrowind migration runner — hosts the migrate103 try/catch warning"
          contains: "migrate103"
    key_links:
        - from: "migrations.js"
          to: "Phase-end done-gate"
          via: "Gate 11 surfaces any silent loss of the migrate103 warning"
          pattern: "morrowind migrate103: mod directory missing"
---

<objective>
Resolve `extensions/games/game-morrowind/src/migrations.js` (single conflict file in this extension). One atomic commit matching `resolve(morrowind): migrations.js — <stance>`. Per-extension typecheck (with caveat: this is a `.js` file, no typecheck script — see action).

**Critical preservation:** The `migrate103` try/catch logging the warning `'morrowind migrate103: mod directory missing'` MUST survive per D-27-02. Gate 11 of the extended grep-checkpoint script enforces this with the prefix-anchored substring match (≥ 1 hit).

Purpose: Independent of plans 27-05 and 27-07 — wave 6 runs all three game extensions in parallel.

Output: One commit on `v8.0/config-bucket`. After plan 27-06 lands, 2 of 25 phase files remain (Witcher3).
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve migrations.js — PRESERVATION-CRITICAL — commit, run typecheck</name>
  <read_first>
    - extensions/games/game-morrowind/src/migrations.js (current state, in full — read every conflict region carefully)
    - `git show f15bbabb8:extensions/games/game-morrowind/src/migrations.js` (base-commit reference; confirm the migrate103 function body has a try/catch logging `'morrowind migrate103: mod directory missing'` — both required to survive per D-27-02)
    - .planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md D-27-02 (Morrowind gate spec — exact substring `morrowind migrate103: mod directory missing`, count ≥ 1)
    - .planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md "Specific Ideas" — count-threshold gate idiom matches Phase 26 D-26-03 defensiveness
    - extensions/games/game-morrowind/package.json (confirm: NO `typecheck` script; this is a `.js` file, not `.ts`)
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Open `extensions/games/game-morrowind/src/migrations.js`. For each conflict region:

    1. Identify whether the region touches the `migrate103` function or its body.
    2. If yes — fork-side wins by default. Confirm both the try/catch structure AND the warning string `'morrowind migrate103: mod directory missing'` survive. The string is the canonical Linux-port marker and is gate 11's prefix-anchored match.
    3. If no (region is unrelated — other migration steps, helper functions): default stance (fork-side for Linux fixes, upstream for new feature scaffolding).

    After all markers resolved, run gate 11's grep BEFORE staging:
    ```
    git grep -n 'morrowind migrate103: mod directory missing' extensions/games/game-morrowind/src/migrations.js | wc -l
    ```
    Must return ≥ 1. If 0, STOP — the resolution ate the warning string. Re-open, re-resolve, re-check.

    Stage only this file. Commit `resolve(morrowind): migrations.js — <stance> (preserves migrate103 warning per D-27-02)`. Body: list resolved regions + which side won + explicit confirmation that the migrate103 warning string survives.

    **Per-extension typecheck (D-27-04 deviation):** game-morrowind has no `typecheck` script and the file is JavaScript (`.js`). Acceptable executor options:

    1. **Build-as-typecheck (preferred for `.js`):** `pnpm -F game-morrowind build` — rolldown bundler will refuse to bundle on syntax / resolution errors. This catches the most likely Phase 27 regression (mid-merge string corruption breaking JS syntax).
    2. **Lightweight tsc --allowJs:** `pnpm exec tsc --noEmit --allowJs --checkJs=false --target es2020 --module commonjs extensions/games/game-morrowind/src/migrations.js` — syntax check only.

    Either is acceptable. Confirm exit zero. If failure, amend the commit (`git commit --amend --no-edit` after staging the fix) before checkpoint.

    Run `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check`. **Gate 11 (Morrowind migrate103) MUST pass here.**

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' extensions/games/game-morrowind/src/migrations.js; test $? -ne 0 &amp;&amp; test $(git grep -n 'morrowind migrate103: mod directory missing' extensions/games/game-morrowind/src/migrations.js | wc -l) -ge 1 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(morrowind\): migrations\.js —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' extensions/games/game-morrowind/src/migrations.js` returns empty
    - **Gate 11 — `git grep -n 'morrowind migrate103: mod directory missing' extensions/games/game-morrowind/src/migrations.js | wc -l` returns ≥ 1** (warning string preserved)
    - HEAD commit title starts with `resolve(morrowind): migrations.js —`
    - HEAD commit body explicitly mentions D-27-02 / migrate103 warning preserved
    - HEAD commit touches exactly one file
    - Per-extension typecheck (build-as-typecheck OR `tsc --noEmit --allowJs`) exits zero
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero (gate 11 specifically passes)
    - `git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\(morrowind\):'` returns 1
  </acceptance_criteria>
  <done>
    Morrowind file resolved with migrate103 warning intact; gate 11 flips to clean; 23/25 phase files done.
  </done>
</task>

</tasks>

<verification>
- migrations.js conflict-clean
- One atomic commit matching `resolve(morrowind): migrations.js — <stance>`
- Gate 11 (Morrowind migrate103) passes
- Per-extension typecheck passes
- `scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
</verification>

<success_criteria>

- 23 of 25 Phase 27 files resolved (cumulative)
- Morrowind migrate103 warning preservation invariant intact

</success_criteria>

<output>
After completion, create `.planning/phases/27-gamebryo-per-game-extensions/27-06-SUMMARY.md`
</output>
