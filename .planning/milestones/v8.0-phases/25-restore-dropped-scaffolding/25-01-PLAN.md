---
phase: 25-restore-dropped-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [SYNC-11, SYNC-12, SYNC-13, SYNC-14, SYNC-15, SYNC-16]

must_haves:
  truths:
    - "On branch v8.0/config-bucket; SHA pin 8b5a9f675 verified against git rev-parse 138da2249^2"
    - "Discovery diff produced canonical list of files added upstream but missing locally, with deny-list filtered out"
    - "User has reviewed any 'surprise' files (not in ROADMAP restore set, not in deny-list) and recorded accept/reject for each"
  artifacts:
    - path: ".planning/phases/25-restore-dropped-scaffolding/25-DISCOVERY-RESULT.md"
      provides: "Discovery output table, surprise classification, executor read-result for D-25-10 (node-ba2tk grep)"
      contains: "## Discovery Diff Result"
  key_links:
    - from: "25-DISCOVERY-RESULT.md"
      to: "Plans 02–04"
      via: "Confirmed restore list and any user-confirmed surprise additions become input to commits 1–5"
      pattern: "Restore set"
---

<objective>
Run the Phase 25 discovery diff against upstream parent SHA `8b5a9f675` and confirm the canonical list of files to restore. Verify the SHA pin is still anchored to the same upstream parent (defends against history rewrite). Surface any "surprise" files (not in ROADMAP restore set, not in deny-list) to the user via a checkpoint. Pre-resolve D-25-10's open executor question (does the fork already vendor a custom `node-ba2tk`?). No commits land in this plan — it produces a written discovery result that Plans 02–04 consume.

Purpose: Reproducibility. Every restoration commit downstream cites this discovery output. If `138da2249` ever changes (history rewrite) or if upstream parent has additional new files this milestone, this plan catches it before any commit lands.

Output: `.planning/phases/25-restore-dropped-scaffolding/25-DISCOVERY-RESULT.md` recording the discovery list, surprise classification, ba2tk catalog source decision, and user approval to proceed.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-restore-dropped-scaffolding/25-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify branch + SHA pin, run discovery diff, grep for fork-vendored node-ba2tk</name>
  <read_first>
    - .planning/phases/25-restore-dropped-scaffolding/25-CONTEXT.md (D-25-01, D-25-02, D-25-10, and the `<specifics>` "Discovery diff command" + "Upstream parent SHA verification" blocks)
    - CLAUDE.md (project — confirms `node-ba2tk` is listed as a custom-fork dependency)
    - .planning/phases/24-config-bucket/24-CONTEXT.md (Phase 24 D-02: branch is `v8.0/config-bucket`, push target is `fork/sync/upstream-v2.0.0`)
  </read_first>
  <action>
    Confirm working tree is on branch `v8.0/config-bucket` (`git rev-parse --abbrev-ref HEAD`). Run the SHA-pin verification per CONTEXT.md `<specifics>` "Upstream parent SHA verification" block — fail loudly with the exact error string `Upstream parent drift!` if `git rev-parse 138da2249^2` does not start with `8b5a9f675`. Run the discovery diff command per CONTEXT.md `<specifics>` "Discovery diff command" block (the `git diff --name-status v8.0/config-bucket 8b5a9f675 -- ':!src/renderer/src/__mocks__' ':!src/renderer/src/__tests__' ':!src/renderer/jest.config.mjs' | grep '^A'` invocation). Capture full output. Classify each line into three buckets: (a) "expected" — listed in CONTEXT.md `<domain>` "Files restored this phase"; (b) "deny-list" — should not appear because of the `:!` filters, but log if any do; (c) "surprise" — anything else.

    Pre-resolve D-25-10's open executor question: run `git grep -nE 'node-ba2tk|atabisz/node-ba2tk' -- '**/package.json' 'pnpm-workspace.yaml' 'pnpm-lock.yaml'` and record whether the fork already vendors a custom `node-ba2tk`. If hits surface, the Plan 03 catalog entry will point at the fork's git URL; if no hits, the Plan 03 catalog entry will pin upstream's npm `ba2tk` version (read from upstream's `pnpm-lock.yaml` at `8b5a9f675` per D-25-10).

    Write `.planning/phases/25-restore-dropped-scaffolding/25-DISCOVERY-RESULT.md` with sections: `## SHA Pin Verification` (echo of expected/actual), `## Discovery Diff (raw)` (raw diff output in a fenced block), `## Classification` (table of file → expected|deny-list|surprise), `## node-ba2tk Grep Result` (grep output + classification: fork-vendored or upstream-npm), `## ba2tk Version Source` (either fork git URL OR upstream npm version read via `git show 8b5a9f675:pnpm-lock.yaml | grep -A2 -B1 'ba2tk'`).

    Do not commit anything in this task. Do not modify the working tree (the discovery result file lives in `.planning/`, which is fork-only per CLAUDE.md branch strategy and is fine to commit, but defer that to Task 2's checkpoint approval).
  </action>
  <verify>
    <automated>test -f .planning/phases/25-restore-dropped-scaffolding/25-DISCOVERY-RESULT.md && grep -q '^## SHA Pin Verification$' .planning/phases/25-restore-dropped-scaffolding/25-DISCOVERY-RESULT.md && grep -q '^## Classification$' .planning/phases/25-restore-dropped-scaffolding/25-DISCOVERY-RESULT.md && grep -q '^## node-ba2tk Grep Result$' .planning/phases/25-restore-dropped-scaffolding/25-DISCOVERY-RESULT.md && grep -q '^## ba2tk Version Source$' .planning/phases/25-restore-dropped-scaffolding/25-DISCOVERY-RESULT.md</automated>
  </verify>
  <acceptance_criteria>
    - Branch confirmed as `v8.0/config-bucket` (output of `git rev-parse --abbrev-ref HEAD` matches exactly)
    - SHA pin verification passes silently OR fails with the exact string `Upstream parent drift!` and stops
    - Discovery diff output captured verbatim into `25-DISCOVERY-RESULT.md` `## Discovery Diff (raw)` section
    - Every file in the discovery output is classified into exactly one of: expected, deny-list (anomaly), surprise
    - `node-ba2tk` grep result recorded with explicit verdict: "fork-vendored at <URL>" OR "no fork vendor; use upstream npm ba2tk@<version>"
    - `ba2tk` version source line cites either the fork git URL OR the exact version read from upstream's lockfile at SHA `8b5a9f675`
    - No working-tree changes outside `.planning/phases/25-restore-dropped-scaffolding/` (verify with `git status -s -- ':!.planning'` — must be empty)
  </acceptance_criteria>
  <done>
    `25-DISCOVERY-RESULT.md` exists with all five required sections; SHA pin verified; ba2tk version source decided; classification table complete.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify discovery output and surprise classification</name>
  <what-built>
    Discovery diff captured at `.planning/phases/25-restore-dropped-scaffolding/25-DISCOVERY-RESULT.md`. Lists every file present at upstream parent `8b5a9f675` but missing on `v8.0/config-bucket`, with the Jest deny-list filtered out. Classifies each entry as `expected` (in ROADMAP restore set) or `surprise` (not in ROADMAP and not in deny-list). Also records the `node-ba2tk` grep result and the `ba2tk` catalog version source for Plan 03.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/25-restore-dropped-scaffolding/25-DISCOVERY-RESULT.md`.
    2. Read `## SHA Pin Verification` — confirm it shows expected `8b5a9f675` and actual matches.
    3. Read `## Classification` table. For every row classified `surprise`, decide accept (restore in Plan 04 commit 4 or 5 alongside the workflows/docs) or reject (note in Plan 04 commit 4 body alongside the Jest deny-list per D-25-14).
    4. Read `## ba2tk Version Source` — confirm either the fork git URL OR the upstream npm version + Plan 03 will use that source for the catalog entry (D-25-10).
    5. If anything in the discovery looks wrong (unexpected deny-list leakage, missing expected files like `packages/paths/`, surprises that need broader discussion), reject and re-scope; otherwise approve.

    Append a final `## User Decisions on Surprises` section to `25-DISCOVERY-RESULT.md` capturing accept/reject per surprise file before resuming.
  </how-to-verify>
  <resume-signal>
    Type "approved" once `## User Decisions on Surprises` is appended; or describe issues to re-run discovery.
  </resume-signal>
  <acceptance_criteria>
    - `25-DISCOVERY-RESULT.md` contains a `## User Decisions on Surprises` section with explicit accept/reject per surprise (or "no surprises" if the classification produced none)
    - User has resumed with "approved"
  </acceptance_criteria>
  <done>
    Surprise list resolved; Plans 02–04 have a frozen, user-approved restore set to draw from.
  </done>
</task>

</tasks>

<verification>
- Branch is `v8.0/config-bucket`
- SHA `8b5a9f675` is the verified second parent of `138da2249`
- Discovery output is byte-for-byte the result of the CONTEXT.md command (no editorialising)
- ba2tk version source is decided before Plan 03 begins
</verification>

<success_criteria>
Discovery result file exists, all surprise files have an accept/reject decision, ba2tk catalog source is fixed, user has approved.
</success_criteria>

<output>
After completion, create `.planning/phases/25-restore-dropped-scaffolding/25-01-SUMMARY.md`. The summary must cite the SHA verification result, classification counts (expected / surprise-accepted / surprise-rejected), and the chosen ba2tk catalog source.
</output>
