---
phase: 25-restore-dropped-scaffolding
plan: 04
type: execute
wave: 4
depends_on: [25-03]
files_modified:
  - src/main/src/downloading/chunking.ts
  - src/main/src/downloading/chunking.test.ts
  - src/main/src/downloading/downloader.test.ts
  - extensions/collections/__tests__/bsdiff-node.test.ts
  - .github/workflows/package.yml
  - .github/workflows/signing-test.yml
  - .github/workflows/update-api-tag.yml
  - .github/workflows/review-extension-issue-created.yml
  - docs/flatpak-maintenance.md
  - docs/flatpak-technical.md
  - AGENTS-DEBUGGING.md
  - VORTEX-LINUX-MERGE-PLAYBOOK.md
autonomous: true
requirements: [SYNC-14, SYNC-15, SYNC-16]

must_haves:
  truths:
    - "src/main/src/downloading/{chunking.ts, chunking.test.ts, downloader.test.ts} restored byte-for-byte from 8b5a9f675"
    - "Four missing CI workflows restored byte-for-byte from 8b5a9f675"
    - "docs/flatpak-{maintenance,technical}.md and AGENTS-DEBUGGING.md restored byte-for-byte"
    - "VORTEX-LINUX-MERGE-PLAYBOOK.md gains §11 'Deliberate test-runner divergences' with the exact body from D-25-12 + a new commit-index row per D-25-13"
    - "Three atomic signed commits land in order (commits 3, 4, 5 per D-25-04); commit 4 body enumerates the deny-list per D-25-14"
    - "Phase 25 done-gate (D-25-15 all five conditions) passes after commit 5"
    - "fork/sync/upstream-v2.0.0 force-with-lease push lands successfully (D-25-07)"
  artifacts:
    - path: "src/main/src/downloading/chunking.ts"
      provides: "Restored DownloadManager chunking primitive required by upstream v2.0.0"
      min_lines: 1
    - path: "VORTEX-LINUX-MERGE-PLAYBOOK.md"
      provides: "Now includes §11 deliberate test-runner divergence policy"
      contains: "## §11 Deliberate test-runner divergences"
    - path: ".github/workflows/package.yml"
      provides: "Restored upstream packaging workflow"
      min_lines: 1
    - path: "docs/flatpak-maintenance.md"
      provides: "Restored upstream flatpak doc"
      min_lines: 1
  key_links:
    - from: "VORTEX-LINUX-MERGE-PLAYBOOK.md §11"
      to: "Phase 25 commit 4"
      via: "Commit-index row added per D-25-13 traces from playbook to commit body deny-list per D-25-14"
      pattern: "§11"
    - from: "Phase 25 final commit (commit 5)"
      to: "fork/sync/upstream-v2.0.0"
      via: "force-with-lease push per D-25-07"
      pattern: "force-with-lease"
---

<objective>
Land Phase 25 commits 3, 4, and 5 per D-25-04 — chunking files, missing CI workflows (with deny-list provenance in the body), docs + playbook §11 — then run the full Phase 25 done-gate per D-25-15 and force-with-lease push to `fork/sync/upstream-v2.0.0` per D-25-07. Three atomic signed commits in order. No package.json or workspace changes; no `pnpm install` needed between these commits (per D-25-06, install is required only after commits 1, 2, and 5).

Purpose:
- Commit 3 (SYNC-14) — restore chunking primitives the upstream DownloadManager rewrite depends on; D-25-11 says restore as-is, defer Linux fixes to Phase 26/28.
- Commit 4 (SYNC-16, partially SYNC-15) — restore four missing GitHub Actions workflows; commit body is the durable provenance record for the Jest deny-list per D-25-14 (paired with playbook §11 from commit 5 per D-25-13's two-source provenance).
- Commit 5 (SYNC-15, SYNC-16) — restore docs (flatpak, AGENTS-DEBUGGING) and ADD playbook §11 verbatim per D-25-12, plus the commit-index row per D-25-13. This is the only commit that is not a pure file restoration — it edits an existing fork-managed playbook.

After commit 5: run the five D-25-15 done-gate checks, then force-with-lease push to `fork/sync/upstream-v2.0.0` (the daily `rebase-upstream.yml` cron may have raced; `--force-with-lease` will refuse safely if so).

Output: three signed commits on `v8.0/config-bucket`, all five done-gate conditions pass, branch pushed to fork.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-restore-dropped-scaffolding/25-CONTEXT.md
@.planning/phases/25-restore-dropped-scaffolding/25-DISCOVERY-RESULT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit 3 — restore chunking + collections bsdiff-node test (D-25-04 commit 3)</name>
  <read_first>
    - .planning/phases/25-restore-dropped-scaffolding/25-CONTEXT.md (D-25-04 commit 3, D-25-11 restore-as-is policy, D-25-15 condition 1)
    - .planning/phases/25-restore-dropped-scaffolding/25-DISCOVERY-RESULT.md (confirms chunking files + bsdiff-node test are in expected restore set)
  </read_first>
  <action>
    Run `git checkout 8b5a9f675 -- src/main/src/downloading/chunking.ts src/main/src/downloading/chunking.test.ts src/main/src/downloading/downloader.test.ts extensions/collections/__tests__/bsdiff-node.test.ts`.

    Per D-25-11, do NOT modify these files post-restore. If `chunking.ts` or `downloader.test.ts` references Linux-incompatible patterns (backslash literals, `process.platform === 'win32'` without guards), Phase 26/28 surface and fix. Phase 25 stays a pure restore.

    Verify byte-for-byte match: `git diff v8.0/config-bucket 8b5a9f675 -- src/main/src/downloading/chunking.ts src/main/src/downloading/chunking.test.ts src/main/src/downloading/downloader.test.ts extensions/collections/__tests__/bsdiff-node.test.ts` produces empty output (D-25-15 condition 1).

    Stage and commit. Title: `restore(downloading): chunking + downloader tests for v2.0.0 DownloadManager` (D-25-04 commit 3 / D-25-05 format). Body cites SHA `8b5a9f675`, the restoration command, and a one-liner that any Linux-specific concerns inside these files are deferred to Phase 26/28 per D-25-11. Include the collections bsdiff-node test in the same commit (it's small, sibling-test-restoration of the same shape, and listed as a Vitest-compatible name in CONTEXT.md `<domain>`). Sign with SSH.
  </action>
  <verify>
    <automated>git diff HEAD~1 HEAD --name-only | sort | tr '\n' ',' | grep -q 'chunking\.test\.ts.*chunking\.ts.*downloader\.test\.ts' && git diff v8.0/config-bucket 8b5a9f675 -- src/main/src/downloading/chunking.ts src/main/src/downloading/chunking.test.ts src/main/src/downloading/downloader.test.ts extensions/collections/__tests__/bsdiff-node.test.ts | wc -l | grep -qx '0' && git log -1 --pretty=%s | grep -qx 'restore(downloading): chunking + downloader tests for v2.0.0 DownloadManager'</automated>
  </verify>
  <acceptance_criteria>
    - HEAD commit title exactly `restore(downloading): chunking + downloader tests for v2.0.0 DownloadManager`
    - Byte-for-byte match against SHA `8b5a9f675` for the four files (D-25-15 condition 1)
    - HEAD commit modifies only those four paths
    - HEAD commit is signed
    - HEAD commit body cites SHA `8b5a9f675` and the D-25-11 deferral note
  </acceptance_criteria>
  <done>
    Commit 3 of 5 landed; chunking primitives present; SYNC-14 satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit 4 — restore four missing CI workflows with deny-list provenance in body (D-25-04 commit 4 / D-25-14)</name>
  <read_first>
    - .planning/phases/25-restore-dropped-scaffolding/25-CONTEXT.md (D-25-04 commit 4, D-25-14 commit-body provenance, D-25-03 deny-list, D-25-15 condition 5)
    - .planning/phases/25-restore-dropped-scaffolding/25-DISCOVERY-RESULT.md `## User Decisions on Surprises` (any surprise files the user said keep dropped must be enumerated in this commit body alongside the Jest deny-list)
  </read_first>
  <action>
    Run `git checkout 8b5a9f675 -- .github/workflows/package.yml .github/workflows/signing-test.yml .github/workflows/update-api-tag.yml .github/workflows/review-extension-issue-created.yml`.

    Verify byte-for-byte match for these four files (D-25-15 condition 1).

    Stage and commit. Title: `restore(ci): missing upstream workflows` (D-25-04 commit 4 / D-25-05 format). Body MUST per D-25-14 enumerate every file the executor did NOT restore from upstream parent and why:
    - The pre-classified deny-list per D-25-03: `src/renderer/jest.config.mjs`, `src/renderer/src/__mocks__/`, `src/renderer/src/__tests__/` — reason: "fork is Vitest-only by deliberate v3.0/v4.0 decision; see VORTEX-LINUX-MERGE-PLAYBOOK.md §11 (added in commit 5 of this phase)"
    - Any surprise files the user marked reject in `25-DISCOVERY-RESULT.md` `## User Decisions on Surprises`
    Body also cites SHA `8b5a9f675` and the restoration command. Sign with SSH.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -qx 'restore(ci): missing upstream workflows' && git diff v8.0/config-bucket 8b5a9f675 -- .github/workflows/package.yml .github/workflows/signing-test.yml .github/workflows/update-api-tag.yml .github/workflows/review-extension-issue-created.yml | wc -l | grep -qx '0' && git log -1 --pretty=%B | grep -q 'jest\.config\.mjs' && git log -1 --pretty=%B | grep -q '__mocks__' && git log -1 --pretty=%B | grep -q '__tests__' && git log -1 --pretty=%B | grep -q '8b5a9f675'</automated>
  </verify>
  <acceptance_criteria>
    - HEAD commit title exactly `restore(ci): missing upstream workflows`
    - Byte-for-byte match against `8b5a9f675` for the four workflow files
    - HEAD commit body literally contains the strings `jest.config.mjs`, `__mocks__`, `__tests__`, and `8b5a9f675`
    - HEAD commit body lists every surprise file marked reject in `25-DISCOVERY-RESULT.md` (or "no rejected surprises" if none)
    - HEAD commit modifies only the four `.github/workflows/*.yml` paths
    - HEAD commit is signed
  </acceptance_criteria>
  <done>
    Commit 4 of 5 landed; CI workflows restored; durable per-phase provenance for the Jest deny-list lives in the commit body.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit 5 — restore docs + add playbook §11 + commit-index row + done-gate + force-with-lease push (D-25-04 commit 5 / D-25-12 / D-25-13 / D-25-15 / D-25-07)</name>
  <read_first>
    - .planning/phases/25-restore-dropped-scaffolding/25-CONTEXT.md (D-25-04 commit 5, D-25-12 §11 body, D-25-13 commit-index row, D-25-15 done-gate, D-25-07 push pattern, and `<specifics>` "Playbook §11 entry shape" block)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md (current end-of-file location for appending §11; existing commit-index table location for D-25-13 row)
    - .planning/phases/24-config-bucket/24-CONTEXT.md (D-02 push target `fork/sync/upstream-v2.0.0` and force-with-lease pattern)
  </read_first>
  <action>
    Step 1 — Restore docs: `git checkout 8b5a9f675 -- docs/flatpak-maintenance.md docs/flatpak-technical.md AGENTS-DEBUGGING.md`. Verify byte-for-byte match (D-25-15 condition 1).

    Step 2 — Add playbook §11: Edit `VORTEX-LINUX-MERGE-PLAYBOOK.md`. Append the §11 section using the EXACT body from CONTEXT.md `<specifics>` "Playbook §11 entry shape" block — copy verbatim, including the heading `## §11 Deliberate test-runner divergences`, the file list, the verification bash block, the rationale, and the `Decided: Phase 25 (restore-dropped-scaffolding), 2026-05-15.` footer. Place §11 in the natural section position (after the existing §10 entry — locate it with `grep -n '^## §10' VORTEX-LINUX-MERGE-PLAYBOOK.md` and append after §10's content ends).

    Step 3 — Add commit-index row per D-25-13: Find the playbook's commit-index table (`grep -n 'commit-index' VORTEX-LINUX-MERGE-PLAYBOOK.md` or scan for a table referencing master/linux-port commit hashes). Append a new row linking back to commit 4 of this phase (the deliberate-drop list lives in commit 4's body per D-25-14). Use the same row shape as existing rows.

    Step 4 — `pnpm install` per D-25-06 (the third and final pre-commit install of Phase 25). Confirms no doc/playbook edit broke the workspace.

    Step 5 — Stage `docs/flatpak-maintenance.md`, `docs/flatpak-technical.md`, `AGENTS-DEBUGGING.md`, `VORTEX-LINUX-MERGE-PLAYBOOK.md`. Commit with title `restore(docs): flatpak + AGENTS-DEBUGGING + playbook §11` (D-25-05 format adapted for commit 5; if shorter is preferred, use `restore(docs): flatpak + AGENTS-DEBUGGING — Vitest-only divergence`). Body cites: SHA `8b5a9f675` for the doc restorations; D-25-12 reason for §11 addition; cross-link to commit 4 of this phase per D-25-13. Sign with SSH.

    Step 6 — Run the FULL D-25-15 done-gate, all five conditions, in order:
    1. Re-run `git diff v8.0/config-bucket 8b5a9f675 -- <every restored path from commits 1–5>` — must be empty for the byte-for-byte set; package.json + playbook + workspace files are intentionally diverged and excluded from this check (they're tracked by their own task acceptance criteria).
    2. `pnpm install` — must exit 0 (already confirmed in step 4 above; re-confirm post-commit).
    3. `pnpm install --frozen-lockfile` — must exit 0 (D-25-15 condition 3, also Phase 24 D-17 norm).
    4. `pnpm typecheck -F @vortex/paths -F @vortex/paths-node` — must exit 0 (D-25-15 condition 4).
    5. `git grep -nE 'jest\.config\.mjs|src/renderer/src/__(mocks|tests)__' v8.0/config-bucket -- ':!VORTEX-LINUX-MERGE-PLAYBOOK.md'` — must produce empty output (D-25-15 condition 5).

    Step 7 — Force-with-lease push per D-25-07 + Phase 24 D-02 inheritance. Use the inline SSH URL pattern from project memory ("Git push SSH URL"): `git push --force-with-lease git@github.com:atabisz/Vortex.git v8.0/config-bucket:sync/upstream-v2.0.0`. Push target is the PR #4 head branch. If the daily `rebase-upstream.yml` cron raced, `--force-with-lease` refuses safely — surface the error and re-fetch + re-attempt only after confirming the cron's commit is benign.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -qE '^restore\(docs\): ' && grep -q '^## §11 Deliberate test-runner divergences$' VORTEX-LINUX-MERGE-PLAYBOOK.md && grep -q 'src/renderer/jest\.config\.mjs' VORTEX-LINUX-MERGE-PLAYBOOK.md && pnpm install --frozen-lockfile --reporter=silent && pnpm typecheck -F @vortex/paths -F @vortex/paths-node && [ -z "$(git grep -nE 'jest\.config\.mjs|src/renderer/src/__(mocks|tests)__' -- ':!VORTEX-LINUX-MERGE-PLAYBOOK.md')" ]</automated>
  </verify>
  <acceptance_criteria>
    - HEAD commit title starts with `restore(docs):` and references both the doc files and §11 / Vitest-only divergence
    - HEAD commit modifies exactly: `docs/flatpak-maintenance.md`, `docs/flatpak-technical.md`, `AGENTS-DEBUGGING.md`, `VORTEX-LINUX-MERGE-PLAYBOOK.md`
    - The three doc files match SHA `8b5a9f675` byte-for-byte (D-25-15 condition 1)
    - `VORTEX-LINUX-MERGE-PLAYBOOK.md` contains the literal heading `## §11 Deliberate test-runner divergences` AND the literal strings `src/renderer/jest.config.mjs`, `src/renderer/src/__mocks__/`, `src/renderer/src/__tests__/`, AND the footer `Decided: Phase 25 (restore-dropped-scaffolding), 2026-05-15.`
    - The playbook commit-index table has a new row referencing commit 4 of Phase 25 (the workflows-restore commit)
    - HEAD commit is signed
    - All five D-25-15 done-gate checks pass (each automated and re-runnable)
    - Force-with-lease push to `git@github.com:atabisz/Vortex.git v8.0/config-bucket:sync/upstream-v2.0.0` exits 0 (or refused safely with a documented re-fetch-and-retry path)
    - `git log --oneline v8.0/config-bucket | head -5` shows commits 5, 4, 3, 2, 1 in that order with the matching D-25-04/D-25-05 titles
  </acceptance_criteria>
  <done>
    Commit 5 of 5 landed; all five D-25-15 conditions green; branch pushed to fork; Phase 25 complete.
  </done>
</task>

</tasks>

<verification>
- Five commits in correct order on `v8.0/config-bucket` matching D-25-04 / D-25-05 titles
- All five D-25-15 done-gate conditions automated and re-runnable
- Playbook §11 + commit-index row present per D-25-12 / D-25-13
- Commit 4 body carries deny-list provenance per D-25-14
- Push target reached (or lease check refused safely)
</verification>

<success_criteria>
SYNC-14, SYNC-15, SYNC-16 satisfied; Phase 25 done-gate clean; PR #4 head reflects the restored tree.
</success_criteria>

<output>
After completion, create `.planning/phases/25-restore-dropped-scaffolding/25-04-SUMMARY.md`. Cite all three commit SHAs (commits 3, 4, 5), the done-gate result table (one row per D-25-15 condition with pass/fail), the push result (lease accepted or refused), and any new gotchas to add to playbook §11 if surfaced during execution.
</output>
