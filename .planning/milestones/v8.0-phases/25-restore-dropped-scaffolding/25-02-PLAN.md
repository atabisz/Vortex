---
phase: 25-restore-dropped-scaffolding
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - packages/paths/**
  - packages/paths-node/**
  - pnpm-lock.yaml
autonomous: true
requirements: [SYNC-11, SYNC-12]

must_haves:
  truths:
    - "Files under packages/paths/ and packages/paths-node/ on v8.0/config-bucket match their counterparts at upstream parent SHA 8b5a9f675 byte-for-byte"
    - "pnpm install succeeds; @vortex/paths and @vortex/paths-node resolve as workspace packages"
    - "pnpm typecheck -F @vortex/paths -F @vortex/paths-node passes"
  artifacts:
    - path: "packages/paths/package.json"
      provides: "Restored @vortex/paths workspace manifest"
      contains: "\"name\": \"@vortex/paths\""
    - path: "packages/paths-node/package.json"
      provides: "Restored @vortex/paths-node workspace manifest"
      contains: "\"name\": \"@vortex/paths-node\""
  key_links:
    - from: "packages/paths-node/package.json"
      to: "packages/paths"
      via: "workspace dependency"
      pattern: "@vortex/paths"
    - from: "pnpm-lock.yaml"
      to: "packages/paths and packages/paths-node"
      via: "workspace package entries added by post-restore pnpm install"
      pattern: "packages/paths"
---

<objective>
Land Phase 25 commit 1 per D-25-04: restore `packages/paths/` (49 files, 49 entries → @vortex/paths workspace) and `packages/paths-node/` (8 files → @vortex/paths-node, depends on @vortex/paths) byte-for-byte from upstream parent `8b5a9f675`. Run `pnpm install` so the workspace graph picks up the two new packages and the lockfile updates additively. Verify typecheck passes for both packages. One atomic commit follows D-25-05's title format.

Purpose: SYNC-11 + SYNC-12 require the paths workspaces present and typechecking. They are foundational — paths-node depends on paths, and other restored content (notably ba2-support and chunking) may transitively reach into them via the workspace graph. Restoring them first means subsequent commits land on a workspace graph that already resolves.

Output: New tree on `v8.0/config-bucket` with packages/paths/ and packages/paths-node/ present; updated pnpm-lock.yaml; one new commit `restore(packages): paths + paths-node from upstream v2.0.0`.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-restore-dropped-scaffolding/25-CONTEXT.md
@.planning/phases/25-restore-dropped-scaffolding/25-DISCOVERY-RESULT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: git checkout 8b5a9f675 -- packages/paths packages/paths-node + pnpm install + verify byte-for-byte match</name>
  <read_first>
    - .planning/phases/25-restore-dropped-scaffolding/25-CONTEXT.md (D-25-01 restore mechanism, D-25-04 commit 1, D-25-06 install discipline, D-25-15 done-gate condition 1)
    - .planning/phases/25-restore-dropped-scaffolding/25-DISCOVERY-RESULT.md (confirms paths + paths-node are in the expected restore set)
    - pnpm-workspace.yaml (workspace globs already include `packages/*` from Phase 24 — no edit needed; verify by reading)
    - .planning/phases/24-config-bucket/24-CONTEXT.md (Phase 24 D-17 two-pass install discipline)
  </read_first>
  <action>
    Run `git checkout 8b5a9f675 -- packages/paths packages/paths-node` from the repo root on branch `v8.0/config-bucket`. Verify byte-for-byte match per D-25-15 condition 1: `git diff v8.0/config-bucket 8b5a9f675 -- packages/paths packages/paths-node` must produce empty output (after the checkout, the index/working tree mirrors the SHA's content; the diff against the SHA is empty by construction — keep this assertion as the explicit gate).

    Run `pnpm install` from the repo root. Per D-25-06 the first install must succeed before the next workspace-affecting commit. Expect the lockfile delta to be additive only (new entries linking the two new workspace packages). If the diff against `pnpm-lock.yaml` shows non-additive churn (entries removed or unrelated versions bumped), STOP and surface to the user — that signals catalog drift inherited from Phase 24's lockfile regen, not a Plan 02 problem.

    Run `pnpm typecheck -F @vortex/paths -F @vortex/paths-node` (matches D-25-15 condition 4 verbatim). Both must pass.

    Stage `packages/paths`, `packages/paths-node`, and `pnpm-lock.yaml`. Commit with title `restore(packages): paths + paths-node from upstream v2.0.0 — byte-for-byte` and a body that cites: (a) restoration command `git checkout 8b5a9f675 -- packages/paths packages/paths-node`; (b) upstream parent SHA `8b5a9f675` (second parent of merge `138da2249`); (c) which Phase 25 commit number this is per D-25-04 (commit 1 of 5); (d) install + typecheck verification results.

    Sign the commit per the project memory ("All commits signed with SSH"). Do not push — Plan 04 handles the single end-of-phase force-with-lease push per D-25-07.
  </action>
  <verify>
    <automated>git diff HEAD~1 HEAD --name-only | grep -E '^(packages/paths|packages/paths-node|pnpm-lock\.yaml)' && git diff v8.0/config-bucket 8b5a9f675 -- packages/paths packages/paths-node | wc -l | grep -qx '0' && pnpm typecheck -F @vortex/paths -F @vortex/paths-node</automated>
  </verify>
  <acceptance_criteria>
    - `git diff v8.0/config-bucket 8b5a9f675 -- packages/paths packages/paths-node` produces empty output (byte-for-byte match per D-25-15 condition 1)
    - `pnpm install` exits 0; `pnpm-lock.yaml` modified non-trivially (it has an entry for the new workspace packages)
    - `pnpm typecheck -F @vortex/paths -F @vortex/paths-node` exits 0 (D-25-15 condition 4)
    - HEAD commit title matches the format `restore(packages): paths + paths-node from upstream v2.0.0 — byte-for-byte` (D-25-05)
    - HEAD commit body contains the literal string `8b5a9f675`
    - HEAD commit is signed (`git log -1 --show-signature` shows a "Good signature" line)
    - HEAD commit modifies only `packages/paths/**`, `packages/paths-node/**`, and `pnpm-lock.yaml` (no other paths)
  </acceptance_criteria>
  <done>
    Commit 1 of 5 landed; paths workspaces resolve; typecheck green for both.
  </done>
</task>

</tasks>

<verification>
- Byte-for-byte match against `8b5a9f675`
- Lockfile delta is additive
- Typecheck passes for `@vortex/paths` AND `@vortex/paths-node`
</verification>

<success_criteria>
SYNC-11 and SYNC-12 satisfied; one atomic commit added on `v8.0/config-bucket`.
</success_criteria>

<output>
After completion, create `.planning/phases/25-restore-dropped-scaffolding/25-02-SUMMARY.md`. Cite the commit SHA, the lockfile delta (line count added/removed), and the typecheck pass.
</output>
