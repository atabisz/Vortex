---
phase: 28-renderer-main-spine
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
    - .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh
    - .planning/milestones/v8.0/scripts/grep-checkpoint.sh
autonomous: true
requirements: [SYNC-18, SYNC-20, SYNC-24, SYNC-25]

must_haves:
    truths:
        - "scripts/grep-checkpoint.sh moved from `.planning/phases/26-mod-management-hot-zone/scripts/` to `.planning/milestones/v8.0/scripts/` per D-28-04 (single `git mv` so history is preserved)"
        - "Relocated script extended in the same atomic commit with four new gates per D-28-02: §2 winapi-bindings allowlist (positive ≥1), §4 transferPath.ts NEGATIVE Windows-only-reject gate (count must be 0), §8 Proton helpers (≥3) + hide-instead-of-quit onSpawned (≥1), §9 findAllLinuxSteamPaths (≥1) + libraryfolders.vdf (≥1)"
        - "All four new gates exit zero on the current pre-resolution `v8.0/config-bucket` tree — playbook §2/§4/§8/§9 invariants pre-date this merge"
        - "Pre-existing Phase 26/27 gates (§1, §3, §6, §7a–d, §10, 140a57217, BG3 4-class, Morrowind migrate103) still pass with `--skip-conflict-check`"
        - "Single atomic commit on `v8.0/config-bucket` titled `chore(28-00): move grep-checkpoint.sh to milestone-shared location + add §2 §4 §8 §9 gates` per D-28-04 + D-28-06 executor-discretion bundling"
        - "`--skip-conflict-check` flag still gates ONLY the conflict-marker gate; the four new §2/§4/§8/§9 gates always run (same idiom as Phase 27 §1/§3/§10/BG3/Morrowind gates)"
    artifacts:
        - path: ".planning/milestones/v8.0/scripts/grep-checkpoint.sh"
          provides: "Relocated and extended re-grep harness — 16 gates total (12 from Phase 27 + 4 new §2/§4/§8/§9 gates) used by every Phase 28 resolution plan"
          contains: "winapi-bindings"
        - path: ".planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh"
          provides: "Removed by `git mv` — pre-existing references in 26-*/27-* SUMMARY.md files remain valid as historical pointers per D-28-04"
          contains: ""
    key_links:
        - from: ".planning/milestones/v8.0/scripts/grep-checkpoint.sh"
          to: "Plans 28-01..28-10 acceptance criteria"
          via: "Each per-file resolution plan invokes the relocated script after every commit; the new gates surface §2/§4/§8/§9 regressions inline"
          pattern: ".planning/milestones/v8.0/scripts/grep-checkpoint.sh"
        - from: ".planning/milestones/v8.0/scripts/grep-checkpoint.sh"
          to: "Plan 28-11 done-gate"
          via: "Done-gate runs the full script (no flags) as item #2 of D-28-05"
          pattern: "grep-checkpoint.sh"
---

<objective>
Relocate `.planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh` to `.planning/milestones/v8.0/scripts/grep-checkpoint.sh` per D-28-04 AND extend it in the same commit (per D-28-06 executor-discretion bundling — "the relocated script is immediately complete") with four new playbook §2/§4/§8/§9 gates per D-28-02. Phase 28 extends, doesn't rewrite — Phase 26/27 gates stay verbatim.

Purpose: Plans 28-01..28-10 invoke this harness after each per-file commit. Without §2/§4/§8/§9 gates, an upstream-side stance on the four playbook-protected files (which are NOT in the conflict list this phase but ARE gate targets per D-28-06) could silently regress and only surface at runtime in Phase 29. With the gates, regressions surface in-line. Promoting the script out of `phases/26-…/` to `.planning/milestones/v8.0/scripts/` keeps the cherry-pick-to-`linux-port` story clean for Phase 30 — the script ships with the merge.

Output: Relocated + extended `grep-checkpoint.sh` (16 gates total: 12 from Phase 27 + 4 new) and one atomic commit on `v8.0/config-bucket`.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-renderer-main-spine/28-CONTEXT.md
@.planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md
@.planning/phases/26-mod-management-hot-zone/26-CONTEXT.md
@.planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv script to milestone-shared location and extend with §2 + §4 + §8 + §9 gates in a single atomic commit</name>
  <read_first>
    - .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh (current 12-gate body — read ALL of it; the existing structure is the template the new gates plug into: same `pass`/`fail` helpers, same `failures` counter, same `--skip-conflict-check` flag, NO `set -e`)
    - .planning/phases/28-renderer-main-spine/28-CONTEXT.md (D-28-02 — exact §2/§4/§8/§9 grep commands; D-28-04 — relocation rationale + commit-bundling discretion; D-28-06 — gated-files list confirming all four targets are NOT in this phase's conflict scope)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md §2 — `winapi-bindings` allowlist in `src/renderer/webpack.config.cjs` `nodeExternals`
    - VORTEX-LINUX-MERGE-PLAYBOOK.md §4 — `testPathTransfer` in `src/renderer/src/util/transferPath.ts` must NOT have the Windows-only reject (NEGATIVE gate, first such in the milestone)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md §8 — `StarterInfo.ts` Proton helpers (`isPathPrefix`, `shouldRunWithProton`, `runToolWithProton`) + hide-on-spawn `onSpawned`
    - VORTEX-LINUX-MERGE-PLAYBOOK.md §9 — `Steam.ts` `resolveSteamPaths()` calls `findAllLinuxSteamPaths()` and reads `libraryfolders.vdf`
    - Verify the four gate-target files are pre-resolution clean: `git grep -nE 'winapi-bindings' src/renderer/webpack.config.cjs | grep -E 'nodeExternals|allowlist|allowList'`, `git grep -nE "platform !== ['\\\"]win32['\\\"]\\) reject\\(UnsupportedOperatingSystem" src/renderer/src/util/transferPath.ts`, `git grep -nE '\\b(isPathPrefix|shouldRunWithProton|runToolWithProton)\\s*\\(' src/renderer/src/util/StarterInfo.ts`, `git grep -n 'onSpawned' src/renderer/src/util/StarterInfo.ts | grep -E 'hide|minimize'`, `git grep -n 'findAllLinuxSteamPaths' src/main/src/util/Steam.ts`, `git grep -n 'libraryfolders\\.vdf' src/main/src/util/Steam.ts`. If ANY pre-resolution gate already fails baseline, STOP and surface — that means the invariant was eaten before Phase 28 started.
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket` (`git rev-parse --abbrev-ref HEAD`).

    Step 1 — relocation:
    `mkdir -p .planning/milestones/v8.0/scripts`
    `git mv .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh .planning/milestones/v8.0/scripts/grep-checkpoint.sh`
    Confirm history preservation: `git log --follow --oneline .planning/milestones/v8.0/scripts/grep-checkpoint.sh | head -3` should show the Phase 26 + Phase 27 commits.

    Step 2 — edit the relocated script. Add four new gates AFTER the existing Phase 27 gates (§1/§3/§10/BG3/Morrowind, gates 7–11) and BEFORE the conflict-marker gate (currently gate 12). Renumber the conflict-marker gate to 16. Use the same `pass`/`fail`/`failures` pattern as the existing gates.

    Update the header comment block to add the four new gate descriptions and cite playbook §2/§4/§8/§9 + D-28-02. Update the `%d gate(s) clean` summary line — total non-conflict gates is now 15; with conflict-marker gate, 16.

    **Gate 12 — §2 winapi-bindings allowlist (D-28-02, success criterion #2):**
    Positive existence; count threshold ≥ 1.
    ```
    git grep -nE 'winapi-bindings' src/renderer/webpack.config.cjs \
      | grep -E 'nodeExternals|allowlist|allowList' | wc -l
    ```
    Threshold: ≥ 1. FAIL message: `"§2 regression — winapi-bindings missing from nodeExternals allowlist in src/renderer/webpack.config.cjs"`.

    **Gate 13 — §4 transferPath.ts Windows-only reject NEGATIVE gate (D-28-02, success criterion #3):**
    First negative gate in the milestone. Threshold: count must be EXACTLY 0.
    ```
    git grep -nE "platform !== ['\"]win32['\"]\\) reject\\(UnsupportedOperatingSystem" \
      src/renderer/src/util/transferPath.ts | wc -l
    ```
    Threshold: must equal 0. FAIL message: `"§4 regression detected — Windows-only reject re-introduced in src/renderer/src/util/transferPath.ts"` (per CONTEXT specifics: "make the gate's intent obvious to future readers").

    **Gate 14 — §8 StarterInfo.ts Proton helpers + hide-on-spawn (D-28-02, success criterion #4):**
    Two sub-checks, both must pass for gate to be green; emit two messages on partial failure (per CONTEXT specifics).
    Sub-check 14a — Proton helpers count ≥ 3:
    ```
    git grep -nE '\b(isPathPrefix|shouldRunWithProton|runToolWithProton)\s*\(' \
      src/renderer/src/util/StarterInfo.ts | wc -l
    ```
    Threshold: ≥ 3 (one per helper; multiple call sites count too). FAIL message on this sub-check: `"§8 regression — Proton helpers missing or fewer than 3 references in src/renderer/src/util/StarterInfo.ts"`.

    Sub-check 14b — hide/minimize on `onSpawned` ≥ 1:
    ```
    git grep -n 'onSpawned' src/renderer/src/util/StarterInfo.ts \
      | grep -E 'hide|minimize' | wc -l
    ```
    Threshold: ≥ 1. FAIL message on this sub-check: `"§8 regression — onSpawned no longer hides/minimizes in src/renderer/src/util/StarterInfo.ts (upstream may have reverted to quit())"`.

    **Gate 15 — §9 Steam.ts findAllLinuxSteamPaths + libraryfolders.vdf (D-28-02, success criterion #5):**
    Two sub-checks, both must pass.
    Sub-check 15a — findAllLinuxSteamPaths reference ≥ 1:
    ```
    git grep -n 'findAllLinuxSteamPaths' src/main/src/util/Steam.ts | wc -l
    ```
    Threshold: ≥ 1. FAIL message: `"§9 regression — findAllLinuxSteamPaths() not called in src/main/src/util/Steam.ts"`.

    Sub-check 15b — libraryfolders.vdf reference ≥ 1:
    ```
    git grep -n 'libraryfolders\.vdf' src/main/src/util/Steam.ts | wc -l
    ```
    Threshold: ≥ 1. FAIL message: `"§9 regression — libraryfolders.vdf parsing missing from src/main/src/util/Steam.ts"`.

    **Gate 16 (was 12): no conflict markers** — keep the path list from Phase 27 verbatim (Phase 27's path list was the seven gamebryo-per-game directories plus mod_management). Phase 28's resolution scope (`src/renderer/`, `src/main/`, `src/preload/`, `src/shared/`, `extensions/nexus_integration/`, `scripts/`, `.github/actions/fingerprints/`) is BROADER than the gate's path list — don't change the gate's path list (Phase 26/27 invariants must continue to be protected by it). The Phase 28 done-gate (plan 28-11) runs a separate, broader `git grep` against the Phase 28 paths per D-28-05 #1.

    The `--skip-conflict-check` flag continues to gate ONLY this last (now numbered 16) gate.

    After editing, run the script with `--skip-conflict-check`. ALL fifteen non-conflict gates must pass on the unmodified `v8.0/config-bucket` tree because §2/§4/§8/§9 invariants pre-date this merge — confirmed in `<read_first>`. If any baseline gate fails, STOP and surface to the user — that means an invariant has already been eaten and Phase 28's premise is wrong for that area.

    Then run the script WITHOUT `--skip-conflict-check`. It must exit non-zero — but ONLY if any of the seven Phase 27 path-list directories still carry conflict markers. After Phase 27 those are clean, so the conflict-marker gate may pass with `pass`. Phase 28's broader scope (renderer/main/preload/shared/nexus/scripts/fingerprints) is NOT in the gate's path list, so unresolved Phase 28 conflicts will NOT trip gate 16. This is intentional — Phase 28 plans 28-01..28-10 each invoke the script with `--skip-conflict-check` (matching Phase 27's per-plan invocation), and plan 28-11 runs a separate broader `git grep` for done-gate item #1.

    Stage both the source-side `git mv` deletion AND the destination + edits in a single commit with title `chore(28-00): move grep-checkpoint.sh to milestone-shared location + add §2 §4 §8 §9 gates`. Body: name the four new gates by playbook section + cite D-28-02; note the §4 negative-gate idiom; note the §8/§9 sub-check idiom. **Use SSH commit signing per CLAUDE.md memory note "SSH commit signing"** (commits are signed with `~/.ssh/id_ed25519`, not GPG — git config should already be set up; do NOT pass `-S` if config already enables signing).

    Do NOT push. D-28-00 specifies one push at phase end via plan 28-11.

  </action>
  <verify>
    <automated>test -x .planning/milestones/v8.0/scripts/grep-checkpoint.sh &amp;&amp; test ! -e .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check &amp;&amp; git log -1 --format=%s | grep -qE '^chore\(28-00\): move grep-checkpoint\.sh to milestone-shared location \+ add §2 §4 §8 §9 gates$'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/milestones/v8.0/scripts/grep-checkpoint.sh` exists and is executable
    - `.planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh` no longer exists (`git mv` source side)
    - `git log --follow --oneline .planning/milestones/v8.0/scripts/grep-checkpoint.sh` includes Phase 26 and Phase 27 commits (history preserved)
    - Header comment names §2, §4 (negative), §8 (helpers + hide-on-spawn), §9 (findAllLinuxSteamPaths + libraryfolders.vdf) by exact label
    - `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero (all 15 non-conflict gates pass on pre-resolution tree)
    - HEAD commit title matches exactly `chore(28-00): move grep-checkpoint.sh to milestone-shared location + add §2 §4 §8 §9 gates`
    - HEAD commit touches exactly two paths (the rename source + destination — `git show --stat HEAD` should show one rename entry plus content delta)
    - HEAD commit is SSH-signed (`git log -1 --show-signature` shows valid SSH signature)
    - No push has happened (push is plan 28-11 territory)
  </acceptance_criteria>
  <done>
    Script relocated to `.planning/milestones/v8.0/scripts/`, four new §2/§4/§8/§9 gates added, baseline-clean with `--skip-conflict-check`, ready for plans 28-01..28-10 to invoke after each per-file commit.
  </done>
</task>

</tasks>

<verification>
- `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
- HEAD commit body cites §2, §4 (negative), §8, §9 + D-28-02
- Single commit on `v8.0/config-bucket` (rename + edit bundled per D-28-06)
- Old script path no longer exists
</verification>

<success_criteria>

- Script moved with history preserved
- Four new gates added (16 total)
- Phase 26/27 gates untouched
- Baseline run clean with `--skip-conflict-check`
- Commit 0 of Phase 28 lands on `v8.0/config-bucket`

</success_criteria>

<output>
After completion, create `.planning/phases/28-renderer-main-spine/28-00-SUMMARY.md`
</output>
