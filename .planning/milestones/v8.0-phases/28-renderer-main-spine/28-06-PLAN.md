---
phase: 28-renderer-main-spine
plan: 06
type: execute
wave: 7
depends_on: [28-05]
files_modified:
    - src/renderer/src/views/components/Header/Notifications/useNotificationFiltering.ts
    - src/renderer/src/views/components/Menu/ToolsSection.tsx
    - src/renderer/src/views/components/Menu/useTools.ts
    - src/renderer/src/views/components/Spine/SpineContext.tsx
    - src/renderer/src/views/layout/ToastContainer.tsx
    - src/renderer/src/views/pages/Tools/ToolRow.tsx
    - src/renderer/src/views/pages/Tools/toolStarters.ts
    - src/renderer/src/views/pages/Tools/useToolsData.ts
    - src/renderer/src/views/pages/Tools/useToolsPage.ts
    - src/renderer/src/views/pages/Tools/index.tsx
autonomous: true
requirements: [SYNC-07]

must_haves:
    truths:
        - "Ten renderer view conflict files resolved per D-28-01: useNotificationFiltering.ts → ToolsSection.tsx → useTools.ts → SpineContext.tsx → ToastContainer.tsx → ToolRow.tsx → toolStarters.ts → useToolsData.ts → useToolsPage.ts → pages/Tools/index.tsx (last per re-export idiom)"
        - "Ten atomic commits on `v8.0/config-bucket` each titled `resolve(renderer): views/<path> — <stance>` per D-28-00"
        - "After EACH commit, `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero"
        - "Per-bucket typecheck for @vortex/renderer is deferred to plan 28-07 per D-28-03"
    artifacts:
        - path: "src/renderer/src/views/pages/Tools/index.tsx"
          provides: "Tools page barrel — last in views sub-order per D-28-01 (re-export composer idiom; consumes ToolRow, toolStarters, useToolsData, useToolsPage)"
          contains: "Tools"
    key_links:
        - from: "views/pages/Tools/index.tsx"
          to: "views/pages/Tools/{ToolRow,toolStarters,useToolsData,useToolsPage}.{ts,tsx}"
          via: "Barrel imports the four leaves — last in pages/Tools sub-order per re-export idiom"
          pattern: "from './(ToolRow|toolStarters|useToolsData|useToolsPage)'"
        - from: "views/components/Menu/{ToolsSection,useTools}"
          to: "views/pages/Tools/*"
          via: "Menu Tools section + hook are part of the same Tools surface — settle before pages/Tools"
          pattern: "useTools|ToolsSection"
---

<objective>
Resolve the 10 renderer view conflict files per D-28-01 sub-order: `components/Header/Notifications/useNotificationFiltering.ts` → `components/Menu/ToolsSection.tsx` → `components/Menu/useTools.ts` → `components/Spine/SpineContext.tsx` → `layout/ToastContainer.tsx` → `pages/Tools/ToolRow.tsx` → `pages/Tools/toolStarters.ts` → `pages/Tools/useToolsData.ts` → `pages/Tools/useToolsPage.ts` → `pages/Tools/index.tsx` (last per re-export-composer idiom). Ten atomic commits on `v8.0/config-bucket` matching D-28-00 title format `resolve(renderer): views/<path> — <stance>`. Run grep-checkpoint script with `--skip-conflict-check` after each commit. Per-bucket typecheck deferred to plan 28-07.

Purpose: Sixth bucket per D-28-01. Renderer views; depend on extensions (settled in 28-05) and renderer leaves (settled in 28-04). Tools/index.tsx is the heaviest views file and lands LAST per re-export-composer idiom.

Output: Ten commits on `v8.0/config-bucket`. After plan 28-06 lands, 44/52 hand-resolved files done.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-renderer-main-spine/28-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve Header/Notifications, Menu (Tools section + hook), Spine, and layout/ToastContainer (5 commits)</name>
  <read_first>
    - src/renderer/src/views/components/Header/Notifications/useNotificationFiltering.ts (every conflict region, in full)
    - src/renderer/src/views/components/Menu/ToolsSection.tsx (every conflict region, in full)
    - src/renderer/src/views/components/Menu/useTools.ts (every conflict region, in full)
    - src/renderer/src/views/components/Spine/SpineContext.tsx (every conflict region, in full)
    - src/renderer/src/views/layout/ToastContainer.tsx (every conflict region, in full)
    - `git show f15bbabb8:` versions for all 5 (base-commit references)
    - .planning/phases/28-renderer-main-spine/28-CONTEXT.md (D-28-01 sub-order; D-28-06 stance defaults)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md — confirm none of these 5 files are named in any playbook gate (they are not)
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Resolve each file in order, ONE COMMIT PER FILE per D-28-00. Default stance D-28-06; bluebird Promise trap watch. Checkpoint after each commit.

    1. `src/renderer/src/views/components/Header/Notifications/useNotificationFiltering.ts` → commit `resolve(renderer): views/components/Header/Notifications/useNotificationFiltering.ts — <stance>`. Imports reducers/notifications.ts shape (settled in 28-04).
    2. `src/renderer/src/views/components/Menu/ToolsSection.tsx` → commit `resolve(renderer): views/components/Menu/ToolsSection.tsx — <stance>`.
    3. `src/renderer/src/views/components/Menu/useTools.ts` → commit `resolve(renderer): views/components/Menu/useTools.ts — <stance>`. Tools-related hook; settles before pages/Tools/* in Task 2.
    4. `src/renderer/src/views/components/Spine/SpineContext.tsx` → commit `resolve(renderer): views/components/Spine/SpineContext.tsx — <stance>`.
    5. `src/renderer/src/views/layout/ToastContainer.tsx` → commit `resolve(renderer): views/layout/ToastContainer.tsx — <stance>`.

    Stage only the named file per commit. SSH-signed.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/views/components/Header/Notifications/useNotificationFiltering.ts src/renderer/src/views/components/Menu/ToolsSection.tsx src/renderer/src/views/components/Menu/useTools.ts src/renderer/src/views/components/Spine/SpineContext.tsx src/renderer/src/views/layout/ToastContainer.tsx; test $? -ne 0 &amp;&amp; test $(git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\(renderer\): views/(components/(Header|Menu|Spine)|layout)/') -ge 5 &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files conflict-clean
    - Five new commits matching `resolve(renderer): views/<path> —`
    - Each commit touches exactly one file; SSH-signed
    - Checkpoint exits zero after each commit
  </acceptance_criteria>
  <done>
    Five views/components + views/layout files resolved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolve pages/Tools/* leaves (4 files), then pages/Tools/index.tsx LAST per re-export idiom (5 commits)</name>
  <read_first>
    - src/renderer/src/views/pages/Tools/ToolRow.tsx (every conflict region, in full)
    - src/renderer/src/views/pages/Tools/toolStarters.ts (every conflict region, in full)
    - src/renderer/src/views/pages/Tools/useToolsData.ts (every conflict region, in full)
    - src/renderer/src/views/pages/Tools/useToolsPage.ts (every conflict region, in full)
    - src/renderer/src/views/pages/Tools/index.tsx (every conflict region, in full — the barrel)
    - `git show f15bbabb8:` versions for all 5 (base-commit references)
    - src/renderer/src/views/components/Menu/useTools.ts (just-resolved final shape from Task 1; pages/Tools/* may share types or imports)
    - .planning/phases/28-renderer-main-spine/28-CONTEXT.md (D-28-01 — Tools/index.tsx LAST per re-export idiom)
  </read_first>
  <action>
    Resolve in order, ONE COMMIT PER FILE. Default stance D-28-06. Checkpoint after each commit.

    1. `src/renderer/src/views/pages/Tools/ToolRow.tsx` → commit `resolve(renderer): views/pages/Tools/ToolRow.tsx — <stance>`.
    2. `src/renderer/src/views/pages/Tools/toolStarters.ts` → commit `resolve(renderer): views/pages/Tools/toolStarters.ts — <stance>`. **Tool starter logic likely consumes StarterInfo.ts** — StarterInfo.ts is a §8 gated file NOT in scope. If toolStarters.ts imports from StarterInfo.ts, the imports survive verbatim — the gate (§8) verifies StarterInfo.ts is unmodified. Watch for any conflict region that touches `runWithProton`/`runToolWithProton` indirectly — fork-side wins.
    3. `src/renderer/src/views/pages/Tools/useToolsData.ts` → commit `resolve(renderer): views/pages/Tools/useToolsData.ts — <stance>`.
    4. `src/renderer/src/views/pages/Tools/useToolsPage.ts` → commit `resolve(renderer): views/pages/Tools/useToolsPage.ts — <stance>`.
    5. `src/renderer/src/views/pages/Tools/index.tsx` → commit `resolve(renderer): views/pages/Tools/index.tsx — <stance>`. **Last per D-28-01 re-export idiom.** Composes ToolRow + toolStarters + useToolsData + useToolsPage. Hand-read every conflict region; symbol shapes from the four leaves above must align.

    Stage only the named file per commit. SSH-signed.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/views/pages/Tools/; test $? -ne 0 &amp;&amp; test $(git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\(renderer\): views/pages/Tools/') -eq 5 &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - All 5 pages/Tools/* files conflict-clean
    - Five new commits matching `resolve(renderer): views/pages/Tools/<file> —`
    - Each commit touches exactly one file; SSH-signed
    - Checkpoint exits zero after each commit
    - index.tsx commit lands LAST among pages/Tools/* commits (verify via `git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep 'resolve(renderer): views/pages/Tools/' | head -1` showing index.tsx)
    - `git grep -l '^<<<<<<< ' src/renderer/src/views/` returns empty (entire views bucket clean)
    - `git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\(renderer\): views/'` returns 10 (this plan's full output)
  </acceptance_criteria>
  <done>
    All 10 views files resolved; index.tsx last; 44/52 hand-resolved files done.
  </done>
</task>

</tasks>

<verification>
- All 10 files conflict-clean
- Ten atomic commits matching `resolve(renderer): views/<file> — <stance>`
- `scripts/grep-checkpoint.sh --skip-conflict-check` exits zero after each commit
- Per-bucket typecheck deferred to plan 28-07 per D-28-03
- index.tsx LAST in pages/Tools/* per re-export-composer idiom
</verification>

<success_criteria>

- 44/52 hand-resolved Phase 28 files done
- Renderer views settled before ExtensionManager.ts (plan 28-07)
- pages/Tools/index.tsx re-export-composer idiom honoured

</success_criteria>

<output>
After completion, create `.planning/phases/28-renderer-main-spine/28-06-SUMMARY.md`
</output>
