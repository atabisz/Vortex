---
phase: 28-renderer-main-spine
plan: 02
type: execute
wave: 3
depends_on: [28-01]
files_modified:
    - src/preload/src/index.ts
autonomous: true
requirements: [SYNC-08]

must_haves:
    truths:
        - "Single @vortex/preload conflict file resolved per D-28-01: index.ts (the typed IPC bridge)"
        - "One atomic commit on `v8.0/config-bucket` titled `resolve(preload): index.ts — <stance>` per D-28-00"
        - "After the commit, `pnpm typecheck -F @vortex/preload` exits zero (D-28-03 per-bucket cadence)"
        - "After the commit, `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero"
    artifacts:
        - path: "src/preload/src/index.ts"
          provides: "Typed IPC bridge — single file in preload bucket; contextBridge surface consumed by renderer"
          contains: "contextBridge"
    key_links:
        - from: "src/preload/src/index.ts"
          to: "@vortex/main IPC handlers + @vortex/renderer api consumers"
          via: "Bridge contract defined by shared types — main-side handler additions ride on top per CONTEXT 'Integration Points'"
          pattern: "ipcRenderer\\.invoke|contextBridge\\.exposeInMainWorld"
---

<objective>
Resolve the single conflict file in `src/preload/src/` per D-28-01: `index.ts`. One atomic commit on `v8.0/config-bucket` matching D-28-00 title format `resolve(preload): index.ts — <stance>`. Per-bucket typecheck after the commit per D-28-03. Run grep-checkpoint script with `--skip-conflict-check` after the commit.

Purpose: Second bucket per D-28-01. Resolved before main because the bridge contract is defined by shared types (now settled in plan 28-01), not main internals; main-side handler additions ride on top.

Output: One commit on `v8.0/config-bucket`. After plan 28-02 lands, 4/52 hand-resolved files done.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-renderer-main-spine/28-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve preload/src/index.ts and run per-bucket typecheck</name>
  <read_first>
    - src/preload/src/index.ts (current state — every conflict region, in full)
    - `git show f15bbabb8:src/preload/src/index.ts` (base-commit reference)
    - src/shared/src/errors.ts and src/shared/src/telemetry/spans.ts (just-resolved final shape — preload imports must align if it consumes either)
    - .planning/phases/28-renderer-main-spine/28-CONTEXT.md (D-28-00 commit format; D-28-06 per-region stance defaults; "Integration Points" — preload bridge contract defined by shared types)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md — confirm `src/preload/src/index.ts` is NOT named in any of the 11 playbook gates (it is not)
    - grep for any `process.platform` or `os.platform()` in the conflict regions — preload runs in renderer context but has Node.js access; platform branches here are usually fork-side wins
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Open `src/preload/src/index.ts` and hand-resolve every conflict region. The file is the typed IPC bridge (see CONTEXT "Integration Points"): contextBridge.exposeInMainWorld, ipcRenderer.invoke wrappers, typed API namespaces. Stance: upstream-side wins for new IPC channels (additive feature scaffolding); fork-side wins for any platform-aware bridge code (Linux-only API exposure, platform-conditional handler registration).

    Watch for the bluebird Promise trap (CLAUDE.md memory): if upstream introduces async `:Promise<void>` in regions that import bluebird, drop the explicit return type per fork-side convention.

    After all markers resolved, stage ONLY this file. Commit with title `resolve(preload): index.ts — <stance>` in casual developer voice.

    Run `pnpm typecheck -F @vortex/preload` per D-28-03. Exit zero required. If typecheck fails, amend (`git commit --amend --no-edit`) before running the checkpoint script.

    Run `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check`. All fifteen non-conflict gates must pass.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/preload/src/index.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(preload\): index\.ts —' &amp;&amp; pnpm typecheck -F @vortex/preload &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' src/preload/src/index.ts` returns empty
    - `git grep -l '^<<<<<<< ' src/preload/` returns empty (entire preload bucket clean)
    - HEAD commit title starts with `resolve(preload): index.ts —`
    - HEAD commit touches exactly one file
    - HEAD commit is SSH-signed
    - `pnpm typecheck -F @vortex/preload` exits zero
    - `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
  </acceptance_criteria>
  <done>
    Preload bucket resolved; per-bucket typecheck clean; checkpoint stays green; 4/52 hand-resolved files done.
  </done>
</task>

</tasks>

<verification>
- File conflict-clean
- One atomic commit matching `resolve(preload): index.ts — <stance>`
- `pnpm typecheck -F @vortex/preload` passes
- `scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
</verification>

<success_criteria>

- 4/52 hand-resolved Phase 28 files done
- @vortex/preload bucket typechecks clean
- Per-bucket typecheck cadence (D-28-03) honoured

</success_criteria>

<output>
After completion, create `.planning/phases/28-renderer-main-spine/28-02-SUMMARY.md`
</output>
