---
phase: 28-renderer-main-spine
plan: 03
type: execute
wave: 4
depends_on: [28-02]
files_modified:
    - src/main/src/cli.ts
    - src/main/src/errorReporting.ts
    - src/main/src/extensions/autoupdater.ts
    - src/main/src/store/DuckDBSingleton.ts
    - src/main/src/store/LevelPersist.ts
    - src/main/src/TrayIcon.ts
    - src/main/src/Application.ts
autonomous: true
requirements: [SYNC-08]

must_haves:
    truths:
        - "Seven @vortex/main conflict files resolved per D-28-01: cli.ts → errorReporting.ts → extensions/autoupdater.ts → store/DuckDBSingleton.ts → store/LevelPersist.ts → TrayIcon.ts → Application.ts (last; main entry composes everything)"
        - "Seven atomic commits on `v8.0/config-bucket` each titled `resolve(main): <file> — <stance>` per D-28-00"
        - "After the seventh commit, `pnpm typecheck -F @vortex/main` exits zero (D-28-03 per-bucket cadence)"
        - "After EACH commit, `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero — §2/§4/§8/§9 + Phase 26/27 gates stay green; §9 (Steam.ts) is a gated-target file NOT in scope this phase but verified untouched after each commit"
    artifacts:
        - path: "src/main/src/cli.ts"
          provides: "Main-process command-line argument parsing — leaf 1/7"
          contains: "argv"
        - path: "src/main/src/errorReporting.ts"
          provides: "Crash reporter + telemetry-error glue — leaf 2/7"
          contains: "errorToReportableError"
        - path: "src/main/src/extensions/autoupdater.ts"
          provides: "electron-updater wrapper — leaf 3/7"
          contains: "autoUpdater"
        - path: "src/main/src/store/DuckDBSingleton.ts"
          provides: "DuckDB query-engine singleton — leaf 4/7"
          contains: "DuckDB"
        - path: "src/main/src/store/LevelPersist.ts"
          provides: "LevelDB persistence interface — leaf 5/7"
          contains: "LevelPersist"
        - path: "src/main/src/TrayIcon.ts"
          provides: "System tray UI integration — leaf 6/7"
          contains: "Tray"
        - path: "src/main/src/Application.ts"
          provides: "Main-process lifecycle composer (last per D-28-01 — composes all six leaves above)"
          contains: "class Application"
    key_links:
        - from: "Application.ts"
          to: "cli.ts, errorReporting.ts, autoupdater.ts, DuckDBSingleton.ts, LevelPersist.ts, TrayIcon.ts"
          via: "Application class instantiates and composes all main-side leaves — last in resolution order so leaf shapes settle first"
          pattern: "import .* from './(cli|errorReporting|TrayIcon)|store/(DuckDBSingleton|LevelPersist)|extensions/autoupdater'"
---

<objective>
Resolve the 7 conflict files in `src/main/src/` per D-28-01 leaf-first sub-order: `cli.ts` → `errorReporting.ts` → `extensions/autoupdater.ts` → `store/DuckDBSingleton.ts` → `store/LevelPersist.ts` → `TrayIcon.ts` → `Application.ts` (last). Seven atomic commits on `v8.0/config-bucket` matching D-28-00 title format `resolve(main): <file> — <stance>`. Per-bucket typecheck after the seventh commit per D-28-03. Run grep-checkpoint script with `--skip-conflict-check` after each commit.

Purpose: Third bucket per D-28-01. Main-process spine; depends on shared (settled in plan 28-01). Application.ts is last because it composes every other main-side leaf.

Output: Seven commits on `v8.0/config-bucket`. After plan 28-03 lands, 11/52 hand-resolved files done.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-renderer-main-spine/28-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve cli.ts (leaf 1/7) and commit</name>
  <read_first>
    - src/main/src/cli.ts (current state — every conflict region, in full)
    - `git show f15bbabb8:src/main/src/cli.ts` (base-commit reference)
    - .planning/phases/28-renderer-main-spine/28-CONTEXT.md (D-28-00 commit format; D-28-01 leaf-first ordering for main bucket; D-28-06 stance defaults)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md — confirm cli.ts is NOT named in any playbook gate
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Open `src/main/src/cli.ts` and hand-resolve every conflict region. CLI argument parsing — usually pure additive surface. Stance: upstream-side wins for new flags, fork-side wins for any Linux-specific argv handling.

    Watch for the bluebird Promise trap on async functions.

    After all markers resolved, stage ONLY this file. Commit with title `resolve(main): cli.ts — <stance>` in casual developer voice.

    Run `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check`. All fifteen non-conflict gates must pass.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/main/src/cli.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(main\): cli\.ts —' &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' src/main/src/cli.ts` returns empty
    - HEAD commit title starts with `resolve(main): cli.ts —`
    - HEAD commit touches exactly one file
    - HEAD commit is SSH-signed
    - `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
  </acceptance_criteria>
  <done>
    cli.ts resolved; one atomic commit lands; checkpoint stays green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolve errorReporting.ts (leaf 2/7) and commit</name>
  <read_first>
    - src/main/src/errorReporting.ts (current state — every conflict region, in full)
    - `git show f15bbabb8:src/main/src/errorReporting.ts` (base-commit reference)
    - src/shared/src/errors.ts (just-resolved upstream of dep tree — errorReporting may import error classes)
  </read_first>
  <action>
    Open `src/main/src/errorReporting.ts` and hand-resolve every conflict region. Crash reporter glue. Stance default. Watch for `errorToReportableError()`-shape additions (upstream-likely additive) vs Linux-specific error message formatting (fork-side wins).

    Stage ONLY this file. Commit with title `resolve(main): errorReporting.ts — <stance>`.

    Run `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check`. All fifteen non-conflict gates must pass.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/main/src/errorReporting.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(main\): errorReporting\.ts —' &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' src/main/src/errorReporting.ts` returns empty
    - HEAD commit title starts with `resolve(main): errorReporting.ts —`
    - HEAD commit touches exactly one file
    - HEAD commit is SSH-signed
    - `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
  </acceptance_criteria>
  <done>
    errorReporting.ts resolved; checkpoint stays green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Resolve extensions/autoupdater.ts (leaf 3/7) and commit</name>
  <read_first>
    - src/main/src/extensions/autoupdater.ts (current state — every conflict region, in full)
    - `git show f15bbabb8:src/main/src/extensions/autoupdater.ts` (base-commit reference)
  </read_first>
  <action>
    Hand-resolve. electron-updater wrapper. Linux distribution path is AppImage/.deb (Phase 29 territory) — if upstream adds Windows-only update-channel handling, leave fork-side platform branch intact. Stance default.

    Stage ONLY this file. Commit with title `resolve(main): extensions/autoupdater.ts — <stance>`.

    Run `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check`.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/main/src/extensions/autoupdater.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(main\): extensions/autoupdater\.ts —' &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' src/main/src/extensions/autoupdater.ts` returns empty
    - HEAD commit title starts with `resolve(main): extensions/autoupdater.ts —`
    - HEAD commit touches exactly one file; SSH-signed
    - Checkpoint exits zero
  </acceptance_criteria>
  <done>
    autoupdater resolved.
  </done>
</task>

<task type="auto">
  <name>Task 4: Resolve store/DuckDBSingleton.ts (leaf 4/7) and commit</name>
  <read_first>
    - src/main/src/store/DuckDBSingleton.ts (current state — every conflict region, in full)
    - `git show f15bbabb8:src/main/src/store/DuckDBSingleton.ts` (base-commit reference)
    - scripts/download-duckdb-extensions.ts (currently has conflicts; same DuckDB family — read for context, but DO NOT resolve here; that's plan 28-08)
  </read_first>
  <action>
    Hand-resolve. DuckDB query engine singleton. Stance default. Watch for native module loading paths — DuckDB has platform-specific binaries; any fork-side Linux path handling wins.

    Stage ONLY this file. Commit with title `resolve(main): store/DuckDBSingleton.ts — <stance>`.

    Run `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check`.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/main/src/store/DuckDBSingleton.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(main\): store/DuckDBSingleton\.ts —' &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - File conflict-clean; HEAD title starts with `resolve(main): store/DuckDBSingleton.ts —`; one file; SSH-signed; checkpoint zero
  </acceptance_criteria>
  <done>
    DuckDBSingleton resolved.
  </done>
</task>

<task type="auto">
  <name>Task 5: Resolve store/LevelPersist.ts (leaf 5/7) and commit</name>
  <read_first>
    - src/main/src/store/LevelPersist.ts (current state — every conflict region, in full)
    - `git show f15bbabb8:src/main/src/store/LevelPersist.ts` (base-commit reference)
    - src/main/src/store/DuckDBSingleton.ts (just-resolved final shape — store layer cohesion)
  </read_first>
  <action>
    Hand-resolve. LevelDB persistence interface (legacy; being phased out per CLAUDE.md but still active). Stance default — upstream-side likely additive (new hive types, new diff handling).

    Stage ONLY this file. Commit with title `resolve(main): store/LevelPersist.ts — <stance>`.

    Run `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check`.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/main/src/store/LevelPersist.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(main\): store/LevelPersist\.ts —' &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - File conflict-clean; HEAD title starts with `resolve(main): store/LevelPersist.ts —`; one file; SSH-signed; checkpoint zero
  </acceptance_criteria>
  <done>
    LevelPersist resolved.
  </done>
</task>

<task type="auto">
  <name>Task 6: Resolve TrayIcon.ts (leaf 6/7) and commit</name>
  <read_first>
    - src/main/src/TrayIcon.ts (current state — every conflict region, in full)
    - `git show f15bbabb8:src/main/src/TrayIcon.ts` (base-commit reference)
  </read_first>
  <action>
    Hand-resolve. System tray. Linux tray-icon support is platform-flaky historically — if fork-side carries any Linux-specific tray fallback, fork-side wins. Stance default for additive menu items.

    Stage ONLY this file. Commit with title `resolve(main): TrayIcon.ts — <stance>`.

    Run `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check`.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/main/src/TrayIcon.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(main\): TrayIcon\.ts —' &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - File conflict-clean; HEAD title starts with `resolve(main): TrayIcon.ts —`; one file; SSH-signed; checkpoint zero
  </acceptance_criteria>
  <done>
    TrayIcon resolved.
  </done>
</task>

<task type="auto">
  <name>Task 7: Resolve Application.ts (leaf 7/7, last per D-28-01), commit, and run per-bucket typecheck</name>
  <read_first>
    - src/main/src/Application.ts (current state — every conflict region, in full; this is the heaviest main file per CONTEXT "Integration Points")
    - `git show f15bbabb8:src/main/src/Application.ts` (base-commit reference)
    - src/main/src/{cli,errorReporting,TrayIcon}.ts (just-resolved final shapes — Application composes them)
    - src/main/src/extensions/autoupdater.ts (just-resolved final shape)
    - src/main/src/store/{DuckDBSingleton,LevelPersist}.ts (just-resolved final shapes)
    - .planning/phases/28-renderer-main-spine/28-CONTEXT.md ("Integration Points" — Application.ts composes main-side init: persistence, extension manager, main window, auto-updater, crash reporting; last in main order)
  </read_first>
  <action>
    Hand-resolve. Application class lifecycle: init → ready → shutdown. Composes all six previous main-side leaves. Hand-read every conflict region; symbol shapes from leaves 1–6 must align. Stance: fork-side wins for any Linux-specific init/shutdown sequencing (e.g. AppImage/sandbox detection); upstream wins for additive lifecycle hooks (new persistence step, new extension-manager init phase).

    Watch for the bluebird Promise trap — Application.ts historically imports bluebird Promise.

    Stage ONLY this file. Commit with title `resolve(main): Application.ts — <stance>`.

    Run `pnpm typecheck -F @vortex/main` per D-28-03. Exit zero required. If typecheck fails, amend (`git commit --amend --no-edit`) before running the checkpoint script.

    Run `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check`. All fifteen non-conflict gates must pass.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/main/src/Application.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(main\): Application\.ts —' &amp;&amp; pnpm typecheck -F @vortex/main &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' src/main/src/Application.ts` returns empty
    - `git grep -l '^<<<<<<< ' src/main/src/` returns empty (entire main bucket clean — preload is separate)
    - HEAD commit title starts with `resolve(main): Application.ts —`
    - HEAD commit touches exactly one file; SSH-signed
    - `pnpm typecheck -F @vortex/main` exits zero
    - `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero
    - `git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\(main\):'` returns 7
  </acceptance_criteria>
  <done>
    All seven main files resolved; per-bucket typecheck clean; checkpoint stays green; 11/52 hand-resolved files done.
  </done>
</task>

</tasks>

<verification>
- All seven files conflict-clean
- Seven atomic commits matching `resolve(main): <file> — <stance>`
- `pnpm typecheck -F @vortex/main` passes
- `scripts/grep-checkpoint.sh --skip-conflict-check` exits zero after each commit
</verification>

<success_criteria>

- 11/52 hand-resolved Phase 28 files done
- @vortex/main bucket typechecks clean
- Per-bucket typecheck cadence (D-28-03) honoured
- Application.ts last per D-28-01 re-export-composer idiom

</success_criteria>

<output>
After completion, create `.planning/phases/28-renderer-main-spine/28-03-SUMMARY.md`
</output>
