---
phase: 28-renderer-main-spine
plan: 05
type: execute
wave: 6
depends_on: [28-04]
files_modified:
    - src/renderer/src/extensions/browse_nexus/views/BrowseNexusPage.tsx
    - src/renderer/src/extensions/extension_manager/installExtension.ts
    - src/renderer/src/extensions/gamemode_management/views/GameRow.tsx
    - src/renderer/src/extensions/health_check/checks/modRequirementsCheck.ts
    - src/renderer/src/extensions/health_check/components/mod_requirement/ModRequirement.tsx
    - src/renderer/src/extensions/health_check/views/HealthCheckPage.tsx
    - src/renderer/src/extensions/installer_fomod_native/installer.ts
    - src/renderer/src/extensions/nexus_integration/util/UIDs.ts
    - src/renderer/src/extensions/nexus_integration/util.ts
    - src/renderer/src/extensions/nexus_integration/eventHandlers.ts
    - src/renderer/src/extensions/nexus_integration/views/FreeUserDLDialog.tsx
    - src/renderer/src/extensions/nexus_integration/selectors.test.ts
    - src/renderer/src/extensions/nexus_integration/index.tsx
    - src/renderer/src/extensions/starter_dashlet/actions.ts
autonomous: true
requirements: [SYNC-07, SYNC-09]

must_haves:
    truths:
        - "Fourteen renderer extension conflict files resolved per D-28-01 sub-order: browse_nexus → extension_manager → gamemode_management → health_check (3) → installer_fomod_native → nexus_integration (6, leaf-first within: util/UIDs.ts → util.ts → eventHandlers.ts → views/FreeUserDLDialog.tsx → selectors.test.ts → index.tsx) → starter_dashlet"
        - "Fourteen atomic commits on `v8.0/config-bucket` per D-28-00; commit-scope tags by extension family: `resolve(renderer): <file> — <stance>` for non-nexus extensions, `resolve(nexus): <file> — <stance>` for nexus_integration files (the dedicated `nexus` scope is enumerated in D-28-00's allowed scope list)"
        - "After EACH commit, `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero — §2/§4/§8/§9 + Phase 26/27 gates stay green"
        - "Per-bucket typecheck for @vortex/renderer is deferred to plan 28-07 (after ExtensionManager.ts) per D-28-03"
    artifacts:
        - path: "src/renderer/src/extensions/nexus_integration/util/UIDs.ts"
          provides: "Nexus UID utilities — leaf 1/6 within nexus_integration"
          contains: "uid"
        - path: "src/renderer/src/extensions/nexus_integration/util.ts"
          provides: "Nexus shared utility helpers — leaf 2/6"
          contains: "util"
        - path: "src/renderer/src/extensions/nexus_integration/eventHandlers.ts"
          provides: "Nexus IPC event handlers — leaf 3/6"
          contains: "eventHandler"
        - path: "src/renderer/src/extensions/nexus_integration/views/FreeUserDLDialog.tsx"
          provides: "Free-user download dialog component — leaf 4/6"
          contains: "FreeUserDL"
        - path: "src/renderer/src/extensions/nexus_integration/selectors.test.ts"
          provides: "Nexus selectors test — leaf 5/6"
          contains: "describe"
        - path: "src/renderer/src/extensions/nexus_integration/index.tsx"
          provides: "Nexus extension barrel — leaf 6/6 (last per D-28-01 re-export-composer idiom; executor MAY invert with selectors.test.ts per D-28-06 Claude's Discretion)"
          contains: "init"
    key_links:
        - from: "nexus_integration/index.tsx"
          to: "nexus_integration/util/UIDs.ts, util.ts, eventHandlers.ts, views/FreeUserDLDialog.tsx"
          via: "Barrel re-exports + init() composes the rest of the nexus_integration leaves — last in nexus sub-order per re-export idiom"
          pattern: "from './(util|eventHandlers|views/FreeUserDLDialog)'"
        - from: "renderer extensions"
          to: "renderer/views (plan 28-06) and ExtensionManager.ts (plan 28-07)"
          via: "Extensions register via context API; ExtensionManager.ts composes; views use extension-registered components"
          pattern: "context.register"
---

<objective>
Resolve the 14 renderer extension conflict files per D-28-01 sub-order (alphabetical-by-extension, leaf-first within each): browse_nexus → extension_manager → gamemode_management → health_check (modRequirementsCheck.ts → ModRequirement.tsx → HealthCheckPage.tsx) → installer_fomod_native → nexus_integration (util/UIDs.ts → util.ts → eventHandlers.ts → views/FreeUserDLDialog.tsx → selectors.test.ts → index.tsx) → starter_dashlet/actions.ts. Fourteen atomic commits on `v8.0/config-bucket` matching D-28-00. Run grep-checkpoint script with `--skip-conflict-check` after each commit. Per-bucket typecheck deferred to plan 28-07.

Purpose: Fifth bucket per D-28-01. Renderer extensions; depend on renderer non-extension leaves (settled in 28-04). nexus_integration is the heaviest sub-bucket and gets its own scope tag (`resolve(nexus): …`) per D-28-00. ExtensionManager.ts is NOT in this plan — it lands in 28-07 last per CONTEXT "Integration Points".

Output: Fourteen commits on `v8.0/config-bucket`. After plan 28-05 lands, 34/52 hand-resolved files done.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-renderer-main-spine/28-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve browse_nexus, extension_manager, gamemode_management, health_check (3 files), installer_fomod_native — 7 commits</name>
  <read_first>
    - src/renderer/src/extensions/browse_nexus/views/BrowseNexusPage.tsx (every conflict region, in full)
    - src/renderer/src/extensions/extension_manager/installExtension.ts (every conflict region, in full)
    - src/renderer/src/extensions/gamemode_management/views/GameRow.tsx (every conflict region, in full)
    - src/renderer/src/extensions/health_check/checks/modRequirementsCheck.ts (every conflict region, in full)
    - src/renderer/src/extensions/health_check/components/mod_requirement/ModRequirement.tsx (every conflict region, in full)
    - src/renderer/src/extensions/health_check/views/HealthCheckPage.tsx (every conflict region, in full)
    - src/renderer/src/extensions/installer_fomod_native/installer.ts (every conflict region, in full)
    - `git show f15bbabb8:` versions for all 7 (base-commit references)
    - .planning/phases/28-renderer-main-spine/28-CONTEXT.md (D-28-01 sub-order; D-28-06 stance defaults; "out of scope" — installer_fomod_native is the .NET 9 recompile path per CLAUDE.md project constraints, NOT the Wine wrapper — fork-side Linux-aware path handling wins)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md — confirm none of these 7 files are named in any playbook gate (they are not; nexus_integration files in Task 2 are not gated either — the gates target source files OUTSIDE Phase 28's resolution list per D-28-06)
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Resolve each file in order, ONE COMMIT PER FILE per D-28-00. Default stance D-28-06; bluebird Promise trap watch. Run `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check` after EACH commit.

    1. `src/renderer/src/extensions/browse_nexus/views/BrowseNexusPage.tsx` → commit `resolve(renderer): extensions/browse_nexus/views/BrowseNexusPage.tsx — <stance>`.
    2. `src/renderer/src/extensions/extension_manager/installExtension.ts` → commit `resolve(renderer): extensions/extension_manager/installExtension.ts — <stance>`. Watch for tar/zip extraction code carrying Linux file-permission handling (chmod) — fork-side wins.
    3. `src/renderer/src/extensions/gamemode_management/views/GameRow.tsx` → commit `resolve(renderer): extensions/gamemode_management/views/GameRow.tsx — <stance>`.
    4. `src/renderer/src/extensions/health_check/checks/modRequirementsCheck.ts` → commit `resolve(renderer): extensions/health_check/checks/modRequirementsCheck.ts — <stance>`.
    5. `src/renderer/src/extensions/health_check/components/mod_requirement/ModRequirement.tsx` → commit `resolve(renderer): extensions/health_check/components/mod_requirement/ModRequirement.tsx — <stance>`.
    6. `src/renderer/src/extensions/health_check/views/HealthCheckPage.tsx` → commit `resolve(renderer): extensions/health_check/views/HealthCheckPage.tsx — <stance>`. Page composes the check + component above; resolve last within health_check.
    7. `src/renderer/src/extensions/installer_fomod_native/installer.ts` → commit `resolve(renderer): extensions/installer_fomod_native/installer.ts — <stance>`. **CRITICAL** — per CLAUDE.md project constraint "FOMOD: .NET 9 recompile path chosen — Wine wrapper explicitly rejected", any fork-side .NET-runtime-locator code wins. If upstream re-introduces a Wine wrapper conditional, fork-side rejects it.

    Stage only the named file per commit. SSH-signed. Casual developer voice in commit body.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/browse_nexus/ src/renderer/src/extensions/extension_manager/installExtension.ts src/renderer/src/extensions/gamemode_management/views/GameRow.tsx src/renderer/src/extensions/health_check/ src/renderer/src/extensions/installer_fomod_native/installer.ts; test $? -ne 0 &amp;&amp; test $(git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\(renderer\): extensions/(browse_nexus|extension_manager|gamemode_management|health_check|installer_fomod_native)/') -ge 7 &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - All 7 named files conflict-clean
    - Seven new commits matching `resolve(renderer): extensions/<path> —`
    - Each commit touches exactly one file; SSH-signed
    - Checkpoint exits zero after each commit
    - installer_fomod_native commit body explicitly notes fork-side .NET 9 path retained (no Wine wrapper)
  </acceptance_criteria>
  <done>
    Seven non-nexus extension files resolved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolve nexus_integration leaves (6 files) in D-28-01 sub-order, scope tag `resolve(nexus): ...`</name>
  <read_first>
    - src/renderer/src/extensions/nexus_integration/util/UIDs.ts (every conflict region, in full)
    - src/renderer/src/extensions/nexus_integration/util.ts (every conflict region, in full)
    - src/renderer/src/extensions/nexus_integration/eventHandlers.ts (every conflict region, in full)
    - src/renderer/src/extensions/nexus_integration/views/FreeUserDLDialog.tsx (every conflict region, in full)
    - src/renderer/src/extensions/nexus_integration/selectors.test.ts (every conflict region, in full)
    - src/renderer/src/extensions/nexus_integration/index.tsx (every conflict region, in full — the barrel)
    - `git show f15bbabb8:` versions for all 6 (base-commit references)
    - .planning/phases/28-renderer-main-spine/28-CONTEXT.md (D-28-01 sub-order — UIDs → util → eventHandlers → FreeUserDLDialog → selectors.test → index; D-28-06 Claude's Discretion — selectors.test.ts and index.tsx ordering may be inverted if selectors.test.ts conflict shape is lighter; D-28-00 — `nexus` is an explicit allowed scope tag)
    - .planning/phases/27-gamebryo-per-game-extensions/27-CONTEXT.md (D-27-01 — re-export composer idiom: barrel last in sub-order)
  </read_first>
  <action>
    Resolve each file in order, ONE COMMIT PER FILE per D-28-00. Scope tag is `resolve(nexus): …` (per D-28-00 explicit allowed scope list: `shared, preload, main, renderer, nexus, scripts, docs`). Default stance D-28-06; bluebird Promise trap watch. Checkpoint after each commit.

    1. `src/renderer/src/extensions/nexus_integration/util/UIDs.ts` → commit `resolve(nexus): util/UIDs.ts — <stance>`.
    2. `src/renderer/src/extensions/nexus_integration/util.ts` → commit `resolve(nexus): util.ts — <stance>`. Imports UIDs.ts shape (just resolved).
    3. `src/renderer/src/extensions/nexus_integration/eventHandlers.ts` → commit `resolve(nexus): eventHandlers.ts — <stance>`. IPC handlers — watch for nxm:// link handling (Linux MIME-type registration is fork-side per playbook history).
    4. `src/renderer/src/extensions/nexus_integration/views/FreeUserDLDialog.tsx` → commit `resolve(nexus): views/FreeUserDLDialog.tsx — <stance>`.
    5. `src/renderer/src/extensions/nexus_integration/selectors.test.ts` → commit `resolve(nexus): selectors.test.ts — <stance>`.

    **Executor discretion (per D-28-06 Claude's Discretion bullet 3):** If `selectors.test.ts` carries a lighter conflict shape than `index.tsx` and inverting the order produces a cleaner per-commit diff, document the deviation in this commit body and proceed in inverted order. Default is selectors.test.ts BEFORE index.tsx.

    6. `src/renderer/src/extensions/nexus_integration/index.tsx` → commit `resolve(nexus): index.tsx — <stance>`. Barrel; composes all five leaves above. **Hand-read every conflict region** — index.tsx is the heaviest nexus_integration file. Stance default; if upstream restructures imports, fork-side wins for any Linux-aware module path (e.g. asar.unpacked-aware require) and upstream wins for additive `context.register*` calls.

    Stage only the named file per commit. SSH-signed.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/nexus_integration/; test $? -ne 0 &amp;&amp; test $(git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\(nexus\): (util/UIDs|util|eventHandlers|views/FreeUserDLDialog|selectors\.test|index)\.(ts|tsx)') -eq 6 &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - All 6 nexus files conflict-clean
    - Six new commits matching `resolve(nexus): <file> —`
    - Each commit touches exactly one file; SSH-signed
    - Checkpoint exits zero after each commit
    - `git grep -l '^<<<<<<< ' src/renderer/src/extensions/nexus_integration/` returns empty (entire nexus_integration sub-bucket clean)
  </acceptance_criteria>
  <done>
    nexus_integration sub-bucket fully resolved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Resolve starter_dashlet/actions.ts (extensions tail) and commit</name>
  <read_first>
    - src/renderer/src/extensions/starter_dashlet/actions.ts (every conflict region, in full)
    - `git show f15bbabb8:src/renderer/src/extensions/starter_dashlet/actions.ts` (base-commit reference)
  </read_first>
  <action>
    Hand-resolve. Starter dashlet redux actions; pure additive surface usually. Stance default.

    Commit `resolve(renderer): extensions/starter_dashlet/actions.ts — <stance>`. Run checkpoint `--skip-conflict-check`.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/starter_dashlet/actions.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qE '^resolve\(renderer\): extensions/starter_dashlet/actions\.ts —' &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - File conflict-clean
    - HEAD commit title starts with `resolve(renderer): extensions/starter_dashlet/actions.ts —`
    - One file; SSH-signed
    - Checkpoint exits zero
    - `git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\((renderer|nexus)\): extensions?/'` returns 14 (this plan's full output: 7 renderer-extensions + 6 nexus + 1 starter_dashlet)
  </acceptance_criteria>
  <done>
    All 14 renderer extension files resolved; 14 atomic commits land; 34/52 hand-resolved files done.
  </done>
</task>

</tasks>

<verification>
- All 14 files conflict-clean
- Fourteen atomic commits matching `resolve(renderer): extensions/<file> —` or `resolve(nexus): <file> —`
- `scripts/grep-checkpoint.sh --skip-conflict-check` exits zero after each commit
- Per-bucket typecheck deferred to plan 28-07 per D-28-03
</verification>

<success_criteria>

- 34/52 hand-resolved Phase 28 files done
- nexus_integration sub-bucket settled before views (plan 28-06) and ExtensionManager.ts (plan 28-07)
- D-28-06 Claude's Discretion (selectors.test.ts vs index.tsx ordering) documented if exercised

</success_criteria>

<output>
After completion, create `.planning/phases/28-renderer-main-spine/28-05-SUMMARY.md`
</output>
