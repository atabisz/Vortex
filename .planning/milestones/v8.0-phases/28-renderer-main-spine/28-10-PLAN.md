---
phase: 28-renderer-main-spine
plan: 10
type: execute
wave: 11
depends_on: [28-09]
files_modified:
    - CHANGELOG.md
    - docs/cherry-pick-workflow.md
    - docs/error-reporting/critical-errors.md
    - etc/Dependency Report.md
    - etc/vortex.api.md
autonomous: true
requirements: [SYNC-08]

must_haves:
    truths:
        - "Five doc-borderline conflict files resolved per D-28-01: CHANGELOG.md → docs/cherry-pick-workflow.md → docs/error-reporting/critical-errors.md → etc/Dependency Report.md → etc/vortex.api.md"
        - "Per D-28-00 default — five atomic commits each titled `resolve(docs): <file> — <stance>`. Per D-28-06 Claude's Discretion bullet 5 — executor MAY bundle into a single `resolve(docs): batch upstream-merge` commit since these files are outside the formal success-criteria paths. Document the chosen mode in plan output."
        - "After EACH commit (whether 5 commits or 1 batch commit), `bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero"
        - "Default stance for docs is upstream-side wins (CHANGELOG entries, doc-merge boilerplate). Fork-side wins for any docs/cherry-pick-workflow.md content that captures fork-specific cherry-pick procedure (the fork's branch-strategy doc analogue)."
    artifacts:
        - path: "CHANGELOG.md"
          provides: "Project changelog — upstream-likely additive (new release entries)"
          contains: "Changelog"
        - path: "docs/cherry-pick-workflow.md"
          provides: "Cherry-pick procedure doc — fork-side wins for fork-specific procedure content"
          contains: "cherry-pick"
        - path: "docs/error-reporting/critical-errors.md"
          provides: "Critical-error documentation"
          contains: "error"
        - path: "etc/Dependency Report.md"
          provides: "Dependency report — upstream-likely additive"
          contains: "Dependency"
        - path: "etc/vortex.api.md"
          provides: "Public API reference — upstream-likely additive (api-extractor output)"
          contains: "vortex"
    key_links:
        - from: "doc borderlines"
          to: "Phase 28 working tree merge-cleanliness"
          via: "Per CONTEXT D-28-01 — 'leaving them blocks the working tree from being merge-clean for Phase 29' even though they're outside formal success-criteria paths"
          pattern: ""
---

<objective>
Resolve the 5 doc-borderline conflict files per D-28-01 sub-order: `CHANGELOG.md` → `docs/cherry-pick-workflow.md` → `docs/error-reporting/critical-errors.md` → `etc/Dependency Report.md` → `etc/vortex.api.md`. Default per D-28-00 is per-file commits titled `resolve(docs): <file> — <stance>`. Per D-28-06 Claude's Discretion bullet 5, executor MAY bundle into a single `resolve(docs): batch upstream-merge` commit since these files are outside the formal success-criteria paths. Run grep-checkpoint script with `--skip-conflict-check` after each commit.

Purpose: Tenth bucket per D-28-01. Doc borderlines — not in success-criteria paths but blocking working-tree merge-cleanliness for Phase 29. Hand-resolve here so Phase 29 starts clean.

Output: Either 5 per-file commits (default) or 1 batch commit (executor discretion) on `v8.0/config-bucket`. After plan 28-10 lands, all 63 conflict files resolved; only the done-gate remains (plan 28-11).
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-renderer-main-spine/28-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve all 5 doc borderline files (per-file commits OR batch commit per executor discretion)</name>
  <read_first>
    - CHANGELOG.md (every conflict region, in full)
    - docs/cherry-pick-workflow.md (every conflict region, in full)
    - docs/error-reporting/critical-errors.md (every conflict region, in full)
    - etc/Dependency Report.md (every conflict region, in full)
    - etc/vortex.api.md (every conflict region, in full)
    - .planning/phases/28-renderer-main-spine/28-CONTEXT.md (D-28-00 default per-file; D-28-06 Claude's Discretion bullet 5 — batch acceptable; D-28-01 ordering)
    - CLAUDE.md "Branch Strategy" — docs/cherry-pick-workflow.md may carry fork-specific procedure that wins over upstream content; read the section to verify what's fork-specific in this fork's context
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Choose mode at execution time:

    **Mode A (default, per D-28-00):** Five atomic commits, one per file. Default stance: upstream-side wins for additive doc content (new CHANGELOG entries, new API doc surface); fork-side wins for `docs/cherry-pick-workflow.md` content that captures fork-specific cherry-pick procedure (the fork's branch-strategy analogue per CLAUDE.md "Branch Strategy"). Per-file commit titles: `resolve(docs): <file> — <stance>`. **Use `git add -f`** for any file under `.planning/` paths if applicable (per CLAUDE.md memory ".planning/ is gitignored") — none of these 5 files are under `.planning/` so `git add` without `-f` is fine.

    Order:
    1. `CHANGELOG.md` → commit `resolve(docs): CHANGELOG.md — <stance>`. Run checkpoint.
    2. `docs/cherry-pick-workflow.md` → commit `resolve(docs): docs/cherry-pick-workflow.md — <stance>`. Watch for fork-specific procedure content; fork-side wins where it captures the linux-port branch strategy. Run checkpoint.
    3. `docs/error-reporting/critical-errors.md` → commit `resolve(docs): docs/error-reporting/critical-errors.md — <stance>`. Run checkpoint.
    4. `etc/Dependency Report.md` → commit `resolve(docs): etc/Dependency Report.md — <stance>`. Run checkpoint.
    5. `etc/vortex.api.md` → commit `resolve(docs): etc/vortex.api.md — <stance>`. (api-extractor output — upstream-side likely matches the post-merge API surface; default upstream-wins.) Run checkpoint.

    **Mode B (per D-28-06 Claude's Discretion bullet 5):** Single batch commit titled `resolve(docs): batch upstream-merge` covering all 5 files. Acceptable since docs are outside formal success-criteria paths. Document the per-file stance in the commit body. Stage all 5 files; one commit; one checkpoint run after.

    Choose Mode A by default. Choose Mode B only if all 5 files have lightweight conflict shapes (mostly trivial CHANGELOG/api-doc additive merges) where 5 separate commits would inflate the log without explanatory value. Document the chosen mode at the top of the SUMMARY.md.

    All commits SSH-signed; casual developer voice.

    Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' CHANGELOG.md docs/cherry-pick-workflow.md docs/error-reporting/critical-errors.md "etc/Dependency Report.md" etc/vortex.api.md; test $? -ne 0 &amp;&amp; test $(git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -cE '^[0-9a-f]+ resolve\(docs\):') -ge 1 &amp;&amp; bash .planning/milestones/v8.0/scripts/grep-checkpoint.sh --skip-conflict-check</automated>
  </verify>
  <acceptance_criteria>
    - All 5 doc files conflict-clean
    - Either 5 atomic commits each titled `resolve(docs): <file> —` (Mode A) OR 1 commit titled `resolve(docs): batch upstream-merge` (Mode B)
    - Each commit SSH-signed
    - Checkpoint exits zero after each commit
    - SUMMARY.md documents which mode was chosen and why
  </acceptance_criteria>
  <done>
    All 5 doc borderlines resolved; working tree merge-clean for Phase 29.
  </done>
</task>

</tasks>

<verification>
- All 5 files conflict-clean
- Either 5 per-file commits (`resolve(docs): <file> —`) or 1 batch commit (`resolve(docs): batch upstream-merge`)
- `scripts/grep-checkpoint.sh --skip-conflict-check` exits zero after each commit
- SUMMARY documents executor's mode choice
</verification>

<success_criteria>

- All 63 Phase 28 conflict files resolved (52 hand + 11 fingerprints squash + 0 still-pending)
- Doc borderlines settled — Phase 29 starts with merge-clean tree
- D-28-06 Claude's Discretion exercise documented if applied

</success_criteria>

<output>
After completion, create `.planning/phases/28-renderer-main-spine/28-10-SUMMARY.md`
</output>
