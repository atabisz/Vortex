---
phase: 29-build-verification
plan: 04
subsystem: test
type: execute
wave: 5
depends_on: [29-03]
files_modified:
    - .planning/phases/29-build-verification/29-VERIFY-RESULTS.md
autonomous: true
requirements: [SYNC-31]
tags: [linux-port, upstream-v2.0.0, phase-29, sync-31, vitest, test]

requirements_in_play: [SYNC-31]

dependency_graph:
    requires:
        - "29-03 complete (SYNC-21 + SYNC-30 PASS)"
    provides:
        - "29-VERIFY-RESULTS.md (SYNC-31 section)"
    affects:
        - "29-05 cascade"
        - "29-10 (rolls up SYNC-31 evidence)"

gates:
    pre_conditions:
        - "29-03 PASS"
        - "Working tree clean"
    post_conditions:
        - "pnpm test exits 0; final test summary captured"
        - "29-VERIFY-RESULTS.md SYNC-31 section PASS"
        - "HEAD commit title is `docs(29-04): SYNC-31 — pnpm test (Vitest)`"
        - "HEAD commit SSH-signed; pushed"

commands:
    - "git status --short"
    - "time pnpm test 2>&1 | tee /tmp/test.log"
    - "tail -30 /tmp/test.log"
    - "git add -f .planning/phases/29-build-verification/29-VERIFY-RESULTS.md"
    - "git commit -m 'docs(29-04): SYNC-31 — pnpm test (Vitest)'"
    - "git log -1 --show-signature"
    - "git push git@github.com:atabisz/Vortex.git v8.0/config-bucket:sync/upstream-v2.0.0"

verification:
    - "pnpm test exits 0"
    - "Final Vitest summary line captured (e.g. 'Test Files X passed (X) | Tests N passed (N)')"
    - "29-VERIFY-RESULTS.md SYNC-31 section PASS"

must_haves:
    truths:
        - "`pnpm test` (Vitest) exits 0 across all configured test projects per SYNC-31"
        - "Renderer-no-Jest divergence is acknowledged — `pnpm test` runs Vitest only; Jest runs are out of scope for SYNC-31 per CONTEXT phase boundary"
        - "Final Vitest summary line captured verbatim (Test Files / Tests counts) into 29-VERIFY-RESULTS.md"
        - "Lockfile drift contingency per CONTEXT R1: if `pnpm install` step inside test setup drifts the lockfile, the fix is `pnpm install` (no --frozen-lockfile) and a separate `fix(29-04): regenerate lockfile post-merge` commit BEFORE the docs commit"
        - "On non-zero exit (after lockfile-drift contingency tried once), STOP and surface — D-29-01 cascade halts here"
        - "Single docs commit `docs(29-04): SYNC-31 — pnpm test (Vitest)` lands and is pushed"
    artifacts:
        - path: ".planning/phases/29-build-verification/29-VERIFY-RESULTS.md"
          provides: "SYNC-31 section appended"
          contains: "## SYNC-31"
    key_links:
        - from: "29-VERIFY-RESULTS.md SYNC-31 section"
          to: "29-10-DONE-GATE.md"
          via: "Done-gate quotes SYNC-31 evidence"
          pattern: "SYNC-31"
---

<objective>
Run `pnpm test` (Vitest). Capture exit code + final test-suite summary into `29-VERIFY-RESULTS.md`. Honour CONTEXT R1 lockfile-drift contingency.

Purpose: SYNC-31 — the merged tree's test suite must pass. The renderer-no-Jest divergence (renderer's Jest config is intentionally not part of `pnpm test`) is a Phase 28-era reality acknowledged in the project conventions; SYNC-31 explicitly scopes to Vitest.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-build-verification/29-CONTEXT.md
@.planning/phases/29-build-verification/29-VERIFY-RESULTS.md
@CLAUDE.md
</context>

## Goal

`pnpm test` exit 0; final Vitest summary captured; SYNC-31 PASS; one signed docs commit pushed.

## Steps

1. **Confirm starting state.**

    ```
    git rev-parse --abbrev-ref HEAD
    git status --short
    ```

2. **Run `pnpm test`.**

    ```
    time pnpm test 2>&1 | tee /tmp/test.log
    echo "exit=${PIPESTATUS[0]}"
    tail -30 /tmp/test.log
    ```

    Expected: exit 0, final line(s) showing `Test Files X passed (X) | Tests N passed (N)` (Vitest format).

3. **Lockfile-drift contingency (CONTEXT R1).** If `pnpm test` fails AND the failure is `pnpm install --frozen-lockfile` rejecting the current lockfile state:

    ```
    pnpm install
    git status --short                    # expect: pnpm-lock.yaml modified
    git add pnpm-lock.yaml
    git commit -m 'fix(29-04): regenerate lockfile post-merge'
    git log -1 --show-signature
    git push git@github.com:atabisz/Vortex.git v8.0/config-bucket:sync/upstream-v2.0.0
    ```

    Then retry `pnpm test`. If retry passes, continue with the docs commit. If retry fails, STOP and surface — that's a real test failure, not lockfile drift.
    This is a one-shot contingency. Document any usage in 29-VERIFY-RESULTS.md SYNC-31 section.

4. **Append SYNC-31 section to `29-VERIFY-RESULTS.md`.** Include:
    - Command, exit code, wall-clock time
    - Final Vitest summary line(s) verbatim
    - Last 30 lines of stdout
    - Note any lockfile contingency commit (with SHA) if it ran
    - PASS/FAIL

5. **Commit and push.**
    ```
    git add -f .planning/phases/29-build-verification/29-VERIFY-RESULTS.md
    git commit -m 'docs(29-04): SYNC-31 — pnpm test (Vitest)'
    git log -1 --show-signature
    git push git@github.com:atabisz/Vortex.git v8.0/config-bucket:sync/upstream-v2.0.0
    ```

## Verification

- `pnpm test` exited 0 (after at most one lockfile-regen retry)
- 29-VERIFY-RESULTS.md SYNC-31 section PASS with Vitest summary line
- HEAD commit title exact match
- HEAD commit SSH-signed
- Remote pushed

## Commit

Title: `docs(29-04): SYNC-31 — pnpm test (Vitest)`

Body:

```
pnpm test → exit 0. Final Vitest summary: <quoted line>.
Renderer-Jest scope per project conventions; SYNC-31 covers Vitest only.

Per D-29-01 sequential cascade. Evidence in 29-VERIFY-RESULTS.md.
```

Signing: SSH.

## Deviations

CONTEXT R1: lockfile drift contingency. Single retry allowed; failure beyond that halts the cascade. Any test failure beyond lockfile is a Phase 28-bis trigger.

<output>
After completion, create `.planning/phases/29-build-verification/29-04-SUMMARY.md`.
</output>
