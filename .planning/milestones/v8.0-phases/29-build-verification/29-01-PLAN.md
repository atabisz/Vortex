---
phase: 29-build-verification
plan: 01
subsystem: typecheck-and-conflict-sweep
type: execute
wave: 2
depends_on: [29-00]
files_modified:
    - .planning/phases/29-build-verification/29-VERIFY-RESULTS.md
autonomous: true
requirements: [SYNC-01, SYNC-28]
tags: [linux-port, upstream-v2.0.0, phase-29, sync-01, sync-28, typecheck, conflict-sweep]

requirements_in_play: [SYNC-01, SYNC-28]

dependency_graph:
    requires:
        - "29-00 complete (29-LINT-BASELINE.md exists; branch is v8.0/config-bucket)"
    provides:
        - "29-VERIFY-RESULTS.md (SYNC-01 + SYNC-28 sections) — used by 29-10 done-gate"
    affects:
        - "29-02..29-05 cascade (D-29-01: stop the cascade on non-zero exit)"
        - "29-10 (rolls up SYNC-01 and SYNC-28 evidence)"

gates:
    pre_conditions:
        - "git rev-parse --abbrev-ref HEAD == v8.0/config-bucket"
        - "29-LINT-BASELINE.md exists with master half populated"
        - "Working tree clean: git status --short empty"
    post_conditions:
        - "29-VERIFY-RESULTS.md exists with SYNC-01 + SYNC-28 sections"
        - "git grep '^<<<<<<< ' across the entire repo returned zero hits (SYNC-01)"
        - "pnpm typecheck exited 0 across all workspaces (SYNC-28)"
        - "HEAD commit title is `docs(29-01): SYNC-01 + SYNC-28 — repo-wide zero conflicts + pnpm typecheck`"
        - "HEAD commit is SSH-signed"
        - "Remote sync/upstream-v2.0.0 HEAD matches local"

commands:
    - "git rev-parse --abbrev-ref HEAD"
    - "git status --short"
    - "git grep '^<<<<<<< '"
    - "pnpm typecheck 2>&1 | tee /tmp/typecheck.log"
    - "git add -f .planning/phases/29-build-verification/29-VERIFY-RESULTS.md"
    - "git commit -m 'docs(29-01): SYNC-01 + SYNC-28 — repo-wide zero conflicts + pnpm typecheck'"
    - "git log -1 --show-signature"
    - "git push git@github.com:atabisz/Vortex.git v8.0/config-bucket:sync/upstream-v2.0.0"

verification:
    - "git grep '^<<<<<<< ' returns non-zero exit (no matches) — quoted in 29-VERIFY-RESULTS.md"
    - "pnpm typecheck exit 0; positive signal (e.g. nx 'Successfully ran target typecheck for N projects' or equivalent) quoted"
    - "HEAD commit body cites SYNC-01 and SYNC-28 by ID"

must_haves:
    truths:
        - "Repo-wide `git grep '^<<<<<<< '` (no path scope, no --skip-conflict-check) returns zero hits — SYNC-01 satisfied"
        - "`pnpm typecheck` exits 0 across all workspaces with positive signal captured to 29-VERIFY-RESULTS.md — SYNC-28 satisfied"
        - "On non-zero exit of either check, STOP and surface — do NOT proceed to 29-02 (D-29-01 cascade-halt rule)"
        - "Single docs commit on v8.0/config-bucket; pushed to fork remote"
    artifacts:
        - path: ".planning/phases/29-build-verification/29-VERIFY-RESULTS.md"
          provides: "Aggregate verification results file — SYNC-01 + SYNC-28 sections written here; 29-02..29-05 will append their sections to the same file"
          contains: "## SYNC-01"
    key_links:
        - from: "29-VERIFY-RESULTS.md"
          to: "29-10-DONE-GATE.md"
          via: "Done-gate quotes per-script evidence from this file"
          pattern: "29-VERIFY-RESULTS.md"
---

<objective>
Run two checks in sequence per D-29-01:
- Repo-wide `git grep '^<<<<<<< '` (zero hits → SYNC-01).
- `pnpm typecheck` (exit 0 across all workspaces → SYNC-28).

Capture both into `29-VERIFY-RESULTS.md` and commit. On non-zero exit of either, halt the Phase 29 cascade per §Verify-script-rules.

Purpose: Phase 28 done-gate already proved zero conflict markers across the seven Phase 28 directories. SYNC-01 widens that to the whole repo (catches anything outside the conflict-resolution scope — extension subtrees, scripts, `.github/`, root-level files). SYNC-28 is the first compile-time gate: per-bucket typechecks ran in Phase 28's done-gate, but the unified `pnpm typecheck` at root catches drift between buckets that per-bucket runs miss.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-build-verification/29-CONTEXT.md
@.planning/phases/29-build-verification/29-LINT-BASELINE.md
@CLAUDE.md
</context>

## Goal

Produce evidence that SYNC-01 (repo-wide zero conflict markers) and SYNC-28 (`pnpm typecheck` clean) are satisfied on `v8.0/config-bucket` HEAD. Halt the cascade on any failure.

## Steps

1. **Confirm starting state.**

    ```
    git rev-parse --abbrev-ref HEAD       # expect: v8.0/config-bucket
    git status --short                    # expect: empty
    ```

2. **SYNC-01 — repo-wide zero conflict markers.** No path scope, no skip flags.

    ```
    git grep '^<<<<<<< '
    echo "exit=$?"
    ```

    Expected: exit 1 (no matches). If exit 0 (matches found), STOP and surface — Phase 28 missed something or upstream drifted in a path Phase 28 didn't scope.

3. **SYNC-28 — pnpm typecheck across all workspaces.**

    ```
    pnpm typecheck 2>&1 | tee /tmp/typecheck.log
    echo "exit=${PIPESTATUS[0]}"
    ```

    Expected: exit 0. Capture: tail of stdout (the nx summary line — "Successfully ran target typecheck for N projects" or equivalent) AND the full log to `/tmp/typecheck.log`. If non-zero exit, STOP and surface — the cascade halts here per D-29-01.

4. **Write `29-VERIFY-RESULTS.md`** (creating the file; later plans append). Sections:
    - `# Phase 29 verification results` (header with branch HEAD SHA, timestamp, environment versions)
    - `## SYNC-01 — repo-wide zero conflict markers` — exact command, exit code, output (empty), PASS/FAIL.
    - `## SYNC-28 — pnpm typecheck` — command, exit code, tail of stdout (positive signal quoted), PASS/FAIL. Reference `/tmp/typecheck.log` and quote the last 20 lines verbatim in a fenced block so evidence survives.
    - `## SYNC-29 — pnpm build` — placeholder `_To be filled by Plan 29-02._`
    - `## SYNC-30 + SYNC-21 — pnpm build:extensions` — placeholder `_To be filled by Plan 29-03._`
    - `## SYNC-31 — pnpm test` — placeholder `_To be filled by Plan 29-04._`
    - `## SYNC-32 — pnpm lint:ci` — placeholder `_To be filled by Plan 29-05._`

5. **Commit and push.**
    ```
    git add -f .planning/phases/29-build-verification/29-VERIFY-RESULTS.md
    git commit -m 'docs(29-01): SYNC-01 + SYNC-28 — repo-wide zero conflicts + pnpm typecheck'
    git log -1 --show-signature
    git push git@github.com:atabisz/Vortex.git v8.0/config-bucket:sync/upstream-v2.0.0
    ```

## Verification

- `git grep '^<<<<<<< '` returns non-zero exit (recorded in evidence)
- `pnpm typecheck` exits 0 (recorded in evidence with stdout tail)
- `29-VERIFY-RESULTS.md` exists with SYNC-01 and SYNC-28 sections marked PASS, plus the four placeholder sections for 29-02..29-05
- HEAD commit title matches exactly `docs(29-01): SYNC-01 + SYNC-28 — repo-wide zero conflicts + pnpm typecheck`
- HEAD commit is SSH-signed (`git log -1 --show-signature` shows `Good "git" signature`)
- Remote `sync/upstream-v2.0.0` matches local HEAD

## Commit

Title: `docs(29-01): SYNC-01 + SYNC-28 — repo-wide zero conflicts + pnpm typecheck`

Body:

```
SYNC-01: git grep '^<<<<<<< ' across whole repo → 0 hits.
SYNC-28: pnpm typecheck → exit 0, all workspaces clean.

Per D-29-01 sequential cascade. Evidence in 29-VERIFY-RESULTS.md.
```

Signing: SSH per CLAUDE.md memory. Confirm via `git log -1 --show-signature`.

## Deviations

None expected. If `pnpm typecheck` fails on a real bug, this is a Phase 28-bis trigger (per CONTEXT R-out-of-scope) — STOP, capture the failure in 29-VERIFY-RESULTS.md, surface to user, do not patch in this plan.

<output>
After completion, create `.planning/phases/29-build-verification/29-01-SUMMARY.md` per Phase 28 SUMMARY shape.
</output>
