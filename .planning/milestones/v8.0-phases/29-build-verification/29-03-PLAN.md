---
phase: 29-build-verification
plan: 03
subsystem: build-extensions
type: execute
wave: 4
depends_on: [29-02]
files_modified:
    - .planning/phases/29-build-verification/29-VERIFY-RESULTS.md
autonomous: true
requirements: [SYNC-21, SYNC-30]
tags: [linux-port, upstream-v2.0.0, phase-29, sync-21, sync-30, build-extensions, bundled-plugins]

requirements_in_play: [SYNC-21, SYNC-30]

dependency_graph:
    requires:
        - "29-02 complete (SYNC-29 PASS — pnpm build)"
    provides:
        - "29-VERIFY-RESULTS.md (SYNC-21 + SYNC-30 sections)"
        - "src/main/build/bundledPlugins/ populated (≥130 entries; ~132 expected)"
    affects:
        - "29-06 (pnpm run start needs bundledPlugins to load extensions)"
        - "29-10 (rolls up SYNC-21 + SYNC-30 evidence)"

gates:
    pre_conditions:
        - "29-02 PASS"
        - "Working tree clean"
    post_conditions:
        - "pnpm build:extensions exits 0"
        - "src/main/build/bundledPlugins/ contains ≥130 entries (~132 expected)"
        - "29-VERIFY-RESULTS.md SYNC-21 + SYNC-30 sections PASS"
        - "HEAD commit title is `docs(29-03): SYNC-30 + SYNC-21 — pnpm build:extensions + bundledPlugins count`"
        - "HEAD commit SSH-signed; pushed"

commands:
    - "git status --short"
    - "time pnpm build:extensions 2>&1 | tee /tmp/build-extensions.log"
    - "ls src/main/build/bundledPlugins/ | wc -l"
    - "ls src/main/build/bundledPlugins/ | sort | head -20"
    - "ls src/main/build/bundledPlugins/ | sort | tail -20"
    - "git add -f .planning/phases/29-build-verification/29-VERIFY-RESULTS.md"
    - "git commit -m 'docs(29-03): SYNC-30 + SYNC-21 — pnpm build:extensions + bundledPlugins count'"
    - "git log -1 --show-signature"
    - "git push git@github.com:atabisz/Vortex.git v8.0/config-bucket:sync/upstream-v2.0.0"

verification:
    - "pnpm build:extensions exits 0"
    - "ls src/main/build/bundledPlugins | wc -l ≥ 130"
    - "29-VERIFY-RESULTS.md SYNC-30 + SYNC-21 sections PASS with count + alphabetised first/last 20 entries quoted"

must_haves:
    truths:
        - "`pnpm build:extensions` exits 0 — every extension compiled per SYNC-30"
        - "`src/main/build/bundledPlugins/` contains ≥130 directory entries (~132 expected) per SYNC-21 success criterion #3 (absolute floor 130 absorbs minor upstream churn)"
        - "Per-extension success markers captured — at minimum, total count + first-20 + last-20 alphabetised entries"
        - "On non-zero exit OR count <130, STOP and surface — D-29-01 cascade halts here"
        - "Single docs commit `docs(29-03): SYNC-30 + SYNC-21 — pnpm build:extensions + bundledPlugins count` lands and is pushed"
    artifacts:
        - path: ".planning/phases/29-build-verification/29-VERIFY-RESULTS.md"
          provides: "SYNC-21 + SYNC-30 sections appended"
          contains: "## SYNC-30"
        - path: "src/main/build/bundledPlugins/"
          provides: "Populated extension dir consumed by pnpm run start (29-06) and electron-builder packaging"
          contains: "≥130 directories"
    key_links:
        - from: "src/main/build/bundledPlugins/"
          to: "29-06 pnpm run start"
          via: "Extension manager loads plugins from this dir at startup; 29-06 boot evidence includes 'extension manager loaded ≥130 extensions'"
          pattern: "bundledPlugins"
---

<objective>
Run `pnpm build:extensions`. Verify the resulting `src/main/build/bundledPlugins/` directory contains ≥130 entries (~132 expected). Capture both signals into `29-VERIFY-RESULTS.md`.

Purpose: SYNC-30 (build:extensions exits 0) and SYNC-21 (bundledPlugins populated) are tightly coupled — count is a quick post-build invariant that proves nothing got silently dropped. Phase 29 success criterion #3 sets the floor at 130.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-build-verification/29-CONTEXT.md
@.planning/phases/29-build-verification/29-VERIFY-RESULTS.md
@CLAUDE.md
</context>

## Goal

`pnpm build:extensions` exit 0 + `bundledPlugins/` count ≥130; SYNC-30 + SYNC-21 sections in `29-VERIFY-RESULTS.md` marked PASS with the count + a sample of entries.

## Steps

1. **Confirm starting state.**

    ```
    git rev-parse --abbrev-ref HEAD
    git status --short
    ```

2. **Run `pnpm build:extensions`.**

    ```
    time pnpm build:extensions 2>&1 | tee /tmp/build-extensions.log
    echo "exit=${PIPESTATUS[0]}"
    ```

    Expected: exit 0, wall-clock ~5–10 min per CONTEXT notes.

3. **Count `bundledPlugins/` entries.**

    ```
    ls src/main/build/bundledPlugins/ | wc -l
    ```

    Expected: ≥130 (~132). If <130, STOP and surface — that means an extension silently failed to copy/build despite a 0 exit code.

4. **Sample entries** (for evidence — alphabetised first 20 + last 20):

    ```
    ls src/main/build/bundledPlugins/ | sort | head -20
    ls src/main/build/bundledPlugins/ | sort | tail -20
    ```

    Quote both blocks verbatim into the evidence file so a future auditor can confirm the expected gamebryo-per-game extensions are present (skyrimse, fallout4, oblivion, morrowind, bg3, nexus_integration, etc.).

5. **Append SYNC-30 + SYNC-21 sections to `29-VERIFY-RESULTS.md`** replacing the placeholder. Include:
    - **SYNC-30:** command, exit code, wall-clock time, last 30 lines of stdout, PASS/FAIL.
    - **SYNC-21:** total count, first-20 + last-20 alphabetised lists, PASS/FAIL.

6. **Commit and push.**
    ```
    git add -f .planning/phases/29-build-verification/29-VERIFY-RESULTS.md
    git commit -m 'docs(29-03): SYNC-30 + SYNC-21 — pnpm build:extensions + bundledPlugins count'
    git log -1 --show-signature
    git push git@github.com:atabisz/Vortex.git v8.0/config-bucket:sync/upstream-v2.0.0
    ```

## Verification

- `pnpm build:extensions` exited 0
- `ls src/main/build/bundledPlugins/ | wc -l` ≥ 130
- 29-VERIFY-RESULTS.md SYNC-30 and SYNC-21 sections both PASS with their evidence
- HEAD commit title exact match
- HEAD commit SSH-signed
- Remote ref pushed

## Commit

Title: `docs(29-03): SYNC-30 + SYNC-21 — pnpm build:extensions + bundledPlugins count`

Body:

```
pnpm build:extensions → exit 0.
src/main/build/bundledPlugins/ → N entries (~132 expected, floor 130 per Phase 29 success criterion #3).

Per D-29-01 sequential cascade. Evidence in 29-VERIFY-RESULTS.md.
```

Signing: SSH.

## Deviations

None expected. If `build:extensions` fails for one bad extension, that's a Phase 28-bis trigger — STOP, capture which extension and the failure mode, surface to user.

<output>
After completion, create `.planning/phases/29-build-verification/29-03-SUMMARY.md`.
</output>
