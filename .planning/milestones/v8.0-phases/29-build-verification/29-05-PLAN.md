---
phase: 29-build-verification
plan: 05
subsystem: lint
type: execute
wave: 6
depends_on: [29-04, 29-00]
files_modified:
    - .planning/phases/29-build-verification/29-VERIFY-RESULTS.md
    - .planning/phases/29-build-verification/29-LINT-BASELINE.md
autonomous: true
requirements: [SYNC-32]
tags: [linux-port, upstream-v2.0.0, phase-29, sync-32, lint, baseline-diff]

requirements_in_play: [SYNC-32]

dependency_graph:
    requires:
        - "29-04 complete (SYNC-31 PASS)"
        - "29-00 complete (29-LINT-BASELINE.md master half populated)"
    provides:
        - "29-VERIFY-RESULTS.md (SYNC-32 section)"
        - "29-LINT-BASELINE.md (v8.0 half + delta table)"
    affects:
        - "29-10 (rolls up SYNC-32 evidence)"

gates:
    pre_conditions:
        - "29-04 PASS"
        - "29-00 evidence file exists with master half"
        - "Working tree clean"
    post_conditions:
        - "pnpm lint:ci exits 0 on v8.0/config-bucket"
        - "29-LINT-BASELINE.md v8.0 half + delta table appended"
        - "Per D-29-05 PASS rule: new-warnings ≤ baseline-warnings (FAIL if strictly greater)"
        - "29-VERIFY-RESULTS.md SYNC-32 section PASS"
        - "HEAD commit title is `docs(29-05): SYNC-32 — pnpm lint:ci diff vs master`"
        - "HEAD commit SSH-signed; pushed"

commands:
    - "git status --short"
    - "time pnpm lint:ci 2>&1 | tee /tmp/lint-v8.log"
    - "git add -f .planning/phases/29-build-verification/29-LINT-BASELINE.md .planning/phases/29-build-verification/29-VERIFY-RESULTS.md"
    - "git commit -m 'docs(29-05): SYNC-32 — pnpm lint:ci diff vs master'"
    - "git log -1 --show-signature"
    - "git push git@github.com:atabisz/Vortex.git v8.0/config-bucket:sync/upstream-v2.0.0"

verification:
    - "pnpm lint:ci exits 0 on v8.0/config-bucket"
    - "v8.0 warnings count ≤ master warnings count (delta ≤ 0 → PASS; delta > 0 → FAIL)"
    - "29-LINT-BASELINE.md has both halves + a delta table per D-29-05 capture format"

must_haves:
    truths:
        - "`pnpm lint:ci` exits 0 on v8.0/config-bucket per SYNC-32"
        - "v8.0 warning count compared against master baseline from 29-00 — PASS if `new ≤ baseline`, FAIL if strictly greater per D-29-05"
        - "29-LINT-BASELINE.md has v8.0 half AND a delta table with columns: workspace | master count | v8.0 count | delta"
        - "Per-workspace deltas captured (not just totals) — so a regression in one workspace masked by improvement in another is visible"
        - "Single docs commit `docs(29-05): SYNC-32 — pnpm lint:ci diff vs master` lands and is pushed"
    artifacts:
        - path: ".planning/phases/29-build-verification/29-LINT-BASELINE.md"
          provides: "Closed master+v8.0 baseline + delta table"
          contains: "## v8.0/config-bucket result"
        - path: ".planning/phases/29-build-verification/29-VERIFY-RESULTS.md"
          provides: "SYNC-32 section appended"
          contains: "## SYNC-32"
    key_links:
        - from: "29-LINT-BASELINE.md delta table"
          to: "29-VERIFY-RESULTS.md SYNC-32 section"
          via: "SYNC-32 PASS/FAIL verdict cites the delta table"
          pattern: "delta"
---

<objective>
Run `pnpm lint:ci` on `v8.0/config-bucket`. Compare against the master baseline captured in 29-00. PASS per D-29-05 iff v8.0 warning count ≤ master warning count. Close `29-LINT-BASELINE.md` with the v8.0 half + a delta table.

Purpose: SYNC-32 — lint:ci passes OR surfaces only pre-existing warnings. D-29-05 operationalises that as a strict-non-regression gate against master. Stricter than exit-code-only; less brittle than zero-new-warnings.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-build-verification/29-CONTEXT.md
@.planning/phases/29-build-verification/29-LINT-BASELINE.md
@.planning/phases/29-build-verification/29-VERIFY-RESULTS.md
@CLAUDE.md
</context>

## Goal

`pnpm lint:ci` exit 0 + delta-vs-master ≤ 0 → SYNC-32 PASS. Both halves of `29-LINT-BASELINE.md` populated; delta table closes the file.

## Steps

1. **Confirm starting state.**

    ```
    git rev-parse --abbrev-ref HEAD
    git status --short
    grep -q '## Master baseline' .planning/phases/29-build-verification/29-LINT-BASELINE.md
    grep -q '_To be filled by Plan 29-05._' .planning/phases/29-build-verification/29-LINT-BASELINE.md
    ```

2. **Run `pnpm lint:ci` on v8.0/config-bucket.**

    ```
    time pnpm lint:ci 2>&1 | tee /tmp/lint-v8.log
    echo "exit=${PIPESTATUS[0]}"
    ```

    Expected: exit 0 (lint:ci tolerates warnings, fails on errors).

3. **Extract per-workspace warning + error counts** from `/tmp/lint-v8.log` and from `29-LINT-BASELINE.md`'s embedded master log. Build a table with columns: `workspace | master warnings | v8.0 warnings | delta | master errors | v8.0 errors`. Total row at the bottom.

    Use whatever count format `lint:ci` actually emits — look at the master log inside 29-LINT-BASELINE.md (captured by 29-00) to see the format, then mirror it for v8.0. Likely an `eslint`-style summary like `N problems (X errors, Y warnings)` per workspace, OR an nx aggregate at the end.

4. **Compute the SYNC-32 verdict per D-29-05:**
    - PASS if `v8.0 total warnings ≤ master total warnings` AND `pnpm lint:ci` exited 0.
    - FAIL if either condition broken.
    - Per-workspace deltas are evidence-bearing but not gate-breaking on their own (a +5 in one workspace + -10 in another is still PASS at the total level — but flag the +5 in the evidence so it's visible).

5. **Close `29-LINT-BASELINE.md`:**
    - Replace the `_To be filled by Plan 29-05._` placeholder under `## v8.0/config-bucket result` with the v8.0 warning + error counts and the full `/tmp/lint-v8.log` quoted.
    - Append a final section `## Delta vs master` with the per-workspace + total table.
    - End with the verdict line: `**SYNC-32 verdict: PASS**` or `**SYNC-32 verdict: FAIL**` + reason.

6. **Append SYNC-32 section to `29-VERIFY-RESULTS.md`** replacing the placeholder. Quote the delta table verbatim from 29-LINT-BASELINE.md and the verdict line.

7. **Commit and push.**
    ```
    git add -f .planning/phases/29-build-verification/29-LINT-BASELINE.md .planning/phases/29-build-verification/29-VERIFY-RESULTS.md
    git commit -m 'docs(29-05): SYNC-32 — pnpm lint:ci diff vs master'
    git log -1 --show-signature
    git push git@github.com:atabisz/Vortex.git v8.0/config-bucket:sync/upstream-v2.0.0
    ```

## Verification

- `pnpm lint:ci` exited 0 on v8.0
- 29-LINT-BASELINE.md has populated `## v8.0/config-bucket result` section + `## Delta vs master` section + verdict line
- 29-VERIFY-RESULTS.md SYNC-32 section quotes the delta table + verdict
- D-29-05 PASS rule applied: PASS iff `v8.0 warnings ≤ master warnings` AND lint:ci exit 0
- HEAD commit title exact match
- HEAD commit SSH-signed
- Remote pushed

## Commit

Title: `docs(29-05): SYNC-32 — pnpm lint:ci diff vs master`

Body:

```
pnpm lint:ci → exit 0 on v8.0/config-bucket.
Master warnings: M. v8.0 warnings: V. Delta: V-M.

Per D-29-05: PASS iff v8.0 ≤ master AND lint:ci exit 0.
Evidence in 29-LINT-BASELINE.md (full log + delta table) and 29-VERIFY-RESULTS.md.
```

Signing: SSH.

## Deviations

If v8.0 warnings strictly exceed master warnings, the verdict is FAIL — STOP and surface to user before proceeding to 29-06. Repair-vs-defer call is the user's per CONTEXT D-29-01 cascade rules.

<output>
After completion, create `.planning/phases/29-build-verification/29-05-SUMMARY.md`.
</output>
