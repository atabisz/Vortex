---
phase: 26-mod-management-hot-zone
plan: 07
type: execute
wave: 2
depends_on: [26-06]
files_modified:
    - src/renderer/src/extensions/mod_management/LinkingDeployment.ts
autonomous: true
requirements: [SYNC-22, SYNC-23, SYNC-27]

must_haves:
    truths:
        - "All conflict markers in LinkingDeployment.ts are resolved"
        - "`git grep -nE 'resolvePathCase\\(dataPath, relDataPath, dirCache\\)' src/renderer/src/extensions/mod_management/LinkingDeployment.ts` returns ≥1 hit AFTER the commit (140a57217 fix half preserved with exact arg shape)"
        - "Post-resolution grep for LinkingDeployment.ts hit count match-or-exceeds the pre-snapshot count from plan 06"
        - "Single atomic commit on v8.0/config-bucket with title `resolve(mod-mgmt): LinkingDeployment.ts — <stance>` whose body contains the pre/post grep diff"
        - "scripts/grep-checkpoint.sh --skip-conflict-check exits zero (140a57217 LinkingDeployment.ts gate is now actively asserted)"
        - "pnpm typecheck -F @vortex/renderer passes after the commit (D-26-04)"
    artifacts:
        - path: "src/renderer/src/extensions/mod_management/LinkingDeployment.ts"
          provides: "Hardlink/symlink deployment engine — sixth in D-26-01 order; hosts the 140a57217 fix half"
          contains: "resolvePathCase(dataPath, relDataPath, dirCache)"
    key_links:
        - from: "LinkingDeployment.ts"
          to: "externalChanges.ts (plan 06)"
          via: "Both files host the 140a57217 fix; arg shape `(dataPath, relDataPath, dirCache)` is shared"
          pattern: "resolvePathCase(dataPath, relDataPath, dirCache)"
        - from: "scripts/140a57217-pre.txt"
          to: "this plan's grep-post diff"
          via: "Pre-snapshot captured in plan 06; post-snapshot taken here for the LinkingDeployment.ts portion"
          pattern: "LinkingDeployment.ts"
---

<objective>
Resolve `src/renderer/src/extensions/mod_management/LinkingDeployment.ts` per D-26-01 order, applying D-26-02 grep-pre + hand-read + grep-post discipline. Pre-snapshot was captured in plan 06; this plan does the LinkingDeployment.ts hand-read and post-grep verification.

Purpose: This is the second of two files hosting the 140a57217 fix. The LinkingDeployment.ts call sites are in the deploy/externalChanges flow per D-26-CONTEXT and the playbook entry. Without this fix, hardlink deployment hits ENOENT on every manifest entry on Linux when staging-folder casing differs from on-disk casing.

Output: One commit on `v8.0/config-bucket` whose only working-tree change is `LinkingDeployment.ts`, with all conflict markers gone, all `resolvePathCase(dataPath, relDataPath, dirCache)` calls preserved, and the pre/post grep diff in the commit body.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-mod-management-hot-zone/26-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hand-resolve LinkingDeployment.ts with grep-post verification against plan 06 pre-snapshot</name>
  <read_first>
    - src/renderer/src/extensions/mod_management/LinkingDeployment.ts (current state, full read — every conflict region; particular attention to deploy() / externalChanges() flow per the playbook entry on line 360)
    - src/renderer/src/extensions/mod_management/externalChanges.ts (already-resolved sibling — read so the executor sees the call-site shape and confirms the arg-shape consistency between the two files)
    - .planning/phases/26-mod-management-hot-zone/scripts/140a57217-pre.txt (pre-snapshot captured in plan 06)
    - .planning/phases/26-mod-management-hot-zone/26-CONTEXT.md (D-26-02 grep-pre/read/grep-post discipline; D-26-00 commit format)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md externalChanges/140a57217 entry (line 360) — failure mode without the fix
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Open `src/renderer/src/extensions/mod_management/LinkingDeployment.ts`.

    Use the LinkingDeployment.ts portion of `.planning/phases/26-mod-management-hot-zone/scripts/140a57217-pre.txt` to navigate to each `resolvePathCase` call site. For every conflict region:
    - If the region is at or near a line number that the pre-snapshot recorded as a `resolvePathCase` call site, the "ours" (fork) side MUST keep the call AND the arg shape `(dataPath, relDataPath, dirCache)`. Detect upstream renames (`relDataPath` → `relativePath`) — keep the fork name.
    - For all other regions, default stance: fork-side wins for Linux fixes (case-folding, polkit, Linux-only path helpers), upstream wins for new feature scaffolding.

    After resolution, run the D-26-02 post-grep:

    `git grep -n 'resolvePathCase' src/renderer/src/extensions/mod_management/LinkingDeployment.ts`

    Compare against the LinkingDeployment.ts portion of the pre-snapshot. Hit count must match-or-exceed; arg shape must be `(dataPath, relDataPath, dirCache)` exactly. Compute the diff for the commit body.

    Stage only `LinkingDeployment.ts` (the pre-snapshot file is already committed in plan 06). Commit with title `resolve(mod-mgmt): LinkingDeployment.ts — <stance>` per D-26-00. Body MUST include:
    - Per-region resolution summary
    - Pre-grep LinkingDeployment.ts hit count (from plan 06 snapshot)
    - Post-grep LinkingDeployment.ts hit count
    - Diff (or "no change") between them
    - Confirmation that the `(dataPath, relDataPath, dirCache)` arg shape is intact

    Run `pnpm typecheck -F @vortex/renderer` per D-26-04. Run `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` per D-26-03. Gate 6 (140a57217 LinkingDeployment.ts) is now actively asserted in addition to gate 7 (externalChanges.ts) — both must pass. Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/LinkingDeployment.ts; test $? -ne 0 &amp;&amp; git grep -nE 'resolvePathCase\(dataPath, relDataPath, dirCache\)' src/renderer/src/extensions/mod_management/LinkingDeployment.ts &amp;&amp; git log -1 --format=%s | grep -qF 'resolve(mod-mgmt): LinkingDeployment.ts —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check &amp;&amp; pnpm typecheck -F @vortex/renderer</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' src/renderer/src/extensions/mod_management/LinkingDeployment.ts` returns empty
    - `git grep -nE 'resolvePathCase\(dataPath, relDataPath, dirCache\)' src/renderer/src/extensions/mod_management/LinkingDeployment.ts` returns ≥1 hit and matches-or-exceeds pre-snapshot count
    - HEAD commit title starts with `resolve(mod-mgmt): LinkingDeployment.ts —`
    - HEAD commit body contains the pre/post grep diff per D-26-02 step 4
    - HEAD commit touches exactly one file: `src/renderer/src/extensions/mod_management/LinkingDeployment.ts`
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh --skip-conflict-check` exits zero — gates 6 AND 7 (140a57217 in both files) pass
    - `pnpm typecheck -F @vortex/renderer` exits zero
    - No upstream rename leaked through: every hit shows `(dataPath, relDataPath, dirCache)` exactly
  </acceptance_criteria>
  <done>
    File 6 of 8 resolved with 140a57217 fix preserved in LinkingDeployment.ts; both halves of the fix (here + externalChanges.ts from plan 06) are now actively asserted by the checkpoint harness.
  </done>
</task>

</tasks>

<verification>
- `git grep '^<<<<<<< ' src/renderer/src/extensions/mod_management/LinkingDeployment.ts` empty
- 140a57217 LinkingDeployment.ts gate (gate 6 in checkpoint script) passes
- `pnpm typecheck -F @vortex/renderer` passes
- Commit body cites pre/post grep diff
</verification>

<success_criteria>

- 140a57217 fix half preserved with exact arg shape `(dataPath, relDataPath, dirCache)` intact
- Both 140a57217 gates (6 + 7) now actively asserted clean by the checkpoint harness
- File 6 of 8 resolved
  </success_criteria>

<output>
After completion, create `.planning/phases/26-mod-management-hot-zone/26-07-SUMMARY.md`
</output>
