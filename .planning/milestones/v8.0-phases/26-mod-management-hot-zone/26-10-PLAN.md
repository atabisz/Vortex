---
phase: 26-mod-management-hot-zone
plan: 10
type: execute
wave: 3
depends_on: [26-09]
files_modified: []
autonomous: true
requirements: [SYNC-04, SYNC-22, SYNC-23, SYNC-27]

must_haves:
    truths:
        - "All five D-26-05 done-gate items pass: zero conflict markers, grep-checkpoint.sh exits zero, pnpm typecheck -F @vortex/renderer passes, 8 atomic commits matching `resolve(mod-mgmt): <file> — <stance>` format are present, force-with-lease push to fork/sync/upstream-v2.0.0 succeeds"
        - "All 4 phase requirement IDs (SYNC-04, SYNC-22, SYNC-23, SYNC-27) verifiable as satisfied via the done-gate evidence"
        - "Branch v8.0/config-bucket is pushed to fork remote with --force-with-lease"
    artifacts:
        - path: ".planning/phases/26-mod-management-hot-zone/26-DONE-GATE.md"
          provides: "Done-gate evidence record — captures the output of each of the 5 D-26-05 checks and the push confirmation, for milestone audit trail"
          contains: "## D-26-05 Done Gate"
    key_links:
        - from: "26-DONE-GATE.md"
          to: "ROADMAP.md Phase 26 row"
          via: "Evidence that success criteria 1–4 in the ROADMAP Phase 26 entry are met"
          pattern: "Phase 26"
        - from: "v8.0/config-bucket"
          to: "fork remote (atabisz/Vortex)"
          via: "--force-with-lease push to sync/upstream-v2.0.0"
          pattern: "fork/sync/upstream-v2.0.0"
---

<objective>
Run the full D-26-05 done-gate — five checks — and push `v8.0/config-bucket` to the fork remote with `--force-with-lease` per D-26-00. Capture evidence in `.planning/phases/26-mod-management-hot-zone/26-DONE-GATE.md` for milestone audit trail. Update STATE.md and ROADMAP.md to mark Phase 26 complete.

Purpose: Single point where the phase declares done. Catches any regression that may have crept in between plan 09 and now (e.g. an inadvertent uncommitted edit). Pushes only AFTER all five checks pass.

Output: A pushed branch on the fork remote, an evidence file `.planning/phases/26-mod-management-hot-zone/26-DONE-GATE.md`, and an updated `.planning/ROADMAP.md` Phase 26 row marked complete.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-mod-management-hot-zone/26-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all five D-26-05 done-gate checks and write evidence file</name>
  <read_first>
    - .planning/phases/26-mod-management-hot-zone/26-CONTEXT.md (D-26-05 — five-item done-gate; D-26-00 — commit title format; D-26-04 — final phase-end typecheck still happens here)
    - .planning/phases/24-config-bucket/24-CONTEXT.md (D-02 push policy — force-with-lease to fork remote at phase end)
    - .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh (the harness this gate runs without flags)
    - CLAUDE.md memory note "Git push SSH URL" — sandbox blocks `.git/config`; push with inline SSH URL `git push git@github.com:atabisz/Vortex.git <branch>` if the standard `fork` remote push fails with permissions error
    - CLAUDE.md memory note "GitHub auth token" — `GH_TOKEN` env var available for atabisz when gh auth is expired
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Run each D-26-05 check and capture output:

    1. **Zero conflict markers across the bucket:** `git grep '^<<<<<<< ' src/renderer/src/extensions/mod_management/`. Capture exit code; expect non-zero (no matches).

    2. **scripts/grep-checkpoint.sh exits zero:** `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh`. Capture full stdout/stderr.

    3. **Renderer typecheck passes:** `pnpm typecheck -F @vortex/renderer`. Capture stdout tail + exit code.

    4. **8 atomic commits matching the title format:** `git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 | grep -E '^[0-9a-f]+ resolve\(mod-mgmt\):' | wc -l`. Expect ≥9 (one for `scripts/grep-checkpoint.sh` from plan 01 + eight per-file commits from plans 02–09). All matching commits' titles must follow `resolve(mod-mgmt): <file> — <stance>` per D-26-00.

    5. **Force-with-lease push:** `git push --force-with-lease fork sync/upstream-v2.0.0` (mapping local `v8.0/config-bucket` to remote `sync/upstream-v2.0.0` per Phase 24 D-02 push policy). Use `git push --force-with-lease fork v8.0/config-bucket:sync/upstream-v2.0.0` to make the refspec explicit.

       If `fork` remote push fails with a permissions/sandbox error, fall back to the inline SSH URL form per CLAUDE.md memory note: `git push --force-with-lease git@github.com:atabisz/Vortex.git v8.0/config-bucket:sync/upstream-v2.0.0`. Capture the push output (the new remote SHA).

    Checks 1–4 must all pass BEFORE check 5 (the push). If any of 1–4 fails, STOP and surface immediately to the user — no push happens.

    Write `.planning/phases/26-mod-management-hot-zone/26-DONE-GATE.md` with sections:
    - `## D-26-05 Done Gate`
    - `### 1. Zero conflict markers` (echo of `git grep` exit code + any output)
    - `### 2. grep-checkpoint.sh` (full stdout/stderr)
    - `### 3. pnpm typecheck -F @vortex/renderer` (stdout tail + exit code)
    - `### 4. 8 atomic commits` (the `git log --oneline` output filtered for the title pattern, with count)
    - `### 5. Force-with-lease push` (remote SHA before + after, push command used)
    - `## Requirements satisfied` — table mapping SYNC-04, SYNC-22, SYNC-23, SYNC-27 to the D-26-05 evidence that proves each one
    - `## Phase 26 status: COMPLETE` (timestamped per the system clock)

    Stage `.planning/phases/26-mod-management-hot-zone/26-DONE-GATE.md` and commit with title `docs(mod-mgmt): Phase 26 done-gate evidence`. Push that commit too (it's a fork-only `.planning/` artefact — branch policy in CLAUDE.md confirms `.planning/` lives on `master`/`v8.0/config-bucket`, never cherry-picked to `linux-port`).

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/; test $? -ne 0 &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh &amp;&amp; pnpm typecheck -F @vortex/renderer &amp;&amp; test $(git log --oneline v8.0/config-bucket --not origin/sync/upstream-v2.0.0 2&gt;/dev/null | grep -cE '^[0-9a-f]+ resolve\(mod-mgmt\):') -ge 9 &amp;&amp; test -f .planning/phases/26-mod-management-hot-zone/26-DONE-GATE.md &amp;&amp; git ls-remote fork sync/upstream-v2.0.0 | grep -qE '^[0-9a-f]+'</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' src/renderer/src/extensions/mod_management/` returns non-zero (no matching files)
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh` exits zero (full §6 + §7a–d + 140a57217 + no-conflict-marker assertion all pass)
    - `pnpm typecheck -F @vortex/renderer` exits zero
    - At least 9 commits on `v8.0/config-bucket` not yet on origin (1 script + 8 file resolutions) match the `resolve(mod-mgmt):` title pattern
    - `.planning/phases/26-mod-management-hot-zone/26-DONE-GATE.md` exists with all required sections
    - `git ls-remote fork sync/upstream-v2.0.0` shows the local HEAD SHA (push succeeded)
    - Done-gate file is itself committed and pushed
  </acceptance_criteria>
  <done>
    All 5 D-26-05 checks pass; branch pushed; evidence captured; phase declared complete.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md and STATE.md to mark Phase 26 complete</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 26 row in `## Phases` v8.0 section + `## Progress` table row 26)
    - .planning/STATE.md (current `current_position` / `stopped_at` fields)
    - .planning/phases/26-mod-management-hot-zone/26-DONE-GATE.md (just-written; provides completion timestamp)
  </read_first>
  <action>
    Update `.planning/ROADMAP.md`:
    - In the `## Phases` v8.0 section, change the Phase 26 line from `- [ ] **Phase 26: Mod-management hot zone**` to `- [x] **Phase 26: Mod-management hot zone** (complete YYYY-MM-DD)` using today's date.
    - In `### Phase 26: Mod-management hot zone` details block, change `**Plans**: TBD` to `**Plans**: 10/10 plans complete` and list each plan with `- [x] 26-NN-PLAN.md — <one-line objective>`.
    - In `## Progress` table, update row 26: `| 26. Mod-management hot zone | v8.0 | 10/10 | Complete | YYYY-MM-DD |`.

    Update `.planning/STATE.md`:
    - Bump `completed_phases` from 2 to 3, `total_plans` from 12 to 22, `completed_plans` from 12 to 22 (1 phase × 10 plans added).
    - Update `last_updated` to current ISO timestamp.
    - Update `last_activity` to today's date.
    - Update `## Current Position` Phase from 26 to 27, Status to `ready_to_plan`.
    - Update `stopped_at` to `Phase 26 complete; push to fork/sync/upstream-v2.0.0 confirmed`.

    Stage both updated files. Commit with title `docs: mark Phase 26 complete in ROADMAP + STATE`. Push.

  </action>
  <verify>
    <automated>grep -qE '^- \[x\] \*\*Phase 26: Mod-management hot zone\*\*' .planning/ROADMAP.md &amp;&amp; grep -qE '^\| 26\. Mod-management hot zone \| v8\.0 \| 10/10 \| Complete' .planning/ROADMAP.md &amp;&amp; grep -qE 'completed_phases: 3' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 26 row in `## Phases` shows `[x]` and a completion date
    - ROADMAP.md `### Phase 26` details block shows `**Plans**: 10/10 plans complete` with each plan listed and checked
    - ROADMAP.md `## Progress` row 26 shows `10/10`, `Complete`, today's date
    - STATE.md frontmatter `completed_phases: 3`, `completed_plans: 22`, `last_activity` updated, `current_position.phase: 27`, `current_position.status: ready_to_plan`
    - Both files committed in a single commit and pushed
  </acceptance_criteria>
  <done>
    Phase 26 is recorded as complete in ROADMAP.md + STATE.md; the next phase is teed up.
  </done>
</task>

</tasks>

<verification>
- D-26-05 all five items pass and are recorded in `26-DONE-GATE.md`
- ROADMAP.md + STATE.md reflect Phase 26 complete
- Branch pushed to `fork/sync/upstream-v2.0.0` with --force-with-lease
</verification>

<success_criteria>

- D-26-05 done-gate clean
- All 4 requirement IDs (SYNC-04, SYNC-22, SYNC-23, SYNC-27) demonstrably satisfied
- Phase formally complete; Phase 27 ready for planning
  </success_criteria>

<output>
After completion, create `.planning/phases/26-mod-management-hot-zone/26-10-SUMMARY.md`
</output>
