---
phase: 26-mod-management-hot-zone
plan: 09
type: execute
wave: 2
depends_on: [26-08]
files_modified:
    - src/renderer/src/extensions/mod_management/index.ts
autonomous: true
requirements: [SYNC-04]

must_haves:
    truths:
        - "All conflict markers in index.ts are resolved"
        - "Re-exports match the final symbol shapes from the 7 sibling files resolved in plans 02–08"
        - "Single atomic commit on v8.0/config-bucket with title `resolve(mod-mgmt): index.ts — <stance>`"
        - "scripts/grep-checkpoint.sh (WITHOUT --skip-conflict-check) exits zero — this is the FIRST plan that runs the full checkpoint with the conflict-marker gate enabled, because index.ts is the last file in D-26-01 order"
        - "pnpm typecheck -F @vortex/renderer passes after the commit (D-26-04) — strongest signal that re-exports line up with sibling exports"
    artifacts:
        - path: "src/renderer/src/extensions/mod_management/index.ts"
          provides: "Public barrel export for the mod_management extension — eighth and final leaf in D-26-01 order"
          contains: "export"
    key_links:
        - from: "index.ts"
          to: "all 7 sibling files (plans 02–08)"
          via: "Re-exports symbols defined in those siblings; conflicts here depend on those siblings' final symbol shapes"
          pattern: "export"
        - from: "index.ts commit"
          to: "Plan 10 done-gate"
          via: "After this commit lands, all 8 mod_management bucket files are resolved; plan 10 runs the full done-gate without --skip-conflict-check"
          pattern: "scripts/grep-checkpoint.sh"
---

<objective>
Resolve `src/renderer/src/extensions/mod_management/index.ts` per D-26-01 — last in the leaf-first order because it re-exports from the other 7 files. Conflicts here depend on those siblings' final symbol shapes (which only exist after plans 02–08 land).

Purpose: Once this commit lands, all 8 mod_management bucket files are conflict-free and the checkpoint harness can run WITHOUT `--skip-conflict-check` for the first time in the phase. That run is the dress rehearsal for plan 10's done-gate.

Output: One commit on `v8.0/config-bucket` whose only working-tree change is `index.ts`, all conflict markers gone across the entire bucket, full checkpoint harness clean.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-mod-management-hot-zone/26-CONTEXT.md
@.planning/phases/24-config-bucket/24-CONTEXT.md
@VORTEX-LINUX-MERGE-PLAYBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve index.ts re-exports against final sibling symbol shapes</name>
  <read_first>
    - src/renderer/src/extensions/mod_management/index.ts (current state, full read)
    - src/renderer/src/extensions/mod_management/InstallManager.ts (just-resolved sibling — to confirm export shapes)
    - src/renderer/src/extensions/mod_management/LinkingDeployment.ts (just-resolved sibling — to confirm export shapes)
    - src/renderer/src/extensions/mod_management/externalChanges.ts (just-resolved sibling)
    - src/renderer/src/extensions/mod_management/stagingDirectory.ts (just-resolved sibling)
    - src/renderer/src/extensions/mod_management/util/deploy.ts (just-resolved sibling)
    - src/renderer/src/extensions/mod_management/eventHandlers.ts (just-resolved sibling)
    - src/renderer/src/extensions/mod_management/views/ModList.tsx (just-resolved sibling)
    - .planning/phases/26-mod-management-hot-zone/26-CONTEXT.md (D-26-00, D-26-01 — explicitly notes index.ts is last because re-exports depend on siblings, D-26-04 typecheck)
    - VORTEX-LINUX-MERGE-PLAYBOOK.md — confirm `index.ts` is NOT named in any playbook gate (it is not — it is a barrel file)
  </read_first>
  <action>
    Confirm branch is `v8.0/config-bucket`. Open `src/renderer/src/extensions/mod_management/index.ts` and hand-resolve every conflict region.

    For each `export ... from './<sibling>'` conflict region: pick the side whose re-export shape matches what the resolved sibling actually exports. Run `git grep -nE '^export' <sibling-path>` for each candidate to confirm. If both sides agree on the export list, prefer the upstream side for ordering / cosmetic differences (perfectionist plugin sorts imports alphabetically per project ESLint conventions).

    If a region exposes Linux-port-only modules (e.g. anything from `@vortex/paths` or fork-only `util/` siblings), keep the fork-side re-export.

    Stage only `index.ts`. Commit with title `resolve(mod-mgmt): index.ts — <stance>`. Body summarises which re-exports came from each side and confirms the file's exports match the final sibling export lists.

    Run `pnpm typecheck -F @vortex/renderer` per D-26-04 — typecheck is the strongest signal that re-exports line up. If it fails, the index.ts re-export shape disagrees with one of the siblings; hand-fix and amend (single atomic commit per D-26-00).

    Run `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh` (NO `--skip-conflict-check` flag this time — gate 8 must pass because all 8 bucket files now have conflict markers gone). All 8 gates (1–7 + the conflict-marker gate) MUST pass. If any gate fails, surface immediately to the user — that means a sibling commit broke a playbook invariant after the fact (e.g. plan 08's commit was amended in a way that lost a fix). Resolve before proceeding to plan 10. Do NOT push.

  </action>
  <verify>
    <automated>git grep -l '^&lt;&lt;&lt;&lt;&lt;&lt;&lt; ' src/renderer/src/extensions/mod_management/index.ts; test $? -ne 0 &amp;&amp; git log -1 --format=%s | grep -qF 'resolve(mod-mgmt): index.ts —' &amp;&amp; bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh &amp;&amp; pnpm typecheck -F @vortex/renderer</automated>
  </verify>
  <acceptance_criteria>
    - `git grep '^<<<<<<< ' src/renderer/src/extensions/mod_management/index.ts` returns empty
    - `git grep -l '^<<<<<<< ' src/renderer/src/extensions/mod_management/` returns no files (the entire bucket is conflict-free)
    - HEAD commit title starts with `resolve(mod-mgmt): index.ts —`
    - HEAD commit touches exactly one file: `src/renderer/src/extensions/mod_management/index.ts`
    - `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh` (no `--skip-conflict-check`) exits zero — first time in the phase that ALL 8 gates pass
    - `pnpm typecheck -F @vortex/renderer` exits zero
    - Commit count from plan 01's commit 0 inclusive: exactly 9 commits on `v8.0/config-bucket` not yet pushed (1 script + 8 file resolutions)
  </acceptance_criteria>
  <done>
    File 8 of 8 resolved. Bucket is conflict-free, the full checkpoint passes, typecheck passes. Plan 10 done-gate is now ready to run.
  </done>
</task>

</tasks>

<verification>
- `git grep '^<<<<<<< ' src/renderer/src/extensions/mod_management/` returns no files
- `bash .planning/phases/26-mod-management-hot-zone/scripts/grep-checkpoint.sh` (no flag) exits zero
- `pnpm typecheck -F @vortex/renderer` passes
- 9 unpushed commits on `v8.0/config-bucket` (script + 8 file commits)
</verification>

<success_criteria>

- All 8 bucket files resolved
- Checkpoint harness fully clean for the first time
- Phase ready for done-gate + push (plan 10)
  </success_criteria>

<output>
After completion, create `.planning/phases/26-mod-management-hot-zone/26-09-SUMMARY.md`
</output>
