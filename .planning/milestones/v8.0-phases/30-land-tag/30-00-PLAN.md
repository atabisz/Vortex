---
phase: 30-land-tag
plan: 00
subsystem: setup
type: execute
wave: 1
depends_on: []
files_modified:
    - .planning/phases/30-land-tag/30-PRE-STATE.md
autonomous: false
requirements: [SYNC-35, SYNC-36]
tags: [linux-port, upstream-v2.0.0, phase-30, setup, sync-35, sync-36]

requirements_in_play: [SYNC-35, SYNC-36]

dependency_graph:
    requires:
        - "Phase 29 done-gate closed (29-DONE-GATE.md exists, all 9 SYNC PASS)"
        - "PR #4 OPEN MERGEABLE on atabisz/Vortex"
    provides:
        - ".planning/phases/30-land-tag/30-PRE-STATE.md (verified pre-rebase SHAs, branch list, RC tag presence, gh repo merge settings)"
        - "fork/master pushed to live fork remote (Open Question §1 resolution: push BEFORE rebase)"
    affects:
        - "30-01 rebase plan (consumes verified pre-rebase SHAs as lease pin)"
        - "30-03 FF-merge plan (consumes gh merge-flag verification)"

gates:
    pre_conditions:
        - "Working tree clean: git status --short empty"
        - "Current branch v8.0/config-bucket"
        - "Phase 29 done-gate evidence file exists"
    post_conditions:
        - "30-PRE-STATE.md contains: master HEAD SHA, fork/master SHA, fork/sync/upstream-v2.0.0 SHA, fork/linux-port SHA, RC tag SHA, gh repo merge-allowed flags, gh pr 4 mergeStateStatus, gh pr 4 head SHA, current Main + Format CI run IDs"
        - "fork/master matches local master (push completed)"

must_haves:
    truths:
        - "Pre-rebase state is captured to disk before any history-rewriting operation runs — recovery baseline if rebase blows up"
        - "fork/master is pushed to atabisz/Vortex so the +20 master-only commits land as their own push event with their own CI run (Open Question §1 recommendation: push first)"
        - "gh repo merge-flag behavior is verified before 30-03 FF-merge attempt (Open Question §2 — `--merge` may or may not produce true FF on this gh version)"
        - "PR #4 confirmed still OPEN MERGEABLE at the SHA we will lease-pin against (no drift since CONTEXT)"
    artifacts:
        - path: ".planning/phases/30-land-tag/30-PRE-STATE.md"
          provides: "Verified pre-rebase SHAs + repo settings — recovery baseline"
          contains: "## Pre-rebase state"
    key_links:
        - from: "30-PRE-STATE.md"
          to: "30-01 rebase lease pin"
          via: "fork/sync/upstream-v2.0.0 verified SHA used as --force-with-lease pin"
          pattern: "force-with-lease=sync/upstream-v2.0.0"
---

<objective>
Capture every pre-rebase SHA + repo state to disk, push local `master` to `fork/master` so the +20 commits get their own CI signal before they land inside the 374-commit FF-merge, and verify gh CLI merge-flag behavior so the 30-03 FF-merge plan picks the right command shape.

Purpose: Phase 30 rewrites history (rebase) and creates side-effects on the fork remote (force-push, FF-merge, tag push, cherry-pick push). Without a recovery baseline written to disk, a botched rebase has no rollback target. Pushing master first is the Open Question §1 resolution from RESEARCH.md — it gives the +20 commits independent CI before they get folded into PR #4.

Output: `.planning/phases/30-land-tag/30-PRE-STATE.md` with verified SHAs and CI run IDs; `fork/master` updated to local master HEAD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-land-tag/30-CONTEXT.md
@.planning/phases/30-land-tag/30-RESEARCH.md
@.planning/phases/29-build-verification/29-DONE-GATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 0-1: Capture pre-rebase remote + local SHAs</name>
  <files>.planning/phases/30-land-tag/30-PRE-STATE.md</files>
  <read_first>
    .planning/phases/30-land-tag/30-RESEARCH.md (live state delta section, Code Examples §1, Pitfall 3)
    .planning/phases/30-land-tag/30-CONTEXT.md (Code context section)
    .planning/phases/29-build-verification/29-DONE-GATE.md (shape reference for evidence-file structure)
  </read_first>
  <action>
    Verify clean working tree (`git status --short` returns empty) and current branch is v8.0/config-bucket. Write `.planning/phases/30-land-tag/30-PRE-STATE.md` capturing in this exact order:

    1. Header: timestamp (ISO-8601 UTC), capture method, captured-by.
    2. **Local SHAs**: output of `git rev-parse master v8.0/config-bucket` plus `git merge-base master v8.0/config-bucket`.
    3. **Fork remote SHAs (verified live)**: `git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master refs/heads/sync/upstream-v2.0.0 refs/heads/linux-port refs/tags/v2.0.0-linux-rebased-rc1` — full output as fenced block.
    4. **Origin remote SHAs**: `git ls-remote git@github.com:Nexus-Mods/Vortex.git refs/heads/master` — single line.
    5. **PR #4 state**: `gh pr view 4 --repo atabisz/Vortex --json state,mergeStateStatus,headRefName,headRefOid,baseRefName,baseRefOid` — full JSON output as fenced block. Record state must be OPEN, mergeStateStatus must be CLEAN or UNSTABLE (not DIRTY/BLOCKED).
    6. **Active CI runs**: `gh run list --repo atabisz/Vortex --workflow=main.yml --branch=sync/upstream-v2.0.0 --limit 3` and `gh run list --repo atabisz/Vortex --workflow=format.yml --branch=sync/upstream-v2.0.0 --limit 3` — full output as fenced block.

    Section header pattern matches Phase 29 done-gate sections (## Pre-rebase state, ### Local SHAs, ### Fork remote SHAs, ### Origin remote SHAs, ### PR #4 state, ### Active CI runs). Casual voice — short sentences explaining what each block means; no formal-ops-review prose. Use `git add -f` because `.planning/` is gitignored.

  </action>
  <verify>
    <automated>test -f .planning/phases/30-land-tag/30-PRE-STATE.md && grep -c "^## Pre-rebase state" .planning/phases/30-land-tag/30-PRE-STATE.md | grep -q "^1$" && grep -c "^### " .planning/phases/30-land-tag/30-PRE-STATE.md | awk '$1 >= 5 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/30-land-tag/30-PRE-STATE.md` exists.
    - Contains exactly one `## Pre-rebase state` heading.
    - Contains at least 5 `### ` subsections (Local SHAs, Fork remote SHAs, Origin remote SHAs, PR #4 state, Active CI runs).
    - `grep -c "git ls-remote" .planning/phases/30-land-tag/30-PRE-STATE.md` returns >= 1.
    - `grep -c "OPEN" .planning/phases/30-land-tag/30-PRE-STATE.md` returns >= 1 (PR #4 state captured).
    - `gh pr view 4 --repo atabisz/Vortex --json state -q .state` returns "OPEN" at capture time (record otherwise — STOP and surface deviation).
  </acceptance_criteria>
  <done>30-PRE-STATE.md exists with all 5 captured-state subsections; live `gh pr view 4 -q .state` returns OPEN at capture time.</done>
</task>

<task type="auto">
  <name>Task 0-2: Verify gh repo merge-flag behavior + record fallback path</name>
  <files>.planning/phases/30-land-tag/30-PRE-STATE.md</files>
  <read_first>
    .planning/phases/30-land-tag/30-RESEARCH.md (Pitfall 2 + Open Questions §2)
    .planning/phases/30-land-tag/30-PRE-STATE.md (just-written file — append to it)
  </read_first>
  <action>
    Append a `## gh CLI merge-flag verification` section to `30-PRE-STATE.md` capturing:

    1. `gh --version` (current gh CLI version + git version).
    2. `gh repo view atabisz/Vortex --json mergeCommitAllowed,squashMergeAllowed,rebaseMergeAllowed` — full JSON output. Record which merge types the fork repo settings permit.
    3. `gh pr merge --help 2>&1 | head -50` — capture the help text snapshot for the current gh version. Specifically note whether `--merge=fast-forward` is a flag value supported by this version OR whether `--merge` is a value-less flag that creates a merge commit (per RESEARCH.md Pitfall 2 ambiguity).
    4. **Decision record:** Based on the help-text scan, write a single paragraph stating the planned 30-03 invocation: either `gh pr merge 4 --repo atabisz/Vortex --merge=fast-forward` (CONTEXT D-30-01 primary) OR fallback `git push fork v8.0/config-bucket:master` (manual local FF) followed by `gh pr close 4 --comment "merged via FF push to master at <new-master-SHA>"` if the gh flag is rejected.
    5. **Branch protection check**: `gh api repos/atabisz/Vortex/branches/master/protection 2>&1 | head -40` — record whether master has required status checks. If status checks are required, the FF push will need to wait for those checks to pass first; if no protection, FF push goes through immediately.

    Casual voice. Append to existing file with `git add -f`.

  </action>
  <verify>
    <automated>grep -c "^## gh CLI merge-flag verification" .planning/phases/30-land-tag/30-PRE-STATE.md | grep -q "^1$" && grep -c "mergeCommitAllowed" .planning/phases/30-land-tag/30-PRE-STATE.md | awk '$1 >= 1 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `30-PRE-STATE.md` has exactly one `## gh CLI merge-flag verification` section.
    - Section contains `gh --version` output (gh version line present).
    - Section contains JSON output with `mergeCommitAllowed` key.
    - Decision paragraph names ONE of: `--merge=fast-forward`, `--merge`, or fallback `git push fork v8.0/config-bucket:master`.
    - `mergeCommitAllowed` value recorded as `true` or `false`.
  </acceptance_criteria>
  <done>30-PRE-STATE.md gh-flag section captures gh version, repo merge-allowed flags, help-text snapshot, planned 30-03 invocation, and master branch-protection state.</done>
</task>

<task type="auto">
  <name>Task 0-3: Push local master to fork/master (resolves Open Question §1)</name>
  <files>.planning/phases/30-land-tag/30-PRE-STATE.md</files>
  <read_first>
    .planning/phases/30-land-tag/30-RESEARCH.md (Pitfall 3, Open Questions §1)
    .planning/phases/30-land-tag/30-PRE-STATE.md (Local SHAs subsection — pull the lease-pin SHA from there)
  </read_first>
  <action>
    Push local master (currently `db8035192` per CONTEXT — verify live with `git rev-parse master` first) to `fork/master`. Lease pin against `d4c0d0da5` (the merge-base, current fork/master HEAD per RESEARCH.md live state) — verify live first via `git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1`.

    Sequence (run from local machine, current branch can stay v8.0/config-bucket):

    1. `LIVE_FORK_MASTER=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1)` — capture the verified-live fork master SHA.
    2. `LOCAL_MASTER=$(git rev-parse master)` — capture local master SHA.
    3. Sanity gate: `test "$LIVE_FORK_MASTER" = "d4c0d0da5..."` (full SHA) — if it doesn't match the merge-base, STOP and surface (someone else pushed to fork/master since CONTEXT was written).
    4. Push: `git push --force-with-lease=master:$LIVE_FORK_MASTER git@github.com:atabisz/Vortex.git master:master`. Note: `--force-with-lease` not `--force` even though this is technically a fast-forward — use the safety pin idiom uniformly.
    5. Verify post-push: `POST=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1); test "$POST" = "$LOCAL_MASTER"`.
    6. Append `## fork/master push (Open Question §1 resolved)` section to `30-PRE-STATE.md` capturing: live fork master SHA before push, local master SHA, push command run, post-push verified SHA. Append a sentence noting which CI runs were triggered on fork/master (`gh run list --repo atabisz/Vortex --workflow=main.yml --branch=master --limit 3`) — that's the +20-commit independent CI signal.

    No commit yet — 30-PRE-STATE.md updates batch into the Phase 30 setup commit at the end of this plan.

  </action>
  <verify>
    <automated>POST_PUSH=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1); LOCAL=$(git rev-parse master); test "$POST_PUSH" = "$LOCAL"</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-remote git@github.com:atabisz/Vortex.git refs/heads/master | cut -f1` equals `git rev-parse master` (fork/master matches local master post-push).
    - `30-PRE-STATE.md` has a `## fork/master push (Open Question §1 resolved)` section.
    - `gh run list --repo atabisz/Vortex --workflow=main.yml --branch=master --limit 1 --json status -q '.[0].status'` returns one of `queued`/`in_progress`/`completed` (push triggered CI).
    - Post-push fork master SHA captured in 30-PRE-STATE.md matches local `git rev-parse master`.
  </acceptance_criteria>
  <done>fork/master matches local master HEAD; CI run on master/main.yml triggered; section in 30-PRE-STATE.md records the operation.</done>
</task>

<task type="auto">
  <name>Task 0-4: Commit + push 30-PRE-STATE.md</name>
  <files>.planning/phases/30-land-tag/30-PRE-STATE.md</files>
  <read_first>
    .planning/phases/29-build-verification/29-10-PLAN.md (commit + push pattern for .planning/ files)
    .planning/phases/30-land-tag/30-PRE-STATE.md (the file we're committing)
  </read_first>
  <action>
    Verify the running fork/sync/upstream-v2.0.0 SHA hasn't drifted while the previous tasks ran: `LIVE_SYNC=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/sync/upstream-v2.0.0 | cut -f1)`. Compare against the SHA captured in Task 0-1's "Fork remote SHAs" block. If it drifted, append a `## drift note` paragraph to 30-PRE-STATE.md flagging the new SHA — do NOT block the commit on drift.

    Commit + push:

    1. `git add -f .planning/phases/30-land-tag/30-PRE-STATE.md`.
    2. `git commit -m 'docs(30-00): phase 30 setup — pre-rebase state captured'` (casual voice; one-line subject only, no body).
    3. `git log -1 --show-signature` — confirm SSH-signed (`Good "git" signature` line present).
    4. Push to fork: `LIVE=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/sync/upstream-v2.0.0 | cut -f1); git push --force-with-lease=sync/upstream-v2.0.0:$LIVE git@github.com:atabisz/Vortex.git v8.0/config-bucket:sync/upstream-v2.0.0`.
    5. Verify post-push: `POST=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/sync/upstream-v2.0.0 | cut -f1); test "$POST" = "$(git rev-parse v8.0/config-bucket)"`.

    This commit is a pure addition on top of the Phase 29 close HEAD `f746a0767` — no force-rewrite needed for the local commit graph, but the lease-pin idiom is still mandatory because of Pitfall 1.

  </action>
  <verify>
    <automated>git log --oneline v8.0/config-bucket -1 | grep -q "docs(30-00): phase 30 setup" && git log -1 --show-signature 2>&1 | grep -q 'Good "git" signature' && POST=$(git ls-remote git@github.com:atabisz/Vortex.git refs/heads/sync/upstream-v2.0.0 | cut -f1); test "$POST" = "$(git rev-parse v8.0/config-bucket)"</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline v8.0/config-bucket -1` first line starts with `docs(30-00): phase 30 setup`.
    - `git log -1 --show-signature 2>&1` output contains `Good "git" signature`.
    - `git ls-remote git@github.com:atabisz/Vortex.git refs/heads/sync/upstream-v2.0.0 | cut -f1` equals `git rev-parse v8.0/config-bucket`.
    - Phase 30 first commit landed; remote sync/upstream-v2.0.0 advanced cleanly (no force-rewrite).
  </acceptance_criteria>
  <done>30-PRE-STATE.md committed + pushed; SSH-signed; remote sync/upstream-v2.0.0 advanced one commit past Phase 29 close HEAD.</done>
</task>

</tasks>

<verification>
- `.planning/phases/30-land-tag/30-PRE-STATE.md` exists with all required sections (## Pre-rebase state, ## gh CLI merge-flag verification, ## fork/master push)
- fork/master matches local master HEAD (Open Question §1 resolved by push)
- 30-00 commit landed + SSH-signed + pushed to fork/sync/upstream-v2.0.0
- gh-flag fallback path documented before 30-03 needs it
</verification>

<success_criteria>
Phase 30 has its recovery baseline: SHAs captured, fork/master pushed, gh CLI behavior verified, commit landed.
</success_criteria>

<output>
After completion, leave `.planning/phases/30-land-tag/30-PRE-STATE.md` in place — 30-01 reads it for the rebase lease pin SHA. No SUMMARY.md needed for setup plans (Phase 29 precedent — only the done-gate plan produces a summary).
</output>
