# Requirements Archive: v5.0 fomod-installer Linux Fixes

**Archived:** 2026-04-09
**Status:** SHIPPED

For current requirements, see `.planning/REQUIREMENTS.md`.

---

# Requirements

## Phase 15: fomod-installer Linux Fixes + Vortex Cleanup

| ID | Description | Status |
|----|-------------|--------|
| FOMD-15-01 | CSharpScript runtime OS guard — wrap CSharpScriptType registration with RuntimeInformation.IsOSPlatform(OSPlatform.Windows) in ModFormatManager.cs | Complete (pre-existing) |
| FOMD-15-02 | XmlScriptInstaller source path normalization — apply TextUtil.NormalizePath to matchedFiles[0] for consistent lowercase forward-slash paths | Complete — Plan 01 |
| FOMD-15-03 | CI Linux IPC build pipeline — build-packages.yml matrix includes linux-x64 dotnet publish for ModInstallerIPC | Complete (pre-existing) |
| FOMD-15-04 | C# script unsupported-instruction warning — Linux-specific notification in reportUnsupported when source === CSharpScript | Complete — Plan 02 |
| FOMD-15-05 | Remove redundant replaceAll workaround — source path replaceAll("\\\\", "/") no longer needed after Parser10 upstream normalization | Complete — Plan 02 |
| FOMD-15-06 | Regenerate vortex-api declarations — api.d.ts missing resolvePathCase export | Complete — Plan 03 |
| FOMD-15-07 | Add FOMD requirements to REQUIREMENTS.md — planning artifact for requirement tracking | Complete — Plan 03 |
