---
phase: 19-staging-directory-wiring
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/renderer/src/extensions/mod_management/stagingDirectory.test.ts
  - src/renderer/src/extensions/mod_management/texts.test.ts
  - src/renderer/src/extensions/gamemode_management/util/discovery.test.ts
autonomous: true
requirements: [ONBRD-02b, ONBRD-02c, ONBRD-02d]

must_haves:
  truths:
    - "stagingDirectory.test.ts exists with failing (red) test stubs for Linux partition-exists check"
    - "texts.test.ts exists with failing (red) test stubs for Linux path example strings"
    - "discovery.test.ts exists with failing (red) test stubs for device-aware suggestStagingPath"
    - "All three test files run without import/mock errors (they fail on assertions, not module resolution)"
  artifacts:
    - path: "src/renderer/src/extensions/mod_management/stagingDirectory.test.ts"
      provides: "Red test stubs for ONBRD-02b partition-exists check"
      contains: "findAccessibleAncestor"
    - path: "src/renderer/src/extensions/mod_management/texts.test.ts"
      provides: "Red test stubs for ONBRD-02c Linux path examples"
      contains: "~/.local/share/Vortex/downloads"
    - path: "src/renderer/src/extensions/gamemode_management/util/discovery.test.ts"
      provides: "Red test stubs for ONBRD-02d device-aware suggestStagingPath"
      contains: "suggestStagingPath"
  key_links:
    - from: "src/renderer/src/extensions/mod_management/stagingDirectory.test.ts"
      to: "src/renderer/src/extensions/mod_management/stagingDirectory.ts"
      via: "vi.mock of ../../util/fs for statAsync"
      pattern: "vi\\.mock.*util/fs"
    - from: "src/renderer/src/extensions/gamemode_management/util/discovery.test.ts"
      to: "src/renderer/src/extensions/gamemode_management/util/discovery.ts"
      via: "vi.mock of ../../../util/fs for statAsync"
      pattern: "vi\\.mock.*util/fs"
---

<objective>
Create failing (red) test stubs for the three new test files required by Phase 19: stagingDirectory.test.ts (ONBRD-02b), texts.test.ts (ONBRD-02c), and discovery.test.ts (ONBRD-02d). These stubs define the behavioral contracts that implementation tasks in Plans 01 and 02 will make green.

Purpose: Satisfy the Nyquist sampling requirement -- every implementation task must have a pre-existing automated test that transitions from red to green. Without these Wave 0 stubs, Plans 01 and 02 would have three consecutive tasks with no behavioral automated verification.

Output: Three new test files with failing assertions. Each file must run without import/mock errors (failures are assertion-only, not module resolution errors).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-staging-directory-wiring/19-CONTEXT.md
@.planning/phases/19-staging-directory-wiring/19-RESEARCH.md
@.planning/phases/19-staging-directory-wiring/19-PATTERNS.md

<interfaces>
<!-- Mock patterns extracted from existing test files in the codebase. -->

From src/renderer/src/extensions/firststeps_dashlet/todos.test.ts (lines 1-66):
```typescript
import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";

vi.mock("winapi-bindings", () => ({
  GetDiskFreeSpaceEx: vi.fn(() => ({ freeToCaller: 100 * 1024 * 1024 * 1024 })),
  GetVolumePathName: vi.fn(() => "C:"),
  default: {
    GetDiskFreeSpaceEx: vi.fn(() => ({ freeToCaller: 100 * 1024 * 1024 * 1024 })),
    GetVolumePathName: vi.fn(() => "C:"),
  },
}));

// setPlatform helper:
function setPlatform(platform: string) {
  Object.defineProperty(process, "platform", {
    value: platform,
    writable: true,
    configurable: true,
  });
}
```

From src/renderer/src/util/fs.ts:
```typescript
export function statAsync(path: string): PromiseBB<fs.Stats>;
```

From src/renderer/src/extensions/mod_management/texts.ts:
```typescript
import type { TFunction } from "../../util/i18n";
function getText(id: string, t: TFunction) { ... }
export default getText;
```

From src/renderer/src/extensions/gamemode_management/util/discovery.ts:
```typescript
export async function suggestStagingPath(
  api: IExtensionApi,
  gameId: string,
): Promise<string>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stagingDirectory.test.ts and texts.test.ts red stubs</name>
  <files>
    src/renderer/src/extensions/mod_management/stagingDirectory.test.ts,
    src/renderer/src/extensions/mod_management/texts.test.ts
  </files>
  <read_first>
    src/renderer/src/extensions/firststeps_dashlet/todos.test.ts,
    src/renderer/src/extensions/mod_management/stagingDirectory.ts,
    src/renderer/src/extensions/mod_management/texts.ts,
    .planning/phases/19-staging-directory-wiring/19-PATTERNS.md
  </read_first>
  <action>
**stagingDirectory.test.ts:**

Create a new test file at `src/renderer/src/extensions/mod_management/stagingDirectory.test.ts`. Use the mock patterns from PATTERNS.md and the `setPlatform` helper from `todos.test.ts`.

Required mocks (hoisted with `vi.mock`):
- `winapi-bindings`: mock `GetVolumePathName` returning `"C:\\"` (both named and default export)
- `../../util/fs`: mock `statAsync` as `vi.fn()` (will be configured per test)
- `@vortex/shared`: mock `isErrorWithSystemCode` returning `true` and `unknownToError` as passthrough
- Any other transitive imports that `stagingDirectory.ts` uses -- read the file to identify them. Mock minimally: `shortid` (generate returns `"test-id"`), and any other imports that cause resolution errors.

The test describe block should be named `"stagingDirectory Linux partition check"`.

Write these test cases as **failing stubs** -- the assertions describe the behavior AFTER Plan 01 Task 2 implements the Linux guard. Since the current code has NO platform guard (it always runs the winapi path), these tests will fail:

1. `"on Linux, findAccessibleAncestor returns true when statAsync resolves for the path itself"` -- Set platform to `"linux"`. Mock `statAsync` to resolve. Assert that the partition check sets `partitionExists = true`. This test fails because the current code calls `winapi.GetVolumePathName` regardless of platform.

2. `"on Linux, findAccessibleAncestor returns false when statAsync rejects all the way to root"` -- Set platform to `"linux"`. Mock `statAsync` to reject with ENOENT for every path up to `/`. Assert `partitionExists = false`. Fails because current code does not have the `findAccessibleAncestor` function.

3. `"on win32, partition check still uses winapi.GetVolumePathName"` -- Set platform to `"win32"`. Assert that `winapi.GetVolumePathName` is called. This test should PASS with current code (serves as a regression guard).

IMPORTANT: Since `ensureStagingDirectoryImpl` is a complex function with many side effects, the test stubs may need to test the partition check logic indirectly. If directly importing and calling `ensureStagingDirectoryImpl` requires too many mocks, create the test stubs to test the `findAccessibleAncestor` helper function that Plan 01 will introduce. In that case, the stubs should import from `stagingDirectory.ts` and assert the existence of the exported helper. These stubs will fail because the helper does not exist yet. Use `it.todo()` for cases that cannot be wired until the implementation exists, but ensure at least one test case has a real assertion that will fail.

**texts.test.ts:**

Create a new test file at `src/renderer/src/extensions/mod_management/texts.test.ts`.

Required mocks: minimal -- `texts.ts` only imports `TFunction` (a type). No heavy mocks needed.

```typescript
import { describe, expect, it } from "vitest";
import getText from "./texts";

const mockT = ((s: string) => s) as any;
```

Write these test cases as **failing stubs** -- they describe the behavior AFTER Plan 01 Task 2 adds the platform ternary:

1. `"downloadspath returns Linux path example on linux"` -- Set platform to `"linux"`. Call `getText("downloadspath", mockT)`. Assert result contains `"~/.local/share/Vortex/downloads"`. Fails because current code always returns Windows text.

2. `"downloadspath returns Windows path example on win32"` -- Set platform to `"win32"`. Call `getText("downloadspath", mockT)`. Assert result contains `"C:\\Users\\Mike"`. Should PASS (regression guard).

3. `"modspath returns Linux path example on linux"` -- Set platform to `"linux"`. Call `getText("modspath", mockT)`. Assert result contains `"~/.local/share/Vortex/mods"`. Fails because current code always returns Windows text.

4. `"modspath returns Windows path example on win32"` -- Set platform to `"win32"`. Call `getText("modspath", mockT)`. Assert result contains `"d:\\vortex_mods"`. Should PASS (regression guard).

Use the same `setPlatform` / `afterEach` restore pattern from `todos.test.ts`.
  </action>
  <verify>
    <automated>cd /home/alex/src/Vortex && pnpm vitest run src/renderer/src/extensions/mod_management/texts.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - stagingDirectory.test.ts exists with at least 2 test stubs that fail on assertions (not import errors)
    - texts.test.ts exists with 4 test cases: 2 fail (Linux assertions) and 2 pass (Windows regression guards)
    - Both files use the setPlatform/afterEach restore pattern from todos.test.ts
  </done>
</task>

<task type="auto">
  <name>Task 2: Create discovery.test.ts red stubs for device-aware suggestStagingPath</name>
  <files>
    src/renderer/src/extensions/gamemode_management/util/discovery.test.ts
  </files>
  <read_first>
    src/renderer/src/extensions/gamemode_management/util/discovery.ts,
    src/renderer/src/extensions/gamemode_management/GameModeManager.test.ts,
    .planning/phases/19-staging-directory-wiring/19-PATTERNS.md
  </read_first>
  <action>
Create a new test file at `src/renderer/src/extensions/gamemode_management/util/discovery.test.ts`.

Read `discovery.ts` to identify all transitive imports that need mocking. At minimum, mock:
- `winapi-bindings`: `GetVolumePathName` returning `"D:\\"` (both named and default)
- `../../../util/fs`: `statAsync` as `vi.fn()`
- `../../../util/application`: `getVortexPath` returning `"/home/user/.local/share/Vortex"` for `"userData"`
- Any other imports that prevent module loading (check `GameModeManager.test.ts` for reference patterns)

The mock for `statAsync` must support returning objects with a `.dev` property (number) to simulate device identity checks. Configure mock implementations per test case:
- Same device: both `statAsync(modPaths[""])` walk and `statAsync(userData)` return `{ dev: 1 }`
- Different device: mod path returns `{ dev: 2 }`, userData returns `{ dev: 1 }`

The `suggestStagingPath` function takes `(api: IExtensionApi, gameId: string)`. Create a minimal mock `api` object:
```typescript
const mockApi: any = {
  getState: () => ({
    settings: {
      mods: {
        installPath: { "skyrim": "{USERDATA}/skyrim/mods" },
        paths: { "skyrim": { "" : "/mnt/games/skyrim" } },
        suggestInstallPathDirectory: "vortex_mods",
      },
      gameMode: { discovered: { "skyrim": { path: "/mnt/games/skyrim" } } },
    },
  }),
};
```

Adjust the mock state structure after reading `suggestStagingPath` to ensure `modPathsForGame(state, gameId)` returns a valid `modPaths` object where `modPaths[""]` is the game mod path.

Write these test cases as **failing stubs**:

1. `"returns {USERDATA}/{game}/mods when mod path and userData are on same device (linux)"` -- Set platform to `"linux"`. Mock `statAsync` so that both mod path walk and userData stat return `{ dev: 1 }`. Call `suggestStagingPath(mockApi, "skyrim")`. Assert result is `path.join("{USERDATA}", "{game}", "mods")`. This test FAILS because the current code has `|| process.platform !== "win32"` which short-circuits to the USERDATA branch always on Linux -- BUT the assertion itself may pass since it returns USERDATA. If this case passes, that is acceptable (regression guard for same-device).

2. `"returns mountpoint-based path when mod path and userData are on different devices (linux)"` -- Set platform to `"linux"`. Mock `statAsync` so mod path returns `{ dev: 2 }`, userData returns `{ dev: 1 }`, and parent directories return `{ dev: 2 }` until hitting `/mnt` where `dev` changes to `1`. Call `suggestStagingPath(mockApi, "skyrim")`. Assert result contains `"/mnt/games"` or the expected mountpoint-based path with `vortex_mods`. This test FAILS because the current `|| process.platform !== "win32"` short-circuit always returns USERDATA on Linux, never reaching the mountpoint walk logic.

3. `"returns {USERDATA}/{game}/mods when same device on win32"` -- Set platform to `"win32"`. Mock both stats with `{ dev: 1 }`. Assert result is USERDATA-based path. Should PASS (regression guard).

4. `"returns volume-based path when different device on win32"` -- Set platform to `"win32"`. Mock different `dev` values. Assert `winapi.GetVolumePathName` is called. Should PASS (regression guard).

IMPORTANT: The mock for `statAsync` needs to handle the `idModPath` recursive walk (which calls `statAsync` on `modPaths[""]` and walks up on ENOENT). Configure mock to resolve immediately for the mod path (no ENOENT walk needed in test).
  </action>
  <verify>
    <automated>cd /home/alex/src/Vortex && pnpm vitest run src/renderer/src/extensions/gamemode_management/util/discovery.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - discovery.test.ts exists with at least 4 test cases
    - Test case 2 (different-device Linux) fails because current code short-circuits to USERDATA
    - Win32 regression tests pass with current code
    - File runs without import/mock resolution errors (failures are assertion-only)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Test-only plan -- no production code changes, no trust boundaries crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | — | Test stubs only; no production code changes |
</threat_model>

<verification>
1. `pnpm vitest run src/renderer/src/extensions/mod_management/stagingDirectory.test.ts` -- runs without import errors; Linux tests fail on assertions
2. `pnpm vitest run src/renderer/src/extensions/mod_management/texts.test.ts` -- Linux tests fail, Windows regression tests pass
3. `pnpm vitest run src/renderer/src/extensions/gamemode_management/util/discovery.test.ts` -- different-device Linux test fails, same-device and win32 tests may pass
</verification>

<success_criteria>
- Three new test files exist and are loadable by vitest
- Each file has red (failing) tests for the Linux behaviors that Plans 01 and 02 will implement
- Each file has green (passing) tests for existing Windows behavior (regression guards)
- No import/mock resolution errors -- all failures are assertion failures only
</success_criteria>

<output>
After completion, create `.planning/phases/19-staging-directory-wiring/19-00-SUMMARY.md`
</output>
