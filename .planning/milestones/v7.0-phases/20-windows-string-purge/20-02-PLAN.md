---
phase: 20-windows-string-purge
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - ONBRD-03c
  - ONBRD-03d

must_haves:
  truths:
    - "No 'Run as Administrator' string is visible to a Linux user in any reachable error path during first run"
    - "decodeSystemError returns undefined on Linux, so no Windows-specific native error text is ever rendered"
    - "message.ts EPERM handler produces Linux-safe copy without any admin/administrator language"
    - "symlink_activator_elevate is unreachable on Linux due to platform guard at line 49"
  artifacts:
    - path: ".planning/phases/20-windows-string-purge/20-02-SUMMARY.md"
      provides: "Audit trail documenting ONBRD-03c and ONBRD-03d verification results"
      contains: "ONBRD-03c"
  key_links:
    - from: "src/renderer/src/util/nativeErrors.ts"
      to: "process.platform"
      via: "early return at line 13"
      pattern: 'process\.platform !== "win32"'
    - from: "src/renderer/src/extensions/symlink_activator_elevate/index.ts"
      to: "process.platform"
      via: "early return at line 49"
      pattern: 'process\.platform !== "win32"'
---

<objective>
Verify that no "Run as Administrator" or Windows-specific error text is reachable by a Linux
user through the EPERM/EACCES error path or any other first-run error path.

Purpose: ONBRD-03c requires that the EPERM/EACCES call path produces actionable Linux messages.
ONBRD-03d requires that zero reachable "Run as Administrator" strings exist on Linux. Both are
satisfied by existing guards — this plan verifies that claim and documents the audit trail.

Output: Verification audit documented in SUMMARY. No code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-windows-string-purge/20-CONTEXT.md
@.planning/phases/20-windows-string-purge/20-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace decodeSystemError callers and verify EPERM path is Linux-safe (ONBRD-03c)</name>
  <files></files>
  <read_first>
    - src/renderer/src/util/nativeErrors.ts (decodeSystemError function, line 13 guard)
    - src/renderer/src/util/message.ts (lines 369-435, decodeSystemError call at 374, EPERM handler at 421)
    - src/renderer/src/util/fs.ts (decodeSystemError call at line 526)
    - .planning/phases/20-windows-string-purge/20-CONTEXT.md (decisions D-06, D-07)
  </read_first>
  <action>
Perform a read-only audit of the `decodeSystemError` call chain to verify ONBRD-03c.

**Step 1: Identify all callers of `decodeSystemError`**

Run: `grep -rn "decodeSystemError" src/` to find all import and call sites.

Expected callers (from RESEARCH.md):
- `src/renderer/src/util/message.ts:374` — `prettifyNodeErrorMessageInner`
- `src/renderer/src/util/fs.ts:526` — inside the Access denied dialog flow

**Step 2: Verify nativeErrors.ts line 13 guard**

Read `src/renderer/src/util/nativeErrors.ts` lines 7-14. Confirm:
- Line 12: `if (code === undefined || process.platform !== "win32") {`
- Line 13: `return undefined;`
- This means on Linux, `decodeSystemError` ALWAYS returns `undefined` regardless of error code.

**Step 3: Trace what happens when decodeSystemError returns undefined**

For `message.ts:374`:
- `const decoded = decodeSystemError(...)` returns `undefined` on Linux
- Line 375: `if (decoded !== undefined)` is `false` — falls through
- Execution continues to the `err.code` checks below (EPERM at line 421, ENOENT, etc.)
- The EPERM handler at line 421-435 produces:
  `'Vortex needs to access "{{filePath}}" but it\'s write protected.'` +
  `"When you configure directories and access rights..."` +
  `"This is usually not a bug in Vortex."`
- This text contains NO "Run as Administrator", NO "UAC", NO "Windows" language.
- VERIFIED: Linux-safe.

For `fs.ts:526`:
- `const decoded = decodeSystemError(err, filePath)` returns `undefined` on Linux
- Line 527: `if (decoded !== undefined)` is `false` — the title/message are NOT overwritten
- Line 532: `if (decoded?.rethrowAs === undefined)` is `true` (decoded is undefined)
- Buttons are set to `["Cancel", "Retry"]` — no "Run as Administrator" language.
- VERIFIED: Linux-safe.

**Step 4: Check EACCES path**

Search message.ts for `EACCES` handling. If EACCES is handled, verify the message text.
If EACCES falls through to a default handler, verify the default does not contain Windows language.
The EPERM handler (line 421) already covers the "write protected" case. EACCES may or may not
have a separate handler — verify by reading the code.

**Step 5: Document findings**

Record in the SUMMARY file:
- All `decodeSystemError` callers identified
- nativeErrors.ts line 13 guard confirmed: returns `undefined` on non-win32
- Each caller's behavior when receiving `undefined` traced and verified Linux-safe
- EPERM handler in message.ts confirmed clean (no admin/Windows language)
- EACCES path traced and confirmed clean
- Conclusion: ONBRD-03c is satisfied by existing guards; no code changes needed (per D-07)
  </action>
  <verify>
    <automated>cd /home/alex/src/Vortex && grep -c 'decodeSystemError' src/renderer/src/util/nativeErrors.ts src/renderer/src/util/message.ts src/renderer/src/util/fs.ts</automated>
  </verify>
  <acceptance_criteria>
    - nativeErrors.ts line 12-13 contains `process.platform !== "win32"` followed by `return undefined`
    - message.ts line 421 contains `err.code === "EPERM"` with message text that does NOT contain "Run as Administrator" or "UAC" or "Windows"
    - fs.ts line 526 contains `decodeSystemError(err, filePath)` with `if (decoded !== undefined)` guard
    - No code changes made to nativeErrors.ts, message.ts, or fs.ts (except the raiseUACDialog change from Plan 01 which is in a different function)
  </acceptance_criteria>
  <done>All decodeSystemError callers traced. nativeErrors.ts returns undefined on Linux. EPERM handler in message.ts is Linux-safe. No "Run as Administrator" fallthrough exists in any EPERM/EACCES path on Linux. ONBRD-03c verified.</done>
</task>

<task type="auto">
  <name>Task 2: Grep audit for zero reachable "Run as Administrator" strings on Linux (ONBRD-03d)</name>
  <files></files>
  <read_first>
    - src/renderer/src/extensions/symlink_activator_elevate/index.ts (lines 48-52 platform guard, line 121 string)
    - .planning/phases/20-windows-string-purge/20-CONTEXT.md (decisions D-08)
    - .planning/phases/20-windows-string-purge/20-RESEARCH.md (ONBRD-03d audit results section)
  </read_first>
  <action>
Perform a comprehensive grep audit to verify ONBRD-03d: no "Run as Administrator" string is
visible to a Linux user in any reachable error path during first run.

**Step 1: Grep for "Run as Administrator" across all source files**

Run: `grep -rn "Run as Administrator" src/`

Expected result: exactly ONE match:
- `src/renderer/src/extensions/symlink_activator_elevate/index.ts:121` — `"Symlink Deployment (Run as Administrator)"`

**Step 2: Verify the single match is unreachable on Linux**

Read `src/renderer/src/extensions/symlink_activator_elevate/index.ts` lines 48-52:
```typescript
if (process.platform !== "win32") {
  // on non-windows platforms we don't need to do any of this.
  return;
}
```

This `monitorConsent` function returns early on non-Windows. The class constructor at line 118
passes `"Symlink Deployment (Run as Administrator)"` to the parent class — but the entire
extension's deployment method is gated by the same platform check. No Linux user can reach
any code path that renders this string.

**Step 3: Grep for "administrator" (case-insensitive) for belt-and-suspenders**

Run: `grep -rni "administrator" src/renderer/src/` to check for any other occurrences.

For each match found, determine:
1. Is it in a code path reachable on Linux?
2. Does it render user-visible text (dialog, notification, error message)?

The `confirmElevate` match ("Create as Administrator") will have been platform-guarded by
Plan 01. Verify this by checking that the grep output for Settings.tsx also shows the Linux
ternary.

**Step 4: Grep for "UAC" across source files**

Run: `grep -rni "UAC" src/renderer/src/`

The `raiseUACDialog` function name contains "UAC" but the user-visible message will have been
platform-guarded by Plan 01. Verify the grep output shows the ternary. The title "Access denied
(2)" is platform-neutral.

**Step 5: Document findings in SUMMARY**

Record:
- Total "Run as Administrator" matches: 1 (symlink_activator_elevate:121)
- Reachability: UNREACHABLE on Linux (platform guard at line 49)
- Total "administrator" matches in renderer: list each, note reachability
- Total "UAC" matches in renderer: list each, note that user-visible UAC text is guarded
- Conclusion: ONBRD-03d is satisfied. Zero reachable "Run as Administrator" strings exist on Linux.
  </action>
  <verify>
    <automated>cd /home/alex/src/Vortex && grep -rn "Run as Administrator" src/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "Run as Administrator" src/` returns exactly 1 match (symlink_activator_elevate/index.ts:121)
    - symlink_activator_elevate/index.ts line 49 contains `process.platform !== "win32"` with `return;`
    - The SUMMARY documents that the single "Run as Administrator" occurrence is unreachable on Linux
    - No code changes made to symlink_activator_elevate/index.ts (per D-08)
  </acceptance_criteria>
  <done>Grep audit complete. One "Run as Administrator" string found in symlink_activator_elevate — confirmed unreachable on Linux due to process.platform guard. Zero reachable Windows-specific error strings exist on Linux. ONBRD-03d verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan is read-only verification; no code is modified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Information Disclosure | nativeErrors.ts | accept | decodeSystemError returns undefined on Linux — no Windows-specific error details are exposed. This is the existing behavior being verified, not introduced. |
</threat_model>

<verification>
1. `grep -rn "Run as Administrator" src/` — exactly 1 match, in symlink_activator_elevate
2. `grep -n "process.platform !== .win32." src/renderer/src/extensions/symlink_activator_elevate/index.ts` — guard at line 49
3. `grep -n "process.platform !== .win32." src/renderer/src/util/nativeErrors.ts` — guard at line 12
4. `grep -n 'err.code === "EPERM"' src/renderer/src/util/message.ts` — EPERM handler at line 421
</verification>

<success_criteria>
- decodeSystemError callers traced: all return undefined on Linux, no Windows text rendered
- EPERM handler in message.ts confirmed Linux-safe (no admin/Windows language)
- Exactly 1 "Run as Administrator" string exists in source, confirmed unreachable on Linux
- No code changes made — ONBRD-03c and ONBRD-03d satisfied by existing guards
- Audit findings documented in SUMMARY
</success_criteria>

<output>
After completion, create `.planning/phases/20-windows-string-purge/20-02-SUMMARY.md`
</output>
