{
  "name": "game-witcher",
  "version": "0.1.0",
  "description": "Support for The Witcher: Enhanced Edition",
  "scripts": {
    "_assets": "copyfiles -u 1 -f ./assets/* dist",
    "_build": "copyfiles -u 1 -f ./src/index.js dist && pnpm run _assets && pnpm extractInfo",
    "build": "pnpm run _build && node ../../copy-extension.mjs"
  },
  "author": "Evanito",
  "license": "GPL-3.0",
  "type": "commonjs",
  "private": true,
  "config": {
    "game": "The Witcher"
  },
  "devDependencies": {
    "vortex-api": "workspace:*"
  }
}
